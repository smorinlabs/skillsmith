# P17-G3B-04 evidence

## Mapping and prerequisites

- **Primary entities:** exactly three: accepted finding `EWP-CF-035`, phase task
  `EWP-P3B-T05`, and phase selector `EWP-P3B-TS05`. Mapping advances only the owning group and
  its first lifecycle gate; all three entities remain pristine `planned`.
- **Formal dependency:** the immutable catalog dependency P17-G3B-02 is signed off at
  `b8630f3607b0945a01fd644755417650a519c653`. Its accepted implementation/test pin is
  `52133f3f824f8cf45d28d1ede65cb0522ef1d739`, and its terminal repository receipt passes 2,013
  tests with 28 intentional skips, zero failures, and 72,844 assertions.
- **Integration baseline:** signed G3B-03 is the required implementation baseline even though it is
  not a new immutable catalog dependency. Its canonical writer/recovery implementation is pinned at
  `7b7d285eee051e24c7e5d7624c33e4cd5a2e9ac6`, its lifecycle sign-off is
  `ea31bd6c4c1aef92dcbc80426fcb601741f959ca`, and its accepted terminal repository receipt passes
  2,147 tests with 28 intentional skips, zero failures, and 94,504 assertions. G3B-04 wraps and
  extracts that authority; it does not replace or reopen it.
- **Validation partition:** exactly four impacted validations. `EWP-P3B-TS05` is the sole
  required-now selector. `EWP-WF06`, `EWP-WF11`, and `EWP-WF13` remain downstream under
  P17-G4B-01, P17-G5-03, and P17-G4A-04 respectively; none advances or changes primary owner here.
- **Accepted target:** ADR 0006 will record the closed direction
  `domain repositories -> immutable observed snapshot -> pure planner -> immutable plan ->
  execution coordinator -> staged domain writes`. Repositories own mechanics but not policy and
  never call one another. The existing execution coordinator remains the sole cross-domain
  orchestration owner; the artifact coordinator and ledger writer remain separate physical
  durability authorities.
- **Current gaps:** no production `ObservedStateSnapshot` or snapshot identity exists; current
  flip planning still accepts read ports and performs observation; acquisition and placement
  runners mix observation, planning, compatibility projection, execution, persistence, and report
  assembly; `SwapCtx` still carries mutable ledger state plus a persistence closure; expected
  revisions privately advance inside the ledger gateway; and no module-size or dependency-cycle
  ratchet enforces the target boundary.
- **Dependency corrections required by Ready:** `artifacts/ledger-writer.ts` currently imports
  `place/history.ts`, which imports artifact ledger/journal/registry authorities. Planning
  compatibility also imports acquire/place action types while those domains depend on planning.
  Ready must freeze a neutral ownership migration and executable cycle/import gates before product
  edits begin.
- **Consolidation boundary:** the existing artifact repository/codec registry, ledger writer,
  artifact coordinator, execution coordinator, immutable ledger transforms, and injected writer
  ports remain the implementation foundation. G3B-04 may add thin behavior-specific domain
  facades and one bounded hostile-data ownership primitive. It may not add generic CRUD,
  `Repository<T>`, a dependency-injection container, unit of work, event store, fourth coordinator,
  second codec registry, or invocation-wide atomicity claim.
- **Successor exclusions:** G3B-04 does not ship saved-plan apply, manifest/lock desired-state
  mutation, public undo or GC, adapter extraction, observation propagation, new CLI commands/options,
  wire-version changes, network behavior, credential handling, or public repository/snapshot APIs.
  Those remain with their catalog owners.
- **Safety and test policy:** this is authorized, credential-free local architecture work for the
  open-source Skillsmith CLI. Mapping changes governance only, so no runtime or full-suite replay is
  required. Test-first begins with the exact P3B-TS05 selector plus standard smoke; recovery smoke
  joins only when durable/recovery behavior is touched, and the full repository suite remains a
  major-milestone/terminal gate.
- **Independent review:** the completed pre-G3B-04 architecture and simplicity audits reported no
  G3B-03 blocker and 97% overall confidence. Read-only mapping and ADR audits are recorded before
  Ready; they have no implementation or completion authority.
- **Result:** exact scope, dependencies, integration baseline, validation ownership, architectural
  direction, current gaps, consolidation limits, successor exclusions, and verification policy are
  mapped. No Ready, test-first, implementation, entity, validation, or sign-off gate is implied.
