# P17-G3B-04 evidence

## Mapping and prerequisites

- **Primary entities:** exactly three: accepted finding `EWP-CF-035`, phase task
  `EWP-P3B-T05`, and phase selector `EWP-P3B-TS05`. Mapping advances only the owning group and
  its first lifecycle gate; all three entities remain pristine `planned`.
- **Formal dependency:** the immutable catalog dependency P17-G3B-02 is signed off at
  `b8630f3607b0945a01fd644755417650a519c653`. Its accepted implementation/test pin is
  `52133f3f824f8cf45d28d1ede65cb0522ef1d739`, and its terminal repository receipt passes 2,013
  tests with 28 intentional skips, zero failures, and 72,844 assertions.
- **Integration baseline:** signed G3B-03 is the required implementation baseline even though it is
  not a new immutable catalog dependency. Its canonical writer/recovery implementation is pinned at
  `7b7d285eee051e24c7e5d7624c33e4cd5a2e9ac6`, its lifecycle sign-off is
  `ea31bd6c4c1aef92dcbc80426fcb601741f959ca`, and its accepted terminal repository receipt passes
  2,147 tests with 28 intentional skips, zero failures, and 94,504 assertions. G3B-04 wraps and
  extracts that authority; it does not replace or reopen it.
- **Validation partition:** exactly four impacted validations. `EWP-P3B-TS05` is the sole
  required-now selector. `EWP-WF06`, `EWP-WF11`, and `EWP-WF13` remain downstream under
  P17-G4B-01, P17-G5-03, and P17-G4A-04 respectively; none advances or changes primary owner here.
- **Accepted target:** ADR 0006 will record the closed direction
  `domain repositories -> immutable observed snapshot -> pure planner -> immutable plan ->
  execution coordinator -> staged domain writes`. Repositories own mechanics but not policy and
  never call one another. The existing execution coordinator remains the sole cross-domain
  orchestration owner; the artifact coordinator and ledger writer remain separate physical
  durability authorities.
- **Current gaps:** no production `ObservedStateSnapshot` or snapshot identity exists; current
  flip planning still accepts read ports and performs observation; acquisition and placement
  runners mix observation, planning, compatibility projection, execution, persistence, and report
  assembly; `SwapCtx` still carries mutable ledger state plus a persistence closure; expected
  revisions privately advance inside the ledger gateway; and no module-size or dependency-cycle
  ratchet enforces the target boundary.
- **Dependency corrections required by Ready:** `artifacts/ledger-writer.ts` currently imports
  `place/history.ts`, which imports artifact ledger/journal/registry authorities. Planning
  compatibility also imports acquire/place action types while those domains depend on planning.
  Ready must freeze a neutral ownership migration and executable cycle/import gates before product
  edits begin.
- **Consolidation boundary:** the existing artifact repository/codec registry, ledger writer,
  artifact coordinator, execution coordinator, immutable ledger transforms, and injected writer
  ports remain the implementation foundation. G3B-04 may add thin behavior-specific domain
  facades and one bounded hostile-data ownership primitive. It may not add generic CRUD,
  `Repository<T>`, a dependency-injection container, unit of work, event store, fourth coordinator,
  second codec registry, or invocation-wide atomicity claim.
- **Successor exclusions:** G3B-04 does not ship saved-plan apply, manifest/lock desired-state
  mutation, public undo or GC, adapter extraction, observation propagation, new CLI commands/options,
  wire-version changes, network behavior, credential handling, or public repository/snapshot APIs.
  Those remain with their catalog owners.
- **Safety and test policy:** this is authorized, credential-free local architecture work for the
  open-source Skillsmith CLI. Mapping changes governance only, so no runtime or full-suite replay is
  required. Test-first begins with the exact P3B-TS05 selector plus standard smoke; recovery smoke
  joins only when durable/recovery behavior is touched, and the full repository suite remains a
  major-milestone/terminal gate.
- **Independent review:** the completed pre-G3B-04 architecture and simplicity audits reported no
  G3B-03 blocker and 97% overall confidence. Read-only mapping and ADR audits are recorded before
  Ready; they have no implementation or completion authority.
- **Result:** exact scope, dependencies, integration baseline, validation ownership, architectural
  direction, current gaps, consolidation limits, successor exclusions, and verification policy are
  mapped. No Ready, test-first, implementation, entity, validation, or sign-off gate is implied.

## Ready plan

- **Accepted plan:** [P17-G3B-04.md](../plans/P17-G3B-04.md), SHA-256
  `0daa799f0c13d7e006930d9e63c351cc3cb7809193f2fed4cec2f4553579327a`.
- **Accepted decision:** [ADR 0006](../../../docs/adr/0006-immutable-observed-state-and-repositories.md)
  freezes the private direction from behavior-specific repositories through one immutable observed
  snapshot and pure planners to the existing execution coordinator and physical durability
  adapters. Revisioned absence, deterministic two-pass snapshot consistency, explicit preconditions,
  logical staging, per-resource cursor advancement, immutable ledger reducers, compatibility
  migration, and anti-overengineering exclusions are normative before red tests are written.
- **Exact scope:** three primary entities, one required-now selector, three downstream workflows,
  and exactly 75 product/test/documentation paths. The group and `EWP-P3B-T05` manifests are
  identical and unique: 5 architecture/documentation paths, 40 production architecture paths, and
  30 focused test paths. Governance plan/evidence/catalog/checklist files remain outside the
  product manifest.
- **Dependency integrity:** the formal catalog dependency remains only signed P17-G3B-02. Signed
  G3B-03 implementation `7b7d285eee051e24c7e5d7624c33e4cd5a2e9ac6` remains the accepted
  durability baseline. G3B-04 adapts its canonical writer/recovery/history authority and may not
  replace, merge, or reopen it.
- **Consolidation decisions:** the existing execution coordinator remains the sole cross-domain
  orchestration owner. Artifact coordination and ledger writing remain separate physical crash
  state machines. Thin domain repositories reuse the artifact reader/codec registry, artifact
  coordinator, ledger writer, live-placement mechanics, store mechanics, and focused ports. The
  plan explicitly rejects generic CRUD, a unit of work, DI container, event store, fourth
  coordinator, public snapshot/repository API, and invocation-wide atomicity.
- **Dependency corrections:** Ready freezes relocation of ledger-history policy beside artifact
  durability and neutral ownership of legacy action vocabulary. Executable tests will reject the
  current artifacts-to-place and planning-to-runner reversals, repository-to-repository imports,
  planner effects, status write capabilities, execution-driven replanning, and owned-source cycles.
- **Snapshot and revision contract:** every present or absent resource has an exact versioned
  revision. Absence includes target and parent identity, so creation is compare-and-set protected.
  The reader owns and freezes all facts, rereads the ordered revision vector once, refuses mixed
  state, and derives `snapshotId` from the revision vector without clock/random input. Semantic
  operation IDs remain stable; explicit preconditions own staleness.
- **Extraction and size policy:** acquisition and placement split vertically into planning,
  execution, recovery, and thin run shells. Ready pins no-growth baselines of 3,955 lines for
  `acquire/run.ts` and 2,917 lines for `place/run.ts`; responsibility/import checks, not a global
  arbitrary line cap, are authoritative. Cohesive durability state machines are not split for
  aesthetics.
- **Test-first boundary:** the sole catalog command is
  `bun test tests/ergonomics/phase/EWP-P3B-TS05.test.ts --test-name-pattern EWP-P3B-TS05`.
  Its 14 executable families cover dependency direction, hostile ownership, revisions, snapshots,
  pure planning, repository isolation, stale-state refusal, revision cursor, fault injection,
  ledger authority, read-only status, compatibility, and module/ownership gates. Missing planned
  files alone cannot constitute the red receipt; characterization-green clauses and named current
  architecture failures are required.
- **Independent acceptance:** read-only reviewers `/root/g3b04_arch_boundary_audit` and
  `/root/g3b04_simplicity_audit` found no G3B-03 blocker and recommended this bounded extraction at
  97%/96% confidence. `/root/g3b04_mapping_audit` accepted the immutable scope, validation
  partition, 75-path corrected manifest, and exclusions at 96%. `/root/g3b04_adr_design` accepted
  the revision/snapshot/repository/coordinator contract and pre-test-first decisions at 97%.
- **Catalog transition:** P17-G3B-04 advances only from `mapped` to `ready`; only its Ready gate
  passes. `EWP-P3B-T05` advances from `planned` to `ready` with the same 75 paths and exact command.
  `EWP-CF-035` and `EWP-P3B-TS05` remain `planned`; WF06/WF11/WF13 remain downstream and pristine.
- **Verification policy:** Ready receives deterministic catalog/checklist, focused governance,
  package, and diff validation only. It changes no runtime/test behavior, so no smoke, recovery,
  full P2-TS04, or complete repository replay is required. Focused selector plus smoke starts at
  test-first; recovery smoke joins on the first coordinator/durability edit; full suites remain
  milestone and terminal gates.
- **Result:** architecture, exact ownership, test-first obligations, implementation partitions,
  compatibility boundaries, and verification policy are fully frozen. G3B-04 is Ready for an
  executable red selector; no implementation or later lifecycle gate is implied.

## Test-first evidence

- **Committed selector:** implementation-free red contract
  `d9d878fac8016eaf20f6a4cba95d938e6add67f1` adds only
  `tests/ergonomics/phase/EWP-P3B-TS05.test.ts`. The exact catalog command executes the complete
  selector without a loader, syntax, type, skip, todo, live-system, network, subprocess, sleep, or
  timing-race failure.
- **Characterization green:** five tests pass. They preserve accepted ADR 0006 and the existing
  codec-registry, artifact-coordinator, ledger-writer, and execution-coordinator authorities;
  immutable semantic planning and under-lock precondition ordering; read-only status inputs;
  detached copy-on-write ledger reducers; the 3,955/2,917 runner no-growth ceilings; and the
  anti-framework/private-surface baseline.
- **Executable behavior reds:** guarded deep imports bind concrete private interfaces and killing
  behavior for hostile ordinary-data ownership, present and revisioned-absence construction,
  deterministic two-pass snapshot reads and mixed-state refusal, deterministic acquisition and
  placement planning, stale/newly-appeared logical-stage refusal, immutable revision-cursor
  advancement, partial-pair truth, stage/commit/rollback/cleanup faults including rollback
  indeterminacy, and pure status derivation from the approved snapshot. Missing future modules do
  not prevent the rest of the selector from executing.
- **Structural reds:** TypeScript-AST checks expose the exact artifacts-to-place and
  planning-to-runner dependency reversals, the current ports/place cycle, mutable `SwapCtx`
  authority, execution-to-planner reversal, missing repository/snapshot/revision boundaries,
  planner effects, missing delegated responsibility modules, and status observation duplication.
  Planner bans are limited to effect, port, repository, execution, application, and runner
  dependencies; pure path/model helpers are not prohibited. Interface, export, framework, and
  public-leakage gates inspect syntax rather than comments or string literals.
- **Independent acceptance:** the first audit correctly blocked marker-only and structurally
  fragile checks. Two remediation audits then required partial-pair and rollback-failure truth.
  After both were added, `/root/g3b04_mapping_audit` returned PASS with no blockers and 99%
  confidence, reproducing 26 selected tests, five passes, 21 intentional failures, and 56
  assertions.
- **Bounded gates:** standard P2-TS04 smoke passes five tests with 3,735 assertions. TypeScript,
  Biome, no-skip/no-live checks, and `git diff --check` pass. No recovery smoke is required because
  no production durability/recovery code changed, and the reserved complete repository suite is
  not run at test-first.
- **Catalog transition:** P17-G3B-04 advances only from `ready` to `active` and passes only its
  test-first gate. `EWP-P3B-T05` advances to `red`; `EWP-P3B-TS05` advances to `failing` with its
  exact target and committed receipt. `EWP-CF-035` remains `planned`; WF06/WF11/WF13 remain
  downstream, pristine, and owned by their future groups.
- **Result:** the Ready architecture is bound by an executable, independently accepted red
  selector. No production implementation, targeted-green, impacted-green, refactor, review,
  traceability, downstream, or sign-off credit is implied.

### Red execution receipt EWP-P3B-TS05

- Validation ID: `EWP-P3B-TS05`
- Command: `bun test tests/ergonomics/phase/EWP-P3B-TS05.test.ts --test-name-pattern EWP-P3B-TS05`
- Exit status: `1`
- Result: 5 passed and 21 failed intentionally with 56 assertions; ownership, revisions, snapshots,
  planners, repositories, cursor/lifecycle faults, dependency reversals, mutable ledger authority,
  status observation, extraction, and cycle closure reached missing G3B-04 behavior.
- Revision: `d9d878fac8016eaf20f6a4cba95d938e6add67f1`

## Implementation ownership amendment — explicit swap-state compatibility replays

- **Discovered gap:** the accepted architecture requires `SwapCtx` to lose mutable ledger state,
  its zero-argument persistence closure, clock, and transaction-ID allocator. Three existing
  hermetic compatibility suites construct that private context directly and therefore must migrate
  with the production boundary to keep crash, resume, rollback, sweep, and acquisition parity
  executable.
- **Added paths:** `packages/core/tests/place/crash-sweep-acquire.test.ts`,
  `packages/core/tests/place/crash-sweep.test.ts`, and
  `packages/core/tests/place/swap-acquire.test.ts` join the G3B-04 product/test manifest. The current
  group and `EWP-P3B-T05` manifests remain byte-set equivalent at exactly **78 unique paths: 5
  architecture/documentation, 40 production architecture, and 33 focused tests**.
- **Bounded authority:** the tests may only adopt the explicit immutable swap-state/effects
  interface and preserve existing signed durability behavior. The amendment adds no product
  command, public API, persisted format, generic unit of work, new coordinator, downstream
  validation, network behavior, or credential handling.
- **Ownership:** `root-codex-goal` remains integration owner. The runner/execution partition may
  edit the three added tests together with the already-owned `place/swap.ts`, `place/types.ts`,
  acquisition/placement runners, and execute/recovery modules. No implementation or lifecycle gate
  is credited by this ownership correction.
