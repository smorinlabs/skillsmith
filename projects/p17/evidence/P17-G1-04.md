# P17-G1-04 evidence

## Mapping and prerequisites

- **Primary IDs:** `EWP-CF-034`, `EWP-P1-T10`, and `EWP-P1-TS08`.
- **Dependency:** P17-G1-03 is signed off at
  `cd7ec024dc9beef0872dfd06eaa72024fffe2966`; accepted implementation revision is
  `3fb4c1a86d424be9010f04394a5546a4eedd93ac`.
- **Required now:** exactly `EWP-P1-TS08` and signed `EWP-WF15`.
  `EWP-WF11` and `EWP-WF16` remain downstream-only.
- Independent read-only mappers `/root/g1_04_scope_map`, `/root/g1_04_port_arch_map`, and
  `/root/g1_04_validation_map` mapped the 51-file ScanEnv production surface, ambient effect
  seams, exact port/config/compatibility design, red validation families, safe parallel ownership,
  and later-group exclusions.
- Mapping found the existing doctor network-reach ambient `fetch` seam. The canonical architecture
  and EWP-CF-034 resolution were consistency-amended to name `HttpPort`, closing that existing
  effect without expanding command or capability-registry behavior.
- During implementation, read-side migration exposed one previously unmapped ambient seam:
  `scope-writable` used `fs.access(W_OK | X_OK)` and `process.getuid`. The plan, ADR 0005, public
  contracts, and TS08 exact type proof were consistency-amended with a one-operation
  `PathAccessPort`; this avoids an ambient allowlist, mutation probe, or cross-platform shell drift.
- The immutable catalog display title for `EWP-P1-T10` is historically truncated after `Process/`
  and the validator rejects title mutation. The canonical multiline task and ready plan retain the
  full Git/HTTP/Clock/Id, typed-config, composition, and compatibility scope.
- Fresh signed WF15 baseline passed 8/8 with 83 assertions. Catalog and generated checklist were
  valid before lifecycle advancement.
- **Mapping result:** exact scope, prerequisite, validation impact, compatibility promise, and
  prohibited downstream work mapped by root Codex goal on 2026-07-12.

## Ready plan

- **Plan:** `projects/p17/plans/P17-G1-04.md`.
- Root owns interfaces, one-real-adapter composition, compatibility, public/application/CLI
  integration, lint, catalog, evidence, and commits. Read-side, effect-side, and test-first work use
  disjoint ownership after the red and interface boundaries are committed.
- The plan freezes cohesive named ports, focused structural subsets, typed configuration and secret
  isolation, structured port errors, high-level Git/HTTP ownership, deterministic clocks/IDs, and a
  thin deprecated 1.x ScanEnv facade backed by the same real adapters.
- Exact target command is
  `bun test tests/ergonomics/phase/EWP-P1-TS08.test.ts --test-name-pattern EWP-P1-TS08`;
  impacted signed regression is
  `bun test tests/ergonomics/workflows/EWP-WF15.test.ts --test-name-pattern EWP-WF15`.
- **Result:** ready for test-first implementation; no later lifecycle gate is implied.

## Test-first evidence

- `EWP-P1-TS08` exited 1 with the public 1.x `defaultScanEnv()` compatibility canary passing and
  nine failures naming the absent planned architecture: exact capability-scoped contracts and
  ownership, production-bound compile-negative read capability, focused compatibility parity,
  one-real-adapter delegation, typed configuration isolation, intent-level Git/HTTP ownership,
  safe structured adapter failures, deterministic clock/ID seams, path-access ownership, and
  executable lint gates.
- The compile fixture binds directly to the production `listSkills` parameter, asserts exact public
  contract keys and the `PortError` code/field shape, and proves a read-only bundle cannot write,
  remove, lock, or execute. The lint family runs ESLint against nine negative stdin canaries rather
  than accepting configuration-token comments.
- Signed `EWP-WF15` remains green at 8/8 with 83 assertions. Root typecheck, targeted Biome, and
  diff checks passed at the red boundary.
- Independent reviewer `/root/g1_04_red_final_review` rejected two earlier drafts, verified every
  correction, and accepted the final boundary with 0.995 confidence. No false red, ungreenable
  contract, compatibility behavior change, downstream scope theft, or remaining loophole was found.
- **Result:** TS08 is genuinely red at the group boundary; all failures name planned G1-04
  production behavior rather than test or fixture defects.

### Red execution receipt EWP-P1-TS08

- Validation ID: `EWP-P1-TS08`
- Command: `bun test tests/ergonomics/phase/EWP-P1-TS08.test.ts --test-name-pattern EWP-P1-TS08`
- Exit status: `1`
- Result: 1 passed, 9 failed, 21 assertions; 10 executable contract families.
- Revision: `a7882641462bc4dddfbc8ce1017d9125483ffa24`

## Implementation and validation

- Interface revision `8f5f42159ce3f1ad563a95336504f36a98125589` froze ADR 0005, the public
  capability/config/error shapes, the one-real-adapter and compatibility boundaries, and exact
  public exports before parallel migration.
- Integrated implementation revision `ecd2ea7e5d24b57fe43db0a1f9e62d91fc86183e` migrated current
  read, verification, acquisition, placement, configuration, doctor, application, and CLI context
  paths from aggregate `ScanEnv`/raw environment authority to focused structural ports and frozen
  typed configuration. `defaultScanEnv()` remains a deprecated projection of the same real
  filesystem/process/lock path.
- Corrected immutable revision `c9d3fa87eaf902692da09335a8f0ff4717965d10` restored exact
  transaction-ID and acquisition-error behavior, option-delimited every Git intent field, restored
  2s/10s/120s Git bounds, added byte-exact blob execution through the sole process adapter, made
  process/Git/HTTP cancellation and timeout classifications truthful, projected doctor checks to
  read/path-access/HTTP authority at runtime, and preserved callback errors through the lock port.
- The final static boundary permits raw runtime discovery only at CLI/default composition, real
  Node/Bun filesystem/process/lock effects only in `ports/default.ts`, network fetch only in
  `ports/http.ts`, and Git argv only in Git infrastructure. Compile-negative fixtures prove a
  read-only service cannot write, remove, lock, or execute.
- Final `bun run check` passed Biome, ESLint capability/dependency boundaries, typecheck,
  actionlint, the P17 package checker, and 1,322 tests with 28 intentional live skips, 0 failures,
  and 6,528 assertions across 160 files. The adversarial correction suite separately passed 58/58
  with 151 assertions.

### Execution receipt EWP-P1-TS08

- Validation ID: `EWP-P1-TS08`
- Command: `bun test tests/ergonomics/phase/EWP-P1-TS08.test.ts --test-name-pattern EWP-P1-TS08`
- Exit status: `0`
- Result: 10 passed, 0 failed, 146 assertions; public shapes, least authority, compatibility parity,
  typed configuration, Git/HTTP ownership, safe errors, deterministic clocks/IDs, and executable
  lint gates passed.
- Revision: `c9d3fa87eaf902692da09335a8f0ff4717965d10`

### Execution receipt EWP-WF15

- Validation ID: `EWP-WF15`
- Command: `bun test tests/ergonomics/workflows/EWP-WF15.test.ts --test-name-pattern EWP-WF15`
- Exit status: `0`
- Result: 8 passed, 0 failed, 83 assertions; shared non-mutating, human/JSON error, usage, and
  cancellation behavior remained unchanged.
- Revision: `c9d3fa87eaf902692da09335a8f0ff4717965d10`

## Adversarial review

- Fresh independent reviewer `/root/g1_04_impl_adversarial` rejected initial implementation
  `ecd2ea7e5d24b57fe43db0a1f9e62d91fc86183e` despite a green full suite. It found eight gaps:
  Git option injection, transaction-ID wire drift, lost Git timeouts, false cancellation/timeout
  codes, non-byte-safe blob reads, excess doctor process authority, acquisition wording drift, and
  lock callback reclassification.
- Correction `c9d3fa87eaf902692da09335a8f0ff4717965d10` closed every finding with direct
  production and adversarial regressions. The same read-only reviewer inspected the immutable diff,
  exercised malicious ref/path arguments and invalid UTF-8 blobs, checked transaction/output bytes,
  cancellation races, runtime doctor projection, and lock error identity, then replayed TS08, WF15,
  and the full repository gate.
- **Canonical final verdict: PASS, confidence 0.997, no residual findings.** Focused correction
  suite: 58 passed, 0 failed, 151 assertions. Full check: 1,322 passed, 28 conditional skips,
  0 failures, 6,528 assertions.
- **Gate result:** adversarial review accepted on 2026-07-12 America/Los_Angeles.

## Traceability and sign-off

- **Accepted revision:** `c9d3fa87eaf902692da09335a8f0ff4717965d10`.
- `EWP-CF-034`, `EWP-P1-T10`, and `EWP-P1-TS08` resolve to ADR 0005, focused public
  ports/configuration/error contracts, the one real adapter composition, compatibility projections,
  migrated current signatures, executable static ownership gates, immutable green receipts, and
  fresh independent acceptance.
- Signed `EWP-WF15` was replayed rather than reassigned. `EWP-WF11` and `EWP-WF16` remain
  downstream-only; G1-05 descriptor bundles, G1-06 wire codecs, G1-07 operation observation, later
  planning/repository work, and G6 generated/manual UX remain explicitly deferred.
- Standing user authorization covers this group without a human-review pause; test-first,
  implementation, correction, adversarial, immutable-revision, and traceability gates all passed.
- **Traceability result:** passed by root Codex goal on 2026-07-12 America/Los_Angeles.
- **Group sign-off:** all three primary entities, ten lifecycle gates, both required-now validation
  receipts, focused correction regressions, full repository validation, canonical reviewer
  acceptance, catalog closure, and generated checklist are complete. P17-G1-04 signed off by root
  Codex goal on 2026-07-12.

## Phase 1 whole-review ownership correction — 2026-07-13

`P17-RV-PHASE-1-F04` compared the accepted `990e533^..c9d3fa8` change union with the group catalog
and found 76 omitted production, fixture, preservation-test, and canonical-plan paths after
excluding lifecycle metadata. The plan and catalog now enumerate all 76 exactly. Cross-group
co-ownership is intentional for migrated call sites and preservation regressions; no new capability
or behavioral scope is claimed. The historical change-union comparison is empty after correction,
resolving the finding for independent phase replay.
