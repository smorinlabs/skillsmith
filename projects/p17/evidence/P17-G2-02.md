# P17-G2-02 evidence

## Mapping and prerequisites

- **Primary IDs:** `D-004`, `EWP-CF-027`, `EWP-P2-T03`, and `EWP-P2-TS03`.
- **Dependency:** P17-G2-01 is signed off at
  `944c41118cf0cc4710fb671a658d3fac443d7754`; its accepted implementation pin is
  `0751951bbb413dd80f5dc6e67eec8725d18c9735`. Phase 2 entry is passed.
- **Required now:** exactly `EWP-P2-TS01` and `EWP-P2-TS03`.
- **Downstream only:** `EWP-CMD-APPLY-TS07`, `EWP-CMD-DOCTOR-TS03`,
  `EWP-CMD-PLAN-TS04`, `EWP-CMD-PLAN-TS09`, `EWP-CMD-UPDATE-TS10`, `EWP-P2-TS04`,
  `EWP-P4B-TS03`, `EWP-WF03`, `EWP-WF07`, and `EWP-WF09` remain immutable planned coverage.
- **Boundary:** G2-01 owns normalized manifest semantics; G2-02 owns pure domain hashes, strict lock
  v1 bytes/relationship, and source-content v1; G2-03 owns coordination/writes/recovery; G2-05 owns
  codecs/repositories/migrations and legacy hash interpretation.
- **Compatibility:** the existing unversioned `place/store.ts::contentHashOf` remains independent
  and unchanged until a version-aware G2-05 migration. A fixed regression will pin its current
  `SKILL.md = z` digest.
- Independent mapper `/root/g2_02_mapper` reproduced a clean baseline of 8 P2-TS01 tests with 907
  assertions and 12 manifest/legacy-content-hash tests with 105 assertions. It confirmed the exact
  four-primary mapping, two required-now validations, ten downstream obligations, and no port
  extension requirement.
- Independent test architect `/root/g2_02_test_arch` confirmed the pure three-module boundary,
  exact seven-domain registry, required selectors, compatibility risk, and complete hash/lock/tree
  acceptance families.
- Independent adversary `/root/g2_02_adversary` initially returned NOT READY with 12 stable finding
  classes: byte grammar, lock optional/source/path/SHA/escaping/version ambiguity, closed runtime
  API, legacy store incompatibility, source-tree framing/Unicode/symlink/race/exclusion behavior,
  projection ownership, typed relationship states, version-first refusal, credential safety,
  overlap boundaries, and trace metadata.
- **Resolution:** `projects/p17/plans/P17-G2-02.md` freezes every finding at byte level without
  expanding required-now coverage into future codec/repository tests.
- **Mapping result:** exact scope, dependency, validation tiers, downstream trace, public boundary,
  compatibility seam, ownership, and prohibited later work are mapped.

## Ready plan

- **Plan:** `projects/p17/plans/P17-G2-02.md`.
- Hash v1 is exact: the seven frozen domains, `skillsmith:<domain>:v1` UTF-8 prefix, one NUL byte,
  untouched canonical input bytes, SHA-256, and lowercase 64-hex serialization. Unknown domain/
  schema and malformed digests refuse.
- Manifest semantics hash exactly the no-final-LF JSON string of G2-01's schema-ordered
  `projectManifestSemantics` result. Exact manifest bytes remain a separate local-only domain and
  never enter the lock.
- Lock v1 has exact normalized fields, total explicit-host GitHub/non-GitHub source spelling,
  null-ref omission, `.` root source path, SHA-1 grammar, fixed TOML key/entry order, one blank line
  per block, LF-only bytes, one final LF, one string encoder, strict unknown/missing/type checks,
  version-first future refusal, and parse-normalize-serialize byte equality.
- Relationship precedence is exact: reader error before correlation, then `missing-lock`,
  `incomplete`, `stale`, and `current`, with stable sorted secret-safe facts.
- Source-content v1 is exact schema-ordered JSON/base64 with NFC relative POSIX paths, UTF-8 byte
  sorting, preserved empty directories, portable executable semantics, safe non-followed symlinks,
  special-node/race refusal, and the frozen `.git`-only exclusion list.
- Planned ownership is limited to pure core artifact modules/public exports/docs, focused tests, and
  three golden fixture files. No CLI, writes, repositories, codecs, migrations, or legacy store
  behavior are claimed.
- Required selector commands are recorded exactly and all later lifecycle gates remain pending.
- Initial independent ready/feasibility review rejected the candidate on exact public signatures and
  error carriers, relationship unions/order, total source encoding, JSON string bytes, symlink
  segment rules, in-place mutation detection, TOML error precedence, and the existing `dir`
  metadata vocabulary. Stable findings `P17-RV-G2-02-F01..F08` bind those eight classes. Follow-up
  review also required synthetic-HTTPS validation for the total explicit-host codec and complete
  fabricated source-projection validation; those are the final corrections under F03 and F08.
- The corrected plan publishes exact sync/async signatures and `Result` carriers, closed error
  tables, relationship unions and total fact order, explicit-host source encoding validated through
  G2-01, exact JSON.stringify bytes, strict symlink segment reduction, paired byte/target reads,
  precise TOML precedence, `dir` metadata mapping, and a closed source-projection schema with
  `invalid-projection` coverage.
- Fresh independent reviewer `/root/g2_02_ready_review` returned **PASS** at 0.995 confidence.
  Fresh feasibility reviewer `/root/g2_02_contract_review` returned **PASS** at 0.998 confidence;
  both confirmed all prior findings closed and made no edits.
- Read-only baseline replay passed: P2-TS01 8 tests/907 assertions; manifest plus legacy content hash
  12 tests/105 assertions; catalog validator 51 tests/120 assertions; catalog/package/type/diff/plan/
  documentation/architecture checks all green. `bun run check` also completed green on the ready
  candidate.
- **Result:** independently reviewed and ready for test-first activation; no implementation gate is
  implied.

## Test-first evidence

- Commit `2833bc657461ab8298129c6d20feba3a931fb854` activates the smallest truthful G2-02
  acceptance reds without production authority. It extends P2-TS01 by one hash relationship family
  and adds P2-TS03's three hash/lock/source-content families plus hard-coded golden fixtures.
- `tests/ergonomics/fixtures/p2-ts03/hash-v1.golden.json` independently pins all seven framed byte
  sequences and digests over the same input. `lock-v1.golden.toml` pins 654 exact canonical bytes
  and lock digest. `source-content-v1.golden.json` pins exact no-LF canonical JSON and its digest.
  Every fixture shape/count/byte/digest guard executes before planned public authority is required.
- P2-TS01 preserves all eight signed G2-01 families green, then fails only because
  `hashManifestSemantics` and `hashManifestBytes` are absent. Its formatting/order/comment/CRLF
  equivalence and representative semantic mutations are validated before the missing-function
  assertion.
- P2-TS03 has three independent actionable failures: absent closed hash authority, absent strict
  lock/relationship authority, and absent source projection/hash authority. The artifact module
  itself loads successfully; no test fails on a missing file import, fixture defect, skip, todo,
  host path, or production exception.
- The source-content acceptance fake uses stable lstat-like metadata, reversed directory
  enumeration, paired file bytes, paired link targets, an inaccessible `.git`, an in-place byte
  race, a root-escaping link canary, and a special node. Those oracles become reachable once the
  named authority exists.
- Independent reviewer `/root/g2_02_red_review` returned **PASS** at 0.99 confidence with no stable
  red-honesty finding. It confirmed hard-coded independent fixtures, exact named failures, no
  skips/todos, type/format/catalog/diff hygiene, and no production change.
- Mandatory pre-sign-off expansion is recorded: full binary/digest/manifest vector coverage;
  strict lock/version/canonical/relationship matrices; Unicode/path/symlink/race/projection cases;
  root exports/types/freezing; secret-safe error code/field/message canaries; and the fixed legacy
  content hash plus static no-import boundary.
- Signed compatibility remains green outside the intentional extensions: P2-TS02 passed 6 tests/
  168 assertions; focused manifest plus legacy content hashing passed 12 tests/105 assertions;
  typecheck, Biome, catalog, package, and diff checks passed.
- Test file SHA-256 values are
  `f63ab92ef8fcd3adde002fa538c8c6728aa811c5336cb55d99d6b203d34eb0f1` for P2-TS01 and
  `1806a2f015740d6d8df63cc9866914ea43deddd94c9c5887199ba50354359979` for P2-TS03.
- **Result:** test-first gate is truthful; only absent G2-02 production authority prevents the two
  required-now selectors from passing.

### Red execution receipt EWP-P2-TS01

- Validation ID: `EWP-P2-TS01`
- Command: `bun test tests/ergonomics/phase/EWP-P2-TS01.test.ts --test-name-pattern EWP-P2-TS01`
- Exit status: `1`
- Result: 8 passed, 1 failed, 925 assertions; the original eight signed families pass and the new
  manifest hash family fails only on absent `hashManifestSemantics`/`hashManifestBytes`.
- Revision: `2833bc657461ab8298129c6d20feba3a931fb854`

### Red execution receipt EWP-P2-TS03

- Validation ID: `EWP-P2-TS03`
- Command: `bun test tests/ergonomics/phase/EWP-P2-TS03.test.ts --test-name-pattern EWP-P2-TS03`
- Exit status: `1`
- Result: 0 passed, 3 failed, 39 assertions; all hard-coded fixture preguards pass before the hash,
  lock/relationship, and source-content families fail on their exact missing public functions.
- Revision: `2833bc657461ab8298129c6d20feba3a931fb854`
