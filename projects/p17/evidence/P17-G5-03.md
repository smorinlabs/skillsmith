# P17-G5-03 evidence

## Mapping and prerequisites

Mapping began from clean governance revision
`2712bea29f81e94531493a084acf28e7653cd208` on 2026-07-22.

- Phase 5 is active.
- Direct dependency P17-G4B-03 is signed off at all ten lifecycle gates.
- G5-03 is planned with no owned files, implementers, reviewer, targets, or passed gates.
- The primary set is exactly 14 entities: `COMMAND:undo`, undo TS01, TS02, TS04..TS09,
  `EWP-P5-T05`, `EWP-P5-TS03`, `EWP-WF05`, `EWP-WF11`, and `P1-11`.
- Required-now is exactly 13 validations: signed DEV-TS05 and PROMOTE-TS05 plus 11 new selectors.
- Downstream coverage is exactly UNDO-TS03, UPDATE-TS07, and WF09 under G5-05.
- All four planned selector owners and the proposed shared fleet are absent before test-first work.

The accepted command authority is the Phase-5 plan, P1-11, D-010, and CF-006/024/026/028/031/034/
035/036. The target CLI registry already freezes the exact undo command, variadic semantic target,
and nine options at G5-03. Current Commander state correctly has no undo command.

## Existing authority audit

The candidate extends these signed owners:

- ledger v2 `transactions` and bounded committed `history` own logical identity and retention;
- `abortPendingLogicalTransaction` owns the validated pending forward-to-rollback projection;
- placement recovery and artifact coordination own physical rollback/resume/cleanup;
- status journal correlation owns pending precedence, membership, retained-resource probes,
  eligibility, and remediation;
- shared selection owns target/scope/tool/all ambiguity and no-widening behavior;
- shared scheduler/application interaction own ordering, failure policy, approval, and exits.

There is no public undo application, domain module, codec, renderer, CommandSpec, or selector owner.
There is also no committed-history reversal constructor. Those are the bounded missing pieces.

## Candidate arithmetic and boundary

- 11 new selectors: undo TS01, TS02, TS04..TS09, P5-TS03, WF05, and WF11.
- 2 signed impacted selectors: DEV-TS05 and PROMOTE-TS05.
- 13 required-now total.
- 3 downstream-only selectors retained at G5-05.
- 78 proposed product/test/governance paths plus plan/evidence catalog records.

The detailed candidate is `projects/p17/plans/P17-G5-03.md`. It freezes scope-aware selection,
pending-before-history precedence, migration ordering, crash-safe pending abort, history-preserving
committed reversal, compatibility alias routing, strict undo@1 output, the five-file red boundary,
the terminal/individual-receipt policy, service-registry activation, and exact undo confirmation
projection through the shared CLI interaction adapter.

## Ready review

Fresh independent review identified `P17-RV-P17-G5-03-F01`: the initial candidate incorrectly
combined a rollback disposition with an already-inverted committed intent. The signed reducer owns
that inversion, so the draft would have double-inverted terminal membership and restoration.

The plan is corrected to retain the original forward intent orientation, bind the approved current
image to original `intent.after`, record the new transaction's `actual.before` as that current image,
and record `actual.after` as restored original `intent.before`. No catalog lifecycle transition or
product/test edit is authorized until fresh independent re-review accepts the correction.

The same review identified `P17-RV-P17-G5-03-F02`: the initial candidate did not distinguish a
forward pending transaction from an already-pending rollback and did not freeze repeated behavior
after the reversal committed. The corrected plan converts only forward pending state, resumes an
existing rollback with its original identities and action class, and treats the newest valid
committed rollback as explained `already-reversed` without toggling or older-history fallback. F22
later distinguishes the cleanup-pending committed-shadow recovery window from a terminal
zero-mutation no-op. TS02, TS04, and TS09 own the retry/convergence proof.

The review also identified two shared-authority manifest gaps:

- `P17-RV-P17-G5-03-F03`: shared mutator/planning command types and validation omitted undo;
- `P17-RV-P17-G5-03-F04`: retained-history rollback planning and placement-plan execution were
  artificially typed to dev/promote, making a private undo implementation likely to duplicate them.

The candidate now owns the four shared planning/placement sources and their four parity/behavior
tests. It adds undo to shared immutable-plan authority, preserves explicit-selection enforcement,
and generalizes the signed rollback planner/executor seam while keeping compatibility output at the
edge.

`P17-RV-P17-G5-03-F05` found that private `already-reversed` classification would have contradicted
status eligibility: the current join can advertise a terminal rollback as reversable. The candidate
now owns `packages/core/src/status/join.ts` and freezes a wire-compatible correction: terminal
rollback disposition is `not-reversible` with no reverse remediation, pending rollback remains
eligible for its undo-resume argv, and `status@1` does not gain a new field. TS05 and TS09 prove the
distinction. The manifest is now 77 unique product/test/governance paths.

`P17-RV-P17-G5-03-F06` found an unsafe scope claim: standalone artifact-pair recovery records do not
carry the skill/tool/scope/parent identity required for target-bound undo selection. The corrected
plan aborts or resumes only selected ledger logical placement/move-scope journals. Overlapping
artifact recovery remains an exact-remediation same-operation blocker under its existing
coordinator; G5-03 neither guesses correlation nor changes that record schema.

`P17-RV-P17-G5-03-F07` found that the logical pending sequence could not apply to a preserved v1
pair journal because no matching logical transaction exists. The corrected plan uses the logical
begin-attempt/conversion sequence only for v2 logical forward pending state, resumes v2 rollback
pending state directly, and preserves the signed legacy pair-journal migration/rollback path without
fabricating a logical identity. TS01 and TS09 own legacy crash/retry coverage.

`P17-RV-P17-G5-03-F08` found that the existing swap terminal seam always projects rollback
`actual.after` from `actual.before`. That is correct for the converted pending-abort model but would
make fresh committed-reversal history falsely report the current image as restored. The candidate
now owns `packages/core/src/place/swap.ts` and freezes origin-aware terminal projection: converted
pending behavior is preserved; fresh reversal records approved current/original-after as
`actual.before` and physically restored parent-forward-before as `actual.after`, proven across crash
points by the already-owned swap tests. The manifest is now 78 unique paths.

## Accepted Ready result

Focused final re-review of plan
`212367814a8285f076c27cdb221b211bd51fcb747cc7038d05a8b296d987e868` and evidence
`6941f05d632283186891f99f7d5974d61291dbb102b4a90a7f8fdf3069bcf583` returned **GO** with
confidence 0.98 and no edits. The reviewer confirmed F08 closed without reopening F01-F07, exact
14-primary/13-required-now/3-downstream partitions, the 78-path 34/18/5/19/2 manifest, shared
planner/executor/status/swap authority, and diff hygiene.

G5-03 may advance to `ready` with mapped/Ready as the only passed lifecycle gates. EWP-P5-T05 may
advance to `ready`; command, recommendation, workflow, and validation entities remain planned. No
selector target, executable receipt, product behavior, or later gate is claimed. The next authorized
action is the implementation-free five-file red boundary for the 11 new selectors.

## Ready transition validation

The transition is recorded coherently: G5-03 is `ready`; mapped/Ready are the only two passed gates;
80 catalog-owned paths equal the exact 78-path manifest plus plan/evidence; 13 required-now commands
are frozen; and EWP-P5-T05 alone is ready among the exact 14 primary entities. All command,
recommendation, workflow, and validation entities remain planned.

```text
bun test scripts/p17-catalog.test.ts
56 pass, 0 fail, 265 assertions

bun test packages/cli/tests/contracts/dev.test.ts --test-name-pattern EWP-CMD-DEV-TS05
1 pass, 0 fail, 133 assertions

bun test packages/cli/tests/contracts/promote.test.ts --test-name-pattern EWP-CMD-PROMOTE-TS05
1 pass, 0 fail, 74 assertions

bun scripts/p17-catalog.ts --check
valid: 425 entities (418 required, 7 deferred; 244 validation obligations), 45 groups;
deterministic checklist

bun scripts/check-p17-package.ts --check
structurally valid; Phase 5 active; G5-03 ready at 2/10 gates; next test-first

git diff --check
PASS
```

## Test-first evidence

The implementation-free five-file boundary is committed at
`bba217718edac66c184defee468b1bc99b3fe6ea`:

- `packages/cli/tests/contracts/undo.test.ts` owns TS01, TS02, and TS04 through TS09;
- `tests/ergonomics/phase/EWP-P5-TS03.test.ts` owns P5-TS03;
- `tests/ergonomics/workflows/EWP-WF05.test.ts` and `EWP-WF11.test.ts` own the workflows;
- `tests/ergonomics/fixtures/p5-undo/fleet.ts` is the shared hermetic public-process fleet.

Each selector's first executable statement checks the public `skillsmith undo` CommandSpec and
`CURRENT_APPLICATION_SERVICES.undo`. All 11 were replayed independently and exited 1 with exactly
zero passes, one failure, and one assertion. The sole received state was
`{ commandSpec: false, applicationService: false }`; no fleet was constructed before failure.
There was no missing file, type error, timeout, skip, TODO, public network access, or placeholder
success.

Root inspection corrected three future-green assertions before the boundary commit: terminal
rollback status/repetition now uses a fresh committed promote rather than fabricating legacy logical
history; TS09 separates legacy retry from fresh logical rollback crash phases; and WF05 accepts the
signed `flipped` action rather than a nonexistent `demoted` action. These corrections do not change
the first-gate red.

The fleet imports no product implementation. Parser-facing `https://fixture.invalid/...` Git
identity rewrites through fixture-local configuration to a local repository. Independent viability
probes passed public dev, promote, install, and uninstall seeds; current status and doctor both
accepted the hand-built v1 pending pair journal.

Supporting receipts on the red revision:

```text
bun run typecheck
PASS

bun run lint:boundaries
PASS

bunx biome check <the five red-boundary paths>
PASS

bun run test:smoke
5 pass, 0 fail, 3,737 assertions

bun test packages/cli/tests/contracts/dev.test.ts --test-name-pattern EWP-CMD-DEV-TS05
1 pass, 0 fail, 133 assertions

bun test packages/cli/tests/contracts/promote.test.ts --test-name-pattern EWP-CMD-PROMOTE-TS05
1 pass, 0 fail, 74 assertions

git diff --check
PASS
```

### Red execution receipt EWP-CMD-UNDO-TS01

- Validation ID: `EWP-CMD-UNDO-TS01`
- Command: `bun test packages/cli/tests/contracts/undo.test.ts --test-name-pattern EWP-CMD-UNDO-TS01`
- Exit status: `1`
- Result: FAIL — exact absent public undo CommandSpec/application-service boundary; 0 pass, 1 fail,
  1 assertion.
- Revision: `bba217718edac66c184defee468b1bc99b3fe6ea`

### Red execution receipt EWP-CMD-UNDO-TS02

- Validation ID: `EWP-CMD-UNDO-TS02`
- Command: `bun test packages/cli/tests/contracts/undo.test.ts --test-name-pattern EWP-CMD-UNDO-TS02`
- Exit status: `1`
- Result: FAIL — exact absent public undo CommandSpec/application-service boundary; 0 pass, 1 fail,
  1 assertion.
- Revision: `bba217718edac66c184defee468b1bc99b3fe6ea`

### Red execution receipt EWP-CMD-UNDO-TS04

- Validation ID: `EWP-CMD-UNDO-TS04`
- Command: `bun test packages/cli/tests/contracts/undo.test.ts --test-name-pattern EWP-CMD-UNDO-TS04`
- Exit status: `1`
- Result: FAIL — exact absent public undo CommandSpec/application-service boundary; 0 pass, 1 fail,
  1 assertion.
- Revision: `bba217718edac66c184defee468b1bc99b3fe6ea`

### Red execution receipt EWP-CMD-UNDO-TS05

- Validation ID: `EWP-CMD-UNDO-TS05`
- Command: `bun test packages/cli/tests/contracts/undo.test.ts --test-name-pattern EWP-CMD-UNDO-TS05`
- Exit status: `1`
- Result: FAIL — exact absent public undo CommandSpec/application-service boundary; 0 pass, 1 fail,
  1 assertion.
- Revision: `bba217718edac66c184defee468b1bc99b3fe6ea`

### Red execution receipt EWP-CMD-UNDO-TS06

- Validation ID: `EWP-CMD-UNDO-TS06`
- Command: `bun test packages/cli/tests/contracts/undo.test.ts --test-name-pattern EWP-CMD-UNDO-TS06`
- Exit status: `1`
- Result: FAIL — exact absent public undo CommandSpec/application-service boundary; 0 pass, 1 fail,
  1 assertion.
- Revision: `bba217718edac66c184defee468b1bc99b3fe6ea`

### Red execution receipt EWP-CMD-UNDO-TS07

- Validation ID: `EWP-CMD-UNDO-TS07`
- Command: `bun test packages/cli/tests/contracts/undo.test.ts --test-name-pattern EWP-CMD-UNDO-TS07`
- Exit status: `1`
- Result: FAIL — exact absent public undo CommandSpec/application-service boundary; 0 pass, 1 fail,
  1 assertion.
- Revision: `bba217718edac66c184defee468b1bc99b3fe6ea`

### Red execution receipt EWP-CMD-UNDO-TS08

- Validation ID: `EWP-CMD-UNDO-TS08`
- Command: `bun test packages/cli/tests/contracts/undo.test.ts --test-name-pattern EWP-CMD-UNDO-TS08`
- Exit status: `1`
- Result: FAIL — exact absent public undo CommandSpec/application-service boundary; 0 pass, 1 fail,
  1 assertion.
- Revision: `bba217718edac66c184defee468b1bc99b3fe6ea`

### Red execution receipt EWP-CMD-UNDO-TS09

- Validation ID: `EWP-CMD-UNDO-TS09`
- Command: `bun test packages/cli/tests/contracts/undo.test.ts --test-name-pattern EWP-CMD-UNDO-TS09`
- Exit status: `1`
- Result: FAIL — exact absent public undo CommandSpec/application-service boundary; 0 pass, 1 fail,
  1 assertion.
- Revision: `bba217718edac66c184defee468b1bc99b3fe6ea`

### Red execution receipt EWP-P5-TS03

- Validation ID: `EWP-P5-TS03`
- Command: `bun test tests/ergonomics/phase/EWP-P5-TS03.test.ts --test-name-pattern EWP-P5-TS03`
- Exit status: `1`
- Result: FAIL — exact absent public undo CommandSpec/application-service boundary; 0 pass, 1 fail,
  1 assertion.
- Revision: `bba217718edac66c184defee468b1bc99b3fe6ea`

### Red execution receipt EWP-WF05

- Validation ID: `EWP-WF05`
- Command: `bun test tests/ergonomics/workflows/EWP-WF05.test.ts --test-name-pattern EWP-WF05`
- Exit status: `1`
- Result: FAIL — exact absent public undo CommandSpec/application-service boundary; 0 pass, 1 fail,
  1 assertion.
- Revision: `bba217718edac66c184defee468b1bc99b3fe6ea`

### Red execution receipt EWP-WF11

- Validation ID: `EWP-WF11`
- Command: `bun test tests/ergonomics/workflows/EWP-WF11.test.ts --test-name-pattern EWP-WF11`
- Exit status: `1`
- Result: FAIL — exact absent public undo CommandSpec/application-service boundary; 0 pass, 1 fail,
  1 assertion.
- Revision: `bba217718edac66c184defee468b1bc99b3fe6ea`

The catalog transition is coherent: G5-03 is `active`; mapped, Ready, and test-first are the only
three passed gates; the exact 11 new validations are `failing` with executable ID-bound targets and
receipts; EWP-P5-T05 remains `ready`; COMMAND:undo and P1-11 remain planned; and the three G5-05
downstream validations remain untouched.

```text
bun test scripts/p17-catalog.test.ts
56 pass, 0 fail, 265 assertions

bun scripts/p17-catalog.ts --check
valid: 425 entities (418 required, 7 deferred; 244 validation obligations), 45 groups;
deterministic checklist

bun scripts/check-p17-package.ts --check
structurally valid; Phase 5 active; G5-03 active at 3/10 gates; next minimal-implementation

git diff --check
PASS
```

## Implementation-discovered ownership amendment

The bounded foundation and domain passes independently reached the same crash-safety blocker before
accepting a committed-reversal executor. The exact call graph is:

`undo plan -> placement rollback operation -> normal inverse runSwap -> pair compatibility shadow
-> ledger codec cross-reference`.

The logical rollback must retain the original committed forward intent orientation. The normal
inverse swap must instead journal the physical operation that restores that intent's before-image.
The existing codec admits only physical `rollback` for every rollback disposition, so it rejects
that truthful pair. Driving a fresh committed reversal through `rollbackSwap` is also invalid
because it can restore only the backup of an interrupted transaction; it cannot restore retained
state from committed history. The provisional high-level reversal seam was removed before handoff,
and the domain intentionally returns a controlled failure for committed candidates until the
amendment is reviewed and implemented. No unreviewed product path was edited.

The proposed amendment adds only:

- `packages/core/src/artifacts/ledger-codec.ts` for the closed origin-aware compatibility-shadow
  predicate and parent-history cross-reference;
- `packages/core/tests/artifacts/codec.test.ts` for allowed inverse-operation mappings and hostile
  linkage/orientation rejection;
- `packages/core/src/artifacts/ledger-history.ts` and
  `packages/core/tests/artifacts/ledger-writer.test.ts` for dependency-closed parent/child retention,
  cleanup, and crash restart;
- `packages/core/src/status/read.ts` and `packages/core/src/status/types.ts` for a request-only
  lifecycle-history mode that observes bounded user/current-project ledger/live/retention facts
  without unrelated manifest/lock I/O. The already-owned status read test covers the mode.

The living plan now owns exactly 84 product/test/governance paths (38 production, 20 focused tests,
5 selectors/fleet, 19 signed compatibility owners, 2 governance owners), plus plan/evidence for 86
catalog-owned paths. It requires one atomic first write containing both the fresh logical rollback
and its physical inverse shadow; fresh rollback pending resumes that inverse shadow, while converted
pending abort retains the signed backup-restoring rollback path. Retained fresh rollback children
transitively retain their exact forward parent through fair selection and cleanup. The private
status mode performs no manifest/lock reads and does not change `status@1` or public status grammar.
The 14-primary, 13-required-now, 3-downstream, command, schema, and exclusion sets do not change.

The reviewer also confirmed two objective red-assertion corrections within the already-owned undo
selector. TS01 must expect the shared inverse kind `remove` after `migrate-ledger` for its
before-absent legacy dev, not a nonexistent rollback/restore operation kind. TS08 cannot place a
deprecation inside strict flip@4 while also requiring JSON stderr to be empty; it must keep JSON
byte/shape compatible and verify the structured deprecation through a separate human alias run.

Independent amendment review is required before any newly owned path is edited. The review must
confirm that the six-path expansion is necessary and sufficient, the four image-derived inverse
operation mappings are exact, invalid/missing parent linkage remains unreadable state, bounded
history/cleanup stays dependency-closed, lifecycle-history observation does no unrelated artifact
I/O, and the proposed atomic/resume sequence closes rather than shifts the crash window.

The first amendment review returned **NO-GO** at 0.98 confidence on the earlier two-path hashes and
identified four stable findings:

- `P17-RV-P17-G5-03-F09` found that the 256-row bounded history selector could retain a fresh
  rollback child while evicting its required parent. The amended six-path plan adds dependency-
  closed protection, fair selection, visible frontier, victim, shared-backup cleanup, and restart
  semantics in the history authority and its existing writer test owner.
- `P17-RV-P17-G5-03-F10` found that undo's proposed status request was invalid and that substituting
  a selected manifest/lock pair would couple undo to unrelated artifacts and extra I/O. The amended
  plan adds a narrowly validated lifecycle-history request mode in status read/types, reuses the
  canonical ledger/live/retention/join pipeline, and preserves the public status wire shape.
- `P17-RV-P17-G5-03-F11` accepted the TS08 correction: strict flip@4 JSON stays stderr-clean and a
  separate human alias invocation proves the structured deprecation warning.
- `P17-RV-P17-G5-03-F12` accepted the TS01 correction: the exact shared inverse after migration is
  `remove`, not a fabricated rollback/restore operation kind.

Fresh re-review added `P17-RV-P17-G5-03-F13`: empty `--tool` selects the capability-bounded undo
tool universe, not effective configuration. The plan now truthfully labels both command-default
tools and command-default user/current-project scopes `unbounded-default` within the narrowly
validated lifecycle-history universe; explicit filters remain `explicit`.

Fresh re-review added `P17-RV-P17-G5-03-F14`: canonical placement swap shadows currently erase the
approved reversibility and retained-resource facts, so status correctly classifies seeded dev,
promote, install, and uninstall history as not reversible. No ownership expansion is needed because
swap, status join/read tests, and TS02 are already owned. The plan now freezes family-specific
forward retention: a non-absent development before-image retains its exact source path/digest under
the journal's source-content retention class; pinned/store-linked before-image retains its exact
store path/digest; both use the pair resource ID and refuse missing/mismatched facts. Fresh install
and link-dev from absent are the sole zero-retention exception and are eligible only while the
current live image exactly matches the recorded forward after-image. No other `none` journal is
made reversible.

## Accepted implementation amendment

Fresh independent re-review returned **GO** at 0.99 confidence with no new stable findings for plan
`ee9a37dd2bf18d27fb049b9ae6b928fd72c7a3dd8644b5cf608c086534717078` and evidence
`3ea9c03623b855ab1410c268262574c919a89a005ab27f88e7bea16ccf6e555b`. The reviewer confirmed
F09 through F14 closed, the exact 84-path 38/20/5/19/2 manifest and 86 catalog-owned paths, all six
new ownership paths untouched before acceptance, the four closed inverse mappings, dependency-
closed history/cleanup, artifact-free lifecycle-history observation, truthful family-specific
forward retention, the fresh install/link-dev zero-retention exception, atomic first publication,
fresh resume versus converted rollback routing, and the TS01/TS08 corrections.

This GO authorizes implementation in the six amended paths. It does not advance a lifecycle gate;
focused hostile-state, capacity/cleanup/restart, and every durable crash-phase receipt remain
required before minimal implementation or targeted green is claimed.

The reviewer also confirmed the four inverse mappings only for committed install/remove/link-dev/
promote parents, with managed store-linked state normalized consistently with pinned state and all
other operation/orientation families refused. A fresh review of the resulting 84-path amendment is
required before any of the six added product/test paths is edited.

## TS08 machine-bound identity proof amendment

Independent follow-up review identified `P17-RV-P17-G5-03-F15` as blocking at 0.99 confidence. The
selector created two independent `mkdtemp` fleets and deep-compared their undo operations, checks,
and diagnostics. Each fleet derives different project, source, store, ledger, and live paths;
placement planning intentionally embeds those machine-bound identities in resources, pair IDs,
operation IDs, and dependent check/precondition identities. A shared planner therefore guarantees
exact equality for the same observation, not for two semantically different machines/projects.

The narrow implementation-free correction uses one seeded fleet for direct undo dry-run, rollback
alias dry-run, and the separate human deprecation check. It snapshots the fleet before invocation
and proves both dry-runs leave that snapshot unchanged before comparing their operations, checks,
and diagnostics exactly. This changes no path, validation ID, ownership, catalog relation, product
behavior, or assertion strength. Fresh independent acceptance of this exact correction is required
before the already-owned selector is edited.

Fresh independent review returned **GO** at 0.99 confidence for plan
`b105b335dfb05d27a02a93f790bd58f7ab76cfe7952a2a283f94b1bfe0ec2a8d` and evidence
`fba56956eccdf5ee630714aa63e25aefeff66cdd64f726dfdae2b5eeb844e21d`. The reviewer confirmed the
one-fleet unchanged-snapshot proof, exact direct/alias equality, separate human deprecation check,
unchanged 84/86 ownership counts, and no new stable findings. This GO authorizes only the already-
owned TS08 selector correction; it does not advance a lifecycle gate.

## Dependency-scoped operation identity compatibility clarification

Concurrent history tests identified `P17-RV-P17-G5-03-F16`: the signed ledger-v2 golden contains a
legacy pending-forward snapshot and committed snapshot of the same operation ID. A global new
operation-ID uniqueness rule would reject existing readable ledgers and break unrelated signed
compatibility. Fresh rollback linkage does not need that widening. The narrowed invariant requires
unique committed-history operation IDs, unique pending-map operation IDs, a fresh distinct-parent
child ID that does not collide with history, and one exact committed-forward resolution for every
fresh parent link. The legacy pending-forward/committed duplicate remains readable but cannot act
as fresh-parent authority. No path, schema, fixture, catalog relation, or product behavior changes;
fresh independent review of this compatibility interpretation is required before implementation
acceptance.

Fresh independent review returned **GO** at 0.99 confidence for plan
`556e5a43191d1a0144ca6e57501c2299e93cd7b00eb8f79debf39d074ac435ef` and evidence
`d56c1e477a46b50a35b94e045054a1121bd543f1e81b9408785fa083d903f964`. The reviewer confirmed the
dependency-scoped rule preserves null/self-parent rollback compatibility and signed pending/history
reuse without weakening exact committed-forward parent authority. Implementation acceptance still
requires golden round-trip plus hostile duplicate pending, duplicate history, child collision,
pending-only parent, missing/non-forward parent, null/self-parent, and shared-ID authority tests.
No ownership or lifecycle gate changes.

## Legacy pending execution-mode assertion clarification

The first green TS01 replay identified `P17-RV-P17-G5-03-F17`: the implementation-free selector
expects `resume-rollback` for its preserved v1 forward-pending pair journal. That journal has no
published logical rollback transaction to resume. The already-accepted F07 design instead migrates
and routes the preserved forward journal through the signed legacy backup-restoring rollback path
without fabricating a logical transaction; the truthful public execution mode is therefore
`convert-to-rollback`. For pending-abort candidates, a journal already carrying rollback
disposition reports `resume-rollback`. This distinction does not change the existing
`resume-rollback` mode for a fresh committed-forward reversal candidate whose first atomic write
publishes its distinct-parent rollback child and inverse physical shadow. The narrow correction
changes only the existing TS01 assertion, with no path, validation ID, ownership count, schema,
fixture, catalog relation, product behavior, or lifecycle gate change. Fresh independent review of
this exact clarification is required before the selector is edited.

Fresh independent review returned **GO** at 0.99 confidence for plan
`7cec544de25faff457e655e54c99ac961788caac6bf9eefa0e9aabcaeaa57074` and evidence
`dbb1f9343276b82ec9c49e2eae32681fb93d228594acedf4042d936808d6b810`. The reviewer confirmed the
pending-abort distinction, preserved legacy recovery route, absence of fabricated logical identity,
and unchanged fresh committed-reversal mode. F17 authorizes only TS01's assertion correction to
`convert-to-rollback`. Implementation acceptance additionally requires an already-owned focused
observation regression proving exact `resume-rollback` classification for both self-parent converted
and distinct-parent fresh pending rollback journals. No path, ownership, catalog, schema, fixture,
count, product-behavior, or lifecycle-gate change is authorized.

## Reachable selector and hermetic fleet clarifications

The first reachable P5-TS03 and workflow replays identified three implementation-free defects:

- `P17-RV-P17-G5-03-F18`: P5-TS03 expects only `project` in selection provenance for an unscoped
  explicit target inside a project, contradicting accepted F13. Command-default selection covers
  the capability-bounded user/current-project universe; the one matched group remains project-only.
- `P17-RV-P17-G5-03-F19`: WF05 requires strict static+deep verification for Claude and Codex, but
  the shared fake executables emit neither Claude's load-bearing stream-JSON init nor Codex's
  marketplace add/list transcript. The fixture therefore fails before reaching undo. The narrow
  correction emits only the deterministic successful local transcripts already required by WF05.
- `P17-RV-P17-G5-03-F20`: after SIGKILL, TS09 immediately retries while the canonical lock is still
  younger than the signed 30-second stale horizon. The coordinator correctly excludes it after its
  bounded retry window. The plan explicitly excludes deadline/retry policy changes. After awaiting
  dead-child exit, the fixture must age only that orphaned lock directory beyond the existing stale
  horizon so the selector exercises canonical stale-lock reclamation and durable recovery without
  bypassing the outer lock or weakening live-holder exclusion.

These corrections stay inside the three already-owned selector/fleet paths and change no product
behavior, validation ID, schema, catalog relation, ownership count, lifecycle gate, runtime deadline,
or retry policy. Fresh independent review of the exact correction is required before those test and
fixture lines are edited.

Fresh independent review returned **GO** at 0.99 confidence for plan
`48425e310542b8986f7a39ec82149a215f4640c0ae439794938a0c1714d7de80` and evidence
`a2692883dfd7f0cb608c2b830c6b7a9d9091ff2982b4b83576936c53e3ee44c0`. The reviewer reproduced each
one-to-one cause and confirmed the 84/86 ownership arithmetic. F18 authorizes only the default scope
expectation. F19 requires exact known Claude/Codex static/deep transcripts and fail-closed unknown
invocations, with no network, subprocess, mutation, environment, or canary output. F20 requires
`lstat` of exactly `${fleet.ledger}.lock` after SIGKILL and awaited exit, directory-only `utimes` to
60 seconds old, and ENOENT-only tolerance; deletion, symlink following, sleeping, other locks, and
runtime policy changes remain forbidden. No lifecycle gate advances.

## Same-scope cross-tool name selection clarification

After F19 made WF05's verifier fixture reachable, its exact canonical
`undo review --project` command identified `P17-RV-P17-G5-03-F21`: both Claude and Codex contain the
same project-scoped skill, but the generic target resolver treats the two tool pairs as ambiguous.
The accepted WF05 command and batch contract require all selected sibling pairs to participate in
one canonical name/scope group. The ambiguity contract is scope/anchor safety: an unscoped name
spanning user and project, a glob, different names, different source operation-group IDs, or
multiple anchors within one tool/scope still refuses. Within one selected scope, one exact
same-name pair per selected tool with one shared source group ID is an intentional multi-pair
selection in deterministic tool order. The generic resolver remains unchanged.

The first independent F21 review returned **NO-GO** at 0.99 confidence because an observation-only
fix would produce two report rows with the same preserved group ID, violating undo@1 uniqueness and
CF-026. It also found that valid cross-tool placements necessarily have different paths, so path
equality cannot define the safe group. The corrected model represents one canonical group with
ordered pair rows. The group carries group ID, skill/scope, aggregate operation IDs/outcome/failure;
each pair carries tool/pair and lifecycle/linkage/retention/operation/outcome/failure facts. The
signed scheduler continues to execute the shared operation group and report every pair; aliases
still project the same prepared operations and pair results. This correction remains within already-
owned undo observation/types/plan/contract/application/output and focused-test paths. It changes no
generic resolver, command grammar, workflow, catalog relation, path/ownership count, or lifecycle
gate. Fresh independent review of the corrected representation and hostile ambiguity boundary is
required before implementation.

The second independent F21 review also returned **NO-GO** at 0.99 confidence. It accepted the nested
group/pair structure but found the aggregate outcome, failure, summary-unit, and alias rules
underspecified, and found stale prose that still prohibited cross-tool grouping. The corrected plan
now fixes canonical registry pair order and public plan operation order; maps failed, cancelled,
missing, and `skipped-after-failure` results exactly; selects the first sanitized operation/pair
failure in deterministic order; defines duplicate-free group operation concatenation and exact
group-outcome precedence; defines mixed actionable/already-reversed behavior; counts summaries by
canonical group while effects remain operation effects; and keeps G5-03 `skipped` at zero because
skipped scheduler results map to `not-run`. It also requires one-for-one pair-result projection and
same-fleet direct/dev-alias/promote-alias operation/check/diagnostic parity while flip@4 remains
unchanged. Fresh independent acceptance of these exact rules is required before implementation.

The third independent F21 review returned **GO** at 0.99 confidence for plan
`46200bd90f44e1f822a784834c9cb2b7a8efda7b23da77ddfde94ca44439da44` and evidence
`901e1f75deaf342e2be1031645b1cdc08c291300822c1201b4b7c02bc321ed73`. The reviewer confirmed the
exact-literal, single-scope, unique-tool, shared-source-group admission boundary; the canonical
group with registry-ordered pair rows; the complete pair/group outcome, failure, ordering, and
summary reductions; and the hostile ambiguity, alias-parity, and one-for-one pair-result coverage.
The generic resolver and flip@4 remain unchanged. F21 is accepted for implementation within the
already-owned G5-03 paths and advances no lifecycle gate.

## Immutable implementation adversarial review and F22/F23 amendment

The complete F21 implementation was committed as
`c34be66b7e2008fbfa713e7024490cb4388925c6` with a clean worktree. Before independent review, root
replayed 61 focused/public tests with 434 assertions, all eight undo selectors with 200 assertions,
P5-TS03, WF05, WF11, DEV-TS05, PROMOTE-TS05, whole-repository Biome, boundary lint, TypeScript,
package validation, smoke, and recovery smoke successfully. No terminal `bun run check` was run.

Independent adversarial review replayed all 13 required selectors individually and 215 focused
tests, then repeated TypeScript, boundary, PR-openable package, ownership, clean-tree, and diff
checks. The reviewer confirmed F21 grouping, lifecycle observation, fresh/converted identity,
dependency-closed retention, deterministic scheduling/reduction, approval, dry-run, alias parity,
and flip@4 compatibility. The verdict was nevertheless **NO-GO** at 0.99 confidence with two new
stable findings:

- `P17-RV-P17-G5-03-F22` (high): logical commit terminalizes the pair before physical backup
  reclamation and fsync, destroying durable cleanup authority during the committed window. Current
  recovery only canonizes committed forward install/remove shadows and cannot cleanup-only resume
  committed fresh rollback shadows for inverse uninstall/install/dev/promote.
- `P17-RV-P17-G5-03-F23` (acceptance-blocking): undo@1 validates effect references and count but not
  the exact ordered canonical effect vector, so omissions, duplicates, wrong roles/actions, and
  outcomes contradicting execution results can pass.

Read-only root and foundation audits reproduced both causes. The smallest F22 correction is a
two-write terminal protocol around cleanup: first commit the distinct-parent child exactly once
while retaining its committed physical shadow; reclaim-or-retain the backup and fsync; then clear
the shadow or delete the pair. Recovery must classify the exact history child plus unique forward
parent and run cleanup-only, never `forward`, across the seven admitted orientations and both
scopes. The audit also found the managed-to-managed promote inverse must match the physical before
image against the rollback child's actual before-image, independently matching the restored pair
against its terminal after-image.

The smallest F23 correction derives effects in operation order and ledger-before-live role order,
with exact action/identity and mode/approval/result-derived outcomes, then requires structural vector
equality. The accepted fixture is corrected to the two effects implied by its ledger+live mutation,
and hostile coverage closes omission, duplication, order, role, action, identity, and outcome.

The first F22/F23 amendment review returned **NO-GO** at 0.99 confidence for F22 and accepted F23.
It found three specification defects before product edit: older `already-reversed` no-mutation prose
did not distinguish recovery-only terminalization or approval timing; the first durable write
incorrectly described an uninstall pair as already terminal even though its shadow must survive in a
carrier pair until deletion; and targeted recovery versus global sweep routing plus cleanup warning/
failure outcomes were not closed.

The corrected F22 text now makes an exact committed child/shadow internally `cleanup-pending` while
retaining public `already-reversed` outcome and emitting one correlated `undo-cleanup-pending`
warning diagnostic. That diagnostic provides strict preview/approval authority without changing the
undo@1 schema version or pair shape. Dry-run and every refusal/cancellation path remain write-free;
mixed and cleanup-only execution both require approval, then cleanup runs before any scheduler work.
Success has no cleanup operation/result/effect, retained-backup warnings surface at application
level, and cleanup failure returns a top-level command error before scheduler work while durable
shadows preserve retry. The intermediate uninstall state is explicitly a nonterminal cleanup-carrier
pair whose physical before-image binds to child `actual.before`, whose `actual.after` and observed
live state must be absent, and whose deletion occurs only after cleanup plus fsync. Present terminal
orientations independently validate their carrier pair against child `actual.after`.

Targeted `recoverPlacement` and global `recoverCommittedAcquirePlacements`, including observed
variants, must share the exact cleanup-only classifier/finalizer and branch before pending attempt
persistence or resume/forward routing. Both reuse the same child/parent, never rename or toggle live,
never append history, preserve the shadow on failure, and terminalize once after successful cleanup.

The amendment adds no owned path or catalog edge: the manifest remains 84 product/test/governance
paths and catalog ownership remains 86 including plan/evidence. The catalog correctly stays at
3/10 gates with reviewer unset. Product/test correction, a new immutable commit, repeat adversarial
review, and only then the terminal repository gate remain outstanding.

Fresh independent re-review returned **GO** at 0.99 confidence for exact plan hash
`15f6311719ea09c4489043add3ef34368f79109f481b94d3585ed289c7aa0dbb` and evidence hash
`69676f5bd5d833199fc2e2c574dfac9e500b7254fc03d8ef2f3bb237a7ad364d`. It confirmed all three F22
specification blockers closed, F23 remained complete, `git diff --check` passed, and ownership stayed
at 84 implementation-review paths/86 catalog paths. No new stable finding remained. Product/test
correction is now authorized inside the existing manifest; lifecycle state remains 3/10.

## F24 coordinator-seam amendment

Before cleanup-pending upper-layer wiring, implementation review found that pre-scheduler cleanup
would change the ledger byte revision outside `executeOperationPlan`. A mixed plan would then reject
its own exact approved ledger precondition. Removing or rewriting that precondition, replanning after
approval, cleaning during observation/refusal, or binding against a fictional terminal revision are
not acceptable fixes.

`P17-RV-P17-G5-03-F24` adds the existing execution coordinator types, implementation, and focused
test to ownership. One optional hook runs exactly once under the existing locks after preconditions
and actual-before bindings validate and before scheduling. Failure/cancellation dispatches no work;
no hook preserves current behavior. For mixed undo, physical cleanup and fsync occur in the hook,
the pure terminal projection remains in memory, and the first actionable P1 write atomically carries
that projection while using the original on-disk revision as CAS authority. For zero-operation
cleanup-only execution, the hook persists the terminal projection itself. Cleanup plus migration is
invalid because an exact committed logical carrier already requires ledger v2. Standalone targeted/
global recovery continues to persist through the same lower classifier, cleanup, and finalizer.

The amended manifest is exactly 87 product/test/governance paths and 89 catalog-owned paths including
plan/evidence: 40 production, 21 focused tests, 5 selectors/fleet, 19 signed compatibility owners,
and 2 governance owners. No new engine, public schema, validation relation, or lifecycle advancement
is proposed. The three new paths remain untouched pending fresh independent review.

The first F24 review returned design **GO** but overall **NO-GO** at 0.99 confidence because catalog
ownership still contained only the prior 86 paths. Root added only the three reviewed paths to
`P17-G5-03.ownedFiles`. Final review returned **GO** at 0.99 confidence for catalog hash
`9e30b2a61ad1ca42e060cb5e283f5e87887ac9a40cc4e42bc096c619ec292cc1` and the unchanged F24 plan/
evidence hashes. It verified 89 unique owned paths, 87 excluding plan/evidence, exact manifest order,
all new paths untouched, diff hygiene, and no new stable finding. F24 is authorized without a
lifecycle transition.

## F25 runtime journal codec amendment

Lower codec characterization reproduced one further blocker before editing the unowned source. The
ledger codec's closed inverse mapper admits forward promote managed A to managed B plus fresh rollback
promote B to A, but `runtimeJournalOperationMatchesMatrix` currently inherits the saved-plan rule
that promote before-state must be development. The valid committed parent is rejected at journal
intent decoding before cross-reference validation.

F25 adds only `packages/core/src/artifacts/plan-codec.ts`. The private runtime-journal matrix may
accept exact pinned/managed-to-pinned/managed promote with portable source, canonical identity,
scope, relationships, and ledger/live flags. The public saved-plan matrix remains byte-for-byte
semantically closed and must reject the same managed-to-managed shape. The already-owned codec test
proves runtime acceptance, saved-plan refusal, exact ledger parent/child/shadow round-trip, and
hostile source/image/identity/scope/flag/family near-misses.

Current ownership becomes 88 product/test/governance paths and 90 catalog paths including plan/
evidence: 41 production, 21 focused tests, 5 selectors/fleet, 19 signed compatibility owners, and 2
governance owners. The new path is untouched pending fresh independent review; no lifecycle gate
advances.

Fresh independent review returned **GO** at 0.99 confidence for catalog
`90eabc051f50a3c35ef6022b7f3e1bcdd4a5dda7624ae8db316d55c5e99025df`, plan
`4de0ceca13802bf1345af29dfb47aacf63c010c301697fbff02eca6b230f95e5`, and evidence
`4c2bc745db4f63f83db84e8481321f81d77fa44f62193637b36f28e0eef5788b`. It confirmed runtime-only
managed promote admission, unchanged saved-plan refusal, hostile codec/ledger coverage, exact 90
unique ownership and manifest order, untouched `plan-codec.ts`, faithful F24 evidence, and no new
stable finding. F25 implementation is authorized with lifecycle state unchanged at 3/10.
