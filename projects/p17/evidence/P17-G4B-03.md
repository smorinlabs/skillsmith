# P17-G4B-03 evidence

## Mapping and dependency audit

- **Dependency:** G4B-02 is signed off at `20ce62d` and its complete repository gate passed with
  2,853 tests passed, 28 skipped, zero failed, and 100,427 assertions.
- **Starting state:** the worktree was clean; G4B-03 was pristine planned with 0/10 gates passed,
  no paths, no commands, no implementer, and no reviewer.
- **Primary scope:** exactly six planned entities: `D-015`, `EWP-P4B-T05`, `EWP-P4B-TS05`,
  `EWP-P4B-TS06`, `EWP-WF03`, and `EWP-WF04`.
- **Required-now partition:** exactly 22 validations: four missing G4B-03 primaries and 18 signed
  dependency replays. There is no downstream validation.
- **Workflow scheduling:** the user-approved Phase-4 workflow ownership amendment already moved
  WF03/WF04 here because both require live plan/apply and cross-machine reproduction; G4A-03 and
  G4A-02 retain downstream traceability respectively.
- **Existing authorities:** selected artifact discovery, canonical manifest/lock correlation,
  default source resolution, visible `write-lock`, exact saved policy, approval, lock hierarchy,
  under-lock snapshot validation, artifact-pair recovery, ledger journal/recovery, live swap,
  export/init writers, redaction, and hermetic Git fixtures already exist and are signed.
- **Existing coverage:** plan TS04 proves current/missing/incomplete/stale/noncanonical lock
  behavior; plan TS09 proves portable saved bytes; export TS09 proves copied-pair canonical
  readability but stops before Machine-B plan/apply. P4A-TS02 and P2-TS04 prove the underlying
  durable authorities, while G4B-03 still lacks apply-level holistic recovery and Machine-B
  execution selectors.
- **Actual gaps:** the four planned selector files do not exist. The current resolver also permits
  bounded and filter-to-zero `--locked` invocations against an incomplete/stale whole lock, which
  conflicts with the authoritative D-004/D-015 whole-pair frozen-input contract. `resolve.ts` and
  its focused unit test own that one known product correction; safe default bounded no-widen and
  refusal semantics remain unchanged.
- **Architecture result:** add one shared test fixture, one bounded apply crash child, and exactly
  four holistic selector files. Reuse current product authorities. Coordinator, apply execution,
  ledger writer, swap/recovery, export, init, parser, renderer, and artifact schema files remain
  outside ownership unless executable red evidence and a reviewed amendment prove a defect.
- **Efficiency correction:** signed dependency selectors are not rebuilt or repeatedly cold-run
  during mapping. They run once at impacted-green. New development loops use only the four new
  selectors, relevant resolver units, and smoke/static checks; the complete durability and
  repository gates run at the terminal recovery milestone.
- **Safety context:** every fixture is local and disposable; canonical public identities use
  `fixture.invalid`; all credentials are synthetic canaries; no public network or external write
  is authorized or needed.
- **Supported-platform proof:** the corrected WF03 boundary parameterizes its content-hash
  reproduction case over darwin/linux runtime ports and requires the exact selector to pass in the
  existing macOS/Ubuntu CI matrix. Different roots on one host are not treated as sufficient
  cross-platform evidence; no CI workflow edit is needed because the matrix already runs the full
  test suite.
- **Result:** mapping is complete. The corrected frozen Ready candidate remains ten paths and 22
  selector commands. No unresolved user-policy, dependency, network, or safety blocker exists.

## Ready review

Fresh independent reviewer `/root/g4a04_corrected_adversarial_review` returned **NO-GO** at 0.98
confidence on the first candidate. Two findings were accepted:

- `P17-RV-G4B-03-F01` (**high**) found that the candidate inherited G4B-01's bounded
  selected-pin `--locked` exception even though consolidated Section 2.2, D-004, and D-015 require
  a current complete whole pair. The correction makes every locked invocation, including bounded
  and filter-to-zero, whole-pair strict and assigns the known resolver/unit correction inside the
  existing frozen paths.
- `P17-RV-G4B-03-F02` (**medium**) found that distinct Machine-A/Machine-B roots on one host do not
  prove WF03 across supported platforms. The correction adds parameterized darwin/linux runtime
  ports and requires the exact WF03 selector's receipts from the repository's existing
  macOS/Ubuntu CI matrix before sign-off, without editing CI.

The first plan SHA-256 was
`c8c54b2cbeeb268848c86ac231d6d062f01043568a95ba4cfd6dea7f8f02c144`. The reviewer edited
nothing.

The corrected candidate received **GO** at 0.99 confidence with no residual high, medium, low, or
blocking finding. The reviewer independently verified the strict whole-pair rule against Section
2.2, D-004, and D-015; confirmed `RuntimePorts.platform` plus the existing macOS/Ubuntu full-test
matrix supplies the supported-platform execution receipts without a CI edit; and rechecked the
six-primary, 22-required, four-new, 18-replay, zero-downstream arithmetic, 22 unique commands, ten
paths, single recovery authorities, Machine-A/Machine-B separation, WF04 fidelity, and terminal
gates.

The accepted Ready-plan SHA-256 is
`175e8b11dfd44fdc311f4773331a8f7225ff3abb4cf07764968f3ecca2827d8b`; the reviewed
pre-acceptance evidence SHA-256 is
`40558531b5e9dd22a4067fcb58bf0fcfae3026c54600da7492b6583985bca54c`. The reviewer edited
nothing.

## Accepted Ready result

The corrected candidate is accepted under the user's standing authorization to continue P17. No
entity ownership, validation target, tier, dependency, schedule, operation vocabulary, artifact
schema, or recovery mechanism changes. G4B-03 advances to `ready`; mapping and Ready gates pass;
`EWP-P4B-T05` advances to ready with this frozen plan/evidence. D-015 and all four new validation
entities remain pristine planned until executable red evidence. No test-first, implementation,
green, refactor, adversarial, traceability, or sign-off gate is implied.

## Test-first evidence

Only the six accepted selector/fixture paths changed at immutable test pin `f253e73`. No product,
documentation, catalog, existing signed selector, coordinator, durable-I/O, schema, or recovery
authority changed.

All four exact new selectors ran independently and reached their hermetic CLI fixture. Each failed
with one failing test, zero passing tests, and zero skipped tests because bounded `--locked`
plan/apply returned success for a selected current pin while another manifest declaration was
absent from the canonical lock. The assertions require exit 3 and `plan-locked-state`, directly
characterizing the D-004/D-015 whole-pair frozen-input rule accepted during Ready review.

The failure is not fixture or infrastructure noise: `bun run test:smoke` passes 5 tests with 3,735
assertions; `bun run typecheck`, changed-file Biome, diff hygiene, pre-commit gitleaks, and the
no-skip/no-todo/no-public-network scan pass. The selectors currently pin the common terminal
policy defect first; their accepted crash/recovery and Machine-A/Machine-B matrices remain required
before minimal implementation can pass.

**Test-first result:** PASS. G4B-03 advances to active with the test-first gate passed;
`EWP-P4B-T05` advances to red; all four new validation entities advance to failing at their exact
executable targets. D-015 remains planned until its complete validation set is green. Minimal
implementation and every green/review/sign-off gate remain pending.

### Red execution receipt EWP-P4B-TS05

- Validation ID: `EWP-P4B-TS05`
- Command: `bun test tests/ergonomics/phase/EWP-P4B-TS05.test.ts --test-name-pattern EWP-P4B-TS05`
- Exit status: `1`
- Result: 1 failed, 0 passed, and 0 skipped because bounded locked apply accepted an incomplete whole pair and exited 0 instead of 3.
- Revision: `f253e73a87623a6c2ceb2ac77e7cfab590892bd7`

### Red execution receipt EWP-P4B-TS06

- Validation ID: `EWP-P4B-TS06`
- Command: `bun test tests/ergonomics/phase/EWP-P4B-TS06.test.ts --test-name-pattern EWP-P4B-TS06`
- Exit status: `1`
- Result: 1 failed, 0 passed, and 0 skipped because bounded locked plan accepted an incomplete copied pair and exited 0 instead of 3.
- Revision: `f253e73a87623a6c2ceb2ac77e7cfab590892bd7`

### Red execution receipt EWP-WF03

- Validation ID: `EWP-WF03`
- Command: `bun test tests/ergonomics/workflows/EWP-WF03.test.ts --test-name-pattern EWP-WF03`
- Exit status: `1`
- Result: 1 failed, 0 passed, and 0 skipped because bounded locked bootstrap accepted an incomplete whole pair and exited 0 instead of 3.
- Revision: `f253e73a87623a6c2ceb2ac77e7cfab590892bd7`

### Red execution receipt EWP-WF04

- Validation ID: `EWP-WF04`
- Command: `bun test tests/ergonomics/workflows/EWP-WF04.test.ts --test-name-pattern EWP-WF04`
- Exit status: `1`
- Result: 1 failed, 0 passed, and 0 skipped because bounded locked restore accepted an incomplete explicit pair and exited 0 instead of 3.
- Revision: `f253e73a87623a6c2ceb2ac77e7cfab590892bd7`

## Minimal implementation

### Cross-machine portable-digest ownership amendment

The expanded WF03 default-saving install and TS06/WF04 Machine-A export to clean Machine-B locked
apply scenarios produced the same independent red product result after the strict locked
correction passed: saved apply returned exit 3 and `apply-source-stale`. Diagnostic comparison
proved the resolved SHA, skill name, and source path were exact. The live Machine-A bytes and fresh
Machine-B materialization also had the same source-content digest. Only the install/export lock
carried a different digest: both writers copied the legacy placement-ledger hash into the portable
source-content field.

The first three-path export-only amendment received an independent **NO-GO** at confidence 0.99.
The reviewer confirmed the missing install writer and proved that simply hashing a live export tree
would be insufficient: the legacy placement digest omits empty directories, while ordinary Git
cleanliness can omit ignored members. A provisional review then found that path membership plus
ordinary Git cleanliness still does not prove executable mode, blob/symlink bytes, or a stable HEAD.
The final review also identified mutable-store resolution elision as an acquisition exact-SHA gap:
a saving rerun could hash a tampered store directory instead of a fresh checkout. The final corrected
candidate therefore adds exactly thirteen paths:
`packages/core/src/acquire/run.ts`, `packages/core/src/acquire/plan.ts`,
`packages/core/src/acquire/resolve.ts`, `packages/core/tests/acquire/install-run.test.ts`,
`packages/core/tests/acquire/resolve.test.ts`,
`packages/core/src/export/observe.ts`, `packages/core/src/export/classify.ts`,
`packages/core/src/export/portable-content.ts`,
`packages/core/tests/export/portable-content.test.ts`, and
`packages/core/tests/export/classify.test.ts`, `packages/core/src/ports/types.ts`,
`packages/core/src/ports/git.ts`, and `packages/core/tests/ports/git.test.ts`.

Saving acquisition will explicitly disable mutable-store elision and use a fresh exact-revision
checkout; `--no-save` retains existing elision because it cannot write a portable artifact itself.
Acquisition will retain its legacy operation-source/store/precondition digest and separately carry
the canonical digest computed from the fresh materialization as an additive optional install-intent
fact into artifact planning and its coherence validation. Tampered-file and legacy-invisible
empty-directory saving-elision cases are frozen by resolver/install tests. Managed export does not
trust a mutable store or legacy ledger as exact-SHA authority: it independently fetches the recorded
full SHA into a cleaned temporary repository, projects the Git objects, and requires its canonical
digest to equal the live projection. WF04 freezes the poisoned `--no-save` ledger followed by closed
managed-export refusal. For Git-dev, the Git port will retain `ls-tree` object mode; the internal
helper projects directories, exact blob bytes/executable bits, and symlink targets at the inspected
HEAD, compares that digest to the live projection, and requires identical clean before/after
HEAD/remote inspections. Only a stable exact-revision digest may become a portable candidate. The optional
`portableContentHash` observation fact is an acknowledged additive public domain shape; versioned
wire reports, schemas, writers, transactions, recovery authorities, and digest domains remain
unchanged. `GitTreeEntry` gains optional object-mode metadata and the existing Git `commit` object
kind is preserved so submodules fail closed. The frozen boundary is twenty-three paths.

Independent corrected-amendment review: **GO** at confidence **0.98**. The reviewer verified the
23-path arithmetic, exact-SHA authority for saving install plus managed/dev export, additive public
type accounting, fail-closed Git object semantics, and absence of wire/schema/ledger/lock-domain,
writer, recovery, or transaction contradictions.

### Same-machine legacy-ledger reconciliation amendment

After the portable writer correction, the expanded WF03 chain advanced through install and locked
planning but saved apply returned exit 3. The plan falsely described the exact installed copy as a
local-dev `modified-managed-target`: reconcile observation/planning still compared the canonical
live/lock digest to `pair.pinned.contentHash`, which intentionally remains in the legacy placement
domain.

The first three-path planning-only proposal received an independent provisional **NO-GO** because
under-lock physical reobservation independently reconstructed the same exact placement as local-dev
and would fail its approved before-image. A second provisional **NO-GO** found move-scope's placement
transaction validator repeating the same cross-domain equality. The corrected candidate adds
`packages/core/src/reconcile/observe.ts`, `packages/core/src/reconcile/plan.ts`,
`packages/core/src/reconcile/apply-execution.ts`, `packages/core/tests/reconcile/plan.test.ts`, and
`packages/core/tests/reconcile/apply.test.ts`, `packages/core/src/place/swap.ts`, and
`packages/core/tests/place/swap.test.ts`. The lock and
canonical live/store projections own desired content; ledger origin/SHA/source/path/placement facts
own provenance without cross-domain digest equality. A canonical live-versus-lock mismatch remains
the modified-managed conflict. Exact/no-op, repair/update, move, prune, and under-lock before-image
reobservation use the same separation. No observation or operation-image schema, ledger byte, or hash
algorithm changes. Move-scope validates canonical operation hash coherence and ledger provenance as
separate facts. Provenance includes the exact placement path, representation, and canonical store
path. Focused tests cover no-op, mismatch conflict, repair/update, move/refusal, prune/preserve, and
under-lock update/remove/move parity. The frozen boundary advances from 23 to 30 paths.

Independent reconciliation-amendment review: **GO** at confidence **0.98**. The reviewer verified
the 23+7=30 arithmetic, complete planning/under-lock/move authority, independent provenance checks,
canonical drift protection, focused test matrix, and absence of schema, ledger-byte, hash-domain,
writer, or recovery-format changes.

### Artifact-group recovery identity amendment

Executable TS05 advanced through the injected manifest and lock artifact barriers and successful
rollback, then exposed two existing composition failures. Legacy-manifest migration and missing-lock
replacement were initially planned as separate artifact groups, which the artifact binding factory
correctly refused; reversing them was also invalid because the lock cannot be committed beside the
legacy manifest. Grouping them and ordering migration then lock allowed recovery, but the public
saved-plan projection changed the lock operation ID after the migration had durably committed: the
two-operation plan derived the projected group from the manifest resource, while the lock-only
replan derived it from the lock resource. This contradicts TS05's reviewed-operation identity and
same-operation recovery requirement. The ledger seam then proved apply also classified its own
exact forward pending transaction as stale before the existing recovery engine could resume it.

The narrow correction adds `packages/core/src/reconcile/saved.ts` and
`packages/core/tests/reconcile/saved.test.ts` to the already-owned planner paths. One stable
artifact-pair projection target will identify manifest migration and lock replacement before and
after partial group recovery; focused tests will freeze the shared group, migration-to-lock
dependency, live lock prefix, and stable public lock ID. The already-owned apply-execution path
will admit only a unique forward journal whose complete durable operation intent matches the
reviewed identity and before/after images, and will pass it to the existing resume authority;
non-matching residue remains a stale-state refusal. No schema, writer, recovery format,
transaction authority, or operation vocabulary changes. The frozen boundary becomes 32 paths.

### Export dependency-fixture transport amendment

The first exact dependency replay, `EWP-CMD-EXPORT-TS01`, exited 1 because its managed migration
preview contained no effects. The export command itself exited successfully; classification had
failed closed because exact managed export now fetches the ledger-recorded SHA, while the contract
fixture seeded through an in-process local transport and spawned its CLI with global/system Git
configuration disabled but without the remote fixture's existing rewrite environment. The public
`fixture.invalid` source was therefore intentionally unreachable.

The narrow test-only correction adds `packages/cli/tests/contracts/export.test.ts` and, after the
same result in the P4A dependency replay, `tests/ergonomics/phase/EWP-P4A-TS03.test.ts`. Boundary
lint then correctly rejected post-helper environment spreading, so the amendment also adds
`packages/core/tests/fixtures/git-env.ts` and its focused `git-env.test.ts`. Each replay writes the
fixture's existing rewrite facts to an isolated config under its disposable fleet root and selects
it through one explicit trusted-global-config helper option; dynamic `GIT_CONFIG_*` and all other
repository-local redirects remain scrubbed. No public network, production behavior, expected
report, schema, writer, or recovery authority changes. The frozen boundary becomes 36 paths.

### Terminal full-suite compatibility amendment

The first terminal full-suite attempt was deliberately stopped after it had produced a definitive
non-signable result and continued deep into unrelated passing units. It exposed three stale signed
fixtures outside the reviewed boundary:

- P4B-TS01's orphan pair used a synthetic noncanonical store path, so the strengthened independent
  provenance check correctly omitted its prune operation;
- P4B-TS04 combined `--locked` with an intentionally extra lock pin, so the new strict whole-pair
  rule correctly returned `plan-locked-state` before its intended prune assertions; and
- P4A-TS02 reset deterministic transaction IDs across separate install invocations and expected
  the superseded standalone ref-resolution call, producing a history collision on rerun.

The narrow correction adds only `tests/ergonomics/phase/EWP-P4A-TS02.test.ts`,
`tests/ergonomics/phase/EWP-P4B-TS01.test.ts`, and
`tests/ergonomics/phase/EWP-P4B-TS04.test.ts`. It preserves each selector's signed purpose while
using canonical store provenance, default-mode prune, strict locked refusal in the G4B-03
primaries, one noncolliding deterministic fixture sequence, and the exact saving transport path.
No product, schema, recovery, wire, assertion, or network behavior changes. The boundary advances
from 36 to exactly **39 paths** and the terminal command inventory advances from 22 to **25**.

The test-only transport amendment preserves the remote fixture's hostile-environment contract:
the helper still scrubs every dynamic `GIT_CONFIG_*` override before selecting the one explicitly
trusted isolated config. Its focused regression passes **8 tests with 50 assertions**.

### Terminal full-gate backup-domain compatibility amendment

The first completed terminal `bun run check` passed every static gate and **2,872 tests with
100,560 assertions**, with 28 intentional live-only skips, but failed three deterministic tests.
The core and CLI edited-copy uninstall cases showed that the new prune backup-reclaim candidate
trusted a general remove operation's freshly observed before-image. That made an edited acquisition
copy appear self-authorized for deletion. The move-scope cleanup-fault test also stopped before its
intended post-commit barrier because its older synthetic pair omitted the canonical origin and
separate digest-domain facts required by the strengthened provenance gate.

The accepted narrow correction keeps `packages/core/src/place/swap.ts` within its existing product
ownership and adds only `packages/core/tests/reconcile/execute.test.ts` to test ownership. Portable
prune may authorize its independently signed canonical before-image; acquisition uninstall has no
portable source and may reclaim only against the ledger's preexisting legacy digest. The move test
now seeds portable operation content independently from legacy ledger content plus exact canonical
origin/SHA/revision facts. The three red selectors then pass, as do the complete affected files:
core uninstall **42/261**, reconciliation execution **25/156**, CLI uninstall **7/101**,
reconciliation apply **40/181**, and swap **21/118**. The final boundary is **40 paths** and the
terminal command inventory is **28 commands**.

The implementation first closed all four executable red results at immutable product pin
`51089325f339a98d1512ba91b84f59ab9004e28c` and test pin
`3438bdbfb57ea1d860279713db14157f9421e3c5`. Independent adversarial review then reopened the
boundary after finding that apply had crossed the portable canonical digest into the legacy
ledger hash domain and that the saved artifact-pair identity did not distinguish different
machine-bound manifest/lock pairs. Focused red regressions reproduced both defects before the
correction.

The initial corrected product pin is `5fedbba305862f127c430081888356c4fcd46c49`; its expanded test
pin is `9b96f3c3391ba69db49929a4e81ea665dee3c55f`. The terminal backup-domain correction advances
the final reviewed code/test pin to `63e425a17d5ac414f7c436a1de28270ce69b2642`:

- every `--locked` invocation validates a current, complete, canonical whole pair before filter
  projection;
- saving acquisition hashes a fresh exact-SHA materialization, while managed and Git-dev export
  independently prove exact object bytes, modes, symlinks, and a stable revision before emitting
  the portable canonical digest;
- reconciliation compares canonical lock/live/store content while retaining the legacy ledger
  only as placement provenance, including under-lock reobservation and move/prune validation;
- apply validates the portable canonical digest independently while preserving the legacy
  ledger `contentHash` computed by the existing placement snapshot authority;
- legacy-manifest migration and lock replacement share one stable artifact-pair identity that
  binds both machine-local artifact locations, while distinct pairs receive distinct group and
  operation IDs; apply resumes only the exact unique forward pending placement intent through the
  existing recovery engine; and
- the final four test-only fixture paths transport local Git rewrites without public network
  access or production behavior changes. The Machine-B fixture also proves that managed export
  remains eligible immediately after both a fresh and reused-store apply.

No saved-plan or artifact schema, operation kind, lock/ledger wire byte, transaction engine,
recovery record, filesystem adapter, credential path, or second writer/recovery authority was
introduced. The final owned boundary is exactly **40 paths**.

**Minimal-implementation result:** PASS.

## Targeted and impacted green

All four primary selectors pass independently at final reviewed pin
`63e425a17d5ac414f7c436a1de28270ce69b2642`: TS05 contributes **4 tests and 44
assertions**, TS06 **2 and 19**, WF03 **2 and 12**, and WF04 **3 and 18**. The combined primary
matrix is therefore **11 tests and 93 assertions**, with zero failures or skips. It covers strict
whole-pair refusal, Machine-A to clean-Machine-B reproduction, darwin/linux runtime-port parity,
explicit and sibling restore, poisoned no-save export refusal, every manifest/lock/ledger/live
barrier, exact same-operation resume, idempotence, cleanup, nonmatching journal refusal, and
post-apply managed-export eligibility for fresh and reused Machine-B stores.

The final serial full export contract replay passes all **9 tests with 168 assertions**. The other
signed dependency selectors passed independently: init TS01-TS05 contribute **23 tests and 301
assertions**; plan TS04/TS09 contribute **3 and 37**; and P4A TS03/TS04 contribute **5 and 79**.
Across the exact 18 dependency replays this is **40 tests and 585 assertions**, with zero failures
or skips. The earlier local-transport failure in export TS01 and P4A TS03 is closed by the isolated
fixture config; the cancellation child in export TS07 receives the same hermetic mapping and
passes with its exact failure/effect assertions.

The focused product layer passes **205 tests with 1,179 assertions** across acquisition,
portable-export, Git ports, reconciliation, saved projection, and placement swap. The hermetic Git
fixture helper passes **8 tests with 50 assertions**. Smoke passes **5 tests with 3,735
assertions**. TypeScript, changed-file Biome, full source/test architecture boundaries, diff
hygiene, secret scanning, no-skip/no-todo, no-public-network-literal scans, and `check:p17` pass.

The three amended terminal-compatibility selectors pass independently: P4A TS02 contributes
**27 tests and 314 assertions**, P4B TS01 **2 and 153**, and P4B TS04 **1 and 28**. Together they
contribute **30 tests and 495 assertions** with zero failures or skips.

The required complete durability selector also passes **19 tests with 49,356 assertions**. It
executes the full topology, collision, external-writer, fault-injection, permission, cancellation,
SIGKILL, overlap, reader, and repeatable recovery matrices beneath TS05.

One deliberately concurrent local replay overlapped the full export contract process with the
four primary files and made WF04 receive `export-lock-contention`; the exact isolated WF04 command
immediately passed **3/3 with 18 assertions**, and the full export file passed **9/9 with 168**.
Terminal CLI integration receipts are therefore serialized to respect their process-wide artifact
lock rather than misclassify orchestration contention as a product regression.

**Targeted-green result:** PASS. **Impacted-green result:** PASS.

### Green execution receipt EWP-P4B-TS05

- Validation ID: `EWP-P4B-TS05`
- Command: `bun test tests/ergonomics/phase/EWP-P4B-TS05.test.ts --test-name-pattern EWP-P4B-TS05`
- Exit status: `0`
- Result: 4 tests passed with 44 assertions; 0 failed and 0 skipped.
- Revision: `63e425a17d5ac414f7c436a1de28270ce69b2642`

### Green execution receipt EWP-P4B-TS06

- Validation ID: `EWP-P4B-TS06`
- Command: `bun test tests/ergonomics/phase/EWP-P4B-TS06.test.ts --test-name-pattern EWP-P4B-TS06`
- Exit status: `0`
- Result: 2 tests passed with 19 assertions; 0 failed and 0 skipped.
- Revision: `63e425a17d5ac414f7c436a1de28270ce69b2642`

### Green execution receipt EWP-WF03

- Validation ID: `EWP-WF03`
- Command: `bun test tests/ergonomics/workflows/EWP-WF03.test.ts --test-name-pattern EWP-WF03`
- Exit status: `0`
- Result: 2 tests passed with 12 assertions; 0 failed and 0 skipped.
- Revision: `63e425a17d5ac414f7c436a1de28270ce69b2642`

### Green execution receipt EWP-WF04

- Validation ID: `EWP-WF04`
- Command: `bun test tests/ergonomics/workflows/EWP-WF04.test.ts --test-name-pattern EWP-WF04`
- Exit status: `0`
- Result: 3 tests passed with 18 assertions; 0 failed and 0 skipped.
- Revision: `63e425a17d5ac414f7c436a1de28270ce69b2642`

## Refactor

The stable green implementation received a bounded consolidation pass:

- canonical portable projection is centralized in `export/portable-content.ts`, while acquisition
  and export retain separate exact-revision authorities appropriate to their inputs;
- portable content remains an explicit optional observation/intent fact and is routed through the
  existing writer metadata port instead of creating a second artifact writer;
- reconciliation content and ledger provenance checks use one separation across planning,
  under-lock execution, move, and prune paths;
- saved manifest/lock migration uses one stable artifact-pair target and the existing dependency
  and recovery machinery;
- the crash child reuses the production journal/recovery protocol, closes stdin, clears timeout
  timers, and exposes only named fixture barriers; and
- the hermetic Git helper owns dynamic-config scrubbing plus the one explicit isolated-config
  option, avoiding post-helper environment mutation in callers.

No temporary diagnostic, duplicate canonical hasher, second coordinator, second recovery state
machine, speculative public abstraction, or relaxed assertion remains. Full TypeScript, Biome, and
architecture-boundary checks pass after consolidation.

**Refactor result:** PASS.

## Adversarial review

The first independent read-only review returned **NO-GO (0.99 confidence)** at revision
`3438bdbfb57ea1d860279713db14157f9421e3c5`. It found two high-severity product defects—the
portable/legacy digest-domain crossing and machine-pair operation-ID collision described above—and
two medium traceability/compatibility defects: the catalog still declared 30 rather than 36 owned
paths, and three terminal fixtures had stale assumptions. All four findings were accepted. The two
product findings received focused failing regressions before their corrections; the catalog was
reconciled to the final 39-path/25-command boundary; and the three terminal selectors now pass
with their original signed purposes intact.

Corrected independent re-review of revision
`9b96f3c3391ba69db49929a4e81ea665dee3c55f` returned **GO (0.99 confidence)** with no critical,
high, or medium findings. The reviewer independently reproduced fresh and reused-store Machine-B
apply, confirmed separate legacy and portable digest domains, confirmed managed export succeeds
after both paths, checked cross-root portable plan byte identity, proved migration-to-lock-only ID
stability and distinct machine-pair IDs, reconciled the exact 39-path/25-command boundary, and
replayed the primary, terminal-compatibility, focused-core, hermetic-helper, catalog, and hygiene
checks. No schema, writer, operation vocabulary, journal, coordinator, recovery-authority, secret,
credential, public-network, or filesystem-security issue remained.

That GO applied to the 39-path/25-command pin. The later completed terminal gate reopened review
after exposing the backup-domain regression and one stale compatibility fixture. Corrected
independent re-review of final pin `63e425a17d5ac414f7c436a1de28270ce69b2642` returned **GO
(0.99 confidence)** with zero critical, high, or medium findings. The reviewer independently
confirmed acquisition edited-copy preservation, unedited managed-copy reclamation, portable prune
authorization with legacy fallback, distinct portable/legacy move provenance, the intended
post-commit cleanup boundary, all five affected-file matrices, and exact 40-path/28-command/22-
required-now arithmetic. No schema, wire, operation-vocabulary, recovery-authority, credential,
secret, or public-network change was introduced.

**Adversarial-review result:** PASS. P17-G4B-03 advances to `reviewed` with 8/10 gates;
traceability closure and sign-off remain pending the required terminal platform receipts.

## Supported-platform receipt reopening and approved harness amendment

The first external receipt attempt ran GitHub Actions workflow
[`29807928942`](https://github.com/smorinlabs/skillsmith/actions/runs/29807928942) at governance
revision `b0d06e1f01aa273bf68e099a4e82f6f6811c8736`. Both the
[Ubuntu job](https://github.com/smorinlabs/skillsmith/actions/runs/29807928942/job/88562280233)
and the
[macOS job](https://github.com/smorinlabs/skillsmith/actions/runs/29807928942/job/88562280256)
reached WF03's pinned install and returned exit 4 because clean runners do not provide a host
`codex` executable. TS06 and WF04 exposed the same missing-tool dependency. This is a deterministic
fixture defect rather than a product-contract failure, but it is still a failed required
validation, so G4B-03 reopens at `targeted-green`; impacted green, refactor, adversarial review,
traceability closure, and sign-off return to pending.

The approved correction adds `.github/workflows/ci.yml` to the owned boundary and provisions a
fixture-local, version-only `codex` stub in the shared G4B-03 fixture. WF03 uses the same helper
directly. The existing platform matrix gains a named `P17 WF03 platform receipt` step before its
unchanged full suite so the exact selector has stable, linkable receipts on macOS and Ubuntu. No
product code, public behavior, network policy, assertion, selector, or required command changes;
the existing full-suite step remains mandatory and is neither skipped nor marked
`continue-on-error`. The boundary is now **41 paths** and the terminal command inventory remains
**28 commands**.

### Corrected execution receipt EWP-P4B-TS06

- Validation ID: `EWP-P4B-TS06`
- Command: `bun test tests/ergonomics/phase/EWP-P4B-TS06.test.ts --test-name-pattern EWP-P4B-TS06`
- Exit status: `0`
- Result: PASS — 2 tests and 19 assertions with fixture-owned Codex detection.
- Revision: `6d178941dc074a96a0f9bac6c79edb74b17a3020`

### Corrected execution receipt EWP-WF03

- Validation ID: `EWP-WF03`
- Command: `bun test tests/ergonomics/workflows/EWP-WF03.test.ts --test-name-pattern EWP-WF03`
- Exit status: `0`
- Result: PASS — 2 tests and 12 assertions with fixture-owned Codex detection.
- Revision: `6d178941dc074a96a0f9bac6c79edb74b17a3020`

### Corrected execution receipt EWP-WF04

- Validation ID: `EWP-WF04`
- Command: `bun test tests/ergonomics/workflows/EWP-WF04.test.ts --test-name-pattern EWP-WF04`
- Exit status: `0`
- Result: PASS — 3 tests and 18 assertions with fixture-owned Codex detection.
- Revision: `6d178941dc074a96a0f9bac6c79edb74b17a3020`

## Amended targeted and impacted green

The corrected working tree passes all four exact G4B-03 primaries independently: TS05 contributes
4 tests and 44 assertions, TS06 2 and 19, WF03 2 and 12, and WF04 3 and 18. The amended primary
matrix is therefore **11 tests and 93 assertions**. The change touches only the G4B-03 fixture,
WF03 harness, CI receipt step, and governance records; no product or accepted contract path
changed. Targeted and impacted green re-pass while refactor, independent review, traceability, and
sign-off remain pending.

The other 18 required-now validations retain their exact green receipts at the final product pin.
None of their executable targets imports the amended `p4b-lock-policy` fixture, and this correction
changes no product path; their signed dependency purposes and passing catalog states therefore
remain continuous across the test-harness-only amendment. Together with the four freshly replayed
primaries, all 22 required-now validations are green.

A second exact WF03 run passes **2 tests with 12 assertions** when the parent PATH contains only
`bun` and `git`, deliberately excluding the host `/usr/bin/codex`. The successful pinned install
therefore depends on the fixture-owned detector binary rather than local workstation state.

## Amended refactor

The correction centralizes the only new behavior in one shared fixture helper. That helper creates
one executable under the fixture root, accepts only `codex --version`, prepends only its own bin
directory, and preserves the existing isolated environment. WF03 opts into the same helper used by
the cross-machine/export cases. The workflow adds one named selector step and leaves the existing
full-suite command and failure semantics intact.

Full Biome, TypeScript, Actions lint, architecture boundaries, deterministic P17 catalog
validation, and the required five-test `EWP-P2-TS04` smoke selector all pass. The smoke selector
contributes 3,735 assertions. `git diff --check` is clean, and no duplicate detector, product
branch, relaxed assertion, skip, or temporary diagnostic remains.

**Amended refactor result:** PASS.

## Amended independent adversarial review

Fresh read-only reviewer `/root/g4b03_platform_receipt_review` returned **GO (0.99 confidence)**
with no critical, high, or medium findings. The reviewer independently matched the original
missing-Codex failure on both CI platforms, traced the exact `codex --version` detector contract,
confirmed the stub fails every other invocation, verified its shared TS06/WF04 and direct WF03
coverage, and confirmed the named step precedes the unchanged mandatory full suite without failure
masking. The reviewer also reconciled the exact 41-path/28-command/22-required-now boundary and
found no product, network, secret, schema, or public-contract change.

`P17-RV-G4B-03-F03` was one low-severity traceability note: the amended impacted-green paragraph
listed only the four fresh primary replays without explicitly explaining the other 18
required-now receipts. The finding is accepted; the paragraph now records why those unaffected
exact dependency receipts remain continuous. At review time, fresh macOS and Ubuntu named-step
receipts were the only external residual risk and still blocked traceability closure and sign-off.

The reviewer's narrow closure check confirms F03 closed, the review record accurate, and the GO
applicable to the final current diff without new findings.

**Amended adversarial-review result:** PASS. P17-G4B-03 returns to `reviewed` with 8/10 gates;
traceability closure and sign-off remain pending fresh macOS and Ubuntu named-step receipts.

## Traceability and terminal verification

The catalog and generated checklist record the exact 41 owned paths, 22 required-now validations,
six terminal-compatibility commands, 28 exact commands, six primary validation entities, and no
deferred downstream coverage. At final reviewed pin
`63e425a17d5ac414f7c436a1de28270ce69b2642`, `bun run check` passes Biome, architecture ESLint,
TypeScript, Actions lint, deterministic P17 catalog validation, and the complete test suite:
**2,875 passed, 28 intentional live-only skips, 0 failed, and 100,573 assertions across 2,903
tests** in 901.33 seconds. The Ready contract also requires exact WF03 receipts on macOS and Ubuntu
at the current corrected code/test pin; the named-step receipts below satisfy that terminal
requirement.

The immutable amended code/test pin is `6d178941dc074a96a0f9bac6c79edb74b17a3020`. Its exact tree
also passes the complete repository gate with
`bun run check -- --timeout 60000 --max-concurrency 1`: Biome, architecture ESLint, TypeScript,
Actions lint, deterministic P17 package validation, and all **2,903 tests** execute serially with
**2,875 passed, 28 intentional live-only skips, 0 failed, and 100,573 assertions** in 948.47
seconds. Serialization changes no selector or assertion; it prevents the shared-state and fixed-
timeout noise observed when a first full run overlapped independent review activity.

### macOS WF03 receipt

- Workflow run: [`29830436714`](https://github.com/smorinlabs/skillsmith/actions/runs/29830436714)
- Job: [`build-test (macos-latest)`](https://github.com/smorinlabs/skillsmith/actions/runs/29830436714/job/88633603360)
- Head revision: `e63cd63464209d37e209173c4f28e83da027cd0a`
- Step: `P17 WF03 platform receipt` (number 7)
- Command: `bun test tests/ergonomics/workflows/EWP-WF03.test.ts --test-name-pattern EWP-WF03`
- Started: `2026-07-21T12:31:54Z`
- Completed: `2026-07-21T12:31:58Z`
- Conclusion: `success`

### Ubuntu WF03 receipt

- Workflow run: [`29830436714`](https://github.com/smorinlabs/skillsmith/actions/runs/29830436714)
- Job: [`build-test (ubuntu-latest)`](https://github.com/smorinlabs/skillsmith/actions/runs/29830436714/job/88633603424)
- Head revision: `e63cd63464209d37e209173c4f28e83da027cd0a`
- Step: `P17 WF03 platform receipt` (number 7)
- Command: `bun test tests/ergonomics/workflows/EWP-WF03.test.ts --test-name-pattern EWP-WF03`
- Started: `2026-07-21T12:32:09Z`
- Completed: `2026-07-21T12:32:14Z`
- Conclusion: `success`

Both receipts execute the unchanged exact WF03 selector on the repository's supported-platform
matrix. The legacy full-suite step follows them unchanged and remains subject to normal failure
semantics; its state is not used to manufacture either receipt. All 41 owned paths, 28 exact
commands, 22 required-now validations, accepted review findings, immutable pins, local terminal
gate, and both platform receipts now resolve.

**Traceability-closure result:** PASS.

## Sign-off

At code/test pin `6d178941dc074a96a0f9bac6c79edb74b17a3020` and platform receipt head
`e63cd63464209d37e209173c4f28e83da027cd0a`, all ten lifecycle gates pass. D-015,
EWP-P4B-T05, EWP-P4B-TS05, EWP-P4B-TS06, EWP-WF03, and EWP-WF04 advance to `signed-off`.
The group owns exactly 41 paths and retains exactly 22 required-now validations, 28 commands, and
zero deferred downstream validations. Independent review is GO at 0.99 confidence with F03
closed; the complete local gate and both supported-platform WF03 receipts pass without a product,
schema, network, secret, or public-contract change.

**Sign-off result:** PASS. P17-G4B-03 advances to `signed-off` with 10/10 gates.

## Phase-4 CI reopening and full-matrix amendment

Whole-Phase-4 reviewer `/root/phase4_whole_review` opened blocking high finding
`P17-RV-P4-F02` after inspecting the completed macOS job in run
[`29830436714`](https://github.com/smorinlabs/skillsmith/actions/runs/29830436714). The named WF03
step remained green, but its following mandatory `Test: bun test` step completed with **2,498
passed, 28 intentional live-only skips, and 377 failed**. Under the canonical P17 lifecycle, that
failed CI job invalidates the later traceability/sign-off claim and reopens G4B-03 at impacted
validation. The existing macOS and Ubuntu WF03 step receipts remain truthful selector evidence;
they do not convert the enclosing failed job into a passing phase boundary.

The log identifies four harness-level causes rather than a new product defect: a depth-one checkout
cannot inspect historical signed-receipt commits; macOS exposes both `/var` and its
`/private/var` realpath in temporary-path assertions; clean runners have no host `claude` or
`codex` for legacy CLI fixtures; and parallel execution lets fixed-name durability fixtures share
state. The approved bounded correction stays in the already-owned `.github/workflows/ci.yml`:
checkout fetches full history, and the mandatory full-suite step uses a runner-owned symlink-free
`TMPDIR`, an isolated copy of the existing executable search path that excludes host Claude/Codex
entries, strict version-only Claude/Codex stubs scoped to that step, and
`bun test --timeout 60000 --max-concurrency 1`.

The complete test inventory remains mandatory with ordinary failure semantics. No selector is
filtered, assertion relaxed, failure masked, retry introduced, product path changed, live-only
gate enabled, network dependency added, or secret consumed. The first local reproduction exposed
one timing race in the terminal `EWP-P2-TS04` durability selector: a spawned holder's 2,800 ms
window could expire after process-launch delay but before the contender exhausted its 2,300 ms
retry schedule. The bounded amendment adds that exact test path, increases only the fixture hold
to 6,200 ms, and preserves the same contention assertion and production retry policy. The owned
boundary becomes **42 paths**, with 22 required-now validations and 28 exact commands. A clean local reproduction,
fresh independent correction review, completed green macOS and Ubuntu jobs, traceability renewal,
and sign-off renewal are required before G4B-03 or Phase 4 may close again.

### Corrected local full-matrix receipt

The first isolated local diagnostic retained the host PATH after prepending the stubs and exposed
six Doctor failures from duplicate real/stub tool observations plus the spawned-lock timing race.
It truthfully completed with 2,868 passed, 28 intentional live-only skips, 7 failed, and 100,523
assertions. The superseding isolated-path and fixture-hold corrections close those exact failures:

- the full Doctor contract passes **63/63 tests and 388 assertions**;
- complete `EWP-P2-TS04` passes **19/19 tests and 49,356 assertions** in 589.98 seconds; and
- the complete repository suite passes **2,875 tests**, retains **28 intentional live-only
  skips**, has **0 failures**, and executes **100,573 assertions across 2,903 tests in 298 files**
  in 919.98 seconds.

The final full command is exactly the workflow's mandatory
`bun test --timeout 60000 --max-concurrency 1` under the runner-owned symlink-free `TMPDIR` and
isolated executable path. Actionlint, full Biome, TypeScript, boundary lint, deterministic
catalog/checklist validation, P17 package validation, and diff hygiene also pass.

Fresh reviewer `/root/phase4_correction_review` returned **GO at 0.99 confidence** for the updated
correction design, independently accepting the 42-path ownership, full-history checkout,
symlink-free temp root, isolated executable path, strict version-only stubs, unfiltered serial
suite, ordinary failure semantics, and 6,200 ms fixture-only hold against the 2,300 ms production
retry schedule. `P17-RV-P4-F02` remains open only for completed green macOS and Ubuntu jobs.
Impacted-green, refactor, and adversarial review re-pass; G4B-03 returns to `reviewed` with 8/10
gates while traceability closure and sign-off remain pending those external receipts.

**Corrected local-matrix result:** PASS.

## Supported-platform full-suite portability correction

The first external run of the full-matrix hermeticity amendment was workflow
[`29837189948`](https://github.com/smorinlabs/skillsmith/actions/runs/29837189948) at head
`9dc97212efbdfa9afd76985560fedc5ff556c3ab`. Both exact named WF03 steps remained green:
Ubuntu step 7 ran from `2026-07-21T14:02:41Z` through `14:02:45Z`, and macOS step 7 ran from
`14:02:36Z` through `14:02:42Z`. The following mandatory full suites then produced bounded,
actionable failures rather than a signable receipt:

- [Ubuntu job](https://github.com/smorinlabs/skillsmith/actions/runs/29837189948/job/88656451322):
  **2,873 passed, 28 intentional live-only skips, 2 failed, and 71,451 assertions** across all
  2,903 tests. Both failures were the EWP-P2-TS04 crash cases observing a central lock directory
  at `0755` rather than the required `0700`.
- [macOS job](https://github.com/smorinlabs/skillsmith/actions/runs/29837189948/job/88656451358):
  **2,871 passed, 28 intentional live-only skips, 4 failed, and 71,443 assertions** across all
  2,903 tests. It reproduced the same two lock-mode failures, plus the config fixture's hard-coded
  symlinked `/tmp` root and the cancellation fixture's two-second cold-start bound.

The lock-mode failures identify a real portability defect in the existing filesystem adapter:
`proper-lockfile` requests its directory without an explicit mode, so a normal `0022` runner umask
made a coordination primitive publicly traversable. The correction injects the standard Node
filesystem surface with only `mkdir` narrowed to `0700`, and masks initial exclusive stage-file
creation to owner read/write bits. The already-signed coordinator boundary continues to request
and record the exact desired final mode, then applies its existing explicit chmod. The macOS
fixtures now use the platform temporary-directory authority and a ten-second bounded marker wait
without changing their anti-symlink or exit-130 assertions.

Final local verification deliberately used `umask 0022`, matching the permissive condition that
exposed the defect:

- complete `EWP-P2-TS04`: **19/19 tests, 49,356 assertions, zero failures** in 404.95 seconds;
- complete node-coordinator, config-save, and CLI verify files: **41/41 tests, 218 assertions,
  zero failures**;
- the two formerly failing crash selectors alone: **2/2 tests and 29,169 assertions**;
- the complete repository suite under the isolated tool path, symlink-free temporary root, serial
  execution, and `umask 0022`: **2,875 passed, 28 intentional live-only skips, 0 failed, and
  100,573 assertions across all 2,903 tests in 298 files** in 1,031.83 seconds;
- full TypeScript, changed-file Biome, architecture-boundary lint, and diff hygiene: PASS.

The accepted ownership amendment adds exactly the three edited production/test paths and three
focused compatibility commands. G4B-03 now owns **45 paths**, retains the same **22 required-now
validations**, and records **31 exact commands**: 22 required selectors plus nine terminal
compatibility commands. It adds no second adapter, writer, recovery authority, schema, retry,
platform branch, skip, filter, or failure mask. Impacted green, refactor, and adversarial review
remain reopened only until a fresh independent correction review and completed green Ubuntu and
macOS jobs apply to the new correction head; traceability closure and sign-off remain pending.

**Supported-platform portability correction result:** PASS locally; external receipts pending.

### Independent portability correction review

Fresh read-only reviewer `/root/g4b03_portability_review` initially returned **NO-GO at 0.99
confidence** with one medium traceability finding and no implementation finding.
`P17-RV-G4B-03-F04` observed that the catalog correctly declared 31 commands, while the plan's
Exact commands block omitted the three already-required terminal compatibility selectors
`EWP-P4A-TS02`, `EWP-P4B-TS01`, and `EWP-P4B-TS04`. The finding was accepted and the exact three
catalog commands were added without a behavioral edit.

The same reviewer independently verified private atomic lock creation, heartbeat and stale-lock
behavior, release, initial private staging followed by exact final-mode chmod, macOS temporary-root
selection, unchanged cancellation assertions, absence of failure masking or a second authority,
the failed-run diagnostics, and the permissive-umask local receipts. Its closure check found the
extracted plan and catalog command sets identical at **31/31**, reran catalog/package/diff checks,
and returned final **GO at 0.99 confidence**. `P17-RV-G4B-03-F04` is **CLOSED** with no new
critical, high, medium, or low finding.

**Independent portability-review result:** PASS. G4B-03 returns to `reviewed` with 8/10 gates;
complete green Ubuntu and macOS jobs remain mandatory for traceability closure and sign-off.

## macOS crash-fixture and package-check compatibility amendment

Workflow [`29842632662`](https://github.com/smorinlabs/skillsmith/actions/runs/29842632662) at
reviewed correction head `0e729602559f858a4b32af9a5e8550c36f928217` kept the named macOS
WF03 receipt green and advanced beyond the previous lock-mode, `/tmp`, and cancellation failures.
Its full macOS suite then completed with **2,872 passed, 28 intentional live-only skips, 3 failed,
and 71,932 assertions** across all 2,903 tests in 1,200.75 seconds.

Two crash cases differed only in one transaction-directory metadata value: exact transaction-subtree entries,
owner bytes, mode `0700`, inode identity, artifact/recovery bytes, categories, and file hard-link
groups matched, while APFS reported directory `nlink` 3 instead of Linux 2. The package-output
compatibility case retained a running child at its 30-second test bound and timed out at 30.01
seconds while executing four serial package checks. Neither failure changes a product contract,
but both remain required full-suite failures and therefore keep F02 and group closure open.

The accepted test-only amendment canonicalizes only the non-portable owned/collision transaction
directory link-count field after asserting it is at least two; recursive entries and all security-
relevant identities remain exact. It also expands only the four-process fixture's test allowance
to 55 seconds, below the workflow's 60-second per-test bound, without changing any expected output
or atomicity assertion. The already-owned durability file and one newly owned package-output test
bring G4B-03 to **46 paths** and **32 commands**. Local re-execution, fresh independent review, and
new complete supported-platform jobs are required before review or receipt closure can return.

The exact two crash selectors pass locally with **2/2 tests, 29,510 assertions, and zero failures**
in 131.32 seconds. The complete package-output compatibility file passes **3/3 tests and 25
assertions**, including the unchanged four-result v1/v2/default/v1 atomicity sequence. Catalog and
generated-checklist validation also pass at the 46-path/32-command amended boundary.

The same Ubuntu job completed its full test step successfully, then failed the mandatory
Actions-lint step because the runner downloaded `actionlint` 1.7.12. Its bundled ShellCheck newly
reported SC2016 for the two intentionally literal `$#`/`$1` strings that become the generated
Claude and Codex stub scripts. The workflow now scopes `# shellcheck disable=SC2016` to each of
those two `printf` commands and explains the literal-variable requirement. A freshly downloaded
`actionlint` 1.7.12 binary passes the complete workflow set after that change. No broader lint
rule, workflow step, test, or failure condition is disabled.

Fresh reviewer `/root/g4b03_macos_fixture_review` opened medium finding
`P17-RV-G4B-03-F05`: the first normalization draft serialized only transaction roots, regular
files, and hard-link groups, so an unexpected empty child directory could disappear when its
effect on the root `nlink` was normalized; symbolic links were also inspected only for secrets.
The accepted correction adds nested transaction directories and symbolic-link target records to
the existing exact snapshot serialization and adds negative controls proving either extra entry
changes the serialized value. The cross-platform canonical root field remains bounded by the
`nlink >= 2` assertion. F05 remains open pending independent closure re-review and a green full
durability selector.

### Independent macOS compatibility amendment review

The corrected-tree complete `EWP-P2-TS04` durability selector passes **19/19 tests, 49,699
assertions, and zero failures** in 313.37 seconds under `umask 0022`. This includes every physical
and structured SIGKILL gap, every capability-permission phase, collision and overlap discovery,
cancellation, and both snapshot-strength negative controls. No committed residue fixture state
changed.

Fresh read-only reviewer `/root/g4b03_macos_fixture_review` independently verified the APFS
directory semantics, exact nested-directory and symbolic-link serialization, the two negative
controls, the bounded 55-second package-test allowance, unchanged package assertions, the scoped
SC2016 annotations, the failed-run diagnostics, and the 46-path/32-command catalog boundary. Its
independent replays passed the focused snapshot-strength test with 1,713 assertions and the full
package-output file with 3/3 tests and 25 assertions. Biome, TypeScript, boundary lint,
`actionlint` 1.7.12, catalog/package validation, and diff hygiene also passed.

`P17-RV-G4B-03-F05` is **CLOSED**. The reviewer returned **GO at 0.99 confidence** with no open
critical, high, medium, or low finding. Impacted-green, refactor, and adversarial review pass;
G4B-03 returns to `reviewed` with 8/10 gates. Fresh complete green macOS and Ubuntu jobs remain
mandatory for traceability closure and sign-off.

**Independent macOS-compatibility review result:** PASS.

### Corrected-tree complete local gate

The exact reviewed tree passes `bun run check -- --timeout 60000 --max-concurrency 1` under the
workflow-equivalent isolated executable path, symlink-free temporary root, serial execution, and
`umask 0022`. Biome checks all 739 files, architecture ESLint, TypeScript, Actions lint, and the
deterministic P17 package check pass before the complete test inventory executes. The test phase
finishes with **2,875 passed, 28 intentional live-only skips, 0 failed, and 100,916 assertions
across all 2,903 tests in 298 files** in 1,369.02 seconds.

**Corrected-tree complete local result:** PASS.

## Ubuntu release-heartbeat race reopening

Workflow [`29848122122`](https://github.com/smorinlabs/skillsmith/actions/runs/29848122122)
at reviewed head `a0c1cf47f5dc910e14c7d4ed7bf83db15f10bdba` produced one complete green
supported-platform job and one bounded failure:

- [macOS job](https://github.com/smorinlabs/skillsmith/actions/runs/29848122122/job/88693823600)
  passed all **2,875 tests**, retained **28 intentional live-only skips**, had **0 failures**, and
  executed **100,916 assertions across 2,903 tests in 298 files** in 1,076.66 seconds. Its named
  WF03, `actionlint` 1.7.12, boundaries, build, and smoke steps all passed.
- [Ubuntu job](https://github.com/smorinlabs/skillsmith/actions/runs/29848122122/job/88693823558)
  completed with **2,874 passed, 28 intentional live-only skips, 1 failed, and 100,916
  assertions**. The sole failure was the eight-way EWP-P2-TS04 permission matrix receiving
  asynchronous `ECOMPROMISED`/`ENOENT` from a heartbeat `stat` of its just-released
  `global.lock`; later job steps were consequently skipped.

Dependency inspection identifies an exact release race: `proper-lockfile` 4.1.2 checks its
released flag after the heartbeat `utimes` callback but not after the preceding `stat` callback.
On the slower Ubuntu run, release removed the directory while that `stat` was in flight, and the
dependency's default compromise callback threw after the protected operation had completed.

The accepted correction adds the existing node-coordinator unit file to ownership, supplies a
release-scoped compromise callback, and adds a deterministic delayed-stat regression. Pre-release
compromise remains fail-fast; after release begins no protected mutation remains and the physical
release promise remains authoritative, while the dependency's benign post-release callback is
ignored. G4B-03 owns **47
paths**, retains **32 commands**, and reopens at 7/10 gates pending local replay, independent
review, and fresh complete green jobs.

The deterministic test-first regression delayed the heartbeat `stat` until physical release
removed the lock directory and failed before implementation with **0 passed, 1 failed**, exact
`ECOMPROMISED`/`ENOENT`, in 2.30 seconds. After the scoped callback guard, that exact selector
passes **1/1 with 2 assertions**, and the complete node-coordinator unit file passes **13/13 with
37 assertions**. The existing release/marker-fault cases remain green and continue to prove that
physical release errors are surfaced.

The exact 1,030-row permission-classification selector that failed on Ubuntu passes on the
corrected tree with **1/1 test, 12,782 assertions, and zero failures** in 113.47 seconds under
`umask 0022`.

### Independent release-heartbeat correction review

The corrected complete `EWP-P2-TS04` durability selector passes **19/19 tests, 49,699 assertions,
and zero failures** in 469.10 seconds under `umask 0022`, including the formerly failing permission
matrix, all physical and structured SIGKILL gaps, collision/overlap recovery, and cancellation.

Fresh read-only reviewer `/root/g4b03_release_race_review` independently inspected
`proper-lockfile` 4.1.2, the CI failure, the adapter control flow, deterministic seam, red/green
receipts, and governance. It verified that `releaseStarted` changes only after operation and marker
cleanup, immediately before physical release; pre-release compromise retains the fail-fast throw,
no protected mutation follows the flag, and non-`ENOENT` physical release failures remain mapped
through the unchanged release-promise catch. The production filesystem object is unchanged when
test hooks are absent. No stale/update constant, retry, rank, ordering, precedence, or error map
changed.

The reviewer independently replayed the focused regression (**1/1, 2 assertions**) and complete
node-coordinator file (**13/13, 37 assertions**), verified static/governance gates, confirmed the
exact **47-path/32-command** boundary, and returned **GO at 0.99 confidence** with no open finding
or safety/traceability gap. Impacted-green, refactor, and adversarial review pass; G4B-03 returns
to `reviewed` with 8/10 gates. Fresh complete green macOS and Ubuntu jobs remain mandatory for
traceability closure and sign-off.

**Independent release-heartbeat review result:** PASS.

## Ledger-history fixture runtime reopening

The complete local gate for the independently reviewed release-heartbeat correction executed all
2,904 tests and kept the new coordinator regression plus the complete EWP-P2-TS04 file green, but
finished with **2,875 passed, 28 intentional live-only skips, 1 failed, 1 post-timeout unhandled
error, and 100,916 assertions**.
The sole failure was
`recomputes and removes one exact victim at a time until history converges`: Bun timed it out after
the 15-second test-only allowance and reported it at 17.28 seconds. The still-running body then
continued into after-test cleanup and emitted the recorded `invalid-state` error after its fixture
path had been removed; that secondary error is part of the same timeout event, not a second test
failure.

That case passed the previous local gate in 10.38 seconds, Ubuntu in 8.50 seconds, and macOS in
4.82 seconds. The accepted correction adds only the existing ledger-writer test file to ownership,
raises only this case's allowance to 30 seconds, and adds its exact selector command. The 258-row
fixture, two exact victim removals, 256-row result, absent-backup assertions, production behavior,
and failure semantics remain unchanged. G4B-03 owns **48 paths**, records **33 commands**, and
reopens at 7/10 pending targeted/full replay and fresh independent review.

The exact amended selector passes **1/1 with 2 assertions** in 3.86 seconds. The complete
ledger-writer file passes **15/15 with 64 assertions** in 31.33 seconds; its other 15-second bounds
remain unchanged.

### Independent ledger-history runtime review

Fresh read-only reviewer `/root/g4b03_history_timeout_review` initially opened medium traceability
finding `P17-G4B03-LHT-01`: the implementation was exact, but the first plan/evidence wording
incorrectly described post-timeout work as completed and omitted Bun's secondary cleanup-induced
`invalid-state` error. The finding was accepted; the record now distinguishes the 15-second
timeout, its 17.28-second report, and the one post-timeout unhandled error.

The reviewer verified that only the intended case changes from 15 to 30 seconds, all neighboring
timeouts and assertions remain exact, production semantics are untouched, and the prior
release-heartbeat guard remains safe. Its independent replays passed the exact selector (**1/1,
2 assertions**), complete ledger-writer file (**15/15, 64 assertions**), and node-coordinator file
(**13/13, 37 assertions**). Static/governance checks pass and plan/catalog sets match at exactly
**48 paths and 33 commands**.

`P17-G4B03-LHT-01` is **CLOSED**. The reviewer returned final **GO at 0.99 confidence** with no
open finding. Impacted-green, refactor, and adversarial review pass; G4B-03 returns to `reviewed`
with 8/10 gates pending fresh complete green supported-platform jobs.

**Independent ledger-history runtime review result:** PASS.

## SIGKILL central-lock staleness fixture reopening

The next complete workflow-equivalent local gate kept the deterministic release-heartbeat
regression, the amended ledger-history case, all EWP-P2 durability cases, and every other test
green until `EWP-CMD-INSTALL-TS08`. It finished with **2,875 passed, 28 intentional live-only
skips, 1 failed, and 100,914 assertions across 2,904 tests in 298 files** in 1,116.13 seconds. The
sole failure expected the opposite operation after a process crash to return pending-journal
refusal exit 2, but intermittently received generic exit 1.

The exact selector first passed alone and then passed five repeated runs with **95 assertions**.
A bounded diagnostic repetition reproduced the failure at run 11. An opt-in redacted sanitizer
diagnostic then reproduced it again and identified the underlying value as artifact-mutation
`lock-contention`, not the corrected release-heartbeat race. The SIGKILL fixture waited for the
child to exit and removed its orphaned 30-second compatibility ledger lock, but it did not wait for
the independent account-stable central coordinator lock. That lock uses a two-second stale window,
a one-second heartbeat/mtime-precision interval, and a 2.3-second retry schedule, leaving a narrow
interval in which the final attempt can still observe the killed lock as current.

The accepted test-only correction adds `packages/cli/tests/contracts/install.test.ts` to
ownership and adds its exact `EWP-CMD-INSTALL-TS08` selector command. Only the confirmed-SIGKILL
branch waits for the configured central stale window plus one heartbeat precision interval before
starting recovery. The real next command must still reclaim the orphaned central lock and prove
the unchanged opposite-operation refusal, same-operation resume, journal cleanup, and final live
symlink. Production stale/update/retry behavior and every semantic assertion remain unchanged.
G4B-03 now owns **49 paths** and records **34 commands**.

### Corrected SIGKILL-staleness fixture validation

After removing every temporary diagnostic, the exact amended selector passed ten consecutive
repetitions with **10/10 tests, 190 assertions, and zero failures** in 75.70 seconds. The complete
install contract file then passed **8/8 tests with 84 assertions** in 18.73 seconds. Focused Biome
and architecture ESLint, complete TypeScript, and P17 package/governance validation also pass.

Targeted-green, impacted-green, and refactor remain passed for this test-only correction. G4B-03
is active at 7/10 gates pending fresh independent adversarial review and a corrected complete
terminal gate.

### Corrected complete terminal gate after SIGKILL fixture amendment

The exact corrected tree passes `bun run check -- --timeout 60000 --max-concurrency 1` under the
workflow-equivalent filtered executable path, fixture-owned Claude/Codex version stubs,
symlink-free temporary root, serial execution, and `umask 0022`. Biome checked all 739 files;
architecture ESLint, TypeScript, Actions lint, and the deterministic P17 package check passed
before the complete test inventory executed.

The test phase finished with **2,876 passed, 28 intentional live-only skips, 0 failed, and 100,918
assertions across all 2,904 tests in 298 files** in 1,292.50 seconds. The exact amended
`EWP-CMD-INSTALL-TS08` case passed inside that gate in 18.20 seconds. The ledger-history case
passed in 4.70 seconds, the 1,030-row permission matrix passed in 87.97 seconds, and the complete
EWP-P2 crash/durability block remained green.

**Corrected complete terminal result:** PASS.

### Independent SIGKILL-staleness fixture review

Fresh read-only reviewer `/root/g4b03_sigkill_stale_review` independently audited the red full-gate
receipt, redacted lock-contention reproduction, `proper-lockfile` timing, fixture correction,
prior two amendments, diff hygiene, and 49-path/34-command governance. It opened medium governance
finding `P17-G4B03-SCS-01` when it observed the checklist before the concurrent regeneration had
completed. The generated checklist was regenerated immediately; the reviewer independently
re-ran both its direct check and `bun run check:p17`, then marked the finding **CLOSED**.

The reviewer confirmed retry attempts occur at cumulative 0/100/300/700/1500/2300ms, while the
strict 2,000ms stale comparison and dependency mtime rounding can defer effective staleness to
approximately 3,005ms. The bounded 3,250ms post-exit wait therefore retains about 245ms of margin
after the killed process can no longer heartbeat. The fixture removes only the legacy
compatibility lock; the real recovery command still reclaims the central lock and proves all
original journal/refusal/resume/live assertions. No product constant, retry, mapping, failure
mask, assertion, skip, or filter changed.

Independent replays passed the exact selector (**1/1, 19 assertions**), five additional
repetitions (**5/5, 95 assertions**), complete install contract file (**8/8, 84 assertions**),
node-coordinator file (**13/13, 37 assertions**), and exact ledger-history case (**1/1, 2
assertions**). Biome, architecture ESLint, TypeScript, catalog/checklist validation, and P17
package validation passed. The reviewer returned **GO at 0.99 confidence** with no open finding.
G4B-03 returns to `reviewed` with 8/10 gates pending fresh complete supported-platform CI receipts.

**Independent SIGKILL-staleness review result:** PASS.
