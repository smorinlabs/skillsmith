# P17-G6-02A evidence

## Mapping and prerequisites

Mapping began from clean governance revision
`73e129eb2a8dbaf39d4734e5c70c9f954cdac33b` on 2026-07-26. The starting catalog SHA-256 was
`eaeb1189848a80903b9f72b0a45391b9928ee6cfe78e33e592a53494e01c13a9`.

- Phase 6 is active with its formal entry gate passed.
- Direct prerequisites P17-G5-04 and P17-G5-05 are signed off at all ten lifecycle gates.
- G6-02A starts pristine and planned: no owned files, test commands, implementers, reviewer,
  evidence, executable targets, or passed gates.
- Primary ownership is exactly 17 entities: `COMMAND:help`, EWP-CF-013, EWP-CF-032,
  EWP-CMD-HELP-TS01..07, EWP-OPT-TS05, EWP-OPT-TS07, EWP-P6-T04, EWP-P6-TS04,
  EWP-P6-TS05, EWP-WF16, and P2-05.
- Required-now is exactly 15 validations: HELP-TS01..07, OPT-TS05/07, P1-TS05, P6-TS04/05,
  WF05, WF15, and WF16.
- Downstream coverage is exactly four validations owned by G6-02B: COMPLETION-TS01/03/05 and WF01.
- The mapped plan proposes 30 unique owned paths and leaves completion, broad rendering/dependency
  cleanup, packaging, publication, and release evidence outside the group.
- Nine newly proposed paths are present only as mapping comments so the catalog can validate exact
  regular-file ownership. They export nothing, register no tests, generate no docs, and change no
  runtime behavior; their planned selectors and implementations remain absent.

## Existing implementation and gap audit

The live registry contains 28 declarative parser nodes: one root, 23 top-level public commands, and
four nested `config` actions. It already closes command names, aliases, arguments, options,
questions, descriptions, examples, exits, application services, and generic runtime attachment.
Current help is partially generated but remains flat; it does not own the five-group index,
workflow metadata, minimal invocations, progressive option families, current complete topics,
near-miss suggestions, README orientation, or a generated current command reference.

`research/commands/README.md` is a historical current/draft inventory governed by documentation
drift annotations, not the future generated authority. The candidate therefore creates
`docs/commands.md` and a delimited generated README region rather than mutating historical command
pages into an accidental second registry.

Commander is 12.1.0 in both `packages/cli/package.json` and `bun.lock`. The approved pre-entry
amendment selected Commander 15.x in G6-02A before G6-02B completion. The isolated spike identified
one bounded source compatibility change: explicit parsed defaults must also be registered for
negated options. It found no command/alias/option growth, no Bun runtime incompatibility, and no
native binary-size increase from Commander alone.

## Signed dependency and current-surface characterization

The following serialized baseline ran on the clean mapping revision before any lifecycle or product
edit:

```text
bun test packages/cli/tests/help.test.ts packages/cli/tests/help/topics.test.ts \
  packages/cli/tests/contracts/options.test.ts \
  tests/ergonomics/phase/EWP-P1-TS05.test.ts \
  tests/ergonomics/workflows/EWP-WF05.test.ts \
  tests/ergonomics/workflows/EWP-WF15.test.ts --max-concurrency 1

74 pass; 0 fail; 1,447 assertions; 6 files; 23.84 seconds
```

This establishes a clean before-state for current help routing/topics, parser inventory/defaults/
relations, active documentation drift, the local development workflow, and shared non-mutating
runtime/error policy. It is characterization evidence, not a G6-02A green or sign-off claim.

## Mapping result

The mapped candidate is one coherent shared-metadata change with a prerequisite parser upgrade:

- Commander 15 compatibility is isolated first and must preserve the complete public grammar;
- the existing declarative registry gains exact questions, minimal invocations, workflows, and
  option-family metadata rather than adding a second help registry;
- pure renderers consume the same metadata for root help, command help, topics, README, and
  `docs/commands.md`;
- generated outputs are byte-checked and a fixture command proves registry extensibility;
- hermetic tests route every minimal invocation and marked runnable workflow without touching the
  real fleet; and
- G6-02B receives stable metadata later but retains sole ownership of completion generation.

No implementation, selector target, red/green receipt, Ready acceptance, adversarial-review GO, or
later lifecycle gate is claimed in this mapping record.

## Ready review at a89674d

Root Codex reviewed immutable mapped commit
`a89674d492a24ef9d4b9993414388085aace65e2` without changing product behavior. Structural review
confirmed exact 30-path plan/catalog ownership, 17 primary entities, 15 required-now validations,
four downstream validations, 19 total impacted validations, signed direct dependencies, and a clean
separation between Commander compatibility, shared help/docs, downstream completion, rendering
cleanup, packaging, and release work.

The review returned **NO-GO** on one stable plan finding:

- **F01 — canonical topic spelling:** the consolidated plan explicitly calls for dedicated
  `source` and `scope` topics at Sections 3.2.1/8.19 and EWP-CMD-HELP-TS03. The mapped plan instead
  treated today's plural `sources` and `scopes` as canonical and interpreted the normative wording
  as merely descriptive. That would make generated docs and the final test target disagree with
  the canonical plan.

The bounded correction is documentation-only and changes no ownership: make `source` and `scope`
the canonical topic metadata/rows, retain shipped `sources` and `scopes` only as compatibility
aliases, and prove aliases neither become command aliases nor duplicate generated index rows.

No Commander-coverage blocker was found. The catalog's immutable impacted set is preserved, while
the plan explicitly requires the full existing options/help/eager-global/completion characterization
as additional unclaimed regression coverage around the dependency upgrade.

## Corrected Ready review at eb90c00

Root Codex reviewed immutable correction commit
`eb90c00311f04f829f14fb760c42190cfb9bf563` with a clean worktree and returned **GO** for Ready.

- F01 is closed: canonical topic rows are `source` and `scope`; shipped plural spellings remain
  compatibility aliases only, with explicit no-duplicate-index coverage.
- Plan and catalog contain the same exact 30 owned paths.
- The group retains exactly 17 primary entities, 15 required-now validations, four downstream
  validations, and 19 total impacted validations.
- G5-04 and G5-05 remain signed off at all ten gates.
- Commander compatibility is an attributable first slice and the full existing parser/options
  suite is mandatory additional regression coverage even though the immutable required-now set is
  unchanged.
- Mapping placeholders remain comment-only and cannot pass executable-selector validation.
- Completion generation/adapter attachment, rendering cleanup, packaging, and publication remain
  outside the boundary.

Exact review verification reported:

```text
bun scripts/p17-catalog.ts --check
valid: 426 entities (419 required, 7 deferred; 244 validation obligations), 45 groups

bun test scripts/p17-catalog.test.ts \
  tests/ergonomics/phase/EWP-P0A-TS09.test.ts \
  packages/cli/tests/help.test.ts packages/cli/tests/help/topics.test.ts \
  packages/cli/tests/contracts/options.test.ts \
  tests/ergonomics/phase/EWP-P1-TS05.test.ts \
  tests/ergonomics/workflows/EWP-WF05.test.ts \
  tests/ergonomics/workflows/EWP-WF15.test.ts --max-concurrency 1
179 pass; 0 fail; 1,856 assertions; 8 files; 33.14 seconds

bun run check:p17
PR-openable; G6-02A mapped at 1/10 gates before Ready acceptance
```

## Accepted Ready result

The corrected Ready GO is accepted for immutable commit
`eb90c00311f04f829f14fb760c42190cfb9bf563`. P17-G6-02A advances from `mapped` to `ready`; only
its mapped and Ready gates pass. EWP-P6-T04 advances to `ready`; the four coverage entities remain
`mapped`; all 12 primary validation entities remain `planned` without targets. The first authorized
implementation work is the bounded Commander 15 compatibility slice. Test-first, implementation,
green, refactor, adversarial review, traceability, and sign-off remain pending.

## Pre-implementation lifecycle audit at 78ef17b

Before changing Commander or runtime source, root Codex found one ordering defect in the accepted
plan:

- **F02 — test-first lifecycle order:** the validation/lifecycle list placed the Commander
  dependency update and explicit-default production correction before activation of all 12 primary
  selector owners. Although the compatibility failure was already characterized by an existing
  test, applying production source before the group's complete primary test-first boundary would
  make the coarse G6-02A lifecycle record ambiguous.

The bounded correction changes no behavior, ownership, entities, tests, or feature order. All 12
primary selectors become executable first and record either the expected implementation-free red
or a truthful already-green characterization. Commander remains the first production change after
the test-first gate. Ready requires a corrected-plan review before implementation begins.

## Corrected lifecycle Ready review at 67854ce

Root Codex reviewed immutable correction commit
`67854ce1655ad8aa5b38c1694cd2997ae2301d5a` with a clean worktree and returned **GO** for renewed
Ready acceptance.

- F02 is closed: all 12 primary selectors are activated and evidenced before dependency, lockfile,
  or runtime source changes.
- Commander remains the first production implementation slice and retains its attributable
  dependency-red and focused-green receipts.
- The correction changes no product file, owned-path manifest, entity set, validation partition,
  topic decision, public surface, or exclusion.
- Catalog check and PR-openable validation remain green at 426 entities, 45 groups, and 244
  validation obligations.

## Renewed Ready acceptance

Ready is renewed against `67854ce1655ad8aa5b38c1694cd2997ae2301d5a`. G6-02A remains at exactly
two passed gates. The next authorized change is test-only: replace the 12 selector placeholders,
execute every required primary command, and record the truthful test-first boundary. No dependency
or production source may change until that gate passes.

## Test-first evidence

All 12 primary selector placeholders were replaced on the Commander-12/product baseline at
`a8a995b15b78db953bdf587732561e13f7ff6837`. Only owned test/fixture paths changed. No dependency,
lockfile, runtime source, metadata source, renderer, topic source, README, generated document,
architecture document, or application service changed.

All 12 selectors fail on intended missing G6-02A behavior. HELP-TS07 first proves the existing
command/global/package version agreement and then fails because generated reference metadata is
absent. OPT-TS07 contains a passing explicit-negated-default Commander-12 characterization beside
its failing option-family assertion; after the dependency update, that specific assertion is
expected to expose the known Commander-15 compatibility red before the production correction.

TypeScript accepted all selector/fixture sources before execution:

```text
bun run typecheck
exit 0
```

### Red execution receipt EWP-CMD-HELP-TS01

- Validation ID: `EWP-CMD-HELP-TS01`
- Command: `bun test packages/cli/tests/contracts/help.test.ts --test-name-pattern EWP-CMD-HELP-TS01`
- Exit status: `1`
- Result: 0 passed, 1 failed because the workflows topic is missing.
- Revision: `working-tree`

```text
bun test packages/cli/tests/contracts/help.test.ts --test-name-pattern EWP-CMD-HELP-TS01
0 pass; 1 fail; workflows topic missing
```

### Red execution receipt EWP-CMD-HELP-TS02

- Validation ID: `EWP-CMD-HELP-TS02`
- Command: `bun test packages/cli/tests/contracts/help.test.ts --test-name-pattern EWP-CMD-HELP-TS02`
- Exit status: `1`
- Result: 0 passed, 1 failed because flat help has only 2 of at least 7 progressive sections.
- Revision: `working-tree`

```text
bun test packages/cli/tests/contracts/help.test.ts --test-name-pattern EWP-CMD-HELP-TS02
0 pass; 1 fail; flat help has 2 of at least 7 progressive sections
```

### Red execution receipt EWP-CMD-HELP-TS03

- Validation ID: `EWP-CMD-HELP-TS03`
- Command: `bun test packages/cli/tests/contracts/help.test.ts --test-name-pattern EWP-CMD-HELP-TS03`
- Exit status: `1`
- Result: 0 passed, 1 failed because the six-topic plural inventory differs from the canonical topics.
- Revision: `working-tree`

```text
bun test packages/cli/tests/contracts/help.test.ts --test-name-pattern EWP-CMD-HELP-TS03
0 pass; 1 fail; current six-topic plural inventory differs from nine canonical topics
```

### Red execution receipt EWP-CMD-HELP-TS04

- Validation ID: `EWP-CMD-HELP-TS04`
- Command: `bun test packages/cli/tests/contracts/help.test.ts --test-name-pattern EWP-CMD-HELP-TS04`
- Exit status: `1`
- Result: 0 passed, 1 failed because the unknown help name has no safe install suggestion.
- Revision: `working-tree`

```text
bun test packages/cli/tests/contracts/help.test.ts --test-name-pattern EWP-CMD-HELP-TS04
0 pass; 1 fail; unknown help name has no safe install suggestion
```

### Red execution receipt EWP-CMD-HELP-TS05

- Validation ID: `EWP-CMD-HELP-TS05`
- Command: `bun test packages/cli/tests/contracts/help.test.ts --test-name-pattern EWP-CMD-HELP-TS05`
- Exit status: `1`
- Result: 0 passed, 1 failed because public specs have no minimal invocations or workflows.
- Revision: `working-tree`

```text
bun test packages/cli/tests/contracts/help.test.ts --test-name-pattern EWP-CMD-HELP-TS05
0 pass; 1 fail; public specs have no minimal invocations or workflows
```

### Red execution receipt EWP-CMD-HELP-TS06

- Validation ID: `EWP-CMD-HELP-TS06`
- Command: `bun test packages/cli/tests/contracts/help.test.ts --test-name-pattern EWP-CMD-HELP-TS06`
- Exit status: `1`
- Result: 0 passed, 1 failed because the active reference remains a mapping placeholder.
- Revision: `working-tree`

```text
bun test packages/cli/tests/contracts/help.test.ts --test-name-pattern EWP-CMD-HELP-TS06
0 pass; 1 fail; active reference is still the explicit mapping placeholder
```

### Red execution receipt EWP-CMD-HELP-TS07

- Validation ID: `EWP-CMD-HELP-TS07`
- Command: `bun test packages/cli/tests/contracts/help.test.ts --test-name-pattern EWP-CMD-HELP-TS07`
- Exit status: `1`
- Result: 0 passed, 1 failed after 5 green version assertions because generated version metadata is absent.
- Revision: `working-tree`

```text
bun test packages/cli/tests/contracts/help.test.ts --test-name-pattern EWP-CMD-HELP-TS07
0 pass; 1 fail after 5 green version assertions; generated version metadata absent
```

### Red execution receipt EWP-OPT-TS05

- Validation ID: `EWP-OPT-TS05`
- Command: `bun test packages/cli/tests/contracts/options.test.ts --test-name-pattern EWP-OPT-TS05`
- Exit status: `1`
- Result: 0 passed, 1 failed because shared specs have no workflow/reference renderer metadata.
- Revision: `working-tree`

```text
bun test packages/cli/tests/contracts/options.test.ts --test-name-pattern EWP-OPT-TS05
0 pass; 1 fail; shared specs have no minimal/workflow/reference renderer metadata
```

### Red execution receipt EWP-OPT-TS07

- Validation ID: `EWP-OPT-TS07`
- Command: `bun test packages/cli/tests/contracts/options.test.ts --test-name-pattern EWP-OPT-TS07`
- Exit status: `1`
- Result: 1 passed and 1 failed; explicit defaults pass while help families are absent.
- Revision: `working-tree`

```text
bun test packages/cli/tests/contracts/options.test.ts --test-name-pattern EWP-OPT-TS07
1 pass; 1 fail; explicit negated defaults pass; option help families are absent
```

### Red execution receipt EWP-P6-TS04

- Validation ID: `EWP-P6-TS04`
- Command: `bun test tests/ergonomics/phase/EWP-P6-TS04.test.ts --test-name-pattern EWP-P6-TS04`
- Exit status: `1`
- Result: 0 passed, 1 failed because documentation generator exports are absent.
- Revision: `working-tree`

```text
bun test tests/ergonomics/phase/EWP-P6-TS04.test.ts --test-name-pattern EWP-P6-TS04
0 pass; 1 fail; documentation generator exports are absent
```

### Red execution receipt EWP-P6-TS05

- Validation ID: `EWP-P6-TS05`
- Command: `bun test tests/ergonomics/phase/EWP-P6-TS05.test.ts --test-name-pattern EWP-P6-TS05`
- Exit status: `1`
- Result: 0 passed, 1 failed because questions differ and minimal invocations are absent.
- Revision: `working-tree`

```text
bun test tests/ergonomics/phase/EWP-P6-TS05.test.ts --test-name-pattern EWP-P6-TS05
0 pass; 1 fail; current questions differ and minimal invocations are absent
```

### Red execution receipt EWP-WF16

- Validation ID: `EWP-WF16`
- Command: `bun test tests/ergonomics/workflows/EWP-WF16.test.ts --test-name-pattern EWP-WF16`
- Exit status: `1`
- Result: 0 passed, 1 failed because hermetic help workflows safely exits 2 while absent.
- Revision: `working-tree`

```text
bun test tests/ergonomics/workflows/EWP-WF16.test.ts --test-name-pattern EWP-WF16
0 pass; 1 fail; hermetic `help workflows` safely exits 2 because the topic is absent
```

The test-first result is accepted: EWP-P6-T04 becomes `red`; all 12 primary validations become
`failing`; G6-02A becomes `active`; and only the test-first gate joins mapped/Ready as passed. No
minimal implementation or green gate is claimed.
