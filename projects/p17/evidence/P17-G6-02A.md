# P17-G6-02A evidence

## Mapping and prerequisites

Mapping began from clean governance revision
`73e129eb2a8dbaf39d4734e5c70c9f954cdac33b` on 2026-07-26. The starting catalog SHA-256 was
`eaeb1189848a80903b9f72b0a45391b9928ee6cfe78e33e592a53494e01c13a9`.

- Phase 6 is active with its formal entry gate passed.
- Direct prerequisites P17-G5-04 and P17-G5-05 are signed off at all ten lifecycle gates.
- G6-02A starts pristine and planned: no owned files, test commands, implementers, reviewer,
  evidence, executable targets, or passed gates.
- Primary ownership is exactly 17 entities: `COMMAND:help`, EWP-CF-013, EWP-CF-032,
  EWP-CMD-HELP-TS01..07, EWP-OPT-TS05, EWP-OPT-TS07, EWP-P6-T04, EWP-P6-TS04,
  EWP-P6-TS05, EWP-WF16, and P2-05.
- Required-now is exactly 15 validations: HELP-TS01..07, OPT-TS05/07, P1-TS05, P6-TS04/05,
  WF05, WF15, and WF16.
- Downstream coverage is exactly four validations owned by G6-02B: COMPLETION-TS01/03/05 and WF01.
- The mapped plan proposes 30 unique owned paths and leaves completion, broad rendering/dependency
  cleanup, packaging, publication, and release evidence outside the group.
- Nine newly proposed paths are present only as mapping comments so the catalog can validate exact
  regular-file ownership. They export nothing, register no tests, generate no docs, and change no
  runtime behavior; their planned selectors and implementations remain absent.

## Existing implementation and gap audit

The live registry contains 28 declarative parser nodes: one root, 23 top-level public commands, and
four nested `config` actions. It already closes command names, aliases, arguments, options,
questions, descriptions, examples, exits, application services, and generic runtime attachment.
Current help is partially generated but remains flat; it does not own the five-group index,
workflow metadata, minimal invocations, progressive option families, current complete topics,
near-miss suggestions, README orientation, or a generated current command reference.

`research/commands/README.md` is a historical current/draft inventory governed by documentation
drift annotations, not the future generated authority. The candidate therefore creates
`docs/commands.md` and a delimited generated README region rather than mutating historical command
pages into an accidental second registry.

Commander is 12.1.0 in both `packages/cli/package.json` and `bun.lock`. The approved pre-entry
amendment selected Commander 15.x in G6-02A before G6-02B completion. The isolated spike identified
one bounded source compatibility change: explicit parsed defaults must also be registered for
negated options. It found no command/alias/option growth, no Bun runtime incompatibility, and no
native binary-size increase from Commander alone.

## Signed dependency and current-surface characterization

The following serialized baseline ran on the clean mapping revision before any lifecycle or product
edit:

```text
bun test packages/cli/tests/help.test.ts packages/cli/tests/help/topics.test.ts \
  packages/cli/tests/contracts/options.test.ts \
  tests/ergonomics/phase/EWP-P1-TS05.test.ts \
  tests/ergonomics/workflows/EWP-WF05.test.ts \
  tests/ergonomics/workflows/EWP-WF15.test.ts --max-concurrency 1

74 pass; 0 fail; 1,447 assertions; 6 files; 23.84 seconds
```

This establishes a clean before-state for current help routing/topics, parser inventory/defaults/
relations, active documentation drift, the local development workflow, and shared non-mutating
runtime/error policy. It is characterization evidence, not a G6-02A green or sign-off claim.

## Mapping result

The mapped candidate is one coherent shared-metadata change with a prerequisite parser upgrade:

- Commander 15 compatibility is isolated first and must preserve the complete public grammar;
- the existing declarative registry gains exact questions, minimal invocations, workflows, and
  option-family metadata rather than adding a second help registry;
- pure renderers consume the same metadata for root help, command help, topics, README, and
  `docs/commands.md`;
- generated outputs are byte-checked and a fixture command proves registry extensibility;
- hermetic tests route every minimal invocation and marked runnable workflow without touching the
  real fleet; and
- G6-02B receives stable metadata later but retains sole ownership of completion generation.

No implementation, selector target, red/green receipt, Ready acceptance, adversarial-review GO, or
later lifecycle gate is claimed in this mapping record.

## Ready review at a89674d

Root Codex reviewed immutable mapped commit
`a89674d492a24ef9d4b9993414388085aace65e2` without changing product behavior. Structural review
confirmed exact 30-path plan/catalog ownership, 17 primary entities, 15 required-now validations,
four downstream validations, 19 total impacted validations, signed direct dependencies, and a clean
separation between Commander compatibility, shared help/docs, downstream completion, rendering
cleanup, packaging, and release work.

The review returned **NO-GO** on one stable plan finding:

- **F01 — canonical topic spelling:** the consolidated plan explicitly calls for dedicated
  `source` and `scope` topics at Sections 3.2.1/8.19 and EWP-CMD-HELP-TS03. The mapped plan instead
  treated today's plural `sources` and `scopes` as canonical and interpreted the normative wording
  as merely descriptive. That would make generated docs and the final test target disagree with
  the canonical plan.

The bounded correction is documentation-only and changes no ownership: make `source` and `scope`
the canonical topic metadata/rows, retain shipped `sources` and `scopes` only as compatibility
aliases, and prove aliases neither become command aliases nor duplicate generated index rows.

No Commander-coverage blocker was found. The catalog's immutable impacted set is preserved, while
the plan explicitly requires the full existing options/help/eager-global/completion characterization
as additional unclaimed regression coverage around the dependency upgrade.

## Corrected Ready review at eb90c00

Root Codex reviewed immutable correction commit
`eb90c00311f04f829f14fb760c42190cfb9bf563` with a clean worktree and returned **GO** for Ready.

- F01 is closed: canonical topic rows are `source` and `scope`; shipped plural spellings remain
  compatibility aliases only, with explicit no-duplicate-index coverage.
- Plan and catalog contain the same exact 30 owned paths.
- The group retains exactly 17 primary entities, 15 required-now validations, four downstream
  validations, and 19 total impacted validations.
- G5-04 and G5-05 remain signed off at all ten gates.
- Commander compatibility is an attributable first slice and the full existing parser/options
  suite is mandatory additional regression coverage even though the immutable required-now set is
  unchanged.
- Mapping placeholders remain comment-only and cannot pass executable-selector validation.
- Completion generation/adapter attachment, rendering cleanup, packaging, and publication remain
  outside the boundary.

Exact review verification reported:

```text
bun scripts/p17-catalog.ts --check
valid: 426 entities (419 required, 7 deferred; 244 validation obligations), 45 groups

bun test scripts/p17-catalog.test.ts \
  tests/ergonomics/phase/EWP-P0A-TS09.test.ts \
  packages/cli/tests/help.test.ts packages/cli/tests/help/topics.test.ts \
  packages/cli/tests/contracts/options.test.ts \
  tests/ergonomics/phase/EWP-P1-TS05.test.ts \
  tests/ergonomics/workflows/EWP-WF05.test.ts \
  tests/ergonomics/workflows/EWP-WF15.test.ts --max-concurrency 1
179 pass; 0 fail; 1,856 assertions; 8 files; 33.14 seconds

bun run check:p17
PR-openable; G6-02A mapped at 1/10 gates before Ready acceptance
```

## Accepted Ready result

The corrected Ready GO is accepted for immutable commit
`eb90c00311f04f829f14fb760c42190cfb9bf563`. P17-G6-02A advances from `mapped` to `ready`; only
its mapped and Ready gates pass. EWP-P6-T04 advances to `ready`; the four coverage entities remain
`mapped`; all 12 primary validation entities remain `planned` without targets. The first authorized
implementation work is the bounded Commander 15 compatibility slice. Test-first, implementation,
green, refactor, adversarial review, traceability, and sign-off remain pending.

## Pre-implementation lifecycle audit at 78ef17b

Before changing Commander or runtime source, root Codex found one ordering defect in the accepted
plan:

- **F02 — test-first lifecycle order:** the validation/lifecycle list placed the Commander
  dependency update and explicit-default production correction before activation of all 12 primary
  selector owners. Although the compatibility failure was already characterized by an existing
  test, applying production source before the group's complete primary test-first boundary would
  make the coarse G6-02A lifecycle record ambiguous.

The bounded correction changes no behavior, ownership, entities, tests, or feature order. All 12
primary selectors become executable first and record either the expected implementation-free red
or a truthful already-green characterization. Commander remains the first production change after
the test-first gate. Ready requires a corrected-plan review before implementation begins.

## Corrected lifecycle Ready review at 67854ce

Root Codex reviewed immutable correction commit
`67854ce1655ad8aa5b38c1694cd2997ae2301d5a` with a clean worktree and returned **GO** for renewed
Ready acceptance.

- F02 is closed: all 12 primary selectors are activated and evidenced before dependency, lockfile,
  or runtime source changes.
- Commander remains the first production implementation slice and retains its attributable
  dependency-red and focused-green receipts.
- The correction changes no product file, owned-path manifest, entity set, validation partition,
  topic decision, public surface, or exclusion.
- Catalog check and PR-openable validation remain green at 426 entities, 45 groups, and 244
  validation obligations.

## Renewed Ready acceptance

Ready is renewed against `67854ce1655ad8aa5b38c1694cd2997ae2301d5a`. G6-02A remains at exactly
two passed gates. The next authorized change is test-only: replace the 12 selector placeholders,
execute every required primary command, and record the truthful test-first boundary. No dependency
or production source may change until that gate passes.

## Test-first evidence

All 12 primary selector placeholders were replaced on the Commander-12/product baseline at
`a8a995b15b78db953bdf587732561e13f7ff6837`. Only owned test/fixture paths changed. No dependency,
lockfile, runtime source, metadata source, renderer, topic source, README, generated document,
architecture document, or application service changed.

All 12 selectors fail on intended missing G6-02A behavior. HELP-TS07 first proves the existing
command/global/package version agreement and then fails because generated reference metadata is
absent. OPT-TS07 contains a passing explicit-negated-default Commander-12 characterization beside
its failing option-family assertion; after the dependency update, that specific assertion is
expected to expose the known Commander-15 compatibility red before the production correction.

TypeScript accepted all selector/fixture sources before execution:

```text
bun run typecheck
exit 0
```

### Red execution receipt EWP-CMD-HELP-TS01

- Validation ID: `EWP-CMD-HELP-TS01`
- Command: `bun test packages/cli/tests/contracts/help.test.ts --test-name-pattern EWP-CMD-HELP-TS01`
- Exit status: `1`
- Result: 0 passed, 1 failed because the workflows topic is missing.
- Revision: `working-tree`

```text
bun test packages/cli/tests/contracts/help.test.ts --test-name-pattern EWP-CMD-HELP-TS01
0 pass; 1 fail; workflows topic missing
```

### Red execution receipt EWP-CMD-HELP-TS02

- Validation ID: `EWP-CMD-HELP-TS02`
- Command: `bun test packages/cli/tests/contracts/help.test.ts --test-name-pattern EWP-CMD-HELP-TS02`
- Exit status: `1`
- Result: 0 passed, 1 failed because flat help has only 2 of at least 7 progressive sections.
- Revision: `working-tree`

```text
bun test packages/cli/tests/contracts/help.test.ts --test-name-pattern EWP-CMD-HELP-TS02
0 pass; 1 fail; flat help has 2 of at least 7 progressive sections
```

### Red execution receipt EWP-CMD-HELP-TS03

- Validation ID: `EWP-CMD-HELP-TS03`
- Command: `bun test packages/cli/tests/contracts/help.test.ts --test-name-pattern EWP-CMD-HELP-TS03`
- Exit status: `1`
- Result: 0 passed, 1 failed because the six-topic plural inventory differs from the canonical topics.
- Revision: `working-tree`

```text
bun test packages/cli/tests/contracts/help.test.ts --test-name-pattern EWP-CMD-HELP-TS03
0 pass; 1 fail; current six-topic plural inventory differs from nine canonical topics
```

### Red execution receipt EWP-CMD-HELP-TS04

- Validation ID: `EWP-CMD-HELP-TS04`
- Command: `bun test packages/cli/tests/contracts/help.test.ts --test-name-pattern EWP-CMD-HELP-TS04`
- Exit status: `1`
- Result: 0 passed, 1 failed because the unknown help name has no safe install suggestion.
- Revision: `working-tree`

```text
bun test packages/cli/tests/contracts/help.test.ts --test-name-pattern EWP-CMD-HELP-TS04
0 pass; 1 fail; unknown help name has no safe install suggestion
```

### Red execution receipt EWP-CMD-HELP-TS05

- Validation ID: `EWP-CMD-HELP-TS05`
- Command: `bun test packages/cli/tests/contracts/help.test.ts --test-name-pattern EWP-CMD-HELP-TS05`
- Exit status: `1`
- Result: 0 passed, 1 failed because public specs have no minimal invocations or workflows.
- Revision: `working-tree`

```text
bun test packages/cli/tests/contracts/help.test.ts --test-name-pattern EWP-CMD-HELP-TS05
0 pass; 1 fail; public specs have no minimal invocations or workflows
```

### Red execution receipt EWP-CMD-HELP-TS06

- Validation ID: `EWP-CMD-HELP-TS06`
- Command: `bun test packages/cli/tests/contracts/help.test.ts --test-name-pattern EWP-CMD-HELP-TS06`
- Exit status: `1`
- Result: 0 passed, 1 failed because the active reference remains a mapping placeholder.
- Revision: `working-tree`

```text
bun test packages/cli/tests/contracts/help.test.ts --test-name-pattern EWP-CMD-HELP-TS06
0 pass; 1 fail; active reference is still the explicit mapping placeholder
```

### Red execution receipt EWP-CMD-HELP-TS07

- Validation ID: `EWP-CMD-HELP-TS07`
- Command: `bun test packages/cli/tests/contracts/help.test.ts --test-name-pattern EWP-CMD-HELP-TS07`
- Exit status: `1`
- Result: 0 passed, 1 failed after 5 green version assertions because generated version metadata is absent.
- Revision: `working-tree`

```text
bun test packages/cli/tests/contracts/help.test.ts --test-name-pattern EWP-CMD-HELP-TS07
0 pass; 1 fail after 5 green version assertions; generated version metadata absent
```

### Red execution receipt EWP-OPT-TS05

- Validation ID: `EWP-OPT-TS05`
- Command: `bun test packages/cli/tests/contracts/options.test.ts --test-name-pattern EWP-OPT-TS05`
- Exit status: `1`
- Result: 0 passed, 1 failed because shared specs have no workflow/reference renderer metadata.
- Revision: `working-tree`

```text
bun test packages/cli/tests/contracts/options.test.ts --test-name-pattern EWP-OPT-TS05
0 pass; 1 fail; shared specs have no minimal/workflow/reference renderer metadata
```

### Red execution receipt EWP-OPT-TS07

- Validation ID: `EWP-OPT-TS07`
- Command: `bun test packages/cli/tests/contracts/options.test.ts --test-name-pattern EWP-OPT-TS07`
- Exit status: `1`
- Result: 1 passed and 1 failed; explicit defaults pass while help families are absent.
- Revision: `working-tree`

```text
bun test packages/cli/tests/contracts/options.test.ts --test-name-pattern EWP-OPT-TS07
1 pass; 1 fail; explicit negated defaults pass; option help families are absent
```

### Red execution receipt EWP-P6-TS04

- Validation ID: `EWP-P6-TS04`
- Command: `bun test tests/ergonomics/phase/EWP-P6-TS04.test.ts --test-name-pattern EWP-P6-TS04`
- Exit status: `1`
- Result: 0 passed, 1 failed because documentation generator exports are absent.
- Revision: `working-tree`

```text
bun test tests/ergonomics/phase/EWP-P6-TS04.test.ts --test-name-pattern EWP-P6-TS04
0 pass; 1 fail; documentation generator exports are absent
```

### Red execution receipt EWP-P6-TS05

- Validation ID: `EWP-P6-TS05`
- Command: `bun test tests/ergonomics/phase/EWP-P6-TS05.test.ts --test-name-pattern EWP-P6-TS05`
- Exit status: `1`
- Result: 0 passed, 1 failed because questions differ and minimal invocations are absent.
- Revision: `working-tree`

```text
bun test tests/ergonomics/phase/EWP-P6-TS05.test.ts --test-name-pattern EWP-P6-TS05
0 pass; 1 fail; current questions differ and minimal invocations are absent
```

### Red execution receipt EWP-WF16

- Validation ID: `EWP-WF16`
- Command: `bun test tests/ergonomics/workflows/EWP-WF16.test.ts --test-name-pattern EWP-WF16`
- Exit status: `1`
- Result: 0 passed, 1 failed because hermetic help workflows safely exits 2 while absent.
- Revision: `working-tree`

```text
bun test tests/ergonomics/workflows/EWP-WF16.test.ts --test-name-pattern EWP-WF16
0 pass; 1 fail; hermetic `help workflows` safely exits 2 because the topic is absent
```

The test-first result is accepted: EWP-P6-T04 becomes `red`; all 12 primary validations become
`failing`; G6-02A becomes `active`; and only the test-first gate joins mapped/Ready as passed. No
minimal implementation or green gate is claimed.

## Commander 15 dependency red and corrected compatibility green

The first production slice started from committed test-first revision
`b48a17e0d181cdbe8fd0b719816780e6dfa3b7d1`. `packages/cli/package.json` changed from Commander
`^12.1.0` to `^15.0.0`; Bun resolved exact 15.0.0. The mechanical lock rewrite also corrected stale
workspace package version entries from 0.5.0 to their already-shipped 0.7.0 package manifests.

Before runtime source changed, the exact OPT-TS07 selector produced the predicted dependency red:

```text
bun test packages/cli/tests/contracts/options.test.ts --test-name-pattern EWP-OPT-TS07
0 pass; 2 fail

failure 1: Phase-6 option help families remain absent
failure 2: pre-parse root prompt and install verify/prompt defaults are absent under Commander 15
```

The isolated-spike candidate—calling `Option.default(true)` for negated options—restored pre-parse
values, but the complete parser registry correctly rejected it. It changed Commander's declared
`Option.defaultValue` for `--no-color`/other negated options, so EWP-OPT-TS01 reported three
current-surface/migration-ledger failures. That candidate was discarded.

The corrected implementation uses only public APIs:

- non-negated defaults continue through `Option.default`;
- a negated option retains undefined declared `Option.defaultValue`; and
- immediately after `Command.addOption`, its explicit `parsedDefault` is seeded with
  `Command.setOptionValueWithSource(attributeName, value, 'default')`.

This preserves the signed Commander declaration and pre-parse semantic contract simultaneously.
Focused correction result:

```text
bun test packages/cli/tests/contracts/options.test.ts \
  --test-name-pattern 'EWP-OPT-TS01|EWP-OPT-TS02|EWP-OPT-TS07'
10 pass; 1 expected Phase-6-family fail; 122 assertions
```

Compatibility regression result:

```text
bun test packages/cli/tests/contracts/options.test.ts \
  --test-name-pattern 'EWP-OPT-TS0(1|2|3|4|6|8|9)|EWP-OPT-TS10|sync CommandSpec|update CommandSpec'
43 pass; 0 fail; 1,004 assertions

bun test packages/cli/tests/help.test.ts packages/cli/tests/help/topics.test.ts \
  packages/cli/tests/completion/bash.test.ts \
  packages/cli/tests/completion/declaration-gate.test.ts \
  packages/cli/tests/completion/fish.test.ts \
  packages/cli/tests/completion/integration.test.ts \
  packages/cli/tests/completion/walk.test.ts \
  packages/cli/tests/completion/zsh.test.ts --max-concurrency 1
37 pass; 0 fail; 172 assertions

bun run typecheck
exit 0
```

Build/runtime compatibility result:

```text
bun run test:smoke
5 pass; 0 fail; 3,737 assertions

bun build --compile --bytecode --target=bun-linux-arm64 packages/cli/src/index.ts ...
exit 0; 114,010,256 bytes; compiled binary --version => 0.7.0

bun build --compile --bytecode --target=bun-linux-x64 packages/cli/src/index.ts ...
exit 0; 114,952,320 bytes
```

Commander 15's native help-group APIs are available and become the next-slice implementation
primitive: `Command.helpGroup`, `Option.helpGroup`, group defaults, and `configureHelp`. G6-02A can
therefore retain Commander's term formatting/wrapping/choice rendering while adding only the
Skillsmith-specific question/workflow/order/docs layer. No completion adapter is attached here.

This is an intermediate compatibility green, not the group's minimal-implementation or targeted-
green gate: all 12 Phase-6 selectors remain intentionally failing on missing help/docs behavior.

## Implementation ownership amendment

The first shared-help green run exposed six necessary downstream compatibility edits outside the
30-path mapped boundary. Work paused before lifecycle advancement and the approved amendment was
applied to plan and catalog ownership:

- four command help-routing tests still asserted the retired duplicate `EXAMPLES` section; their
  behavioral checks now assert the registry-owned `COMMON WORKFLOWS` section while preserving
  alias, option, exit, and command-routing assertions; and
- the signed G0-04 documentation-drift validator and ledger still required the obsolete six-topic
  inventory and internal future-plan markers; they now close against the canonical nine current
  topics and their shipped source authorities.

The amended boundary is exactly 36 paths. No entity, public command, alias, option, application,
wire contract, mutation behavior, completion selector, or required-now validation was added. A
temporary attempt to make parser-registry array order double as help order was rejected during
refactor because it disturbed signed inventory order. The final design adds explicit `helpOrder`
metadata, preserving the historical parser/wire order while deriving canonical help and docs order.

## Minimal implementation and targeted green at 76b2354

Commit `76b2354f0a350a75b03aa3a7572c5cb8de70d5b8` freezes the shared help implementation and the
approved 36-path ownership amendment. The implementation:

- retains the exact 23-command, alias, option, application, and wire surface;
- uses Commander 15 public help-group/global-option APIs and an explicit registry `helpOrder` while
  preserving the signed parser-registry order;
- classifies every option into one stable family and common/advanced level;
- owns exact questions, minimal invocations, and two or three workflows in `CommandSpec`;
- exposes nine canonical topics plus the two plural compatibility lookups;
- suggests one safe close help name without autocorrection or execution; and
- generates the README index and `docs/commands.md` deterministically from the live registry.

All 15 required-now validation commands were executed together with serialized public-process
concurrency on the clean immutable commit:

```text
bun test packages/cli/tests/contracts/help.test.ts \
  packages/cli/tests/contracts/options.test.ts \
  tests/ergonomics/phase/EWP-P1-TS05.test.ts \
  tests/ergonomics/phase/EWP-P6-TS04.test.ts \
  tests/ergonomics/phase/EWP-P6-TS05.test.ts \
  tests/ergonomics/workflows/EWP-WF05.test.ts \
  tests/ergonomics/workflows/EWP-WF15.test.ts \
  tests/ergonomics/workflows/EWP-WF16.test.ts \
  --test-name-pattern 'EWP-CMD-HELP-TS0[1-7]|EWP-OPT-TS05|EWP-OPT-TS07|EWP-P1-TS05|EWP-P6-TS04|EWP-P6-TS05|EWP-WF05|EWP-WF15|EWP-WF16' \
  --max-concurrency 1

31 pass; 43 filtered out; 0 fail; 1,241 assertions; 8 files; 11.55 seconds
```

The 31 tests comprise the 12 primary selectors and all assertions beneath the three signed replay
selectors. Each formerly red primary selector is now passing. The minimal-implementation and
targeted-green gates pass; impacted-green remains pending until the four downstream completion
selectors and the broader regression/terminal commands are recorded independently.

### Green execution receipt EWP-CMD-HELP-TS01

- Validation ID: `EWP-CMD-HELP-TS01`
- Command: `bun test packages/cli/tests/contracts/help.test.ts --test-name-pattern EWP-CMD-HELP-TS01`
- Exit status: `0`
- Result: PASS — five ordered groups and one canonical command row are present in root/workflow help.
- Revision: `76b2354f0a350a75b03aa3a7572c5cb8de70d5b8`

### Green execution receipt EWP-CMD-HELP-TS02

- Validation ID: `EWP-CMD-HELP-TS02`
- Command: `bun test packages/cli/tests/contracts/help.test.ts --test-name-pattern EWP-CMD-HELP-TS02`
- Exit status: `0`
- Result: PASS — significant command pages retain the progressive section order and option families.
- Revision: `76b2354f0a350a75b03aa3a7572c5cb8de70d5b8`

### Green execution receipt EWP-CMD-HELP-TS03

- Validation ID: `EWP-CMD-HELP-TS03`
- Command: `bun test packages/cli/tests/contracts/help.test.ts --test-name-pattern EWP-CMD-HELP-TS03`
- Exit status: `0`
- Result: PASS — nine canonical topics and two plural compatibility lookups are current and reachable.
- Revision: `76b2354f0a350a75b03aa3a7572c5cb8de70d5b8`

### Green execution receipt EWP-CMD-HELP-TS04

- Validation ID: `EWP-CMD-HELP-TS04`
- Command: `bun test packages/cli/tests/contracts/help.test.ts --test-name-pattern EWP-CMD-HELP-TS04`
- Exit status: `0`
- Result: PASS — near misses suggest safely without mutation, autocorrection, or execution.
- Revision: `76b2354f0a350a75b03aa3a7572c5cb8de70d5b8`

### Green execution receipt EWP-CMD-HELP-TS05

- Validation ID: `EWP-CMD-HELP-TS05`
- Command: `bun test packages/cli/tests/contracts/help.test.ts --test-name-pattern EWP-CMD-HELP-TS05`
- Exit status: `0`
- Result: PASS — all 23 commands own bounded minimal invocations and two or three workflows.
- Revision: `76b2354f0a350a75b03aa3a7572c5cb8de70d5b8`

### Green execution receipt EWP-CMD-HELP-TS06

- Validation ID: `EWP-CMD-HELP-TS06`
- Command: `bun test packages/cli/tests/contracts/help.test.ts --test-name-pattern EWP-CMD-HELP-TS06`
- Exit status: `0`
- Result: PASS — active public help/docs contain no stale internal or expanded command surface.
- Revision: `76b2354f0a350a75b03aa3a7572c5cb8de70d5b8`

### Green execution receipt EWP-CMD-HELP-TS07

- Validation ID: `EWP-CMD-HELP-TS07`
- Command: `bun test packages/cli/tests/contracts/help.test.ts --test-name-pattern EWP-CMD-HELP-TS07`
- Exit status: `0`
- Result: PASS — command, flag, package, core, and generated-reference versions agree.
- Revision: `76b2354f0a350a75b03aa3a7572c5cb8de70d5b8`

### Green execution receipt EWP-OPT-TS05

- Validation ID: `EWP-OPT-TS05`
- Command: `bun test packages/cli/tests/contracts/options.test.ts --test-name-pattern EWP-OPT-TS05`
- Exit status: `0`
- Result: PASS — one registry owns parser, help, docs, and generic workflow metadata.
- Revision: `76b2354f0a350a75b03aa3a7572c5cb8de70d5b8`

### Green execution receipt EWP-OPT-TS07

- Validation ID: `EWP-OPT-TS07`
- Command: `bun test packages/cli/tests/contracts/options.test.ts --test-name-pattern EWP-OPT-TS07`
- Exit status: `0`
- Result: PASS — all options have one help family/level and Commander 15 defaults remain exact.
- Revision: `76b2354f0a350a75b03aa3a7572c5cb8de70d5b8`

### Green execution receipt EWP-P6-TS04

- Validation ID: `EWP-P6-TS04`
- Command: `bun test tests/ergonomics/phase/EWP-P6-TS04.test.ts --test-name-pattern EWP-P6-TS04`
- Exit status: `0`
- Result: PASS — generated README/reference outputs close against the live command tree.
- Revision: `76b2354f0a350a75b03aa3a7572c5cb8de70d5b8`

### Green execution receipt EWP-P6-TS05

- Validation ID: `EWP-P6-TS05`
- Command: `bun test tests/ergonomics/phase/EWP-P6-TS05.test.ts --test-name-pattern EWP-P6-TS05`
- Exit status: `0`
- Result: PASS — the exact ordered 23-row question/minimal-invocation matrix is registry-owned.
- Revision: `76b2354f0a350a75b03aa3a7572c5cb8de70d5b8`

### Green execution receipt EWP-WF16

- Validation ID: `EWP-WF16`
- Command: `bun test tests/ergonomics/workflows/EWP-WF16.test.ts --test-name-pattern EWP-WF16`
- Exit status: `0`
- Result: PASS — documentation-driven help is hermetic, current, and cleanup-safe.
- Revision: `76b2354f0a350a75b03aa3a7572c5cb8de70d5b8`

## Impacted green at 112cafa

The targeted-green governance commit `112cafa74b0cf81c0e4aab3ac442c88d174817f5` changes no product
or test source. The four downstream completion entities remain planned and unclaimed for G6-02B;
their existing implementation surfaces were characterized against the final G6-02A metadata:

```text
bun test packages/cli/tests/completion \
  packages/cli/tests/help.test.ts \
  packages/cli/tests/help/topics.test.ts --max-concurrency 1
38 pass; 0 fail; 180 assertions; 8 files; 5.89 seconds

bun test packages/cli/tests/contracts/options.test.ts \
  --test-name-pattern 'EWP-OPT-TS0(1|2|3|4|6|8|9)|EWP-OPT-TS10|sync CommandSpec|update CommandSpec'
43 pass; 3 filtered out; 0 fail; 1,004 assertions; 7.64 seconds
```

This adds current Bash/zsh/fish emission, integration, recursive walk, declaration, alias/value,
parser inventory/default/relation, and eager-global coverage to the immutable 31-test required-now
green. No completion selector, adapter, command, or output is added or signed early. The complete
frozen impacted set is green, so the impacted-green gate passes; refactor remains pending.

## Refactor at daca91f

Commit `daca91fccf1411383e7fbdcf4f4cd7b141526d6e` freezes the post-green refactor. Review of the
generated-help boundary found three incomplete parts of the mapped contract: stored workflow
explanations were not rendered, the full generated reference omitted arguments and exit codes, and
Skillsmith fixed Commander's help width instead of allowing Commander to own wrapping. The refactor:

- renders concise, command-specific workflow explanations and truthful safety labels in help and
  Markdown;
- adds every declared argument and exit meaning to each generated command section;
- removes the fixed help width while retaining Commander 15's public grouping and wrapping APIs;
- validates the unique 23 questions, closed zero-based help order, command-bounded minimal
  invocations, and complete workflow metadata; and
- strengthens `EWP-P6-TS04` from presence-only coverage to per-command generated-section closure
  across questions, minimal invocations, aliases, workflows, arguments, options, and exits.

The immutable commit passed the exact required-now and frozen impacted sets:

```text
bun test packages/cli/tests/contracts/help.test.ts \
  packages/cli/tests/contracts/options.test.ts \
  tests/ergonomics/phase/EWP-P1-TS05.test.ts \
  tests/ergonomics/phase/EWP-P6-TS04.test.ts \
  tests/ergonomics/phase/EWP-P6-TS05.test.ts \
  tests/ergonomics/workflows/EWP-WF05.test.ts \
  tests/ergonomics/workflows/EWP-WF15.test.ts \
  tests/ergonomics/workflows/EWP-WF16.test.ts \
  --test-name-pattern 'EWP-CMD-HELP-TS0[1-7]|EWP-OPT-TS05|EWP-OPT-TS07|EWP-P1-TS05|EWP-P6-TS04|EWP-P6-TS05|EWP-WF05|EWP-WF15|EWP-WF16' \
  --max-concurrency 1
31 pass; 43 filtered out; 0 fail; 1,759 assertions; 8 files; 11.22 seconds

bun test packages/cli/tests/completion \
  packages/cli/tests/help.test.ts \
  packages/cli/tests/help/topics.test.ts --max-concurrency 1
38 pass; 0 fail; 180 assertions; 8 files; 4.65 seconds

bun test packages/cli/tests/contracts/options.test.ts \
  --test-name-pattern 'EWP-OPT-TS0(1|2|3|4|6|8|9)|EWP-OPT-TS10|sync CommandSpec|update CommandSpec' \
  --max-concurrency 1
43 pass; 3 filtered out; 0 fail; 1,004 assertions; 1 file; 9.45 seconds
```

Biome, ESLint boundaries, TypeScript, generated-document drift, and the P17 pr-openable catalog
check also passed before the immutable commit. The work remains within the approved 36-path
ownership boundary, preserves parser/wire order and the 23-command surface, and claims no G6-02B
completion behavior. The refactor gate passes; adversarial review remains pending.

## Renewed refactor after independent-review corrections at dbe57bc

An independent adversarial review of correction candidate
`057f72d93452362c3333dc8aea8509e9c0a81dce` returned **NO-GO** rather than advancing the review
gate. It found four bounded gaps:

1. the lifecycle evidence and catalog still pointed only to the pre-correction refactor;
2. `additionalSpecs` retained runtime fallbacks, but extension-facing TypeScript still required the
   new progressive-help fields;
3. EWP-CMD-HELP-TS05 parsed declarations through an inert Commander tree rather than the production
   `buildProgram`/preflight/application path; and
4. Commander rendered only the first alias in root help, so the signed `rm` alias was absent from
   the `uninstall` row.

Commit `dbe57bc7b61c044b2c34f308f223949ba823eaa2` is the immutable corrected refactor candidate. It:

- preserves the historical `CommandSpec` source name and adds an extension input form whose new
  help-order, workflow, minimal-invocation, option-family, and help-level fields are optional;
- normalizes every extension declaration into a strict `NormalizedCommandSpec` before Commander,
  eager-option analysis, application routing, or action attachment consumes it;
- adds a root-typecheck-owned legacy fixture that omits every new help field and proves the
  normalized defaults, help, aliases, and runtime attachment;
- routes every materialized minimal invocation and every declared workflow through a fresh
  production `buildProgram` with real preflight, injected applications/renderers, deterministic
  operation/runtime ports, exact application assertions, exit-0 assertions, and a byte-stable
  hermetic fixture tree; and
- customizes Commander through its public `configureHelp().subcommandTerm` API so every declared
  alias is rendered, with generic exact closure plus explicit `dev|demote` and
  `uninstall|remove|rm` assertions.

The exact required-now selector command passed on the immutable candidate:

```text
bun test packages/cli/tests/contracts/help.test.ts \
  packages/cli/tests/contracts/options.test.ts \
  tests/ergonomics/phase/EWP-P1-TS05.test.ts \
  tests/ergonomics/phase/EWP-P6-TS04.test.ts \
  tests/ergonomics/phase/EWP-P6-TS05.test.ts \
  tests/ergonomics/workflows/EWP-WF05.test.ts \
  tests/ergonomics/workflows/EWP-WF15.test.ts \
  tests/ergonomics/workflows/EWP-WF16.test.ts \
  --test-name-pattern 'EWP-CMD-HELP-TS0[1-7]|EWP-OPT-TS05|EWP-OPT-TS07|EWP-P1-TS05|EWP-P6-TS04|EWP-P6-TS05|EWP-WF05|EWP-WF15|EWP-WF16' \
  --max-concurrency 1
32 pass; 43 filtered out; 0 fail; 2,131 assertions; 8 files; 14.63 seconds
```

The additional test under EWP-OPT-TS05 accounts for the change from 31 to 32 tests; the immutable
set of 15 validation IDs is unchanged. Frozen impacted and supplemental results are:

```text
bun test packages/cli/tests/completion \
  packages/cli/tests/help.test.ts \
  packages/cli/tests/help/topics.test.ts --max-concurrency 1
38 pass; 0 fail; 180 assertions; 8 files; 6.33 seconds

bun test packages/cli/tests/contracts/options.test.ts \
  --test-name-pattern 'EWP-OPT-TS0(1|2|3|4|6|8|9)|EWP-OPT-TS10|sync CommandSpec|update CommandSpec' \
  --max-concurrency 1
43 pass; 4 filtered out; 0 fail; 1,004 assertions; 1 file; 15.96 seconds

bun test tests/ergonomics/phase/EWP-P1-TS07.test.ts --max-concurrency 1
17 pass; 0 fail; 406 assertions; 1 file; 1.59 seconds

bun test tests/ergonomics/phase/EWP-P1-TS11.test.ts --max-concurrency 1
10 pass; 0 fail; 487 assertions; 1 file; 28.51 seconds

bun run test:smoke
5 pass; 14 filtered out; 0 fail; 3,737 assertions; 1 file; 12.27 seconds
```

`bun run lint`, `bun run lint:boundaries`, `bun run typecheck`, generated-reference check mode,
`bun run actions-lint`, and `bun run check:p17` all exited 0. The generated artifacts remain
byte-current. The command/alias/option/application/wire surface and the 36-path product ownership
boundary are unchanged.

This section resolves the stale-lifecycle finding and renews the refactor gate against
`dbe57bc7b61c044b2c34f308f223949ba823eaa2`. It does not accept the prior NO-GO as an adversarial
pass: reviewer identity remains null and the adversarial-review, traceability, and sign-off gates
remain pending until a fresh independent review of this immutable candidate succeeds.

## Approved terminal-suite ownership amendment

A full-suite attempt begun from the clean pre-correction candidate exposed two deterministic
failures in `tests/ergonomics/phase/EWP-P0A-TS05.test.ts` before later live edits made that process
ineligible as a terminal receipt. Both failures were expectation drift from the already-approved
G6-02A documentation authority:

- the test still hard-coded the retired six-topic inventory with plural `sources` and `scopes`
  rather than the current ordered nine-topic registry; and
- it still attempted to distinguish shipped exit codes from a future P17 taxonomy even though
  Phase 6 now treats `packages/cli/src/util/exit-codes.ts` as the live canonical authority.

The user approved adding exactly that test file as the 37th G6-02A ownership path. The bounded
amendment changes no product source, validation identity, entity ownership, command, alias, option,
application, report, exit semantics, or completion behavior. EWP-P0A-TS05 remains a signed Phase-0
selector; G6-02A owns only the compatibility correction needed to keep its assertions current.

The corrected test now expects the exact canonical order `workflows`, `manifest`, `lock`, `plan`,
`source`, `environment`, `scope`, `exit-codes`, and `formatting`, and verifies that replacing the
current exit-code authority marker with a superseded research pointer is rejected. Focused and
terminal execution receipts follow after the amendment is frozen.

## Terminal and amended refactor green at 1b6c1f4

Commit `1b6c1f4fc873e6ab45d395fd19848e869b61f823` freezes the approved 37-path amendment on top of the
corrected product candidate. The amended selector passed by itself with 15 tests and 51 assertions.
The complete required-now set plus the supplemental Phase-0 selector then passed together:

```text
bun test packages/cli/tests/contracts/help.test.ts \
  packages/cli/tests/contracts/options.test.ts \
  tests/ergonomics/phase/EWP-P0A-TS05.test.ts \
  tests/ergonomics/phase/EWP-P1-TS05.test.ts \
  tests/ergonomics/phase/EWP-P6-TS04.test.ts \
  tests/ergonomics/phase/EWP-P6-TS05.test.ts \
  tests/ergonomics/workflows/EWP-WF05.test.ts \
  tests/ergonomics/workflows/EWP-WF15.test.ts \
  tests/ergonomics/workflows/EWP-WF16.test.ts \
  --test-name-pattern 'EWP-CMD-HELP-TS0[1-7]|EWP-OPT-TS05|EWP-OPT-TS07|EWP-P0A-TS05|EWP-P1-TS05|EWP-P6-TS04|EWP-P6-TS05|EWP-WF05|EWP-WF15|EWP-WF16' \
  --max-concurrency 1
47 pass; 43 filtered out; 0 fail; 2,182 assertions; 9 files; 18.91 seconds
```

The frozen impacted and supplemental lanes remained green:

```text
completion/help impact: 38 pass; 0 fail; 180 assertions
broad option impact: 43 pass; 4 filtered out; 0 fail; 1,004 assertions
EWP-P1-TS07 plus EWP-P1-TS11: 27 pass; 0 fail; 893 assertions
smoke: 5 pass; 14 filtered out; 0 fail; 3,737 assertions
```

Biome, ESLint boundaries, TypeScript, generated-reference check mode, Actionlint, and the P17
PR-openable check all exited 0. The catalog retained 426 valid entities, 244 validation obligations,
45 groups, and the unchanged 7/10 G6-02A lifecycle position.

The first serialized full-suite attempt used Bun's five-second default timeout for tests without an
explicit bound. It completed with 3,236 passes, 28 skips, two timeout failures, and one associated
unhandled assertion error across 3,266 tests. Both failures were pre-existing elapsed-time bounds:

- EWP-P2-TS06's explicit 30-second static TypeScript ownership check took 32.60 seconds under the
  accumulated full-suite load; and
- EWP-P4B-TS06's otherwise-unbounded locked preview reached Bun's five-second default and returned
  the expected cancellation product instead of its state product.

The unchanged selectors passed immediately in isolation: EWP-P2-TS06 passed all six tests and its
formerly timed-out case completed in 15.35 seconds; EWP-P4B-TS06 passed both tests and its formerly
timed-out case completed in 0.28 seconds. No source or fixture timeout was changed.

The complete serialized suite was then rerun with a 30-second runner default, matching the existing
explicit bound for the long static selector while preserving every explicit and fixture-internal
timeout:

```text
bun test --max-concurrency 1 --timeout 30000 --dots
3,238 pass; 28 skip; 0 fail; 106,679 assertions
3,266 tests; 357 files; 1,882.05 seconds
```

Both formerly timed-out selectors passed inside that terminal run. Native compilation on the same
immutable candidate also passed without repository output residue:

```text
bun build --compile --bytecode --target=bun-linux-arm64 packages/cli/src/index.ts ...
exit 0; 114,141,328 bytes
compiled ARM64 --version => 0.7.0
compiled ARM64 --help => exact uninstall|remove|rm row present

bun build --compile --bytecode --target=bun-linux-x64 packages/cli/src/index.ts ...
exit 0; 115,038,336 bytes
```

The amended 37-path candidate therefore re-passes refactor, required, impacted, static, smoke,
native, and terminal repository gates. Adversarial review remains pending until a fresh independent
reviewer accepts this exact immutable candidate.

## Final independent adversarial review GO at 5fe0bb9

Fresh read-only reviewer `/root/g5_05_f15_final_review` reviewed immutable commit
`5fe0bb91803d306e7d48a67d34a7e10af93dbd9e` and returned **GO at 0.98 confidence** with no
blocking or non-blocking findings. The worktree and index remained clean throughout review.

The reviewer independently closed every prior NO-GO concern:

- extension-owned legacy `CommandSpec` declarations may omit progressive-help fields, while every
  extension crosses `normalizeCommandSpec()` into strict `NormalizedCommandSpec` records before
  Commander, eager parsing, action dispatch, or application routing;
- every materialized minimal and workflow case in EWP-CMD-HELP-TS05 now traverses production
  `buildProgram`, real preflight, exact injected applications and renderers, deterministic ports,
  exit-zero assertions, and a byte-stable fixture boundary;
- the public Commander `subcommandTerm` customization renders every command alias, including the
  exact native `uninstall|remove|rm` row; and
- the lifecycle evidence preserves the original NO-GO, corrections at `dbe57bc`, renewed refactor,
  approved P0A amendment, terminal evidence at `1b6c1f4`, and the exact immutable reviewed head.

Independent verification passed:

```text
amended required set:             47 pass; 0 fail; 2,182 assertions
P0A + timeout-sensitive set:      23 pass; 0 fail;   990 assertions
EWP-P1-TS07:                      17 pass; 0 fail;   406 assertions
completion/help impact:           38 pass; 0 fail;   180 assertions
broad option/parser impact:       43 pass; 0 fail; 1,004 assertions
smoke:                             5 pass; 0 fail; 3,737 assertions
typecheck, Biome, boundaries, generated docs, documentation drift,
catalog/checklist/package validation, and range whitespace: PASS
```

The reviewer confirmed exact plan/catalog equality at 37 owned paths. The implementation range
contains 36 changed owned paths plus the lifecycle-managed catalog and generated checklist; the one
remaining owned core test is unchanged. The P0A amendment changes expectations only and preserves
its signed Phase-0 ownership. G6-02B remains pristine and planned: completion source/tests,
`@bomb.sh/tab`, shell generation, PowerShell, and installer behavior remain excluded.

The adversarial-review gate therefore passes on this immutable candidate. Traceability closure and
group sign-off remain pending their own recorded audit.
