# P17-G1-02A evidence

## Mapping and prerequisites

- **Primary IDs:** `P0-06`, `P2-01`, `EWP-P1-T04`, `EWP-P1-T08`, `EWP-CF-004`,
  `EWP-CF-043`, `EWP-OPT-TS04`, `EWP-OPT-TS10`, `EWP-P1-TS06`, and `EWP-WF15`.
- **Dependencies:** none; Phase 1 entry is passed and Phase 1 is active.
- **Required now:** exactly `EWP-OPT-TS04`, `EWP-OPT-TS10`, `EWP-P1-TS06`, and the Phase-1 slice
  of `EWP-WF15`; command matrices and `EWP-WF08..10` remain downstream.
- Read-only mapper `/root/g1_02a_map` verified ownership, current implementation seams, genuine-red
  cases, downstream coverage, and the boundary with G1-01.
- **Mapping result:** exact scope, prerequisites, validation impact, and integration order mapped by
  root Codex goal on 2026-07-12.

## Ready plan

- **Plan:** `projects/p17/plans/P17-G1-02A.md`.
- Root owns integration across the shared Commander/options seams. G1-02A lands before G1-01.
- Exact red targets, implementation slices, impacted commands, and prohibited later-group work are
  recorded.
- **Result:** ready for test-first implementation; no later lifecycle gate is implied.

## Test-first evidence

- `EWP-OPT-TS04` exact selector exited 1 because the planned shared
  `packages/cli/src/util/non-mutating-mode.ts` contract did not exist.
- `EWP-OPT-TS10` exact selector exited 1 for the same absent declarative policy/validator.
- `EWP-P1-TS06` exact selector exited 1 because `selectExitCode` was not exported by the existing
  numeric-only exit utility.
- `EWP-WF15` exact selector exited 1 because the planned shared
  `packages/cli/src/output/error-boundary.ts` did not exist.
- The acceptance targets also encode hermetic spawned live-mutator conflicts, Commander-native
  `prompt:false`, state-tree invariance, semantic 0..7/130 precedence, stable cancellation, and
  sanitized human/JSON error contracts. Biome and diff checks passed before implementation.
- **Result:** all four required-now selectors are genuinely red at the group boundary; no runtime
  implementation existed when these receipts were recorded on 2026-07-12.

### Execution receipt EWP-OPT-TS04

- Validation ID: `EWP-OPT-TS04`
- Command: `bun test packages/cli/tests/contracts/options.test.ts --test-name-pattern EWP-OPT-TS04`
- Exit status: `0`
- Result: 3 passed, 0 failed, 40 assertions; current option families and preview policy passed.
- Revision: `ebcbedd72046f8f1644d5902d4a7408f236de9d6`

### Execution receipt EWP-OPT-TS10

- Validation ID: `EWP-OPT-TS10`
- Command: `bun test packages/cli/tests/contracts/options.test.ts --test-name-pattern EWP-OPT-TS10`
- Exit status: `0`
- Result: 7 passed, 0 failed, 68 assertions; pure and spawned mode matrices passed.
- Revision: `ebcbedd72046f8f1644d5902d4a7408f236de9d6`

### Execution receipt EWP-P1-TS06

- Validation ID: `EWP-P1-TS06`
- Command: `bun test tests/ergonomics/phase/EWP-P1-TS06.test.ts --test-name-pattern EWP-P1-TS06`
- Exit status: `0`
- Result: 8 passed, 0 failed, 48 assertions; spawned cross-command and adapter exits passed.
- Revision: `ebcbedd72046f8f1644d5902d4a7408f236de9d6`

### Execution receipt EWP-WF15

- Validation ID: `EWP-WF15`
- Command: `bun test tests/ergonomics/workflows/EWP-WF15.test.ts --test-name-pattern EWP-WF15`
- Exit status: `0`
- Result: 8 passed, 0 failed, 83 assertions; human/JSON, usage, config, and cancellation passed.
- Revision: `ebcbedd72046f8f1644d5902d4a7408f236de9d6`

## Implementation and validation

- Added the typed 0..7/130 exit taxonomy and centralized semantic selector: cancellation wins;
  actual errors 1..6 beat drift 7; existing deterministic precedence within 1..6 remains numeric.
  Acquire and flip batch adapters delegate to this selector, and both handled signals normalize to
  public cancellation 130 without replacing the first recorded cancellation.
- Added one hostile-input-safe error boundary for core, Commander, abort, Error, and unknown values.
  Human errors are one sanitized stderr line; JSON errors are one versioned stdout envelope with no
  stack, nested cause, raw core object, or duplicate Commander diagnostic.
- Added one declarative non-mutating policy and wired install, uninstall, dev, and promote to reject
  approval plus preview before environment construction or state access while retaining
  `--no-prompt` and meaningful preview-shaping controls.
- Migrated every current JSON-capable handler through the shared Commander/error boundary, including
  config, list, commands, check, doctor, verify, agents, and the process entrypoint. This establishes
  the shared boundary; G1-03 still owns eliminating command-local process output/exit orchestration.
- Exact required selectors passed at revision `ebcbedd72046f8f1644d5902d4a7408f236de9d6`:
  TS04 3/3 (40 assertions), TS10 7/7 (68), P1-TS06 8/8 (48), and WF15 8/8 (83).
- CLI focused suite passed 262 tests with 20 conditional live/network skips, 0 failures, and 750
  expectations. Full `bun run check` passed lint, boundaries, typecheck, actionlint, P17 package and
  catalog checks, and the complete test suite.

## Adversarial review

- Independent reviewer `/root/g1_02a_review` initially rejected revision `95d32d6` with four
  blockers: `P17-RV-G1-02A-F01` JSON usage bypass; `F02` verify invalid-path exit regression; `F03`
  remaining raw config/list/commands error paths; and `F04` helper-only rather than cross-command
  TS06 coverage.
- Correction revision `ebcbedd72046f8f1644d5902d4a7408f236de9d6` routes early and Commander
  usage errors through the format-aware boundary, restores verify invalid paths to 2, migrates all
  current JSON-capable handlers, and adds hermetic spawned current-command plus adapter-delegation
  coverage to TS06.
- The same independent reviewer replayed every original exploit, aliases and root failures, malformed
  config in human/JSON modes, and all four exact selectors. F01-F04 are closed; focused results were
  262 passed, 20 conditional skips, 0 failed, 750 expectations.
- **Gate result:** adversarial-review accepted on 2026-07-12 America/Los_Angeles.

## Traceability and sign-off

- **Implementation revision:** `ebcbedd72046f8f1644d5902d4a7408f236de9d6` (including correction
  of all independent F01-F04 findings); initial implementation revision was
  `95d32d6afbb12da47c7251be5ca72dea7229c231`.
- The four exact executable targets, primary work items, accepted findings/recommendations, owned
  files, shared-boundary call sites, and downstream command/workflow obligations resolve without
  claiming future drift-producing commands or G1-03's application-runtime migration.
- Required validations at the implementation revision: TS04 3/3, TS10 7/7, P1-TS06 8/8, WF15
  8/8; no required skip. Focused CLI and full repository gates passed with zero failures.
- Independent reviewer `/root/g1_02a_review` accepted the corrected implementation after replaying
  every original exploit and verifying no duplicate/raw output path remained.
- **Traceability result:** passed by root Codex goal on 2026-07-12 America/Los_Angeles.
- **Group sign-off:** all ten primary entities, automated gates, F01-F04 corrections, exact
  revision receipts, and fresh independent acceptance are complete. P17-G1-02A signed off by root
  Codex goal on 2026-07-12.
