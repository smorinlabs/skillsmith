# P17-G3B-01 evidence

## Mapping and prerequisites

- **Primary entities:** exactly 21 after the reviewed workflow-ownership correction:
  `COMMAND:dev`, `COMMAND:promote`, `D-009`, `EWP-CF-002`,
  `EWP-CF-025`, `EWP-P3B-T01`, `EWP-P3B-T03`, `EWP-CMD-DEV-TS01..TS06`,
  `EWP-CMD-PROMOTE-TS01..TS06`, `EWP-P3B-TS01`, and `EWP-P3B-TS02`. This is two commands, one
  decision, two findings, two phase tasks, twelve command tests, and two phase tests. All remain
  pristine `planned` at mapping; no entity target or evidence advances.
- **Required now:** exactly 15 validations: the six dev selectors, six promote selectors,
  `EWP-OPT-TS04`, `EWP-P3B-TS01`, and `EWP-P3B-TS02`. The full immutable impact set remains 32
  validations. The other 17 are downstream-only selectors: `EWP-WF05` under G5-03 plus export
  TS05, init TS03, install TS05..TS06, plan TS01..TS02/TS09, sync TS05..TS08, uninstall TS03/TS05,
  and P4B-TS01..TS03. They remain planned and must not be executed or advanced by this group.
- **Planned targets:** dev and promote each own six filtered families in
  `packages/cli/tests/contracts/dev.test.ts` and `packages/cli/tests/contracts/promote.test.ts`;
  the two phase selectors own
  `tests/ergonomics/phase/EWP-P3B-TS01.test.ts`,
  `tests/ergonomics/phase/EWP-P3B-TS02.test.ts`. Those four current-group files do not yet exist.
  `tests/ergonomics/workflows/EWP-WF05.test.ts` also remains absent and is now correctly targeted to
  G5-03 rather than serving as a partial or fake G3B-01 workflow.
  `EWP-OPT-TS04` is a signed dependency selector in
  `packages/cli/tests/contracts/options.test.ts` and must be replayed after its current mutation
  option-family assertions are extended within reviewed ownership.
- **Dependency:** sole direct dependency P17-G3A-02 is signed off at `bcd6a41`. Its final terminal
  repository gate passed 1,922 tests with 28 intentional environment-gated skips, zero failures,
  and 70,776 assertions across 1,950 tests/215 files in 324.74 seconds. Phase 3 remains active under
  its passed entry gate; no new phase transition or human-review pause is required.
- **Outcome boundary:** G3B-01 introduces the immutable executable-operation, check, diagnostic,
  bounded-conflict, and execution-result domain used by current mutation previews. It migrates the
  current install/uninstall/dev/promote planning and dry-run paths to one ordered operation model,
  while completing the mapped dev/promote scope, target-or-all, approval, compatibility routing,
  and structured human/JSON contracts. Existing action unions may remain only as compatibility
  report DTOs and must not become executable saved-plan variants.
- **Existing foundations:** G2-05 already owns the strict saved-plan v1 persistence DTO and codec,
  including operation/check/diagnostic/precondition shapes. G1-03 owns shared application services,
  G1-02A owns non-mutating-mode policy, G1-01 owns project/scope/target selection, G1-05 owns the
  tool capability registry, G1-06 owns versioned JSON codecs, and G1-07 owns OperationContext. This
  group must reuse those authorities rather than create another plan schema, option table, exit
  selector, tool switch, or runtime composition root.
- **Current gaps:** `InstallAction`, `UninstallAction`, and `FlipAction` mix executable outcomes with
  noop/skip/refuse/failure presentation. No public execution-result type correlates actual outcome
  to an immutable operation ID. `acquire/run.ts` and `place/run.ts` maintain separate dry-run
  prediction branches rather than projecting one reviewed operation set. Dev/promote lack the
  target scope/sugar and continue option surface in current Commander state, their core options do
  not carry scope or selection-source facts, `--yes` is still documented as a no-op, and no current
  contract proves operation ordering, deep immutability, diagnostic separation, or preview versus
  execution-plan identity.
- **Strict exclusions:** G3B-02 retains lock hierarchy, deterministic scheduler/coordinator, partial
  pair transactions, and new fail-fast/continue execution mechanics. G3B-03 retains the canonical
  ledger writer, visible migration execution, journals, recovery/history, and doctor repair.
  G3B-04 retains observed-state snapshots, domain repositories, pure full planners,
  expected-revision staging, and monolith extraction. G3B-05 retains moving tool-specific lifecycle
  behavior behind adapter bundles. G3B-06 retains OperationContext propagation and lifecycle events
  through transaction/recovery. G4 and later groups retain desired-state save/apply/new-command
  execution. G3B-01 may characterize compatibility and project current actions into immutable
  operations, but it must not pull any of those later authorities forward.
- **Governance correction:** the mapping audit found that the original WF05 primary assignment
  required direct `undo` before its G5-03 command owner and that original TS04/TS05 wording mixed
  successor scheduler/undo assertions into G3B-01. The catalog now assigns full WF05 to G5-03,
  records it as G3B-01 downstream coverage, and requires G5-03 to replay dev/promote TS05 alongside
  its undo command tests; together they prove compatibility alias equivalence. TS04 owns canonical planned group policy and is replayed by G3B-02 for
  actual fail-fast/continue execution; TS05 owns current rollback inverse intent and is replayed by
  G5-03 for public undo equivalence. No validation is skipped or weakened.
- **Verification policy:** iteration uses the 15 exact selectors, focused compatibility tests,
  static checks, and the bounded five-test smoke lane. The five-minute full repository suite is
  reserved for terminal G3B-01 sign-off or a direct change to its high-cost mechanics; it is not an
  iteration loop.
- The initial read-only governance audit reproduced the pre-correction 22-primary and 16/16/32
  partition. Subsequent architecture and trace reviewers identified the future-owner conflict at
  0.99+ confidence before Ready or test-first advancement. The corrected partition is 21 primary
  entities and 15 required-now/17 downstream/32 impacted validations, with four absent current-group
  target files.
- **Result:** prerequisites, entity and validation ownership, current architecture and gaps,
  successor boundary, and strict exclusions are mapped. No Ready, test-first, implementation,
  entity, validation, or sign-off gate is implied.

## Ready plan

- **Accepted plan:** `projects/p17/plans/P17-G3B-01.md`, SHA-256
  `e020af58766b1b951d2ba818db1bf25c1d4d9fae4d4ee2276ca023f4b7e9135f`.
- **Corrected scope:** 21 primary entities, 15 required-now validations, 17 downstream obligations,
  and 32 impacted validations. Full WF05 is G5-03-primary/G3B-01-downstream; G5-03 also replays dev
  and promote TS05 alongside its undo tests. G3B-02 owns TS04's actual scheduler replay.
- **Architecture/schema review:** `/root/g3b01_code_architecture` and
  `/root/g3b01_ready_schema_review` identified and drove separation from saved-plan DTOs, complete
  operation/result facts, closed sanitized execution errors and force effects, unambiguous public
  naming, the neutral semantic vocabulary boundary, strict flip@3 ownership, and gating
  `--continue-on-error` until its scheduler owner.
- **Trace/test review:** `/root/g3b01_ready_test_review` passed the corrected test partition at 0.995
  confidence: 14 absent selectors plus one substantive option-selector red after a signed green
  baseline, exact later-selector replay, focused recovery smoke, and terminal-only full suite.
- **Governance review:** `/root/g3b01_governance_correction` passed the primary-group correction and
  immutable catalog fallout at 0.98 confidence. It confirmed no EXECUTION dependency/title change
  is required and reproduced the exact 32/15/17 and G5-03 13/13/0 partitions.
- **Ownership:** integration owner `root-codex-goal`; implementer partitions
  `/root/g3b01_operation_domain`, `/root/g3b01_mutator_migration`, and
  `/root/g3b01_command_contract`; exactly 49 production/test paths are named by the plan. Any extra
  path requires a reviewed amendment before edit.
- **Commands:** exactly 15 runnable selectors: dev TS01..TS06, promote TS01..TS06, OPT TS04,
  P3B-TS01, and P3B-TS02. WF05 remains absent and future-owned.
- **Entity advance:** only phase tasks `EWP-P3B-T01` and `EWP-P3B-T03` advance from planned to ready.
  Commands, findings, decision, and every validation remain pristine planned until test-first or
  later owning gates.
- **Result:** product/schema, architecture/ownership, and trace/test boundaries are accepted. Ready
  authorizes test-first work only; it implies no implementation or green validation.

## Test-first evidence

- **Boundary:** four new selector files and the owned `EWP-OPT-TS04` extension contain tests only;
  no production implementation, successor scheduler, public undo command, saved-plan execution, or
  placeholder behavior is present. The 14 new validation entities each have one exact executable
  selector owner; the signed option entity retains its existing catalog status and target.
- **Signed dependency baseline:** before extension,
  `bun test packages/cli/tests/contracts/options.test.ts --test-name-pattern EWP-OPT-TS04`
  exited 0 with 3 passed, 0 failed, 40 assertions. After the bounded-force assertions were added,
  the same exact command exits 1 with 3 passed, 1 failed, and 41 assertions at the missing public
  `createBoundedForceEffect` constructor. The added case pins no-force, requested-but-unused,
  applicable-but-refused, and applied force effects with copied/deep-frozen conflict targets.
- **Exact red replay:** all 15 accepted commands exit 1. In aggregate they execute 3 passing
  dependency tests, 15 failing G3B-01 tests, and 127 assertions. Every failure is an assertion on
  absent G3B-01 behavior after its current behavior/fixture setup executes; there are zero skipped,
  todo, missing-module, syntax, impossible-fixture, or successor-owned failures.
- **False-green review:** three implementer partitions cross-reviewed one another before gate
  advance. Amendments replaced synthetic promote reports with real runner/rollback reports; added
  plan-before-confirm-before-execute observation; performed hermetic injected backed-up-journal
  interruption and same-operation recovery; exhaustively projected all 20 current action values; tested 64 generated
  semantic identities and 192 input permutations; closed dependency-domain/version/duplicate and
  semantic-ID mismatch cases; corrected selection/check/rollback semantics; and completed the
  bounded-force union. Final focused reviews passed at 0.97-0.98 confidence.
- **flip@3 seam clarification:** the exact top-level fields are `schemaVersion`, `kind`, `op`,
  `dryRun`, `summary`, `selection`, `operations`, `checks`, `diagnostics`, and `results`.
  `selection` contains source, outcome, targets, all, tools, scopes, group IDs, and batch policy.
  Legacy `requested`, nested `plan`, nested `executionResults`, and a duplicate top-level batch
  policy are forbidden; `results` are the separately correlated redacted execution-result DTOs.
- **Static and suite policy:** Biome on the five changed selector files, repository typecheck, and
  `git diff --check` pass. The bounded smoke lane passes 5 tests with 3,735 assertions and zero
  failures. Deterministic catalog/checklist validations are replayed after these receipts are bound.
  The five-minute full suite is intentionally reserved for terminal G3B-01 sign-off because this
  gate changes no production code.
- **Result:** G3B-01 passes `test-first` and becomes `active`; `EWP-P3B-T01` and `EWP-P3B-T03`
  become `red`; the 14 new validation entities become `failing` with the exact receipts below.
  `EWP-OPT-TS04` remains signed off under G1-02A while this group records its substantive extension
  red. No minimal implementation or green gate is claimed.

### Red execution receipt EWP-CMD-DEV-TS01

- Validation ID: `EWP-CMD-DEV-TS01`
- Command: `bun test packages/cli/tests/contracts/dev.test.ts --test-name-pattern EWP-CMD-DEV-TS01`
- Exit status: `1`
- Result: exit 1; 0 passed, 1 failed, 8 assertions; six-state compatibility passed before the missing immutable operation plan failed.
- Revision: `working-tree`

### Red execution receipt EWP-CMD-DEV-TS02

- Validation ID: `EWP-CMD-DEV-TS02`
- Command: `bun test packages/cli/tests/contracts/dev.test.ts --test-name-pattern EWP-CMD-DEV-TS02`
- Exit status: `1`
- Result: exit 1; 0 passed, 1 failed, 12 assertions; the validation matrix reached the missing scope-consistency refusal and failed as intended.
- Revision: `working-tree`

### Red execution receipt EWP-CMD-DEV-TS03

- Validation ID: `EWP-CMD-DEV-TS03`
- Command: `bun test packages/cli/tests/contracts/dev.test.ts --test-name-pattern EWP-CMD-DEV-TS03`
- Exit status: `1`
- Result: exit 1; 0 passed, 1 failed, 10 assertions; current verification behavior passed before the missing verification-check plan failed.
- Revision: `working-tree`

### Red execution receipt EWP-CMD-DEV-TS04

- Validation ID: `EWP-CMD-DEV-TS04`
- Command: `bun test packages/cli/tests/contracts/dev.test.ts --test-name-pattern EWP-CMD-DEV-TS04`
- Exit status: `1`
- Result: exit 1; 0 passed, 1 failed, 3 assertions; scoped fixture planning reached the missing immutable bulk plan and failed before approval assertions.
- Revision: `working-tree`

### Red execution receipt EWP-CMD-DEV-TS05

- Validation ID: `EWP-CMD-DEV-TS05`
- Command: `bun test packages/cli/tests/contracts/dev.test.ts --test-name-pattern EWP-CMD-DEV-TS05`
- Exit status: `1`
- Result: exit 1; 0 passed, 1 failed, 6 assertions; pre-I/O conflict behavior passed before preview/execution plan correlation failed.
- Revision: `working-tree`

### Red execution receipt EWP-CMD-DEV-TS06

- Validation ID: `EWP-CMD-DEV-TS06`
- Command: `bun test packages/cli/tests/contracts/dev.test.ts --test-name-pattern EWP-CMD-DEV-TS06`
- Exit status: `1`
- Result: exit 1; 0 passed, 1 failed, 28 assertions; injected backed-up-swap recovery succeeded and then failed on the missing flip@3 recovery contract.
- Revision: `working-tree`

### Red execution receipt EWP-CMD-PROMOTE-TS01

- Validation ID: `EWP-CMD-PROMOTE-TS01`
- Command: `bun test packages/cli/tests/contracts/promote.test.ts --test-name-pattern EWP-CMD-PROMOTE-TS01`
- Exit status: `1`
- Result: exit 1; 0 passed, 1 failed, 3 assertions; real scoped promotion reached the missing immutable operation plan and failed as intended.
- Revision: `working-tree`

### Red execution receipt EWP-CMD-PROMOTE-TS02

- Validation ID: `EWP-CMD-PROMOTE-TS02`
- Command: `bun test packages/cli/tests/contracts/promote.test.ts --test-name-pattern EWP-CMD-PROMOTE-TS02`
- Exit status: `1`
- Result: exit 1; 0 passed, 1 failed, 2 assertions; real provenance setup passed before the missing clean-preview plan failed.
- Revision: `working-tree`

### Red execution receipt EWP-CMD-PROMOTE-TS03

- Validation ID: `EWP-CMD-PROMOTE-TS03`
- Command: `bun test packages/cli/tests/contracts/promote.test.ts --test-name-pattern EWP-CMD-PROMOTE-TS03`
- Exit status: `1`
- Result: exit 1; 0 passed, 1 failed, 4 assertions; real verification behavior passed before the missing check-bearing plan failed.
- Revision: `working-tree`

### Red execution receipt EWP-CMD-PROMOTE-TS04

- Validation ID: `EWP-CMD-PROMOTE-TS04`
- Command: `bun test packages/cli/tests/contracts/promote.test.ts --test-name-pattern EWP-CMD-PROMOTE-TS04`
- Exit status: `1`
- Result: exit 1; 0 passed, 1 failed, 1 assertion; the current service executed before the required plan-confirm-execute sequence and failed as intended.
- Revision: `working-tree`

### Red execution receipt EWP-CMD-PROMOTE-TS05

- Validation ID: `EWP-CMD-PROMOTE-TS05`
- Command: `bun test packages/cli/tests/contracts/promote.test.ts --test-name-pattern EWP-CMD-PROMOTE-TS05`
- Exit status: `1`
- Result: exit 1; 0 passed, 1 failed, 5 assertions; pre-I/O conflict behavior passed before preview/execution/rollback plan identity failed.
- Revision: `working-tree`

### Red execution receipt EWP-CMD-PROMOTE-TS06

- Validation ID: `EWP-CMD-PROMOTE-TS06`
- Command: `bun test packages/cli/tests/contracts/promote.test.ts --test-name-pattern EWP-CMD-PROMOTE-TS06`
- Exit status: `1`
- Result: exit 1; 0 passed, 1 failed, 2 assertions; real store/hash fixture setup passed before the missing correlated plan failed.
- Revision: `working-tree`

### Red execution receipt EWP-P3B-TS01

- Validation ID: `EWP-P3B-TS01`
- Command: `bun test tests/ergonomics/phase/EWP-P3B-TS01.test.ts --test-name-pattern EWP-P3B-TS01`
- Exit status: `1`
- Result: exit 1; 0 passed, 1 failed, 1 assertion; the exhaustive compatibility matrix failed on the missing public createOperationPlan behavior.
- Revision: `working-tree`

### Red execution receipt EWP-P3B-TS02

- Validation ID: `EWP-P3B-TS02`
- Command: `bun test tests/ergonomics/phase/EWP-P3B-TS02.test.ts --test-name-pattern EWP-P3B-TS02`
- Exit status: `1`
- Result: exit 1; 0 passed, 1 failed, 1 assertion; deterministic ID/permutation coverage failed on the missing public createOperationPlan behavior.
- Revision: `working-tree`

## Implementation ownership amendment

- **Discovery:** making `FlipReport.plan` and `FlipReport.executionResults` required, as pinned by
  Ready and the accepted red selectors, produces direct compile fallout in two existing manual
  compatibility fixtures outside the original 49-path manifest:
  `packages/cli/tests/output/flip-human.test.ts` and
  `packages/cli/tests/util/flip-exit.test.ts`.
- **Authorization:** both test paths are added to the G3B-01 group and `EWP-P3B-T01` ownership.
  The command-contract implementer may update only their report constructors/fixtures and focused
  compatibility assertions; production renderer or exit behavior is not broadened.
- **Boundary:** this amendment adds no production path, command, option, scheduler, public undo,
  saved-plan behavior, lock/journal authority, or validation entity. The 15 required-now selector
  commands and 32/15/17 impact partition remain unchanged.
- **Result:** implementation ownership is exactly 51 paths. The required report fields may be
  integrated without weakening the public type or using unsafe optional compatibility fields.

## Recovery race ownership amendment

- **Discovery:** the real PROMOTE-TS06 interruption replay exposes an existing race in
  `packages/core/src/place/swap.ts`: the test pause registers its abort listener without first
  checking `signal.aborted`. An abort delivered immediately after the durable staged phase can be
  missed, causing the bounded test seam to sleep for 30 seconds and allowing phase expectations to
  race with later swap work.
- **Authorization:** `packages/core/src/place/swap.ts` and its existing focused regression file
  `packages/core/tests/place/swap.test.ts` are added to G3B-01 and `EWP-P3B-T03` ownership. The
  current-mutator implementer may make the pause abort-safe and add a focused pre-aborted/abort-race
  assertion.
- **Boundary:** this is a deterministic fix to the existing test/recovery seam. It adds no lock
  hierarchy, scheduler, ledger format, recovery command, journal authority, or successor behavior;
  G3B-02 and G3B-03 ownership remains unchanged.
- **Result:** implementation ownership is exactly 53 paths. PROMOTE-TS06 can exercise the existing
  interrupted-swap recovery behavior without a lost abort or a 30-second timeout.

## Dev-source compatibility ownership amendment

- **Discovery:** the command-contract focused replay passes 79 tests with 10 intentional
  environment-gated skips and isolates three failures to
  `packages/cli/tests/commands/dev-source.test.ts`. Its filesystem and ledger assertions remain
  valid, but three default JSON assertions still require flip@2 compatibility rows after G3B-01
  intentionally moves current dev/promote structured output to strict flip@3.
- **Authorization:** the existing characterization file is added to G3B-01 and `EWP-P3B-T03`
  ownership. The command-contract implementer may migrate only those JSON assertions to the pinned
  flip@3 operation/result shape while preserving its filesystem, ledger, recovery, and human-output
  behavior.
- **Boundary:** explicit flip@2 codec/fixture compatibility remains frozen and separately tested.
  This amendment adds no production path, validation entity, command, option, or successor behavior;
  the exact 15 selector commands and 32/15/17 partition remain unchanged.
- **Result:** implementation ownership is exactly 54 paths. The default current dev JSON
  characterization can follow the separately versioned flip@3 contract without weakening flip@2.

## Minimal implementation

- Candidate commit `a6b6bea4c1f151f99364f30a08fe7725b492bcd6` introduces the deeply immutable
  runtime planning domain, structured semantic identity constructors, current-mutator compatibility
  projection, and the strict flip@3 codec while retaining explicit flip@2 compatibility.
- Install, uninstall, dev, promote, and rollback now prepare one immutable plan plus a private
  one-to-one operation-binding map before mutation. Dry-run projects the prepared product without
  invoking a binding; execution walks the exact prepared operation order and builds separately
  correlated actual results without rebuilding or changing the approved plan.
- The application resolves effective cwd and project root once, approves the exact prepared bulk
  plan, and invokes that prepared product once. A newly eligible placement appearing during
  confirmation cannot widen execution. A genuinely absent explicit target refuses the whole
  invocation before confirmation, lock, or write, while filter-noop and capability reports retain
  their accepted behavior.
- Structured SHA-256 identities cover groups, pairs, operations, checks, and diagnostics. Source
  semantics participate in dev identity; failed and cancelled execution results retain
  `actualAfter === actualBefore`; interrupted operations remain represented. The constructors
  enforce semantic references, deep ordinary snapshots, prototype/accessor/proxy/sparse/cycle
  rejection, sensitive-string rejection, dependency order, and recursive freezing.
- The implementation retains sequential current-runner behavior only. It adds no G3B-02 scheduler
  or dev/promote continuation option, no G3B-03 ledger/recovery authority, no G5 undo surface, and
  no saved-plan persistence or execution behavior.
- **Result:** minimal implementation passes at the reviewed candidate revision. No traceability or
  terminal sign-off is implied.

## Targeted and impacted green

- All 15 exact catalog commands pass individually and sequentially on
  `a6b6bea4c1f151f99364f30a08fe7725b492bcd6`: **20 passed tests, 1,294 assertions,
  0 failures, and 0 skips**. The twelve dev/promote commands contribute 16 tests and 729
  assertions; `EWP-OPT-TS04` contributes 4 tests and 54 assertions; the two P3B commands
  contribute 2 tests and 511 assertions.
- Prepared acquisition replay passes install **35/35** with 204 assertions and uninstall
  **23/23** with 131 assertions. Their temporal killing tests observe immutable plans and exact
  binding maps before the first live write/removal and prove preview/execution plan identity plus
  one-to-one execution-result correlation.
- Prepared place/application replay passes the twelve exact dev/promote selectors **14/14** with
  729 assertions, lifecycle/CLI/flip compatibility **77/77** with 246 assertions, placement
  plan/dev-source **45/45** with 167 assertions, runner **30/30**, round-trip **14/14**, and flip
  exit projection **10/10**. Planning/P3B supporting replay passes **12/12** with 575 assertions;
  public API replay passes **8/8** with 170 assertions.
- The bounded smoke lane passes **5/5** with 3,735 assertions and recovery smoke passes **4/4**
  with 453 assertions. TypeScript, Biome over 577 files, ESLint boundaries, `git diff --check`,
  deterministic catalog/checklist validation, and `check:p17 --pr-openable` pass.
- Three 28-file aggregate attempts each passed 300 of 301 tests but hit one rotating shared-host
  subprocess timeout (`EWP-OPT-TS10`, a help alias case, then PROMOTE-TS01). Each timed-out test
  passed immediately on the unchanged candidate in isolation: TS10 1/1 with 48 assertions, help
  1/1 with 8 assertions, and PROMOTE-TS01 1/1 with 31 assertions in 94 ms. The exact catalog
  commands and all bounded partitions are green, so repeated low-signal aggregate retries stopped;
  the milestone-only full repository suite remains reserved for terminal sign-off.
- **Result:** targeted-green and impacted-green pass. `EWP-P3B-T01` and `EWP-P3B-T03`
  advance to green; the 14 G3B-01-owned validations advance to passing; the two commands, decision,
  and two findings advance to validated. `EWP-OPT-TS04` remains signed off under G1-02A with its
  exact replay recorded here. Downstream coverage remains pristine.

### Final green execution receipt EWP-CMD-DEV-TS01

- Validation ID: `EWP-CMD-DEV-TS01`
- Command: `bun test packages/cli/tests/contracts/dev.test.ts --test-name-pattern EWP-CMD-DEV-TS01`
- Exit status: `0`
- Result: 1 passed, 0 failed, 0 skipped, 59 assertions.
- Revision: `a6b6bea4c1f151f99364f30a08fe7725b492bcd6`

### Final green execution receipt EWP-CMD-DEV-TS02

- Validation ID: `EWP-CMD-DEV-TS02`
- Command: `bun test packages/cli/tests/contracts/dev.test.ts --test-name-pattern EWP-CMD-DEV-TS02`
- Exit status: `0`
- Result: 1 passed, 0 failed, 0 skipped, 68 assertions.
- Revision: `a6b6bea4c1f151f99364f30a08fe7725b492bcd6`

### Final green execution receipt EWP-CMD-DEV-TS03

- Validation ID: `EWP-CMD-DEV-TS03`
- Command: `bun test packages/cli/tests/contracts/dev.test.ts --test-name-pattern EWP-CMD-DEV-TS03`
- Exit status: `0`
- Result: 1 passed, 0 failed, 0 skipped, 41 assertions.
- Revision: `a6b6bea4c1f151f99364f30a08fe7725b492bcd6`

### Final green execution receipt EWP-CMD-DEV-TS04

- Validation ID: `EWP-CMD-DEV-TS04`
- Command: `bun test packages/cli/tests/contracts/dev.test.ts --test-name-pattern EWP-CMD-DEV-TS04`
- Exit status: `0`
- Result: 2 passed, 0 failed, 0 skipped, 86 assertions.
- Revision: `a6b6bea4c1f151f99364f30a08fe7725b492bcd6`

### Final green execution receipt EWP-CMD-DEV-TS05

- Validation ID: `EWP-CMD-DEV-TS05`
- Command: `bun test packages/cli/tests/contracts/dev.test.ts --test-name-pattern EWP-CMD-DEV-TS05`
- Exit status: `0`
- Result: 1 passed, 0 failed, 0 skipped, 133 assertions.
- Revision: `a6b6bea4c1f151f99364f30a08fe7725b492bcd6`

### Final green execution receipt EWP-CMD-DEV-TS06

- Validation ID: `EWP-CMD-DEV-TS06`
- Command: `bun test packages/cli/tests/contracts/dev.test.ts --test-name-pattern EWP-CMD-DEV-TS06`
- Exit status: `0`
- Result: 1 passed, 0 failed, 0 skipped, 61 assertions.
- Revision: `a6b6bea4c1f151f99364f30a08fe7725b492bcd6`

### Final green execution receipt EWP-CMD-PROMOTE-TS01

- Validation ID: `EWP-CMD-PROMOTE-TS01`
- Command: `bun test packages/cli/tests/contracts/promote.test.ts --test-name-pattern EWP-CMD-PROMOTE-TS01`
- Exit status: `0`
- Result: 1 passed, 0 failed, 0 skipped, 31 assertions.
- Revision: `a6b6bea4c1f151f99364f30a08fe7725b492bcd6`

### Final green execution receipt EWP-CMD-PROMOTE-TS02

- Validation ID: `EWP-CMD-PROMOTE-TS02`
- Command: `bun test packages/cli/tests/contracts/promote.test.ts --test-name-pattern EWP-CMD-PROMOTE-TS02`
- Exit status: `0`
- Result: 1 passed, 0 failed, 0 skipped, 43 assertions.
- Revision: `a6b6bea4c1f151f99364f30a08fe7725b492bcd6`

### Final green execution receipt EWP-CMD-PROMOTE-TS03

- Validation ID: `EWP-CMD-PROMOTE-TS03`
- Command: `bun test packages/cli/tests/contracts/promote.test.ts --test-name-pattern EWP-CMD-PROMOTE-TS03`
- Exit status: `0`
- Result: 1 passed, 0 failed, 0 skipped, 32 assertions.
- Revision: `a6b6bea4c1f151f99364f30a08fe7725b492bcd6`

### Final green execution receipt EWP-CMD-PROMOTE-TS04

- Validation ID: `EWP-CMD-PROMOTE-TS04`
- Command: `bun test packages/cli/tests/contracts/promote.test.ts --test-name-pattern EWP-CMD-PROMOTE-TS04`
- Exit status: `0`
- Result: 2 passed, 0 failed, 0 skipped, 47 assertions.
- Revision: `a6b6bea4c1f151f99364f30a08fe7725b492bcd6`

### Final green execution receipt EWP-CMD-PROMOTE-TS05

- Validation ID: `EWP-CMD-PROMOTE-TS05`
- Command: `bun test packages/cli/tests/contracts/promote.test.ts --test-name-pattern EWP-CMD-PROMOTE-TS05`
- Exit status: `0`
- Result: 1 passed, 0 failed, 0 skipped, 74 assertions.
- Revision: `a6b6bea4c1f151f99364f30a08fe7725b492bcd6`

### Final green execution receipt EWP-CMD-PROMOTE-TS06

- Validation ID: `EWP-CMD-PROMOTE-TS06`
- Command: `bun test packages/cli/tests/contracts/promote.test.ts --test-name-pattern EWP-CMD-PROMOTE-TS06`
- Exit status: `0`
- Result: 1 passed, 0 failed, 0 skipped, 54 assertions.
- Revision: `a6b6bea4c1f151f99364f30a08fe7725b492bcd6`

### Final green execution receipt EWP-P3B-TS01

- Validation ID: `EWP-P3B-TS01`
- Command: `bun test tests/ergonomics/phase/EWP-P3B-TS01.test.ts --test-name-pattern EWP-P3B-TS01`
- Exit status: `0`
- Result: 1 passed, 0 failed, 0 skipped, 195 assertions.
- Revision: `a6b6bea4c1f151f99364f30a08fe7725b492bcd6`

### Final green execution receipt EWP-P3B-TS02

- Validation ID: `EWP-P3B-TS02`
- Command: `bun test tests/ergonomics/phase/EWP-P3B-TS02.test.ts --test-name-pattern EWP-P3B-TS02`
- Exit status: `0`
- Result: 1 passed, 0 failed, 0 skipped, 316 assertions.
- Revision: `a6b6bea4c1f151f99364f30a08fe7725b492bcd6`

## Refactor review

- The first independent architecture review rejected the initial post-hoc implementation at 0.99
  confidence because runners rebuilt plans from completed compatibility results, bulk approval
  independently replanned, project context was rediscovered, mixed missing targets could partially
  execute, and several adapter IDs/result facts were not canonical.
- Remediation replaced those shortcuts with exact prepared products and killing tests. Install and
  uninstall now prepare lazy captured bindings before mutation; dev/promote/rollback expose one
  opaque single-use prepared executor; application approval consumes and executes that same product.
  The candidate planner consumes normalized project facts and performs no Git discovery.
- Canonical helpers now hash closed semantic group, pair, operation, check, and diagnostic inputs;
  constructor validation enforces every plan/reference relation. Compatibility and temporal tests
  prove no plan widening, no unmatched-target partial mutation, source-sensitive identity,
  delimiter/permutation resistance, and truthful actual results.
- The accepted plan hash remains
  `e020af58766b1b951d2ba818db1bf25c1d4d9fae4d4ee2276ca023f4b7e9135f`; ownership remains exactly
  54 paths. The candidate commit changes 47 of those paths plus the two governance files, with no
  unowned implementation path.

## Adversarial review

- Independent reviewer `/root/g3b01_independent_architecture_review` returned **PASS** at 0.97
  confidence after re-testing every mandatory finding. Install/uninstall prepared-plan tests,
  dev/promote late-discovery and atomic-target tests, planning/P3B constructors, and OPT conflict
  coverage contributed 17 passing tests plus three hostile probes with zero product failures.
- A poisoned Git getter proved `planFlips` consumes the application-resolved project context with
  zero Git reads. A source-change probe changed both group and operation IDs. An abort-after-prepare
  probe produced a correlated cancelled result with `error: null` and
  `actualAfter === actualBefore` while retaining the immutable operation.
- The reviewer confirmed every prepared plan has one binding per operation and every execution
  result correlates to that original plan; mixed missing targets refuse before confirmation/lock/
  write; filter-noop and capability reports remain intact.
- No G3B-02 scheduler/coordinator or dev/promote continuation behavior, G3B-03 writer/history/
  recovery authority, G5 public undo, saved-plan execution, or other successor-owned behavior is
  present. No waiver, deferred mandatory finding, or human-review pause remains.
- **Result:** refactor and adversarial-review pass. G3B-01 advances to `reviewed` with 8/10 gates;
  traceability closure and terminal sign-off remain pending.

## Terminal full-gate failure and remediation ownership amendment

- The first reserved terminal `bun run check` on clean reviewed commit
  `8f0a4542bd39a36c3d6a60f24042e3b9496f3f64` exits 1. Biome over 577 files, ESLint
  boundaries, TypeScript, actionlint, and the P17 PR-openable package/catalog checks all pass.
  The full test lane executes **1,983 tests across 224 files in 510.72 seconds**: **1,942 pass,
  28 intentional skips, 13 fail, 2 errors, and 71,420 assertions**.
- Three spawned-process failures are host-load timeouts, not reproducible behavior: P1-TS07 global
  version passes alone with 48 assertions in 356 ms, P1-TS07 eager shielding passes with 11
  assertions in 360 ms, and CONFIG-TS04 missing-key migration passes with 6 assertions in 370 ms.
  Their files require no correction.
- Two product failures remain inside existing ownership. `packages/core/src/place/run.ts` contains
  one new known-tool verification branch instead of consulting the signed tool registry.
  `packages/core/src/acquire/run.ts` accepts a same-SHA install noop before comparing fetched and
  stored bytes, bypassing the existing store-integrity refusal. Existing P1-TS09 and install exit
  tests kill the minimal registry and read-only hash-comparison fixes.
- Strict flip@3 introduced bounded compatibility-fixture and inventory fallout. Before any edit,
  ownership expands by exactly eight paths:
  `tests/ergonomics/phase/EWP-P1-TS07.test.ts`,
  `tests/ergonomics/phase/EWP-P1-TS10.test.ts`,
  `tests/ergonomics/fixtures/p1-ts10/reports.ts`,
  `tests/ergonomics/fixtures/p1-ts10/goldens/flip.stdout`,
  `tests/ergonomics/fixtures/p1-ts10/public-wire-types.ts`,
  `tests/ergonomics/phase/EWP-P2-TS08.test.ts`,
  `tests/ergonomics/fixtures/p2-ts08/public-types.ts`, and
  `tests/ergonomics/phase/EWP-P1-TS11.test.ts`.
- P1-TS07 receives only the required immutable empty plan/result sidecar. P1-TS10/P2-TS08 and their
  fixtures add the current flip@3 codec/export/DTO/byte inventory while retaining historical
  flip@2 bytes independently. P1-TS11 updates its signed option inventory for the reviewed six
  dev/promote scope rows. The already-owned `packages/core/src/contracts/v3/flip.ts` aligns its
  explicit source DTO type with the existing public declaration without changing runtime bytes.
- **Boundary:** no renderer, command, option, scheduler, continuation, ledger, recovery, undo,
  network, or saved-plan behavior is added. The exact 21 primary entities, 32/15/17 validation
  partition, and accepted plan hash remain unchanged. Ownership is now exactly **62 paths**; a
  post-remediation terminal full rerun is mandatory before traceability closure or sign-off.

## Terminal remediation and full repository gate

- Governance commit `3ae0045` records the failed terminal receipt and expands ownership before any
  correction. Remediation commit `99594536d24b6fe8f31df793fad74ba09517f049` changes exactly
  11 authorized paths: three existing production paths and eight amended test/fixture paths.
- Production correction removes the known-tool branch in favor of the signed adapter registry and
  checks fresh materialization against the existing store entry before accepting a same-SHA noop.
  Strict flip@3 source/public type parity, current registry/export inventories, compile fixtures,
  current bytes, lifecycle sidecars, and the exact 166-option snapshot are aligned while literal
  historical flip@2 bytes and codec coverage remain independent.
- Independent remediation reviewer `/root/g3b01_independent_architecture_review` returns **PASS** at
  0.98 confidence. Its focused unique replay passes **99 tests, 4,558 assertions, 0 failures, and
  0 skips**; all three former timeout canaries also pass alone with 65 assertions. The exact
  15-command G3B-01 replay on the correction commit passes **20 tests and 1,294 assertions** with
  zero failures or skips.
- Pre-terminal gates pass on the clean correction: smoke **5/5** with 3,735 assertions, recovery
  smoke **4/4** with 453 assertions, TypeScript, Biome over 577 files, ESLint boundaries,
  `git diff --check`, deterministic catalog/checklist validation, and `check:p17 --pr-openable`.
- The mandatory post-remediation `bun run check` on
  `99594536d24b6fe8f31df793fad74ba09517f049` exits **0**. Biome, ESLint boundaries,
  TypeScript, actionlint, and P17 package/catalog validation all pass. The complete repository suite
  runs **1,983 tests across 224 files in 418.32 seconds**: **1,955 passed, 28 intentional skips,
  0 failures, and 72,205 assertions**.
- **Result:** the failed first terminal receipt is closed by focused killing tests, independent
  review, a clean exact-selector replay, bounded/static gates, and the required passing full-suite
  retry. No waiver or unresolved timeout remains.

## Traceability and sign-off

- Accepted implementation/test pin:
  `99594536d24b6fe8f31df793fad74ba09517f049`. The append-only sequence is test-first
  `e9364d2`, implementation candidate `a6b6bea`, reviewed catalog `8f0a454`, terminal ownership
  amendment `3ae0045`, and terminal correction `9959453`.
- Accepted plan SHA-256 remains
  `e020af58766b1b951d2ba818db1bf25c1d4d9fae4d4ee2276ca023f4b7e9135f`. Scope closes at
  exactly **21 primary entities, 15 required-now validations, 17 downstream obligations, 32 total
  impacted validations, and 62 owned paths**. Every changed implementation/test/fixture path is in
  that manifest; downstream entities and files remain pristine.
- All 21 primary entities close here: `COMMAND:dev`, `COMMAND:promote`, `D-009`, `EWP-CF-002`,
  `EWP-CF-025`, `EWP-P3B-T01`, `EWP-P3B-T03`, `EWP-CMD-DEV-TS01..TS06`,
  `EWP-CMD-PROMOTE-TS01..TS06`, `EWP-P3B-TS01`, and `EWP-P3B-TS02`. The signed dependency
  `EWP-OPT-TS04` retains its G1-02A ownership and exact replay; all 17 downstream validations retain
  their future primary owners.
- Exact selector receipts, acquisition/place/application compatibility, immutable identity and
  binding probes, strict flip@3/current and flip@2/historical byte/type coverage, bounded smoke,
  static/package gates, independent adversarial reviews, and the passing full repository receipt
  establish the accepted plan and implementation on committed bytes.
- G3B-01 is signed off. G3B-02 remains the next group and retains lock hierarchy, coordinator,
  deterministic scheduling, and actual fail-fast/continue execution semantics. No later authority
  is advanced here.
- This remains authorized, credential-free, defensive local development for the open-source
  Skillsmith CLI. Standing authorization is recorded in
  `projects/p17/evidence/standing-authorization.md`; no human-review pause is required. Sign-off
  owner/date: root Codex goal, 2026-07-15 America/Los_Angeles.
