# P17-G3A-02 evidence

## Mapping and prerequisites

- **Primary entities:** exactly 24: `COMMAND:agents`, `COMMAND:commands`, `COMMAND:list`,
  `EWP-P3A-T01`, `EWP-P3A-T04`, `EWP-P3A-T05`, `EWP-CMD-AGENTS-TS01..TS03`,
  `EWP-CMD-COMMANDS-TS01..TS04`, `EWP-CMD-LIST-TS01..TS07`, `EWP-P3A-TS01`,
  `EWP-P3A-TS03`, `P1-01`, and `P1-03`. This is three commands, three phase tasks,
  fourteen command tests, two phase tests, and two recommendations. All remain pristine
  `planned` at mapping; no entity target or evidence is advanced.
- **Required now and impacted:** the same exact 16 validations: the three agents selectors, four
  commands selectors, seven list selectors, `EWP-P3A-TS01`, and `EWP-P3A-TS03`. Downstream
  coverage is empty. Their immutable planned targets occupy five files: the agents, commands, and
  list command-contract files plus the two phase-test files. None of those five files exists yet.
- **Dependency:** sole direct dependency P17-G3A-01 is signed off at `0e8ec7c`; its final full
  repository gate passed with 1,839 tests passing, 28 intentional opt-in skips, zero failures, and
  70,320 assertions. G3B-01 is the direct successor and cannot begin until this group signs off.
- **Secondary trace:** signed `EWP-CF-007` and `EWP-CF-036` retain the operation-scoped adapter
  capability model and executable registry authority. Signed `EWP-CF-022` retains the single
  shared project-context authority. G3A-02 reuses those authorities rather than introducing tool
  branches, a second capability table, or project-root discovery.
- **Outcome boundary:** G3A-02 owns deterministic, non-mutating agents/list/commands inventory and
  presentation: compact and long human modes, complete versioned JSON, tool/scope/name filtering,
  the planned list metadata filters, `(tool, skill-name)` duplicate identity, explicit shadow
  presentation, registry-derived per-operation capabilities, and bounded large-fleet behavior.
- **Current gaps:** current list and commands enumeration has no canonical final sort. List
  duplicate filtering groups only by name across scopes, so different tools can become false
  duplicates and no shadow winner is reported. The list product does not model all planned long
  fields or filters, compact output exposes non-compact fields, agents lacks the JSON alias and
  registry-derived capability presentation, and human large-fleet output is not bounded.
- **Existing architecture:** the three application services live in
  `packages/core/src/application/read-services.ts`; read-only scanners own adapter/plugin inventory;
  the signed adapter registry and capability-snapshot codec own capability facts; CLI renderers own
  human/JSON presentation; shared command specifications own option grammar. Ready must freeze the
  exact sequentially reused paths, public-model and codec changes, test commands, compatibility
  replays, implementation assignments, and deterministic shadow precedence without claiming
  future tool-lifecycle behavior.
- **Exclusions:** G3A-01 retains correlated status and journal/drift presentation. G3B and later
  groups retain mutation operations, lifecycle planning, writers, locks, journals, recovery, and
  enable/disable execution. This group performs no writes, network access, verification execution,
  or generalized status reconciliation.
- Read-only catalog auditor `/root/g3a02_catalog_scope_audit` independently reproduced the 24
  primaries, 16/16 validation partition, five affected contracts, zero downstream coverage, signed
  dependency, three signed secondary traces, and five missing selector files at 0.99 confidence.
- Read-only architecture auditor `/root/g3a02_current_architecture_audit` independently reproduced
  the scanner/application/registry/renderer boundaries and the sorting, duplicate, metadata,
  capability, JSON, and large-fleet gaps at 0.94 confidence.
- **Result:** prerequisites, entity and validation ownership, current architecture, product gaps,
  successor boundary, and strict exclusions are mapped. No Ready, test-first, implementation,
  entity, validation, or sign-off gate is implied.

## Ready plan

- **Plan:** `projects/p17/plans/P17-G3A-02.md`.
- **Accepted SHA-256:**
  `76932c0c70aab73c553e4bff7164b243a6e199b0a11ef0649ea4a60ace28ea98`.
- The accepted plan freezes exact selection/default/filter behavior, immutable inventory products,
  mode-aware exact-ledger enrichment, internal all-observations scanner seams, adapter-owned runtime
  identity and collision rules, strict agents detection, complete deterministic human/JSON output,
  the 100-row compact cap, linear fleet bounds, and the no-write/no-network boundary.
- Historical `agents@1`, `commands@1`, `list@2`, and `status@1` remain compatible. Current mappings
  advance to `agents@2`, `commands@2`, and `list@3`; the existing exit selector is extracted without
  changing its order, and public scanner/detection compatibility is retained.
- Product/error review passed at 0.99 confidence, architecture/ownership review passed at 0.99,
  and exact trace/test review passed at 0.995. All three independently confirmed the immutable plan
  digest with no residual findings.
- The reviews reproduced exactly 24 primary entities, the identical 16/16 required-now/impacted
  selector set, zero downstream coverage, 32 substantive families numbered 27–58, and 95 unique
  owned paths: 46 production/documentation/compatibility and 49 test/fixture paths.
- The frozen small-fixture design contains 24 mixed inventory rows and 18 raw duplicate
  observations normalizing to 17 placements and five collision identities. Large 10,000-skill and
  4,096-command fleets are generated in memory. Literal fixture byte lengths and SHA-256 values are
  reserved for the test-first commit.
- The 16 catalog commands are the three separately filtered agents selectors, four commands
  selectors, seven list selectors, and the separately filtered `EWP-P3A-TS01` and
  `EWP-P3A-TS03` phase selectors. Focused supporting tests, eight compatibility replays, smoke, and
  static gates are executable and exact in the plan; the full suite is reserved for terminal
  G3A-02 sign-off.
- `git diff --check`, catalog/checklist validation, full P17 documentation-drift validation, and
  `bun run check:p17` pass on the Ready governance diff. Ready changes no production/test behavior,
  so smoke and the full repository suite are intentionally not run at this gate.
- **Result:** P17-G3A-02 advances to `ready`; only `EWP-P3A-T01`, `EWP-P3A-T04`, and
  `EWP-P3A-T05` advance with it. The two recommendation coverage entities and all 16 validation
  entities remain pristine `planned`. No test-first or implementation advancement is claimed.
