# P17-G5-05 evidence

## Mapping and prerequisites

Mapping began from clean governance revision
`b5d87002fc70f84cf137b224c6054c61f963e01c` on 2026-07-23. The starting catalog SHA-256 was
`bde0c3a3fd16aa1b616a20ea0613a314cb96fbb4e59940960566d8a7831d9e15`.

- Phase 5 is active with its entry gate passed.
- Direct prerequisites P17-G5-01, P17-G5-02, and P17-G5-03 are signed off at all ten lifecycle
  gates with resolving evidence.
- G5-05 starts pristine and planned: no owned files, test commands, implementers, reviewer,
  evidence, executable targets, or passed gates.
- Primary ownership is exactly seven entities: EWP-CMD-UNDO-TS03, EWP-CMD-UPDATE-TS07,
  EWP-OPT-TS08, EWP-P5-TS05, EWP-WF09, EWP-WF13, and P1-04.
- Required-now is exactly 14 validations: the six primary selectors plus APPLY-TS09/11/13,
  SYNC-TS09, UPDATE-TS06/08/09, and P4A-TS02 from signed dependencies.
- Downstream coverage is empty.
- COMMAND:update, COMMAND:undo, D-010, D-011, CF-003, CF-008, CF-026, CF-027, CF-031, CF-040,
  P1-11, and P2-02 are signed. P1-04 is the only planned coverage authority in the group.

The detailed mapped/Ready candidate is `projects/p17/plans/P17-G5-05.md`. Its proposed ownership
manifest contains 47 unique paths and its proposed implementation-free boundary contains the six
primary validation owners plus one hermetic shared fleet.

## Existing implementation and gap audit

Signed dependencies already implement one deterministic scheduler, exact bulk approval,
fail-fast/continue, cancellation, exits, custom artifact pairs, update ref/pin/live/lock behavior,
prior-store retention, and scope-aware undo for every operation family except update.

The exact product gap is explicit in `packages/core/src/undo/observe.ts`: update history is
recognized and rejected as `undo-update-unavailable` because committed update placement journals do
not yet carry authoritative manifest/lock history. The current implementation must not infer a
custom artifact pair or prior portable intent from cwd/current bytes. The candidate therefore adds
strict internal artifact lineage to forward update and consumes it through the signed undo,
artifact-coordinator, placement-reversal, scheduler, approval, and output authorities.

Six planned validation owners do not yet contain executable selectors:

- EWP-CMD-UNDO-TS03;
- EWP-CMD-UPDATE-TS07;
- EWP-OPT-TS08 (references exist only in inventory/governance tests);
- EWP-P5-TS05 (inventory reference only);
- EWP-WF09 (migration-ledger reference only); and
- EWP-WF13.

## Signed dependency characterization

The eight already-owned dependency selectors were replayed before mapping. The first attempt ran
eight independent stateful CLI processes concurrently. Six passed, while UPDATE-TS06 and TS09 both
reported `apply-ledger-bootstrap` because their stateful fixture processes collided at ledger
directory preparation. Neither reproduced when run serially, and both passed inside the preceding
complete repository gate with `--max-concurrency 1`. This is a fixture scheduling constraint, not a
product failure; G5-05 records serialized public-process commands as mandatory.

Final serialized/current receipts:

```text
bun test packages/cli/tests/contracts/apply.test.ts --test-name-pattern EWP-CMD-APPLY-TS09
1 pass; 0 fail; 3 assertions

bun test packages/cli/tests/contracts/apply.test.ts --test-name-pattern EWP-CMD-APPLY-TS11
1 pass; 0 fail; 6 assertions

bun test packages/cli/tests/contracts/apply.test.ts --test-name-pattern EWP-CMD-APPLY-TS13
1 pass; 0 fail; 19 assertions

bun test packages/cli/tests/contracts/sync.test.ts --test-name-pattern EWP-CMD-SYNC-TS09
1 pass; 0 fail; 111 assertions

bun test packages/cli/tests/contracts/update.test.ts --test-name-pattern EWP-CMD-UPDATE-TS06
1 pass; 0 fail; 61 assertions

bun test packages/cli/tests/contracts/update.test.ts --test-name-pattern EWP-CMD-UPDATE-TS08
1 pass; 0 fail; 25 assertions

bun test packages/cli/tests/contracts/update.test.ts --test-name-pattern EWP-CMD-UPDATE-TS09
1 pass; 0 fail; 62 assertions

bun test tests/ergonomics/phase/EWP-P4A-TS02.test.ts --test-name-pattern EWP-P4A-TS02
27 pass; 0 fail; 314 assertions
```

The preceding full terminal gate on the same product revision plus documentation-only lifecycle
changes reported 3,143 pass, 28 intentional live-environment skips, zero failures, and 104,168
assertions across 3,171 tests and 341 files. It is characterization evidence, not a G5-05 terminal
sign-off.

## Mapping result

The candidate maps one coherent missing behavior rather than fourteen independent implementations:

- update must durably retain exact selected artifact before/after lineage;
- undo must correlate that lineage with the already-selected update placement group;
- the existing artifact coordinator and placement reversal authorities execute the inverse work;
- the existing scheduler/approval/output authorities retain their signed semantics; and
- the six new selectors complete the dependency-level matrices while the eight signed selectors
  guard against regression.

No implementation, selector target, red/green receipt, Ready acceptance, reviewer GO, or later
lifecycle gate is claimed in this mapping record.

## Ready review at e7405cc

The first fresh independent Ready review examined immutable commit
`e7405ccc549b7034b203af06d6dc29c57d65f467` without editing the worktree. It returned **NO-GO**
with five stable blocking findings:

- `P17-RV-P17-G5-05-F01`: operation manifest snapshots contain normalized values and hashes, not
  the original authored bytes, trivia, newline form, or filesystem mode. The coordinator removes
  its attempt backup after commit, so the original candidate could not perform its claimed exact
  manifest restoration.
- `P17-RV-P17-G5-05-F02`: assigning artifact inverses to the first live pair contradicted all three
  current authorities. Artifact execution and logical history require pair-null identity, while
  `undo@1` required every executable operation to be pair-bound and did not admit artifact kinds in
  its undo vocabulary.
- `P17-RV-P17-G5-05-F03`: journal v1 can represent pair-null artifact work, but fresh rollback
  lineage and logical reversal currently require a placement inverse and legacy pair shadow, so an
  artifact rollback child would make the ledger invalid.
- `P17-RV-P17-G5-05-F04`: bounded-history dependency closure covers rollback parent/child only.
  It can evict an artifact carrier while retaining the newest selectable placement journals from
  the same update group.
- `P17-RV-P17-G5-05-F05`: deterministic update `groupId` and artifact operation IDs can recur when
  a resolution is revisited. Group ID alone cannot select one historical carrier occurrence.

The reviewer independently confirmed that the 7-primary/14-required mapping and lifecycle state
remain truthful, old pre-lineage refusal is correct, custom machine-bound paths can be retained
once exact occurrence authority exists, and all structural checks pass. Its characterization run
reported 73 pass, zero fail, and 485 assertions across the relevant codec, history/writer, undo,
and update suites. No file was changed by the reviewer, and Ready remained pending.

## Accepted Ready amendment candidate

All five findings are accepted as blockers. The plan is amended without advancing lifecycle state:

- **F01:** each changing manifest or lock carrier declares a conditional retention resource,
  publishes its prepared journal under the existing locks, creates an owner-private durable backup
  envelope containing the exact regular-file bytes plus a canonical source-role/mode receipt,
  fsyncs it, records its complete digest in `actual.retained`, and only then invokes the existing
  artifact coordinator. The envelope digest authenticates the mode without adding a journal field.
  The pending logical journal remains the sole added recovery authority; the envelope is retained
  restoration material, not a second recovery format. Undo uses the validated retained bytes/mode
  rather than normalized reconstruction, and ordinary bounded-history cleanup owns eventual
  deletion.
- **F02:** inverse artifact operations remain group-level and pair-null. `group.operations`
  partitions into its artifact prefix plus the live-only pair vectors. Pair outcome stays pair-local;
  group outcome/failure reduces all group results. Strict negative codec coverage freezes omission,
  duplication, order, pair-binding, and artifact failure/cancellation truth without adding a public
  field or enum.
- **F03:** logical history gains an explicit schema-preserving artifact parent/child branch with no
  legacy shadow. Exact parent operation/transaction, path, images, retained backup, phase,
  command/workflow, and one-child rules are validated through prepared, resume, commit, and hostile
  cases.
- **F04:** the bounded selector treats the exact forward update occurrence—artifact carriers,
  correlated placement journals, rollback children, and retained resources—as one dependency
  unit. Protection expands effective capacity; exclusion and restartable cleanup remove the whole
  unit. More-than-256-entry, pending-child, two-tool, custom-pair, and cleanup-restart tests are
  mandatory.
- **F05:** update group identity adds a non-sensitive versioned digest of exact manifest/lock
  before/after identities. Historical correlation still starts from the selected placement source
  transaction and commit/image chain rather than assuming group IDs are globally unique. A->B->C->B,
  exact-repeat, different-trivia, intervening-history, two-tool, and custom-pair cases are mandatory.

The ownership manifest expands from 47 to 49 unique paths by adding the shared artifact execution
authority and its focused test. The existing planned `update/history.ts` and `undo/artifacts.ts`
boundaries own retention/correlation; the artifact coordinator remains the only portable-file
mutator. Public-process validation remains serialized. This amendment is a new Ready candidate and
requires structural validation, an immutable commit, and a fresh independent rerun before Ready
can pass.

## Ready rerun at d13d907

The fresh independent rerun examined immutable amendment commit
`d13d907ed2ce5421581df8db0e27c19339b81108` and returned **NO-GO** with five new stable blockers:

- `P17-RV-P17-G5-05-F06`: the proposed carrier jumped `prepared -> backed-up` and added
  `actual.retained` after transaction identity was frozen. Existing authority requires every
  adjacent phase and exact retained identity across ordinary transitions.
- `P17-RV-P17-G5-05-F07`: the wrapper had no exact codec or compatible observation/hash contract.
  Status treated the wrapper as direct manifest/lock bytes, cleanup required equal digests over raw
  bytes, and the plan did not own `status/read.ts`.
- `P17-RV-P17-G5-05-F08`: current public pair actions cannot replace a present manifest with raw
  bytes, restore exact lock bytes, or set the retained mode. Coordinator source/types/tests were
  outside the candidate boundary.
- `P17-RV-P17-G5-05-F09`: a pair-null inverse with the same update group and kind reproduces the
  forward semantic operation ID. A fresh group requires every live pair/operation plus placement
  logical/swap authority to use the new identity coherently.
- `P17-RV-P17-G5-05-F10`: hostile codec coverage was incomplete because the candidate changed
  ledger/plan codecs without owning their existing focused `artifacts/codec.test.ts` authority.

Prior F02's pair-null partition and reduction design, F03's artifact branch, F04's atomic occurrence
unit, and F05's occurrence correlation were accepted conceptually. F01 remained open through
F06-F08. The reviewer confirmed the contiguous-suffix/custom-pair refusal, mapped lifecycle state,
and structural package remained truthful. Its focused characterization reported 75 pass, zero
fail, and 485 assertions. Catalog tests reported 56 pass, zero fail, and 267 assertions; catalog,
package, PR-openable, plan-structure, and diff checks passed. The reviewer made no file change.

## Accepted second Ready amendment candidate

F06-F10 are accepted, and Ready remains pending. The corrected candidate makes the following
implementable commitments:

- **F06:** a forward artifact journal begins `prepared` with one declared conditional resource and
  no factual retained row. After its deterministic preimage envelope is durable, exactly one
  narrowly validated `prepared -> staged` transition may add the matching row. All later phases are
  adjacent and keep that row identical. Prepared/staged/backed-up/live crash and abort behavior is
  explicit; no forward artifact mutation occurs before `backed-up`.
- **F07:** a new private `retained-preimage@1` codec canonically binds kind/version, forward
  operation, role, absolute path, exact preimage bytes/mode, and expected forward-after
  digest/mode. The retained repository revision is the resource-domain envelope digest; content
  hash is the decoded role-domain payload digest. Status and cleanup parse the same codec, verify
  both domains and owner-private filesystem facts, and fail closed on noncanonical or hostile
  material.
- **F08:** exact restore is an internal coordinator request, not a new public
  `HumanManifestAction`/`GeneratedLockAction`. It validates the current role digest/mode and target
  operation image, then reuses the coordinator's existing desired-role staging, recovery record,
  install, verification, and cleanup machinery with exact target bytes/mode. Manifest precedes
  lock when both restore, so the final lock is checked against the restored manifest. The recovery
  record format is unchanged.
- **F09:** undo derives one deterministic occurrence-specific group from the exact ordered source
  transaction/operation lineage. It recomputes every live pair and operation ID under that group;
  artifact inverses remain pair-null and receive fresh IDs. Placement plan/swap/logical and ledger
  validation accept only the exactly derived child pair while retaining source authority solely in
  existing parent/source fields.
- **F10:** focused codec ownership now includes `packages/core/tests/artifacts/codec.test.ts`.

The correction also closes the adjacent scheduler invariant: only update/undo pair-null manifest or
lock writes may carry the one conditional artifact retention resource; all other artifact
prerequisites retain the signed non-reversible rule. The ownership manifest expands from 49 to 62
unique paths: 28 product/architecture paths, 26 focused tests, six acceptance owners/fixtures, and
two governance files. It adds coordinator source/types/test, the private retained-preimage codec and
test, status read authority, placement plan/swap and tests, focused codec coverage, and scheduler
source/test. This is still a mapped/Ready candidate and requires structural validation, an immutable
commit, and another fresh independent rerun before Ready can pass.

## Final Ready review at b779a87

The independent reviewer examined immutable second-amendment commit
`b779a87e62fb7e5902a15b3bdbae625372b97042` without editing the worktree and returned **GO** for
P17-G5-05 Ready. It found no new stable blocker or ownership omission and closed every prior
finding:

- F01/F07/F08 are closed by the canonical retained-preimage codec, dual-domain observation and
  cleanup, and private exact coordinator restoration;
- F02 is closed by strict pair-null artifact carriers and schema-preserving group/pair reductions;
- F03 is closed by explicit artifact rollback lineage plus freshly derived placement identities;
- F04 is closed by atomic retention/eviction of the complete update occurrence;
- F05 is closed by transition-specific, source-transaction, commit-order, image-chain, and
  nearest-unconsumed occurrence correlation;
- F06 is closed by the one narrow adjacent `prepared -> staged` enrichment and explicit crash/
  abort behavior;
- F09 is closed by a fresh undo-occurrence group and complete live pair/operation plus artifact
  operation identity remapping; and
- F10 is closed by explicit focused artifact-codec test ownership.

Reviewer verification on the immutable commit reported 276 pass, zero fail, and 2,082 assertions
across 16 focused files. Catalog validation reported 56 pass, zero fail, and 267 assertions.
Catalog check, package structural check, PR-openable check, plan-structure check, exact 62-path
plan/catalog agreement, immutable HEAD, and clean-worktree checks all passed.

## Accepted Ready result

The Ready GO is accepted for immutable commit
`b779a87e62fb7e5902a15b3bdbae625372b97042`. P17-G5-05 advances from `mapped` to `ready`; only its
mapped and Ready gates are passed. The exact implementation boundary is the 62-path manifest in the
accepted plan/catalog, the primary boundary remains seven entities, and required-now remains 14
serialized validation commands. Test-first, implementation, green, refactor, later adversarial,
traceability, and sign-off gates remain pending. P1-04 remains mapped because coverage entities do
not have an independent Ready lifecycle state.

## Test-first evidence

The implementation-free seven-path boundary is frozen at
`6e3ae9de8742647bebdc7fe8b6ed29827020b2d8`:

- `packages/cli/tests/contracts/undo.test.ts` adds EWP-CMD-UNDO-TS03 with ordinary-update and pin
  transition cases, exact artifact/live restoration, fresh inverse identities, and repeated
  no-toggle convergence;
- `packages/cli/tests/contracts/update.test.ts` adds EWP-CMD-UPDATE-TS07 with two-role exact
  preimage retention, mode preservation, strict envelope shape, owner-private storage, pair-null
  artifact carriers, and redaction canaries;
- `packages/cli/tests/contracts/options.test.ts` adds EWP-OPT-TS08 with the full declarative
  relation matrix plus spawned absolute/portable-relative custom-pair and ordinary-omission
  behavior;
- `tests/ergonomics/fixtures/p5-batch/fleet.ts` composes the signed hermetic update and sync fleets,
  state snapshots, cancellation, and public-process parsing without adding product seams;
- `tests/ergonomics/phase/EWP-P5-TS05.test.ts` composes real sync/update/undo dry-run identity,
  fail-fast/continuation policy, bulk refusal/approval, deterministic group truth, cancellation,
  rerun, and no-write checks;
- `tests/ergonomics/workflows/EWP-WF09.test.ts` encodes targetless refusal, moving/fixed selection,
  explicit ref/pin check, bulk preview, source failure, check/dry-run identity, execute, and exact
  undo restoration; and
- `tests/ergonomics/workflows/EWP-WF13.test.ts` cross-products Claude-success/Codex-failure and
  Codex-success/Claude-failure over install/apply/sync/update/promote through the public scheduler,
  proves fresh rerun convergence, and retains a real public-process partial-update/no-write case.

The first P5 draft exposed two test-authoring mismatches before the immutable boundary: update
reports publish `options.continueOnError` rather than an undo-style `selection.batchPolicy`, and a
single-target update correctly requires no bulk approval. The corrected selector uses the public
fields and a two-group `--all --yes` execution. EWP-OPT-TS08 likewise corrected the signed artifact
selection source from `explicit-file` to `explicit`, treats an escaping relative selector as the
existing refusal behavior, and compares stable operation identity rather than path-spelling-bound
revision preconditions. WF13 enriches wire operations with the public dependency metadata and
normalizes legacy wire-only before images before passing them to the strict public scheduler. None
of these corrections changes product behavior or disguises a capability failure.

Final primary result:

- EWP-OPT-TS08 passes as intended characterization/integration: 1 pass, 0 fail, 66 assertions;
- EWP-WF13 passes as intended dependency-scheduler composition: 1 pass, 0 fail, 33 assertions;
- EWP-CMD-UPDATE-TS07 fails when a pin transition reaches the existing reconciliation executor,
  which rejects `write-manifest` before exact artifact retention can commit;
- EWP-CMD-UNDO-TS03 and EWP-WF09 fail only at the explicit `undo-update-unavailable` milestone
  after an ordinary update has committed;
- EWP-P5-TS05 fails at `undo-operation-family` after sync characterization, cancellation,
  refusal, dry-run, approved two-group update, and update rerun have all passed; and
- no selector fails because of a missing file/module, syntax/type error, timeout, skip, TODO,
  public-network dependency, or fixture bootstrap error.

The two already-green characterizations remain catalog `planned` at this gate. Catalog `passing`
is tied to targeted-green and cannot truthfully jump over minimal implementation. The four honest
capability reds advance to `failing` with exact targets and immutable receipts. This preserves both
the observed execution truth and the ordered group lifecycle.

### Red execution receipt EWP-CMD-UPDATE-TS07

- Validation ID: `EWP-CMD-UPDATE-TS07`
- Command: `bun test packages/cli/tests/contracts/update.test.ts --test-name-pattern EWP-CMD-UPDATE-TS07`
- Exit status: `1`
- Result: FAIL — pin execution reaches the intended missing artifact path and reports `reconcile-execution-operation-unsupported` for `write-manifest`; 0 pass, 1 fail, 3 assertions.
- Revision: `6e3ae9de8742647bebdc7fe8b6ed29827020b2d8`

### Red execution receipt EWP-CMD-UNDO-TS03

- Validation ID: `EWP-CMD-UNDO-TS03`
- Command: `bun test packages/cli/tests/contracts/undo.test.ts --test-name-pattern EWP-CMD-UNDO-TS03`
- Exit status: `1`
- Result: FAIL — committed ordinary update reaches the exact `undo-update-unavailable` lock-restoration milestone; 0 pass, 1 fail, 6 assertions.
- Revision: `6e3ae9de8742647bebdc7fe8b6ed29827020b2d8`

### Red execution receipt EWP-P5-TS05

- Validation ID: `EWP-P5-TS05`
- Command: `bun test tests/ergonomics/phase/EWP-P5-TS05.test.ts --test-name-pattern EWP-P5-TS05`
- Exit status: `1`
- Result: FAIL — the completed sync/policy/approval/cancellation prefix reaches update undo and reports the unsupported retained operation family; 0 pass, 1 fail, 12 assertions.
- Revision: `6e3ae9de8742647bebdc7fe8b6ed29827020b2d8`

### Red execution receipt EWP-WF09

- Validation ID: `EWP-WF09`
- Command: `bun test tests/ergonomics/workflows/EWP-WF09.test.ts --test-name-pattern EWP-WF09`
- Exit status: `1`
- Result: FAIL — the normative selection/check/dry-run/execute prefix reaches the exact `undo-update-unavailable` milestone; 0 pass, 1 fail, 7 assertions.
- Revision: `6e3ae9de8742647bebdc7fe8b6ed29827020b2d8`

### Characterization execution receipt EWP-OPT-TS08

- Validation ID: `EWP-OPT-TS08`
- Command: `bun test packages/cli/tests/contracts/options.test.ts --test-name-pattern EWP-OPT-TS08`
- Exit status: `0`
- Result: PASS — live declarations, relation validation, and spawned custom-pair/omission behavior agree; 1 pass, 0 fail, 66 assertions.
- Revision: `6e3ae9de8742647bebdc7fe8b6ed29827020b2d8`

### Characterization execution receipt EWP-WF13

- Validation ID: `EWP-WF13`
- Command: `bun test tests/ergonomics/workflows/EWP-WF13.test.ts --test-name-pattern EWP-WF13`
- Exit status: `0`
- Result: PASS — both failure directions preserve independent truth and converge across the five-command public scheduler matrix; 1 pass, 0 fail, 33 assertions.
- Revision: `6e3ae9de8742647bebdc7fe8b6ed29827020b2d8`

All eight dependency selectors were then replayed as serialized public-process commands on the
same test boundary:

```text
bun test packages/cli/tests/contracts/apply.test.ts --test-name-pattern EWP-CMD-APPLY-TS09
1 pass; 0 fail; 3 assertions

bun test packages/cli/tests/contracts/apply.test.ts --test-name-pattern EWP-CMD-APPLY-TS11
1 pass; 0 fail; 6 assertions

bun test packages/cli/tests/contracts/apply.test.ts --test-name-pattern EWP-CMD-APPLY-TS13
1 pass; 0 fail; 19 assertions

bun test packages/cli/tests/contracts/sync.test.ts --test-name-pattern EWP-CMD-SYNC-TS09
1 pass; 0 fail; 111 assertions

bun test packages/cli/tests/contracts/update.test.ts --test-name-pattern EWP-CMD-UPDATE-TS06
1 pass; 0 fail; 61 assertions

bun test packages/cli/tests/contracts/update.test.ts --test-name-pattern EWP-CMD-UPDATE-TS08
1 pass; 0 fail; 25 assertions

bun test packages/cli/tests/contracts/update.test.ts --test-name-pattern EWP-CMD-UPDATE-TS09
1 pass; 0 fail; 62 assertions

bun test tests/ergonomics/phase/EWP-P4A-TS02.test.ts --test-name-pattern EWP-P4A-TS02
27 pass; 0 fail; 314 assertions
```

Supporting receipts on the immutable test revision:

```text
bun run typecheck
PASS

bun run lint:boundaries
PASS

bun run test:smoke
5 pass; 14 filtered out; 0 fail; 3,737 assertions

bunx biome check <the seven test-first paths>
Checked 7 files; no fixes required

git diff --check
PASS
```

After the immutable test commit was anchored, the catalog advanced P17-G5-05 from `ready` to
`active` and passed only the test-first gate. The four capability selectors are `failing` with
exact ID-bound targets and receipts; EWP-OPT-TS08 and EWP-WF13 remain pristine `planned` records
until targeted-green; P1-04 remains `mapped`. Regeneration retained 426 entities across 45 groups.

```text
bun test scripts/p17-catalog.test.ts
56 pass; 0 fail; 267 assertions

bun scripts/p17-catalog.ts --check
valid: 426 entities (419 required, 7 deferred; 244 validation obligations), 45 groups;
deterministic checklist

bun scripts/check-p17-package.ts --check
structurally valid; Phase 5 active; G5-05 active at 3/10 gates; next minimal-implementation

git diff --check
PASS
```

## Approved implementation compatibility amendment

The user approved the recommended G5-05 implementation amendment and serialized rerun in the
active root-goal session on 2026-07-23. During that rerun, the full required `EWP-P4A-TS02`
dependency selector initially reported 10 failures. Isolating the first failure proved a common
false-stale manifest boundary rather than ten independent acquisition regressions:

- retained update history correctly requires `manifest-bytes` for its role-domain operation and
  envelope digest;
- acquisition expected revisions correctly retain the generic `resource` digest used by the
  snapshot repository; and
- `createAcquisitionPlan` was comparing those intentionally different domains as if they were one
  value, so unchanged manifest bytes with identical semantics were rejected on rerun.

The compatibility amendment adds a private, non-serialized `manifestResourceRevision` to the
acquisition transition envelope. Install/uninstall planning carries it from the already-approved
snapshot; the operation image continues to carry the role-domain byte identity. The correction
also records the existing application/placement integration seam needed to pass the artifact
coordinator into undo, carry exact retained placement authority, and rebase the ledger after a
private rollback-artifact prerequisite.

Seven paths are added to the accepted ownership manifest:

- `packages/core/src/acquire/plan.ts`;
- `packages/core/src/acquire/run.ts`;
- `packages/core/src/acquire/uninstall.ts`;
- `packages/core/src/application/lifecycle-services.ts`;
- `packages/core/src/place/execute.ts`;
- `packages/core/src/place/types.ts`; and
- `packages/core/tests/place/execute.test.ts`.

The boundary expands from 62 to 69 paths: 34 product/architecture paths, 27 focused tests, six
acceptance owners/fixtures, and two governance files. Primary ownership remains seven entities and
required-now remains the same 14 serialized validations. No public schema, command, option, output,
journal field, artifact action, recovery record, or portable-file content changes. A fresh
immutable amendment review is required before the minimal-implementation gate is recorded.

## Implementation amendment review at b901bb8

Fresh independent reviewer `g5_05_implementation_amendment_review` examined only immutable commit
`b901bb8` and returned **GO** with no stable finding. The reviewer did not inspect or rely on the
dirty implementation worktree and made no edit.

The review independently confirmed:

- exactly seven paths were added and they cover the acquisition producers/validator, application
  entry seam, placement adapter/context seam, and its direct focused test without omitting another
  shared authority;
- the `manifest-bytes` operation-image domain and `resource` repository-snapshot domain are
  intentionally distinct, and the private `manifestResourceRevision` is the narrow compatibility
  bridge;
- the field is neither package-root exported nor encoded by a public codec, and no command, option,
  wire value, artifact action, or recovery format changes;
- the boundary arithmetic is exact at 69 unique paths: 34 product/architecture, 27 focused tests,
  six acceptance paths, and two governance files;
- all catalog evidence links resolve; and
- primary ownership remains seven entities, all 14 frozen validation commands are unchanged, and
  lifecycle state truthfully remains at three passed gates before minimal implementation.

No `P17-RV-P17-G5-05-F11` finding was opened. The approved implementation amendment may proceed
inside the exact 69-path boundary.

## Minimal implementation and targeted green at 86fd011

The minimal implementation is frozen by these ordered commits:

- `c9033e2` — canonical private retained-artifact preimage codec;
- `7f65c18` — narrow conditional retention authorization for update/undo artifact writes;
- `d93287b` — strict pair-null forward artifact history and role-domain identities;
- `8465e03` — durable artifact-history binding through every adjacent logical phase;
- `86fd011` — exact coordinator restoration, artifact rollback lineage, fresh occurrence identity,
  whole-group reduction, contiguous multi-occurrence suffix ordering, update-associated repair
  reversal, status/cleanup observation, application integration, and acquisition dual-domain
  compatibility.

The implementation reuses the existing artifact-pair coordinator, placement swap/recovery,
operation scheduler, approval path, and strict journal/undo v1 codecs. It adds no command, option,
public enum, output version, portable configuration, recovery record, or second mutation engine.
The complete implementation diff is contained by the reviewed 69-path manifest. Commit `86fd011`
passed its normal pre-commit gitleaks, Biome, boundary-lint, and conventional-message hooks.

The six primary validations then ran as six separate serialized processes on clean immutable
`86fd011`:

```text
bun test packages/cli/tests/contracts/undo.test.ts --test-name-pattern EWP-CMD-UNDO-TS03
1 pass; 8 filtered out; 0 fail; 74 assertions

bun test packages/cli/tests/contracts/update.test.ts --test-name-pattern EWP-CMD-UPDATE-TS07
1 pass; 9 filtered out; 0 fail; 23 assertions

bun test packages/cli/tests/contracts/options.test.ts --test-name-pattern EWP-OPT-TS08
1 pass; 42 filtered out; 0 fail; 66 assertions

bun test tests/ergonomics/phase/EWP-P5-TS05.test.ts --test-name-pattern EWP-P5-TS05
1 pass; 0 fail; 20 assertions

bun test tests/ergonomics/workflows/EWP-WF09.test.ts --test-name-pattern EWP-WF09
1 pass; 0 fail; 9 assertions

bun test tests/ergonomics/workflows/EWP-WF13.test.ts --test-name-pattern EWP-WF13
1 pass; 0 fail; 33 assertions
```

Targeted green is 6/6 tests, zero failures, zero skips, and 225 assertions. The formerly red
selectors now prove the exact missing update-to-undo capability; the two original green
characterizations remain green against the implemented boundary. P1-04 is validated by the complete
P5 bulk-safety owner but is not signed off before later lifecycle gates.

As an extra adjacent-contract receipt, the complete undo CLI contract file ran serialized on the
same immutable tree: 9 pass, zero fail, and 274 assertions. It covers old-history refusal, scope,
approval, aliases, migration, crash retry, and repeated no-toggle behavior in addition to TS03.

### Green execution receipt EWP-CMD-UNDO-TS03

- Validation ID: `EWP-CMD-UNDO-TS03`
- Command: `bun test packages/cli/tests/contracts/undo.test.ts --test-name-pattern EWP-CMD-UNDO-TS03`
- Exit status: `0`
- Result: PASS — committed update restores exact lock, manifest, live, and retained preimages with fresh identities and convergent rerun; 1 pass, 0 failed, 74 assertions.
- Revision: `86fd0111e00335d15e46e1ef6d90569abbd4eae8`

### Green execution receipt EWP-CMD-UPDATE-TS07

- Validation ID: `EWP-CMD-UPDATE-TS07`
- Command: `bun test packages/cli/tests/contracts/update.test.ts --test-name-pattern EWP-CMD-UPDATE-TS07`
- Exit status: `0`
- Result: PASS — update commits exact reversible manifest/lock lineage and owner-private retained preimages; 1 pass, 0 failed, 23 assertions.
- Revision: `86fd0111e00335d15e46e1ef6d90569abbd4eae8`

### Green execution receipt EWP-OPT-TS08

- Validation ID: `EWP-OPT-TS08`
- Command: `bun test packages/cli/tests/contracts/options.test.ts --test-name-pattern EWP-OPT-TS08`
- Exit status: `0`
- Result: PASS — advanced manifest/lock selector relations and custom-pair behavior remain exact; 1 pass, 0 failed, 66 assertions.
- Revision: `86fd0111e00335d15e46e1ef6d90569abbd4eae8`

### Green execution receipt EWP-P5-TS05

- Validation ID: `EWP-P5-TS05`
- Command: `bun test tests/ergonomics/phase/EWP-P5-TS05.test.ts --test-name-pattern EWP-P5-TS05`
- Exit status: `0`
- Result: PASS — bulk approval, refusal, scheduling, cancellation, two-group update undo, exact restoration, and rerun truth pass; 1 pass, 0 failed, 20 assertions.
- Revision: `86fd0111e00335d15e46e1ef6d90569abbd4eae8`

### Green execution receipt EWP-WF09

- Validation ID: `EWP-WF09`
- Command: `bun test tests/ergonomics/workflows/EWP-WF09.test.ts --test-name-pattern EWP-WF09`
- Exit status: `0`
- Result: PASS — moving update preview, execution, exact artifact/live undo, and no-toggle rerun pass; 1 pass, 0 failed, 9 assertions.
- Revision: `86fd0111e00335d15e46e1ef6d90569abbd4eae8`

### Green execution receipt EWP-WF13

- Validation ID: `EWP-WF13`
- Command: `bun test tests/ergonomics/workflows/EWP-WF13.test.ts --test-name-pattern EWP-WF13`
- Exit status: `0`
- Result: PASS — both partial-failure directions preserve independent group truth and no-write preview across the shared scheduler; 1 pass, 0 failed, 33 assertions.
- Revision: `86fd0111e00335d15e46e1ef6d90569abbd4eae8`

## Impacted green at c33a75f

All eight remaining frozen commands ran as separate serialized processes at
`c33a75fdd7466031b6aaaf5077bd497898f02348`. That revision contains the immutable
`86fd0111e00335d15e46e1ef6d90569abbd4eae8` implementation plus targeted-green governance only;
no product or test file changed between the primary and impacted runs.

```text
bun test packages/cli/tests/contracts/apply.test.ts --test-name-pattern EWP-CMD-APPLY-TS09
1 pass; 13 filtered out; 0 fail; 3 assertions

bun test packages/cli/tests/contracts/apply.test.ts --test-name-pattern EWP-CMD-APPLY-TS11
1 pass; 13 filtered out; 0 fail; 6 assertions

bun test packages/cli/tests/contracts/apply.test.ts --test-name-pattern EWP-CMD-APPLY-TS13
1 pass; 13 filtered out; 0 fail; 19 assertions

bun test packages/cli/tests/contracts/sync.test.ts --test-name-pattern EWP-CMD-SYNC-TS09
1 pass; 9 filtered out; 0 fail; 111 assertions

bun test packages/cli/tests/contracts/update.test.ts --test-name-pattern EWP-CMD-UPDATE-TS06
1 pass; 9 filtered out; 0 fail; 61 assertions

bun test packages/cli/tests/contracts/update.test.ts --test-name-pattern EWP-CMD-UPDATE-TS08
1 pass; 9 filtered out; 0 fail; 25 assertions

bun test packages/cli/tests/contracts/update.test.ts --test-name-pattern EWP-CMD-UPDATE-TS09
1 pass; 9 filtered out; 0 fail; 62 assertions

bun test tests/ergonomics/phase/EWP-P4A-TS02.test.ts --test-name-pattern EWP-P4A-TS02
27 pass; 0 fail; 314 assertions
```

The impacted run is 34/34 tests, zero failures, zero skips, and 601 assertions. Combined with the
six exact primary receipts above, all 14 frozen commands are green. The downstream matrix proves
that retained update preimages do not regress apply continuation/output truth, direct sync group
truth, update artifact/approval/failure semantics, or the complete P4A acquisition, placement,
repair, cancellation, crash, and convergence contract. P1-04 may therefore advance from mapped to
validated, but remains unsigned until review and traceability closure.

## Refactor gate at 7ec493c

The post-green refactor assessment intentionally makes no further product or test change. The
green implementation already performed the useful structural extraction:

- `undo/artifacts.ts` owns retained-history discovery, contiguous suffix selection, whole-group
  reduction, inverse construction, and artifact restoration planning;
- `retained-preimage-codec.ts` is the single strict codec for private retained bytes;
- the existing coordinator, placement swap/recovery, and scheduler remain the only mutation and
  execution authorities;
- the acquisition compatibility bridge keeps operation-image `manifest-bytes` and repository
  `resource` revisions distinct through one private, nonserialized field; and
- the external artifact lifecycle binding is limited to the private rollback-artifact inverse,
  avoiding a second generalized lifecycle path.

At `7ec493cdffe8b5648f5e70d815d2e13315e0dafe`, `git diff --name-only 86fd011..HEAD`
contained only the generated checklist, catalog, and this evidence file. A scan of all 37 files in
the implementation commit found no `SKILLSMITH_DEBUG_THROW`, console debug/log calls, TODO/FIXME,
test/describe skips, or TypeScript suppression directives. `bun run typecheck`,
`bun run lint:boundaries`, and diff checks passed. Because all 14 exact commands were already green
and no cleanup defect remained, additional refactoring would add churn without narrowing an
authority or removing duplication. The immutable implementation proceeds unchanged to fresh
independent adversarial review.

## Independent adversarial review NO-GO at a5a0897

Fresh independent reviewer `g5_05_adversarial_review` inspected immutable
`a5a08977a3f5947f167a42c874a1db1f946d9f9a` and returned **NO-GO**. The reviewer made no edit and
opened four stable findings:

- **P17-RV-P17-G5-05-F11 (high):** `historyDependencies()` and its protection/cleanup closures join
  only rollback parents and children. They do not join a forward artifact carrier to every
  placement journal and retained preimage in the same update occurrence. Under history pressure,
  the carrier can therefore become the cleanup victim while a newest placement remains selectable,
  violating the plan's indivisible occurrence contract.
- **P17-RV-P17-G5-05-F12 (high):** forward artifact history can resume a prepared transaction only
  when the next invocation has the same operation ID. A changed or abandoned update has no
  authenticated abort/orphan-cleanup path, and bounded cleanup cannot remove pending transactions.
- **P17-RV-P17-G5-05-F13 (medium):** `packages/core/src/acquire/execute.ts`,
  `packages/core/src/reconcile/execute.ts`, and
  `packages/core/tests/reconcile/execute.test.ts` were changed by the implementation sequence but
  omitted from the accepted 69-path manifest.
- **P17-RV-P17-G5-05-F14 (medium):** the completed selectors genuinely passed, but none covered the
  missing greater-than-256 occurrence unit or abandoned prepared carrier. The impacted-green and
  refactor narratives therefore overstate coverage.

The review confirmed that exact bytes/mode, pair-null rollback children, fresh IDs, group-level
reduction, the private dual-digest bridge, public-schema compatibility, and ordinary crash/rerun
paths held. The correction order is ownership amendment, occurrence dependency closure,
prepared-orphan abort/cleanup, missing adversarial tests, proportional gates, all 14 exact commands,
and a fresh independent review.

The earlier statement that the complete implementation diff was contained by 69 paths is
superseded: all product/test implementation changes require the corrected 72-path manifest. The
earlier passing receipts remain exact historical execution results, but impacted green and refactor
return to pending; P1-04 returns to mapped until the corrected impacted boundary passes.

## Adversarial correction amendment

The corrected ownership manifest adds exactly three paths:

- `packages/core/src/acquire/execute.ts`;
- `packages/core/src/reconcile/execute.ts`; and
- `packages/core/tests/reconcile/execute.test.ts`.

The boundary expands from 69 to 72 paths: 36 product/architecture paths, 28 focused tests, six
acceptance owners/fixtures, and two governance files. No public command, option, output, schema,
journal field, recovery record, artifact action, or portable content changes. Primary ownership
remains seven entities and the 14 frozen serialized commands are unchanged. A fresh immutable
amendment review is required before correction tests or product edits proceed.

## Adversarial correction amendment review at 78ba654

Fresh independent reviewer `g5_05_correction_amendment_review` examined only immutable
`78ba6543312a1115fbb4dd4d54590ac3cd47526c` and returned **GO** with no amendment finding. The
reviewer confirmed F11–F14 against `a5a0897`, exact 69-to-72 containment, 36/28/6/2 arithmetic,
complete product/test containment across all six implementation commits, truthful gate resets,
byte-identical seven-primary and 14-command boundaries, and no public-contract or non-goal
expansion. Catalog, package structure, deterministic checklist, evidence anchors, and diff checks
passed. Correction tests and implementation may proceed inside the exact 72-path boundary.

## Adversarial correction test-first evidence at 23eb290

Immutable test commit `23eb2905012f849c4031f14e8f01eb9e402ee98e` adds the missing F11/F12
adversarial boundary before product correction. Both focused files typecheck and fail only on the
intended absent behavior:

```text
bun test packages/core/tests/artifacts/ledger-history.test.ts
3 pass; 1 fail; 15 assertions
failure: artifact-occurrence-carrier is a pending cleanup victim while its placement is protected

bun test packages/core/tests/update/history.test.ts
3 pass; 2 fail; 32 assertions
failures: absent and present superseded prepared carriers remain in durable transactions
```

The exact-repeat characterization is green before correction, preventing a group-ID-only closure
that would conflate separate occurrences. The orphan tests also prove that the next operation can
otherwise commit successfully while leaving the prior prepared authority behind. F11 and F12 are
therefore reproducible red findings; implementation must make these tests green without weakening
the earlier 14-command boundary.

## Adversarial correction implementation and impacted green at e778e6b

Immutable correction commit `e778e6bf12195eb4e909a91a2987c85ca80746e1` closes F11 and F12
without changing the accepted public or serialized boundary.

- `ledger-history.ts` partitions deterministic update groups into commit-order occurrences. A
  carrier-role repeat or the first carrier after placement work starts a new occurrence, so exact
  repeated transitions are not joined by group identity alone. Artifact carriers and every unique
  update/repair placement in the occurrence are sibling dependencies. Existing rollback
  parent/child traversal then makes the whole occurrence indivisible during protection, effective
  capacity expansion, victim selection, deletion projection, and restart.
- `update/history.ts` strictly discovers every pending update-artifact journal before a later
  operation mutates. Physical-before prepared/staged/backed-up carriers enter a restartable cleanup
  protocol: first persist a pair-null rollback marker with released retention, then remove the
  authenticated owner-private envelope, then commit the rollback. A crash before deletion leaves
  the marker plus envelope; a crash after deletion leaves the same marker without the envelope.
  Both states converge on the next invocation. Physical-after backed-up/live carriers finalize
  forward, while corrupt envelopes, malformed journals, and physical mismatches refuse before the
  next physical binding runs.
- `logical-transactions.ts` adds only the narrow internal retention-release projection required by
  that protocol. It remains absent from the package root and requires the exact pair-null update
  manifest/lock workflow plus stable file identity.

The correction-focused immutable replay is green:

```text
bun test packages/core/tests/artifacts/ledger-history.test.ts packages/core/tests/update/history.test.ts packages/core/tests/place/logical-transactions.test.ts
20 pass; 0 fail; 108 assertions
```

This covers greater-than-256 complete-occurrence protection, two placements in one occurrence,
pending-child closure, exact-repeat separation, absent/present prepared orphans, cleanup-marker
restart with the envelope present or already removed, corrupted envelope/journal refusal, and the
staged/backed-up/live physical recovery matrix. The restartable shared-backup ledger-writer case
also passed in isolation with eight assertions. Broader core replays produced only combined-load
timeouts; each timed selector passed in a clean isolated process, including both lifecycle cursor
orders and retained undo artifact restoration.

All six frozen primary selectors passed as separate serialized processes at the correction
revision:

```text
EWP-CMD-UNDO-TS03    1 pass; 0 fail; 74 assertions
EWP-CMD-UPDATE-TS07  1 pass; 0 fail; 23 assertions
EWP-OPT-TS08         1 pass; 0 fail; 66 assertions
EWP-P5-TS05          1 pass; 0 fail; 20 assertions
EWP-WF09             1 pass; 0 fail; 9 assertions
EWP-WF13             1 pass; 0 fail; 33 assertions
```

The primary result is 6/6 tests, zero failures, and 225 assertions. Options TS08 and P5-TS05 each
needed one clean rerun after a harness timeout; the authoritative reruns completed with their full
expected assertion counts.

All eight frozen dependency commands also passed separately and serialized:

```text
EWP-CMD-APPLY-TS09    1 pass; 0 fail; 3 assertions
EWP-CMD-APPLY-TS11    1 pass; 0 fail; 6 assertions
EWP-CMD-APPLY-TS13    1 pass; 0 fail; 19 assertions
EWP-CMD-SYNC-TS09     1 pass; 0 fail; 111 assertions
EWP-CMD-UPDATE-TS06   1 pass; 0 fail; 61 assertions
EWP-CMD-UPDATE-TS08   1 pass; 0 fail; 25 assertions
EWP-CMD-UPDATE-TS09   1 pass; 0 fail; 62 assertions
EWP-P4A-TS02         27 pass; 0 fail; 314 assertions
```

The dependency result is 34/34 tests, zero failures, and exactly 601 assertions. Sync TS09 first
encountered its transient prepared-journal observation race and update TS09 first exceeded its
harness timeout; their clean serialized reruns passed the complete 111- and 62-assertion matrices.
Thus all 14 frozen commands are green at `e778e6b`, and P1-04 returns from mapped to validated.

Proportional recovery gates are also green:

```text
bun run test:smoke
5 pass; 0 fail; 3,737 assertions

bun run test:smoke:p2-ts04:recovery
4 pass; 0 fail; 453 assertions

bun test tests/ergonomics/phase/EWP-P2-TS04.test.ts
19 pass; 0 fail; 49,699 assertions
```

## Adversarial correction refactor gate at e778e6b

The correction keeps each new authority at its existing narrow owner: bounded-history dependency
construction remains in `ledger-history.ts`, physical retained-artifact recovery remains in
`update/history.ts`, and the pure transition exception remains in `logical-transactions.ts`. The
durable cleanup marker reuses journal v1 and the existing ledger persistence gateway; no second
recovery record, artifact coordinator, public export, or wire shape was introduced. Corrupt and
malformed authority is validated before cleanup-marker mutation, and once the marker is durable,
envelope deletion is restartable from either physical state. Additional extraction would require a
new path outside the accepted 72-path manifest without narrowing an authority. The correction is
therefore ready for a fresh independent adversarial review unchanged.

## Corrected adversarial review NO-GO at e497dcc

Fresh independent reviewer `g5_05_corrected_adversarial_review` inspected immutable
`e497dcceff58db4ea8e6562114a60562f9bcea95`, made no edit, and returned **NO-GO** with one stable
finding:

- **P17-RV-P17-G5-05-F15 (high):** recovery discovery begins from the two exact workflow strings
  `update-artifact-history` and `update-artifact-orphan-cleanup`. A codec-valid pair-null update
  artifact journal whose workflow alone is corrupted therefore falls outside both strict authority
  validators. A later different update can physically execute and commit while leaving the
  malformed pending journal and retained envelope behind. The existing corrupt-journal test
  changes `command` while preserving the recognized workflow, so it does not cover this evasion.

The reviewer recommended structural artifact-journal discovery independent of command/workflow,
followed by exact recognized-authority validation before persistence, envelope deletion, or a new
physical binding. Both forward and cleanup workflow-only corruption require negative tests proving
failure, zero physical executions, unchanged artifact bytes, and unchanged pending/envelope state.
Legitimate `undo-artifact-history` rollback state must remain explicitly distinct through its
source-parent identity.

The review confirmed that F11 whole-occurrence closure, F12 durable marker ordering for discovered
journals, F13 exact 72-path containment, and the corrected F14 evidence otherwise hold. Focused,
smoke, recovery, typecheck, boundary, catalog, and package checks independently passed; no public
field, enum, export, or recovery record was added. Impacted-green and refactor return to pending,
and P1-04 returns to mapped until F15 is reproduced red, corrected, rerun, and independently
reviewed.
