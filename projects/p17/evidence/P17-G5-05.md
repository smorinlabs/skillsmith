# P17-G5-05 evidence

## Mapping and prerequisites

Mapping began from clean governance revision
`b5d87002fc70f84cf137b224c6054c61f963e01c` on 2026-07-23. The starting catalog SHA-256 was
`bde0c3a3fd16aa1b616a20ea0613a314cb96fbb4e59940960566d8a7831d9e15`.

- Phase 5 is active with its entry gate passed.
- Direct prerequisites P17-G5-01, P17-G5-02, and P17-G5-03 are signed off at all ten lifecycle
  gates with resolving evidence.
- G5-05 starts pristine and planned: no owned files, test commands, implementers, reviewer,
  evidence, executable targets, or passed gates.
- Primary ownership is exactly seven entities: EWP-CMD-UNDO-TS03, EWP-CMD-UPDATE-TS07,
  EWP-OPT-TS08, EWP-P5-TS05, EWP-WF09, EWP-WF13, and P1-04.
- Required-now is exactly 14 validations: the six primary selectors plus APPLY-TS09/11/13,
  SYNC-TS09, UPDATE-TS06/08/09, and P4A-TS02 from signed dependencies.
- Downstream coverage is empty.
- COMMAND:update, COMMAND:undo, D-010, D-011, CF-003, CF-008, CF-026, CF-027, CF-031, CF-040,
  P1-11, and P2-02 are signed. P1-04 is the only planned coverage authority in the group.

The detailed mapped/Ready candidate is `projects/p17/plans/P17-G5-05.md`. Its proposed ownership
manifest contains 47 unique paths and its proposed implementation-free boundary contains the six
primary validation owners plus one hermetic shared fleet.

## Existing implementation and gap audit

Signed dependencies already implement one deterministic scheduler, exact bulk approval,
fail-fast/continue, cancellation, exits, custom artifact pairs, update ref/pin/live/lock behavior,
prior-store retention, and scope-aware undo for every operation family except update.

The exact product gap is explicit in `packages/core/src/undo/observe.ts`: update history is
recognized and rejected as `undo-update-unavailable` because committed update placement journals do
not yet carry authoritative manifest/lock history. The current implementation must not infer a
custom artifact pair or prior portable intent from cwd/current bytes. The candidate therefore adds
strict internal artifact lineage to forward update and consumes it through the signed undo,
artifact-coordinator, placement-reversal, scheduler, approval, and output authorities.

Six planned validation owners do not yet contain executable selectors:

- EWP-CMD-UNDO-TS03;
- EWP-CMD-UPDATE-TS07;
- EWP-OPT-TS08 (references exist only in inventory/governance tests);
- EWP-P5-TS05 (inventory reference only);
- EWP-WF09 (migration-ledger reference only); and
- EWP-WF13.

## Signed dependency characterization

The eight already-owned dependency selectors were replayed before mapping. The first attempt ran
eight independent stateful CLI processes concurrently. Six passed, while UPDATE-TS06 and TS09 both
reported `apply-ledger-bootstrap` because their stateful fixture processes collided at ledger
directory preparation. Neither reproduced when run serially, and both passed inside the preceding
complete repository gate with `--max-concurrency 1`. This is a fixture scheduling constraint, not a
product failure; G5-05 records serialized public-process commands as mandatory.

Final serialized/current receipts:

```text
bun test packages/cli/tests/contracts/apply.test.ts --test-name-pattern EWP-CMD-APPLY-TS09
1 pass; 0 fail; 3 assertions

bun test packages/cli/tests/contracts/apply.test.ts --test-name-pattern EWP-CMD-APPLY-TS11
1 pass; 0 fail; 6 assertions

bun test packages/cli/tests/contracts/apply.test.ts --test-name-pattern EWP-CMD-APPLY-TS13
1 pass; 0 fail; 19 assertions

bun test packages/cli/tests/contracts/sync.test.ts --test-name-pattern EWP-CMD-SYNC-TS09
1 pass; 0 fail; 111 assertions

bun test packages/cli/tests/contracts/update.test.ts --test-name-pattern EWP-CMD-UPDATE-TS06
1 pass; 0 fail; 61 assertions

bun test packages/cli/tests/contracts/update.test.ts --test-name-pattern EWP-CMD-UPDATE-TS08
1 pass; 0 fail; 25 assertions

bun test packages/cli/tests/contracts/update.test.ts --test-name-pattern EWP-CMD-UPDATE-TS09
1 pass; 0 fail; 62 assertions

bun test tests/ergonomics/phase/EWP-P4A-TS02.test.ts --test-name-pattern EWP-P4A-TS02
27 pass; 0 fail; 314 assertions
```

The preceding full terminal gate on the same product revision plus documentation-only lifecycle
changes reported 3,143 pass, 28 intentional live-environment skips, zero failures, and 104,168
assertions across 3,171 tests and 341 files. It is characterization evidence, not a G5-05 terminal
sign-off.

## Mapping result

The candidate maps one coherent missing behavior rather than fourteen independent implementations:

- update must durably retain exact selected artifact before/after lineage;
- undo must correlate that lineage with the already-selected update placement group;
- the existing artifact coordinator and placement reversal authorities execute the inverse work;
- the existing scheduler/approval/output authorities retain their signed semantics; and
- the six new selectors complete the dependency-level matrices while the eight signed selectors
  guard against regression.

No implementation, selector target, red/green receipt, Ready acceptance, reviewer GO, or later
lifecycle gate is claimed in this mapping record.

## Ready review at e7405cc

The first fresh independent Ready review examined immutable commit
`e7405ccc549b7034b203af06d6dc29c57d65f467` without editing the worktree. It returned **NO-GO**
with five stable blocking findings:

- `P17-RV-P17-G5-05-F01`: operation manifest snapshots contain normalized values and hashes, not
  the original authored bytes, trivia, newline form, or filesystem mode. The coordinator removes
  its attempt backup after commit, so the original candidate could not perform its claimed exact
  manifest restoration.
- `P17-RV-P17-G5-05-F02`: assigning artifact inverses to the first live pair contradicted all three
  current authorities. Artifact execution and logical history require pair-null identity, while
  `undo@1` required every executable operation to be pair-bound and did not admit artifact kinds in
  its undo vocabulary.
- `P17-RV-P17-G5-05-F03`: journal v1 can represent pair-null artifact work, but fresh rollback
  lineage and logical reversal currently require a placement inverse and legacy pair shadow, so an
  artifact rollback child would make the ledger invalid.
- `P17-RV-P17-G5-05-F04`: bounded-history dependency closure covers rollback parent/child only.
  It can evict an artifact carrier while retaining the newest selectable placement journals from
  the same update group.
- `P17-RV-P17-G5-05-F05`: deterministic update `groupId` and artifact operation IDs can recur when
  a resolution is revisited. Group ID alone cannot select one historical carrier occurrence.

The reviewer independently confirmed that the 7-primary/14-required mapping and lifecycle state
remain truthful, old pre-lineage refusal is correct, custom machine-bound paths can be retained
once exact occurrence authority exists, and all structural checks pass. Its characterization run
reported 73 pass, zero fail, and 485 assertions across the relevant codec, history/writer, undo,
and update suites. No file was changed by the reviewer, and Ready remained pending.

## Accepted Ready amendment candidate

All five findings are accepted as blockers. The plan is amended without advancing lifecycle state:

- **F01:** each changing manifest or lock carrier declares a conditional retention resource,
  publishes its prepared journal under the existing locks, creates an owner-private durable backup
  envelope containing the exact regular-file bytes plus a canonical source-role/mode receipt,
  fsyncs it, records its complete digest in `actual.retained`, and only then invokes the existing
  artifact coordinator. The envelope digest authenticates the mode without adding a journal field.
  The pending logical journal remains the sole added recovery authority; the envelope is retained
  restoration material, not a second recovery format. Undo uses the validated retained bytes/mode
  rather than normalized reconstruction, and ordinary bounded-history cleanup owns eventual
  deletion.
- **F02:** inverse artifact operations remain group-level and pair-null. `group.operations`
  partitions into its artifact prefix plus the live-only pair vectors. Pair outcome stays pair-local;
  group outcome/failure reduces all group results. Strict negative codec coverage freezes omission,
  duplication, order, pair-binding, and artifact failure/cancellation truth without adding a public
  field or enum.
- **F03:** logical history gains an explicit schema-preserving artifact parent/child branch with no
  legacy shadow. Exact parent operation/transaction, path, images, retained backup, phase,
  command/workflow, and one-child rules are validated through prepared, resume, commit, and hostile
  cases.
- **F04:** the bounded selector treats the exact forward update occurrence—artifact carriers,
  correlated placement journals, rollback children, and retained resources—as one dependency
  unit. Protection expands effective capacity; exclusion and restartable cleanup remove the whole
  unit. More-than-256-entry, pending-child, two-tool, custom-pair, and cleanup-restart tests are
  mandatory.
- **F05:** update group identity adds a non-sensitive versioned digest of exact manifest/lock
  before/after identities. Historical correlation still starts from the selected placement source
  transaction and commit/image chain rather than assuming group IDs are globally unique. A->B->C->B,
  exact-repeat, different-trivia, intervening-history, two-tool, and custom-pair cases are mandatory.

The ownership manifest expands from 47 to 49 unique paths by adding the shared artifact execution
authority and its focused test. The existing planned `update/history.ts` and `undo/artifacts.ts`
boundaries own retention/correlation; the artifact coordinator remains the only portable-file
mutator. Public-process validation remains serialized. This amendment is a new Ready candidate and
requires structural validation, an immutable commit, and a fresh independent rerun before Ready
can pass.

## Ready rerun at d13d907

The fresh independent rerun examined immutable amendment commit
`d13d907ed2ce5421581df8db0e27c19339b81108` and returned **NO-GO** with five new stable blockers:

- `P17-RV-P17-G5-05-F06`: the proposed carrier jumped `prepared -> backed-up` and added
  `actual.retained` after transaction identity was frozen. Existing authority requires every
  adjacent phase and exact retained identity across ordinary transitions.
- `P17-RV-P17-G5-05-F07`: the wrapper had no exact codec or compatible observation/hash contract.
  Status treated the wrapper as direct manifest/lock bytes, cleanup required equal digests over raw
  bytes, and the plan did not own `status/read.ts`.
- `P17-RV-P17-G5-05-F08`: current public pair actions cannot replace a present manifest with raw
  bytes, restore exact lock bytes, or set the retained mode. Coordinator source/types/tests were
  outside the candidate boundary.
- `P17-RV-P17-G5-05-F09`: a pair-null inverse with the same update group and kind reproduces the
  forward semantic operation ID. A fresh group requires every live pair/operation plus placement
  logical/swap authority to use the new identity coherently.
- `P17-RV-P17-G5-05-F10`: hostile codec coverage was incomplete because the candidate changed
  ledger/plan codecs without owning their existing focused `artifacts/codec.test.ts` authority.

Prior F02's pair-null partition and reduction design, F03's artifact branch, F04's atomic occurrence
unit, and F05's occurrence correlation were accepted conceptually. F01 remained open through
F06-F08. The reviewer confirmed the contiguous-suffix/custom-pair refusal, mapped lifecycle state,
and structural package remained truthful. Its focused characterization reported 75 pass, zero
fail, and 485 assertions. Catalog tests reported 56 pass, zero fail, and 267 assertions; catalog,
package, PR-openable, plan-structure, and diff checks passed. The reviewer made no file change.

## Accepted second Ready amendment candidate

F06-F10 are accepted, and Ready remains pending. The corrected candidate makes the following
implementable commitments:

- **F06:** a forward artifact journal begins `prepared` with one declared conditional resource and
  no factual retained row. After its deterministic preimage envelope is durable, exactly one
  narrowly validated `prepared -> staged` transition may add the matching row. All later phases are
  adjacent and keep that row identical. Prepared/staged/backed-up/live crash and abort behavior is
  explicit; no forward artifact mutation occurs before `backed-up`.
- **F07:** a new private `retained-preimage@1` codec canonically binds kind/version, forward
  operation, role, absolute path, exact preimage bytes/mode, and expected forward-after
  digest/mode. The retained repository revision is the resource-domain envelope digest; content
  hash is the decoded role-domain payload digest. Status and cleanup parse the same codec, verify
  both domains and owner-private filesystem facts, and fail closed on noncanonical or hostile
  material.
- **F08:** exact restore is an internal coordinator request, not a new public
  `HumanManifestAction`/`GeneratedLockAction`. It validates the current role digest/mode and target
  operation image, then reuses the coordinator's existing desired-role staging, recovery record,
  install, verification, and cleanup machinery with exact target bytes/mode. Manifest precedes
  lock when both restore, so the final lock is checked against the restored manifest. The recovery
  record format is unchanged.
- **F09:** undo derives one deterministic occurrence-specific group from the exact ordered source
  transaction/operation lineage. It recomputes every live pair and operation ID under that group;
  artifact inverses remain pair-null and receive fresh IDs. Placement plan/swap/logical and ledger
  validation accept only the exactly derived child pair while retaining source authority solely in
  existing parent/source fields.
- **F10:** focused codec ownership now includes `packages/core/tests/artifacts/codec.test.ts`.

The correction also closes the adjacent scheduler invariant: only update/undo pair-null manifest or
lock writes may carry the one conditional artifact retention resource; all other artifact
prerequisites retain the signed non-reversible rule. The ownership manifest expands from 49 to 62
unique paths: 28 product/architecture paths, 26 focused tests, six acceptance owners/fixtures, and
two governance files. It adds coordinator source/types/test, the private retained-preimage codec and
test, status read authority, placement plan/swap and tests, focused codec coverage, and scheduler
source/test. This is still a mapped/Ready candidate and requires structural validation, an immutable
commit, and another fresh independent rerun before Ready can pass.

## Final Ready review at b779a87

The independent reviewer examined immutable second-amendment commit
`b779a87e62fb7e5902a15b3bdbae625372b97042` without editing the worktree and returned **GO** for
P17-G5-05 Ready. It found no new stable blocker or ownership omission and closed every prior
finding:

- F01/F07/F08 are closed by the canonical retained-preimage codec, dual-domain observation and
  cleanup, and private exact coordinator restoration;
- F02 is closed by strict pair-null artifact carriers and schema-preserving group/pair reductions;
- F03 is closed by explicit artifact rollback lineage plus freshly derived placement identities;
- F04 is closed by atomic retention/eviction of the complete update occurrence;
- F05 is closed by transition-specific, source-transaction, commit-order, image-chain, and
  nearest-unconsumed occurrence correlation;
- F06 is closed by the one narrow adjacent `prepared -> staged` enrichment and explicit crash/
  abort behavior;
- F09 is closed by a fresh undo-occurrence group and complete live pair/operation plus artifact
  operation identity remapping; and
- F10 is closed by explicit focused artifact-codec test ownership.

Reviewer verification on the immutable commit reported 276 pass, zero fail, and 2,082 assertions
across 16 focused files. Catalog validation reported 56 pass, zero fail, and 267 assertions.
Catalog check, package structural check, PR-openable check, plan-structure check, exact 62-path
plan/catalog agreement, immutable HEAD, and clean-worktree checks all passed.

## Accepted Ready result

The Ready GO is accepted for immutable commit
`b779a87e62fb7e5902a15b3bdbae625372b97042`. P17-G5-05 advances from `mapped` to `ready`; only its
mapped and Ready gates are passed. The exact implementation boundary is the 62-path manifest in the
accepted plan/catalog, the primary boundary remains seven entities, and required-now remains 14
serialized validation commands. Test-first, implementation, green, refactor, later adversarial,
traceability, and sign-off gates remain pending. P1-04 remains mapped because coverage entities do
not have an independent Ready lifecycle state.

## Test-first evidence

The implementation-free seven-path boundary is frozen at
`6e3ae9de8742647bebdc7fe8b6ed29827020b2d8`:

- `packages/cli/tests/contracts/undo.test.ts` adds EWP-CMD-UNDO-TS03 with ordinary-update and pin
  transition cases, exact artifact/live restoration, fresh inverse identities, and repeated
  no-toggle convergence;
- `packages/cli/tests/contracts/update.test.ts` adds EWP-CMD-UPDATE-TS07 with two-role exact
  preimage retention, mode preservation, strict envelope shape, owner-private storage, pair-null
  artifact carriers, and redaction canaries;
- `packages/cli/tests/contracts/options.test.ts` adds EWP-OPT-TS08 with the full declarative
  relation matrix plus spawned absolute/portable-relative custom-pair and ordinary-omission
  behavior;
- `tests/ergonomics/fixtures/p5-batch/fleet.ts` composes the signed hermetic update and sync fleets,
  state snapshots, cancellation, and public-process parsing without adding product seams;
- `tests/ergonomics/phase/EWP-P5-TS05.test.ts` composes real sync/update/undo dry-run identity,
  fail-fast/continuation policy, bulk refusal/approval, deterministic group truth, cancellation,
  rerun, and no-write checks;
- `tests/ergonomics/workflows/EWP-WF09.test.ts` encodes targetless refusal, moving/fixed selection,
  explicit ref/pin check, bulk preview, source failure, check/dry-run identity, execute, and exact
  undo restoration; and
- `tests/ergonomics/workflows/EWP-WF13.test.ts` cross-products Claude-success/Codex-failure and
  Codex-success/Claude-failure over install/apply/sync/update/promote through the public scheduler,
  proves fresh rerun convergence, and retains a real public-process partial-update/no-write case.

The first P5 draft exposed two test-authoring mismatches before the immutable boundary: update
reports publish `options.continueOnError` rather than an undo-style `selection.batchPolicy`, and a
single-target update correctly requires no bulk approval. The corrected selector uses the public
fields and a two-group `--all --yes` execution. EWP-OPT-TS08 likewise corrected the signed artifact
selection source from `explicit-file` to `explicit`, treats an escaping relative selector as the
existing refusal behavior, and compares stable operation identity rather than path-spelling-bound
revision preconditions. WF13 enriches wire operations with the public dependency metadata and
normalizes legacy wire-only before images before passing them to the strict public scheduler. None
of these corrections changes product behavior or disguises a capability failure.

Final primary result:

- EWP-OPT-TS08 passes as intended characterization/integration: 1 pass, 0 fail, 66 assertions;
- EWP-WF13 passes as intended dependency-scheduler composition: 1 pass, 0 fail, 33 assertions;
- EWP-CMD-UPDATE-TS07 fails when a pin transition reaches the existing reconciliation executor,
  which rejects `write-manifest` before exact artifact retention can commit;
- EWP-CMD-UNDO-TS03 and EWP-WF09 fail only at the explicit `undo-update-unavailable` milestone
  after an ordinary update has committed;
- EWP-P5-TS05 fails at `undo-operation-family` after sync characterization, cancellation,
  refusal, dry-run, approved two-group update, and update rerun have all passed; and
- no selector fails because of a missing file/module, syntax/type error, timeout, skip, TODO,
  public-network dependency, or fixture bootstrap error.

The two already-green characterizations remain catalog `planned` at this gate. Catalog `passing`
is tied to targeted-green and cannot truthfully jump over minimal implementation. The four honest
capability reds advance to `failing` with exact targets and immutable receipts. This preserves both
the observed execution truth and the ordered group lifecycle.

### Red execution receipt EWP-CMD-UPDATE-TS07

- Validation ID: `EWP-CMD-UPDATE-TS07`
- Command: `bun test packages/cli/tests/contracts/update.test.ts --test-name-pattern EWP-CMD-UPDATE-TS07`
- Exit status: `1`
- Result: FAIL — pin execution reaches the intended missing artifact path and reports `reconcile-execution-operation-unsupported` for `write-manifest`; 0 pass, 1 fail, 3 assertions.
- Revision: `6e3ae9de8742647bebdc7fe8b6ed29827020b2d8`

### Red execution receipt EWP-CMD-UNDO-TS03

- Validation ID: `EWP-CMD-UNDO-TS03`
- Command: `bun test packages/cli/tests/contracts/undo.test.ts --test-name-pattern EWP-CMD-UNDO-TS03`
- Exit status: `1`
- Result: FAIL — committed ordinary update reaches the exact `undo-update-unavailable` lock-restoration milestone; 0 pass, 1 fail, 6 assertions.
- Revision: `6e3ae9de8742647bebdc7fe8b6ed29827020b2d8`

### Red execution receipt EWP-P5-TS05

- Validation ID: `EWP-P5-TS05`
- Command: `bun test tests/ergonomics/phase/EWP-P5-TS05.test.ts --test-name-pattern EWP-P5-TS05`
- Exit status: `1`
- Result: FAIL — the completed sync/policy/approval/cancellation prefix reaches update undo and reports the unsupported retained operation family; 0 pass, 1 fail, 12 assertions.
- Revision: `6e3ae9de8742647bebdc7fe8b6ed29827020b2d8`

### Red execution receipt EWP-WF09

- Validation ID: `EWP-WF09`
- Command: `bun test tests/ergonomics/workflows/EWP-WF09.test.ts --test-name-pattern EWP-WF09`
- Exit status: `1`
- Result: FAIL — the normative selection/check/dry-run/execute prefix reaches the exact `undo-update-unavailable` milestone; 0 pass, 1 fail, 7 assertions.
- Revision: `6e3ae9de8742647bebdc7fe8b6ed29827020b2d8`

### Characterization execution receipt EWP-OPT-TS08

- Validation ID: `EWP-OPT-TS08`
- Command: `bun test packages/cli/tests/contracts/options.test.ts --test-name-pattern EWP-OPT-TS08`
- Exit status: `0`
- Result: PASS — live declarations, relation validation, and spawned custom-pair/omission behavior agree; 1 pass, 0 fail, 66 assertions.
- Revision: `6e3ae9de8742647bebdc7fe8b6ed29827020b2d8`

### Characterization execution receipt EWP-WF13

- Validation ID: `EWP-WF13`
- Command: `bun test tests/ergonomics/workflows/EWP-WF13.test.ts --test-name-pattern EWP-WF13`
- Exit status: `0`
- Result: PASS — both failure directions preserve independent truth and converge across the five-command public scheduler matrix; 1 pass, 0 fail, 33 assertions.
- Revision: `6e3ae9de8742647bebdc7fe8b6ed29827020b2d8`

All eight dependency selectors were then replayed as serialized public-process commands on the
same test boundary:

```text
bun test packages/cli/tests/contracts/apply.test.ts --test-name-pattern EWP-CMD-APPLY-TS09
1 pass; 0 fail; 3 assertions

bun test packages/cli/tests/contracts/apply.test.ts --test-name-pattern EWP-CMD-APPLY-TS11
1 pass; 0 fail; 6 assertions

bun test packages/cli/tests/contracts/apply.test.ts --test-name-pattern EWP-CMD-APPLY-TS13
1 pass; 0 fail; 19 assertions

bun test packages/cli/tests/contracts/sync.test.ts --test-name-pattern EWP-CMD-SYNC-TS09
1 pass; 0 fail; 111 assertions

bun test packages/cli/tests/contracts/update.test.ts --test-name-pattern EWP-CMD-UPDATE-TS06
1 pass; 0 fail; 61 assertions

bun test packages/cli/tests/contracts/update.test.ts --test-name-pattern EWP-CMD-UPDATE-TS08
1 pass; 0 fail; 25 assertions

bun test packages/cli/tests/contracts/update.test.ts --test-name-pattern EWP-CMD-UPDATE-TS09
1 pass; 0 fail; 62 assertions

bun test tests/ergonomics/phase/EWP-P4A-TS02.test.ts --test-name-pattern EWP-P4A-TS02
27 pass; 0 fail; 314 assertions
```

Supporting receipts on the immutable test revision:

```text
bun run typecheck
PASS

bun run lint:boundaries
PASS

bun run test:smoke
5 pass; 14 filtered out; 0 fail; 3,737 assertions

bunx biome check <the seven test-first paths>
Checked 7 files; no fixes required

git diff --check
PASS
```

After the immutable test commit was anchored, the catalog advanced P17-G5-05 from `ready` to
`active` and passed only the test-first gate. The four capability selectors are `failing` with
exact ID-bound targets and receipts; EWP-OPT-TS08 and EWP-WF13 remain pristine `planned` records
until targeted-green; P1-04 remains `mapped`. Regeneration retained 426 entities across 45 groups.

```text
bun test scripts/p17-catalog.test.ts
56 pass; 0 fail; 267 assertions

bun scripts/p17-catalog.ts --check
valid: 426 entities (419 required, 7 deferred; 244 validation obligations), 45 groups;
deterministic checklist

bun scripts/check-p17-package.ts --check
structurally valid; Phase 5 active; G5-05 active at 3/10 gates; next minimal-implementation

git diff --check
PASS
```

## Approved implementation compatibility amendment

The user approved the recommended G5-05 implementation amendment and serialized rerun in the
active root-goal session on 2026-07-23. During that rerun, the full required `EWP-P4A-TS02`
dependency selector initially reported 10 failures. Isolating the first failure proved a common
false-stale manifest boundary rather than ten independent acquisition regressions:

- retained update history correctly requires `manifest-bytes` for its role-domain operation and
  envelope digest;
- acquisition expected revisions correctly retain the generic `resource` digest used by the
  snapshot repository; and
- `createAcquisitionPlan` was comparing those intentionally different domains as if they were one
  value, so unchanged manifest bytes with identical semantics were rejected on rerun.

The compatibility amendment adds a private, non-serialized `manifestResourceRevision` to the
acquisition transition envelope. Install/uninstall planning carries it from the already-approved
snapshot; the operation image continues to carry the role-domain byte identity. The correction
also records the existing application/placement integration seam needed to pass the artifact
coordinator into undo, carry exact retained placement authority, and rebase the ledger after a
private rollback-artifact prerequisite.

Seven paths are added to the accepted ownership manifest:

- `packages/core/src/acquire/plan.ts`;
- `packages/core/src/acquire/run.ts`;
- `packages/core/src/acquire/uninstall.ts`;
- `packages/core/src/application/lifecycle-services.ts`;
- `packages/core/src/place/execute.ts`;
- `packages/core/src/place/types.ts`; and
- `packages/core/tests/place/execute.test.ts`.

The boundary expands from 62 to 69 paths: 34 product/architecture paths, 27 focused tests, six
acceptance owners/fixtures, and two governance files. Primary ownership remains seven entities and
required-now remains the same 14 serialized validations. No public schema, command, option, output,
journal field, artifact action, recovery record, or portable-file content changes. A fresh
immutable amendment review is required before the minimal-implementation gate is recorded.

## Implementation amendment review at b901bb8

Fresh independent reviewer `g5_05_implementation_amendment_review` examined only immutable commit
`b901bb8` and returned **GO** with no stable finding. The reviewer did not inspect or rely on the
dirty implementation worktree and made no edit.

The review independently confirmed:

- exactly seven paths were added and they cover the acquisition producers/validator, application
  entry seam, placement adapter/context seam, and its direct focused test without omitting another
  shared authority;
- the `manifest-bytes` operation-image domain and `resource` repository-snapshot domain are
  intentionally distinct, and the private `manifestResourceRevision` is the narrow compatibility
  bridge;
- the field is neither package-root exported nor encoded by a public codec, and no command, option,
  wire value, artifact action, or recovery format changes;
- the boundary arithmetic is exact at 69 unique paths: 34 product/architecture, 27 focused tests,
  six acceptance paths, and two governance files;
- all catalog evidence links resolve; and
- primary ownership remains seven entities, all 14 frozen validation commands are unchanged, and
  lifecycle state truthfully remains at three passed gates before minimal implementation.

No `P17-RV-P17-G5-05-F11` finding was opened. The approved implementation amendment may proceed
inside the exact 69-path boundary.

## Minimal implementation and targeted green at 86fd011

The minimal implementation is frozen by these ordered commits:

- `c9033e2` — canonical private retained-artifact preimage codec;
- `7f65c18` — narrow conditional retention authorization for update/undo artifact writes;
- `d93287b` — strict pair-null forward artifact history and role-domain identities;
- `8465e03` — durable artifact-history binding through every adjacent logical phase;
- `86fd011` — exact coordinator restoration, artifact rollback lineage, fresh occurrence identity,
  whole-group reduction, contiguous multi-occurrence suffix ordering, update-associated repair
  reversal, status/cleanup observation, application integration, and acquisition dual-domain
  compatibility.

The implementation reuses the existing artifact-pair coordinator, placement swap/recovery,
operation scheduler, approval path, and strict journal/undo v1 codecs. It adds no command, option,
public enum, output version, portable configuration, recovery record, or second mutation engine.
The complete implementation diff is contained by the reviewed 69-path manifest. Commit `86fd011`
passed its normal pre-commit gitleaks, Biome, boundary-lint, and conventional-message hooks.

The six primary validations then ran as six separate serialized processes on clean immutable
`86fd011`:

```text
bun test packages/cli/tests/contracts/undo.test.ts --test-name-pattern EWP-CMD-UNDO-TS03
1 pass; 8 filtered out; 0 fail; 74 assertions

bun test packages/cli/tests/contracts/update.test.ts --test-name-pattern EWP-CMD-UPDATE-TS07
1 pass; 9 filtered out; 0 fail; 23 assertions

bun test packages/cli/tests/contracts/options.test.ts --test-name-pattern EWP-OPT-TS08
1 pass; 42 filtered out; 0 fail; 66 assertions

bun test tests/ergonomics/phase/EWP-P5-TS05.test.ts --test-name-pattern EWP-P5-TS05
1 pass; 0 fail; 20 assertions

bun test tests/ergonomics/workflows/EWP-WF09.test.ts --test-name-pattern EWP-WF09
1 pass; 0 fail; 9 assertions

bun test tests/ergonomics/workflows/EWP-WF13.test.ts --test-name-pattern EWP-WF13
1 pass; 0 fail; 33 assertions
```

Targeted green is 6/6 tests, zero failures, zero skips, and 225 assertions. The formerly red
selectors now prove the exact missing update-to-undo capability; the two original green
characterizations remain green against the implemented boundary. P1-04 is validated by the complete
P5 bulk-safety owner but is not signed off before later lifecycle gates.

As an extra adjacent-contract receipt, the complete undo CLI contract file ran serialized on the
same immutable tree: 9 pass, zero fail, and 274 assertions. It covers old-history refusal, scope,
approval, aliases, migration, crash retry, and repeated no-toggle behavior in addition to TS03.

### Green execution receipt EWP-CMD-UNDO-TS03

- Validation ID: `EWP-CMD-UNDO-TS03`
- Command: `bun test packages/cli/tests/contracts/undo.test.ts --test-name-pattern EWP-CMD-UNDO-TS03`
- Exit status: `0`
- Result: PASS — committed update restores exact lock, manifest, live, and retained preimages with fresh identities and convergent rerun; 1 pass, 0 failed, 74 assertions.
- Revision: `86fd0111e00335d15e46e1ef6d90569abbd4eae8`

### Green execution receipt EWP-CMD-UPDATE-TS07

- Validation ID: `EWP-CMD-UPDATE-TS07`
- Command: `bun test packages/cli/tests/contracts/update.test.ts --test-name-pattern EWP-CMD-UPDATE-TS07`
- Exit status: `0`
- Result: PASS — update commits exact reversible manifest/lock lineage and owner-private retained preimages; 1 pass, 0 failed, 23 assertions.
- Revision: `86fd0111e00335d15e46e1ef6d90569abbd4eae8`

### Green execution receipt EWP-OPT-TS08

- Validation ID: `EWP-OPT-TS08`
- Command: `bun test packages/cli/tests/contracts/options.test.ts --test-name-pattern EWP-OPT-TS08`
- Exit status: `0`
- Result: PASS — advanced manifest/lock selector relations and custom-pair behavior remain exact; 1 pass, 0 failed, 66 assertions.
- Revision: `86fd0111e00335d15e46e1ef6d90569abbd4eae8`

### Green execution receipt EWP-P5-TS05

- Validation ID: `EWP-P5-TS05`
- Command: `bun test tests/ergonomics/phase/EWP-P5-TS05.test.ts --test-name-pattern EWP-P5-TS05`
- Exit status: `0`
- Result: PASS — bulk approval, refusal, scheduling, cancellation, two-group update undo, exact restoration, and rerun truth pass; 1 pass, 0 failed, 20 assertions.
- Revision: `86fd0111e00335d15e46e1ef6d90569abbd4eae8`

### Green execution receipt EWP-WF09

- Validation ID: `EWP-WF09`
- Command: `bun test tests/ergonomics/workflows/EWP-WF09.test.ts --test-name-pattern EWP-WF09`
- Exit status: `0`
- Result: PASS — moving update preview, execution, exact artifact/live undo, and no-toggle rerun pass; 1 pass, 0 failed, 9 assertions.
- Revision: `86fd0111e00335d15e46e1ef6d90569abbd4eae8`

### Green execution receipt EWP-WF13

- Validation ID: `EWP-WF13`
- Command: `bun test tests/ergonomics/workflows/EWP-WF13.test.ts --test-name-pattern EWP-WF13`
- Exit status: `0`
- Result: PASS — both partial-failure directions preserve independent group truth and no-write preview across the shared scheduler; 1 pass, 0 failed, 33 assertions.
- Revision: `86fd0111e00335d15e46e1ef6d90569abbd4eae8`

## Impacted green at c33a75f

All eight remaining frozen commands ran as separate serialized processes at
`c33a75fdd7466031b6aaaf5077bd497898f02348`. That revision contains the immutable
`86fd0111e00335d15e46e1ef6d90569abbd4eae8` implementation plus targeted-green governance only;
no product or test file changed between the primary and impacted runs.

```text
bun test packages/cli/tests/contracts/apply.test.ts --test-name-pattern EWP-CMD-APPLY-TS09
1 pass; 13 filtered out; 0 fail; 3 assertions

bun test packages/cli/tests/contracts/apply.test.ts --test-name-pattern EWP-CMD-APPLY-TS11
1 pass; 13 filtered out; 0 fail; 6 assertions

bun test packages/cli/tests/contracts/apply.test.ts --test-name-pattern EWP-CMD-APPLY-TS13
1 pass; 13 filtered out; 0 fail; 19 assertions

bun test packages/cli/tests/contracts/sync.test.ts --test-name-pattern EWP-CMD-SYNC-TS09
1 pass; 9 filtered out; 0 fail; 111 assertions

bun test packages/cli/tests/contracts/update.test.ts --test-name-pattern EWP-CMD-UPDATE-TS06
1 pass; 9 filtered out; 0 fail; 61 assertions

bun test packages/cli/tests/contracts/update.test.ts --test-name-pattern EWP-CMD-UPDATE-TS08
1 pass; 9 filtered out; 0 fail; 25 assertions

bun test packages/cli/tests/contracts/update.test.ts --test-name-pattern EWP-CMD-UPDATE-TS09
1 pass; 9 filtered out; 0 fail; 62 assertions

bun test tests/ergonomics/phase/EWP-P4A-TS02.test.ts --test-name-pattern EWP-P4A-TS02
27 pass; 0 fail; 314 assertions
```

The impacted run is 34/34 tests, zero failures, zero skips, and 601 assertions. Combined with the
six exact primary receipts above, all 14 frozen commands are green. The downstream matrix proves
that retained update preimages do not regress apply continuation/output truth, direct sync group
truth, update artifact/approval/failure semantics, or the complete P4A acquisition, placement,
repair, cancellation, crash, and convergence contract. P1-04 may therefore advance from mapped to
validated, but remains unsigned until review and traceability closure.

## Refactor gate at 7ec493c

The post-green refactor assessment intentionally makes no further product or test change. The
green implementation already performed the useful structural extraction:

- `undo/artifacts.ts` owns retained-history discovery, contiguous suffix selection, whole-group
  reduction, inverse construction, and artifact restoration planning;
- `retained-preimage-codec.ts` is the single strict codec for private retained bytes;
- the existing coordinator, placement swap/recovery, and scheduler remain the only mutation and
  execution authorities;
- the acquisition compatibility bridge keeps operation-image `manifest-bytes` and repository
  `resource` revisions distinct through one private, nonserialized field; and
- the external artifact lifecycle binding is limited to the private rollback-artifact inverse,
  avoiding a second generalized lifecycle path.

At `7ec493cdffe8b5648f5e70d815d2e13315e0dafe`, `git diff --name-only 86fd011..HEAD`
contained only the generated checklist, catalog, and this evidence file. A scan of all 37 files in
the implementation commit found no `SKILLSMITH_DEBUG_THROW`, console debug/log calls, TODO/FIXME,
test/describe skips, or TypeScript suppression directives. `bun run typecheck`,
`bun run lint:boundaries`, and diff checks passed. Because all 14 exact commands were already green
and no cleanup defect remained, additional refactoring would add churn without narrowing an
authority or removing duplication. The immutable implementation proceeds unchanged to fresh
independent adversarial review.

## Independent adversarial review NO-GO at a5a0897

Fresh independent reviewer `g5_05_adversarial_review` inspected immutable
`a5a08977a3f5947f167a42c874a1db1f946d9f9a` and returned **NO-GO**. The reviewer made no edit and
opened four stable findings:

- **P17-RV-P17-G5-05-F11 (high):** `historyDependencies()` and its protection/cleanup closures join
  only rollback parents and children. They do not join a forward artifact carrier to every
  placement journal and retained preimage in the same update occurrence. Under history pressure,
  the carrier can therefore become the cleanup victim while a newest placement remains selectable,
  violating the plan's indivisible occurrence contract.
- **P17-RV-P17-G5-05-F12 (high):** forward artifact history can resume a prepared transaction only
  when the next invocation has the same operation ID. A changed or abandoned update has no
  authenticated abort/orphan-cleanup path, and bounded cleanup cannot remove pending transactions.
- **P17-RV-P17-G5-05-F13 (medium):** `packages/core/src/acquire/execute.ts`,
  `packages/core/src/reconcile/execute.ts`, and
  `packages/core/tests/reconcile/execute.test.ts` were changed by the implementation sequence but
  omitted from the accepted 69-path manifest.
- **P17-RV-P17-G5-05-F14 (medium):** the completed selectors genuinely passed, but none covered the
  missing greater-than-256 occurrence unit or abandoned prepared carrier. The impacted-green and
  refactor narratives therefore overstate coverage.

The review confirmed that exact bytes/mode, pair-null rollback children, fresh IDs, group-level
reduction, the private dual-digest bridge, public-schema compatibility, and ordinary crash/rerun
paths held. The correction order is ownership amendment, occurrence dependency closure,
prepared-orphan abort/cleanup, missing adversarial tests, proportional gates, all 14 exact commands,
and a fresh independent review.

The earlier statement that the complete implementation diff was contained by 69 paths is
superseded: all product/test implementation changes require the corrected 72-path manifest. The
earlier passing receipts remain exact historical execution results, but impacted green and refactor
return to pending; P1-04 returns to mapped until the corrected impacted boundary passes.

## Adversarial correction amendment

The corrected ownership manifest adds exactly three paths:

- `packages/core/src/acquire/execute.ts`;
- `packages/core/src/reconcile/execute.ts`; and
- `packages/core/tests/reconcile/execute.test.ts`.

The boundary expands from 69 to 72 paths: 36 product/architecture paths, 28 focused tests, six
acceptance owners/fixtures, and two governance files. No public command, option, output, schema,
journal field, recovery record, artifact action, or portable content changes. Primary ownership
remains seven entities and the 14 frozen serialized commands are unchanged. A fresh immutable
amendment review is required before correction tests or product edits proceed.

## Adversarial correction amendment review at 78ba654

Fresh independent reviewer `g5_05_correction_amendment_review` examined only immutable
`78ba6543312a1115fbb4dd4d54590ac3cd47526c` and returned **GO** with no amendment finding. The
reviewer confirmed F11–F14 against `a5a0897`, exact 69-to-72 containment, 36/28/6/2 arithmetic,
complete product/test containment across all six implementation commits, truthful gate resets,
byte-identical seven-primary and 14-command boundaries, and no public-contract or non-goal
expansion. Catalog, package structure, deterministic checklist, evidence anchors, and diff checks
passed. Correction tests and implementation may proceed inside the exact 72-path boundary.

## Adversarial correction test-first evidence at 23eb290

Immutable test commit `23eb2905012f849c4031f14e8f01eb9e402ee98e` adds the missing F11/F12
adversarial boundary before product correction. Both focused files typecheck and fail only on the
intended absent behavior:

```text
bun test packages/core/tests/artifacts/ledger-history.test.ts
3 pass; 1 fail; 15 assertions
failure: artifact-occurrence-carrier is a pending cleanup victim while its placement is protected

bun test packages/core/tests/update/history.test.ts
3 pass; 2 fail; 32 assertions
failures: absent and present superseded prepared carriers remain in durable transactions
```

The exact-repeat characterization is green before correction, preventing a group-ID-only closure
that would conflate separate occurrences. The orphan tests also prove that the next operation can
otherwise commit successfully while leaving the prior prepared authority behind. F11 and F12 are
therefore reproducible red findings; implementation must make these tests green without weakening
the earlier 14-command boundary.

## Adversarial correction implementation and impacted green at e778e6b

Immutable correction commit `e778e6bf12195eb4e909a91a2987c85ca80746e1` closes F11 and F12
without changing the accepted public or serialized boundary.

- `ledger-history.ts` partitions deterministic update groups into commit-order occurrences. A
  carrier-role repeat or the first carrier after placement work starts a new occurrence, so exact
  repeated transitions are not joined by group identity alone. Artifact carriers and every unique
  update/repair placement in the occurrence are sibling dependencies. Existing rollback
  parent/child traversal then makes the whole occurrence indivisible during protection, effective
  capacity expansion, victim selection, deletion projection, and restart.
- `update/history.ts` strictly discovers every pending update-artifact journal before a later
  operation mutates. Physical-before prepared/staged/backed-up carriers enter a restartable cleanup
  protocol: first persist a pair-null rollback marker with released retention, then remove the
  authenticated owner-private envelope, then commit the rollback. A crash before deletion leaves
  the marker plus envelope; a crash after deletion leaves the same marker without the envelope.
  Both states converge on the next invocation. Physical-after backed-up/live carriers finalize
  forward, while corrupt envelopes, malformed journals, and physical mismatches refuse before the
  next physical binding runs.
- `logical-transactions.ts` adds only the narrow internal retention-release projection required by
  that protocol. It remains absent from the package root and requires the exact pair-null update
  manifest/lock workflow plus stable file identity.

The correction-focused immutable replay is green:

```text
bun test packages/core/tests/artifacts/ledger-history.test.ts packages/core/tests/update/history.test.ts packages/core/tests/place/logical-transactions.test.ts
20 pass; 0 fail; 108 assertions
```

This covers greater-than-256 complete-occurrence protection, two placements in one occurrence,
pending-child closure, exact-repeat separation, absent/present prepared orphans, cleanup-marker
restart with the envelope present or already removed, corrupted envelope/journal refusal, and the
staged/backed-up/live physical recovery matrix. The restartable shared-backup ledger-writer case
also passed in isolation with eight assertions. Broader core replays produced only combined-load
timeouts; each timed selector passed in a clean isolated process, including both lifecycle cursor
orders and retained undo artifact restoration.

All six frozen primary selectors passed as separate serialized processes at the correction
revision:

```text
EWP-CMD-UNDO-TS03    1 pass; 0 fail; 74 assertions
EWP-CMD-UPDATE-TS07  1 pass; 0 fail; 23 assertions
EWP-OPT-TS08         1 pass; 0 fail; 66 assertions
EWP-P5-TS05          1 pass; 0 fail; 20 assertions
EWP-WF09             1 pass; 0 fail; 9 assertions
EWP-WF13             1 pass; 0 fail; 33 assertions
```

The primary result is 6/6 tests, zero failures, and 225 assertions. Options TS08 and P5-TS05 each
needed one clean rerun after a harness timeout; the authoritative reruns completed with their full
expected assertion counts.

All eight frozen dependency commands also passed separately and serialized:

```text
EWP-CMD-APPLY-TS09    1 pass; 0 fail; 3 assertions
EWP-CMD-APPLY-TS11    1 pass; 0 fail; 6 assertions
EWP-CMD-APPLY-TS13    1 pass; 0 fail; 19 assertions
EWP-CMD-SYNC-TS09     1 pass; 0 fail; 111 assertions
EWP-CMD-UPDATE-TS06   1 pass; 0 fail; 61 assertions
EWP-CMD-UPDATE-TS08   1 pass; 0 fail; 25 assertions
EWP-CMD-UPDATE-TS09   1 pass; 0 fail; 62 assertions
EWP-P4A-TS02         27 pass; 0 fail; 314 assertions
```

The dependency result is 34/34 tests, zero failures, and exactly 601 assertions. Sync TS09 first
encountered its transient prepared-journal observation race and update TS09 first exceeded its
harness timeout; their clean serialized reruns passed the complete 111- and 62-assertion matrices.
Thus all 14 frozen commands are green at `e778e6b`, and P1-04 returns from mapped to validated.

Proportional recovery gates are also green:

```text
bun run test:smoke
5 pass; 0 fail; 3,737 assertions

bun run test:smoke:p2-ts04:recovery
4 pass; 0 fail; 453 assertions

bun test tests/ergonomics/phase/EWP-P2-TS04.test.ts
19 pass; 0 fail; 49,699 assertions
```

## Adversarial correction refactor gate at e778e6b

The correction keeps each new authority at its existing narrow owner: bounded-history dependency
construction remains in `ledger-history.ts`, physical retained-artifact recovery remains in
`update/history.ts`, and the pure transition exception remains in `logical-transactions.ts`. The
durable cleanup marker reuses journal v1 and the existing ledger persistence gateway; no second
recovery record, artifact coordinator, public export, or wire shape was introduced. Corrupt and
malformed authority is validated before cleanup-marker mutation, and once the marker is durable,
envelope deletion is restartable from either physical state. Additional extraction would require a
new path outside the accepted 72-path manifest without narrowing an authority. The correction is
therefore ready for a fresh independent adversarial review unchanged.

## Corrected adversarial review NO-GO at e497dcc

Fresh independent reviewer `g5_05_corrected_adversarial_review` inspected immutable
`e497dcceff58db4ea8e6562114a60562f9bcea95`, made no edit, and returned **NO-GO** with one stable
finding:

- **P17-RV-P17-G5-05-F15 (high):** recovery discovery begins from the two exact workflow strings
  `update-artifact-history` and `update-artifact-orphan-cleanup`. A codec-valid pair-null update
  artifact journal whose workflow alone is corrupted therefore falls outside both strict authority
  validators. A later different update can physically execute and commit while leaving the
  malformed pending journal and retained envelope behind. The existing corrupt-journal test
  changes `command` while preserving the recognized workflow, so it does not cover this evasion.

The reviewer recommended structural artifact-journal discovery independent of command/workflow,
followed by exact recognized-authority validation before persistence, envelope deletion, or a new
physical binding. Both forward and cleanup workflow-only corruption require negative tests proving
failure, zero physical executions, unchanged artifact bytes, and unchanged pending/envelope state.
Legitimate `undo-artifact-history` rollback state must remain explicitly distinct through its
source-parent identity.

The review confirmed that F11 whole-occurrence closure, F12 durable marker ordering for discovered
journals, F13 exact 72-path containment, and the corrected F14 evidence otherwise hold. Focused,
smoke, recovery, typecheck, boundary, catalog, and package checks independently passed; no public
field, enum, export, or recovery record was added. Impacted-green and refactor return to pending,
and P1-04 returns to mapped until F15 is reproduced red, corrected, rerun, and independently
reviewed.

## F15 structural discovery correction and immutable green at ec831af

Immutable test-first commit `e056fbad723814c54e5feadf86f2791490bc28e2` reproduces F15 without
changing product code. It adds both required workflow-only corruption cases: a pending forward
artifact carrier and a durable orphan-cleanup marker. Each case requires the operation to fail with
`update-artifact-history-failed`, records the physical binding invocation count, and proves the
artifact bytes plus pending journal/envelope remain unchanged.

A detached replay of the test-first revision produced the exact intended red result:

```text
bun test packages/core/tests/update/history.test.ts
13 pass; 2 fail; 83 assertions

cleanup workflow corruption: expected physicalExecutions 0; received 1
forward workflow corruption: expected physicalExecutions 0; received 1
```

Immutable correction commit `ec831af33fe317bb5f9bc2727e611377f3a76f09` closes F15 structurally.
`pendingPairNullArtifactRole()` now discovers every nonterminal pair-null `write-manifest` or
`write-lock` transaction without trusting its command, workflow, selection, reversibility, or
retention fields. Before a new physical binding, each discovered journal must satisfy the complete
existing forward or cleanup authority validator. A malformed journal refuses; a legitimate
`undo-artifact-history` rollback is kept distinct by disposition/workflow and explicitly requires
undo recovery before update. The strict validators, journal v1, retained-envelope codec, public
contracts, command/options, output, and recovery-record formats are unchanged.

The correction-focused replay is green at the immutable correction revision:

```text
bun test packages/core/tests/artifacts/ledger-history.test.ts packages/core/tests/update/history.test.ts packages/core/tests/place/logical-transactions.test.ts
22 pass; 0 fail; 120 assertions

bun run typecheck
PASS
```

The two workflow-corruption cases now refuse before physical execution while preserving the
artifact, pending transaction, and retained envelope. The replay also retains F11 whole-occurrence
protection, F12 restartable orphan cleanup, cleanup restart with the envelope present or absent,
corrupt envelope/command refusal, physical before/after recovery, and the narrow pair-null rollback
transition.

All 14 frozen commands then passed as separate serialized processes on the same immutable
`ec831af` revision. The six primary commands are:

```text
EWP-CMD-UNDO-TS03    1 pass; 0 fail; 74 assertions
EWP-CMD-UPDATE-TS07  1 pass; 0 fail; 23 assertions
EWP-OPT-TS08         1 pass; 0 fail; 66 assertions
EWP-P5-TS05          1 pass; 0 fail; 20 assertions
EWP-WF09             1 pass; 0 fail; 9 assertions
EWP-WF13             1 pass; 0 fail; 33 assertions
```

The eight dependency commands are:

```text
EWP-CMD-APPLY-TS09    1 pass; 0 fail; 3 assertions
EWP-CMD-APPLY-TS11    1 pass; 0 fail; 6 assertions
EWP-CMD-APPLY-TS13    1 pass; 0 fail; 19 assertions
EWP-CMD-SYNC-TS09     1 pass; 0 fail; 111 assertions
EWP-CMD-UPDATE-TS06   1 pass; 0 fail; 61 assertions
EWP-CMD-UPDATE-TS08   1 pass; 0 fail; 25 assertions
EWP-CMD-UPDATE-TS09   1 pass; 0 fail; 62 assertions
EWP-P4A-TS02         27 pass; 0 fail; 314 assertions
```

The immutable boundary result is 40/40 tests, zero failures, no skips, and exactly 826 assertions.
Before that authoritative replay, undo TS03, P5-TS05, WF09, and update TS09 had incomplete harness
runs that stopped at their existing timeouts, and sync TS09 once missed its deliberately sampled
prepared-transaction observation. None was credited as green. Clean serialized reruns completed
their full expected 74, 20, 9, 62, and 111 assertions, followed by the complete 14-command replay.
No product change, assertion weakening, selector change, or timeout increase was used to obtain the
result.

The proportional and full durability gates are also green at `ec831af`:

```text
bun run test:smoke
5 pass; 0 fail; 3,737 assertions

bun run test:smoke:p2-ts04:recovery
4 pass; 0 fail; 453 assertions

bun test tests/ergonomics/phase/EWP-P2-TS04.test.ts
19 pass; 0 fail; 49,699 assertions
```

The F15 refactor boundary is deliberately narrow: structural classification is one private helper
beside the existing authority validators, while forward recovery and cleanup continue to use their
single established implementations. Extracting a second scanner or recovery abstraction would
duplicate authority without narrowing behavior. The correction remains inside the accepted
72-path manifest and adds no public surface. Impacted-green and refactor may therefore pass, and
P1-04 returns to `validated`; adversarial review, traceability closure, and sign-off remain pending
until a fresh independent reviewer inspects the immutable governance commit.

## F15 final adversarial review NO-GO at b72ef1c

Fresh independent read-only reviewer `g5_05_f15_final_review` inspected immutable
`b72ef1c4d39eb61e8420dbaf382820291bfe29d8` and returned **NO-GO** at 0.97 confidence. The
reviewer made no edit and opened one stable high-severity finding:

- **P17-RV-P17-G5-05-F16 (high):** structural recovery currently begins only inside
  `bindForwardUpdateArtifactHistoryV1.execute()`, and that binding is installed only for update
  `write-manifest`/`write-lock` operations. An established live-only repair update has no artifact
  operation, so it can enter placement execution without invoking the structural scan. With either
  F15 workflow-corrupted pending shape already in the ledger, the repair can physically mutate and
  commit while the corrupt journal and retained envelope remain.

The exact counterexample is an update whose manifest and lock are already current while one live
tool is stale. The existing update contract proves this plan has `artifact: false, live: true` and
executes a repair. F16 requires both the corrupted forward carrier and corrupted cleanup-marker
shape to refuse before that repair, with unchanged artifact, live, ledger, pending journal, and
envelope state.

The reviewer confirmed that F11, F12 on discovered paths, F13, and the original artifact-operation
form of F15 are closed; the 72-path boundary, 14-command counts, durability receipts, 7/10 catalog
state, and P1-04 `validated` transition were numerically truthful. F14/F15 completeness is
nevertheless overstated because the accepted invariant applies before any later update mutation,
not only when the later plan itself contains an artifact write.

The correction must run once at invocation/group scope under the existing complete lock hierarchy,
after validation and before scheduler execution, then leave the per-artifact retained-history
binder responsible for its own current carrier. It must add no new public contract, scheduler,
lock rank, journal format, or recovery record. Impacted-green and refactor return to pending, and
P1-04 returns to mapped until the two live-only red cases pass, the complete frozen boundary is
rerun, and a fresh independent reviewer returns GO.

## F16 invocation-scope recovery correction and immutable green at aefc1ba

Immutable test-first commit `6a8e4334b7d6c0326250091a9615f9f61283cb6b` adds two public CLI
regressions for the reviewer's exact live-only counterexample. Each case first creates the
established `artifact: false, live: true` repair plan, seeds either a workflow-corrupted forward
carrier or workflow-corrupted cleanup marker, then requires an error before mutation with exact
manifest, lock, ledger, Codex live, Claude live, and retained-envelope byte preservation.

The first red assertion compared independently read byte arrays by JavaScript object identity.
That comparison was diagnostically invalid and is not credited as no-mutation evidence. A
disposable detached replay of the `6a8e433` product/test-first revision applied only the later
assertion correction from `aefc1ba` so every state field was compared by byte content. Both cases
then produced the intended product red:

```text
bun test packages/cli/tests/contracts/update.test.ts --test-name-pattern 'F16'
0 pass; 2 fail; 30 assertions

forward corruption:
exitCode 0; kind skillsmith.update; Claude live changed; ledger changed; envelope unchanged

cleanup corruption:
exitCode 0; kind skillsmith.update; Claude live changed; ledger changed; envelope unchanged
```

The invalid detached dependency-resolution attempt that accidentally resolved `@skillsmith/core`
to the corrected main worktree was discarded and is not credited. The authoritative detached red
resolved the CLI workspace dependency to the detached `6a8e433` core. The disposable worktree was
then removed; the pre-existing `skillsmith-p17g502-baseline` worktree was not changed.

Correction commit `6eecc4993541c4ff8930742662824e5de0a1d69c` moves the established
update-artifact recovery authority to the existing coordinator's one-time `beforeSchedule`
boundary. The callback runs after under-lock precondition validation and binding, while the existing
artifact-group, artifact-member, and ledger lock hierarchy is held, and before any scheduler
operation can execute. Live resources are not separately locked; lifecycle binding revalidates
their revisions before the hook and schedule. It passes every current invocation operation ID to
the existing structural discovery and forward/cleanup validators, so a legitimate current carrier
remains current while every other pair-null artifact carrier must recover or fail closed. If
recovery persists the ledger, the existing repository lifecycle cursor is rebased before
scheduling. The per-artifact binder remains the sole writer of a current artifact operation's
retained history.

Assertion-only follow-up `aefc1bab6bd39cb711f08ba7451f727bd4da43de` replaces the invalid
object-identity comparison with byte-content equality and makes a missing generic record key fail
closed. It changes no product behavior and weakens no expected outcome. At that immutable revision,
both public F16 regressions pass with 30 assertions and prove exit 1 plus byte-for-byte preservation
of manifest, lock, ledger, both live targets, and retained envelope.

Focused correction and integration receipts at `aefc1ba` are green:

```text
bun test packages/core/tests/artifacts/ledger-history.test.ts packages/core/tests/update/history.test.ts packages/core/tests/place/logical-transactions.test.ts
22 pass; 0 fail; 120 assertions

bun test packages/core/tests/update/history.test.ts packages/core/tests/reconcile/execute.test.ts packages/core/tests/reconcile/apply.test.ts
80 pass; 0 fail; 427 assertions

bun run typecheck
PASS

bun run lint:boundaries
PASS

targeted Biome and git diff checks
PASS
```

The proportional and full durability gates also pass on the same immutable content:

```text
bun run test:smoke
5 pass; 0 fail; 3,737 assertions

bun run test:smoke:p2-ts04:recovery
4 pass; 0 fail; 453 assertions

bun test tests/ergonomics/phase/EWP-P2-TS04.test.ts
19 pass; 0 fail; 49,699 assertions; 412.47 seconds
```

All 14 frozen commands then ran as separate, fail-fast, serialized Bun processes at `aefc1ba`.
The six primary selectors remain 6/6 with 225 assertions: undo TS03 74, update TS07 23, options
TS08 66, P5-TS05 20, WF09 9, and WF13 33. The eight dependency commands remain 34/34 tests with
601 assertions: apply TS09 3, apply TS11 6, apply TS13 19, sync TS09 111, update TS06 61, update
TS08 25, update TS09 62, and P4A-TS02 27 tests/314 assertions. The exact frozen result is therefore
40/40 tests, zero failures, no skips, and 826 assertions. UPDATE selectors now report 11 filtered
tests because F16 added two independent cases; no frozen selector or assertion count changed.

F16 changes three existing core files and one existing CLI contract file, creates no path, and
remains inside the accepted 72-path boundary. It adds no command, option, public enum, schema,
output field, journal format, recovery record, scheduler, lock rank, or mutation engine. Reusing the
coordinator hook, structural recovery implementation, validators, persistence gateway, and
lifecycle rebase is the narrow refactor result; a second scanner or recovery path would duplicate
authority. Impacted-green and refactor may pass, and P1-04 returns to `validated`. Adversarial
review, traceability closure, and sign-off remain pending until a fresh independent reviewer
inspects the immutable governance commit and returns GO.

## F16 final adversarial review NO-GO at a0a3cf6

Fresh independent read-only reviewer `g5_05_f16_final_review` inspected immutable
`a0a3cf6d47a9089f6d3d2c5c4f03dbe2af491130` and returned **NO-GO** at 0.98 confidence. The
reviewer made no edit and opened one stable high-severity finding:

- **P17-RV-P17-G5-05-F17 (high):** the new current-invocation exclusion passes every canonical
  plan operation ID to artifact recovery. A codec-valid forward artifact carrier may reuse a live
  repair operation ID because the reduced journal intent validates the artifact-operation matrix
  but preserves its supplied opaque operation ID rather than recomputing full plan identity. In a
  live-only repair plan, recovery therefore misclassifies the aliased carrier as current, leaves it
  pending, and schedules the live repair.

The exact counterexample uses live repair operation ID `L` as both the staged artifact carrier's
`intent.operationId` and `context.parentOperationId`, with a retained envelope authenticated to
`L`. The carrier otherwise satisfies forward artifact authority. Because `L` is in the overbroad
all-operation exclusion set, neither recovery nor refusal occurs before placement execution.

The minimal correction is to derive one artifact-only current-ID set from the canonical plan,
containing only `write-manifest` and `write-lock` operations, and pass that set to both invocation
recovery and the per-artifact binder. A live-only regression must seed a structurally valid staged
carrier that aliases the repair ID and prove refusal before live mutation when its retained
authority is missing or invalid. A recoverable variant should prove the carrier is completed before
placement execution.

The reviewer independently confirmed that ordinary-identity F16 is closed: the hook runs after
under-lock validation and binding and before scheduling, lifecycle rebasing is sound when recovery
persists, and per-artifact commit rebasing remains intact. F11, F12 on discovered carriers, F13,
F14 receipt accuracy, and F15 remain closed. Independent replay passed F16 2/30, focused history
22/120, and typecheck on the clean immutable revision.

One evidence-only wording correction is also required. The supplied coordinator descriptors are
artifact-group, two artifact-member, and ledger locks; there is no separate live lock. The recovery
hook does not mutate live state, and lifecycle binding revalidates live revisions, so this is not a
separate product blocker. The F16 evidence must stop claiming a live lock before sign-off.

Impacted-green and refactor return to pending, and P1-04 returns to mapped until F17 is reproduced
red, corrected with the artifact-only identity set, fully rerun, and independently reviewed.

## F17 artifact-only identity correction and immutable green at 42179d1

Immutable test-first commit `09a877f6722bdae22f7e2b8809ec6de366dffc27` adds both reviewed
live-only alias cases. Each creates a codec-valid staged manifest carrier whose opaque operation ID
equals the canonical live repair operation ID. The unrecoverable case deliberately omits its
declared retained envelope; the recoverable case supplies the authenticated owner-private envelope.

The test-first revision produced the intended red without product changes:

```text
bun test packages/cli/tests/contracts/update.test.ts --test-name-pattern 'F17'
0 pass; 2 fail; 30 assertions

missing retention:
exitCode 1; kind skillsmith.update rather than error; all tracked bytes unchanged

recoverable retention:
exitCode 1; kind skillsmith.update; live unchanged; transaction still pending;
cleanup not committed before placement; retained envelope still present
```

The missing-retention carrier reached a later semantic execution failure because the aliased
placement ID collided with the still-pending journal. That later collision happened to preserve
state, but it does not satisfy the required invocation preflight and is not credited as F17
closure. The recoverable carrier was likewise excluded, remained pending with its envelope, and
prevented the legitimate live repair. Both results prove the all-operation exclusion was wrong.

Correction commit `42179d1025c19d27283459e1cca9879982139bc2` derives one immutable
artifact-only current-ID vector from the canonical plan. It contains exactly current
`write-manifest` and `write-lock` operation IDs and is passed to both invocation recovery and the
per-artifact history binder. The recovery request and internal set are named
`currentArtifactOperationIds`, making the authority boundary explicit. A live placement, repair,
move, migration, or unrelated operation ID can no longer suppress artifact recovery, while current
manifest and lock carriers remain protected from being mistaken for superseded work.

At immutable `42179d1`, the missing-retention alias returns the coordinator-boundary error before
mutation. The recoverable alias first commits `update-artifact-orphan-cleanup`, removes the retained
envelope, clears the pending transaction, rebases the ledger lifecycle cursor, and only then commits
the live repair journal. The regression checks cleanup-history order against the aliased repair
journal, proving cleanup precedes placement rather than only observing the final state.

Correction-focused receipts are green:

```text
bun test packages/cli/tests/contracts/update.test.ts --test-name-pattern 'F1[67]'
4 pass; 0 fail; 60 assertions

bun test packages/core/tests/update/history.test.ts packages/core/tests/reconcile/execute.test.ts packages/core/tests/reconcile/apply.test.ts
80 pass; 0 fail; 427 assertions

bun test packages/core/tests/artifacts/ledger-history.test.ts packages/core/tests/update/history.test.ts packages/core/tests/place/logical-transactions.test.ts
22 pass; 0 fail; 120 assertions

bun run typecheck
PASS

bun run lint:boundaries
PASS
```

The proportional and exhaustive durability gates also pass on the same immutable content:

```text
bun run test:smoke
5 pass; 0 fail; 3,737 assertions

bun run test:smoke:p2-ts04:recovery
4 pass; 0 fail; 453 assertions

bun test tests/ergonomics/phase/EWP-P2-TS04.test.ts
19 pass; 0 fail; 49,699 assertions; 312.22 seconds
```

All 14 frozen commands then ran as separate fail-fast serialized processes at `42179d1`. The six
primary commands remain 6/6 tests with 225 assertions, and the eight dependency commands remain
34/34 tests with 601 assertions. The exact result is 40/40 tests, zero failures, no skips, and 826
assertions. UPDATE selectors report 13 filtered tests because F16 and F17 add four independent
cases; no frozen selector, expected behavior, timeout, or assertion count changed.

F17 adds no path and changes only the existing update CLI contract plus two existing core files.
It remains inside the accepted 72-path boundary and adds no public surface, schema, journal format,
recovery record, scheduler, lock descriptor, or mutation engine. The artifact-only derivation is
the narrow refactor result; teaching the reduced journal codec to recompute a full plan identity
would require authority it intentionally does not possess and would expand this correction beyond
the reviewed gap. Impacted-green and refactor may pass, and P1-04 returns to `validated`.
Adversarial review, traceability closure, and sign-off remain pending until a new fresh independent
reviewer inspects the immutable governance revision and returns GO.

## F17 final adversarial review NO-GO at 2df4e45

Fresh independent read-only reviewer `g5_05_f17_final_review` inspected immutable
`2df4e45a95aca497c467859f11534a2be5d6f317` and returned **NO-GO** at 0.99 confidence. The
reviewer made no edit and opened one stable high-severity finding:

- **P17-RV-P17-G5-05-F18 (high):** the artifact-only exclusion still trusts only the journal's
  opaque operation ID. In a pin update with current manifest operation `M` and lock operation `L`,
  a codec-valid staged manifest carrier can use `L` as its intent, parent, and envelope operation
  ID while retaining manifest role, path, images, and retention authority. Invocation recovery
  sees a current artifact ID and skips it. The scheduler commits `M` before the lock binder finally
  detects that the `L` carrier is a manifest carrier rather than the approved lock authority.

The reviewer's independent public reproduction returned exit 1, `skillsmith.update`, and partial
state: manifest changed, lock and both live targets unchanged, the stale transaction still pending,
and the manifest journal already committed. A stale carrier can likewise alias a later group's
artifact ID, and multiple carriers can claim the same current artifact ID without invocation-scope
rejection.

The minimal correction is to pass an immutable map of full canonical current artifact authorities,
not an ID vector. A carrier may remain current only when exactly one pending carrier matches the
approved operation's complete reduced journal intent: ID, group, role/kind, path, before/after
images, mutation flags, reversibility, and retention identity. Mismatched or ambiguous current-ID
carriers must refuse before scheduler entry; genuinely superseded carriers continue through the
existing recovery path. Public pin regressions must cover cross-role aliasing, same-role changed
intent/path, and duplicate current-ID carriers with zero artifact or live mutation.

The reviewer confirmed the F17 non-artifact alias tests are honest and independently green 2/30,
including cleanup-before-placement history order. F11-F15 remain closed. F16's ordinary-identity
hook placement and lifecycle rebase remain sound, but its complete pre-schedule invariant and F17
are not closed under F18. The 72-path/public-surface boundary and recorded focused, smoke, recovery,
static, and catalog receipts remain truthful.

Impacted-green and refactor return to pending, and P1-04 returns to mapped until F18 is reproduced
red, corrected with full current artifact authorities, fully rerun, and independently reviewed.

## F18 full-current-authority correction and immutable green at 7f868bf

Immutable test-first commit `5c7abae1f5fce49dfcdb6f53e7b675566b1a52eb` adds three public
pin-update counterexamples without changing product code: a staged manifest carrier using the
current lock operation ID, a same-role lock carrier with the current lock ID but a changed group
intent, and an exact current lock carrier whose declared retained envelope is missing. The first
test is the reviewer's cross-role partial-commit reproduction; the second proves that role and ID
alone are insufficient; the third proves that even an exact current intent must authenticate its
durable retention before scheduling an earlier artifact operation.

The test-first revision produced the intended red:

```text
bun test packages/cli/tests/contracts/update.test.ts --test-name-pattern F18
0 pass; 3 fail; 15 assertions

all three attempts:
exitCode 1; kind skillsmith.update rather than error;
manifest changed; ledger changed; lock and both live targets unchanged
```

This is the prohibited partial state. ID-only exclusion left the selected current carrier pending,
allowed the earlier manifest operation to commit, and deferred authority validation until the lock
binder. The same ordering affected both mismatched intents and missing retention.

Product correction `91930633c6c64cd53ff18932b823391d40c3bdb8` replaces the artifact-ID
vector with one immutable map of the canonical current `write-manifest` and `write-lock`
operations. Construction rejects a non-artifact operation, an inexact artifact shape, or a
duplicate current operation ID. Invocation preflight and each artifact binder receive the same
full authorities rather than independently deriving weaker identities.

One shared read-only carrier observer now validates every current-ID claim before any cleanup
marker or superseded-carrier persistence. It requires at most one matching pending transaction,
observes the canonical operation's exact role and path, checks the physical image permitted by the
durable phase, authenticates the owner-private envelope against operation ID, role, path,
expected-after digest, and mode, reconstructs the canonical journal sequence, and compares the
pending phase against the complete reduced intent. That comparison includes ID, group, kind,
before/after images, mutation set, conditional reversibility, and retention resource identity.
Only carriers whose IDs have no current canonical authority continue through the existing
superseded recovery path.

The same observer is reused by the per-artifact binder after preflight. This removes the prior
split between invocation-scope exclusion and binder-scope validation without adding a scanner,
writer, scheduler, recovery record, journal format, or mutation engine. All validation of claimed
current carriers completes before scheduler entry, so a later lock carrier cannot permit an
earlier manifest commit.

At immutable product revision `9193063`, all three original F18 cases return the coordinator
boundary error with byte-for-byte preservation of manifest, lock, ledger, both live targets, and
every present retained envelope. Correction-focused and static receipts are green:

```text
bun test packages/cli/tests/contracts/update.test.ts --test-name-pattern 'F16|F17|F18'
7 pass; 0 fail; 75 assertions

bun test packages/core/tests/update/history.test.ts packages/core/tests/reconcile/execute.test.ts packages/core/tests/reconcile/apply.test.ts
80 pass; 0 fail; 427 assertions

bun test packages/core/tests/artifacts/ledger-history.test.ts packages/core/tests/update/history.test.ts packages/core/tests/place/logical-transactions.test.ts
22 pass; 0 fail; 120 assertions

bun run typecheck
PASS

bun run lint:boundaries
PASS
```

The proportional and exhaustive durability gates also pass on the same product revision:

```text
bun run test:smoke
5 pass; 0 fail; 3,737 assertions

bun run test:smoke:p2-ts04:recovery
4 pass; 0 fail; 453 assertions

bun test tests/ergonomics/phase/EWP-P2-TS04.test.ts
19 pass; 0 fail; 49,699 assertions; 311.14 seconds
```

All 14 frozen commands ran as separate fail-fast serialized processes at `9193063`. The six
primary commands remain 6/6 tests with 225 assertions, and the eight dependency commands remain
34/34 tests with 601 assertions. The exact frozen result is 40/40 tests, zero failures, no skips,
and 826 assertions.

Test-only follow-up `7f868bfad2c87608cfc98bfb2ee0992356f88d3a` closes the reviewer's
duplicate-current-ID question. The canonical ledger writer refuses a second pending carrier with
the same operation ID, so two such carriers cannot form a valid persisted ledger model. The public
case therefore injects two individually structured lock carriers into a raw invalid ledger and
proves the CLI codec returns input exit 3 before planning or recovery, with manifest, lock, ledger,
both live targets, and both retained envelopes byte-for-byte unchanged. This accurately assigns
the duplicate case to the earlier ledger authority instead of claiming it reaches the new recovery
observer.

At `7f868bf`, F18 is 4/20 and combined F16-F18 is 8/80. UPDATE-TS07, TS06, TS08, and
TS09 were rerun after the test-only follow-up and remain green with their unchanged 23, 61, 25, and
62 assertions. UPDATE selectors now report 17 filtered tests; no frozen selector, expected
behavior, timeout, or assertion count changed.

F18 creates no path. It changes the existing update CLI contract and two existing core files,
remaining within the accepted 72-path boundary. It adds no public command, option, type, schema,
output field, journal format, recovery record, lock descriptor, or mutation surface. The shared
full-authority observer is the narrow refactor result. Impacted-green and refactor may pass, and
P1-04 returns to `validated`. Adversarial review, traceability closure, and sign-off remain pending
until a new fresh independent reviewer inspects the immutable governance revision and returns GO.

## F18 final adversarial review NO-GO at 3399124

Fresh independent read-only reviewer `g5_05_f18_final_review` inspected immutable governance
revision `33991241c531b380fee45c15e222ef36656dc89e` and returned **NO-GO** at 0.99
confidence. The reviewer made no edit and opened two stable high-severity findings:

- **P17-RV-P17-G5-05-F19 (high):** invocation recovery prevalidates current carriers, but validates
  and mutates cleanup markers and superseded carriers sequentially. In a live-only repair with two
  unique codec-valid staged superseded manifest carriers ordered by transaction ID, the first has
  its authenticated envelope and the second declares missing retention. The command refuses with
  exit 1/error and preserves artifact/live bytes, but it first commits the first cleanup marker,
  deletes that envelope, and changes the ledger. This contradicts the invocation-wide claim that
  every pending carrier is authenticated before ledger, cleanup-marker, or envelope mutation.
- **P17-RV-P17-G5-05-F20 (high):** current-carrier observation requires physical-before for
  `prepared` and `staged` but permits a `live` carrier whose physical artifact is still the
  before-image. The reviewer seeded the exact canonical current lock carrier at `live` with an
  authenticated envelope while leaving the lock at its before-image. Public `update factor-scan
  --pin --json` returned exit 0/completed and changed manifest, lock, ledger, and both selected live
  targets while committing the invalid carrier. The accepted phase contract requires
  `prepared/staged -> before`, `backed-up -> before or authenticated after`, and
  `live -> authenticated after` before scheduler entry.

F19's minimal correction is a complete read-only observation/authentication pass across current,
cleanup, and superseded carriers that produces immutable recovery actions. Mutation may begin only
after every carrier passes, with identity reobservation immediately before destructive cleanup.
F20's minimal correction is exact phase/image enforcement in the shared current-carrier observer.
Public regressions must prove the two-carrier live-only failure leaves both carriers, envelopes,
ledger, artifacts, and live targets unchanged, and that current `live` plus physical-before refuses
with every tracked byte unchanged.

The reviewer confirmed F11-F17 remain closed, including F17 cleanup-before-placement ordering. F18
is reopened by F20 because complete current authority includes durable physical phase. The
duplicate-carrier case remains honestly assigned to canonical ledger-codec refusal at exit 3. The
72-path boundary remains exact, the implementation/test diff is contained apart from canonical
catalog/checklist governance files, and no public export/schema, second scheduler, scanner, writer,
recovery record, or lock rank was introduced.

Independent receipts passed F16-F18 8/80, history aggregate 22/120, focused core 80/427,
typecheck, boundary lint, catalog tests 56/267, catalog/package structure, and the exact
7-primary/14-required and 40-test/826-assertion arithmetic. Those green receipts do not close the
new counterexamples. Impacted-green and refactor return to pending, P1-04 returns to `mapped`, and
adversarial review may not advance until F19/F20 are reproduced red, corrected, fully rerun, and
inspected by another fresh independent reviewer.

## F19/F20 complete recovery preflight correction and immutable green at 65c3907

Immutable test-first commit `f14259825e1f3188eedfbf65f34edf426dc799ae` adds the two
reviewed public counterexamples without product changes.

The corrected F19 fixture uses the pin preview's two canonical artifact operation IDs for two
manifest-shaped staged carriers, avoiding any synthetic identity outside the private codec. The
lexically first carrier has its authenticated envelope; the second declares retained authority but
has no envelope. Before correction, public live-only repair returned exit 1/error and preserved
manifest, lock, and both live targets, but changed ledger bytes and deleted the first envelope
before discovering the second failure. The desired all-carrier zero-mutation assertion failed on
exactly those two facts.

F20 seeds the exact current lock carrier at canonical `live`, with its owner-private authenticated
envelope, while leaving the physical lock at intent-before. Before correction, public pin update
returned exit 0/`skillsmith.update`; manifest, lock, ledger, and both live targets changed and the
invalid carrier committed. Its retained envelope remained present. This reproduces the reviewer's
phase-authority bypass rather than a later semantic failure.

Product correction `65c390702bedeb0ffa1db69adbf336f4c0130905` keeps recovery in the existing
`update/history.ts` authority and splits it into explicit observation and execution phases:

- current carriers are fully observed first through the existing canonical-operation validator;
- every cleanup marker is then structurally, physically, and envelope-authenticated into a frozen
  cleanup action without persistence or deletion;
- every superseded carrier is likewise classified into a frozen abort or finalize action only
  after its exact journal, physical image, mode/bytes, and retained authority pass;
- no recovery action executes until all three sets have completed that read-only pass; and
- immediately before each action mutates ledger or retained state, the physical artifact is
  reobserved for identical file identity, mode, bytes, and image, and the envelope is reobserved for
  identical presence, directory/file identity, and bytes. Existing owner-private removal performs
  its own final identity check before deletion.

The shared current-carrier observer now enforces the complete durable phase relation:
`prepared/staged` require physical-before, `backed-up` admits physical-before or authenticated
physical-after, and `live` requires authenticated physical-after. Thus a journal cannot claim that
the after-image is live while scheduler execution is still needed to create it.

At immutable `65c3907`, F19 and F20 pass 2/20 and the complete correction sequence F16-F20 passes
10/100. Focused and static receipts are green:

```text
bun test packages/core/tests/update/history.test.ts packages/core/tests/reconcile/execute.test.ts packages/core/tests/reconcile/apply.test.ts
80 pass; 0 fail; 427 assertions

bun test packages/core/tests/artifacts/ledger-history.test.ts packages/core/tests/update/history.test.ts packages/core/tests/place/logical-transactions.test.ts
22 pass; 0 fail; 120 assertions

bun run typecheck
PASS

bun run lint:boundaries
PASS
```

The required recovery and exhaustive durability gates pass on the same immutable product content:

```text
bun run test:smoke
5 pass; 0 fail; 3,737 assertions

bun run test:smoke:p2-ts04:recovery
4 pass; 0 fail; 453 assertions

bun test tests/ergonomics/phase/EWP-P2-TS04.test.ts
19 pass; 0 fail; 49,699 assertions; 308.41 seconds
```

All 14 frozen commands then ran as separate fail-fast serialized processes. The six primary
commands remain 6/6 tests with 225 assertions, and the eight dependency commands remain 34/34
tests with 601 assertions. The exact result is 40/40 tests, zero failures, no skips, and 826
assertions. UPDATE selectors now report 19 filtered tests because F19/F20 add two cases; no frozen
selector, expected behavior, timeout, or assertion count changed.

F19/F20 create no path and change only the existing update CLI contract plus the existing recovery
module. They remain inside the accepted 72-path boundary and add no public command, option, type,
schema, output field, journal format, recovery record, lock descriptor, writer, scanner, scheduler,
or mutation surface. The read-only action plan is a separation inside the sole existing recovery
authority, not a second recovery engine. Impacted-green and refactor may pass, P1-04 returns to
`validated`, and adversarial review remains pending until another fresh independent reviewer
inspects the immutable governance revision and returns GO.

## F21 final envelope revalidation review NO-GO at b3cf18e

Fresh independent read-only reviewer `g5_05_f20_final_review` inspected immutable governance
revision `b3cf18e840dce98c9e3fedb68ed5e1452852a285` and returned **NO-GO**. The
reviewer made no edit and opened one stable high-severity finding:

- **P17-RV-P17-G5-05-F21 (high):** superseded abort execution persists its durable cleanup marker
  before retained-envelope deletion, but deletion still receives the envelope observation made
  before that persistence barrier. `removePrivateEnvelope()` rechecks the identity, permissions,
  link count, and parent of an envelope that was present, but does not reread and compare its bytes;
  for an envelope that was absent, it also does not refuse if a new owner-private envelope and
  directory appear after marker persistence. A deterministic ledger-barrier interleaving can
  therefore replace the observed envelope bytes before cleanup and have those unapproved bytes
  deleted, or create an initially absent envelope that is left orphaned while the abort is marked
  complete.

The minimal correction is a final complete observation comparison inside the removal helper after
the cleanup marker is durable and immediately before any retained-file or directory mutation. A
barrier-driven regression must exercise the post-marker interleaving and prove refusal preserves
the pending cleanup marker plus the changed or newly created envelope. This is bounded final-state
validation inside the existing recovery authority; it requires no new format, writer, scanner,
scheduler, or public contract.

F19 and F20's all-carrier preflight and physical-phase corrections remain valid, but their claim of
complete action-time envelope revalidation is reopened by F21. Impacted-green and refactor return
to pending, P1-04 returns to `mapped`, and adversarial review may not advance until the regression
is red, the final cleanup observation is corrected, all recovery-sensitive validations are rerun,
and a brand-new independent reviewer returns GO.

## F21 final envelope revalidation correction and immutable green at a311a53

Immutable test-first commit `c801a33d6c0c556d68a67f6e13bcc4b60a139693` adds two deterministic
ledger-barrier interleavings without product changes. Both start from a superseded prepared
artifact carrier. Immediately after its rollback cleanup marker reaches the durable
`writer-live-parent-fsync` barrier, one case replaces the previously authenticated envelope bytes
in place and the other creates the previously absent owner-private directory and envelope. Before
correction, both cases continued through physical execution and returned success; the changed
envelope was deleted, while the newly created envelope was orphaned after the carrier committed.

The final red receipt was exact and honest:

```text
bun test packages/core/tests/update/history.test.ts --test-name-pattern 'refuses a retained envelope'
0 pass; 2 fail; 4 assertions

both attempts:
interleaving injected after the durable cleanup marker;
received succeeded rather than update-artifact-history-failed;
physical execution ran
```

Product correction `a311a53a1543b08e3dde06ce1f1fd94c89a04fec` changes only the existing
owner-private removal helper. Its first action is now a complete optional-envelope reobservation
against the supplied recovery observation. Present envelopes must retain the same parent identity,
file identity, and bytes; absent envelopes must remain absent under the same observed parent state.
Only that final observation may supply the identities used by the immediately following deletion
checks. Because superseded abort execution calls this helper after the cleanup marker persistence
returns, the comparison occurs on the required side of the durable barrier.

At immutable `a311a53`, both F21 cases refuse before physical execution, retain the durable pending
cleanup marker, and preserve the intervening envelope bytes. Focused and static receipts are green:

```text
bun test packages/core/tests/update/history.test.ts --test-name-pattern 'refuses a retained envelope'
2 pass; 0 fail; 14 assertions

bun test packages/core/tests/update/history.test.ts
17 pass; 0 fail; 104 assertions

bun test packages/cli/tests/contracts/update.test.ts --test-name-pattern 'F16|F17|F18|F19|F20'
10 pass; 0 fail; 100 assertions

bun test packages/core/tests/update/history.test.ts packages/core/tests/reconcile/execute.test.ts packages/core/tests/reconcile/apply.test.ts
82 pass; 0 fail; 441 assertions

bun test packages/core/tests/artifacts/ledger-history.test.ts packages/core/tests/update/history.test.ts packages/core/tests/place/logical-transactions.test.ts
24 pass; 0 fail; 134 assertions

bun run typecheck
PASS

bun run lint:boundaries
PASS
```

The required recovery and exhaustive durability gates also pass:

```text
bun run test:smoke
5 pass; 0 fail; 3,737 assertions

bun run test:smoke:p2-ts04:recovery
4 pass; 0 fail; 453 assertions

bun test tests/ergonomics/phase/EWP-P2-TS04.test.ts
19 pass; 0 fail; 49,699 assertions; 323.79 seconds
```

All 14 frozen commands ran as separate fail-fast serialized processes. The six primary commands
remain 6/6 tests with 225 assertions, and the eight dependency commands remain 34/34 tests with
601 assertions. The exact result is 40/40 tests, zero failures, no skips, and 826 assertions.
UPDATE selectors report 19 filtered tests, reflecting the prior F19/F20 additions; F21 changes no
frozen selector.

F21 creates no path and changes only the existing core history test plus the existing recovery
module. It adds no public command, option, type, schema, output field, journal format, recovery
record, lock descriptor, writer, scanner, scheduler, or mutation surface. The final reobservation
closes the stale cleanup window inside the sole recovery authority. Impacted-green and refactor may
pass, P1-04 returns to `validated`, and adversarial review remains pending until a brand-new
independent reviewer inspects the immutable governance revision and returns GO.

## F21 final independent adversarial review GO at f21a00d

Fresh independent read-only reviewer `/root/g5_05_f21_final_review` inspected immutable governance
revision `f21a00d93d88def93bef2c6b4bbebc3c01fa9be3` and returned **GO** at 0.98
confidence with no new finding. The reviewer did not implement or edit the group.

The review covered the complete accepted G5-05 plan, catalog ownership, evidence and validation
claims, and the accumulated artifact-history corrections. Its explicit falsification targets were
the F19 invocation-wide observation boundary, F20 durable-phase/physical-image relation, and F21
post-marker presence/identity/byte revalidation. The brief also required a concrete reproducible
counterexample and stable finding ID for any NO-GO rather than treating unavoidable filesystem
non-atomicity outside the trusted port abstraction as a product defect.

No critical, high, medium, or low finding remains open. The independent adversarial-review gate may
pass. Traceability closure and sign-off remain pending until the terminal whole-repository gate and
the final catalog/checklist/package closure are recorded.

## Terminal repository gate lint-size failure and tooling amendment at 2691c32

The required post-review terminal command ran at immutable reviewed revision
`2691c322b89b017b6bcd328d90913e0dbf613a00`:

```text
bun run check -- --timeout 60000 --max-concurrency 1
FAIL during `biome check`; exit 1
projects/p17/catalog.json: 1,054,695 bytes exceeds configured/default maximum 1.0 MiB
844 files reached by Biome; no fixes applied
lint:boundaries, typecheck, actions-lint, check:p17, and the repository tests did not start
```

This is a real terminal structural-gate failure, not a product or test failure. The canonical
catalog crossed Biome 1.9.4's 1,048,576-byte default during the accumulated G5-05 review evidence;
it exceeded the limit by 6,119 bytes while catalog/checklist and package validators remained green
at 426 entities, 244 validation obligations, and 45 groups.

The minimal tooling amendment adds `biome.json` to G5-05's accepted manifest and sets
`files.maxSize` to 2,000,000 bytes. This preserves Biome parsing, formatting, linting,
organize-import, and assist checks for the catalog rather than ignoring the file. The ownership
boundary expands from 72 to 73 paths solely by that governance/tooling path; the 14 frozen commands,
product/test ownership, schemas, outputs, recovery authorities, non-goals, and Phase-6 exclusion
remain unchanged.

Because the exact reviewed head did not pass the terminal structural gate and the accepted ownership
manifest changes, impacted-green, refactor, and adversarial review return to pending and the group
returns to active at 5/10. P1-04 mechanically returns to `mapped` with its owning impacted-green
gate, although every product, recovery, smoke, P2, and frozen-contract receipt remains green and the
failure occurred before those commands were entered. The config correction must pass focused
configuration/catalog checks and the complete terminal command, then receive another fresh
independent review before traceability closure.

## Terminal lint tooling amendment review GO at d1051fe

Fresh independent read-only reviewer `/root/g5_05_terminal_lint_amend_review` inspected immutable
amendment revision `d1051fe620cd358c75ea10964ce49c12cf16047d` and returned **GO** at
0.99 confidence without editing the worktree.

The reviewer independently confirmed the 6,119-byte overage, exact 73-path plan/catalog set and
36+28+6+3 arithmetic, coherent 5/10 lifecycle reset, and mechanically required P1-04 `mapped`
state. It also confirmed that the catalog is the only non-ignored repository file between 1 MiB
and 2,000,000 bytes, the pinned Biome 1.9.4 accepts the proposed `files.maxSize` setting, and a
read-only CLI limit override checks the catalog successfully. Phase 6 remains planned with zero
owned paths.

The amendment is accepted. The one-line configuration correction may proceed; impacted-green,
refactor, and adversarial review remain pending until the corrected terminal gate and a fresh final
review pass.

## Exact post-lint terminal failure audit and F22-F24 amendment at 806a9ff

The exact terminal retry ran from clean immutable product revision
`806a9ff`:

```text
bun run check -- --timeout 60000 --max-concurrency 1
Biome: PASS across 844 files, including the catalog
boundary lint: PASS
TypeScript: PASS
actionlint: PASS
check:p17: PASS
serialized tests: 3,173 pass; 28 intentional skip; 20 fail
104,392 assertions; 3,221 tests across 349 files; 1,455.23 seconds
terminal exit: 1
```

The failure count is exactly 20, not the earlier 31-failure G5-03 terminal event. The exact failed
set is four export-dependent phase/workflow tests (`EWP-P4A-TS03`, `EWP-P4B-TS05`,
`EWP-P4B-TS06`, `EWP-WF04`), seven successful-path export command cases (TS01, TS02, TS04, TS05,
TS06, TS07, TS09), six focused acquisition artifact-controller cases, the Phase-4A digest-domain
case, the init-output producer-domain case, and DEV TS05.

- **P17-RV-P17-G5-05-F22 (high):** `packages/core/src/export/plan.ts::manifestImage()` hashes
  operation `byteHash` with `hashCanonicalInput('resource', 1, bytes)`. The shared artifact
  executor observes and recomputes the same role with `hashManifestBytes()`, as required by the
  accepted G5-05 manifest-operation contract. Every changing export therefore binds a valid action
  to an after image in a different digest domain and fails closed as `export-filesystem-failure`.
  This single product defect accounts for eleven failures. The correction must use the existing
  role-specific manifest-byte authority in the export planner; generic repository snapshot and
  precondition revisions remain unchanged.
- **P17-RV-P17-G5-05-F23 (medium):** six focused acquisition-controller fixtures construct
  manifest images with the old generic resource digest, while `EWP-P4A-TS04` and the init-output
  fixture explicitly assert that obsolete operation digest. These eight failures are signed test
  maintenance, not authorization to change production back. The correction must use/assert
  `manifest-bytes` while retaining the same-byte inode replacement refusal and exact output field
  placement.
- **P17-RV-P17-G5-05-F24 (medium):** DEV TS05 still expects inverse rollback operations to reuse
  their forward `groupId` and `pairId`. The accepted G5-05 contract requires a fresh rollback
  occurrence group, fresh pair, and fresh operation while keeping the original pair in retained
  lineage. The correction must assert fresh IDs plus that retained source-pair relationship.

Focused isolated replay confirms the taxonomy:

```text
bun test packages/core/tests/acquire/execute.test.ts --timeout 60000
20 pass; 6 fail; 122 assertions
all six fail at the stale manifest fixture/action equality check

bun test packages/cli/tests/contracts/export.test.ts --timeout 60000
2 pass; 7 fail; 66 assertions
the two non-mutating/refusal cases pass; all seven changing exports fail artifact execution

bun test tests/ergonomics/phase/EWP-P4A-TS04.test.ts \
  packages/cli/tests/output/init.test.ts packages/cli/tests/contracts/dev.test.ts --timeout 60000
12 pass; 3 fail; 471 assertions
the three failures are the two obsolete digest expectations and one reused-identity expectation
```

The narrow correction adds `packages/core/src/export/plan.ts`,
`packages/core/tests/acquire/execute.test.ts`, `packages/cli/tests/contracts/dev.test.ts`,
`packages/cli/tests/output/init.test.ts`, and `tests/ergonomics/phase/EWP-P4A-TS04.test.ts`.
Ownership becomes 78 unique paths with exact 37+31+7+3 accounting. None of the eleven failing
export acceptance owners will be edited. Lifecycle state remains active at 5/10, P1-04 remains
mapped, Phase 6 remains planned with zero owned paths, and implementation is blocked until fresh
independent review accepts this amendment.
