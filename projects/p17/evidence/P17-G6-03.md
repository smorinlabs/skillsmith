# P17-G6-03 evidence

## Mapping and prerequisites

Mapping began from clean signed G6-02B revision
`f4ab3badf4bffd28824e0abe6e17f42edd7746bb` on 2026-07-26. The starting catalog SHA-256 was
`0e98f829c365235c4112e4be87b5328241d28f7d45d1ec92a46248f0dee56c14`.

- Phase 6 is active; G6-02A and G6-02B are signed off at all ten lifecycle gates.
- G6-03 starts pristine and planned: no owned files, commands, implementers, reviewer, evidence,
  executable target, or passed gate.
- Primary ownership is exactly four entities: EWP-P6-T05, EWP-P6-T06, EWP-P6-TS03, and P2-08.
- Required-now is exactly EWP-P6-TS03. Downstream coverage is exactly EWP-WF01, still planned
  under G6-04.
- The amended mapping candidate owns exactly 35 paths and adds only two comment-only placeholders:
  `packages/cli/src/runtime/presentation.ts` and
  `tests/ergonomics/phase/EWP-P6-TS03.test.ts`.

## Current rendering and observation characterization

The current runtime already closes the signed quiet/verbosity/observer behavior but does not render
color. A non-TTY process matrix at the starting revision produced:

```text
version                  exit 0; stdout 6 B; stderr 0 B; 0 ANSI
--color always version   exit 0; stdout 6 B; stderr 0 B; 0 ANSI
--color never version    exit 0; stdout 6 B; stderr 0 B; 0 ANSI
--quiet version          exit 0; stdout 0 B; stderr 0 B; 0 ANSI
--verbose version        exit 0; stdout 6 B; stderr 256 B; 0 ANSI
--debug version          exit 0; stdout 6 B; stderr 642 B; 0 ANSI
pseudo-TTY auto help      3,727 B; 0 ANSI
```

`agents --capabilities --format json --color always` exits 0 with 13,753 stdout bytes, no stderr,
and no ANSI. This is correct JSON containment but does not prove functional human color.

Focused unchanged characterization passed:

```text
bun test packages/cli/tests/util/color.test.ts \
  packages/cli/tests/runtime/diagnostics.test.ts --max-concurrency 1
14 pass; 0 fail; 44 assertions

bun test tests/ergonomics/phase/EWP-P1-TS11.test.ts \
  --test-name-pattern 'family 6|family 8' --max-concurrency 1
2 pass; 0 fail; 134 assertions

bun test packages/cli/tests/contracts/agents.test.ts \
  packages/cli/tests/contracts/help.test.ts --max-concurrency 1
13 pass; 0 fail; 538 assertions

bun scripts/generate-command-reference.ts --check
exit 0

bun scripts/p17-documentation-drift.ts --check
P17-G0-04 documentation drift valid (27 documents)
```

The old color unit suite explicitly characterizes `--color always`, `FORCE_COLOR`, and
`CLICOLOR_FORCE` as on without a TTY. The mapped G6-03 plan intentionally changes that priority to
honor the consolidated no-piped-color acceptance; independent Ready review must confirm this
authority reconciliation before the test is edited.

## Dependency, compatibility, and native-size audit

The CLI currently declares seven direct runtime dependencies. Production import counts are:

```text
@bomb.sh/tab     1 file
@clack/prompts   1 file
chalk            0 files
commander       22 files
consola          0 files
zod              0 files
@skillsmith/core workspace application/runtime authority
```

The lock resolves Chalk 5.6.2 and Consola 3.4.2. The mapping selects exact Chalk 5.6.2 for the one
new presentation seam, removes direct Consola and CLI-level Zod, and leaves core's separate codec
dependency untouched.

Core's three scan entrypoints currently import `noopLogger` and route an absent observation/logger
through `observationFromLegacyLogger`. ADR 0009 and current architecture simultaneously promise the
public Logger facade through 1.x. The mapped correction therefore preserves explicit legacy callers
but moves native no-logger fallback to `noopObserver`; it does not perform the incompatible facade
removal mentioned in an older G1-07 future-work sentence.

Using repository-pinned Bun 1.3.14, the unchanged Linux x64 current-host build produced:

```text
430 modules; compile exit 0
binary bytes: 114,403,472
runtime: skillsmith version -> 0.7.0, exit 0
elapsed: 1.63 seconds
```

The plan sets a 128 MiB current-host ceiling and leaves exact four-target bytes/checksums to G6-01.

## Documentation and capability audit

The live registry already contains four ordered tools, 16 exact operations per tool, capability
version 1 for each adapter, and registry-derived last-verified versions:

- Claude Code 2.1.202;
- Codex 0.142.5; and
- Kilo Code/OpenCode have no deep verifier baseline because they are read-only adapters.

README instead maintains a four-row detection table plus prose support summary. Its install section
correctly says no prebuilt binaries exist, but it has no source-checkout upgrade steps and no
generated availability/version/capability block. Extending the current command-reference generator
keeps one drift authority and avoids a second documentation pipeline.

G6-03 documentation must remain honest: source checkout is the only current path. G6-01 later owns
the first truthful Homebrew/npm/Bun/native commands and clean-install receipts; G6-04 later owns the
exact-SHA final workflow.

## Mapping result

The mapped candidate is one coherent pre-packaging closure:

- invocation-scoped per-stream TTY presentation replaces global environment mutation;
- human color becomes functional and lossless while JSON, pipes, completion, and observation lines
  remain ANSI-free;
- quiet preserves signed warning/deprecation/error stderr while suppressing human report stdout and
  observation detail, without changing domain results or exits;
- explicit 1.x Logger callers remain compatible while native fallback uses typed no-op observation;
- Chalk is pinned and wired, disconnected direct dependencies are removed, and current-host native
  size is bounded; and
- README capability/version/install/upgrade facts become checked against live authorities without
  claiming future package assets.

No Ready acceptance, executable selector, test-first receipt, dependency edit, implementation,
green result, reviewer, traceability closure, or sign-off is claimed by this mapping record.

## First Ready review at 2097fbf

Fresh independent reviewer `/root/g6_02b_ready_review` returned **NO-GO** with 0.97 confidence on
immutable commit `2097fbf4c151c48b8ecfbe81eb52e0917e5d9576`. The reviewer edited nothing and
left the worktree/index clean. Plan/catalog ownership was exact at 34/34 paths, primary ownership
remained four entities, required-now remained exactly EWP-P6-TS03, downstream remained exactly
EWP-WF01, and all Logger/dependency/native-size/docs/distribution boundaries otherwise passed.

`P17-G6-03-RR-B01` found that the plan claimed coverage of every failure before the command
lifecycle, but the final rejected-`main()` handler in `packages/cli/src/index.ts` writes directly to
stderr and was outside ownership. The approved correction adds that exact path and a final
entrypoint rejection case, increasing both plan and catalog to 35 unique paths.

`P17-G6-03-RR-B02` found that complete successful/drift quiet silence contradicted signed G1-07.
The corrected plan preserves the exact signed policy: quiet suppresses human report stdout and
observation detail, but never warning/deprecation or error/refusal stderr; JSON remains complete.
TS03 now explicitly covers successful warnings, deprecations, failure/cancellation, and JSON quiet.

The reviewer accepted non-TTY precedence over `--color always`, `FORCE_COLOR`, and
`CLICOLOR_FORCE` under the newer absolute P0-01 pipe requirement. The corrected documentation must
describe `always` as always on an eligible TTY and never through a pipe.

Independent checks passed the P17 PR-openable validator, command-reference drift, 27-document
documentation drift, the 14-test/44-assertion color and diagnostic characterization, and the
2-test/134-assertion TS11 family 6/8 characterization. Ready remains pending until this amendment
is frozen and receives a fresh independent read-only review.

## Corrected Ready review at 201629f

Fresh independent reviewer `/root/g6_02b_ready_review` returned **GO** with 0.99 confidence on
immutable commit `201629f272018c39278d1344f4125b2012dac8c1`. The reviewer edited nothing,
left the worktree/index clean, and reported no blocking or nonblocking findings.

Both prior blockers are closed. Plan and catalog contain the same 35 unique paths including the
final entrypoint; EWP-P6-TS03 owns its rejected-`main()` path. Quiet preserves signed
warning/deprecation and error/refusal stderr while suppressing human report stdout and observation
detail, with explicit successful-warning, deprecation, failure/cancellation, and JSON cases. The
eligible-TTY wording is owned by the mapped README paths without another ownership expansion.

The reviewer independently reconciled exactly four primary entities, sole required-now
EWP-P6-TS03, sole downstream EWP-WF01, dependency G6-02A, signed/disjoint G6-02B, and all Logger,
dependency, native-size, documentation, source-only availability, completion, distribution, and
release boundaries. PR-openable validation, generated command-reference drift, 27-document drift,
14 color/diagnostic tests with 44 assertions, and two TS11 families with 134 assertions passed.

## Accepted Ready result

The corrected plan is accepted against immutable commit
`201629f272018c39278d1344f4125b2012dac8c1`. P17-G6-03 advances from `mapped` to `ready` with
exactly its mapped and Ready gates passed. EWP-P6-T05 and EWP-P6-T06 advance to `ready`; P2-08
remains mapped; EWP-P6-TS03 remains pristine planned with a null target and no execution evidence.

The next authorized change is test-only: replace the EWP-P6-TS03 placeholder with the accepted
ID-bound families, reproduce the implementation-free product reds, and record exact receipts before
editing dependencies, production sources, or generated/current documentation. No test-first,
implementation, green, refactor, adversarial-review, traceability, or sign-off gate is implied by
Ready acceptance.

## Test-first evidence

The sole EWP-P6-TS03 placeholder was replaced after Ready acceptance while the production,
dependency, lockfile, generated documentation, and current user documentation baseline remained
unchanged. The selector contains seven independently runnable families matching the accepted plan.

An initial execution attempt imported `observationFromLegacyLogger` from the root core entrypoint;
that symbol intentionally lives in the observation entrypoint. The test import was corrected before
any product red was accepted. Biome and root TypeScript then passed, and each family executed
independently. The import error is not counted as product evidence.

Exact full-selector command:

```text
bun test tests/ergonomics/phase/EWP-P6-TS03.test.ts \
  --test-name-pattern EWP-P6-TS03
```

Result at the test-only working tree:

```text
1 pass; 6 fail; 29 assertions; 7 tests; 1 file; exit 1
```

The signed quiet/JSON family passed with 20 assertions. It proves warnings, deprecations,
failure/cancellation error streams, and JSON remain visible/complete under quiet on the unchanged
runtime. It is retained characterization rather than a false red.

### Red execution receipt EWP-P6-TS03

- Validation ID: `EWP-P6-TS03`
- Command: `bun test tests/ergonomics/phase/EWP-P6-TS03.test.ts --test-name-pattern EWP-P6-TS03`
- Exit status: `1`
- Result: 1 passed and 6 failed on the implementation-free product/dependency/documentation gaps.
- Revision: `working-tree`
- Family 1: 0 pass, 1 fail, 1 assertion because non-TTY `--color always` resolves `on` instead of
  the accepted absolute `off`.
- Family 2: 0 pass, 1 fail, 3 assertions because eligible pseudo-TTY help contains no ANSI.
- Family 3: 1 pass, 0 fail, 20 assertions; signed quiet and JSON behavior is preserved.
- Family 4: 0 pass, 1 fail, 1 assertion because preflight still invokes global
  `applyRuntimeColorMode` process-environment mutation.
- Family 5: 0 pass, 1 fail, 2 assertions because native scans still import/use `noopLogger`
  instead of the accepted typed no-op observer fallback.
- Family 6: 0 pass, 1 fail, 1 assertion because Chalk is still a caret range, not the selected exact
  wired 5.6.2 dependency; disconnected direct dependencies remain.
- Family 7: 0 pass, 1 fail, 1 assertion because the generated README capability/version marker
  block and checked source install/upgrade guidance are absent.

No implementation or green gate is claimed. The next authorized edit is the minimal product,
dependency, and documentation implementation inside the reviewed 35-path boundary.

## Minimal implementation at 4b860c2

Immutable commit `4b860c23b3273c63444bf8e8697ac9c72cec8d04` contains the complete bounded
implementation. The new invocation-scoped presentation seam resolves stdout/stderr color
independently, makes JSON and every non-TTY destination absolute color-off, and uses Chalk only for
lossless semantic human styling. Ordinary runtime output, eager version, Commander help and usage
errors, pre-lifecycle failures, and the final rejected-main handler all route through that seam.
Quiet still removes only the human report and observation detail; warning, deprecation, failure,
refusal, and cancellation stderr remain visible, while requested JSON remains complete.

The global color-environment mutation module is deleted. The presentation adapter owns one
read-only process color-environment accessor and accepts an explicit environment for tests and
embedders. `CliRuntimeIo` now exposes optional per-stream TTY facts, with absence conservatively
treated as non-TTY. The normalized boundary strips hostile ANSI before any semantic styling, and
observation detail/trace/debug lines remain unstyled.

Native scan and cross-scope paths now resolve observation in the accepted order: an explicit typed
bundle by reference, an explicitly supplied deprecated 1.x Logger through the compatibility
adapter, or a typed `noopObserver` bundle. No ordinary native fallback imports `noopLogger`; the
published Logger facade and its exact messages/redaction remain intact.

The CLI pins and imports Chalk 5.6.2, removes direct `consola` and CLI-level `zod`, and updates the
lockfile without touching core's Zod codec dependency. The current-host Bun 1.3.14 build is
114,534,544 bytes, exits 0, and prints exact version `0.7.0`, below the accepted 128 MiB ceiling.

The existing command-reference generator now owns a marker-delimited four-tool, 16-operation
capability matrix derived from `toolRegistry`, `TOOL_OPERATIONS`, `VERIFIED_AGAINST`, and `VERSION`.
README documents source checkout and exact upgrade commands, explicitly marks Homebrew, npm/Bun
global, and native release assets as not yet published, and describes eligible-TTY/never-pipe
color behavior. The CLI package README lists all 23 current public commands and the exact
human/JSON/quiet stream policy.

## Green execution receipt EWP-P6-TS03

- Validation ID: `EWP-P6-TS03`
- Command: `bun test tests/ergonomics/phase/EWP-P6-TS03.test.ts --test-name-pattern EWP-P6-TS03`
- Exit status: `0`
- Result: 7 tests passed with 139 assertions; per-stream TTY/pipe color, lossless styling, quiet/JSON, failure routing, Logger/no-op observation, dependency/native build, and generated documentation families all closed.
- Revision: `4b860c23b3273c63444bf8e8697ac9c72cec8d04`

## Targeted green at 4b860c2

The sole required-now selector passed on the immutable implementation revision: 7 tests, 0
failures, and 139 assertions. Its current-host native family compiled and ran the executable under
the 128 MiB ceiling. Generated command-reference check mode also returned no drift.

## Impacted green at 4b860c2

The post-commit owned runtime, help/options, agents, observation, scan, documentation, and generated
matrix completed as one 12-file run: 127 tests passed, 0 failed, with 3,153 assertions.

The signed context/capability compatibility authorities also passed post-commit:

```text
EWP-P1-TS01/02/05/08                              26 pass; 363 assertions
EWP-P1-TS03 and EWP-P1-TS09                       18 pass; 574 assertions
EWP-P1-TS11                                       10 pass; 487 assertions
required smoke                                     5 pass; 3,737 assertions
```

Root typecheck, the complete capability-boundary ESLint suite, Biome over affected files,
command-reference drift, and the 27-document compatibility drift check passed. The first focused
compatibility run correctly exposed three old non-TTY force-color unit expectations and the old
TS11 direct-dependency inventory; both files were inside the reviewed boundary and were reconciled
to the accepted P0-01 precedence and dependency removal before the final green runs above.

## Refactor result at 4b860c2

Refactor removed the mutating environment adapter and disconnected dependencies, centralized
semantic styling after pure rendering, retained separate injected stream capabilities, and
centralized native observation fallback without widening Logger compatibility. The commit hook
initially rejected two raw `process.env` reads; the final implementation contains one documented
read-only accessor in the focused presentation adapter, and the complete boundary lint passes. The
first adversarial review later established that the corresponding TS08 allowlist edit was omitted
from the mapped 35-path boundary; the correction amendment below supersedes the earlier boundary
claim. No distribution channel, release asset, checksum, completion surface, new command, domain
state, observer authority, or downstream EWP-WF01 work was added.

Minimal implementation, targeted green, impacted green, and refactor are accepted. The exact
terminal repository gate, fresh independent adversarial review, traceability closure, and sign-off
remain pending.

## Terminal repository gate at ae7963f

The exact terminal command ran against immutable green lifecycle revision
`ae7963f` (implementation revision `4b860c23b3273c63444bf8e8697ac9c72cec8d04`):

```text
bun run check
```

It exited 0. Biome checked 859 files, capability-boundary ESLint passed, TypeScript passed,
generated command-reference check mode passed, Actionlint passed, and the P17 PR-openable package
validator accepted 426 entities, 419 required entities, 244 validation obligations, and 45 groups.

The complete test phase reported:

```text
3,251 pass
28 skip
0 fail
110,258 assertions
3,279 tests across 359 files
1,702.13 seconds
```

The skips are the repository's explicitly configured live external-tool/process cases. No new skip,
todo, timeout, or failure was introduced. The worktree and index remained clean after the gate.
Adversarial review, traceability closure, and sign-off remain pending.

## First final adversarial review NO-GO at a7ef5bb

Fresh independent reviewer `/root/g6_02b_ready_review` returned **NO-GO** with 0.995 confidence on
immutable clean commit `a7ef5bb10c0029a41c0a418be76daa05c163f1b3`. The reviewer edited nothing
and confirmed the worktree/index remained clean. EWP-P6-TS03 passed 7/7 with 139 assertions,
EWP-P6-TS04 passed 1/1 with 587 assertions, catalog/package/generator/documentation checks passed,
the native binary was 114,534,544 bytes and printed 0.7.0, JSON hostile-error containment passed,
dependencies and Logger/no-op observation closure passed, and the recorded terminal receipt was
internally consistent.

Four findings block adversarial review:

- `P17-G6-03-AR-B01` (high): `tests/ergonomics/phase/EWP-P1-TS08.test.ts` changed its process-global
  allowlist but was omitted from the exact 35-path ownership boundary.
- `P17-G6-03-AR-B02` (high): `presentHumanOutput` strips escape/control bytes only on the color-on
  styling branch. A direct color-off probe retained ANSI, NUL, and BEL, allowing pre-styled or
  hostile renderer bytes through a pipe, `--no-color`, or `--color never`.
- `P17-G6-03-AR-B03` (high): Commander help/error overrides use global `process.exit`; an injected
  runtime probe received stderr but never its `io.exit(2)` call, so an embedder host can terminate.
- `P17-G6-03-AR-B04` (medium): TS03 inspects the final rejected-main source but dynamically runs
  only Commander unknown-option handling, not the `index.ts` rejection callback.

`P17-G6-03-AR-N01` (low) additionally notes that the presentation accessor returns all of
`process.env` rather than the accepted five-key filtered color view. Adversarial review remains
pending; no waiver, traceability closure, or sign-off is claimed.

## Approved adversarial correction amendment

The user's instruction approving the amendment and continuing authorizes the narrow correction.
Ownership expands from 35 to exactly 36 paths by adding only
`tests/ergonomics/phase/EWP-P1-TS08.test.ts`; primary entities, required-now EWP-P6-TS03,
downstream EWP-WF01, dependencies, and G6-01/G6-04 exclusions do not change.

The corrected design always sanitizes human bytes before choosing styling, snapshots only the five
color environment keys, routes every Commander exit through injected IO plus a handled parse
sentinel, and exposes an injected final-error emission helper exercised by TS03. A fresh independent
amendment review is required before adding correction assertions or changing product code. The
prior terminal receipt remains historical evidence only and must be replaced after the correction.

## Independent correction amendment review at 437d73e

Fresh read-only reviewer `/root/g6_02b_ready_review` returned **GO** with 0.99 confidence on
immutable commit `437d73e9a8cb21bf017c81daedb025fceaf5016e`. The reviewer made no edits and
confirmed a clean worktree and index. Plan and catalog contain the same 36/36 unique paths, TS08 is
owned exactly once, and primary entities, required-now TS03, downstream EWP-WF01, the G6-02A
dependency, and all completion/release/distribution exclusions remain unchanged.

The review confirmed that sanitizing before the color branch closes every human destination,
throwing a narrowly branded handled sentinel after injected exits prevents Commander 15's fallback
global exit without swallowing real errors, an injected final-error helper makes rejected-main
behavior dynamically testable, and a frozen five-key environment snapshot closes the remaining
adapter concern. Catalog/package, documentation, command-reference, and amendment diff checks
passed. The correction test-first cycle may begin; adversarial review and sign-off remain pending.

## Adversarial correction red execution receipt EWP-P6-TS03

The owned TS03 selector was extended with three correction families before any product edit and run
against unchanged product commit `8377c0719e24bc23f774e249cf35aa49072e3c5d`. The global-exit
probe replaced `process.exit` only inside a `try/finally`, so the current defect was observed without
terminating or contaminating the test host.

- Validation ID: `EWP-P6-TS03`
- Command: `bun test tests/ergonomics/phase/EWP-P6-TS03.test.ts --test-name-pattern EWP-P6-TS03`
- Exit status: `1`
- Result: 7 passed and 3 failed with 143 assertions; all three failures are intended correction reds
- Revision: `working-tree`

Family 8 received ANSI-stripped but still NUL/BEL-contaminated bytes on the color-off branch before
it could reach the absent five-key snapshot helper. Family 9 observed the Commander help promise
reject after the trapped global exit instead of resolving through injected IO. Family 10 found no
`emitFinalCliError` export. The seven original families remained green, so the red boundary is not a
compile, fixture, timeout, skip/todo, network, or unrelated regression. Product correction may now
begin inside the reviewed boundary. The affected T05 work returns to red and P2-08 returns to
mapped; impacted-green and refactor are reopened to pending. T06 and its unchanged targeted-green
documentation/capability result remain green, while later receipts for the superseded revision are
historical only.
