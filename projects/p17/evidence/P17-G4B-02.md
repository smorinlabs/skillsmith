# P17-G4B-02 evidence

## Mapping and dependency audit

- **Dependency:** G4B-01 is signed off at `51b83c2faaa7a972fe1ad60ae841d5cc50dd1164`.
- **Current catalog:** G4B-02 is planned with 0/10 gates passed, no frozen product ownership, no
  executable test commands, no implementer list, and no reviewer.
- **Primary scope:** exactly 31 pristine planned entities: the apply command, two decisions, three
  findings, one recommendation, two phase tasks, 15 command selectors, one option gate, three
  phase selectors, and three workflows.
- **Required-now partition:** exactly 29 validations: 22 new G4B-02 primaries and seven signed
  dependency replays. The new partition is apply TS01..TS14, plan TS11, option TS09, P4B-TS02/03/07,
  and WF06/07/08. The replay partition is plan TS06..TS10/TS12 and WF15.
- **Downstream:** WF13 remains planned under G5-05 and is not a G4B-02 sign-off prerequisite.
- **Existing authorities:** saved-plan v1 codec/repository/writer, portable projection, deterministic
  planner, scoped facts, scheduler, coordinator, locks, under-lock actual-before validation,
  artifact/live/ledger controllers, interaction port, exit classes, and redaction already exist.
- **Actual gaps:** no live apply surface/service/report/renderers; no shared fresh prepared-product
  seam; no saved-plan token/compatibility/scoped-staleness validator; no exact operation binding
  adapter; no pre-approval exact preview; and none of the 22 new selectors exists.
- **Architecture result:** reuse all signed authorities. Add only shared preparation, one saved-plan
  validator, one narrow reconcile execution adapter, and one thin apply service/report boundary.
  No second planner, schema, scheduler, coordinator, transaction engine, parser, or writer.
- **Highest-risk rule:** a comment-only manifest edit remains valid at the saved authorization
  layer, then execution binds the freshly observed byte revision for under-lock CAS. A global
  producing-byte comparison would be incorrect. All other staleness is restricted to referenced
  facts and complete selected-set fingerprints.
- **Frozen candidate:** 54 exact product/test paths and 37 catalog commands. The planning/create
  addition is limited to the matching closed `apply` runtime literal. The three interaction
  additions are limited to a structured exact preview, explicit No-default confirmation, and its
  focused runtime regression. Existing executor files are extraction-only candidates;
  coordinator/recovery primitives are excluded unless executable red evidence and an independent
  amendment prove a gap.
- **Safety context:** all work is authorized local open-source CLI development with hermetic
  filesystem fixtures, synthetic canaries, deterministic interruption, and no external systems,
  accounts, credentials, or third-party networks.
- **Result:** mapping is complete. No policy, human-review, network, or dependency blocker exists.
  The only pre-implementation gate is independent Ready review and exact catalog freeze.

## Ready review

Fresh independent reviewer `/root/g4b02_ready_review` completed a read-only assessment across the
normative plan, catalog, signed G4B-01 authorities, current implementation, G4B-03 boundary, and
repository test policy. The first immutable candidate was
`f70f64c190c9e8e957d1b044a21027b01679feb1`.

The review raised six bounded findings:

- `P17-RV-G4B-02-F01` (**high**) found that the existing terminal confirmation defaults to Yes and
  the 50-path candidate could not carry/render the exact pre-approval selection. The correction
  adds only the shared interaction request type, CLI adapter, and focused runtime test; the
  structured preview carries exact operation IDs/order and confirmation explicitly defaults No.
- `P17-RV-G4B-02-F02` (**medium**) found that saved-plan v1 has no batch-policy field. The corrected
  contract uses schema-v1 implicit fail-fast behavior and rejects a CLI batch override instead of
  claiming nonexistent recorded batch semantics.
- `P17-RV-G4B-02-F03` (**high**) found that APPLY-TS10's original-byte and bounded migration/
  first-version-2-cleanup crash assertions were omitted. The correction retains those exact cases
  on existing authorities while leaving G4B-03's holistic P4B-TS05 matrix untouched.
- `P17-RV-G4B-02-F04` (**low**) corrected the accepted grammar from `--yes` to `-y, --yes`.
- `P17-RV-G4B-02-F05` (**medium**) clarified that closed portable-location remapping is the sole
  permitted representation change. Every remapped resource/image must reproject byte-for-byte to
  the saved authorization; all IDs, edges, and non-location facts remain unchanged.
- `P17-RV-G4B-02-F06` (**medium**) found that the private runtime planning-command validator would
  reject `apply`. The final 54-path boundary includes `planning/create.ts` only for the matching
  additive runtime literal and normal closed validation; casts and bypasses are forbidden.

Corrected clean pin `3c288e53a9622279435b9857f5e6374c45a9c618` received **GO** with 0.99
confidence. The accepted Ready-plan SHA-256 is
`bcb1cae71b6710ca9b871e09211681b5c2f1fe112507aeb336baeb80596f4306`; the reviewed
pre-acceptance evidence SHA-256 is
`77cb18e0fc6279562cb2589d70c5c3b5806fbe42173ccd12cd85f3f5d0266ad9`.

The reviewer independently verified the exact 31-primary, 29-required-now, 22-new, seven-replay,
one-downstream arithmetic; all selector targets; 37-command matrix; 54 unique paths; four focused
architecture seams; fresh No-default approval; saved schema-v1 fail-fast behavior; exact token
reprojection; APPLY-TS10/G4B-03 separation; and closed planning runtime validation. `bun run
check:p17` passed. The reviewer edited nothing. No blocker, major, or minor Ready finding remains.

**Ready review result:** PASS.

## Accepted Ready result

The user's standing authorization to complete P17 without another approval stop applies to this
reviewed, in-scope governance transition. No schedule, entity ownership, selector, target, tier,
normative behavior, or dependency changes.

**Ready result:** PASS. G4B-02 advances to `ready`; mapping and Ready gates pass; P4B-T03/T04
advance to ready with the frozen plan/evidence, 54 paths, and 37 commands. The other 29 primary
entities remain pristine planned. No test-first, implementation, green, refactor, adversarial,
traceability, or sign-off gate is implied.

## Test-first evidence

Only the ten accepted selector/fixture paths changed at immutable test pin `425bf3bf6665d1713f9f391f49d2389a1bf5b138`. No
production, documentation, catalog, existing signed selector semantics, coordinator, recovery,
lock, durable-I/O, or saved-plan schema file changed.

All 22 exact selectors ran independently and failed for the absent live `apply` product surface.
The 14 apply selectors and plan TS11 fail on unregistered `--file`/`--plan`/`--dry-run`/
`--locked` grammar or absent apply help; option TS09 fails because no current apply CommandSpec or
preflight relations exist. P4B-TS02/03/07 and WF06/07/08 reach their hermetic setup and fail at the
same missing apply grammar after exercising their prerequisite plan, artifact, portability,
redaction, and CI checks.

The exact aggregate is 28 failing tests, zero passing tests, and zero skipped tests across 22
independent selector commands. No failure comes from TypeScript, formatting, fixture construction,
missing data, public network, external secrets, timeout, skip/todo, or process leakage.
`bun run test:smoke` passes 5 tests with 3,735 assertions. TypeScript, changed-file Biome,
architecture boundaries, diff hygiene, pre-commit gitleaks, and selector-owner cardinality pass.
Recovery smoke and the full matrix were not run because this checkpoint changes test definitions
only and does not modify coordinator/recovery mechanics.

**Test-first result:** PASS. G4B-02 may advance to active with the test-first gate passed;
P4B-T03/T04 advance to red; all 22 new validation entities advance to failing at their exact
executable targets. Minimal implementation and every green/review/sign-off gate remain pending.

## Approved option-relation ownership amendment

The first minimal CLI-surface implementation expressed every accepted fresh/saved option rule
except one exact `EWP-OPT-TS09` obligation: repeatable distinct `--tool` values must pass while an
identical repeated value must fail with usage exit 2 before I/O. Existing cardinality relations can
limit occurrences but cannot distinguish those two cases. The generic invocation parser already
canonicalizes short/long spellings and captures their values, so no parser or command-specific
branch is required.

Independent read-only reviewer `/root/g4b02_distinct_review` returned **GO** at **0.99 confidence**
with no high, medium, or blocking finding. The accepted amendment adds exactly:

- `packages/cli/src/spec/types.ts`, limited to one generic `distinct-values` relation variant; and
- `packages/cli/src/spec/relations.ts`, limited to exact duplicate detection over already captured
  option values.

The already-owned registry supplies the one `skillsmith.apply.tool.distinct-values` record and
self-validates that its operand exists, accepts a value, and is repeatable. The already-owned option
contract proves zero/one/distinct values pass; identical values, including mixed flag spellings,
fail; malformed records fail self-validation; and removing the record removes the behavior. Exact
string equality is the complete comparison policy. No normalization layer, comparator abstraction,
schema, coordinator, recovery, durable-I/O, or execution-boundary change is introduced.

The frozen boundary therefore advances from exactly 54 to exactly 56 unique product/test paths.
No entity, selector, tier, dependency, schedule, or future-group scope changes. The user standing
authorization permits this independently reviewed in-scope correction without another approval
stop.

### Red execution receipt EWP-CMD-APPLY-TS01

- Validation ID: `EWP-CMD-APPLY-TS01`
- Command: `bun test packages/cli/tests/contracts/apply.test.ts --test-name-pattern EWP-CMD-APPLY-TS01`
- Exit status: `1`
- Result: Selector failed with 1 failing test, 0 passed, and 0 skipped because the live apply product surface is absent.
- Revision: `425bf3bf6665d1713f9f391f49d2389a1bf5b138`

### Red execution receipt EWP-CMD-APPLY-TS02

- Validation ID: `EWP-CMD-APPLY-TS02`
- Command: `bun test packages/cli/tests/contracts/apply.test.ts --test-name-pattern EWP-CMD-APPLY-TS02`
- Exit status: `1`
- Result: Selector failed with 1 failing test, 0 passed, and 0 skipped because the live apply product surface is absent.
- Revision: `425bf3bf6665d1713f9f391f49d2389a1bf5b138`

### Red execution receipt EWP-CMD-APPLY-TS03

- Validation ID: `EWP-CMD-APPLY-TS03`
- Command: `bun test packages/cli/tests/contracts/apply.test.ts --test-name-pattern EWP-CMD-APPLY-TS03`
- Exit status: `1`
- Result: Selector failed with 1 failing test, 0 passed, and 0 skipped because the live apply product surface is absent.
- Revision: `425bf3bf6665d1713f9f391f49d2389a1bf5b138`

### Red execution receipt EWP-CMD-APPLY-TS04

- Validation ID: `EWP-CMD-APPLY-TS04`
- Command: `bun test packages/cli/tests/contracts/apply.test.ts --test-name-pattern EWP-CMD-APPLY-TS04`
- Exit status: `1`
- Result: Selector failed with 1 failing test, 0 passed, and 0 skipped because the live apply product surface is absent.
- Revision: `425bf3bf6665d1713f9f391f49d2389a1bf5b138`

### Red execution receipt EWP-CMD-APPLY-TS05

- Validation ID: `EWP-CMD-APPLY-TS05`
- Command: `bun test packages/cli/tests/contracts/apply.test.ts --test-name-pattern EWP-CMD-APPLY-TS05`
- Exit status: `1`
- Result: Selector failed with 1 failing test, 0 passed, and 0 skipped because the live apply product surface is absent.
- Revision: `425bf3bf6665d1713f9f391f49d2389a1bf5b138`

### Red execution receipt EWP-CMD-APPLY-TS06

- Validation ID: `EWP-CMD-APPLY-TS06`
- Command: `bun test packages/cli/tests/contracts/apply.test.ts --test-name-pattern EWP-CMD-APPLY-TS06`
- Exit status: `1`
- Result: Selector failed with 1 failing test, 0 passed, and 0 skipped because the live apply product surface is absent.
- Revision: `425bf3bf6665d1713f9f391f49d2389a1bf5b138`

### Red execution receipt EWP-CMD-APPLY-TS07

- Validation ID: `EWP-CMD-APPLY-TS07`
- Command: `bun test packages/cli/tests/contracts/apply.test.ts --test-name-pattern EWP-CMD-APPLY-TS07`
- Exit status: `1`
- Result: Selector failed with 1 failing test, 0 passed, and 0 skipped because the live apply product surface is absent.
- Revision: `425bf3bf6665d1713f9f391f49d2389a1bf5b138`

### Red execution receipt EWP-CMD-APPLY-TS08

- Validation ID: `EWP-CMD-APPLY-TS08`
- Command: `bun test packages/cli/tests/contracts/apply.test.ts --test-name-pattern EWP-CMD-APPLY-TS08`
- Exit status: `1`
- Result: Selector failed with 1 failing test, 0 passed, and 0 skipped because the live apply product surface is absent.
- Revision: `425bf3bf6665d1713f9f391f49d2389a1bf5b138`

### Red execution receipt EWP-CMD-APPLY-TS09

- Validation ID: `EWP-CMD-APPLY-TS09`
- Command: `bun test packages/cli/tests/contracts/apply.test.ts --test-name-pattern EWP-CMD-APPLY-TS09`
- Exit status: `1`
- Result: Selector failed with 1 failing test, 0 passed, and 0 skipped because the live apply product surface is absent.
- Revision: `425bf3bf6665d1713f9f391f49d2389a1bf5b138`

### Red execution receipt EWP-CMD-APPLY-TS10

- Validation ID: `EWP-CMD-APPLY-TS10`
- Command: `bun test packages/cli/tests/contracts/apply.test.ts --test-name-pattern EWP-CMD-APPLY-TS10`
- Exit status: `1`
- Result: Selector failed with 1 failing test, 0 passed, and 0 skipped because the live apply product surface is absent.
- Revision: `425bf3bf6665d1713f9f391f49d2389a1bf5b138`

### Red execution receipt EWP-CMD-APPLY-TS11

- Validation ID: `EWP-CMD-APPLY-TS11`
- Command: `bun test packages/cli/tests/contracts/apply.test.ts --test-name-pattern EWP-CMD-APPLY-TS11`
- Exit status: `1`
- Result: Selector failed with 1 failing test, 0 passed, and 0 skipped because the live apply product surface is absent.
- Revision: `425bf3bf6665d1713f9f391f49d2389a1bf5b138`

### Red execution receipt EWP-CMD-APPLY-TS12

- Validation ID: `EWP-CMD-APPLY-TS12`
- Command: `bun test packages/cli/tests/contracts/apply.test.ts --test-name-pattern EWP-CMD-APPLY-TS12`
- Exit status: `1`
- Result: Selector failed with 1 failing test, 0 passed, and 0 skipped because the live apply product surface is absent.
- Revision: `425bf3bf6665d1713f9f391f49d2389a1bf5b138`

### Red execution receipt EWP-CMD-APPLY-TS13

- Validation ID: `EWP-CMD-APPLY-TS13`
- Command: `bun test packages/cli/tests/contracts/apply.test.ts --test-name-pattern EWP-CMD-APPLY-TS13`
- Exit status: `1`
- Result: Selector failed with 1 failing test, 0 passed, and 0 skipped because the live apply product surface is absent.
- Revision: `425bf3bf6665d1713f9f391f49d2389a1bf5b138`

### Red execution receipt EWP-CMD-APPLY-TS14

- Validation ID: `EWP-CMD-APPLY-TS14`
- Command: `bun test packages/cli/tests/contracts/apply.test.ts --test-name-pattern EWP-CMD-APPLY-TS14`
- Exit status: `1`
- Result: Selector failed with 1 failing test, 0 passed, and 0 skipped because the live apply product surface is absent.
- Revision: `425bf3bf6665d1713f9f391f49d2389a1bf5b138`

### Red execution receipt EWP-CMD-PLAN-TS11

- Validation ID: `EWP-CMD-PLAN-TS11`
- Command: `bun test packages/cli/tests/contracts/plan.test.ts --test-name-pattern EWP-CMD-PLAN-TS11`
- Exit status: `1`
- Result: Selector failed with 1 failing test, 0 passed, and 0 skipped because the live apply product surface is absent.
- Revision: `425bf3bf6665d1713f9f391f49d2389a1bf5b138`

### Red execution receipt EWP-OPT-TS09

- Validation ID: `EWP-OPT-TS09`
- Command: `bun test packages/cli/tests/contracts/options.test.ts --test-name-pattern EWP-OPT-TS09`
- Exit status: `1`
- Result: Selector failed with 2 failing tests, 0 passed, and 0 skipped because the live apply product surface is absent.
- Revision: `425bf3bf6665d1713f9f391f49d2389a1bf5b138`

### Red execution receipt EWP-P4B-TS02

- Validation ID: `EWP-P4B-TS02`
- Command: `bun test tests/ergonomics/phase/EWP-P4B-TS02.test.ts --test-name-pattern EWP-P4B-TS02`
- Exit status: `1`
- Result: Selector failed with 2 failing tests, 0 passed, and 0 skipped because the live apply product surface is absent.
- Revision: `425bf3bf6665d1713f9f391f49d2389a1bf5b138`

### Red execution receipt EWP-P4B-TS03

- Validation ID: `EWP-P4B-TS03`
- Command: `bun test tests/ergonomics/phase/EWP-P4B-TS03.test.ts --test-name-pattern EWP-P4B-TS03`
- Exit status: `1`
- Result: Selector failed with 3 failing tests, 0 passed, and 0 skipped because the live apply product surface is absent.
- Revision: `425bf3bf6665d1713f9f391f49d2389a1bf5b138`

### Red execution receipt EWP-P4B-TS07

- Validation ID: `EWP-P4B-TS07`
- Command: `bun test tests/ergonomics/phase/EWP-P4B-TS07.test.ts --test-name-pattern EWP-P4B-TS07`
- Exit status: `1`
- Result: Selector failed with 3 failing tests, 0 passed, and 0 skipped because the live apply product surface is absent.
- Revision: `425bf3bf6665d1713f9f391f49d2389a1bf5b138`

### Red execution receipt EWP-WF06

- Validation ID: `EWP-WF06`
- Command: `bun test tests/ergonomics/workflows/EWP-WF06.test.ts --test-name-pattern EWP-WF06`
- Exit status: `1`
- Result: Selector failed with 1 failing test, 0 passed, and 0 skipped because the live apply product surface is absent.
- Revision: `425bf3bf6665d1713f9f391f49d2389a1bf5b138`

### Red execution receipt EWP-WF07

- Validation ID: `EWP-WF07`
- Command: `bun test tests/ergonomics/workflows/EWP-WF07.test.ts --test-name-pattern EWP-WF07`
- Exit status: `1`
- Result: Selector failed with 1 failing test, 0 passed, and 0 skipped because the live apply product surface is absent.
- Revision: `425bf3bf6665d1713f9f391f49d2389a1bf5b138`

### Red execution receipt EWP-WF08

- Validation ID: `EWP-WF08`
- Command: `bun test tests/ergonomics/workflows/EWP-WF08.test.ts --test-name-pattern EWP-WF08`
- Exit status: `1`
- Result: Selector failed with 1 failing test, 0 passed, and 0 skipped because the live apply product surface is absent.
- Revision: `425bf3bf6665d1713f9f391f49d2389a1bf5b138`

## Minimal implementation

### Approved scoped saved-validation ownership amendment

The first saved-plan validator proved that the existing v1 shape loses two facts required by the
Ready contract when a reviewed plan is already converged: it records a global manifest/lock hash
and the realized selected tools/scopes, but not whether those results came from wildcard selection
or explicit predicates. Consequently, validator-only logic cannot distinguish a selected
declaration change from an unrelated unselected declaration change without guessing.

Independent read-only reviewer `/root/g4b02_saved_review` returned **NO-GO** on the incomplete
validator and **GO** at **0.96 confidence** for one narrow projection amendment. The accepted
amendment adds exactly these two product/test paths to the frozen boundary:

- `packages/core/src/reconcile/saved.ts`; and
- `packages/core/tests/reconcile/saved.test.ts`.

The projection reuses the existing v1 `selectionPrecondition.skills/tools/scopes` fields as the
original selection predicates, with an empty list meaning wildcard, while `plan.selection`
continues to carry the realized output. Existing manifest-semantic, lock-canonical, resource, and
selection-set fields carry deterministic scoped fingerprints for matching normalized
declarations, pins, and complete-set prune/delete authority. The validator recomputes those facts
from current read-only repositories without invoking the planner. Older v1 artifacts without the
scoped marker retain conservative exact-global validation.

No codec, plan type, schema version, hash domain, public wire contract, planner, coordinator,
recovery mechanism, or transaction engine changes. The frozen boundary advances from exactly 56
to exactly 58 unique product/test paths. All other entity, selector, tier, dependency, and schedule
ownership remains unchanged.

Implementation remains in progress pending focused green tests and repeat independent review.

### Approved P2-TS08 compatibility-inventory amendment

After the live `apply-report@1` contract and `applyV1Codec` runtime export were added, the required
`EWP-P2-TS08` compatibility replay passed every saved-artifact, hostile-input, relationship, and
migration behavior family but failed its two additive closed-inventory canaries. Independent
read-only audit `/root/g4b02_saved_compat` classified both failures as expected inventory drift,
not a runtime or saved-plan regression, and recommended the exact correction at **0.99
confidence**.

The accepted amendment adds only `tests/ergonomics/phase/EWP-P2-TS08.test.ts` to G4B-02 ownership,
limited to adding `apply-report@1` to `EXPECTED_WIRE_IDENTITIES` and `applyV1Codec` to
`V1_RUNTIME_EXPORTS`. No behavior assertion, artifact schema, migration, malformed-input case, or
existing identity changes. The frozen boundary advances from exactly 58 to exactly 59 unique
paths; all other ownership remains unchanged.

The first corrected replay passed the original two canaries and then reached the fixture's closed
TypeScript export union, where `_V1RuntimeClosed` lacked the same new `applyV1Codec` member. The
replay was otherwise 6/7 green with 1,681 assertions. The same independent audit classified this
as the companion compile-time inventory edge at 0.99 confidence. Ownership therefore adds only
`tests/ergonomics/fixtures/p2-ts08/public-types.ts`, limited to that one union member. The frozen
boundary advances from exactly 59 to exactly 60 unique paths; no runtime or contract behavior
changes.

### Corrected non-directory placement-root ownership amendment

Exact `EWP-CMD-APPLY-TS09` and `EWP-CMD-APPLY-TS11` prove one shared observation gap after the
physical apply path became live. A tool skills root that is a non-directory filesystem node is
reported as existing by the followed-stat `fileExists` port, after which shared placement
inventory calls `listDir` and aborts the whole plan observation. Desired-placement classification
can independently probe `root/skill` and receive `ENOTDIR`. The accepted apply contract instead
needs executable mode to reach physical execution, report the tool's own truthful failure, and
leave independent tools eligible for fail-fast or continue-on-error scheduling. Fresh `--check`
must report the same read-only integrity conflict with failure exit 1, which outranks drift,
without invoking physical execution.

The proposed ownership amendment adds exactly one product path to the frozen boundary:

- `packages/core/src/agents/placement-shared.ts`.

The corrected implementation uses one shared root classification in both `listPlacements` and
`classifyPlacement`: `absent` and the port's broad `file` kind (regular files and other
non-directory/non-symlink nodes) yield empty inventory/an absent child without probing beneath the
root; directory and symlink roots retain followed filesystem behavior. Thus symlinks to
directories remain listable, dangling root symlinks retain the current empty result under
`ENOENT`, and symlinks to non-directories or unreadable roots retain safe observation failures.
The already-owned application service adds only a fresh-check read-only destination-root
integrity probe. A non-directory destination root returns the existing strict apply report with a
sanitized failure diagnostic and exit 1; results stay empty and no mutation authority runs.

Existing owned TS09/TS11 scenarios prove physical partial/fail-fast behavior and check-mode exit
precedence. The already-owned `packages/core/tests/reconcile/apply.test.ts` will characterize
absent, file/special-kind, directory, symlink-to-directory, symlink-to-file, dangling-symlink, and
unreadable-root behavior without adding another path. No adapter descriptor, planner, saved-plan
schema, codec, coordinator, transaction, journal, ledger, hash domain, or output contract changes.
The frozen boundary advances from exactly 68 to exactly 69 unique product/test paths.

Independent reviewer `/root/g4b02_placement_root_amend_review` returned **NO-GO** at **0.99
confidence** on the first list-only candidate because `classifyPlacement` retained an `ENOTDIR`
path, fresh check never executes physical mutations, and the claimed symlink/special-node
semantics were inaccurate and unproved. The corrected candidate closes all four findings and
received fresh **GO** from the same independent reviewer at **0.98 confidence**. The reviewer
confirmed that exact-root read-only check probes, empty results/zero failed-result counts, and the
documented root-kind matrix preserve scheduler authority and fail-closed symlink/permission
behavior.

The catalog and catalog-owner cardinality checks are structurally valid at exactly 70 unique paths.
The user's standing approval authorizes this corrected in-scope implementation.

### Physical executor module ownership correction

A focused direct-source replay after physical integration found that the executor implementation
had already been extracted from the validator into the untracked
`packages/core/src/reconcile/apply-execution.ts`, while the catalog omitted that path and the
existing `reconcile/index.ts` export still named `apply.ts`. CLI subprocess coverage reached the
implementation through its running module graph, but direct core/application tests correctly
failed the missing named export. The implementation must not sign off with an unowned product file
or an environment-dependent export graph.

The accepted correction adds exactly that existing product path to the frozen boundary. The
already-owned `reconcile/apply.ts` remains the saved/fresh validation authority;
`reconcile/apply-execution.ts` owns only physical guard observation, binding construction, source
materialization, transaction dispatch, and cleanup; and the already-owned `reconcile/index.ts`
exports the physical entry point and its runtime types from their actual module. Direct tests may
import the executor module rather than relying on a compatibility re-export from the validator.

Independent read-only reviewer `/root/g4b02_placement_root_amend_review` returned **GO** at **0.99
confidence** for this module correction. It verified structural catalog validity, 70 unique owned
paths, TypeScript, 26/26 reconcile/apply tests, 25/25 reconcile/execute tests, and 12/12 application
apply-service tests. This is a module-boundary and ownership correction, not a behavior or contract
amendment. It adds no operation, option, schema, codec, coordinator, transaction, journal, ledger,
or hash domain. Together with the corrected placement-root amendment, the frozen boundary advances
from exactly 68 to exactly 70 unique product/test paths.

### Approved cross-scope transaction ownership amendment

Execution design review proved that `move-scope` cannot be truthfully composed from the existing
single-location placement binding. Its approved before and after live images intentionally belong
to different user/project roots. Running an uninstall followed by an install would create two
durable transaction identities for one reviewed operation and leave a split crash window after the
first commit.

Independent read-only reviewer `/root/move_scope_amend_review` rejected the initial six-path
candidate and returned **GO** at **0.98 confidence** for this corrected eight-path amendment:

- `packages/core/src/place/swap.ts`;
- `packages/core/src/place/logical-transactions.ts`;
- `packages/core/src/place/recovery.ts`;
- `packages/core/src/artifacts/ledger-history.ts`;
- `packages/core/tests/place/swap.test.ts`;
- `packages/core/tests/place/recovery.test.ts`;
- `packages/core/tests/place/crash-sweep-acquire.test.ts`; and
- `packages/core/tests/artifacts/ledger-writer.test.ts`.

The two added history paths are mandatory because a cross-root journal deliberately has no single
pair identity. Existing history anchoring otherwise produces no placement anchor and rejects both
pending recovery preflight and committed finalization as invalid history. The accepted correction
extends the existing logical transaction engine's anticipated multi-pair branch: one transaction
ID, operation ID, pair ID, phase sequence, and history entry move one ledger pair from source to
destination; resume and rollback cover both live locations. It adds no journal/ledger schema,
codec, vocabulary, coordinator, scheduler, lock-order, or hash-domain change.

The frozen boundary advances from exactly 60 to exactly 68 unique paths. This is an in-scope
reliability correction required to implement the already accepted `move-scope` operation, not a
product-contract or schedule change. The user's standing P17 authorization applies without another
human-review pause.

### Saved-plan validation and read-only application checkpoint

The saved-plan validator and saved dry-run/check application boundary now validate the exact
reviewed operation projection without invoking the planner, prompting, acquiring mutation locks,
writing state, changing the plan artifact, or disclosing validation-machine paths. Fresh apply uses
the same strict in-memory validator so its rendered and approved operation IDs are the IDs supplied
to execution.

The first independent adversarial review recorded and reopened these findings:

- `P17-RV-G4B-02-F07` (**high**): recompute the complete current selected/prune live inventory so a
  newly added in-scope placement invalidates delete authority;
- `P17-RV-G4B-02-F08` (**high**): validate exact whole-artifact before-images for project migration
  and lock replacement without converting ordinary scoped validation into a global byte guard;
- `P17-RV-G4B-02-F09` (**medium**): replace the `Map` subclass with composition so even
  `Map.prototype.set.call(...)` cannot mutate token bindings;
- `P17-RV-G4B-02-F10` (**medium**): bind machine absolute artifact selectors to the exact signed
  project context rather than an ancestor heuristic; and
- `P17-RV-G4B-02-F11` (**medium**): preserve sanitized capability, executor-schema, and hash-schema
  incompatibility taxonomy instead of collapsing all decode failures into one generic artifact
  message.

All five findings are closed. The projection signs observed live inventory and a separate exact
migration semantic/byte fact; the validator replays current relevant inventory, validates
operation before-images, enforces an exact project-context resource, exposes a composition-based
immutable `ReadonlyMap`, and maps only sanitized saved-codec field paths to domain-specific state
diagnostics with regeneration guidance. Legacy v1 artifacts retain conservative validation.

Verification receipts:

- saved projection/validator/application: **41/41 passed**, **319 assertions**;
- final core apply/application correction replay: **25/25 passed**, **186 assertions**;
- exact P4B-TS03 capability/executor/hash compatibility case: **1/1 passed**, **12 assertions**;
- EWP-CMD-APPLY-TS07: **1/1 passed**;
- EWP-P4B-TS07: **3/3 passed**;
- smoke lane: **5/5 passed**, **3,735 assertions**; and
- TypeScript, changed-file Biome, architecture boundaries, and diff hygiene: PASS.

Fresh reviewer `/root/g4b02_saved_rereview` returned final **GO** at **0.99 confidence** with no
stable finding. Saved execution itself remains pending on the shared execution adapter and is not
claimed by this checkpoint.

### Exact reconciliation execution-dispatch checkpoint

The shared reconciliation dispatcher now accepts only an exactly owned, canonical
`OperationPlan<'apply'>`, preclassifies every operation before invoking a physical authority, and
requires one immutable execution binding for each approved operation in the approved topological
order. Reconciliation v1 fails closed for operation kinds whose physical semantics are not yet
implemented; `move-scope` remains deliberately blocked at this boundary until the reviewed
cross-root transaction primitive is connected.

Independent adversarial review recorded and closed these execution-boundary defects:

- `P17-RV-G4B-02-F12` (**high**): own and canonically validate the complete plan before any
  physical factory can observe it;
- `P17-RV-G4B-02-F13` (**high**): snapshot returned binding identities, callbacks, observations,
  and bounded-force data rather than retaining mutable factory-owned references;
- `P17-RV-G4B-02-F14` (**medium**): sanitize hostile plan/binding access failures so getters and
  thrown payloads cannot escape the execution boundary;
- `P17-RV-G4B-02-F15` (**high**): preclassify the whole plan so a later unsupported or cross-root
  operation cannot cause an earlier physical factory call; and
- `P17-RV-G4B-02-F16` (**medium**): reject non-ordinary arrays, including proxy, subclassed,
  symbol-bearing, sparse, accessor, and non-enumerable-index inputs, before making the immutable
  owned copy.

The dispatcher also validates the full bounded-force effect canonically and rejects duplicate,
missing, malformed, reordered, or semantically mismatched operation/binding identities.

Verification receipts:

- reconciliation execution dispatcher: **11/11 passed**, **83 assertions**;
- changed-file Biome and diff hygiene: PASS; and
- independent reviewer `/root/execution_dispatch_review`: final **GO** at **0.995 confidence** with
  no remaining finding.

This is a boundary checkpoint only. Physical execution and application-service integration remain
pending and are not claimed here.

### Artifact registry guard-validation export amendment

The required `EWP-P2-TS08` architecture replay correctly rejected the reconciliation executor's
direct import from `artifacts/plan-codec.ts`: concrete persisted-codec ownership remains limited to
the artifact registry and public contract surfaces. The executor still needs the existing strict
execution-guard subset validator before it constructs coordinator preconditions, so bypassing or
duplicating that saved-plan grammar would weaken the boundary.

The approved amendment adds exactly `packages/core/src/artifacts/registry.ts` to G4B-02 ownership.
That registry only re-exports the already implemented `validatePlanExecutionGuardsV1` seam from its
existing concrete plan-codec import; `reconcile/execute.ts` consumes the seam through the registry.
No schema, parser, operation, guard identity, hash, coordinator, or persistence behavior changes.
The frozen boundary advances from exactly 70 to exactly 71 unique paths. The independent
adversarial review at the major G4B-02 gate must verify this shared signed-off registry amendment
and the exact `EWP-P2-TS08` parse-ownership closure before sign-off.

### Cross-root move-scope transaction checkpoint

The reviewed cross-scope amendment now implements `move-scope` as one durable transaction with one
operation, pair, transaction, and terminal history identity. The source ledger membership remains
authoritative until terminal commit atomically removes it and inserts the destination membership;
the transaction deliberately creates no legacy pair shadow. Copy and symlink representations move
in both directions between user and project roots while exact pinned content, representation,
classification, and symlink store targets remain bound to the reviewed operation.

The first independent implementation review recorded two blocking defects:

- `P17-RV-G4B-02-F17` (**high**): a journal whose rollback disposition was already durable could
  enter forward recovery when a later caller requested resume; and
- `P17-RV-G4B-02-F18` (**high**): prepared preflight did not yet bind all pinned-content and symlink
  target facts to the authoritative ledger pair.

Both findings are closed. Durable rollback disposition now overrides requested resume at both the
recovery router and direct transaction seam. A crash immediately after the rollback boundary
resumes rollback, restores the source, removes destination/staging, and commits one rollback
history row. Prepared preflight binds the exact pinned hash, operation source, before/after images,
representation, classification, and store target before persisting `prepared`. Recovery discovers
an unshadowed pending move from either live root; committed-journal sweep reclaims a source backup
left after terminal ledger persistence while retaining source and destination history anchors.

Verification receipts:

- move transaction and routed recovery: **20/20 passed**, **113 assertions**;
- committed move backup sweep: **1/1 passed**, **4 assertions**;
- dual history anchors: **1/1 passed**, **4 assertions**;
- TypeScript and architecture boundaries: PASS;
- Biome across all eight amended files and owned diff hygiene: PASS; and
- independent child re-review: final **GO** at **0.98 confidence**.

The reviewed reconcile binding APIs are `runMoveScopeTransaction[Observed](request, operation)`;
normal recovery remains behind `recoverPlacement[Observed]`. The exhaustive recovery matrix is
reserved for the G4B-02 major sign-off lane under the repository test policy. One non-blocking
maintainability note remains: shared two-location phase/probe helpers may be extracted later if a
second consumer appears; no speculative abstraction is introduced in this checkpoint.

### Approved exact guard and live topology ownership amendment

Physical integration exposed three necessary files outside the prior 71-path freeze. Independent
read-only reviewer `/root/g4b02_architecture_review` returned **GO** at **0.99 confidence** for
adding exactly:

- `packages/core/src/artifacts/plan-codec.ts` for strict ownership/decoding of the saved-plan
  execution-guard subset and canonical capability-scope domain ordering;
- `packages/core/src/place/live-repository.ts` for authoritative absent-child observation when a
  signed parent is a non-directory, without an invalid child probe; and
- `packages/core/tests/place/live-repository.test.ts` for the exact `ENOTDIR` and parent-replacement
  stale-revision proof.

The guard decoder reuses the existing saved-plan grammar and adds no schema, version, hash domain,
or parallel validator. Capability scopes now share one tuple for strict decoding and canonical
ordering; a focused multi-scope round trip preserves `user`, then `project`, and recomputes the
signed capability hash exactly. The live repository binds the definite non-directory parent's
metadata identity while treating the unreachable child as absent; symlink and unreadable-parent
behavior remain fail closed. Relaxing default filesystem ports or bypassing repository observation
would have been broader and was rejected.

The same review confirmed that one shared lifecycle cursor is correct. The apply binding now uses
the existing placement rule: stage the ledger, operation-owned live/store resources, and every
snapshotted mutable sibling with the same authenticated parent identity. This advances only the
controller-owned shared-parent effects between independent groups while retaining exact repository
stage comparison for external changes. A two-group physical regression completes both placements
and ledger records. No new coordinator, lifecycle controller, lock, transaction engine, or recovery
mechanism was introduced.

Additional focused corrections remained inside already-owned files: every materialized source is
cleaned until successful ownership transfer; live-content validation uses the same v1 source-content
hash as planning/execution; canonical inverse projection is normalized through the saved-plan codec;
and a fresh pre-approval stale report truthfully records `approval: not-required`,
`validation: stale`, no results, and no prompt. Focused receipts are: application stale-before-prompt
**1/1**, strict apply contract **8/8**, reconcile apply **30/30**, capability-scope round trip
**1/1**, and live repository non-directory observation **1/1**, all with zero failures.

The frozen boundary therefore advances from exactly **71 to 74 unique product/test paths**. No
entity, selector, tier, dependency, schedule, public grammar, operation kind, artifact schema,
coordinator, or recovery ownership changes. No policy, human-review, network, or external-system
blocker exists.

### Impacted test ownership and cold-run efficiency amendment

Owned-diff closure found two required impacted tests outside the frozen G4B-02 path list. The
existing `packages/core/tests/place/execute.test.ts` additions directly characterize the placement
binding seam used by the placement coordinator: exact pair delegation, immutable captured
authorities, and rejection of missing/duplicate stage identities. Reconciliation composes the same
lifecycle contract through its acquisition authority; it does not call this helper directly. The
required `EWP-WF15` replay
also exposed a pre-existing five-second harness bottleneck because five fixture fleets were
constructed and executed serially. `tests/ergonomics/workflows/EWP-WF15.test.ts` now starts four
independent fleets concurrently and safely reuses the mutation-free refusal fleet for the later
fail-fast human product. It retains all five result, stream, cancellation, redaction, and
no-mutation assertions; its focused scheduling runtime fell from the timeout boundary to about one
second without increasing a timeout or weakening a contract.

The approved amendment adds exactly those two test paths. It changes no product behavior, public
surface, schema, parser, codec, scheduler, coordinator, recovery rule, or durability format. The
frozen boundary advances from exactly 74 to exactly 76 unique paths. The major-gate independent
review must verify both additions and confirm that the workflow fixtures share no state.

### Permission taxonomy ownership amendment

The adversarial exit-taxonomy review traced local Git `EACCES`/`EPERM` loss to the acquisition
fetch boundary: `packages/core/src/acquire/fetch.ts` converted every Git exception to
`source-unresolvable` before apply could apply the signed permission precedence. The bounded
correction preserves `permission-denied` only for the existing permission codes and leaves every
other Git failure on the existing source-error path. Its focused regression in
`packages/core/tests/acquire/fetch.test.ts` proves both classifications and removes the synthetic
private error material from the returned permission result.

These are the only two paths outside the 76-path boundary needed by the correction. They add no
operation kind, public grammar, artifact schema, coordinator, recovery mechanism, error class, or
network behavior. The frozen boundary advances from exactly **76 to 78 unique paths**, and the
terminal command inventory advances from **37 to 38** by adding the full hermetic fetch unit file.
The standing authorization covers this local reliability correction; final adversarial and
traceability review must verify the amendment before sign-off.

### Bootstrap lock and v1 swap compatibility amendments

The physical executor cannot acquire the existing ledger-target lock while its parent is absent,
and creating that parent before the execution snapshot leaves an unowned mutation and makes the
snapshot stale against itself. The approved bounded lock amendment adds one stable
`apply-ledger-bootstrap` lock beneath the existing artifact coordination root. Its explicit order
is bootstrap, artifact central, artifact members, then ledger. The bootstrap lease encloses parent
creation, authoritative snapshot capture, final coordinator validation, and execution. When it
created the parent and snapshot or pre-execution validation fails without operation results, it
uses atomic empty-directory removal; it never recursively removes a directory that acquired
content. The post-materialization preliminary-snapshot regression proves no directory is created,
and a separate locked-snapshot regression proves the newly created parent is removed after a
failure in the former residue window.

Owned-diff closure also identified `packages/core/tests/place/swap-acquire.test.ts` as the direct
compatibility characterization for the owned `packages/core/src/place/swap.ts` v1 hash correction.
Its focused cases prove that direct acquisition still accepts the legacy copy hash and that replace
and uninstall reclaim unchanged copies pinned with the v1 source-content hash. This adds no product,
public, schema, coordinator, recovery, or lock scope. The frozen boundary advances from exactly
**78 to 79 unique paths**, and the terminal command inventory advances from **38 to 39** by adding
the full hermetic swap-acquire unit file. The user's standing approval covers both amendments; the
final independent review must verify their implementation and governance closure.

### Terminal matcher-mutation harness amendment

The terminal full-suite replay exposed a Bun asymmetric-matcher mutation in the existing doctor
capability test: matching the shared registry capability with `expect.arrayContaining` could
replace its `scopes` array, corrupting later registry consumers when test-file order changed. The
test now asserts `supported`, array shape, and the three required scopes directly without placing an
asymmetric matcher against shared state. This is a test-only reliability correction and changes no
product, public, schema, command, coordinator, recovery, or lock behavior. The frozen boundary
advances from exactly **79 to 80 unique paths**, and the terminal command inventory advances from
**39 to 40** with the focused doctor capability assertion. Final independent review must verify
that the exact facts are preserved and the previously poisoned doctor-to-agent-registry order is
green.

The literal downstream-order replay is:

```sh
bun test packages/cli/tests/contracts/doctor.test.ts packages/core/tests/agents/registry.test.ts --test-name-pattern 'every known adapter advertises the diagnostics capability used by doctor|registry has an Agent for every supported tool|normalizes built-in placements without changing legacy method identity|rejects duplicate roots and any nonempty root set without one destination|does not invent notices for neutral alternate roots'
```

It passes **5 tests with 38 assertions**, zero failures, and zero skips. This proves the doctor
assertion leaves the shared registry facts intact for the four downstream registry consumers.

### Terminal fixture determinism and load-bound correction amendment

The terminal repository gate exposed three test-harness assumptions while the product behavior
remained correct. Two virtual placement filesystems reported an ancestor as absent despite owning
declared descendants; the helpers now preserve explicit node kinds and classify implicit ancestors
as directories, matching the real filesystem contract. Export TS07 previously sent SIGINT after a
fixed 800 ms startup delay; it now waits until both artifact compatibility locks prove that the
child reached the intended execution boundary before cancellation. The bounded-history test still
uses its explicit 15-second limit but derives the 258th journal from the durable 257-row model and
seeds canonical 0600 bytes directly, removing duplicate fixture construction and an unrelated
writer round trip while preserving the exact 257-to-258-to-256 scenario.

The correction adds no production, public, schema, planner, coordinator, recovery, lock, or timeout
behavior. It adds three newly touched test paths to group ownership:

- `packages/core/tests/agents/registry.test.ts`;
- `packages/cli/tests/contracts/export.test.ts`; and
- `tests/ergonomics/phase/EWP-P3B-TS06.test.ts`.

`packages/core/tests/artifacts/ledger-writer.test.ts` was already owned. The exact command inventory
adds the full ledger-writer and agent-registry files plus export TS07 and P3B-TS06 selectors.
Therefore the frozen boundary advances from **80 to 83 unique paths**, and the command inventory
advances from **40 to 44**. Focused receipts are green: the two placement-fixture files pass **27
tests with 186 assertions**; export TS07 passes three post-correction serial replays with **28
assertions each**; the optimized ledger row passes three idle replays in 4.41-6.78 seconds and the
full ledger-writer file passes **15 tests with 64 assertions**. Biome and diff hygiene pass without
changing any test-owned timeout.

**Minimal implementation result:** PASS. The implementation composes the existing planner,
saved-plan codec, interaction port, scheduler, coordinator, artifact authority, placement
transactions, recovery, and wire boundary. It introduces no parallel planner, parser, scheduler,
coordinator, transaction engine, recovery mechanism, or artifact schema; the sole added lock is the
approved bootstrap lease and explicit order documented above.

## Targeted and impacted green

All **29 required-now selector commands** ran serially from the catalog and passed. The exact
aggregate is **46 tests passed, 738 assertions, 0 failures, and 0 skips**:

- apply TS01..TS14: 14 selector tests, all green;
- plan TS06..TS12: seven selector commands and nine tests, all green;
- option TS09: two tests green;
- P4B-TS02/03/07: eight tests green; and
- WF06/07/08/15: 13 tests green.

All ten compatibility commands then passed independently: full options, planning types, acquisition
fetch, placement swap-acquire, P1-TS05/07/09/10/11, and P2-TS08 contributed **141 tests and 5,827
assertions**. Across the then-current exact **39-command** catalog matrix, that serial replay
completed **187 tests and 6,565 assertions** with zero assertion failures or skips. Two initial
timeout-only attempts overlapped a stale repository-wide test process: WF15 stopped at its
five-second harness limit and P2-TS08 stopped at its 20-second compile-fixture limit after 1,681
passing assertions. After the stale pre-edit process was stopped, isolated replays passed WF15
**10/10 with 187 assertions** and P2-TS08 **7/7 with 1,681 assertions**; no timeout or assertion was
weakened.

The five terminal harness commands then contributed **44 tests and 300 assertions**: doctor 1/22,
ledger writer 15/64, agent registry 7/39, export TS07 1/28, and P3B-TS06 20/147. Across the final
exact **44-command** catalog matrix, the serial receipts therefore total **231 tests and 6,865
assertions**, with zero failures or skips.

The focused correction layer is also green: reconcile apply **40/40** with 177 assertions;
application apply **16/16** with 162 assertions; reconciliation dispatch **25/25** with 156
assertions; placement swap **20/20** with 114 assertions; swap-acquire **15/15** with 72
assertions; and acquisition fetch **20/20** with 65 assertions. Direct legacy acquisition, strict
apply reports and renderers, absent-lock saved projection, permission exit mapping, update/removal
after-state truthfulness, both mixed lifecycle orderings, mid-sequence sibling mutation, and v1
commit/rollback/recovery cleanup all pass independently.

The default smoke lane passes **5/5** with **3,735 assertions**. Because G4B-02 owns coordinator,
transaction, and recovery integration, the recovery smoke lane also ran and passes **4/4** with
**453 assertions**. TypeScript, full Biome over 731 files, architecture boundaries, actionlint,
diff hygiene, and `check:p17 --pr-openable` pass. Catalog validation remains deterministic at 425
entities, 45 groups, and 244 validation obligations.

**Targeted-green result:** PASS. **Impacted-green result:** PASS. The full repository suite remains
reserved for the stable major sign-off after final adversarial review, per repository test policy.

### Green execution receipt EWP-CMD-APPLY-TS01

- Validation ID: `EWP-CMD-APPLY-TS01`
- Command: `bun test packages/cli/tests/contracts/apply.test.ts --test-name-pattern EWP-CMD-APPLY-TS01`
- Exit status: `0`
- Result: 1 test passed with 27 assertions; 0 failed and 0 skipped.
- Revision: `5091928f01f5ba9797942b1e81f93ba2be107081`

### Green execution receipt EWP-CMD-APPLY-TS02

- Validation ID: `EWP-CMD-APPLY-TS02`
- Command: `bun test packages/cli/tests/contracts/apply.test.ts --test-name-pattern EWP-CMD-APPLY-TS02`
- Exit status: `0`
- Result: 1 test passed with 9 assertions; 0 failed and 0 skipped.
- Revision: `5091928f01f5ba9797942b1e81f93ba2be107081`

### Green execution receipt EWP-CMD-APPLY-TS03

- Validation ID: `EWP-CMD-APPLY-TS03`
- Command: `bun test packages/cli/tests/contracts/apply.test.ts --test-name-pattern EWP-CMD-APPLY-TS03`
- Exit status: `0`
- Result: 1 test passed with 16 assertions; 0 failed and 0 skipped.
- Revision: `5091928f01f5ba9797942b1e81f93ba2be107081`

### Green execution receipt EWP-CMD-APPLY-TS04

- Validation ID: `EWP-CMD-APPLY-TS04`
- Command: `bun test packages/cli/tests/contracts/apply.test.ts --test-name-pattern EWP-CMD-APPLY-TS04`
- Exit status: `0`
- Result: 1 test passed with 6 assertions; 0 failed and 0 skipped.
- Revision: `5091928f01f5ba9797942b1e81f93ba2be107081`

### Green execution receipt EWP-CMD-APPLY-TS05

- Validation ID: `EWP-CMD-APPLY-TS05`
- Command: `bun test packages/cli/tests/contracts/apply.test.ts --test-name-pattern EWP-CMD-APPLY-TS05`
- Exit status: `0`
- Result: 1 test passed with 13 assertions; 0 failed and 0 skipped.
- Revision: `5091928f01f5ba9797942b1e81f93ba2be107081`

### Green execution receipt EWP-CMD-APPLY-TS06

- Validation ID: `EWP-CMD-APPLY-TS06`
- Command: `bun test packages/cli/tests/contracts/apply.test.ts --test-name-pattern EWP-CMD-APPLY-TS06`
- Exit status: `0`
- Result: 1 test passed with 11 assertions; 0 failed and 0 skipped.
- Revision: `5091928f01f5ba9797942b1e81f93ba2be107081`

### Green execution receipt EWP-CMD-APPLY-TS07

- Validation ID: `EWP-CMD-APPLY-TS07`
- Command: `bun test packages/cli/tests/contracts/apply.test.ts --test-name-pattern EWP-CMD-APPLY-TS07`
- Exit status: `0`
- Result: 1 test passed with 9 assertions; 0 failed and 0 skipped.
- Revision: `5091928f01f5ba9797942b1e81f93ba2be107081`

### Green execution receipt EWP-CMD-APPLY-TS08

- Validation ID: `EWP-CMD-APPLY-TS08`
- Command: `bun test packages/cli/tests/contracts/apply.test.ts --test-name-pattern EWP-CMD-APPLY-TS08`
- Exit status: `0`
- Result: 1 test passed with 7 assertions; 0 failed and 0 skipped.
- Revision: `5091928f01f5ba9797942b1e81f93ba2be107081`

### Green execution receipt EWP-CMD-APPLY-TS09

- Validation ID: `EWP-CMD-APPLY-TS09`
- Command: `bun test packages/cli/tests/contracts/apply.test.ts --test-name-pattern EWP-CMD-APPLY-TS09`
- Exit status: `0`
- Result: 1 test passed with 3 assertions; 0 failed and 0 skipped.
- Revision: `5091928f01f5ba9797942b1e81f93ba2be107081`

### Green execution receipt EWP-CMD-APPLY-TS10

- Validation ID: `EWP-CMD-APPLY-TS10`
- Command: `bun test packages/cli/tests/contracts/apply.test.ts --test-name-pattern EWP-CMD-APPLY-TS10`
- Exit status: `0`
- Result: 1 test passed with 6 assertions; 0 failed and 0 skipped.
- Revision: `5091928f01f5ba9797942b1e81f93ba2be107081`

### Green execution receipt EWP-CMD-APPLY-TS11

- Validation ID: `EWP-CMD-APPLY-TS11`
- Command: `bun test packages/cli/tests/contracts/apply.test.ts --test-name-pattern EWP-CMD-APPLY-TS11`
- Exit status: `0`
- Result: 1 test passed with 6 assertions; 0 failed and 0 skipped.
- Revision: `5091928f01f5ba9797942b1e81f93ba2be107081`

### Green execution receipt EWP-CMD-APPLY-TS12

- Validation ID: `EWP-CMD-APPLY-TS12`
- Command: `bun test packages/cli/tests/contracts/apply.test.ts --test-name-pattern EWP-CMD-APPLY-TS12`
- Exit status: `0`
- Result: 1 test passed with 25 assertions; 0 failed and 0 skipped.
- Revision: `5091928f01f5ba9797942b1e81f93ba2be107081`

### Green execution receipt EWP-CMD-APPLY-TS13

- Validation ID: `EWP-CMD-APPLY-TS13`
- Command: `bun test packages/cli/tests/contracts/apply.test.ts --test-name-pattern EWP-CMD-APPLY-TS13`
- Exit status: `0`
- Result: 1 test passed with 19 assertions; 0 failed and 0 skipped.
- Revision: `5091928f01f5ba9797942b1e81f93ba2be107081`

### Green execution receipt EWP-CMD-APPLY-TS14

- Validation ID: `EWP-CMD-APPLY-TS14`
- Command: `bun test packages/cli/tests/contracts/apply.test.ts --test-name-pattern EWP-CMD-APPLY-TS14`
- Exit status: `0`
- Result: 1 test passed with 12 assertions; 0 failed and 0 skipped.
- Revision: `5091928f01f5ba9797942b1e81f93ba2be107081`

### Green execution receipt EWP-CMD-PLAN-TS11

- Validation ID: `EWP-CMD-PLAN-TS11`
- Command: `bun test packages/cli/tests/contracts/plan.test.ts --test-name-pattern EWP-CMD-PLAN-TS11`
- Exit status: `0`
- Result: 1 test passed with 4 assertions; 0 failed and 0 skipped.
- Revision: `5091928f01f5ba9797942b1e81f93ba2be107081`

### Green execution receipt EWP-OPT-TS09

- Validation ID: `EWP-OPT-TS09`
- Command: `bun test packages/cli/tests/contracts/options.test.ts --test-name-pattern EWP-OPT-TS09`
- Exit status: `0`
- Result: 2 tests passed with 96 assertions; 0 failed and 0 skipped.
- Revision: `5091928f01f5ba9797942b1e81f93ba2be107081`

### Green execution receipt EWP-P4B-TS02

- Validation ID: `EWP-P4B-TS02`
- Command: `bun test tests/ergonomics/phase/EWP-P4B-TS02.test.ts --test-name-pattern EWP-P4B-TS02`
- Exit status: `0`
- Result: 2 tests passed with 12 assertions; 0 failed and 0 skipped.
- Revision: `5091928f01f5ba9797942b1e81f93ba2be107081`

### Green execution receipt EWP-P4B-TS03

- Validation ID: `EWP-P4B-TS03`
- Command: `bun test tests/ergonomics/phase/EWP-P4B-TS03.test.ts --test-name-pattern EWP-P4B-TS03`
- Exit status: `0`
- Result: 3 tests passed with 38 assertions; 0 failed and 0 skipped.
- Revision: `5091928f01f5ba9797942b1e81f93ba2be107081`

### Green execution receipt EWP-P4B-TS07

- Validation ID: `EWP-P4B-TS07`
- Command: `bun test tests/ergonomics/phase/EWP-P4B-TS07.test.ts --test-name-pattern EWP-P4B-TS07`
- Exit status: `0`
- Result: 3 tests passed with 29 assertions; 0 failed and 0 skipped.
- Revision: `5091928f01f5ba9797942b1e81f93ba2be107081`

### Green execution receipt EWP-WF06

- Validation ID: `EWP-WF06`
- Command: `bun test tests/ergonomics/workflows/EWP-WF06.test.ts --test-name-pattern EWP-WF06`
- Exit status: `0`
- Result: 1 test passed with 18 assertions; 0 failed and 0 skipped.
- Revision: `5091928f01f5ba9797942b1e81f93ba2be107081`

### Green execution receipt EWP-WF07

- Validation ID: `EWP-WF07`
- Command: `bun test tests/ergonomics/workflows/EWP-WF07.test.ts --test-name-pattern EWP-WF07`
- Exit status: `0`
- Result: 1 test passed with 23 assertions; 0 failed and 0 skipped.
- Revision: `5091928f01f5ba9797942b1e81f93ba2be107081`

### Green execution receipt EWP-WF08

- Validation ID: `EWP-WF08`
- Command: `bun test tests/ergonomics/workflows/EWP-WF08.test.ts --test-name-pattern EWP-WF08`
- Exit status: `0`
- Result: 1 test passed with 11 assertions; 0 failed and 0 skipped.
- Revision: `5091928f01f5ba9797942b1e81f93ba2be107081`

## Refactor

The stable implementation received a bounded consolidation pass:

- validation and physical execution remain separated between `reconcile/apply.ts` and
  `reconcile/apply-execution.ts`, with the shared dispatcher in `reconcile/execute.ts`;
- one capability-scope tuple now drives strict decoding and canonical domain ordering;
- the application service types its read-only destination probe through the application context,
  removing a runtime-adapter layer dependency;
- lifecycle coverage reuses the signed shared-parent resource-closure rule instead of adding a new
  controller or refresh mechanism;
- inverse projection reuses the existing plan codec; and
- temporary diagnostics were removed, source cleanup has one ownership-transfer boundary, and no
  speculative utility or second-consumer abstraction was introduced.

Full Biome, TypeScript, and architecture-boundary lint pass after the refactor. The remaining large
move-scope phase/probe helper extraction stays deferred until a second consumer exists, matching
the earlier independent maintainability recommendation.

**Refactor result:** PASS.

## Adversarial review

The first independent major review returned **NO-GO** at 98% confidence despite the focused green
matrix. It found four contract-correctness gaps that must be closed before the gate can pass:

1. fresh apply suppressed the sole missing-to-empty `write-lock` operation, breaking the exact
   plan/apply product and default-lock visibility contract;
2. physical and pre-physical permission failures could collapse to generic execution failure
   instead of exit class 6;
3. `move-scope` did not compose through the same retained repository lifecycle cursor as ordinary
   placement, so a mixed operation sequence could make later valid work appear stale; and
4. an ordinary install/update/remove whose durable commit succeeded but cleanup failed could report
   its before-image and `changed: 0` instead of the truthful partial mutation.

The review also identified one evidence-accuracy issue: reconciliation composes the acquisition
lifecycle contract directly and does not call the extracted placement binding helper. The impacted
test amendment above now describes that relationship exactly. It independently confirmed that the
WF15 parallel fixture fleets have unique temporary roots, paths, locks, ports, controllers, output
buffers, and cleanup, with no process-global mutation or network access.

The corrections and final dispositions are:

- `P17-RV-G4B-02-F19` (**high, resolved**): removed the empty-lock suppression transform; exact
  plan/check/dry-run/apply projections, approval, write, and present-lock no-op regressions pass.
- `P17-RV-G4B-02-F20` (**high, resolved**): preserved permission classification through ledger,
  store, source materialization, placement, move-scope, application, and numeric CLI exit mapping;
  the fetch boundary retains all non-permission source-error behavior and redacts private markers.
- `P17-RV-G4B-02-F21` (**high, resolved**): routed `move-scope` through the same repository
  lifecycle cursor and shared-parent closure as ordinary placement; both operation orderings and a
  sibling mutation injected between operations fail closed correctly.
- `P17-RV-G4B-02-F22` (**medium-high, resolved**): ordinary update/removal and move cleanup
  failures now retain truthful durable after-images and mutation counts while preserving failure,
  permission, and cancellation precedence.
- `P17-RV-G4B-02-F23` (**high, resolved**): one v1-first, legacy-fallback matcher now serves move
  observation, staging, backup reclaim, rollback, and committed recovery; legacy direct acquisition
  and v1 replace/uninstall compatibility regressions pass.

The final independent rerun passed **21 focused tests with 100 assertions** and returned **GO** at
**98.5% confidence**. It found no remaining critical, high, or medium product-code issue, no second
planner/coordinator/lifecycle controller, and no durable schema expansion. The traceability review
confirmed the then-current **79 unique owned paths** and **39 unique commands** after adding the
fetch and swap-acquire compatibility boundaries. Gitleaks initially caught four intentionally PAT-shaped
synthetic markers; they were replaced with non-secret canaries without weakening redaction
coverage, and the unchanged repository gitleaks configuration now reports no leaks.

**Adversarial result:** GO. No policy, human-review, network, or external-system blocker exists.

### Final placement-root, report, and bootstrap amendment review

A later independent read-only pass found three additional correctness gaps before terminal
traceability closure. First, an absent child below a symlink-to-directory root bound only the
literal symlink metadata, so replacing the followed directory could leave an old stage apparently
current. The live repository now binds the literal link, its immediate target, and the followed
directory identity; a real-filesystem regression proves target replacement changes the revision
and rejects the stale stage. Second, strict root/cleanup failures needed first-class report
diagnostics without relaxing the existing non-null plan-v1 refusal contract. The temporary codec
relaxation was reverted; state and permission diagnostics now satisfy the unchanged schema and are
visible in human and JSON output. Third, ledger-parent creation had moved under a new stable lease
but could leave an empty directory when its authoritative snapshot failed. The documented
bootstrap order and atomic empty-directory rollback now close that window without recursively
deleting a directory that acquired content.

The reviewer also required the direct `swap-acquire.test.ts` characterization to be present in the
owned boundary and replay inventory. After those then-current 79-path/39-command amendments, the independent
review returned **GO at 0.99 confidence**, with no blocking or nonblocking finding. Its reconcile
plus swap replay passed **55 tests and 249 assertions**, and its strict report/contract replay
passed **33 tests and 235 assertions**. A narrow post-GO review of the WF15 optimization separately
confirmed that refusal records zero mutation before the shared fleet's fail-fast run, every product
retains independent proxy/controller/observer/stream state, and all five product assertions remain
unchanged; isolated WF15 passed **10 tests and 187 assertions** in that review.

### Terminal correction independent review

The final terminal-correction delta received an independent read-only review after the complete
83-path/44-command inventory was frozen. It returned **GO at 99% confidence**, with no critical,
high, medium, or low product, architecture, or test-quality finding. The review confirmed that the
five corrected files are test-only; the doctor assertion no longer exposes the shared registry to
an asymmetric matcher; export cancellation waits for the actual two-lock execution boundary; the
ledger fixture preserves the exact 257-to-258-to-256 transition and its explicit 15-second timeout;
and both virtual filesystems preserve explicit node kinds while treating implicit ancestors as
directories.

The independent focused replay passed **44 tests with 300 assertions**: doctor **1/1** with 22
assertions, ledger writer **15/15** with 64, agent registry **7/7** with 39, export TS07 **1/1** with
28, and P3B-TS06 **20/20** with 147. The ledger row that had failed only under accumulated terminal
load completed in 10.95 seconds. The default smoke lane then passed **5/5** with 3,735 assertions;
TypeScript, affected-file Biome, diff hygiene, and `check:p17 --pr-openable` also passed. The
reviewer independently confirmed **83/83 unique owned paths** and **44/44 unique commands**.

## Traceability and terminal verification

The stable working-tree candidate passed the complete repository milestone gate in a persistent
user service with a 30-second default test timeout and serial test execution:

```sh
bun run check -- --timeout 30000 --max-concurrency 1
```

The command exited 0 on 2026-07-19. Its static stages passed full Biome over 731 files, ESLint
architecture boundaries, TypeScript, actionlint, and `check:p17 --pr-openable`. The test stage then
ran **2,881 tests across 293 files** in 843.79 seconds: **2,853 passed**, 28 intentional
environment-gated live-E2E tests skipped, **0 failed**, and **100,427 assertions** executed. The
service completed with `Result=success`, no swap use, and no test-owned timeout or assertion was
changed for the terminal replay.

Earlier non-signoff attempts were retained as diagnostic evidence rather than hidden. They exposed
two stale virtual-filesystem fixtures, a fixed-delay export cancellation race, and an unnecessarily
duplicated ledger-history fixture. Focused replays distinguished these harness and load issues from
product behavior before the final gate. The resulting corrections are covered by the terminal
amendment and independent-review sections above.

The catalog, plan, and evidence agree on **83 unique owned paths** and **44 unique commands**. The
implementation is pinned by the 40-character revision
`5091928f01f5ba9797942b1e81f93ba2be107081`: it contains exactly 75 changed owned product/test
paths and zero paths outside the frozen boundary. The remaining eight owned paths are unchanged
replay or compatibility coverage. Four separately authorized governance files carry this closure.

All 22 primary validation receipts above are now bound to that immutable revision. Together with
the command, three decisions, three findings, two phase tasks, and one recommendation, they close
all **31 primary G4B-02 entities**. The P17 validator remains deterministic at 425 entities, 45
groups, and 244 validation obligations.

## Sign-off

G4B-02 is signed off on immutable implementation revision
`5091928f01f5ba9797942b1e81f93ba2be107081`. Independent reviewer
`/root/g4b02_placement_root_amend_review` returned final **GO at 99.5% confidence** with no blocking
or nonblocking finding after replaying the literal doctor-to-agent-registry order and confirming
the exact 83-path/44-command governance boundary. All ten lifecycle gates pass, all 31 primary
entities are signed off, the complete repository gate is green, and all 44 exact catalog commands
have green receipts.

This advances the verified catalog totals to **33/44 required groups** and **303/418 required
entities** signed off. Phase 4 remains active; G4B-03 is the next major group. No product,
architecture, test, policy, human-review, network, or dependency blocker remains.
