# P17-G1-07 evidence

## Mapping and prerequisites

- **Primary IDs:** `EWP-CF-038`, `EWP-P1-T13`, and `EWP-P1-TS11`.
- **Formal dependency:** P17-G1-03 is signed off at
  `cd7ec024dc9beef0872dfd06eaa72024fffe2966`; its accepted implementation is
  `3fb4c1a86d424be9010f04394a5546a4eedd93ac`.
- **Preservation constraints:** G1-04 supplies focused clock/ID ports, G1-05 supplies the tool
  registry, and G1-06 is signed off at `bf801b92bf653605a60370a5832d7a436600c84d`
  with accepted implementation `5d5d6780cd4caa5ca5cb7a58fb6160e84ad243bb`. G1-07 must preserve
  capability authority, exact JSON bytes, and focused effects.
- **Required now:** exactly `EWP-P1-TS11`. `EWP-P3B-TS07` remains downstream-only under
  P17-G3B-06 and owns live planning, transaction, journal, rollback, recovery, retry, and resume
  propagation.
- Independent mapper `/root/g1_06_final_pinned_review_3`, with delegated code-surface audit
  `/root/g1_06_final_pinned_review_3/g1_07_code_surface`, confirmed the exact IDs, dependency,
  Phase-1 boundary, current authority gaps, owned-file candidates, false-red guards, baselines, and
  downstream exclusions at clean revision `bf801b92bf653605a60370a5832d7a436600c84d`.
- Reusable current seams are `ClockPort`, `IdPort`, `ApplicationContext`, the shared CLI runtime,
  `CliRuntimeIo`, structured `CommandOutcome` diagnostics, the validated tool registry, and
  canonical wire codecs. Missing authority is `OperationContext`, `ObserverPort`, the event
  registry, recursive event redaction, and a single diagnostic sink.
- Quiet/verbose/debug options parse and conflict correctly but are discarded. Free-form `Logger`
  remains in scan/list/doctor surfaces; only scan modules currently emit free-form debug/warn
  strings. Application context does not carry operation, observer, or verbosity policy.
- Core focused baseline passed 44 tests with 154 assertions across 8 files. CLI runtime/output
  passed 106 tests with 684 assertions across 12 files. Architecture/workflow passed 30 tests with
  478 assertions across 3 files. After verification integration entered exact ownership,
  `packages/core/tests/verify/run.test.ts` passed 11 tests with 48 assertions: 191 tests and 1,364
  assertions across 24 files total.
- **Mapping result:** exact scope, prerequisites, current observation/verbosity surfaces,
  compatibility seams, required validation, and prohibited downstream work mapped by root Codex
  goal on 2026-07-12.

## Ready plan

- **Plan:** `projects/p17/plans/P17-G1-07.md`.
- The plan freezes ADR 0009, immutable operation context and derivation, a 15-kind typed event
  registry, best-effort sync/async observation, pure recursive redaction, one CLI stderr diagnostic
  sink, quiet/normal/verbose/trace/debug semantics, current command and scan correlation, public
  compatibility, and exact Phase-3B deferral.
- Normal output and all G1-06 JSON bytes remain unchanged. Quiet suppresses only human stdout while
  retaining diagnostics; verbosity adds only stderr observations; JSON stdout remains one canonical
  document at every level.
- Exact required command is
  `bun test tests/ergonomics/phase/EWP-P1-TS11.test.ts --test-name-pattern EWP-P1-TS11`.
- Independent reviewer `/root/g1_06_red_review` rejected the first draft at 0.99 confidence for
  underdefined event payloads, span/observer failure semantics, redaction bounds, verbosity output,
  lifecycle order, static allowlists, and ownership. After those corrections it identified eight
  remaining contradictions: registry membership was not present in the helper API, Logger-only
  compatibility lacked context, verify instrumentation was above its feasible layer, command error
  fallback and verify result mapping were incomplete, current command/workflow identity and
  symbol-key output were unspecified, and JSON diagnostic wording was overbroad.
- Independent focused audits `/root/g1_07_verify_contract` and `/root/g1_07_runtime_identity`
  inspected current source without edits. They froze a registry-bound `ObservationEmitter`, one
  truthful modes span per checker call inside `runVerify`, total structured result mapping,
  canonical action identity, and error-diagnostic/exit-class completion fallback at 0.995 and 0.997
  confidence respectively.
- Final re-audit by `/root/g1_06_red_review` returned **PASS at 0.99 confidence**. It found the
  corrected plan implementable, preservation-safe, and confined to G1-07, reproduced the verify
  baseline at 11/11 tests and 48 assertions, and passed the 425-entity/45-group catalog plus P17
  PR-openable check.
- **Result:** ready for test-first implementation; no P3B transaction/recovery propagation or
  event persistence is implied.

## Test-first evidence

- `EWP-P1-TS11` contains two current-behavior characterizations followed by eight executable
  authority families. Current normal human/JSON bytes, the exact option/conflict inventory, legacy
  Logger messages, and structured result authority remain green. The eight families fail only on
  absent G1-07 operation/event authority, runtime and scan/verify integration, diagnostic policy,
  quiet behavior, Logger migration, and ADR 0009.
- The standalone public fixture freezes the exact root exports, immutable context/event/payload
  shapes, generic emitter and opaque-span signatures, atomic observation fields on application,
  scan, doctor, verify, and runtime requests, deterministic program operation ports, completion
  pair ownership, void helper authority, and compile-negative secret/metadata/IO fields.
- Runtime families cover all 15 kinds, strict low-level validation, fresh/child/target/retry context
  semantics, registry membership and cross-emitter spans, independent clocks, command lifecycle and
  error fallbacks, observer throw/reject/hang isolation, hostile recursive redaction, real
  detection error and inventory/doctor propagation, total verify result precedence and
  short-circuiting, exact diagnostic keys/order, spawned quiet/verbosity/JSON behavior, and parsed
  AST ownership/P3B deferral.
- Initial reviewer `/root/g1_07_red_review` rejected `13cdd37cf02dc638b80446e79c8f85afacbb7f16`
  for a `void`/`undefined` fixture contradiction, detached method calls, shallow failure and verify
  mapping coverage, vacuous sink redaction, missing context derivations/inventory propagation, and
  incomplete static gates. Corrections `8de784cfedc193e909cb96cfa8c3d9710fe64bae`,
  `6bde37206d8f7d34db7f8551593a0ae75cc04c19`, and
  `fa24f21a7aed45a84a7daa9f098103c7cab71bc7` closed those findings and moved spawned Bun cache out
  of the worktree.
- Final overall reviewer `/root/g1_07_red_review` returned **PASS at 0.997 confidence** on exact
  clean `fa24f21a7aed45a84a7daa9f098103c7cab71bc7`. Focused runtime reviewer
  `/root/g1_07_red_runtime_audit` passed at 0.995 and public/static reviewer
  `/root/g1_07_red_static_audit` passed at 0.996. They replayed the selector and focused baselines
  and found no remaining false red, overconstraint, unsafe semantic authority, implementation
  contradiction, scope theft, or material coverage gap.
- Core, verify, and CLI baselines remain green at 44/154, 11/48, and 106/684 respectively. The
  architecture/workflow set has 29 passes and one intentional TS07 failure solely for the new
  observation bundle before it reaches the accepted eager-version conflict assertions.
- **Result:** `EWP-P1-TS11` is genuinely red against only the planned G1-07 production authority;
  both characterizations remain green and all eight failures are actionable.

### Red execution receipt EWP-P1-TS11

- Validation ID: `EWP-P1-TS11`
- Command: `bun test tests/ergonomics/phase/EWP-P1-TS11.test.ts --test-name-pattern EWP-P1-TS11`
- Exit status: `1`
- Result: 2 passed, 8 failed, 24 reached assertions; both current-behavior characterizations passed,
  while all eight planned authority families failed.
- Revision: `fa24f21a7aed45a84a7daa9f098103c7cab71bc7`

## Implementation and validation

The implementation was delivered as four reviewed commits:

- `acc795c5f5a1c659a11333a0933bbc6a8bc5e759` — public operation context, typed observer/event
  registry, recursive redaction, legacy logger adapter, current scan/doctor/verify propagation,
  CLI diagnostic sink, runtime lifecycle, ADR 0009, and focused tests.
- `44642e9038fc2cd2229cc174a7cda19a24ed18fc` — diagnostics operation ownership, event count
  closure, hostile input containment, logger parity/redaction, Unicode line safety, renderer
  snapshotting, buffer settlement, and required extension-value shielding.
- `8ad7aa475792d232fe0a3876c298076fb4c22682` — optional extension-value parsing plus owned
  renderer/classifier/registry/failure/outcome presentation boundaries.
- `9e0594b185bde7724a0f4a5ef5a14d7a932a3e46` — deterministic option-spelling shape ownership,
  preventing an unrelated extension from changing eager parsing while retaining same-shape reuse.

### Execution receipt EWP-P1-TS11

- Validation ID: `EWP-P1-TS11`
- Command: `bun test tests/ergonomics/phase/EWP-P1-TS11.test.ts --test-name-pattern EWP-P1-TS11`
- Exit status: `0`
- Result: 10 passed, 0 failed, 487 assertions; all two characterizations and eight authority
  families passed.
- Revision: `9e0594b185bde7724a0f4a5ef5a14d7a932a3e46`

Focused and impacted validation at the accepted revision:

- `EWP-P1-TS07`: 14/14 passed with 299 assertions, including required/optional value shielding and
  long/short/root extension-shape isolation.
- CLI runtime diagnostics: 15/15 passed with 134 assertions; option contracts: 34/34 with 480;
  TS08, WF15, and CLI help: 27/27 with 297.
- Independent core replay: 214/214 passed with 1,553 assertions across 29 focused files. Independent
  CLI replay: 109/109 passed with 721 assertions, plus a 24/24 collision matrix and five
  byte-identical baseline/reuse probes.
- `bun run typecheck`, focused Biome, `git diff --check`, catalog/package structural gates, and all
  targeted commands passed.
- Final `bun run check`: exit 0; 1,391 passed, 28 expected live/integration skips, 0 failed, 9,032
  assertions across 169 files. No required-now validation was skipped.

Minimal implementation, targeted green, impacted green, and refactor gates pass. Current normal and
JSON bytes remain unchanged, observation cannot acquire semantic authority, and no Phase-3B
transaction/recovery behavior was implemented early.

## Adversarial review

Independent reviewers `/root/g1_07_final_core_review` and
`/root/g1_07_final_cli_rereview` repeatedly reviewed clean, exact implementation pins without
editing. The following stable findings were closed:

- `P17-RV-G1-07-F01` — health lacked a paired diagnostics operation span; fixed in `44642e9`.
- `P17-RV-G1-07-F02` — cross-field operation counts admitted invalid inventory/diagnostics states;
  fixed in `44642e9`.
- `P17-RV-G1-07-F03` — catalog and plan omitted five actual owned files; corrected in this
  traceability update.
- `P17-RV-G1-07-F04` — exotic and huge sparse arrays could bypass or overconsume validation;
  fixed in `44642e9`.
- `P17-RV-G1-07-F05` — child-context accessors/proxies could be read unsafely; fixed in `44642e9`.
- `P17-RV-G1-07-F06` — legacy token-like values and failed inventory summaries drifted from safe
  compatibility; fixed in `44642e9`.
- `P17-RV-G1-07-F07` — a sensitive accessor marker lost redaction precedence; fixed in `44642e9`.
- `P17-RV-G1-07-F08` — C1 and Unicode line separators could split diagnostics; fixed in `44642e9`.
- `P17-RV-G1-07-F09` — required attached-spec values could be mistaken for eager version flags;
  fixed in `44642e9`.
- `P17-RV-G1-07-F10` — renderer mutation and hostile diagnostic reads were not fully snapshotted;
  fixed in `44642e9`.
- `P17-RV-G1-07-F11` — diagnostic-buffer settlement failure could affect command semantics; fixed
  in `44642e9`.
- `P17-RV-G1-07-F12` — optional attached-spec values consumed following flags incorrectly; fixed
  in `8ad7aa4`.
- `P17-RV-G1-07-F13` — hostile renderer output accessors/proxies escaped containment; fixed in
  `8ad7aa4`.
- `P17-RV-G1-07-F14` — a failure renderer could mutate failure identity and exit; fixed in
  `8ad7aa4`.
- `P17-RV-G1-07-F15` — classifier results were not validated and owned; fixed in `8ad7aa4`.
- `P17-RV-G1-07-F16` — registry proxy reads and returned semantic outcomes were not fully owned;
  fixed in `8ad7aa4`.
- `P17-RV-G1-07-F17` — global last-writer option maps let an unrelated extension change current
  eager parsing; fixed in `9e0594b` with deterministic shape-conflict rejection.

Both final reviewers returned **PASS at 0.999 confidence** on exact clean
`9e0594b185bde7724a0f4a5ef5a14d7a932a3e46`. Core review replayed every prior observation/runtime
finding and passed TS11 10/10, the 214-test focused matrix, TS07, option contracts, typecheck,
Biome, and diff checks. CLI review passed a 24-case collision matrix, same-shape reuse and five
byte-parity probes, TS11, runtime/diagnostics, TS07, the 109-test broader CLI baseline, TS08/WF15,
typecheck, and diff checks. No stable blocker remains.

## Traceability and sign-off

- `EWP-CF-038` resolves to the public observation authority and the deferred Phase-3B propagation
  obligation; `EWP-P1-T13` resolves to ADR 0009 plus the integrated implementation; and
  `EWP-P1-TS11` resolves to the executable receipt above.
- Read-only trace auditor `/root/g1_07_final_trace_audit` inspected the complete implementation
  change union and found exactly five missing group/plan ownership paths. All five are now recorded;
  all other 37 implementation paths were already covered.
- `EWP-P3B-TS07` remains planned under P17-G3B-06 with `target: null` and `evidence: []`; it was not
  run or implemented early.
- `bun scripts/p17-catalog.ts --check`, `bun scripts/check-p17-package.ts --check`,
  `bun run check:p17`, `git diff --check`, the catalog trace suite, TS11, and the focused receipt
  replays passed on the sign-off candidate based on
  `9e0594b185bde7724a0f4a5ef5a14d7a932a3e46`. Final read-only trace audit completed on
  2026-07-13 America/Los_Angeles; its three metadata-only findings were corrected, and the
  correction replay returned **PASS at 0.999 confidence** before commit.
- Standing human authorization is recorded in
  `projects/p17/evidence/standing-authorization.md`; no additional human pause is required.
- Accepted implementation: `9e0594b185bde7724a0f4a5ef5a14d7a932a3e46`.
- Sign-off owner/date: root Codex goal, 2026-07-13 America/Los_Angeles.

## Phase 1 whole-review correction — 2026-07-13

- `P17-RV-PHASE-1-F05` showed that the eager scanner borrowed value shapes from unrelated commands,
  allowing `agents --ref --version` to miss the global version short-circuit. Revision
  `77c22c766db5635189819f295e53f68a1d033348` resolves value authority from root plus active
  canonical command only, including nested paths and aliases, while retaining Commander required
  and optional value semantics and deterministic shape-collision validation.
- `P17-RV-PHASE-1-F06` showed that the returned runtime outcome retained nested references that a
  renderer could mutate. The same revision recursively owns and freezes report values,
  diagnostics/details, mutation, deprecations, cycles, sparse arrays, errors, and current
  `ReadonlyMap` reports; hostile snapshot failure remains normalized.
- Corrected TS07 passed 16/16 with 320 assertions; runtime adapter/diagnostics passed 18/18; TS11
  passed 10/10 with 487 assertions; the combined correction matrix passed 89/89 with 1,521
  assertions. Final `bun run check` passed 1,396 tests, 28 expected skips, 0 failures, and 9,090
  assertions.
- These corrections close the adjacent whole-phase findings without adding an event kind, command,
  option, JSON schema, or downstream transaction/recovery behavior.
