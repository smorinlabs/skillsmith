# P17-G2-04 evidence

## Mapping and prerequisites

- **Primary IDs:** exactly `EWP-P2-T05` and `EWP-P2-TS05`.
- **Initial required-now mapping:** exactly `EWP-P2-TS05`; its immutable planned target is
  `planned:P17-G2-04:tests/ergonomics/phase/EWP-P2-TS05.test.ts#EWP-P2-TS05`. This was the
  mapping-time baseline and is superseded by the six-selector mapping amendment below.
- **Downstream coverage:** none. The future init command, option, workflow, and shared-planner
  validations remain owned by G4A-03 and later groups; G2-04 does not activate or claim them.
- **Secondary trace:** signed `EWP-CF-001` requires the corrected dependency order, while planned
  `EWP-CF-041` names G2-04 only for its pure Phase-2 init prerequisite and remains primarily owned
  by G4A-03.
- **Dependencies:** G2-01 is signed at
  `944c41118cf0cc4710fb671a658d3fac443d7754` with accepted implementation
  `0751951bbb413dd80f5dc6e67eec8725d18c9735`; G2-03 is signed at
  `15c3c67658571b79f8b24059ea1db6b7e0518471` with accepted implementation
  `6151cdea0ece5727e5a11f36d619e278b64792bf`.
  Phase 2 entry remains passed. G2-02 is transitively available through those signed dependencies.
- **Boundary:** this group owns pure request validation, deterministic canonical empty-manifest
  skeleton bytes, and a byte-owning create/replace/migrate/noop/refusal classification. It does not
  register or execute `init`, select Git/XDG destinations, write files, invoke the artifact
  coordinator, create a lock, inspect live/ledger state, implement the shared planner, or add
  renderers/help/completion/codecs/repositories.
- Independent product mapper `/root/g2_03_final_product_review` confirmed the accepted T05/TS05,
  init, and CF-041 requirements and found the absent selector/module/API plus an unresolved legacy
  comment-preservation seam.
- Independent architecture mapper `/root/g2_03_core_final_independent` confirmed the smallest pure
  operation-input boundary and reproduced that `manifest-edit.ts` currently reserializes legacy
  input while `config/human-edit.ts` owns the existing range-preserving migration. Ready planning
  must establish one lossless authority rather than duplicate either path.
- Independent trace auditor `/root/g2_03_required_validation` reproduced two unique primaries, two
  unique secondary relations, one required/impacted validation, zero downstream obligations, and
  zero duplicates. It confirmed that both entities remain pristine `planned` at the mapped gate;
  T05 advances only at ready and TS05 only when its executable selector exists at test-first.
- **Result:** prerequisites, entity ownership, validation tier, accepted-contract trace, current
  gaps, and later-work exclusions are mapped. Exact bytes, operation algebra, file ownership, and
  lossless migration composition remain readiness decisions; no implementation or test-first gate
  is implied.

## Mapping amendment

- Initial mapping named TS05 alone from the baseline task/test relation. Ready falsification found
  that the required single lossless migration extraction amends signed G2-01/G2-03 production
  paths, so treating their selectors as optional checks would make impacted-green untruthful.
- The mechanical mapping is amended before ready: required-now and impacted validation are exactly
  `EWP-CMD-CONFIG-TS01`, `EWP-CMD-CONFIG-TS04`, `EWP-P1-TS08`, `EWP-P2-TS01`,
  `EWP-P2-TS04`, and `EWP-P2-TS05`; downstream coverage remains empty. T05's immutable validation
  trace now names signed G2-01, G1-04, and G2-03 as secondary groups.
- `scripts/p17-catalog.ts` records this trace correction in the self-describing catalog generator.
  The generated catalog/checklist and this evidence are governance metadata, not G2-04 product
  ownership. Catalog/package validation passes with all six selectors required and no future-group
  validation imported.
- **Result:** mapping remains passed with the corrected six-selector impact set. The group remains
  `mapped`; no ready or implementation claim is implied.

## Ready plan

- **Plan:** `projects/p17/plans/P17-G2-04.md`, accepted at SHA-256
  `5e2bc7d325f7a25d9b56dc817504a9689a1428ea5e796d4564df2f0f50115a4e`.
- The candidate closes the public boundary around strict unknown-input validation, exact canonical
  empty-manifest bytes, immutable hash-coherent before/write images, per-field legacy intent, a
  total create/replace/migrate/noop/refusal matrix, and one internal lossless legacy migration
  authority. It adds no path selection, CLI registration, filesystem effect, planner, coordinator
  call, lock, ledger, or live-state behavior.

### Readiness finding closure

- Initial product and architecture reviews returned **NOT READY** for
  `P17-RV-G2-04-F01..F06`: mutable byte views could desynchronize hashes; before images retained raw
  input; explicit legacy intent could be discarded; the validation impact set omitted amended
  signed paths; semantically invalid canonical input had no row; and the migration extraction had
  reverse type coupling plus byte/source-offset ambiguity and shared-buffer risk.
- The next architecture pass returned **NOT READY** for `P17-RV-G2-04-F07..F10`: one global intent
  mode could not express per-field explicit choices; generated skeletons lacked a complete-candidate
  sensitive-material guard; byte-view validation could observe shadow accessors/iterators/species;
  and focused verification omitted the owned human-TOML test and `empty` null-semantic case.
- Trace re-audit returned **NOT READY** for `P17-RV-G2-04-F11`: the original mapping anchor still
  described its historical TS05-only set and the passed mapped gate did not link the superseding
  six-selector amendment.
- Implementation reconnaissance returned **NOT READY** for `P17-RV-G2-04-F12`: treating a no-blank
  file-leading legacy comment as field-attached contradicted the signed config migration bytes.
- F01/F02 close by exposing immutable strings and hashes only, with no returned mutable byte view or
  raw before source. F03/F07 close with the required `legacyIntent.requireMatch` per-field projection.
  F04/F11 close in the mechanically generated six-selector trace and both mapping evidence links.
  F05 closes in the ordered canonical-but-semantically-invalid refusal/force row. F06/F09 close with
  a leaf-local migration result, exact JavaScript code-unit `keyRange`, proxy-first ordinary-buffer
  validation, captured typed-array intrinsics, and hostile shadow/shared-buffer fixtures. F08 closes
  by guarding every completed skeleton and migration candidate. F10 closes in the focused command
  and null-semantic fixture family. F12 closes by preserving every contiguous file-leading comment
  run before `version`, including the signed no-blank form, while later separated comments remain
  attached to their field/header.
- Independent product reviewer `/root/g2_03_final_product_review` returned final **PASS**, no
  blocking or major finding, at high confidence and reproduced the accepted plan hash.
- Independent architecture reviewer `/root/g2_03_core_final_independent` returned final **PASS**, no
  blocking or major finding, at 0.98 confidence and reproduced the accepted plan hash.
- Independent trace reviewer `/root/g2_03_required_validation` returned final **PASS**, no blocking
  or major finding, reproduced the accepted plan hash, and confirmed the exact ownership partition,
  evidence links, and selector set.

### Ready receipts and ownership

- The focused dependency baseline is 89 tests, 1,807 assertions, and zero failures across signed
  manifest, edit, config, architecture, and public API paths. Independent trace replay additionally
  exercised five signed selectors with 47 tests, 50,593 assertions, and zero failures.
- Full `bun run check` is green: 1,691 passed, zero failed, with 28 pre-existing opt-in live/manual
  skips. Catalog validation reports 425 entities, 45 groups, and 244 validation obligations;
  package structure is PR-openable; 51 catalog tests/120 assertions and `git diff --check` pass.
- The exact sorted 18-path group union is:
  `docs/architecture.md`, `packages/core/README.md`,
  `packages/core/src/artifacts/human-toml.ts`, `packages/core/src/artifacts/index.ts`,
  `packages/core/src/artifacts/init.ts`, `packages/core/src/artifacts/legacy-migration.ts`,
  `packages/core/src/artifacts/manifest-edit.ts`, `packages/core/src/config/human-edit.ts`,
  `packages/core/src/public-types.ts`, `packages/core/tests/artifacts/human-toml.test.ts`,
  `packages/core/tests/artifacts/init.test.ts`,
  `packages/core/tests/artifacts/manifest-edit.test.ts`, `packages/core/tests/config/save.test.ts`,
  `packages/core/tests/public-api.test.ts`,
  `tests/ergonomics/fixtures/p2-ts05/init-cases.json`,
  `tests/ergonomics/fixtures/p2-ts05/public-types.ts`,
  `tests/ergonomics/fixtures/p2-ts05/tsconfig.json`, and
  `tests/ergonomics/phase/EWP-P2-TS05.test.ts`.
- T05 owns exactly the seven `packages/core/src/**` production paths. TS05 will own exactly its test
  and three fixtures when they exist at test-first. The two documentation paths, new focused init
  test, and four existing focused compatibility tests are the seven group-only paths.
- The exact required commands, in validation order, are:
  `bun test packages/cli/tests/contracts/config.test.ts --test-name-pattern EWP-CMD-CONFIG-TS01`;
  `bun test packages/cli/tests/contracts/config.test.ts --test-name-pattern EWP-CMD-CONFIG-TS04`;
  `bun test tests/ergonomics/phase/EWP-P1-TS08.test.ts --test-name-pattern EWP-P1-TS08`;
  `bun test tests/ergonomics/phase/EWP-P2-TS01.test.ts --test-name-pattern EWP-P2-TS01`;
  `bun test tests/ergonomics/phase/EWP-P2-TS04.test.ts --test-name-pattern EWP-P2-TS04`; and
  `bun test tests/ergonomics/phase/EWP-P2-TS05.test.ts --test-name-pattern EWP-P2-TS05`.

**Ready result:** PASS. G2-04 and T05 advance to `ready` with the exact ownership above. TS05 remains
pristine `planned` until its executable selector and fixtures exist at test-first. No product
implementation or test-first gate is implied by this result, and no human-review stop is required.
