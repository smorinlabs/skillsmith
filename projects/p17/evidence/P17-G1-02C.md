# P17-G1-02C evidence

## Mapping and prerequisites

- **Primary IDs:** `EWP-P1-T06`, `EWP-P1-T07`, `EWP-P1-TS05`, and `P0-05`.
- **Dependencies:** G1-01 and G1-02A are signed off; overlapping G1-02B is also signed off.
- **Required now:** exactly `EWP-P1-TS05`; this group has no catalogued downstream validation.
- Read-only mapper `/root/g1_01_fix_validation` verified live documentation/help drift, package/API
  contradictions, the absent TS05 target, exact-legacy read support, the missing warning seam,
  G0-04 overlap, prohibited later-phase ownership, and safe parallel file families.
- **Mapping result:** exact scope, prerequisites, validation impact, and prohibited migration/help
  generation work mapped by root Codex goal on 2026-07-12.

## Ready plan

- **Plan:** `projects/p17/plans/P17-G1-02C.md`.
- Root owns integration. One agent owns the exact TS05 red contract; docs/help and legacy-warning
  implementation remain separate until that red boundary is committed.
- Existing G0-04 documentation drift, G1-01 config precedence, G1-02A output policy, and G1-02B check
  behavior remain impacted compatibility checks.
- **Result:** ready for test-first implementation; no later lifecycle gate is implied.

## Test-first evidence

- `EWP-P1-TS05` exited 1 with one passing compatibility guard and six independent failures.
- README inventory omitted four live aliases; capability/context documentation did not explicitly
  bound Kilo Code/opencode to detection, check's default/report-only behavior, or inherited cwd/config
  flags; and rendered topics exposed internal P17, superpowers-plan, and research paths.
- Current shell examples omitted representative config/check/verify/uninstall/dev/promote/completion
  flows. Root package documentation contradicted the unpublished core package and used asynchronous
  `defaultScanEnv()` without `await`.
- An exact discovered legacy project `skillsmith.toml` parsed successfully and remained byte-identical,
  but the human read emitted no actionable Phase-2 migration warning.
- The seventh subtest proved the signed G0-04 documentation-drift inventory remained green before
  implementation. Typecheck, targeted Biome, and diff checks also passed.
- **Result:** the sole required-now selector is genuinely red at the group boundary; every failure
  names planned production/documentation behavior rather than a fixture or harness defect.

### Execution receipt EWP-P1-TS05

- Validation ID: `EWP-P1-TS05`
- Command: `bun test tests/ergonomics/phase/EWP-P1-TS05.test.ts --test-name-pattern EWP-P1-TS05`
- Exit status: `0`
- Result: 7 passed, 0 failed, 110 assertions; current docs, help, package/API, and all-reader legacy warning contracts passed.
- Revision: `e30043c72e56d2e51643fb6e8ece6635905fb957`

## Implementation and validation

- Root and package documentation now close against the live Commander command/alias inventory,
  current tool detection versus verify/write capability, check's default/report-only behavior,
  inherited cwd/config flags, representative parseable examples, package publication metadata, and
  asynchronous public environment construction.
- Executable help topics retain their signed G0-04 source-authority markers as non-rendered comments
  while user output contains only live current behavior and current command references. Architecture
  prose now distinguishes injected domain capabilities from the real default filesystem/process
  adapters; the signed G0-03 evidence pointer moved mechanically with its unchanged marker.
- Effective config adds optional structured compatibility metadata only for successfully parsed,
  nonempty discovered project legacy configuration. Values, sources, layer precedence, explicit
  config behavior, malformed handling, and source bytes are unchanged; empty/comment-only,
  user/system, and separate explicit files are not marked.
- A pure CLI renderer deduplicates notices, emits one actionable path-specific human warning that
  names Phase 2, and emits nothing for JSON. Config get/list, list, commands, and check consume the
  same resolved result once; writers and doctor repair remain untouched, and no migration, schema,
  lock, or write behavior was introduced.
- At corrected implementation revision `e30043c72e56d2e51643fb6e8ece6635905fb957`, exact TS05 passed
  7/7 with 110 assertions. Focused documentation, config, check/doctor, phase/workflow, help, type,
  formatting, and dependency-boundary gates passed. Corrected full `bun run check` passed 1,229
  tests with 28 conditional live/network skips, 0 failures, and 5,564 assertions.

## Adversarial review

- Fresh independent reviewer `/root/g1_01_fix_validation/g1_02c_independent_review` rejected initial
  implementation revision `a63078a64118df4569679e7be5487f7a9a2bb634` with one medium finding.
- `P17-RV-G1-02C-F01`: doctor read an exact legacy project configuration through its config-parse
  check but did not emit the Phase-2 compatibility warning. The correction resolves compatibility
  metadata only for human doctor reads without an explicit artifact override, renders the same
  deduplicated warning after successful diagnostics, and leaves JSON, doctor findings/exits,
  malformed-config behavior, explicit artifact selection, and bytes unchanged.
- TS05 now spawns bounded human and JSON doctor regressions alongside the original config read and
  byte-identity assertions. Exact TS05 passes 7/7 (110 assertions), and focused doctor/output/core
  coverage passes 13/13 (64 assertions).
- The same fresh reviewer replayed F01 plus explicit-artifact suppression, malformed discovered
  configuration, byte identity, JSON parsing/silence, and the original config get/list, list,
  commands, check, and doctor reader matrix. Every human reader warned exactly once; every JSON
  variant remained silent. TS05 passed 7/7 (110 assertions), focused replay passed 18/18, and type,
  lint, and diff checks passed. F01 is closed with no new finding.
- **Gate result:** adversarial-review accepted on 2026-07-12 America/Los_Angeles.

## Traceability and sign-off

- **Implementation revisions:** initial `a63078a64118df4569679e7be5487f7a9a2bb634`; corrected and
  accepted `e30043c72e56d2e51643fb6e8ece6635905fb957`.
- `EWP-P1-T06`, `EWP-P1-T07`, `EWP-P1-TS05`, and `P0-05` resolve to current user/package docs,
  executable help, structured effective-config metadata, all current human readers, one exact test
  owner, exact-revision receipt, full-repository validation, and fresh independent acceptance.
- Existing G0-03/G0-04 authority remains intact; the architecture pointer moved only to follow its
  unchanged source marker. G1-01 precedence, G1-02A output/error policy, and G1-02B check/doctor
  findings and JSON contracts remain green.
- No canonical manifest schema/editor, config migration/write, lock creation, doctor fix, future
  command, G1-03 runtime generation, G6 help generation, or WF16 expansion was claimed.
- Standing user authorization covers this group boundary without a human-review pause; all automated
  and independent-agent gates were preserved and passed.
- **Traceability result:** passed by root Codex goal on 2026-07-12 America/Los_Angeles.
- **Group sign-off:** all four primary entities, ten lifecycle gates, exact validation receipt,
  F01 correction, corrected full-repository gate, and fresh independent acceptance are complete.
  P17-G1-02C signed off by root Codex goal on 2026-07-12.

## Phase 1 whole-review ownership correction — 2026-07-13

`P17-RV-PHASE-1-F03` found that the accepted change union updated the checked reconciliation pair
`projects/p17/reviews/P17-G0-03-reconciliation.json` and
`scripts/p17-architecture-reconciliation.ts` without recording G1-02C co-ownership. Both paths now
appear exactly in the plan/catalog. They retain the same architecture source pointer and introduce
no new migration or future-command behavior; the finding is resolved for phase replay.
