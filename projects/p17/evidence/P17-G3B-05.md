# P17-G3B-05 evidence

## Mapping and prerequisites

- **Primary entities:** exactly two: phase task `EWP-P3B-T06` and phase selector
  `EWP-P3B-TS06`. Mapping advances only the owning group and its first lifecycle gate; both
  entities remain pristine `planned`.
- **Formal dependency:** the sole immutable catalog dependency, P17-G3B-01, is signed off at
  `1789c71`. Its accepted implementation/test pin is
  `99594536d24b6fe8f31df793fad74ba09517f049`, with a terminal repository receipt of 1,955
  passes, 28 intentional skips, zero failures, and 72,205 assertions.
- **Registry baseline:** signed G1-05 implementation
  `05f55938c5bced26f0c6d05dc2dae443b6201ea7` remains authoritative for ADR 0007, validated
  descriptors, cohesive inventory/verification/placement bundles, the four built-in adapters,
  fixture registry construction, current compatibility projections, and known-unsupported exit 4.
  Signed `EWP-CF-036` remains owned by G1-05 and is only a secondary trace here.
- **Immutable-planning baseline:** signed G3B-04 implementation
  `b7817ac4eb5aa5c85e12977e5721a4b990490631` and sign-off `3704cc7` remain authoritative for
  immutable observations, pure planners, expected revisions, repositories, coordinator execution,
  recovery, and operation identity. G3B-05 narrows capability observation but does not replace or
  reopen those foundations.
- **Validation partition:** `EWP-P3B-TS06` is the sole required-now and impacted validation.
  Downstream coverage is empty. `EWP-WF01`, `EWP-WF05`, and `EWP-WF07` retain their future primary
  owners and receive no lifecycle advancement here.
- **Already complete:** adapter-owned roots, verification implementations and modes, verification
  gate facts, placement discovery, Codex legacy notices, install hints, and current renderer facts
  already exist. Verification already demonstrates an injected registry. G3B-05 must complete
  generic consumption and injection rather than repeat the G1-05 registry migration.
- **Current residuals:** acquisition, placement planning/execution, lifecycle selection, planning
  validation/order, capability observation, and lifecycle renderers still close over production
  `toolRegistry`, `FLIP_TOOLS`, or `SUPPORTED_TOOLS`. Generic placement code also interprets root
  ordinal order, reconstructs project-scope classification/duplicate policy, and repeats uninstall
  discovery. Install and promote separately reduce the same verification gate outcomes. The
  capability state reader fingerprints the entire registry, so an unrelated adapter change can
  stale approved work.
- **Accepted architectural direction:** keep `ToolRegistry` as the sole capability authority and
  pass narrow structural registry projections into generic lifecycle consumers. Production
  wrappers retain built-in public types and defaults; internal fixture-capable kernels may accept
  arbitrary registered string IDs without widening every public report, ledger, or wire type.
- **Placement consolidation:** extend the existing `PlacementBundle` additively so it owns
  scope-aware roots, resolution, duplicates, legacy/noncanonical notices, and destination facts.
  Remove generic `[0]`/`slice(1)` root-role assumptions and duplicate project/uninstall policy.
  Shared physical classification remains in `placement-shared.ts`; custom-path and cross-scope
  selection remain generic. No root-topology framework or second placement authority is accepted.
- **Verification and rendering consolidation:** preserve the existing `VerificationBundle`; add
  only one pure generic gate-policy/evaluation seam for duplicated install/dev/promote reduction.
  Renderers consume injected adapter facts or a registry-bound renderer composition while
  preserving every current human and JSON byte. No renderer DSL, callback language, or report
  schema field is introduced.
- **Capability boundary:** relevant fingerprints contain only the selected adapter version and the
  used operation/support/mode/scope facts. Snapshot authorities observe that relevant projection,
  so a used capability-version change stales work while an unrelated adapter addition/version
  change remains valid. G3B-05 supplies the deterministic fingerprint/precondition authority;
  saved-plan persistence and apply remain with their Phase-4 owners.
- **Static and behavior proof:** the selector will ban executable known-ID policy branches, direct
  production-registry closure in generic lifecycle kernels, independent built-in capability
  matrices, and root-ordinal legacy policy outside adapters. Fixture read-only/write adapters must
  traverse real generic selection, planning, placement, verification, and rendering seams. Exact
  Claude/Codex lifecycle/deep behavior and Kilo/OpenCode effect-free capability exit 4 remain
  unchanged.
- **Explicit exclusions:** no Kilo/OpenCode mutation, production adapter addition, adaptation
  implementation, new verification mode, public/wire/schema/codec change, desired-state command,
  manifest/lock mutation, ledger/journal/coordinator/recovery redesign, operation-event work,
  dynamic plugin loading, DI container, inheritance hierarchy, workflow engine, or broad public
  widening of `SupportedTool`/`FlipTool`.
- **Safety and test policy:** this is authorized, credential-free defensive local work for the
  open-source Skillsmith CLI. Mapping changes governance only, so no runtime or full-suite replay is
  required. Test-first begins with the exact TS06 selector and standard smoke. Focused replays include
  signed P1-TS09, agents/verify registry contracts, P3B-TS05, and affected lifecycle tests. Recovery
  smoke joins only if coordinator/recovery mechanics enter scope; the full repository suite remains
  reserved for terminal G3B-05 sign-off.
- **Independent review:** read-only mapping, architecture, and simplicity audits agree that the
  existing registry foundation is sound and the residual is bounded consumption/injection rather
  than a new framework. Confidence is 96% overall; Ready must freeze the additive bundle shape,
  fixture typing boundary, renderer injection, static allowlist, and relevant-capability contract.
- **Result:** exact scope, dependency and integration baselines, validation ownership, residual
  architecture, consolidation opportunities, capability semantics, exclusions, and verification
  policy are mapped. No Ready, test-first, implementation, entity, validation, or sign-off gate is
  implied.

## Ready plan

- **Accepted plan:** [P17-G3B-05.md](../plans/P17-G3B-05.md), SHA-256
  `407b2dc8e83ccf28d73884db462d7ef9917edde90e3d1729028b12d729b13df2`.
- **Accepted ADR amendment:** [ADR 0007](../../../docs/adr/0007-tool-adapter-registry.md) now
  completes registry-bound lifecycle consumption while preserving the original validated registry,
  cohesive bundle, focused-port, and compatibility decisions. No second adapter or lifecycle
  framework is introduced.
- **Exact scope:** two primary entities, one required-now/impacted selector, no downstream
  coverage, and exactly 55 owned product/test paths: 33 architecture/production/fixture paths and
  22 focused tests. Governance files remain outside the product manifest.
- **Registry boundary:** generic kernels receive the least-authority structural registry projection
  `ids/get/toolsFor/capability`; production wrappers provide the built-in registry once. Derived
  1.x compatibility exports remain, but they are not hidden lifecycle authority.
- **Fixture boundary:** internal lifecycle/planning types retain built-in defaults while private
  generic seams can carry validated fixture IDs through selection, immutable planning, verification,
  placement, hermetic prepared execution binding, and rendering. Fixtures never enter fixed public
  reports, durable swaps, ledgers, journals, artifact codecs, or saved-plan DTOs. Production
  Claude/Codex replays prove durable lifecycle behavior.
- **Placement boundary:** additive root facts plus scope-aware list/resolve methods normalize once
  into a structural superset of the legacy placement bundle. Registry normalization requires one
  destination for every nonempty root set, rejects duplicate paths, preserves old user behavior,
  treats later undeclared roots as alternate, blocks duplicates, and invents no legacy wording.
  Generic lifecycle code no longer interprets root ordinals or rebuilds project/uninstall policy.
- **Verification/rendering boundary:** the existing verification bundle remains authoritative. One
  pure helper may reduce gate mode/verdict policy while command messages and error mapping remain
  separate. Current renderer composition injects adapter facts; flip consumes the actual prepared
  verification mode. No report field, renderer DSL, or human/JSON byte change is accepted.
- **Capability boundary:** the existing full capability reader/product remains unchanged for
  status and agents. Mutation uses a separately named private relevant reader over immutable
  tool/operation/scope queries. The canonical query digest participates in resource identity and
  each observation resolves current registry facts, so used capability changes stale while
  unrelated adapters remain valid. State snapshot/repository/read types become generic with the
  full model as the default and no cast bridge.
- **Saved-plan boundary:** only a cast-free built-in relevant-fact mapping to the existing
  `CapabilityPreconditionV1` shape is supplied. Saved-plan persistence, apply, options, stale exits,
  and fixture serialization remain with Phase 4.
- **Selector contract:** the exact test command is
  `bun test tests/ergonomics/phase/EWP-P3B-TS06.test.ts --test-name-pattern EWP-P3B-TS06`.
  Ten executable families cover static authority, fixture selection/planning/binding/rendering,
  adapter placement and gate facts, exact Claude/Codex lifecycle/deep behavior, Kilo/OpenCode
  effect-free exit 4, relevant/unrelated capability staleness, and current compatibility bytes.
- **Independent acceptance:** `/root/g3b05_mapping_audit` accepted scope, validation ownership,
  exclusions, and the 55-path manifest at 97%; `/root/g3b04_absent_live_recovery` accepted the
  registry/placement/capability/renderer architecture at 95%; and
  `/root/g3b04_d9_equivalence_fix` accepted the consolidation and anti-overengineering boundary at
  98%. All reviews were read-only and had no implementation authority.
- **Immutable-title note:** the catalog's truncated one-line T06/TS06 titles are a pre-existing
  immutable generator baseline. The full canonical wording is preserved in the plan and evidence;
  repairing the generator requires a separate project-audit change outside this group.
- **Catalog transition:** P17-G3B-05 advances only from `mapped` to `ready` and passes only its Ready
  gate. `EWP-P3B-T06` advances from `planned` to `ready` with the same 55 paths and exact command.
  `EWP-P3B-TS06` remains `planned`; no test-first, implementation, validation, workflow, review, or
  sign-off gate is implied.
- **Verification policy:** Ready receives deterministic catalog/checklist, package, diff, manifest,
  and governance validation only. Runtime behavior is unchanged. TS06 plus standard smoke starts at
  test-first; focused signed selectors replay during implementation; recovery smoke joins only if
  durability mechanics enter scope; the complete repository suite remains terminal-only.
- **Result:** architecture, ownership, fixture/public boundary, placement normalization,
  relevant-capability semantics, renderer compatibility, selector obligations, exclusions, and
  verification policy are fully frozen. G3B-05 is Ready for its executable red selector.

## Test-first evidence

- **Committed boundary:** implementation-free commit
  `d99cdf1edd2f1a0400edd506df348a20dac4d1a7` adds exactly the TS06 selector and its two hermetic
  fixture adapters. Selector-only correction `efa869487fb8fe8d5e25f6b0d60d392a170d0054`
  removes false-red coupling and nests the ten families under the catalog's single executable
  selector owner. Neither commit changes production, architecture, governance, public contracts,
  durable state, or recovery. The test-first boundary SHA-256 values were
  `6f47b72a277c642da31e8d4107d558a49c9bbd796d526ba6ead5410ad6a7713b` for TS06,
  `59e0b9c8d696059f8eedcc5b2afe82c49477e2a13a272d3edd1abf29cb4280c9` for the read-only adapter,
  and `0e83072a97dd7eaa41a1b818a2e6b0cb4216877a5a390e6b61af1c4eb0eff810` for the writable adapter.
- **Characterization green:** ten tests and 98 assertions preserve centralized built-in adapter
  composition, unknown-versus-unsupported exits, current four-command canonical planning,
  operation identity, injected static/deep verification, exact Claude/Codex roots and gate facts,
  twelve effect-free Kilo/OpenCode application paths, the unchanged complete public capability
  reader, exact human/JSON byte fingerprints, current wire bindings, compatibility projections,
  and operation-ID independence from capability versions.
- **Executable reds:** ten tests fail only on missing G3B-05 authority. They bind registry-injected
  selection and planning for a novel writable ID; normalized scoped placement with duplicate and
  alternate routing; fixture identity through a hermetic plan/controller/coordinator binding;
  one pure verification gate reducer; per-renderer selected facts and actual flip mode; exact
  built-in placement roles; registry-bound lifecycle capability policy; relevant capability query
  identity and staleness; and cast-free generic internal state models without widening public wire
  DTOs. Missing future modules are guarded and never prevent selector loading.
- **Static authority:** TypeScript-AST checks distinguish imported production authority from
  shadowing injected parameters/locals and permit one exported wrapper/default composition call.
  They reject generic known-tool policy, production-global reads inside kernels, root-ordinal
  placement policy, sibling-adapter type authority, renderer registry lookups, complete-capability
  mutation readers, public fixture widening, and capability-model double casts.
- **Independent acceptance:** the first review blocked incomplete binding, renderer, capability,
  compatibility, and effect-free coverage. Two correction rounds removed an invented low-level
  one-shot contract, exact source-wording checks, renderer-signature coupling, exposed-query
  coupling, mapper-name coupling, and production-wrapper false reds. The final independent audit
  returned PASS at 98% confidence and reproduced ten passes, ten intentional failures, and 120
  assertions; the characterization-only replay reproduced ten passes and 98 assertions.
- **Bounded validation:** the exact TS06 command exits 1 with the intentional red split. Standard
  smoke passes five tests with 3,735 assertions. TypeScript, targeted Biome, no skip/todo/live/
  subprocess scan, secret scan, commit hooks, and `git diff --check` pass. Recovery smoke is not
  required because no coordinator, journal, ledger, swap, recovery, or production code changed.
  The complete repository suite remains reserved for terminal G3B-05 sign-off.
- **Catalog transition:** P17-G3B-05 advances only from `ready` to `active` and passes only its
  test-first gate. `EWP-P3B-T06` advances from `ready` to `red`; `EWP-P3B-TS06` advances from
  `planned` to `failing` with its exact target, command, three owned paths, and receipt below. No
  minimal-implementation, targeted-green, impacted-green, refactor, review, traceability, or
  sign-off credit is implied.
- **Result:** the accepted G3B-05 architecture is now bound by a truthful, executable, hermetic red
  selector without production implementation or later-gate advancement.

### Red execution receipt EWP-P3B-TS06

- Validation ID: `EWP-P3B-TS06`
- Command: `bun test tests/ergonomics/phase/EWP-P3B-TS06.test.ts --test-name-pattern EWP-P3B-TS06`
- Exit status: `1`
- Result: 10 passed and 10 failed intentionally with 120 assertions; all ten failures reach missing
  registry-bound lifecycle, placement, gate, renderer, relevant-capability, or generic-state
  authority while every characterization clause remains green.
- Revision: `efa869487fb8fe8d5e25f6b0d60d392a170d0054`

## Shared lifecycle interface implementation

- **Committed boundary:** commit `a5e064dd298a8750a919f09987ef20de6b84043a` adds the
  shared G3B-05 interfaces without advancing the minimal-implementation gate. It normalizes
  scope-aware placement at registry construction, injects lifecycle selection authority,
  genericizes immutable planning and the private execution binding, adds the relevant-capability
  reader/model, and makes observed-state internals capability-model generic while preserving the
  complete public capability reader and built-in report/wire defaults.
- **Selector maintenance:** TS06 now passes the accepted explicit planning context to fixture
  pair-ID, operation-ID, execution-result, and coordinator calls. This corrects the executable
  harness to match the Ready requirement that novel IDs are accepted only through supplied
  registry context; it does not change scope or expected behavior. The current TS06 SHA-256 is
  `23766aeb8dbc4edbae2bb0a8406c0a59d7c7a5300e020865b7645d4d8d30bd76`; both fixture SHAs are
  unchanged from test-first.
- **Architecture result:** the production surface retains one `ToolRegistry`, one normalized
  placement bundle, one planning model, and one execution model. Public built-in unions, reports,
  ledgers, codecs, saved-plan DTOs, operation IDs, and renderer bytes remain closed. The relevant
  reader exposes only `resourceId`, `observe`, and `observeRevision`; no unused query accessor or
  second lifecycle framework was retained.
- **Independent audit:** `/root/g3b05_interface_integration_audit` found and closed two integration
  issues: the no-registry selection overload is now strictly built-in, and lifecycle application
  injection uses the exported built-in-default `LifecycleToolRegistry`. Its refreshed architecture
  audit passed least-authority injection, private-ID containment, placement compatibility, generic
  planning/execution, cast-free generic state, full-reader compatibility, and absence of duplicate
  lifecycle authority at 97% confidence.
- **Bounded validation:** TypeScript passes. The independent combined replay passed 66 targeted
  tests with 435 assertions. The exact TS06 selector now has 16 passes and four intentional later
  migration failures with 147 assertions: acquire/place root authority, the shared gate reducer,
  renderer facts, and mutation consumers of the relevant capability reader. Standard smoke passes
  five tests with 3,735 assertions. Recovery smoke passes four tests with 453 assertions because
  the private coordinator/scheduler type seam changed. Targeted Biome, commit hooks, secret scan,
  boundary lint, and `git diff --check` pass. The full suite remains terminal-only.
- **Gate state:** P17-G3B-05 remains `active` with 3/10 gates passed. `EWP-P3B-T06` remains `red`
  and `EWP-P3B-TS06` remains `failing`; no minimal-implementation or later-gate credit is claimed.
- **Next:** migrate acquire/place to normalized adapter placement, consolidate verification-gate
  reduction, bind mutation snapshots to exact relevant capability queries, and inject selected
  renderer facts.

## Minimal implementation

- **Accepted implementation sequence:** shared lifecycle interfaces landed at
  `a5e064dd298a8750a919f09987ef20de6b84043a`; the complete registry-bound lifecycle migration
  landed at `f97e885216fb56c1597e3e679f0c02ba6ec8091c`.
- **Registry-bound lifecycle:** application-service defaults, acquisition detection and
  verification, placement verification, operation-specific tool routing, immutable planning,
  prepared execution, relevant capability snapshots, and renderer composition now remain bound to
  the supplied lifecycle registry. Production wrappers supply the built-in registry once and
  retain every current public type and default.
- **Placement policy:** normalized adapter bundles own destination/alternate roles, scoped
  resolution, duplicates, and notices. Name and path targets consume that authority, including
  cross-scope shadows, custom uninstall capability scope, multiple adapter notices, and refusal of
  alternate-root duplicate creation.
- **Planning and gate consolidation:** acquisition and placement planners receive the explicit
  registry/tool order for pair, operation, check, diagnostic, and canonical plan construction. One
  pure verification reducer owns strict verdict semantics; explicit custom detector/verifier
  dependencies remain authoritative while the defaults dispatch through the injected adapter.
- **Compatibility boundary:** public built-in unions, reports, ledgers, journals, codecs, saved-plan
  DTOs, operation IDs, current human/JSON bytes, and Kilo/OpenCode effect-free capability exits are
  unchanged. No plugin loader, DI container, renderer DSL, root-topology framework, or second
  lifecycle authority was introduced.
- **Result:** the minimal-implementation gate passes on accepted implementation pin
  `f97e885216fb56c1597e3e679f0c02ba6ec8091c`.

## Targeted and impacted green

- **Required selector:** exact `EWP-P3B-TS06` passes **20/20 with 147 assertions**. Its SHA-256 is
  `8108a6c0fe4c15f9bd6cfe399355aa713ca5c9d1391d2d5b7172de7e172e5a6c`.
- **Complete owned focused aggregate:** all **21 non-selector focused files** pass **291/291 with
  1,460 assertions** in **59.02 seconds**. This includes registry/placement, lifecycle application,
  acquisition, placement, planning, state, and CLI renderer compatibility coverage.
- **Focused correction proofs:** injected default detection, injected default verification,
  registry-bound application defaults, active-operation tool routing, alternate registry planning
  order, cross-scope legacy shadows, custom uninstall capability scope, duplicate path targets, and
  plural adapter notices all pass on the accepted bytes.
- **Bounded recurring gates:** standard smoke passes **5/5 with 3,735 assertions**; recovery smoke
  passes **4/4 with 453 assertions** because private prepared execution/planning seams changed.
- **Static and governance gates:** TypeScript, targeted Biome over 27 changed TypeScript files,
  boundary and secret-scanning commit hooks, `git diff --check`, and `check:p17 --pr-openable` pass.
  Package validation remains deterministic at **425 entities, 45 groups, and 244 validation
  obligations**.
- **Result:** targeted-green and impacted-green pass on
  `f97e885216fb56c1597e3e679f0c02ba6ec8091c`. There is no downstream coverage to advance, and the
  complete repository suite remains reserved for terminal sign-off.

### Final green execution receipt EWP-P3B-TS06

- Validation ID: `EWP-P3B-TS06`
- Command: `bun test tests/ergonomics/phase/EWP-P3B-TS06.test.ts --test-name-pattern EWP-P3B-TS06`
- Exit status: `0`
- Result: 20 passed, 0 failed, 0 skipped, 147 assertions.
- Revision: `f97e885216fb56c1597e3e679f0c02ba6ec8091c`

## Refactor review

- The pre-G3B-04 architectural review and the final G3B-05 simplicity audit both reject a second
  registry, service locator, lifecycle framework, verification policy matrix, renderer DSL, or
  public fixture-ID widening. The accepted design threads one least-authority registry projection
  through existing seams and keeps adapter facts at composition boundaries.
- Review found and closed residual production-authority fallbacks in default acquire detection,
  default place verification, lifecycle application-service defaults, and canonical planning
  order. It also replaced install-support coupling with the actual `dev`, `promote`, or `undo`
  capability and closed path-target duplicate/alternate bypasses.
- Nonblocking successors are intentionally deferred: remove one redundant private registry default
  when next touched, consider sharing the small place operation/tool projection after a third
  consumer exists, and align internal `*WithRegistry` parameter order opportunistically. Extracting
  either local verifier projection now would increase coupling without reducing authority.
- **Result:** no known refactor debt or required architecture change remains. The refactor gate
  passes on `f97e885216fb56c1597e3e679f0c02ba6ec8091c`.

## Adversarial review

- Independent architecture audit `/root/g3b05_minimal_arch_audit` and adversarial audit
  `/root/g3b05_minimal_adversarial_audit` found and caused correction of custom uninstall scope,
  generic alternate-root policy, incomplete cross-scope inspection, reconstructed duplicate
  reasons, truncated notices, renderer policy fallback, and out-of-manifest test placement.
- Final simplicity reviewer `/root/g3b05_final_simplicity_audit` then found and caused correction of
  path-target duplicate creation, default detector/verifier production fallback, production-bound
  application defaults, canonical planning fallback, and install-capability routing reused by
  dev/promote/undo. Its refreshed final verdict is **PASS with no blockers at 98% confidence**.
- The final independent matrix rechecks normalized root roles, zero/duplicate/missing destinations,
  exact adapter notices/reasons, read-only capability exits, strict warn/inconclusive gates,
  no-verify/static/deep behavior, relevant capability staleness, recorded renderer mode precedence,
  built-in bytes, private fixture containment, public/wire closure, and ownership.
- Final receipts are TS06 **20/20 with 147 assertions**, all owned focused files **291/291 with
  1,460 assertions**, smoke **5/5 with 3,735 assertions**, recovery smoke **4/4 with 453
  assertions**, plus green TypeScript, Biome, hooks, package validation, and diff checks.
- **Result:** the adversarial-review gate passes. Traceability closure and signed-off remain pending
  until this reviewed state is committed and the single reserved terminal full repository suite
  passes on committed bytes.

## Terminal compatibility correction and mandatory architecture review

- **First reserved terminal replay:** reviewed commit
  `d7afc35718be3529d3f682f64fc87af4c429319f` ran the complete repository lane in **616.23
  seconds**: **2,312 passed, 28 skipped, 5 failed, and 95,211 assertions**. The failures exposed
  real compatibility and extraction issues in map-callback operation construction, coordinator
  export/import ownership, and the acquisition/placement runner no-growth ratchets. Sign-off was
  withheld.
- **Compatibility correction:** commit
  `d3879c6b91ab329ddb8f63ef55c3aac1ce4339ae` recognizes only genuine owned array-map callback
  invocations, restores cycle-safe coordinator and scheduler ownership, and moves pure acquisition
  and placement responsibilities behind the accepted private modules while keeping report/result
  projection at the runner edge. The second complete replay ran in **683.20 seconds** with **2,317
  passed, 28 skipped, 1 failed, and 95,705 assertions**. The sole failure was the pre-existing
  ambient-effects gate detecting `new Date` after timestamp normalization moved into
  `acquire/execute.ts`.
- **Final correction:** commit `462ab46a0ed1911ba9ad586e2fd6d91143ab1545` injects the journal
  clock and transaction-ID callbacks into acquisition execution. The runner retains fallback and
  ISO-normalization authority. Production `detectTool` composition also remains at the runner edge;
  generic execution receives only the supplied detector sentinel and injected registry authority.
- **No-growth and ownership:** `packages/core/src/acquire/run.ts` is exactly **3,955/3,955 lines**;
  `packages/core/src/place/run.ts` remains **2,916/2,917 lines**. Report summaries, diagnostics,
  install hints, and public result projection remain runner-edge concerns. No public API, wire,
  schema, codec, persisted-state, or package-root export changed.
- **Independent review:** `/root/g3b05_final_simplicity_audit` first blocked production detection
  ownership in the generic execution module. After correction it returned **PASS with zero
  blockers and 99% confidence**, confirming least-authority effects, registry injection, report
  ownership, no import cycles, no framework growth, and no public-surface expansion.
- **Focused terminal proof:** compatibility selectors pass **34/34 with 369 assertions**; the
  final affected P1 selectors pass **25/25 with 306 assertions**; focused acquisition execution and
  uninstall pass **33/33 with 174 assertions**; injected registry detection passes; smoke passes
  **5/5 with 3,735 assertions**; and recovery smoke passes **4/4 with 453 assertions**. TypeScript,
  targeted Biome, ESLint boundaries, and `git diff --check` pass.
- **Architecture recommendation:** no additional architecture change belongs in G3B-05. A future
  touch may narrow `detectAcquireTool` from the complete `InstallDeps` object to the detector
  function itself, but the current access is already bounded and another abstraction would not
  improve the sign-off boundary.

## Terminal full repository gate

- The accepted terminal `bun run check` on clean committed pin
  `462ab46a0ed1911ba9ad586e2fd6d91143ab1545` exits **0**. Biome over **638 files**, ESLint
  capability boundaries, TypeScript, actionlint, and P17 package/catalog validation all pass.
- The complete repository lane runs **2,346 tests across 246 files in 595.98 seconds**:
  **2,318 passed, 28 intentional live/network skips, 0 failures, and 95,705 assertions**.
- The exact required selector was then replayed on the same clean pin and passes **20/20 with 147
  assertions**. No test, selector, baseline, skip policy, or assertion was weakened for sign-off.
- **Result:** G3B-05 has a clean terminal receipt on committed bytes. No waiver or further complete
  repository replay is required.

## Traceability and sign-off

- Accepted implementation/test pin:
  `462ab46a0ed1911ba9ad586e2fd6d91143ab1545`. The append-only implementation sequence includes
  registry migration `f97e885216fb56c1597e3e679f0c02ba6ec8091c`, terminal compatibility
  correction `d3879c6b91ab329ddb8f63ef55c3aac1ce4339ae`, and final clock/ID injection
  `462ab46a0ed1911ba9ad586e2fd6d91143ab1545`.
- Scope closes at exactly **2 primary entities, 1 required-now/impacted validation, 0 downstream
  obligations, and 55 owned product/test paths**. Group and task manifests remain identical and
  unique; governance evidence, catalog, and generated checklist remain outside the product
  manifest.
- `EWP-P3B-T06` and `EWP-P3B-TS06` close here. The selector retains its exact executable target,
  command, red receipt, final green receipt, and terminal full-suite coverage.
- Required selector, owned focused aggregate, smoke/recovery lanes, static/package gates, two
  terminal correction cycles, and the final independent zero-blocker architecture review establish
  the accepted implementation. No known item or architecture change remains before successor work.
- G3B-05 is signed off. The next planned Phase-3B group is **P17-G3B-06**, operation context and
  lifecycle events through execution and recovery; no G3B-06 entity or gate is advanced here.
- This remains authorized, credential-free, defensive local development for the open-source
  Skillsmith CLI. Standing authorization is recorded in
  `projects/p17/evidence/standing-authorization.md`; no human-review pause is required. Sign-off
  owner/date: root Codex goal, 2026-07-16 America/Los_Angeles.
