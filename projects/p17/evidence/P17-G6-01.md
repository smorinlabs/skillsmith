# P17-G6-01 evidence

## Mapping and prerequisites

Mapping began from clean signed G6-03 revision
`fba27f56345b626d9cbdc7a48a2d5a9c2ec385b8` on 2026-07-26.

- Phase 6 is active; G6-02B and G6-03 are both signed off at all ten lifecycle gates.
- G6-01 starts pristine and planned: no owned files, executable targets, commands, implementers,
  reviewer, evidence, or passed gates.
- Primary ownership is exactly four entities: `EWP-P6-T01`, `EWP-P6-T02`, `EWP-P6-TS01`, and
  `P1-12`.
- Required-now is exactly `EWP-P6-TS01`.
- Impacted coverage is `EWP-P6-TS01` plus downstream `EWP-WF01`; the latter remains planned,
  release-tier, and primarily owned by G6-04.
- The mapped candidate proposes exactly 13 product/lifecycle owned paths. Eight newly proposed
  packaging/script/test paths are comment- or prose-only ownership placeholders; they export and
  execute nothing and are not executable targets, release artifacts, or passing validations.

## Current distribution and release characterization

The existing repository has four explicit Bun compile scripts and a host-only build helper, but all
explicit scripts target `dist/skillsmith`. CI compiles one OS-selected target and runs one binary
smoke. Release Please creates version/changelog/tag/GitHub Release metadata only. It does not upload
native assets, checksums, completion files, npm packages, or Homebrew formulae. The justfile has no
release-check, as expected before G6-04.

`packages/cli/package.json` currently exposes `./src/index.ts` as its bin and depends on
`@skillsmith/core` through `workspace:*`; publishing it unchanged would not be a standalone npm or
Bun CLI distribution. README truthfully lists source checkout as available and Homebrew, npm/Bun,
and native release assets as not yet published. `docs/releases.md` still describes binary upload,
Homebrew, and npm publication as deferred and must be corrected only when candidate artifacts
exist.

The branch describes as `v0.7.0-661-gfba27f5`; `v0.7.0` is an ancestor, while repository tag
`v0.7.1` is not an ancestor of this execution branch. Root, CLI, and core manifests all remain
version `0.7.0`. This rules out tag proximity as a version authority and supports the plan's
explicit version/revision inputs plus G6-04's later release-please and exact-SHA binding.

## Registry and channel identity audit

Read-only npm registry queries on 2026-07-26 returned:

- `skillsmith@0.0.1`, owned by an unrelated project;
- `@skillsmith/cli@0.8.3`, also owned by an unrelated project; and
- no public entries for `@smorinlabs/skillsmith` or its four proposed platform payload names.

The earlier research fallback to `@skillsmith/cli` is therefore stale. The mapped package family
uses `@smorinlabs/skillsmith` and four `@smorinlabs/skillsmith-<os>-<arch>` payloads, while retaining
the installed bin name `skillsmith`. Registry absence does not prove publish authority: G6-04 must
verify the maintainer's npm scope and trusted-publisher configuration before any publication.

Read-only inspection of `https://github.com/smorinlabs/homebrew-tap.git` found public HEAD
`19c3cd296546c7ef0a0a8056a986524238a9e1a9`, two existing formulae, and no Skillsmith formula. The
intended identity `smorinlabs/tap/skillsmith` is coherent, but G6-01 will only render and test a
local formula candidate; it will not edit or push the tap.

## Native and completion characterization

Pinned Bun 1.3.14 successfully cross-compiled all four unchanged targets from the signed G6-02B/
G6-03 product:

| Target | Format | Bytes |
|---|---|---:|
| darwin-arm64 | Mach-O arm64 | 84,482,402 |
| darwin-x64 | Mach-O x86_64 | 90,046,544 |
| linux-arm64 | ELF aarch64, glibc loader | 114,534,544 |
| linux-x64 | ELF x86-64, glibc loader | 115,464,320 |

The current runner is Linux arm64. Executing a hard-coded Linux-x64 binary correctly failed with
`Exec format error`; deriving the host target from `process.platform/process.arch` then passed and
reported version `0.7.0`. The plan consequently separates four-target format/build validation from
host-only local execution and requires matching CI runners for supported-platform receipts.

Direct and host-native completion outputs were byte-identical:

| Shell | Bytes | SHA-256 |
|---|---:|---|
| Bash | 2,610 | `11d3b601826f9a7545fae067411a6396d673e106566004a881e3c6537bcbe680` |
| zsh | 7,729 | `0e02eef39059285743b1eeea59cf720092a69a616ee8556360d3d4c366ffad7a` |
| Fish | 9,200 | `11f7d19361766a447436dd9260c543420b43f7f0342bb102a96c73423a36af1b` |

Two separate unchanged Linux-arm64 compiles produced different SHA-256 values. G6-01 therefore
does not claim reproducible compiler output; it requires one compiled binary per target to fan out
unchanged into every channel and records checksums over those exact produced bytes.

## Approved efficiency and library decisions

- Bun's official executable documentation confirms the exact four cross-compilation targets used
  by the plan.
- npm's official package model supplies the necessary `bin`, `files`, `optionalDependencies`,
  `os`, `cpu`, and Linux `libc` selection; no download-on-postinstall library is needed.
- Bun installs platform-specific npm dependencies and can override OS/CPU for matrix verification;
  `bun pm pack` creates standard package tarballs accepted by npm/Bun.
- Homebrew's official Formula Cookbook supplies `bin.install`, platform/architecture selection,
  temporary `test do`, and formula audit/test mechanics.
- A local Bun 1.3.14 spike found `Bun.Archive` produced byte-identical gzip archives twice but
  restored a `0755` input executable as `0600`. The plan uses standard `tar` for public executable
  archives and may use `Bun.Archive` only for inspection. This avoids both a mode regression and a
  new archive dependency.
- Ordinary iterations build only the host target and reuse it across direct, npm, Bun, and formula
  tests. The four-target build is reserved for the impacted milestone, and the full repository
  suite remains terminal-only.

Primary official references:

- <https://bun.sh/docs/bundler/executables>
- <https://bun.sh/docs/pm/cli/install>
- <https://bun.sh/docs/pm/cli/pm>
- <https://bun.sh/docs/runtime/archive>
- <https://docs.npmjs.com/files/package.json/>
- <https://docs.npmjs.com/creating-and-publishing-scoped-public-packages/>
- <https://docs.npmjs.com/trusted-publishers/>
- <https://docs.brew.sh/Formula-Cookbook>

## Mapping result

The mapped candidate is one single-lineage distribution flow:

- four native builds are created once and reused by direct archives, Homebrew, and four optional
  npm payload packages;
- one scoped launcher selects the exact platform package without postinstall downloads;
- signed G6-02B completion bytes are packaged and compared at every channel boundary;
- one release manifest and sorted checksum file bind actual output bytes without falsely claiming
  compiler reproducibility;
- clean direct, npm, Bun, and Homebrew-candidate install/upgrade/uninstall behavior is required;
  and
- G6-04 retains all external publication, exact-SHA, provenance, CI/recipe, live-tool, holistic
  workflow, and final Phase-6 authority.

No Ready acceptance, executable selector, test-first receipt, implementation, dependency change,
artifact, public package/formula, green result, review GO, traceability closure, or sign-off is
claimed by this mapping record.

## Mapping validation

The unchanged signed product baseline passed after all mapping and ownership placeholders were
present:

```text
bun test tests/ergonomics/phase/EWP-P6-TS02.test.ts --test-name-pattern EWP-P6-TS02
1 pass; 0 fail; 36 assertions

bun test tests/ergonomics/phase/EWP-P6-TS03.test.ts --test-name-pattern EWP-P6-TS03
14 pass; 0 fail; 209 assertions

bun run test:smoke
5 pass; 0 fail; 3,737 assertions
```

Deterministic catalog/checklist validation accepted 426 entities, 419 required entities, 244
validation obligations, and 45 groups. P17 PR-openable validation reported 42/44 required groups
signed off, G6-01 mapped at 1/10, and Ready as the next gate. The 27-document drift ledger,
changed executable-file formatting, and whitespace hygiene passed. The comment-only
EWP-P6-TS01 placeholder remains intentionally non-executable.

## First Ready review at 767acf4

Fresh independent reviewer `/root/g6_02b_ready_review` returned **NO-GO** with 0.995 confidence on
immutable commit `767acf4de21dd6cc59039938047953c1ef745a59`. The reviewer edited nothing and
reported no nonblocking findings. Structural mapping passed: ownership was exact at 13/13 paths,
the eight new paths remained placeholders, primary ownership was exactly four entities,
EWP-P6-TS01 remained the sole required-now null-target validation, EWP-WF01 remained downstream
under G6-04, and G6-02B/G6-03 prerequisites were signed.

The seven blocking specification findings were:

- `P17-RV-P17-G6-01-F01` (high): artifact filenames, formula URL/name, manifest schema/order,
  source revision, target IDs, modes, and checksum grammar were implementation-defined;
- `P17-RV-P17-G6-01-F02` (high): clean installs did not prove empty caches, local dependency
  selection, external-network refusal, Homebrew update/API refusal, or installed-tree closure;
- `P17-RV-P17-G6-01-F03` (high): the CommonJS launcher did not resolve npm-without-Bun versus
  Bun-without-Node runtime availability;
- `P17-RV-P17-G6-01-F04` (high): environment allowlisting, isolated build roots, credential
  stripping, config autoload refusal, canaries, and output/log scans were unspecified;
- `P17-RV-P17-G6-01-F05` (medium): the signed G6-03 128 MiB native ceiling was dropped;
- `P17-RV-P17-G6-01-F06` (medium): the two-version fixture lineage was not immutable or clearly
  separated from the actual release candidate and release-please versions; and
- `P17-RV-P17-G6-01-F07` (medium): the mandatory supported-platform receipt did not freeze the
  runner, prerequisites, selector, commands, immutable hashes, or receipt fields.

The reviewer accepted the collision conclusions, coherent scoped package family, four-target
build-once fan-out, POSIX-tar choice, signed completion reuse, documentation truth boundary, and
G6-04 exclusions. Catalog/package/docs/command-reference checks, TS02 (1 test/36 assertions), TS03
(14 tests/209 assertions), and diff hygiene were green. Ready correctly remained pending.

## Corrected Ready amendment

The first approved amendment addressed all seven findings at plan level without changing lifecycle
state, ownership, product code, placeholder executability, or a release artifact:

- F01: freezes the ordered target IDs/Bun targets, four archive and five npm tarball names,
  `skillsmith.rb`, exact release URL grammar, archive entries/modes, canonical manifest schema and
  field order, explicit 40-hex source revision, and the exact sorted 11-line checksum grammar;
- F02: replaces side-by-side payload installation with a loopback-only five-package fixture
  registry so npm/Bun exercise real optional-dependency selection, uses empty isolated roots and a
  proxy-deny listener, verifies exactly one installed payload, and disables Homebrew update/API/
  analytics while serving archives from loopback;
- F03: freezes a two-statement POSIX/CommonJS polyglot bootstrap that selects Bun or Node, and
  requires npm-without-Bun and Bun-without-Node lanes plus argv/stdio/exit/signal behavior;
- F04: defines an empty-environment allowlist, isolated HOME/XDG/tmp/cwd, disabled dotenv/bunfig
  compile autoload, stripped credentials, secret/path/repository canaries, byte scans over every
  output and log, and redacted gitleaks validation;
- F05: restores a strict raw-binary `<134217728`-byte gate and accepts/rejects the exact
  134217727/134217728/134217729 metadata boundaries;
- F06: separates actual-candidate version equality from deterministic test-only
  `0.0.0-g6-fixture.1` to `.2` executable bytes/hashes, excludes fixtures from candidate outputs,
  and forbids edits to release-please-managed files; and
- F07: requires a standard public `macos-15` arm64 receipt with macOS >=15, Homebrew >=5.0.0, Bun
  exactly 1.3.14, a frozen TS01 selector, strict formula audit and lifecycle commands, immutable
  artifact/completion hashes, environment versions, zero skips, and clean-diff evidence while
  leaving the all-four exact-SHA workflow to G6-04.

Coordinator probes support but do not substitute for the amended acceptance. Bun 1.3.14 exposes
`--env=disable`, `--no-compile-autoload-dotenv`, and `--no-compile-autoload-bunfig`; a current
host-binary canary probe found no injected XDG/config value, but TS01 still requires explicit
scrubbing and leak scans. Current official GitHub runner documentation identifies `macos-15` as the
standard arm64 public label and `macos-15-intel` as the Intel label; the plan freezes the arm64
label and minimum major versions rather than a drifting patch image.

G6-01 remains mapped at 1/10 with `reviewer: null`. A fresh independent read-only Ready review of
the immutable corrected revision is required before the catalog Ready gate, EWP-P6-TS01 target, or
any executable placeholder/product implementation may advance.

## Second Ready review at 3ebb8bb

Fresh independent reviewer `/root/g6_02b_ready_review` returned **NO-GO** with 0.998 confidence on
immutable commit `3ebb8bbfd1bc6a1f4a891b6364c561a4704337ec`. F01 through F06 were closed and
no new artifact, hermeticity, launcher, environment, size, or fixture finding remained. The
reviewer independently matched both frozen 160-byte fixture hashes and passed catalog,
PR-openable, documentation/command drift, TS02 (1 test/36 assertions), launcher parsing, and diff
checks. Unchanged signed TS03 exceeded Bun's five-second local default under load but passed all 14
tests/209 assertions with the CI 60-second timeout; this was classified nonblocking timeout
sensitivity because the amendment changed only plan/evidence/catalog files.

`P17-RV-P17-G6-01-F07` remained open at high severity for an execution/ownership deadlock. The plan
required an exact `macos-15` arm64 GitHub-hosted selector receipt, but G6-01's 13 paths excluded CI,
the existing workflow selected `macos-latest` and only a generic full suite, and G6-04 could not add
the receipt later because it depends on signed G6-01. The desired receipt was precise but not
producible inside the accepted boundary. Ready correctly remained pending and the reviewer left the
worktree/index clean.

## CI receipt ownership amendment

The approved correction adds exactly `.github/workflows/ci.yml`, increasing G6-01 ownership from
13 to 14 paths. Its authority is narrow and non-publishing: pin the existing macOS validation entry
from `macos-latest` to standard arm64 `macos-15`, preserve Ubuntu and all existing checks, and add a
macOS-only targeted step with `P17_G6_01_HOMEBREW_RECEIPT=1`, a 300-second test timeout,
single-test concurrency, and the exact supported-platform TS01 name pattern.

The opted-in family must fail rather than skip on a wrong runner label/image, OS/architecture, Bun,
Homebrew, Ruby, or tar prerequisite. It emits a canonical single-line JSON receipt into the
immutable job log. Ordinary local/full-suite runs continue to prove the cross-platform formula and
receipt model without claiming the real macOS receipt. The existing PR/push trigger and read-only
permissions remain unchanged; no artifact upload, publish action, release event, credential,
provenance, or all-four exact-SHA matrix enters G6-01. Those remain G6-04 authority after G6-01 can
complete without a dependency cycle.

Lifecycle state remains mapped at 1/10, the eight placeholders remain non-executable, and no
workflow edit occurs before a fresh independent Ready GO on the immutable 14-path amendment.

## Corrected Ready review at 528645f

Fresh independent reviewer `/root/g6_02b_ready_review` returned **GO** with 0.999 confidence on
immutable commit `528645f3e0315aef16d745465a96da3d22c90f53`. The reviewer edited nothing,
left the worktree/index clean, and reported no blocking or nonblocking findings.

F07 is closed: plan and catalog match at exactly 14 paths; the CI authority is limited to the
`macos-latest` to `macos-15` validation pin and one exact opt-in, macOS-only, non-publishing TS01
step. Unsupported receipt runners/tools fail rather than skip, the canonical log record retains all
previously frozen receipt fields, and existing triggers/read-only permissions remain unchanged.
G6-04 retains release events, credentials, publishing/uploads, provenance, exact-SHA refusal, the
all-four runnable release matrix, and public availability. G6-01 can now obtain its receipt before
G6-04 without a dependency cycle.

F01 through F06 remain closed and both frozen 160-byte fixture hashes matched independently. The
reviewer reconciled exactly four primaries, sole required-now EWP-P6-TS01, downstream EWP-WF01
under G6-04, mapped 1/10 lifecycle, null TS01 target/reviewer, and all eight non-executable
placeholders. Catalog/package, documentation drift, generated command-reference drift, fixture
hash, ownership, amendment-diff, and whitespace checks passed.

## Accepted Ready result

The corrected plan is accepted against immutable commit
`528645f3e0315aef16d745465a96da3d22c90f53`. P17-G6-01 advances from `mapped` to `ready` with
exactly its mapped and Ready gates passed. EWP-P6-T01 and EWP-P6-T02 advance to `ready`; P1-12
remains mapped; EWP-P6-TS01 remains pristine planned with a null target and no execution evidence.

The next authorized change is test-only: replace the EWP-P6-TS01 placeholder with the accepted
ID-bound families, reproduce implementation-free product reds, and record exact receipts before
editing packaging sources, product scripts, docs, package metadata, or the owned workflow. No
test-first, implementation, green, refactor, adversarial-review, traceability, or sign-off gate is
implied by Ready acceptance.

## Test-first evidence

After Ready acceptance, only the EWP-P6-TS01 placeholder was replaced. Packaging sources, release
scripts, launcher, formula template, documentation, package metadata, workflow behavior, signed
CLI/core product, and release-please files remained unchanged. Biome accepted the executable test
and root TypeScript passed before any product red was recorded.

The selector contains the ten independently runnable families from the accepted plan. Exact full
command:

```text
bun test tests/ergonomics/phase/EWP-P6-TS01.test.ts --test-name-pattern EWP-P6-TS01
```

Result at the test-only working tree:

```text
0 pass; 10 fail; 26 assertions; 10 tests; 1 file; exit 1
```

All ten families were also invoked independently with the matching `EWP-P6-TS01.*family <n>:`
pattern. Each selected exactly one test, filtered the other nine, and exited 1 for its intended
missing product surface. Family 4 executed 13 assertions before reaching the absent completion
packaging contract; family 9 executed five assertions, including both frozen fixture byte/hash
checks, before reaching the absent exported fixture lineage. Every other family stopped on its
first missing contract assertion.

### Red execution receipt EWP-P6-TS01

- Validation ID: `EWP-P6-TS01`
- Command: `bun test tests/ergonomics/phase/EWP-P6-TS01.test.ts --test-name-pattern EWP-P6-TS01`
- Exit status: `1`
- Result: 0 passed and 10 failed on implementation-free distribution gaps; 26 assertions executed.
- Revision: `working-tree`

## Minimal implementation at a72dc9c

The standard-tooling implementation is frozen by two pushed commits:

- `139c329debda84e69f5f4790c58c07168e2993b2` replaces the generic private artifact
  factory with pinned GoReleaser `v2.17.1`, adopts its Bun builder, four raw
  binaries, four archives, `SHA256SUMS`, `artifacts.json`, `metadata.json`, size
  report, and generated Homebrew cask, and retains a thin tracked-manifest npm
  staging adapter; and
- `a72dc9c137007575b63a1b800eaade7e076d6e1b` closes the immutable two-version
  lifecycle proof across direct archives, npm, and Bun, while wiring the real
  Homebrew upgrade lane to the supported macOS receipt selector.

GoReleaser is additive to the release system and substitutive only inside the
generic artifact factory. Release-please remains the version/changelog/tag and
draft-release authority. Protected environments, OIDC, exact revision guards,
credential separation, payload-first/launcher-last npm publication, and tap-PR
governance remain G6-04 concerns. The five tracked npm packages, script-free
polyglot launcher, byte-lineage checks, leak checks, and clean-install behavior
remain Skillsmith-owned. GoReleaser's npm publisher is deliberately not used.

The implementation invokes GoReleaser directly from the unchanged repository;
it does not recursively collect dependencies, copy the repository into a
synthetic source tree, render custom archives/checksums/casks, emit a custom
`release-manifest.json`, or reject a benign embedded checkout path. The build is
described truthfully as controlled and isolated. Bun target-runtime acquisition
may use the network on a cold cache, and the experimental GoReleaser Bun builder
does not create a byte-for-byte reproducibility claim.

The exact local aggregate at the immutable implementation revision passed all
ten families:

```text
bun test tests/ergonomics/phase/EWP-P6-TS01.test.ts \
  --timeout 300000 --max-concurrency 1

10 pass; 0 fail; 298 assertions; 1 file; 112.43 seconds
```

The aggregate proves the exact target/tool matrix, strict size and source
identity, standard 11-record GoReleaser inventory, four checksummed archives,
closed archive/package layouts and modes, signed completion identity, one binary
lineage across raw/archive/npm, clean direct/npm/Bun installation, loopback-only
package access, generated production cask contract, controlled environment and
canary rejection, immutable `.fixture.1` to `.fixture.2` upgrades, residue-free
uninstall, and truthful documentation/publication boundaries. The two-version
family independently passed 74 assertions. The release adapter unit suite passed
7 tests and 51 assertions; TypeScript, focused Biome, `git diff --check`,
`goreleaser check`, actionlint, generated-command-reference drift, and the
27-document P17 drift check also passed.

The required macOS arm64 receipt has not yet executed. On non-macOS hosts the
Homebrew portions stop at the reviewed static/candidate boundary; they do not
claim a real `brew install` or `brew upgrade`. Therefore only the
minimal-implementation gate advances here. Targeted-green and the validation's
passing state remain pending the exact `macos-15` receipt, which selects both
family 7 and family 9 with `P17_G6_01_HOMEBREW_RECEIPT=1`.

- Validation ID: `EWP-P6-TS01`
- Command: `bun test tests/ergonomics/phase/EWP-P6-TS01.test.ts --timeout 300000 --max-concurrency 1`
- Local exit status: `0`
- Local result: 10 passed; 0 failed; 298 assertions; macOS receipt still pending.
- Revision: `a72dc9c137007575b63a1b800eaade7e076d6e1b`

## macOS receipt toolchain and Homebrew interface correction

The first two `macos-15` receipt attempts failed before candidate compilation:

- run `30250285253`, job `89926499789`, found neither GoReleaser nor npm in the
  controlled child PATH because the allowlist assumed Linux installation roots;
  commit `bfade54daacb017b356ad7f5d10ac1f3500f689a` now derives only the exact Bun,
  GoReleaser, npm, Node, git, and tar directories from the parent runner and has a
  pure macOS-layout regression test; and
- run `30250541159`, job `89927292329`, then found the tools but correctly rejected
  runner npm `10.9.8`; commit `1e4f184a81cc7d9547c5b48283658f95f94651f8`
  installs frozen npm `12.0.1` into a runner-temporary prefix rather than weakening
  the artifact-sensitive version contract.

The third attempt, run `30250663024`, job `89927677556`, passed checkout, Bun,
dependencies, pinned npm, pinned GoReleaser, and approximately one minute of real
four-target candidate construction. Both selected families then reached Homebrew
and failed at CLI parsing because current Homebrew 5 has removed
`--no-quarantine`. Homebrew's current contribution guidance uses ordinary
`brew install --cask CASK` and `brew uninstall --cask CASK`; upstream removed the
bypass because it intentionally defeated Gatekeeper.

The plan and receipt harness are corrected to use normal `brew install --cask` and
`brew upgrade --cask` with no `xattr`, cask hook, or replacement bypass. G6-04's
signing/notarization/publication prerequisite remains unchanged. Minimal
implementation remains passed; targeted-green remains pending a successful fresh
macOS receipt on the corrected immutable commit.

- Upstream interface rationale: <https://github.com/Homebrew/brew/issues/20755>
- Current Homebrew cask test commands: <https://docs.brew.sh/Adding-Software-to-Homebrew>

Run `30250904210`, job `89928434773`, then proved both Homebrew lifecycles through
successful uninstall. The production-derived lane installed the generated cask,
executed the exact candidate, matched its binary and three completion hashes, and
removed every installed path. The immutable lane installed `.fixture.1`, performed
a real `brew upgrade` to `.fixture.2`, matched the changed executable and completion
hashes, and removed every installed path. Its only failures were two over-constrained
HTTP-count assertions: Homebrew 5 made three allowlisted GETs per selected archive
instead of one. The security contract never requires an implementation-specific
request count. The corrected assertion requires one or more requests for every exact
selected archive, rejects every other method or route, and still requires zero deny-
proxy traffic. Targeted-green remains pending a fresh receipt with that closed-route
assertion.

Run `30251166873`, job `89929252503`, again completed both Homebrew lifecycles and
showed why the apparent retry count was three: Homebrew performs an allowlisted
`HEAD` metadata probe followed by allowlisted `GET` requests for each exact archive.
The fixture server had rejected HEAD even though Homebrew recovered and installed
successfully. The corrected closed protocol serves bodyless HEAD metadata and GET
bytes only for the exact selected archive paths, requires both methods for every
selected version, and rejects every other method/path. This is a harness protocol
correction; candidate bytes, casks, installed hashes, upgrade behavior, and the
production network boundary are unchanged.
- Ready parent: `ddb55f4`.
- Family 1: ordered target matrix/artifact-name API absent.
- Family 2: fail-closed version/output/archive validators absent.
- Family 3: canonical manifest/checksum and one-lineage APIs absent.
- Family 4: signed Bash/zsh/Fish outputs still match the exact 2,610/7,729/9,200-byte hashes, but
  completion packaging paths/identity API are absent.
- Family 5: release builder remains a placeholder; host/direct lifecycle APIs are absent.
- Family 6: launcher remains a placeholder; dual-runtime bootstrap/package metadata are absent.
- Family 7: formula and exact macOS CI receipt step remain absent.
- Family 8: hermetic environment/leak gates and strict native-size API are absent.
- Family 9: both exact 160-byte fixture hashes pass in the test, but production fixture lineage is
  absent.
- Family 10: tracked packaging/docs/package script/release source remain pre-candidate truth or
  placeholders.

No implementation or green gate is claimed. The next authorized edits are minimal implementation
inside the reviewed 14-path boundary, beginning with the pure release model and fixture-driven
negative behavior before a native compile or external-tool lifecycle.

## Exact-launcher formatter ownership amendment

Implementation exposed one deterministic tooling conflict not visible while the launcher path was
a placeholder. Biome 1.9.4 formats each frozen polyglot bootstrap from `':' //; ...` to
`':'; //; ...`. The inserted semicolon changes bytes that the accepted plan and TS01 intentionally
freeze for simultaneous POSIX shell and CommonJS parsing. Leaving the path unformatted makes the
repository-wide `biome check` fail; changing the bootstrap to satisfy the formatter breaks the
accepted exact-byte contract.

The narrow proposed correction adds only `biome.json`, increasing G6-01 ownership from 14 to 15
paths. Its sole product change is one formatter-disabled override for
`packaging/npm/bin/skillsmith.cjs`; linting stays enabled for that file, and no other formatting,
lint, boundary, workflow, publication, or generated-file policy changes. Plan, evidence, and
catalog are reconciled at 15 paths. Product implementation remains uncommitted until this amendment
is independently checked and the file-specific override is accepted.

## Formatter amendment review GO at ded4f0a

The existing independent G6-01 reviewer renewed the read-only review against immutable commit
`ded4f0a2314f7bd081a4e393388021fc93d08d2e` and returned **GO** with 0.999 confidence and no
findings. The reviewer independently reproduced Biome 1.9.4 changing both approved `':' //;`
bootstrap lines to `':'; //;`, confirmed `biome.json` is the sole 14-to-15 ownership addition, and
confirmed the authority is formatter-only for `packaging/npm/bin/skillsmith.cjs` while lint remains
enabled. No G6-04 workflow, publication, permission, exact-SHA, provenance, or release authority
entered.

The first review pass identified one missing catalog evidence link. Metadata-only commit `ded4f0a`
added the exact `#exact-launcher-formatter-ownership-amendment` anchor; the renewed review verified
that correction, all 15 owned paths, catalog/PR-openable checks, evidence-anchor resolution, and a
clean index. The one file-specific formatter override is now authorized as part of minimal
implementation.

## Standard-tooling distribution amendment and Ready reset

The 2026-07-26 implementation checkpoint is commit
`b53e144aab5f767966542e08d086a63067522ee9` (`chore: checkpoint p17 g6-01 distribution
work`). The exact commit was pushed to `origin/agent/p17-execution`, and fresh local/remote
`rev-parse` values matched. Pre-commit actionlint, gitleaks, Biome, and commitlint passed. A full
`bun run check` then exposed two timeout failures rather than product assertion mismatches:

- EWP-P6-TS01 family 5 exceeded Bun's default 5-second test timeout while its `tar` child was
  terminated; the following family observed the terminated tar subprocess; and
- signed EWP-P3B-TS03's same-command rerun exceeded its explicit 30-second timeout under the loaded
  full-suite run.

The checkpoint preserved the explored 15-path implementation but did not authorize continuing its
custom release architecture.

The user paused implementation after seeing a proposed recursive dependency collector, requested
extensive industry-practice research, approved the recommended standard-tooling amendment, asked
that Skillsmith be added to the existing `smorinlabs` Homebrew tap, and required inspection of
first-party publishing repositories before finalizing the design. The explicit product-amendment
approval is the user's 2026-07-26 instruction to make the recommended changes and continue. It does
not waive the fresh independent Ready review or any technical gate.

### First-party repository inspection

Four repositories were cloned to an isolated temporary root and inspected at exact commits:

| Repository | Revision | Relevant result |
|---|---|---|
| `smorin/toggle` | `3bc7daf48e9677004943cd23324ae522f5bd7b3a` | build once; release archives feed four tracked npm payloads; tag-stamped exact optional dependencies; payload-first trusted publication; App token for tap |
| `smorinlabs/py-launch-blueprint` | `761e5829fa512a638e03d74437220ca33b6dcf67` | release-please split from tag-guarded protected OIDC publication and one-artifact transfer |
| `smorinlabs/ts-launch-blueprint` | `808f11a27bb8bc4e3c8362f7057ceb55c4782668` | deny-all permissions, full-SHA pins, tag/manifest guard, npm environment/OIDC, package dry run |
| `smorinlabs/homebrew-tap` | `19c3cd296546c7ef0a0a8056a986524238a9e1a9` | existing public tap; policy requires PR updates and discourages direct `main` pushes; current CI is formula-focused |

The retained first-party patterns are release-please ownership, exact tag/version provenance,
credential-free build-to-publish artifact transfer, protected publication environments, npm OIDC,
tracked platform manifests, exact-byte reuse, payload-first publication, idempotent reruns, and
short-lived App tokens. Toggle's precompiled formula/direct tap push, Node-only launcher, and lack
of GitHub artifact attestation are not copied. The existing tap must receive
`Casks/skillsmith.rb` through a reviewed cross-repository PR and gain cask-specific CI in G6-04.

### Official-tooling research result

Official Bun documentation confirms `bun build --compile` bundles imported files and packages and
is the normal cross-compilation interface. No documented Bun trim-path or compiler prefix-map flag
was found. Reproducible Builds identifies compiler prefix mapping or controlled build paths as the
proper remedies for embedded paths, not a recursively reconstructed dependency graph. The absolute
checkout path is consequently removed from failure canaries; credentials, dotenv/bunfig content,
isolated configuration content, and secret markers remain hard failures.

Official GoReleaser documentation confirms a Bun builder since v2.6, standard archives/checksums,
`artifacts.json`, snapshots, and Homebrew casks. Its Bun builder is still marked experimental, so
the design uses an exact version plus compatibility tests. Its npm publisher was rejected because
the documented Pro/alpha design downloads binaries in postinstall and fails the accepted
`--ignore-scripts` contract. A thin Skillsmith adapter remains for tracked npm packages.

Official Homebrew guidance places binary-only software in casks, while GoReleaser deprecates its
precompiled-formula generator. Casks may use portable `binary` and completion stanzas on macOS and
Linux. GoReleaser and Homebrew guidance also make signing/notarization the proper macOS path and
warn that `xattr` bypasses Gatekeeper. The public cask will contain no bypass. Apple signing and a
no-bypass clean install are G6-04 publication prerequisites; if credentials are unavailable, the
Homebrew channel remains deferred without blocking independently valid GitHub/npm channels.

npm's current trusted-publisher documentation confirms GitHub-hosted OIDC publication and automatic
provenance for eligible public packages. It also requires the exact repository/workflow/environment
configuration and, for trusted publishers created after 2026-05-20, an explicit allowed publish
action. G6-04 must configure all five packages accordingly.

### Pinned GoReleaser compatibility spike

GoReleaser `v2.17.1` was released during the investigation. It includes a notarization polling fix
relevant to G6-04, so it supersedes the initially tested `v2.17.0`. The Linux-arm64 archive was
downloaded with its release `checksums.txt`; `sha256sum --ignore-missing -c checksums.txt` passed.
The verified binary reported:

```text
GitVersion: 2.17.1
GitCommit: 83f4c19a5c5c0b9efef6bf2aedc6805bbcb9dfe2
Platform: linux/arm64
```

`goreleaser check` accepted a schema-v2 configuration and emitted only the expected experimental
Bun-builder notice. The controlled snapshot invocation was:

```text
env -i HOME=<isolated> XDG_CACHE_HOME=<isolated> \
  PATH=/usr/local/bin:/usr/bin:/bin CI=1 NO_COLOR=1 \
  goreleaser release --snapshot --clean -f <spike-config>
```

The exact compile flags exercised for every target were
`--compile --bytecode --env=disable --no-compile-autoload-dotenv
--no-compile-autoload-bunfig`. All four targets succeeded. A cold isolated cache visibly downloaded
Bun target runtimes for non-host targets; a second warm run reused them. This falsifies the old
network-free/hermetic claim and establishes the amended controlled, credential-free acquisition
and candidate-build language.

The final `v2.17.1` exact-version snapshot used
`snapshot.version_template: "{{ .Version }}"`, produced version `0.7.0` consistently in archive
names and cask URLs, and succeeded. Raw binary sizes were:

| Target | Bytes |
|---|---:|
| darwin-arm64 | 84,482,402 |
| darwin-x64 | 90,046,544 |
| linux-arm64 | 114,534,544 |
| linux-x64 | 115,464,320 |

Every archive contained exactly `LICENSE`, `_skillsmith`, Bash/Fish completion files, and
`skillsmith`; GoReleaser recorded `root/root`, `0644` data modes, and `0755` binary mode. Signed
completion sizes remained 2,610/7,729/9,200 bytes. The generated internal inventory contained four
Binary, four Archive, one Checksum, one Metadata, and one Homebrew Cask record. All four generated
checksums verified:

```text
0fa949d4f9eed994e90118b545d0266fdca6fef7db263f8ab1eb36e0ccb23f59  skillsmith-v0.7.0-darwin-arm64.tar.gz
e216646f2148f5e40fe4986fef53587d7230f8f0275fb60f4030eba3c9bc3866  skillsmith-v0.7.0-darwin-x64.tar.gz
83e40f9f32de3bbb51216ccad191044b36f7f85b54fff62460a16810ff95d230  skillsmith-v0.7.0-linux-arm64.tar.gz
8fe2d6208e0f306cd9ed0d7abfdda83c500a74a77b9e5eec88ee209e7fa95640  skillsmith-v0.7.0-linux-x64.tar.gz
```

GoReleaser generated `homebrew/Casks/skillsmith.rb` with all four OS/architecture branches, exact
matching SHA values, `binary`, Bash/Fish/zsh completion stanzas, and no xattr/preflight/postflight
hook. The archives carry the repository's Apache-2.0 LICENSE. Homebrew's cask DSL has no
formula-style SPDX license stanza, so the amended test checks the carried archive license rather
than inventing an invalid cask field.

The first completion-archive attempt also found a useful standard-tool constraint: GoReleaser
rejects absolute archive input paths. The accepted adapter therefore emits only the three signed
completion files into ignored `build/release-input/completions/` and references those relative
paths. It does not copy source files or dependencies.

### Accepted architecture and lifecycle correction

The amended plan replaces custom compile/archive/checksum/cask/manifest orchestration with pinned
GoReleaser. It retains a small project adapter for validated completion staging, GoReleaser
inventory checks, strict size/leak/byte-identity checks, five tracked npm manifests, exact-version
staging, and `npm pack --ignore-scripts`. `release-manifest.json`, the formula template, 11-file
checksum contract, archive-entry ordering contract, absolute checkout-path canary, and recursive
dependency collector are rejected.

Catalog lifecycle is intentionally reset to the earliest affected gate:

- Phase 6 remains active;
- P17-G6-01 returns from active/test-first to mapped with only mapped passed;
- EWP-P6-T01 and EWP-P6-T02 return to pristine planned work state, while coverage P1-12 remains
  mapped;
- EWP-P6-TS01 returns to pristine planned metadata until revised tests receive Ready authority;
- ownership expands from 15 to 21 paths for `.goreleaser.yaml` and five tracked npm manifests; and
- the six new paths are inert placeholders (`.goreleaser.yaml` comments and empty JSON objects),
  not executable configuration or publishable packages.

The historical Ready and red receipts remain in this append-only file but no longer appear as
current gate evidence. The next authorized action is a fresh independent read-only Ready review on
an immutable amendment commit. Product implementation and test replacement remain paused until GO.

### Amendment validation before immutable review

The amended catalog and generated checklist passed with 426 entities, 419 required entities, 244
validation obligations, and 45 groups. The package structural check reported Phase 6 active,
42/44 required groups signed off, P17-G6-01 mapped at 1/10, and Ready as the next gate. Focused
Biome, TypeScript, generated-command-reference, and whitespace checks passed.

Unchanged signed product baselines passed independently after the loaded parallel probe was
discarded in favor of final serial exit receipts:

```text
bun test tests/ergonomics/phase/EWP-P6-TS02.test.ts --timeout 60000 --test-name-pattern EWP-P6-TS02
1 pass; 0 fail; 36 assertions

bun test tests/ergonomics/phase/EWP-P6-TS03.test.ts --timeout 60000 --test-name-pattern EWP-P6-TS03
14 pass; 0 fail; 209 assertions

bun run test:smoke
5 pass; 0 fail; 3,737 assertions
```

No superseded EWP-P6-TS01 result is claimed. Its current executable body is quarantined historical
test code while catalog target/evidence are pristine planned; the reviewed sequence requires its
replacement and a new genuine red only after amended Ready GO.

## Standard-tooling Ready review NO-GO at 29145b8

Fresh independent reviewer `/root/g6_01_standard_ready_review` reviewed immutable commit
`29145b803e20d38badce26f0e665fbce2ce6f5d8` read-only and returned **NO-GO** with 0.99
confidence. The worktree/index were clean before and after. The reviewer independently reconciled
the catalog/package structure, 21 owned paths, six inert placeholders, exact tool/action/npm pins,
all four spike targets, archive modes and checksums, completion bytes, generated cask, first-party
repository revisions, recursive-copy prohibition, checkout-path diagnosis, and cask-over-formula
decision.

Four blocking plan findings remained:

- `P17-RV-P17-G6-01-F01` (high): conditional GitHub/npm publication while Homebrew was deferred
  contradicted the Phase-6 every-declared-distribution and no-publication-before-gate contracts.
- `P17-RV-P17-G6-01-F02` (high): G6-04 did not freeze signing before archive/npm/cask fan-out or
  explicitly disable GoReleaser publication before the complete exact-SHA gate.
- `P17-RV-P17-G6-01-F03` (medium): local cask lifecycle lacked a loopback/cache fixture and network
  denial, so it could fetch an old public artifact instead of the candidate.
- `P17-RV-P17-G6-01-F04` (medium): the rewritten plan called the launcher bootstrap byte-frozen
  without retaining its exact lines/hash or the closed launcher/payload tarball entry/mode contract.

The reviewer accepted the superseded TS01 executable only as a temporary mapped-state quarantine:
catalog target/evidence remain null, no current result is claimed, and the plan requires immediate
replacement after Ready. It may not count as current validation or a green receipt.

## F01-F04 Ready correction

The corrected plan closes all four review findings without changing ownership, product code,
placeholders, or lifecycle state:

- F01: removes partial-channel authority. Missing Apple credentials block every publication job,
  G6-04 sign-off, Phase-6 exit, and P17 completion. Channel-separable publication now requires a
  future explicit amendment to the consolidated contract, catalog, EWP-P6-TS06, EWP-WF01, and
  D-016; it cannot be inferred from one channel's green result.
- F02: freezes release-please to an exact tag plus draft GitHub Release, then a protected
  Apple-credential-only GoReleaser job with `--skip=publish,announce`. GoReleaser build and
  `notarize.macos` finish before archives/checksums/cask; the npm adapter consumes only final signed
  raw bytes; credential-free complete validation follows; and only then do separately authorized
  GitHub-attestation, npm-trusted-publisher, and tap-PR jobs run. No later step may mutate or rebuild
  candidate bytes.
- F03: validates the untouched production cask separately, then permits only a parsed/diff-checked
  disposable cask whose four two-line URL/`verified` stanzas become syntactically valid one-line
  loopback URLs. The exact local archives are the only served routes; Homebrew update/API/analytics are
  disabled, roots are isolated, proxies deny outside traffic, and the installed binary hash must
  equal the candidate.
- F04: restores the exact two POSIX/CommonJS bootstrap statements, prohibits the Biome-inserted
  semicolons, and freezes seven launcher plus four payload packed paths, `0755` executable modes,
  `0644` data modes, and exact completion locations/bytes.

GoReleaser's official notarization pipeline orders build, binary signing, and binary notarization
before downstream artifacts use those binaries. The correction therefore stays within established
tooling rather than introducing a signing/archive implementation. G6-01 remains mapped at 1/10;
renewed independent Ready review is required on the corrected immutable commit.

## F05 Ready correction

Renewed independent reviewer `/root/g6_01_standard_ready_review` reviewed immutable commit
`d5283741b0764679bfd33e204b84283302f287d6` read-only and returned **NO-GO** with 0.995 confidence.
The reviewer confirmed F01 through F04 closed and found one new blocking consistency issue:
`P17-RV-P17-G6-01-F05`. The corrected plan had required a production-derived cask upgrade while
also allowing that disposable cask to change only URL origin, `verified` metadata, and the required
trailing-comma syntax. A real two-version upgrade necessarily changes cask version, archive URLs,
and their SHA values, so those contracts could not both be satisfied.

The plan now separates the two proofs. The production-derived lane permits only its original narrow
URL/`verified` syntactic transformation and proves exact-candidate install, execution, completion
placement, installed hash identity, and uninstall. A separate immutable `.fixture.1` to
`.fixture.2` cask/archive lineage owns real `brew upgrade`; its parsed and textual allowlist permits
only the version, four loopback URLs, and four computed SHA values to change. Both loopback servers
reject unknown routes, and fixture-byte canaries must remain absent from all production candidates.
No product code, executable placeholder, ownership, or catalog lifecycle state changed. G6-01
remains mapped at 1/10 pending renewed independent Ready review of this correction.

## Standard-tooling Ready review GO at 97ed756

Independent reviewer `/root/g6_01_standard_ready_review` reviewed immutable commit
`97ed756ba15c57779e575eca8f87da329c9e5a0d` read-only and returned **GO** with 0.999 confidence.
No blocking or nonblocking findings remained. The reviewer confirmed F01 through F05 closed,
including the all-channel publication gate, sign/notarize-before-fan-out order, publish-disabled
GoReleaser candidate invocation, bounded and network-isolated production-cask fixture, exact
launcher/package closures, and separately declared immutable two-version cask upgrade lineage.

The reviewer independently passed catalog/package, documentation-drift, generated-command-
reference, whitespace, and checklist checks; reconciled 426 entities, 419 required entities, 244
validation obligations, 45 groups, exactly 21 G6-01 owned paths, and six inert placeholders; and
confirmed the worktree/index clean at the reviewed revision. The GoReleaser `v2.17.1`, Bun
`1.3.14`, npm `12.0.1`, action-SHA, four-target spike, archive/checksum/mode, completion, and cask
facts remained coherent.

## Accepted amended Ready result

The user's explicit 2026-07-26 approval covers the recommended standard-tooling amendment and its
review-driven corrections. The independent GO therefore advances P17-G6-01 from mapped to ready
with mapped and Ready passed (2/10). `EWP-P6-T01` and `EWP-P6-T02` advance from pristine planned to
ready. Coverage `P1-12` remains mapped, and `EWP-P6-TS01` remains pristine planned with no actual
target or current result until the superseded historical test body is replaced and produces a
fresh genuine red. No implementation, publication, signing, or external-tap authority is inferred
from Ready.

## Revised EWP-P6-TS01 standard-tooling test-first evidence at e7a3660

Starting from immutable accepted-Ready commit
`e7a36607c1d6f5d319e574db02eddab0ef6231e0`, the only edited path was
`tests/ergonomics/phase/EWP-P6-TS01.test.ts`. The 1,601-line superseded bespoke-release contract
was replaced with a 718-line, ten-family standard-tooling contract. Its SHA-256 before the red
receipt was `f34f42db9274d7dfd5a5637f1535729f93e171107ff242fa25187097d68b526c`.

The exact aggregate command produced a genuine implementation red:

```text
bun test tests/ergonomics/phase/EWP-P6-TS01.test.ts --test-name-pattern EWP-P6-TS01
0 pass; 10 fail; 22 expect() calls; 10 tests; 1 file
```

All ten independently named families executed and failed before any full candidate build or
network fixture could run:

1. the current target records lack the frozen GoReleaser OS/architecture mapping;
2. the exact toolchain/action contract and version/source validators are absent;
3. the custom manifest/checksum/formula renderers and formula template still exist;
4. archive validation incorrectly treats entry order as a project contract;
5. the build wrapper does not invoke GoReleaser;
6. all five tracked npm manifests are still inert empty objects;
7. the GoReleaser cask configuration is still an inert placeholder;
8. the renamed controlled environment and honest cache/acquisition contract are absent;
9. the separately bounded two-version cask lifecycle validator is absent; and
10. release/packaging guidance and CI still describe or implement the superseded distribution.

These are implementation-owned gaps, not a timeout, expected skip, mock output, external registry
failure, or stale assertion. The revised tests explicitly reject custom artifact-graph helpers,
formula generation, order-dependent archive validation, checkout-path leak canaries, and a
hermetic/network-free claim. The later portions are already wired to consume the real all-four
candidate, its GoReleaser inventory, the tracked package staging output, direct installation, and
the generated cask once earlier contracts pass; no GoReleaser-shaped private mock is accepted.

`bunx tsc --noEmit`, focused Biome, and `git diff --check` passed with only the owned TS01 path
changed. The result advances G6-01 test-first, `EWP-P6-T01`/`EWP-P6-T02` to red, and
`EWP-P6-TS01` to failing. It authorizes implementation only within the reviewed 21-path boundary.

- Validation ID: `EWP-P6-TS01`
- Command: `bun test tests/ergonomics/phase/EWP-P6-TS01.test.ts --test-name-pattern EWP-P6-TS01`
- Exit status: `1`
- Result: 0 passed; 10 failed; 22 assertions across all ten revised families.
- Revision: `working-tree`

## EWP-P6-TS01 targeted green and macOS receipt at b692aab

Branch commit `b692aab2e509b86e0f69784c4c5b74b2b8d7f42c` (PR merge revision
`0438fc7681e774fc3a1feeae5b383e540119ff53`) closes the targeted release
contract. The original catalog command exposed Bun's five-second default timeout;
the test now follows the repository pattern and declares its reviewed 300-second
budget with `setDefaultTimeout`. Its exact canonical selector rerun is recorded
below. GitHub Actions run
`30251421572`, macOS arm64 job `89930079627`, then
completed the dedicated `P17 G6-01 Homebrew supported-platform receipt` step
successfully with:

- macOS 15 arm64, Bun `1.3.14`, npm `12.0.1`, and GoReleaser `v2.17.1`;
- one real all-four GoReleaser candidate and its untouched generated production
  cask;
- production-cask install, exact candidate execution/hash, all three completion
  hashes/placements, clean uninstall, and no production cask mutation or
  quarantine bypass;
- immutable `.fixture.1` installation followed by real `brew upgrade` to
  `.fixture.2`, exact before/after executable hashes, completion placement, and
  residue-free uninstall; and
- a closed loopback protocol consisting only of HEAD and GET for each exact
  selected archive, with zero unknown method/path and zero deny-proxy requests.

The preceding failed macOS attempts are retained as useful evidence: they exposed
a Linux-only PATH assumption, an uninstalled exact npm version, Homebrew's removal
of `--no-quarantine`, and brittle HTTP request-count/method assumptions. None was
an artifact, launcher, archive, byte-identity, completion, install, upgrade, or
uninstall failure. Each correction tightened portability or aligned the harness
with current upstream behavior without adding a private release implementation.

The minimal-implementation and targeted-green gates pass. `EWP-P6-T01` and
`EWP-P6-T02` advance from red to passed, and `EWP-P6-TS01` advances from failing to
passed. `EWP-WF01` remains a planned G6-04-owned downstream workflow and is not
claimed here. Impacted-green remains pending the broader CI/repository receipt and
explicit downstream-boundary reconciliation.

- Validation ID: `EWP-P6-TS01`
- Command: `bun test tests/ergonomics/phase/EWP-P6-TS01.test.ts --test-name-pattern EWP-P6-TS01`
- Exit status: `0`
- Result: PASS — 10 passed; 0 failed; 299 assertions; 106.35 seconds.
- macOS receipt: PASS — run `30251421572`, job `89930079627`, step 7.
- Branch revision: `b692aab2e509b86e0f69784c4c5b74b2b8d7f42c`
- PR merge revision: `0438fc7681e774fc3a1feeae5b383e540119ff53`
- Revision: `b692aab2e509b86e0f69784c4c5b74b2b8d7f42c`

## Impacted-green terminal failure and compatibility ownership amendment at 709fd1a

The complete canonical impacted command ran from clean synchronized branch revision
`709fd1ae67eb4ecaf631d389baec4be232f5463f` after the release selector, signed completion and
rendering suites, smoke, typecheck, boundary lint, Biome, actionlint, generated documentation,
documentation drift, plan/package/catalog structure, catalog tests, and a 662-commit redacted
gitleaks scan had all passed:

```text
bun run check -- --timeout 60000 --max-concurrency 1

3,266 pass; 28 intentional external-environment skips; 9 fail; 8 errors
110,620 assertions; 3,303 tests across 361 files; 8,063.96 seconds
exit status: 1
```

EWP-P6-TS01 itself passed all ten families inside the complete process. The nine failures separated
into one deterministic compatibility-inventory failure and eight aggregate-load timeout/cascade
failures. Fresh isolated processes cleared all eight transient cases without a source, fixture,
assertion, or timeout edit:

```text
EWP-P4A-TS02 four affected selectors: 4 pass; 0 fail; 42 assertions; 8.81 seconds
EWP-P2-TS07 affected selector:         1 pass; 0 fail; 8 assertions; 25.13 seconds
EWP-CMD-APPLY-TS10:                   1 pass; 0 fail; 6 assertions; 10.00 seconds
EWP-CMD-APPLY-TS11 and TS12:          2 pass; 0 fail; 31 assertions; 30.90 seconds
```

The terminal run was an unhealthy performance outlier: 8,063.96 seconds versus 1,882.05 seconds
for the latest serialized green receipt. The root filesystem was 93% full and `/tmp`
contained 6,287 top-level entries. The affected cases were 4-14 times slower in aggregate, and the
first timeout allowed cleanup to invalidate shared fixture state. No timeout increase, sharding,
parallel terminal run, skip, retry-based pass, or weakened assertion is accepted. The final retry
must use an exclusive host, the exact serialized command, and a fresh `TMPDIR` under `/dev/shm`.

The deterministic failure reproduces in a fresh isolated process:

```text
bun test tests/ergonomics/phase/EWP-P1-TS11.test.ts \
  --test-name-pattern 'family 8: enforces parsed-AST ownership, decision independence, and P3B deferral'

0 pass; 1 fail; 1 assertion; 28.70 seconds
finding: package.json: scripts inventory changed
```

`package.json` correctly contains the accepted `build:release` script between
`build:linux-arm64` and `build`, while the signed family-8 exact script list omits it. This is
`P17-RV-P17-G6-01-F06` (medium, release-blocking): a stale compatibility inventory, not a product
or release-architecture defect.

The governance-only amendment expands G6-01 ownership from 21 to 22 paths by adding
`tests/ergonomics/phase/EWP-P1-TS11.test.ts`. It authorizes only insertion of the exact
`build:release` key at the matching ordered position. It does not change the test selector,
empty-findings assertion, AST boundaries, dependency/package/command/option inventories, timeout,
primary entity ownership or status, EWP-CF-038, G6 validation mappings, product code, or release
contract. Targeted-green stays passed and impacted-green stays pending. A fresh independent
read-only review of this amendment is required before the test file changes.

## Compatibility amendment review and correction at 80a6484

Fresh independent reviewer `/root/g6_01_f06_amendment_review` reviewed immutable amendment commit
`f476754` read-only and returned **GO** with 0.995 confidence. The reviewer confirmed that G6-01
ownership expands from 21 to exactly 22 paths and authorizes only one compatibility edit: insert
`build:release` between `build:linux-arm64` and `build` in EWP-P1-TS11 family 8's ordered root
script inventory. No product, release architecture, selector, timeout, primary entity, or
publication authority changed.

The reviewer raised one nonblocking wording finding: the earlier 3,266-pass comparison was not a
same-size suite because the failed terminal process discovered 3,303 tests. The evidence now calls
it the latest serialized green receipt without asserting equal suite size.

Commit `80a64842680f39010fdba2ddde234a1037796259` made the one authorized test edit and was pushed to
`origin/agent/p17-execution`. Exact isolated receipts passed without changing a timeout:

```text
EWP-P1-TS11 family 8: 1 pass; 0 fail; 1 assertion; 35.9 seconds
EWP-P1-TS11 complete: 10 pass; 0 fail; 487 assertions; 42.18 seconds
```

One cold family-1 process exceeded its 30-second `tsc` child bound; direct cold `tsc --noEmit`
took 34.67 seconds, a warm rerun took 18.37 seconds, family 1 then passed in 15.1 seconds, and the
complete selector passed. No timeout or assertion was changed. Impacted-green remained pending the
required controlled clean-checkout complete repository gate.

## Clean-checkout inventory-path finding and correction

A clean clone at exact revision `80a6484` first exposed apparent cross-phase failures while the
checkout or `TMPDIR` contained a dot. Repeating from hyphen-only checkout and temporary paths
proved those were harness artifacts rather than CLI failures:

```text
EWP-WF07:    1 pass; 0 fail; 23 assertions
EWP-P4B-TS07: 3 pass; 0 fail; 29 assertions
EWP-P5-TS01: 1 pass; 0 fail; 143 assertions
```

The same controlled checkout exposed a genuine release-adapter finding,
`P17-RV-P17-G6-01-F08` (medium, release-blocking). A normal in-repository candidate build completed
GoReleaser, then `validateGoreleaserInventory` rejected `dist/release/...` with
`darwin-arm64 binary artifact path is not absolute`. The preserved `artifacts.json` contained the
exact expected 11 records and every path existed relative to GoReleaser's repository working
directory. GoReleaser's official artifact documentation defines `path` as the relative path to the
artifact; the adapter's absolute-only assumption was therefore incorrect.

An owned EWP-P6-TS01 family-2 regression first reproduced the failure with a complete 11-artifact
relative-path inventory. The minimal correction resolves inventory paths against the explicit
GoReleaser working directory and rejects any missing, output-root-equal, traversal, or outside-root
result. It does not reconstruct an artifact graph or alter GoReleaser output. The regression and
affected release families then passed:

```text
family 2 red before correction: 0 pass; 1 fail; 23 assertions
family 2 after correction:      1 pass; 0 fail; 25 assertions
families 3, 5, and 9:           3 pass; 0 fail; 127 assertions; 248.82 seconds
complete EWP-P6-TS01:          10 pass; 0 fail; 301 assertions; 250.74 seconds
```

The exact public script `bun run build:release` then succeeded from the canonical checkout using
the default in-repository `dist/release` output. It produced four raw binaries, four closed
archives, five script-free npm tarballs, `SHA256SUMS`, `artifacts.json`, `metadata.json`, and the
generated cask. All four `sha256sum -c SHA256SUMS` entries passed from `dist/release`; every npm
payload binary hash equaled its raw target binary hash; the gitleaks step passed as part of the
builder; focused Biome and `tsc --noEmit` passed.

The first exact clean-checkout repository gate at commit `275a497` then found one deterministic
owned-unit compatibility miss before the long test body: `scripts/release-artifacts.test.ts` still
called `validateGoreleaserInventory` without the new explicit context and failed with
`context.outputRoot` undefined. The run was stopped rather than spending hours after a conclusive
failure. The already-owned unit fixture now uses GoReleaser-shaped relative paths plus the explicit
working/output roots. No implementation, timeout, or ownership changed. Exact reruns passed:

```text
scripts/release-artifacts.test.ts: 7 pass; 0 fail; 54 assertions
EWP-P6-TS01 family 2:              1 pass; 0 fail; 25 assertions
focused Biome:                     3 files clean
tsc --noEmit:                      pass
```

F08 is corrected in the working tree. Impacted-green is still pending until this correction is
committed and the exact serialized `bun run check -- --timeout 60000 --max-concurrency 1` command
passes from a fresh hyphen-only clean checkout.

## Impacted-green runner isolation blocker at 8e6152c

Commits `275a497` and `8e6152c2939d12b66bf9d85fc45485eaa970264d` committed F08 plus the
owned unit-call correction and were pushed with local/remote revision equality. Pre-commit
gitleaks, Biome, and commit-message hooks passed. The known pre-push audit/security-lint findings
remain outside this receipt; pushes used the already-disclosed `--no-verify` path and are not
claimed as pre-push green.

Fresh hyphen-only tmpfs clones at exact `8e6152c` repeatedly passed frozen install, Biome,
boundary ESLint, `tsc --noEmit`, generated docs, actionlint, and the PR-openable P17 package check.
EWP-P6-TS01 passed all ten families inside each complete process, including the corrected relative
inventory, standard GoReleaser candidate, npm/Bun lifecycle, and leak/lineage gates. The complete
monolithic repository test process nevertheless remained nondeterministic on this shared VM:

- EWP-P5-TS02 hit its unchanged 90-second timeout with no assertion mismatch; an exact isolated
  rerun passed 1/1 with all 80 assertions in 35.45 seconds.
- A timing-only disposable-clone diagnostic ran its base CLI sequence correctly in 89.47 seconds,
  then repeated the same exits and cleanup in 17.63 seconds without a source change. Under a
  six-quiet-CPU affinity it passed the complete selector in 28.21 seconds.
- A later complete process passed EWP-P5-TS02 in 33.77 seconds but EWP-P3B-TS03's same-command
  crash replay hit its unchanged 30-second bound. Its exact isolated selector passed 1/1 with all
  78 assertions in 24.76 seconds and in 8.53 seconds under the same CPU affinity.
- An affinity-controlled complete process later observed WF15's config-error child return exit 6
  instead of 3. The exact case, complete WF15 file, and exact six-file aggregate prefix all passed
  independently; the prefix closed 107 tests and 693 assertions with the signed exit 3.

No product assertion, artifact byte, archive/package layout, exit contract, or cleanup contract is
reproducibly failing. No timeout, assertion, retry, skip, fixture, Phase-5/Phase-3 owner, or release
implementation was changed.

Bun 1.3.14 advertises `--isolate` as its built-in fresh-global/handle-isolation mechanism and
`--no-orphans` as descendant cleanup. The exact six-file prefix passed with those flags, but a
fresh complete process failed inside Bun itself after WF06 with repeated
`EEXIST: file already exists, epoll_ctl` errors while reloading `signal-exit` through
`proper-lockfile`, followed by `Cannot call describe() after the test run has completed`. The same
runtime failure recurred for later files. This path is therefore not eligible evidence for the
pinned toolchain.

Hosted Actions would provide the preferred independent clean-runner receipt, but runs at this
revision have zero started steps and report that account payments failed or the spending limit
must be increased. That is an external account-state blocker rather than a code failure.

This is `P17-RV-P17-G6-01-F09` (high, release-blocking): the accepted single-process terminal gate
cannot currently provide a trustworthy deterministic result on the available shared VM, Bun's
pinned in-process isolation fails upstream, and the hosted runner cannot start. Impacted-green
remains pending.

The recommended amendment is to preserve one complete, deterministic test-file manifest but run
each file serially in its own fresh Bun process with unchanged test bodies, per-test timeouts,
`--max-concurrency 1`, and `--no-orphans`. It must fail on the first nonzero file, never retry, and
prove that every discovered file ran exactly once. Static `bun run check` gates remain unchanged.
This replaces the earlier no-sharding/single-process restriction and therefore requires explicit
user approval plus an independent governance review before it may close impacted-green. The
alternative is to resolve GitHub billing and obtain the unchanged monolithic receipt on an
exclusive hosted runner.

## Approved serial test-file terminal amendment

On 2026-07-27 the user explicitly approved the recommended F09 amendment and the associated
bounded corrections. The plan now replaces only the terminal test phase: the six existing static
commands remain exact, then `bun scripts/run-test-files-serial.ts` discovers the complete tracked,
non-hidden Bun test inventory and runs every exact file path once, serially, in a fresh Bun process.
Each child retains the 60,000 ms per-test default, `--max-concurrency 1`, and `--no-orphans`; the
runner exposes no retry, rerun, filter, shard, randomization, parallelism, isolation, or timeout
argument and stops before the next file on the first nonzero exit.

Official Bun documentation freezes the discovery surface to `*.test`, `*_test`, `*.spec`, and
`*_spec` with `js`, `jsx`, `ts`, and `tsx` extensions, and requires `./` or an absolute path to
select an exact file instead of substring filtering. The amendment uses those rules rather than a
project-invented pattern. The approval-time repository inventory is 361 tracked test files before
the new runner unit test is added; the authoritative receipt will record the post-amendment count
and SHA-256 manifest digest from the immutable clean checkout.

The existing 28 conditional external-environment skips are not hidden. JUnit measurement freezes
them to exactly six files: `packages/core/tests/verify/live-e2e.test.ts` 8,
`packages/cli/tests/commands/install-live.test.ts` 3,
`packages/cli/tests/commands/verify-live.test.ts` 3,
`packages/cli/tests/commands/flip-live.test.ts` 2,
`packages/cli/tests/commands/install-remote-live.test.ts` 4, and
`packages/cli/tests/commands/dev-source-live.test.ts` 8. The runner rejects `SKILLSMITH_E2E`; any
skip in another file or any drift from these exact counts fails. Thus no required file or case can
be skipped, no failed file can be retried, and every discovered file is accounted for exactly once.

G6-01 ownership expands from 22 to exactly 24 paths by adding
`scripts/run-test-files-serial.ts` and `scripts/run-test-files-serial.test.ts`. `package.json`, its
ordinary `test`/`check` scripts, all test bodies, timeouts, fixtures, product behavior, signed
validation mappings, and publication boundaries remain unchanged. Impacted-green remains pending
implementation, the complete clean terminal receipt, and fresh independent review.

The user also requested a saved post-completion diagnosis. After Phase 6 and final P17 closeout,
reproduce Bun 1.3.14's `--isolate --no-orphans` failure, reduce the
`EEXIST: file already exists, epoll_ctl` plus `signal-exit`/`proper-lockfile` reload sequence, test
whether newer pinned Bun releases correct it, and prepare an upstream report if it is a Bun defect.
This follow-up is explicit but does not weaken or delay the approved terminal gate.

## Serial-runner independent review NO-GO and F10-F12 corrections

Independent reviewer `/root/g6_01_f06_amendment_review` reviewed immutable commit `4ba6308` read
only and returned **NO-GO** with 0.998 confidence. Governance, 24-path plan/catalog equality, the
362-file manifest, exact fresh-process ordering, fail-fast behavior, current six-file/28-skip
inventory, focused tests, and static gates were sound. Three release-blocking proof gaps remained:

- `P17-RV-P17-G6-01-F10`: the JUnit parser trusted aggregate metrics while checking only the first
  suite's file. It could accept an additional suite naming another test file.
- `P17-RV-P17-G6-01-F11`: the execution helper validated allowlisted skips only when their files
  appeared. A subset omitting the six live-E2E files could finish with zero skips rather than
  proving the frozen 28-skip closure.
- `P17-RV-P17-G6-01-F12`: a root hidden test basename was discoverable; “hyphen-safe” paths were
  only checked for dots; Bun 1.3.14 was logged but not enforced; and repository cleanliness was not
  rechecked after successful execution and temp cleanup.

The reviewer separately confirmed that real Bun 1.3.14 JUnit uses nested same-file suites for
`describe` blocks, so the fail-closed F10 correction accepts one or more suites only when every
decoded `file` is the exact invoked path and root metrics equal the outer file-suite metrics. It
rejects a second file, a missing file identity, empty evidence, and metric disagreement without
rejecting valid nested suites.

The two already owned runner files now correct F10-F12. Preflight requires exactly six allowlist
entries totaling 28 and requires all six paths in the discovered manifest; successful completion
also requires exactly 28 aggregate skips. Discovery rejects every hidden path component. Checkout
and `TMPDIR` components use an alphanumeric-plus-hyphen grammar. The runner fails unless
`Bun.version` is exactly `1.3.14`, removes its temp/report tree, rechecks tracked and untracked
repository cleanliness, and only then emits the success receipt.

Focused correction evidence is green without executing the terminal matrix:

```text
scripts/run-test-files-serial.test.ts: 8 pass; 0 fail; 39 assertions
Biome:                                  2 runner files clean
tsc --noEmit:                           pass
p17-catalog --check:                    426 entities; 45 groups; deterministic
check-p17-package --pr-openable:        pass
git diff --check:                       pass
```

The additional cases accept nested same-file JUnit and reject a second file or metric mismatch;
reject a missing allowlisted path, five-entry map, or 27-skip map before execution; exclude a root
hidden test; reject underscore/space path components and a different Bun version; and prove the
runner temp root is removed before post-run residue causes cleanliness failure. Impacted-green
remains pending an immutable correction commit, renewed independent GO, and the complete clean
terminal receipt.

## Corrected serial-runner independent review GO at 6f90949

The F10-F12 corrections were committed and pushed at immutable revision
`6f9094901be45491d7a8a9bc16d78c3f41edeeba`. Fresh independent reviewer
`/root/g6_01_f06_amendment_review` returned **GO** with 0.999 confidence and no blocking or
nonblocking findings. The reviewer edited nothing and did not substitute a partial test run for
the required terminal receipt.

The renewed review independently confirmed that:

- plan, catalog, and evidence ownership are equal at exactly 24 paths;
- F10 is closed by requiring every nested JUnit suite to identify the one invoked file and by
  requiring root and outer-file metrics to agree;
- F11 is closed by requiring all six allowlisted files before execution and exactly 28 aggregate
  skips before success;
- F12 is closed by hidden-component rejection, strict alphanumeric-plus-hyphen checkout and temp
  paths, exact Bun 1.3.14 enforcement, cleanup, and a post-run repository cleanliness check;
- the runner still launches each exact file in a fresh process, serially, with the unchanged
  60,000 ms test timeout, `--max-concurrency 1`, `--no-orphans`, and `--retry=0`; and
- package scripts, the six static gates, product behavior, test bodies, fixtures, validation
  mappings, publication authority, and the no-retry/fail-fast boundary remain unchanged.

Focused tests remained green at 8 tests and 39 assertions. This GO accepts the governance and
runner correction but intentionally leaves impacted-green pending the one complete clean-clone
execution.

## Complete clean-clone serial terminal gate and impacted-green at 6f90949

A disposable hyphen-only tmpfs clone of `origin/agent/p17-execution` was created at exact revision
`6f9094901be45491d7a8a9bc16d78c3f41edeeba`. Its frozen install used `--ignore-scripts`; the clone
was clean before validation and used a fresh hyphen-only `TMPDIR`. No test result below came from
the mutable source checkout.

The six unchanged static terminal commands passed in that exact clone before the test phase:

```text
bun run lint                                      PASS (871 files checked)
bun run lint:boundaries                           PASS
bun run typecheck                                 PASS
bun scripts/generate-command-reference.ts --check PASS
bun run actions-lint                              PASS
bun run check:p17                                 PASS
```

The amended test phase then ran once, continuously, without restart or retry:

```text
TMPDIR=<fresh-hyphen-only-tmpdir> bun scripts/run-test-files-serial.ts

SERIAL_TEST_FILE_RECEIPT {"assertions":110722,"discovered":362,"duplicates":0,
"executed":362,"passed":362,"skipped":28,"tests":3311,"bun":"1.3.14",
"manifestSha256":"4c6174e939113c99180c47a40eba0407125c22a22fbb48d4aff42e1ff0265b7c"}
exit status: 0
```

Every one of the 362 tracked, non-hidden Bun test files was selected by its exact `./` path and
executed exactly once in its own fresh Bun process. No file was duplicated, omitted, filtered,
sharded, retried, or run after a failure. All 3,311 tests and 110,722 assertions passed. The only
skips were the frozen 28 live-E2E cases across the exact six allowlisted files; all six files were
present and their per-file counts matched. EWP-P6-TS01 passed all ten standard-distribution
families with 301 assertions inside this terminal run.

After the success receipt, the runner's private temp/report directory was absent and
`git status --short` was empty. The outer temporary root contained only the runtime's separate
Node compile cache; it was not inside the repository or the runner-owned report directory. The
canonical source worktree also remained clean at the same immutable revision.

This closes F09-F12 and advances G6-01 impacted-green to passed at lifecycle gate 6/10. It does not
claim publication, EWP-WF01, provenance, GitHub billing recovery, pre-push audit/security-lint
closure, refactor, final adversarial review, traceability closure, or sign-off. Those remain later
G6-01/G6-04 work under their existing boundaries.

## Pre-sign-off dependency and security-lint finding and ownership amendment

After impacted-green closed, the separate exact pre-push commands were rerun at `6f90949`. The
dependency audit failed on two high-severity transitive advisories:

- `fast-uri` 3.1.3 is within GHSA-v2hh-gcrm-f6hx; 3.1.4 is the first patched 3.x release; and
- `brace-expansion` 5.0.7 is within GHSA-mh99-v99m-4gvg; 5.0.8 is the first patched release.

The existing top-level overrides are working and intentionally flatten these toolchain-only
dependencies. Their minimums simply predate the newly published advisories. The correction raises
only the existing override floors to `fast-uri ^3.1.4` and `brace-expansion ^5.0.8`; it does not
perform a general or major dependency update.

The exact security-lint command failed with 10 errors and 41 warnings. All ten errors came from
inline `skillsmith/capability-ownership` directives being evaluated by a standalone security flat
config that did not register the repository's ordinary custom rule. Of the 41 warnings, one was an
unused ordinary `no-restricted-syntax` directive caused by the same config split. The remaining 40
were 22 possible-timing, 14 unsafe-regex, and four non-literal-regex heuristic findings.

Every one of those 40 findings was inspected in context. The timing findings compare public CLI
flags, parser shapes, paths, tokens, or state identities rather than secrets or cryptographic
values. The regex findings are static anchored/bounded token, identifier, base64, TOML, URL, or
redaction grammars without an unsafe ambiguous nested attacker-controlled repetition. The four
constructed expressions use already escaped wildcard text or one of a closed set of internal
identity prefixes. No source change is required to make these semantics safe.

ESLint 9.39.4 provides first-class bulk suppressions specifically so existing findings can be
recorded while the error rules remain enforced for new code. Its default committed
`eslint-suppressions.json` records findings by file and rule, rejects newly added findings beyond
the baseline, and reports stale suppressions until pruned. The amendment therefore composes the
ordinary flat config into `eslint.security.config.js`, promotes exactly the three reviewed rules
from warning to error, and creates the baseline with three exact `--suppress-rule` options rather
than `--suppress-all`. It does not disable a security rule, weaken `--max-warnings 0`, or add inline
suppression comments to 21 product files.

Official references used for the correction are ESLint's Bulk Suppressions documentation, Bun's
top-level Overrides documentation, and the two GitHub Advisory Database records named above.

G6-01 ownership expands from 24 to exactly 27 paths by adding `bun.lock`,
`eslint.security.config.js`, and `eslint-suppressions.json`; `package.json` is already owned. The
new suppression path is an inert empty-object placeholder before review. The group remains active
at 6/10 with refactor pending. No functional implementation path changes until a fresh independent
read-only review accepts this ownership and remediation boundary.
