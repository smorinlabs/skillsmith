# P17-G5-02 evidence

## Mapping and prerequisites

- **Starting head:** `43b185c901ecfaab265cd8e693a95c24705407b1`, clean and exactly synchronized
  with `origin/agent/p17-execution` at mapping start.
- **Catalog revision/hash:** SHA-256
  `00c284f4912141b6955b6b4bfade8d4ebc8e1b4d6d217cdc7038d76e3d056555`, 425 entities,
  418 required, seven deferred, 244 validation obligations, and 45 groups.
- **Dependency:** P17-G4B-03 is signed off; Phase 5 entry and G5-01 sign-off are recorded. G5-02 is
  the lowest dependency-ready unfinished group.
- **Starting lifecycle:** Phase 5 is active. G5-02 is pristine planned with 0/10 gates, no owned
  files, test commands, implementer, or reviewer.
- **Current primary scope:** 18 catalog entities: `COMMAND:update`, D-011, EWP-CF-008,
  EWP-P5-T03, EWP-P5-T04, update TS01..TS10, EWP-P5-TS02, EWP-WF09, and P2-02.
- **Current partition:** 13 required-now selectors (signed INSTALL-TS05, update TS01..TS10,
  P5-TS02, WF09) and downstream WF13.
- **Dependency-completeness finding:** UPDATE-TS07 normatively covers prior-store retention and
  undo; UNDO-TS03 covers update reversal and lock restoration; WF09 invokes `skillsmith undo
  review` after update and asserts restoration. Independent G5-02/G5-03 cannot execute those joint
  selectors. G5-05 already depends on both and owns terminal shared batch closure.
- **Grammar finding:** target registry row `argument:skillsmith update:<skill...>` is required,
  contradicting the approved safe targetless `update --check` case. The live parser must admit an
  optional variadic and enforce target-or-all semantically only for mutation/dry-run.
- **Git-authority finding:** current `resolveRemoteRef` returns only a SHA and prefers tags when a
  name matches tag and branch. It cannot prove moving-versus-fixed candidate policy or bind an
  inspected moving head to the exact fetched/materialized SHA.
- **Verification-policy finding:** adapter gate policy names install and promote only. Update TS05's
  verify/strict/deep reuse must become an explicit adapter-owned update policy before code can claim
  it.
- **Architecture result:** reuse exact reconciliation planning/execution and add update-specific
  selection/candidate/ref policy, verification projection, per-declaration artifact after-images,
  report, and service. One declaration is one group; source hash uses `projectSourceContent` plus
  `hashSourceContentV1`; old store/history use existing authorities.
- **Corrected candidate partition:** 16 G5-02 primary entities and 11 required-now validations;
  downstream exactly UPDATE-TS07, UNDO-TS03, WF09, and WF13. G5-03 has 14 primary, 13
  required-now, and the first three joint selectors downstream. G5-05 has seven primary and 14
  required-now. D-010 reciprocity adds UPDATE-TS07 and WF09 to signed G3B-03's downstream ledger,
  yielding 27 impacted, nine required-now, and 18 downstream without reopening its gates. Global
  counts do not change.
- **Candidate selector ownership:** exactly three new first-commit paths and 10 new exact selectors
  (update TS01..06, TS08..10, and P5-TS02); INSTALL-TS05 is the eleventh required-now replay.
- **Candidate file ownership:** exactly 92 bounded product/test/governance implementation paths in
  the Ready plan.
- **Safety context:** all planned remotes and roots are hermetic/disposable. No public network,
  account, credential, or external mutation is required.

## Resume and baseline gates

Fresh resume checks on the starting head passed:

```text
bun scripts/p17-catalog.ts --check
valid: 425 entities (418 required, 7 deferred; 244 validation obligations), 45 groups,
deterministic checklist

bun scripts/check-p17-package.ts --check
structurally valid; 5/7 phases approved; Phase 5 active; 35/44 required groups signed;
326/418 required entities signed; nine groups planned

bun test packages/cli/tests/contracts/options.test.ts
41 pass, 0 fail, 877 assertions

bun test packages/cli/tests/contracts/install.test.ts --test-name-pattern EWP-CMD-INSTALL-TS05
1 pass, 0 fail, 9 assertions

bun test scripts/p17-catalog.test.ts
55 pass, 0 fail, 208 assertions
```

The first catalog-test attempt ran concurrently with two other process-heavy owners and hit only
its five-second nested-process wall-clock ceiling (`exitCode: null`) while all other 54 cases
passed. The immediate isolated replay above passed the complete owner in 5.62 seconds, including
the formerly timed case in 1.01 seconds. No product/catalog assertion failed, and no timeout change
is authorized by this mapping.

## Ready candidate

The detailed candidate is `projects/p17/plans/P17-G5-02.md`. It freezes:

- the exact pre-Ready three-selector scheduling and optional-target authority amendment;
- corrected group partitions and generator assertions;
- artifact selection and semantic-preflight order;
- declaration-first target/tool selection;
- exact default/branch/tag/SHA Git authority, collision refusal, and inspected/fetched SHA binding;
- moving/fixed/ref/pin candidate rules;
- one materialization for source hash, verification, and execution;
- explicit Claude static/Codex static+deep update gate policy;
- shared reconciliation projection with per-declaration artifact/live groups;
- exact artifact-prefix, partial-failure, rerun, recovery, and retention behavior;
- D-011 plan-confirm-verify-execute ordering, including verifier-free refusal/cancellation;
- check/dry-run/execution, output, and exit contracts;
- 10 new test-first selectors, one signed impacted replay, and four downstream selectors; and
- an exact 92-path implementation ownership boundary.

The corrected frozen candidate plan SHA-256 is
`0f798dcc7fc1598894ea89e4f8e7bcb2bfb9e044c09f7b16bfd512516079910b`. This differs from the
independently approved semantic packet only by normalizing the two exact downstream arrays to the
generator's stable lexical order after mechanical generation.

At the initial freeze no lifecycle gate had advanced and no amendment or product/test file had been
edited. Fresh independent Ready review and a post-amendment recheck were required next.

## Ready review

Fresh independent review of plan
`3b967c28bc42feba14f9ef63a89a9894b51f8e301c32d66da47fd3d49cec8307` and evidence
`cd9540aac9801c6f49f897e43142dc8e171911ad3f832b1a2e33a22295601a88` returned NO-GO with
confidence 0.99 and no edits:

- `P17-RV-G5-02-F01`: UPDATE-TS07, UNDO-TS03, and WF09 all require dependency-complete G5-05;
  G5-02/G5-03 must retain reciprocal downstream/contract traceability.
- `P17-RV-G5-02-F02`: verification-before-approval contradicted D-011. Execution must plan,
  confirm, verify, then execute; refusal/cancellation invokes no verifier.
- `P17-RV-G5-02-F03`: inspected ref SHA must equal the fetched/materialized SHA; advancement must
  fail closed without replanning.
- `P17-RV-G5-02-F04`: three typed adapter/report fixtures were missing from the owned-path manifest.

The plan/evidence were corrected without advancing a lifecycle gate or editing product, selector,
catalog, or generated checklist bytes. A focused independent re-review of the corrected hashes is
required before the pre-Ready amendment may be applied.

The first focused re-review of plan `48c48171957ab52f1aacba8e013d6c7ca21b8f39bebfcb145419b5bf0eedbc45`
identified one residual mechanical omission: reciprocal D-010 ownership adds UPDATE-TS07 and WF09
to signed G3B-03's impacted/downstream partition. The corrected plan now freezes that exact
27/9/18 partition and byte-identical required-now array; this is traceability only and does not
reopen a signed executable gate.

The next focused re-review of plan `7745ec968124728ca6f32c9dab8a4eeb95b36b6f0c37bd31a6baaf4987ab24e3`
and evidence `245f311734dc7c1f0538e6689a26e4c72c8624b9afa7b819b4e2e3fa31a49f50`
confirmed F02-F04 closed and found only one remaining F01 vector mismatch. UPDATE-TS07 and
UNDO-TS03 now both retain authoritative CF-026/CF-031 reciprocity, while UNDO-TS03 no longer
falsely claims CF-008. Both exact secondary arrays include G3B-02 and G1-01. Arithmetic and the
G3B-03 27/9/18 partition are unchanged.

Final narrow re-review of plan
`1e23e9b54fa0e664191390af6acccd11f8bf160752e99cf34c2763c866b473ec` and evidence
`bac506b341c86c9ad1a515f572e3bb44bf2dbca8558dfde2e0073c80dd70927c` returned **GO** with
confidence 0.99 and no edits. The reviewed pre-Ready amendment may now be applied and mechanically
rechecked before any lifecycle transition.

## Pre-Ready amendment result

The reviewed scheduling/traceability and optional-target amendment is applied. No runtime command,
service, selector owner, adapter, planner, executor, or current-state snapshot has been activated.

- Generator-owned catalog structure is byte-for-structure equal to a fresh reviewed baseline for
  every entity ownership/traceability field and every group validation partition; lifecycle
  status, gates, owners, receipts, evidence, targets, and signed state were preserved.
- G5-02 is 16 primary / 11 required-now / four downstream; G5-03 is 14 / 13 / three; G5-05 is
  seven / 14 / zero. Signed G3B-03 is 27 impacted / nine required-now / 18 downstream.
- UPDATE-TS07, UNDO-TS03, and WF09 have the reviewed G5-05 targets, affected contracts, secondary
  groups, and reciprocal `validatedBy` relationships.
- The optional target authority is `[skill...]`, `required: false`, variadic, no choices, unset
  default, and no default source. The contradictory required `<skill...>` target row is absent;
  both historical v0.7.0 artifacts remain unchanged.
- Generated catalog SHA-256 is
  `9f74a79aa6753dedd268a56b5710705b30bf32290dd00f97d107e538761350ea`; generated checklist
  SHA-256 is `c791694fb1f14e71ff667a6b52897ce573eaff7398e9d6ed44c78039faab91fa`.

Validation receipts on the amended worktree:

```text
bun test scripts/p17-catalog.test.ts
56 pass, 0 fail, 261 assertions

bun test packages/cli/tests/contracts/options.test.ts
41 pass, 0 fail, 880 assertions

bun test packages/cli/tests/contracts/install.test.ts --test-name-pattern EWP-CMD-INSTALL-TS05
1 pass, 0 fail, 9 assertions

bun scripts/p17-catalog.ts --check
valid: 425 entities (418 required, 7 deferred; 244 validation obligations), 45 groups

bun scripts/check-p17-package.ts --check
structurally valid; 5/7 phases approved; Phase 5 active; 35/44 required groups signed;
326/418 required entities signed; nine groups planned

git diff --check
PASS
```

Fresh independent post-amendment review is required before mapped/Ready state is recorded.

## Accepted Ready result

Post-amendment review of plan
`0f798dcc7fc1598894ea89e4f8e7bcb2bfb9e044c09f7b16bfd512516079910b` and evidence
`83ccc51f7021db0f3fb3f005ee799d891fd4906bf930fbd0fce9e0a82db145b5` returned **GO** with
confidence 0.99 and no edits. The reviewer independently confirmed deterministic catalog/checklist
hashes, exact four-group partitions and reciprocity, optional target authority, immutable historical
artifacts/current-state snapshot, all 92 product/test/governance paths, absent test-first files, all
focused replays, both structural gates, and `git diff --check`.

G5-02 may advance to `ready` with mapped/Ready gates passed. Its two work entities EWP-P5-T03 and
EWP-P5-T04 become ready; coverage and validation entities remain planned. No selector target,
receipt, executable status, or later lifecycle gate is claimed. The next authorized action is the
implementation-free three-file red boundary for the 10 new primary selectors.

The transition is recorded coherently: G5-02 is `ready`, mapped/Ready are the only 2/10 passed
gates, 94 group-owned paths include the exact 92 implementation paths plus plan/evidence, 11
required-now commands are frozen, the two work entities are ready, and every coverage/validation
entity remains planned. Catalog owner 56/56, both structural validators, and `git diff --check`
remain green after checklist regeneration.

## Test-first evidence

The implementation-free boundary is committed at
`d6b98bb32e0c539b445e9367cc450bf2e3740cbf`. It adds only the two exact selector owners and
their shared hermetic fleet fixture:

- `packages/cli/tests/contracts/update.test.ts`
- `tests/ergonomics/phase/EWP-P5-TS02.test.ts`
- `tests/ergonomics/fixtures/p5-update/fleet.ts`

Each of the 10 selectors was replayed individually three times. Every replay exited 1 only after
constructing its local bare-remote/live-state fleet and observing that both the update CommandSpec
and update application-service boundary were absent. There were no fixture, type, timeout, skip,
TODO, or public-network failures. UPDATE-TS07, UNDO-TS03, and WF09 remain intentionally absent and
scheduled downstream in G5-05.

Supporting baseline receipts on the red revision:

```text
bun run typecheck
PASS

bun run lint:boundaries
PASS

bun run test:smoke
5 pass, 0 fail, 3,737 assertions

bun test packages/cli/tests/contracts/install.test.ts --test-name-pattern EWP-CMD-INSTALL-TS05
1 pass, 0 fail, 9 assertions

bun scripts/p17-catalog.ts --check
PASS

bun scripts/check-p17-package.ts --check
PASS

git diff --check
PASS
```

### Red execution receipt EWP-CMD-UPDATE-TS01

- Validation ID: `EWP-CMD-UPDATE-TS01`
- Command: `bun test packages/cli/tests/contracts/update.test.ts --test-name-pattern EWP-CMD-UPDATE-TS01`
- Exit status: `1`
- Result: FAIL — hermetic fleet reached the intended absent update CommandSpec/application-service boundary.
- Revision: `d6b98bb32e0c539b445e9367cc450bf2e3740cbf`

### Red execution receipt EWP-CMD-UPDATE-TS02

- Validation ID: `EWP-CMD-UPDATE-TS02`
- Command: `bun test packages/cli/tests/contracts/update.test.ts --test-name-pattern EWP-CMD-UPDATE-TS02`
- Exit status: `1`
- Result: FAIL — hermetic fleet reached the intended absent update CommandSpec/application-service boundary.
- Revision: `d6b98bb32e0c539b445e9367cc450bf2e3740cbf`

### Red execution receipt EWP-CMD-UPDATE-TS03

- Validation ID: `EWP-CMD-UPDATE-TS03`
- Command: `bun test packages/cli/tests/contracts/update.test.ts --test-name-pattern EWP-CMD-UPDATE-TS03`
- Exit status: `1`
- Result: FAIL — hermetic fleet reached the intended absent update CommandSpec/application-service boundary.
- Revision: `d6b98bb32e0c539b445e9367cc450bf2e3740cbf`

### Red execution receipt EWP-CMD-UPDATE-TS04

- Validation ID: `EWP-CMD-UPDATE-TS04`
- Command: `bun test packages/cli/tests/contracts/update.test.ts --test-name-pattern EWP-CMD-UPDATE-TS04`
- Exit status: `1`
- Result: FAIL — hermetic fleet reached the intended absent update CommandSpec/application-service boundary.
- Revision: `d6b98bb32e0c539b445e9367cc450bf2e3740cbf`

### Red execution receipt EWP-CMD-UPDATE-TS05

- Validation ID: `EWP-CMD-UPDATE-TS05`
- Command: `bun test packages/cli/tests/contracts/update.test.ts --test-name-pattern EWP-CMD-UPDATE-TS05`
- Exit status: `1`
- Result: FAIL — hermetic fleet reached the intended absent update CommandSpec/application-service boundary.
- Revision: `d6b98bb32e0c539b445e9367cc450bf2e3740cbf`

### Red execution receipt EWP-CMD-UPDATE-TS06

- Validation ID: `EWP-CMD-UPDATE-TS06`
- Command: `bun test packages/cli/tests/contracts/update.test.ts --test-name-pattern EWP-CMD-UPDATE-TS06`
- Exit status: `1`
- Result: FAIL — hermetic fleet reached the intended absent update CommandSpec/application-service boundary.
- Revision: `d6b98bb32e0c539b445e9367cc450bf2e3740cbf`

### Red execution receipt EWP-CMD-UPDATE-TS08

- Validation ID: `EWP-CMD-UPDATE-TS08`
- Command: `bun test packages/cli/tests/contracts/update.test.ts --test-name-pattern EWP-CMD-UPDATE-TS08`
- Exit status: `1`
- Result: FAIL — hermetic fleet reached the intended absent update CommandSpec/application-service boundary.
- Revision: `d6b98bb32e0c539b445e9367cc450bf2e3740cbf`

### Red execution receipt EWP-CMD-UPDATE-TS09

- Validation ID: `EWP-CMD-UPDATE-TS09`
- Command: `bun test packages/cli/tests/contracts/update.test.ts --test-name-pattern EWP-CMD-UPDATE-TS09`
- Exit status: `1`
- Result: FAIL — hermetic fleet reached the intended absent update CommandSpec/application-service boundary.
- Revision: `d6b98bb32e0c539b445e9367cc450bf2e3740cbf`

### Red execution receipt EWP-CMD-UPDATE-TS10

- Validation ID: `EWP-CMD-UPDATE-TS10`
- Command: `bun test packages/cli/tests/contracts/update.test.ts --test-name-pattern EWP-CMD-UPDATE-TS10`
- Exit status: `1`
- Result: FAIL — hermetic fleet reached the intended absent update CommandSpec/application-service boundary.
- Revision: `d6b98bb32e0c539b445e9367cc450bf2e3740cbf`

### Red execution receipt EWP-P5-TS02

- Validation ID: `EWP-P5-TS02`
- Command: `bun test tests/ergonomics/phase/EWP-P5-TS02.test.ts --test-name-pattern EWP-P5-TS02`
- Exit status: `1`
- Result: FAIL — hermetic fleet reached the intended absent update CommandSpec/application-service boundary.
- Revision: `d6b98bb32e0c539b445e9367cc450bf2e3740cbf`

## Minimal implementation

### Proposed ledger-only repair-history ownership amendment

The first executable TS09 multi-declaration branch reached the shared placement writer and exposed
one exact failure after the earlier group completed successfully. The second declaration's remote
SHA advanced while its canonical source-content hash remained unchanged. Its approved `repair`
operation correctly left live bytes untouched and attempted only the pinned/origin ledger update,
but the writer returned `invalid-state`.

The failure is deterministic and isolated. `commitRecordOnlyLogicalTransactionInternal` creates a
valid committed repair journal and a valid terminal pair model. Its shared `logicalJournalFor`
projection gives both ledger actuals the same zero digest; because this repair also has identical
live before/after bytes and representation, the complete actual arrays are equal.
`selectBoundedHistory` rejects that non-rollback history entry as `invalid-history`, and
`finalizeHistory` maps the refusal to `invalid-state`. A temporary local proof that assigned the
exact pre/post ledger semantic revisions to those two ledger actuals made TS09 pass end to end,
including the final all-current rerun; that exploratory edit was then reverted because
`packages/core/src/place/swap.ts` was outside the 92-path Ready boundary.

The proposed amendment adds only `packages/core/src/place/swap.ts`, limited to the record-only
repair terminal branch and the shared journal helper's optional exact ledger revision pair. It
does not weaken history validation or alter physical mutation, planning, codec, writer, recovery,
or retention behavior. Coverage stays in already-owned update execution/CLI tests. The bounded
implementation manifest becomes exactly 93 paths; plan/evidence remain status artifacts outside
that count. Fresh independent review and human approval are required before the shared file is
edited or catalog ownership is regenerated.

Independent reviewer `/root/g5_02_git_refs` returned **GO** with **0.98 confidence**, no edits,
and no blocking findings. The reviewer reproduced the exact TS09 boundary, confirmed shared swap
is the sole truthful transaction authority, and accepted the one-path 92-to-93 expansion as
minimal. The reviewer also confirmed that hashing the canonical pre-pair model and the terminal
post-pair/pre-journal model avoids self-hash circularity while preserving atomic pair-plus-history
publication, writer CAS, compatibility shadows, bounded selection, and record-only recovery.
Human approval remains required before applying the amendment.

### Pre-amendment validation checkpoint — 2026-07-22

The reviewed amendment remains unapplied and `packages/core/src/place/swap.ts` is byte-clean.
Expanded porcelain inspection reports 78 changed paths; every path is in the frozen 93-path plan
manifest or is this plan/evidence record. No skip or TODO marker
exists in either new selector owner.

All independently executable required-now selectors except the amendment-blocked TS09 are green:

```text
EWP-CMD-UPDATE-TS01  1 pass, 34 assertions
EWP-CMD-UPDATE-TS02  1 pass, 44 assertions
EWP-CMD-UPDATE-TS03  1 pass, 16 assertions
EWP-CMD-UPDATE-TS04  1 pass, 35 assertions
EWP-CMD-UPDATE-TS05  1 pass, 23 assertions
EWP-CMD-UPDATE-TS06  1 pass, 61 assertions
EWP-CMD-UPDATE-TS08  1 pass, 25 assertions
EWP-CMD-UPDATE-TS10  1 pass, 33 assertions
EWP-P5-TS02          1 pass, 80 assertions
EWP-CMD-INSTALL-TS05 1 pass, 9 assertions
```

The signed compatibility replays are green: P1-TS05, P1-TS07, P1-TS08, P1-TS09, P1-TS10,
P1-TS11, P2-TS08, P3B-TS05, and P3B-TS06. P1-TS10 now closes exactly 21 live JSON command paths,
the `update@1` registry binding, public/runtime type parity, and the committed
`tests/ergonomics/fixtures/p1-ts10/goldens/update.stdout` bytes. The first concurrent P2-TS08 run
reached only its existing 20-second compile ceiling; the isolated full replay passed 7/7 with
1,686 assertions.

Focused update gates are green: 16 update-domain tests/63 assertions; six application-service
tests/35 assertions; three codec tests/12 assertions; 15 Git-port tests/41 assertions; and the
update output plus interaction/options owners. A broader 179-test reconcile/acquire/adapter/public
API replay found one compatibility regression: apply conflict preflight read the new optional
update authority from a guarded runtime before returning its signed conflict class. The fix now
checks own-property presence without reading absent update-only authority. Its exact apply replay
and all four update execution-authority tests pass.

Supporting gates are green:

```text
bun run typecheck
PASS

bun run lint:boundaries
PASS

bun run test:smoke
5 pass, 0 fail, 3,737 assertions

bun scripts/p17-catalog.ts --check
valid: 425 entities (418 required, 7 deferred; 244 validation obligations), 45 groups

bun scripts/check-p17-package.ts --check
structurally valid; Phase 5 active; G5-02 active at 3/10 gates

git diff --check
PASS
```

Fresh bounded TS09 replay remains the single expected red: it completed the first declaration,
then the second record-only repair returned exit 1 with one succeeded group and one failed group
(47 assertions reached). Its lock effect succeeded and its repair store/live/ledger effects failed
at the already-reviewed bounded-history finalization boundary. This reproduces the amendment's
exact premise; it does not authorize the shared-file edit.

### Approved amendment application — 2026-07-22

The user explicitly approved the independently reviewed recommendation and instructed execution
to continue. The 93-path amendment is now applied exactly as reviewed:

- `logicalJournalFor` accepts an optional exact before/after ledger-revision pair while every
  existing caller retains the prior zero-digest compatibility projection;
- only the record-only `repair` terminal branch computes `ledgerSemanticRevision` for the
  canonical pre-pair model and terminal post-pair/pre-history model;
- the terminal pair and its committed repair history still publish through one ledger write; and
- physical swap phases, live bytes, schemas, validation rules, recovery, retention, and all other
  record-only operations are unchanged.

The strengthened TS09 replay passes with 62 assertions. It now proves two moving declarations
succeed, the byte-identical `review` placement is unchanged, its exact `pinned.gitSha` and
`origin.refResolved` advance, pending transactions are empty, one forward committed repair entry
exists, its before/after ledger semantic revisions differ, and the next check is fully current.

TS09 initially exceeded its old 30-second file budget after the repair allowed the test to reach
all later Git-backed scenarios. A diagnostic-marker replay completed all sections in 45.7 seconds;
the markers were removed and the owned contract file now uses the same 90-second bounded
cold-start allowance as the P5 update suite. The uninstrumented strengthened selector passed in
31.7 seconds. This is a harness-budget correction, not a production timeout or retry change.

Post-application structural receipts are green: `bun run typecheck`, the full
`scripts/p17-catalog.test.ts` suite (56 tests, 263 assertions), catalog validation, package
validation, and `git diff --check`. Catalog ownership now contains 95 tracked paths: exactly 93
implementation/test/governance paths plus the plan and evidence records. A validator assertion
pins both that cardinality and `packages/core/src/place/swap.ts` membership.

## Targeted and impacted green

### Targeted-green checkpoint — 2026-07-22

Every required-now selector passed independently on the amended candidate. The nine update command
rows retained their exact selectors; P5-TS02 retained its independent fleet owner. The already
signed install compatibility row also passed with 9 assertions.

```text
EWP-CMD-UPDATE-TS01  1 pass, 34 assertions
EWP-CMD-UPDATE-TS02  1 pass, 44 assertions
EWP-CMD-UPDATE-TS03  1 pass, 16 assertions
EWP-CMD-UPDATE-TS04  1 pass, 35 assertions
EWP-CMD-UPDATE-TS05  1 pass, 23 assertions
EWP-CMD-UPDATE-TS06  1 pass, 61 assertions
EWP-CMD-UPDATE-TS08  1 pass, 25 assertions
EWP-CMD-UPDATE-TS09  1 pass, 62 assertions
EWP-CMD-UPDATE-TS10  1 pass, 33 assertions
EWP-P5-TS02          1 pass, 80 assertions
EWP-CMD-INSTALL-TS05 1 pass, 9 assertions
```

All receipts below describe the uncommitted candidate worktree based on revision
`f9825257b3e8fd68948b6090a780ab9ff9abea89`; final same-commit receipts remain required at sign-off.

### Green execution receipt EWP-CMD-UPDATE-TS01

- Validation ID: `EWP-CMD-UPDATE-TS01`
- Command: `bun test packages/cli/tests/contracts/update.test.ts --test-name-pattern EWP-CMD-UPDATE-TS01`
- Exit status: `0`
- Result: 1 test passed; 0 failed and 0 skipped; 34 assertions.
- Revision: `working-tree`

### Green execution receipt EWP-CMD-UPDATE-TS02

- Validation ID: `EWP-CMD-UPDATE-TS02`
- Command: `bun test packages/cli/tests/contracts/update.test.ts --test-name-pattern EWP-CMD-UPDATE-TS02`
- Exit status: `0`
- Result: 1 test passed; 0 failed and 0 skipped; 44 assertions.
- Revision: `working-tree`

### Green execution receipt EWP-CMD-UPDATE-TS03

- Validation ID: `EWP-CMD-UPDATE-TS03`
- Command: `bun test packages/cli/tests/contracts/update.test.ts --test-name-pattern EWP-CMD-UPDATE-TS03`
- Exit status: `0`
- Result: 1 test passed; 0 failed and 0 skipped; 16 assertions.
- Revision: `working-tree`

### Green execution receipt EWP-CMD-UPDATE-TS04

- Validation ID: `EWP-CMD-UPDATE-TS04`
- Command: `bun test packages/cli/tests/contracts/update.test.ts --test-name-pattern EWP-CMD-UPDATE-TS04`
- Exit status: `0`
- Result: 1 test passed; 0 failed and 0 skipped; 35 assertions.
- Revision: `working-tree`

### Green execution receipt EWP-CMD-UPDATE-TS05

- Validation ID: `EWP-CMD-UPDATE-TS05`
- Command: `bun test packages/cli/tests/contracts/update.test.ts --test-name-pattern EWP-CMD-UPDATE-TS05`
- Exit status: `0`
- Result: 1 test passed; 0 failed and 0 skipped; 23 assertions.
- Revision: `working-tree`

### Green execution receipt EWP-CMD-UPDATE-TS06

- Validation ID: `EWP-CMD-UPDATE-TS06`
- Command: `bun test packages/cli/tests/contracts/update.test.ts --test-name-pattern EWP-CMD-UPDATE-TS06`
- Exit status: `0`
- Result: 1 test passed; 0 failed and 0 skipped; 61 assertions.
- Revision: `working-tree`

### Green execution receipt EWP-CMD-UPDATE-TS08

- Validation ID: `EWP-CMD-UPDATE-TS08`
- Command: `bun test packages/cli/tests/contracts/update.test.ts --test-name-pattern EWP-CMD-UPDATE-TS08`
- Exit status: `0`
- Result: 1 test passed; 0 failed and 0 skipped; 25 assertions.
- Revision: `working-tree`

### Green execution receipt EWP-CMD-UPDATE-TS09

- Validation ID: `EWP-CMD-UPDATE-TS09`
- Command: `bun test packages/cli/tests/contracts/update.test.ts --test-name-pattern EWP-CMD-UPDATE-TS09`
- Exit status: `0`
- Result: 1 test passed; 0 failed and 0 skipped; 62 assertions.
- Revision: `working-tree`

### Green execution receipt EWP-CMD-UPDATE-TS10

- Validation ID: `EWP-CMD-UPDATE-TS10`
- Command: `bun test packages/cli/tests/contracts/update.test.ts --test-name-pattern EWP-CMD-UPDATE-TS10`
- Exit status: `0`
- Result: 1 test passed; 0 failed and 0 skipped; 33 assertions.
- Revision: `working-tree`

### Green execution receipt EWP-P5-TS02

- Validation ID: `EWP-P5-TS02`
- Command: `bun test tests/ergonomics/phase/EWP-P5-TS02.test.ts --test-name-pattern EWP-P5-TS02`
- Exit status: `0`
- Result: 1 test passed; 0 failed and 0 skipped; 80 assertions.
- Revision: `working-tree`

### Impacted-green checkpoint — 2026-07-22

The first default-concurrency `bun run check` replay exposed one deterministic signed-inventory
delta and 25 contention/cascade failures. The deterministic delta was P1-TS11's command-registry
fingerprint: the approved current-state correction places `uninstall` before `update`, while the
signed expected path order and option-row hash still described the earlier order. Updating only
that exact inventory produced a green isolated P1-TS11 replay: 10 tests, 487 assertions.

The other failures were not product regressions. Bun's local default executes up to 20 tests at
once, while the repository's authoritative CI workflow runs `bun test --timeout 60000
--max-concurrency 1`. Under 20-way load, filesystem-, Git-, compiler-, and subprocess-heavy tests
overran their local budgets; timed-out bodies then raced teardown and produced secondary temporary
directory, exit-143, and stale-state errors. A serial replay of every affected owner file passed
337 tests with 3,036 assertions and no timeout changes. This included P4B-TS05, WF15, WF14, WF10,
help, coordinator, ledger-writer, reconcile/apply, place execute/swap/store-link, dev, options,
doctor, and config owners. P2-TS08 had already passed its isolated full owner with 1,686
assertions.

The authoritative CI-equivalent full repository suite then passed:

```text
bun test --timeout 60000 --max-concurrency 1
2,990 pass, 28 intentional live-E2E skips, 0 fail
102,663 assertions across 3,018 tests / 319 files
20m38s
```

The amended update selectors all passed inside that full run, including TS09's byte-identical
record-only repair/history case. The non-test milestone stages also passed on the same candidate:

```text
bun run lint
PASS — 786 files

bun run lint:boundaries
PASS

bun run typecheck
PASS

bun run actions-lint
PASS

bun run check:p17
PASS — pr-openable structural package; G5-02 active at 5/10 gates before this transition

git diff --check
PASS
```

No runtime timeout, retry, recovery, assertion, or individual test budget was weakened. The local
`bun run check` orchestration remains less reliable than the already-committed CI policy; aligning
that script is a separate repository-maintenance amendment, not required to establish G5-02
product correctness. The impacted-green gate is therefore supported by exact isolated-owner,
integrated serial-suite, and non-test milestone receipts.

## Terminal load-budget amendment

The first clean committed terminal command,
`bun run check -- --timeout 60000 --max-concurrency 1`, ran at immutable revision
`9da467501e9015ec660e960b0d883d1a11bb640b`. Biome over 786 files, architecture boundaries,
TypeScript, Actions lint, and P17 package validation all passed before the test stage. The test
stage then exposed three timeout-only results with no assertion mismatch:

- P3A-TS02 family 25 hit its explicit 15-second timeout while its spawned focused public-type
  compile was still running;
- P2-TS04's permission matrix hit an inner crash-fixture deadline after 112.08 seconds; and
- P2-TS04's physical SIGKILL matrix hit its 300-second enclosing timeout.

The attempt was interrupted after the third timeout rather than spending the remaining full-suite
time repeating a known invalid sign-off result. Both unchanged P2 cases pass serially in isolation:
the permission matrix passes **1/1 with 12,782 assertions in 183.18 seconds**, and the physical-gap
matrix passes **1/1 with 19,078 assertions in 113.44 seconds**. They authorize no product, test,
timeout, or fixture change.

P3A family 25 is reproducible: two unchanged isolated replays terminate at 15.01 and 15.00 seconds
with 59 assertions reached and one dangling compiler process cleaned up. Its exact spawned `tsc`
command exits zero when allowed to complete. A detached pre-G5-02 comparison at
`f9825257b3e8fd68948b6090a780ab9ff9abea89` passes the complete unchanged family in 8.23 seconds
with 61 assertions, proving that the approved update application/report exports moved this focused
compile beyond its historical budget.

The proposed bounded amendment adds only `tests/ergonomics/phase/EWP-P3A-TS02.test.ts` to G5-02's
owned compatibility paths and raises only family 25's enclosing timeout from `15_000` to `60_000`.
The selector, body, compile command, fixtures, assertions, production behavior, runtime deadlines,
retries, skips, and todos remain exact. This section and the matching plan/catalog ownership change
precede the test edit; a fresh independent review is required before application.

Fresh independent reviewer `/root/g5_02_final_adversarial` returned **GO at 0.99 confidence** with
no finding. It independently reproduced family 25's 15.001-second timeout with 59 assertions and
no assertion mismatch, then ran the exact spawned compile successfully in 19.60 seconds. It also
confirmed that the test blob is identical at the current and pre-G5 revisions
(`c32df649e2b2af0aa8d02f311ac0132cc4b07814`), the 60-second allowance is bounded and
proportionate, plan/catalog ownership is exact, all 56 catalog tests and structural package checks
pass, no P2 edit is authorized, and the prior product GO remains applicable. The reviewer made no
file, staging, or commit change. The timeout-only edit is approved.

The approved P3A correction is committed at
`a0d94850a35d62928286055628aeb928f25815c9`. The focused family passes with 61 assertions in
22.31 seconds, the complete P3A owner passes 2/2 with 80 assertions, and independent post-edit
confirmation passes the family with 61 assertions in 11.77 seconds while verifying the exact
one-line diff. TypeScript, boundaries, catalog/package validation, and diff checks pass.

A subsequent clean terminal replay in a fresh symlink-free in-memory temporary directory cleared
P3A family 25 in 26.08 seconds, the P3B process-order case, P2-TS04 permission classification in
183.59 seconds, and P2-TS04 physical gaps in 141.20 seconds. It then reached the already-owned
P2-TS08 family 6 and killed that family's synchronous compiler at its explicit 20-second timeout.
The failure had no compiler exit code and no assertion mismatch; 60 assertions had completed.
The attempt was stopped after this invalid terminal result.

P2-TS08 family 6 reproduces unchanged in isolation at 20.35 seconds with the same 60 reached
assertions. Its exact spawned `tsc` command exits zero in 30.06 seconds. A detached pre-G5-02
comparison also reaches its unchanged 20-second ceiling with 57 assertions, showing that the
compiler guard does not reliably fit its historical budget under terminal load even before the
additive update contract assertions. The proposed correction raises only family 6's enclosing
timeout from `20_000` to `60_000`. Because P2-TS08 is already owned, no manifest or catalog path
changes. No selector, test body, compile command, fixture, assertion, production behavior, runtime
deadline, retry, skip, or todo may change. Fresh independent approval is required before edit.

Fresh reviewer `/root/g5_02_final_adversarial` returned **GO at 0.97 confidence** for the one-line
edit. Its independent family-6 replay passed in 14.43 seconds with 60 assertions and its exact
compiler passed in 14.28 seconds, confirming load sensitivity rather than a semantic failure. It
opened `P17-RV-G5-02-F07` (**low, resolved**) because the proposed prose described the ceiling as
deterministic/permanent; the corrected language above now says it does not reliably fit under
terminal load. The reviewer confirmed the test remained byte-identical and unedited, the file was
already owned, 60 seconds is bounded and proportionate, no other change is authorized, structural
checks pass, and the prior product GO remains applicable. It made no file, staging, or commit
change. The timeout-only edit is approved.

## Refactor

The stable working tree received a bounded structural audit at `2026-07-22T17:10:05Z`.

- **Shared engines remain authoritative:** update delegates artifact observation, live/store/ledger
  observation, reconciliation planning, saved-plan projection and validation, execution scheduling,
  transaction journaling, rollback/recovery, and artifact coordination to the existing acquisition
  and reconciliation modules. `packages/core/src/update/execute.ts` is an authority-checking adapter
  over `executeValidatedReconcilePlan`; it is not a second executor.
- **One materialization and one approved graph:** `prepareUpdateObservationV1` resolves each selected
  exact SHA once and retains that prepared source for verification and execution. The application
  service validates the saved projection before constructing the update artifact prefix, and the
  executor consumes the same approved operation/check IDs without replanning after approval.
- **Duplicate-projection decision:** the update report's operation/check/diagnostic maps are kept
  local because they additionally redact local-development source identities and project
  update-specific group, verification, approval, effect, and drift state. The superficially similar
  plan/sync projections consume different authority products. Extracting them now would couple
  command domains without one exact shared contract or a second exact consumer.
- **Local adapters are bounded:** the remaining type assertions are confined to branded digest and
  codec/tuple compatibility boundaries already exercised by the signed P1/P2 contract suites. The
  audit found no command-local planner, scheduler, coordinator, writer, journal, recovery path,
  ambient clock/randomness, process spawning, network fetch, ignore directive, or deferred-work
  marker in the new update modules.
- **Hygiene:** both required skip/todo and non-fixture URL scans returned no matches. Gitleaks 8.21.2
  scanned all 619 commits with `gitleaks detect --no-banner --redact --source .` and found no leaks.
- **Green baseline retained:** no source change was justified by the audit, so the impacted-green
  receipts remain byte-for-byte applicable: the CI-equivalent full suite passed 2,990 tests with
  102,663 assertions and zero failures, and lint, boundary lint, TypeScript, Actions lint,
  P17/package validation, and diff checks all passed.

**Refactor result:** PASS. No speculative abstraction or behavior change was introduced.

## Independent adversarial review

Fresh read-only reviewer `/root/g5_02_final_adversarial`, who did not implement G5-02, reviewed the
complete working-tree candidate and returned an initial **NO-GO at 0.98 confidence**. It confirmed
the ref/pin authority, collision handling, one-materialization binding, approval ordering,
artifact-prefix planning, record-only repair history, codec redaction, ownership, and governance,
but rejected two application-envelope failure paths:

- `P17-RV-G5-02-F05` (**high, resolved**): preparation cleanup errors were discarded; post-report
  cleanup replaced a truthful report and mutation with an empty refusal; executor cleanup errors
  carrying completed results could lose their failure; and artifact read permission denials were
  classified as state. The correction now classifies `EACCES`/`EPERM` as permission, propagates
  preparation cleanup failure while retaining cancellation precedence, preserves the existing
  report/mutation while appending one constant redacted cleanup diagnostic, and retains completed
  executor results with a nonzero cleanup exit.
- `P17-RV-G5-02-F06` (**medium, resolved**): the application mutation envelope counted successful
  groups, causing an operation-free current rerun to claim one change and obscuring durable partial
  work. The correction returns `NO_MUTATION` for an empty execution and derives planned, changed,
  unchanged, and failed counts from unique exact operation/effect IDs plus actual-image deltas.

The focused red regressions reproduced both findings before production changes. The corrected
application/artifact suite passes **12/12 with 54 assertions**, including successful mutation plus
cleanup failure, preparation cleanup permission denial, artifact-read permission denial, current
rerun zero mutation, and a committed artifact prefix followed by live failure. The complete 11-row
required-now replay passes **11/11 with 422 assertions**. Focused update, application, codec, Git,
and swap coverage passes **44/44 with 160 assertions**; standard smoke passes **5/5 with 3,737
assertions** and recovery smoke passes **4/4 with 453 assertions**. Full Biome over 786 files,
TypeScript, boundary lint, Actions lint, catalog/package validation, diff hygiene, skip/URL scans,
and the 619-commit gitleaks scan all pass after correction.

The same reviewer then independently re-inspected the corrections and replayed the focused
artifact/application suite (**12/12, 54 assertions**), TS08 (**1/1, 25 assertions**), TS09
(**1/1, 62 assertions**), TypeScript, boundary lint, catalog/package validation, and diff checks.
It closed F05 and F06, found no new issue or widened scope, and returned final **GO at 0.99
confidence** at `2026-07-22T17:48:52Z`. The reviewer explicitly made no file, staging, or commit
change.

**Independent adversarial-review result:** PASS.

## Traceability and sign-off

Pending.
