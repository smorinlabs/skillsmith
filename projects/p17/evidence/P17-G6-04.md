# P17-G6-04 evidence

## Mapping and prerequisites

Mapping began from clean pushed G6-01 sign-off revision
`ef56020a1c217e7b6e0c19965f12bac56af5ced0` on 2026-07-28.

- Phase 6 is active and G6-01 is signed off at all ten lifecycle gates.
- G6-04 began pristine and planned at 0/10: no owned files, implementer, reviewer, commands,
  executable targets, evidence, or passed gates.
- Primary ownership is exactly `D-016`, `EWP-P6-T07`, `EWP-P6-TS06`, and `EWP-WF01`.
- Required-now coverage is exactly `EWP-P6-TS06` plus `EWP-WF01`; downstream coverage is empty.
- `EWP-CF-039` remains signed under G0-05 and names G6-04 as its secondary release-gate owner.
- The mapped candidate proposes exactly 27 repository-owned paths. The seven new executable or
  automation paths are inert comment-only placeholders until a Ready GO; no validation target is
  activated and no product/release behavior changes at mapping.

## First-party repository refresh

The local precedent repositories were refreshed/cloned and inspected at exact commits:

| Repository | Revision | Retained pattern |
|---|---|---|
| `smorin/toggle` | `3bc7daf48e9677004943cd23324ae522f5bd7b3a` | App-token release-please, tag guards, build-once fan-out, protected environments, npm OIDC/payload-first, tap update |
| `smorinlabs/py-launch-blueprint` | `761e5829fa512a638e03d74437220ca33b6dcf67` | cheap exact-tag guard, transferred build, protected OIDC publication |
| `smorinlabs/ts-launch-blueprint` | `808f11a27bb8bc4e3c8362f7057ceb55c4782668` | deny-all, release-please App, manifest equality, full-SHA pins, package dry-run |
| `smorinlabs/homebrew-tap` | `19c3cd296546c7ef0a0a8056a986524238a9e1a9` | existing public destination and PR policy |

The tap currently has formula-oriented CI with floating Homebrew/cache/upload action references and
no `Casks/skillsmith.rb`. The mapped plan therefore requires a reviewed, full-SHA-pinned,
cask-specific infrastructure PR and then a separate exact-cask release PR; no direct default-branch
push is authorized.

## Official mechanism and version research

Official GoReleaser documentation confirms cross-platform `notarize.macos` signs and notarizes raw
binaries through quill and uses those signed bytes downstream. Official GitHub documentation
recommends draft -> attach assets -> publish for immutable releases and documents
`actions/attest@v4`. Official npm documentation confirms Node >=22.14, npm >=11.5.1,
GitHub-hosted/OIDC requirements, exact workflow/environment matching, automatic provenance for a
public repository/package, and the private-repository provenance exclusion. Homebrew documentation
confirms precompiled software belongs in a cask.

Read-only npm registry probes confirmed exact real-tool baselines exist:

- `@anthropic-ai/claude-code@2.1.202` (Node >=22; bin `claude`); and
- `@openai/codex@0.142.5` (Node >=16; bin `codex`).

An npm 12.0.1 dry run of `npm trust github @smorinlabs/skillsmith --repo smorinlabs/skillsmith
--file release.yml --env npm --allow-publish --yes --dry-run --json` returned the expected
`createPackage` permission. This supports preconfiguring trusted publishing before the first package
version instead of performing an unauthenticated placeholder publication. The local shell is not
npm-authenticated (`npm whoami` returns `ENEEDAUTH`), so actual trust configuration remains an
external maintainer ceremony.

Exact action revisions recorded in the plan were resolved from their upstream repositories with
`git ls-remote`; no floating action reference is accepted.

## Live GitHub readiness characterization

Read-only GitHub REST inspection on 2026-07-28 reports:

```json
{"private":true,"visibility":"private","default_branch":"main","archived":false,"disabled":false}
```

The only current Actions environment is `copilot`; repository-level Actions secrets and variables
returned empty lists. `main` is protected and the repository has prior v0.3.2 through v0.7.0
releases, but there is no current Apple/npm/release/tap environment setup.

Draft PR [#44](https://github.com/smorinlabs/skillsmith/pull/44) currently points at mapping SHA
`6a94082bb895f25913238167b544e5dfc994b1fa`. CI run `30350739325` and commitlint run
`30350739659` were both rejected before a runner started with GitHub's account payment/spending-
limit annotation. This is an external infrastructure failure, not a test result, and is now a
required executable readiness receipt rather than an informal caveat.

A read-only `gitleaks 8.21.2` all-history probe scanned 857 commits with zero findings, and the
repository currently contains zero Git LFS objects. This is encouraging but not the final public-
visibility audit: existing releases/assets, workflow logs/artifacts/caches, every public GitHub
conversation/attachment surface, refs/metadata, and third-party license/publication facts remain
explicitly required before visibility authorization.

This is a real external prerequisite, not a reason to weaken the release contract. npm provenance
is unavailable from a private source repository, and GitHub attestations for private repositories
depend on Enterprise Cloud. The project is intended to be open source, so the plan freezes a
full-history/publication audit plus an explicit visibility-change authorization immediately before
public promotion. No visibility, secret, package, tag, release, environment, or tap state was
changed during mapping.

## Mapping result

The mapped architecture is one immutable candidate lineage:

```text
release-please App -> exact tag + draft release
       -> Apple-only GoReleaser signing/notarization candidate
       -> credential-free exact-SHA release-check
       -> GitHub attest/upload/publish
       -> npm OIDC payloads then launcher
       -> reviewed tap cask PR
       -> public EWP-WF01 after tap merge
```

The candidate is never rebuilt, resigned, rearchived, or restaged after validation. Missing and
exact-existing publication state is idempotent; an occupied mismatch fails. No Ready acceptance,
executable selector, test-first receipt, implementation, publication, external mutation, green
result, review GO, traceability closure, or sign-off is claimed by this mapping record.

## Mapping validation

The unchanged signed product and the inert mapping placeholders passed:

```text
bun run lint
Checked 879 files; no fixes; exit 0

bun run lint:boundaries
exit 0

bun run typecheck
exit 0

bun test scripts/release-artifacts.test.ts scripts/run-test-files-serial.test.ts \
  --timeout 60000 --max-concurrency 1
15 passed; 0 failed; 93 assertions

bun scripts/p17-catalog.ts --check
426 entities; 419 required; 7 deferred; 244 validation obligations; 45 groups; deterministic

bun scripts/check-p17-package.ts --check
43/44 required groups signed; G6-04 mapped 1/10; Ready next; 4 required entities incomplete

bun scripts/p17-plan-structure.ts --all
EWP-P0A-TS01/06/07 valid

bun scripts/p17-documentation-drift.ts --all
27 documents valid

bun run actions-lint
exit 0

./scripts/check-gitleaks.sh full
no leaks found

git diff --check
exit 0
```

The `release.yml` placeholder is a valid deny-all workflow whose only job has an impossible empty-
repository condition; a manual dispatch therefore cannot execute release work before Ready GO.
EWP-P6-TS06 and EWP-WF01 remain comment-only and do not contain an executable selector.

## Saved post-P17 toolchain follow-up

After Phase 6 and final P17 closeout, reproduce and minimize Bun 1.3.14's
`--isolate --no-orphans` failure: `EEXIST: file already exists, epoll_ctl` while reloading
`signal-exit` through `proper-lockfile`. Separate a Bun runtime defect from module-reload/handle
lifecycle interaction, compare a newer deliberately pinned Bun, and prepare an upstream-quality
report if confirmed. This follow-up does not weaken or delay the exact-once serial terminal gate.

## First independent Ready review NO-GO at `6a94082`

The independent reviewer returned `NO-GO`, confidence `0.999`, against immutable mapping commit
`6a94082bb895f25913238167b544e5dfc994b1fa`. The review changed no state and assigned seven stable
finding IDs:

| Finding | Severity | Blocking fact |
|---|---|---|
| `P17-RV-P17-G6-04-F01` | high | Three tracked zero-test placeholders expanded signed discovery from 363 to 366; Bun emitted no JUnit, so the exact serial harness rejected them. |
| `P17-RV-P17-G6-04-F02` | high | One credential-free runner could not execute and prove the same candidate on all four supported native targets or produce a closed aggregate receipt. |
| `P17-RV-P17-G6-04-F03` | high | Generic SemVer plus current `0.7.0`/pre-major policy could create `0.8.0`; nothing forced P17's terminal release to exact `v1.0.0`. |
| `P17-RV-P17-G6-04-F04` | high | Release Please would bump package manifests but leave `bun.lock`, README, and generated command docs at `0.7.0`, making the release PR fail frozen/generated checks. |
| `P17-RV-P17-G6-04-F05` | high | Draft-only reruns, incomplete tap states, and all-channel language did not define the unavoidable partial-publication recovery state machine. |
| `P17-RV-P17-G6-04-F06` | medium | gitleaks accepted arbitrary/floating/unverified installs and actionlint executed a mutable upstream installer. |
| `P17-RV-P17-G6-04-F07` | medium | Billing currently prevents Actions jobs from starting; runner, environment/job/secret, full visibility-surface, npm-trust, immutable-release, and tap readiness receipts were incomplete. |

The reviewer affirmed that the four primaries, two required-now validations, G6-01 candidate
lineage/signing order, authority-separation direction, public-only WF01 closure, exclusions, and
saved Bun follow-up were otherwise sound. Ready remains pending; no red test or implementation is
authorized by this NO-GO.

## Approved first mapping correction

The approved correction addresses every finding before a renewed Ready review:

- delete the three empty test placeholders and remove them from current group ownership, while
  retaining the exact EWP entity `plannedTarget` paths and reserving all three for an atomic
  ownership-plus-genuine-red transition at test-first;
- run the complete rediscovered repository suite only once in the Linux-x64 `common` lane, then run
  four bounded native candidate lanes on `ubuntu-24.04`, `ubuntu-24.04-arm`, `macos-15`, and
  `macos-15-intel`, followed by an `always()` credential-free exact-receipt aggregate;
- freeze P17 to exact version `1.0.0`/tag `v1.0.0` through a one-shot `Release-As: 1.0.0` commit
  footer, with prerelease/build-metadata refusal and ordinary SemVer only after the manifest reaches
  1.0.0;
- add a short-lived-App release-PR sync job for `bun install --ignore-scripts` plus generated docs,
  limited to `bun.lock`, `README.md`, and `docs/commands.md` on the exact same-repository branch;
- add separate authority preflights and explicit GitHub draft/public, npm missing/exact/mismatch,
  and tap absent/open-exact/merged-exact/mismatch recovery from one 90-day retained candidate;
- replace both mutable tool installation paths with exact-version, per-platform SHA-256-verified,
  wrong-version-refusing installers; and
- make billing/four-runner scheduling, named environment/job/secret bindings, complete
  visibility-surface audit, immutable releases, five npm trusted publishers, Apple authority, and
  tap cask CI executable external prerequisites.

The plan now acknowledges that registries cannot publish atomically. Detectable authority/state
failures stop before mutation; an unexpected later outage creates an incomplete release that may
resume only from exact retained bytes and can never produce sign-off while any channel is absent.

Official release checksum records were fetched directly on 2026-07-28. The four supported
gitleaks 8.21.2 archives resolve to `5b42c6e...`, `cad3de5d...`, `5bc41815...`, and `654c9355...`;
actionlint 1.7.12 resolves to `5b44c3bc...`, `aba9ced2...`, `8aca8db9...`, and `325e971b...` for
Darwin x64/arm64 and Linux x64/arm64 respectively. The plan records every complete digest.

The corrected mapped ownership is 31 existing repository paths. It adds the release manifest,
changelog, lockfile, generated command reference, CLI/core manifests, and the new inert non-test
actionlint installer; it excludes the three absent future tests until the authorized test-first
transition.

After staging the three deletions, the unchanged signed discovery contract returns to exactly 363
files with manifest SHA-256
`ee6032848c6f6b09c40d1689cc89748e589406c96e82e5676f39d556029264a8`; none of the three
reserved future paths appears. This matches the signed G6-01 terminal manifest rather than
changing or exempting the runner.

Corrected mapping validation is green while G6-04 remains mapped at 1/10:

```text
bun run lint
Checked 876 files; no fixes; exit 0

bun run lint:boundaries && bun run typecheck
exit 0

bun test scripts/release-artifacts.test.ts scripts/run-test-files-serial.test.ts \
  --timeout 60000 --max-concurrency 1
15 passed; 0 failed; 93 assertions

bun scripts/p17-catalog.ts --check
426 entities; 419 required; 7 deferred; 244 validation obligations; 45 groups; deterministic

bun scripts/check-p17-package.ts --check
43/44 required groups signed; G6-04 mapped 1/10; Ready next; 4 required entities incomplete

bun scripts/p17-plan-structure.ts --all
EWP-P0A-TS01/06/07 valid

bun scripts/p17-documentation-drift.ts --all
27 documents valid

bun run actions-lint
exit 0

./scripts/check-gitleaks.sh full
no leaks found

git diff --check && git diff --cached --check
exit 0
```

## Renewed independent Ready review GO at `360297a`

The same independent reviewer examined immutable pushed commit
`360297ad4b3dc5a822df5a77dba05589e5bd3e90` (parent `6a94082b`, tree `15865b8`) and returned
`GO`, high confidence approximately `0.96`. The branch was clean and equal to
`origin/agent/p17-execution`. No finding remained unresolved and no new stable finding ID was
needed.

Independent evidence reproduced:

- 31 unique, regular, existing mapped ownership paths with no duplicate/missing path;
- all three reserved test-first files absent and outside group ownership, while TS06/WF01 retain
  exact planned targets and required-now coverage;
- 363 discovered test files with exact signed digest
  `ee6032848c6f6b09c40d1689cc89748e589406c96e82e5676f39d556029264a8`;
- focused release/serial-runner tests at 15 passed, 0 failed, 93 assertions;
- one common plus four exact native lanes and the `always()` aggregate;
- the one-shot 1.0.0 release and same-PR App-token lock/docs synchronization;
- initial-versus-retained candidate recovery, closed channel states, 90-day expiry refusal, and no
  rebuild/clobber seam;
- all eight installer archive digests matching official release records and all ten action SHAs
  resolving to their named official tags; and
- executable billing/runners, visibility audit/authorization, exact environment/secret bindings,
  Apple, immutable release, five npm trusts, and tap-CI prerequisites.

Catalog/package, plan/documentation drift, actionlint, gitleaks, diff, and focused-test checks were
green. Unsatisfied external account/service facts remain deliberate execution prerequisites; they
do not prevent the repository-contained design from reaching Ready.

## Accepted corrected Ready result

The integration owner accepts the renewed independent GO. G6-04 advances from mapped 1/10 to Ready
2/10 with exactly two future commands, one for `EWP-P6-TS06` and one for `EWP-WF01`. It does not
claim either target exists or passes. The next atomic change adds the three pre-reviewed test paths
to group ownership, creates genuine selectable failing contracts, records implementation-free red,
and advances only test-first.

## G6-04 test-first evidence at `d203718`

Starting from immutable accepted-Ready commit
`d2037188d38e41b7064d85372a9b5b640ccc74cf`, the three pre-reviewed reserved paths were created
as genuine tests and atomically added to group ownership. No release gate, workflow, recipe,
installer, ADR, publication client, candidate builder, or external state was implemented or
changed before the red receipts.

The test sources are 536 lines with pre-red SHA-256 values:

- `scripts/release-gates.test.ts` — 190 lines,
  `922b65824d01ea7f47b7c4ffb69ddf0adf07745d323a187c32158e1d6dd2bfa0`;
- `tests/ergonomics/phase/EWP-P6-TS06.test.ts` — 218 lines,
  `5cb1acdef37eefa7aba17074fe2bbfb18881cd469aeddfac56a0b1a053f3f363`; and
- `tests/ergonomics/workflows/EWP-WF01.test.ts` — 128 lines,
  `a5b2e9fe446dc10e4c2545a7582609ec076e88918812fcbdf9be2330eb6577cc`.

All independently named tests executed and failed for implementation-owned gaps:

```text
bun test scripts/release-gates.test.ts --timeout 60000 --max-concurrency 1
exit 1; 0 passed; 6 failed; 6 assertions; 6 tests; 1 file

bun test tests/ergonomics/phase/EWP-P6-TS06.test.ts \
  --test-name-pattern EWP-P6-TS06 --timeout 60000 --max-concurrency 1
exit 1; 0 passed; 10 failed; 10 assertions; 10 tests; 1 file

bun test tests/ergonomics/workflows/EWP-WF01.test.ts \
  --test-name-pattern EWP-WF01 --timeout 60000 --max-concurrency 1
exit 1; 0 passed; 4 failed; 4 assertions; 4 tests; 1 file
```

The six low-level failures identify absent executable seams for exact P17 identity,
initial-versus-retained candidate selection, common/four-native receipt closure, publication-state
classification, official tool archive verification, and external readiness. The ten TS06 families
independently expose the current noncanonical recipe/CI/pre-push path, unsafe legacy Release Please
workflow, absent exact-identity gate, absent candidate-source gate, absent native aggregate, absent
real-tool lane, absent publisher preflights, absent recovery state machine, absent readiness gate,
and inert ADR/installers. The four WF01 failures identify the absent public four-runner/channel
receipt validator and its negative lifecycle cases.

These are not timeouts, skips, network failures, unavailable credentials, or expectations against
mock success output. The tests exercise public pure gate inputs plus exact repository workflow and
recipe surfaces; later live receipts must consume the transferred candidate and actual public
channels. Focused formatting and TypeScript no-emit checks passed after the red runs.

With the files staged, Git-tracked discovery is now exactly 366 paths with digest
`58466748415ce22652f743e2ab08755ca3ee80849be6ab55cfe7c66e10961095`; the only additions to
the signed 363-path baseline are the three declared owned tests. Unlike the rejected empty
placeholders, each file executes nonzero tests and writes nonempty JUnit on failure: 2,321 bytes for
the six-test unit owner, 3,428 bytes for TS06, and 1,685 bytes for WF01. All exit 1 because their
assertions are genuinely red, so no discovery or receipt exception is needed.

This transition advances G6-04 to active at test-first 3/10, `D-016` to mapped,
`EWP-P6-T07` to red, and both `EWP-P6-TS06` and `EWP-WF01` to failing with exact selectable targets.
Group ownership is now exactly 34 existing paths. Minimal implementation is next; no green gate is
claimed.

### Red execution receipt EWP-P6-TS06

- Validation ID: `EWP-P6-TS06`
- Command: `bun test tests/ergonomics/phase/EWP-P6-TS06.test.ts --test-name-pattern EWP-P6-TS06`
- Exit status: `1`
- Result: 0 passed; 10 failed; 10 assertions across all ten independently named families.
- Revision: `working-tree`

### Red execution receipt EWP-WF01

- Validation ID: `EWP-WF01`
- Command: `bun test tests/ergonomics/workflows/EWP-WF01.test.ts --test-name-pattern EWP-WF01`
- Exit status: `1`
- Result: 0 passed; 4 failed; 4 assertions across all four independently named public-workflow cases.
- Revision: `working-tree`

## Minimal implementation

The implementation closes the repository-contained seams authorized by the accepted Ready design
without publishing, changing repository visibility, creating a tag/release/package, changing a
protected environment, or editing the external tap:

- `just check` is the sole ordinary recipe and delegates once to the exact serial fresh-process
  runner; package and pre-push entry points use the same terminal harness.
- CI has one Linux-x64 canonical gate plus bounded Linux-arm64/macOS-arm64/macOS-x64 native
  receipts. Every workflow action is pinned to a full commit SHA and Dependabot owns reviewable
  GitHub Actions updates.
- Release Please uses a protected short-lived App token, makes a draft release, enforces the
  one-shot `Release-As: 1.0.0` lineage, and synchronizes only the release PR's lockfile and generated
  command references.
- GoReleaser release mode requires the exact tag plus all five Apple values, signs/notarizes before
  fan-out, and continues to own the four binaries, archives, checksums, metadata, npm staging, and
  cask. Snapshot mode refuses signing values.
- `release.yml` implements exact tag/source/version guards; exclusive initial or 90-day retained
  candidate selection; one common and four native credential-free receipts; an `always()` receipt
  aggregate; separate GitHub/npm/Homebrew preflights; ordered GitHub then four-payload-plus-launcher
  npm then reviewed-tap-PR publication; exact-candidate recovery; and post-merge four-runner public
  installation/orientation aggregation.
- Exact digest-verified gitleaks and actionlint installers replace mutable installer paths. The
  canonical `just` tool is pinned to 1.50.0 locally and in CI.
- ADR 0010 and release documentation describe the unavoidable partial-publication state machine,
  credential isolation, public prerequisites, and exact recovery inputs.

Implementation review found two accepted-contract seams outside the original 34-path test-first
manifest. `.github/workflows/commitlint.yml` still had two floating action tags despite the
all-workflow full-SHA rule, and `scripts/check-p17-package.ts` required the obsolete embedded
package-script gate despite `just check` becoming the sole recipe authority. The ownership manifest
was amended to 36 paths, both assumptions were corrected, and executable tests now audit every
workflow `uses:` reference.

Pure implementation checks are green:

```text
bun test scripts/release-gates.test.ts \
  tests/ergonomics/phase/EWP-P6-TS06.test.ts \
  tests/ergonomics/workflows/EWP-WF01.test.ts --timeout 60000 --max-concurrency 1
20 passed; 0 failed; 209 assertions

bun run typecheck
exit 0

actionlint .github/workflows/*.yml
exit 0

bun scripts/p17-catalog.ts --check
426 entities; 419 required; 7 deferred; 244 validation obligations; 45 groups; deterministic

bun scripts/check-p17-package.ts --check
structurally valid; G6-04 remains active

bun scripts/p17-plan-structure.ts --all
valid

bun scripts/p17-documentation-drift.ts --all
27 documents valid

git diff --check
exit 0
```

A fresh ignored snapshot directory exercised the actual pinned Bun/GoReleaser/npm adapter. It
compiled all four targets and produced four validated archives, five validated npm tarballs,
`SHA256SUMS`, metadata, and the cask with raw/archive/npm byte identity for every target. The first
attempt exhausted the workspace volume because two stale P17 `/tmp` spikes consumed 768 MB; after
removing only those generated spike directories, the same snapshot completed. No source or retained
candidate was deleted.

The canonical `just check` passed lint, architectural boundaries, typecheck, generated-reference
drift, actionlint, and the P17 PR-openable gate, then correctly refused to start the terminal suite
because the implementation tree was not clean. The implementation is checkpointed next; targeted
green then reruns both named validations and the complete 366-file terminal suite from that clean
immutable revision. No targeted-green claim is made by this section.

### Minimal implementation impacted characterization correction

The first clean 366-file terminal execution progressed through 307 files and then failed only
`EWP-P1-TS11` family 8. That signed characterization freezes the root package script key order; it
still expected the pre-G6-04 inventory and therefore rejected the intentional `test:terminal`
registration. The failure was not a timeout, skip, product regression, or Bun runtime crash.

G6-04 ownership and terminal regression coverage are amended to include
`tests/ergonomics/phase/EWP-P1-TS11.test.ts`. The catalog's immutable impacted set remains exactly
`EWP-P6-TS06` and `EWP-WF01`; this earlier-phase characterization is additional terminal coverage,
not a retroactive lifecycle expansion. Its expected inventory adds `test:terminal` at the exact
manifest position while retaining every other dependency, script, command, option, ADR, and
P3B-deferral assertion. The focused selector and then a new clean exact-once terminal run are
required before targeted or impacted green can advance.

### Minimal implementation native-receipt prerequisite correction

The second clean terminal execution passed the first 343 files, including the corrected
`EWP-P1-TS11`, and then failed only `EWP-P6-TS01` family 10. The G6-04 CI rewrite retained the
bounded macOS Homebrew receipt command but had accidentally dropped its G6-01-pinned Node/npm and
GoReleaser setup. The signed test correctly caught a real workflow regression: this was not a stale
test, artifact failure, Bun crash, timeout, or publication attempt. Families 1 through 9 passed,
including the full GoReleaser candidate, archive, byte-lineage, npm/Bun lifecycle, cask, leak, and
two-version assertions.

The native macOS receipt now restores exact Node 24, npm 12.0.1, and GoReleaser 2.17.1 installation
with the already accepted full-SHA actions. Linux arm64 remains a bounded host-native smoke lane,
the complete suite remains solely in the Linux-x64 `just check` job, and no publish, credential,
attestation, or tap-write authority is added. Family 10, actionlint, workflow validation, and a new
clean exact-once terminal run are required before lifecycle green can advance.

## Green execution receipt EWP-P6-TS06 at 6964b11

- Validation ID: `EWP-P6-TS06`
- Command: `bun test tests/ergonomics/phase/EWP-P6-TS06.test.ts --test-name-pattern EWP-P6-TS06`
- Exit status: `0`
- Result: 10 tests passed, 0 failed, with 146 assertions across canonical gate parity, Release
  Please identity, exact-tag refusal, candidate isolation/recovery, four native receipts, real-tool
  selector declarations, authority preflights, publication recovery, external-readiness refusal,
  and documentation/tool-pin coherence.
- Revision: `6964b111ccb7cb3aac6a4bd831cde757db8ed4be`

## Green execution receipt EWP-WF01 at 6964b11

- Validation ID: `EWP-WF01`
- Command: `bun test tests/ergonomics/workflows/EWP-WF01.test.ts --test-name-pattern EWP-WF01`
- Exit status: `0`
- Result: 4 tests passed, 0 failed, with 23 assertions across four-runner direct/npm/Bun identity,
  two-runner Homebrew cask identity, all-command orientation/lifecycle behavior, and fail-closed
  skip/version/SHA/completion/uninstall validation.
- Revision: `6964b111ccb7cb3aac6a4bd831cde757db8ed4be`

## Targeted green at 6964b11

Both required-now selectors passed on the immutable implementation revision: 14 tests, 0 failures,
and 169 assertions. They validate the complete executable workflow and public-audit contracts while
truthfully retaining refusal when the repository's external environments, credentials, public
visibility, immutable-release setting, npm trusts, Apple signing identity, tap CI, or public assets
are absent. No tag, release, package, cask PR, visibility change, or external credential mutation
was performed.

## Impacted green and terminal repository gate at 6964b11

The canonical command ran once from a clean index and worktree:

```text
PATH=/tmp/skillsmith-just-1.50.0/bin:$PATH just check
```

Biome checked 879 files, architectural-boundary ESLint passed, TypeScript no-emit passed, generated
command references had no drift, actionlint passed, and the P17 PR-openable checker accepted 426
entities, 419 required entities, 244 validation obligations, and 45 groups. The terminal runner
rediscovered the stable 366-file manifest with SHA-256
`58466748415ce22652f743e2ab08755ca3ee80849be6ab55cfe7c66e10961095` and emitted:

```text
SERIAL_TEST_FILE_RECEIPT {"assertions":111022,"discovered":366,"duplicates":0,
"executed":366,"passed":366,"skipped":28,"tests":3341,"bun":"1.3.14",
"manifestSha256":"58466748415ce22652f743e2ab08755ca3ee80849be6ab55cfe7c66e10961095"}
```

Every discovered test file ran exactly once, serially, in a fresh Bun process. All 366 files passed,
with zero duplicates and zero failures; the 28 skips are the frozen opt-in live external-tool and
network cases. `EWP-P1-TS11`, all ten `EWP-P6-TS01` families, `EWP-P6-TS06`, and `EWP-WF01` passed
inside this same clean run. The two prior clean attempts remain recorded above because they exposed
and led to correction of one signed package-inventory mismatch and one real native-receipt
prerequisite regression. Neither was a Bun crash, timeout, retry, or test-order failure.

Targeted green and impacted green are accepted. This is repository-contained validation, not a
claim that the externally blocked public release has run. Refactor review, independent adversarial
review, external readiness/public receipts, traceability closure, and sign-off remain pending.

## Refactor audit NO-GO and approved correction red

The first refactor audit of immutable checkpoint `bac378f` returned NO-GO. The candidate builder,
compile-once fan-out, platform packages, cask architecture, serial terminal runner, and
credential-separated publication graph remain accepted. The blocker is narrower: several workflow
receipts asserted success without measuring all facts named by the accepted plan.

The audit found that lane and preflight receipts omitted the candidate metadata digest and version;
the recorded metadata digest was not compared with the actual file; the external-readiness
validator was never invoked by the workflow; manual recovery could check out the named tag while
running with a different dispatch ref/SHA; authority preflights were partly variable-based rather
than state-observing; publisher recovery did not fail closed on every occupied mismatch; and public
receipts did not actually prove checksum/attestation validity, zsh syntax, exact command ordering,
no-tool diagnosis, isolated writes, uninstall cleanup, or native-byte identity across channels.

The approved amendment adds only focused assertions inside the existing three owned G6-04 tests.
Before any product/workflow correction, the unchanged implementation produced these intended reds:

```text
bun test scripts/release-gates.test.ts
5 passed; 2 failed; 44 assertions

bun test tests/ergonomics/phase/EWP-P6-TS06.test.ts --test-name-pattern EWP-P6-TS06
6 passed; 4 failed; 145 assertions

bun test tests/ergonomics/workflows/EWP-WF01.test.ts --test-name-pattern EWP-WF01
2 passed; 2 failed; 20 assertions
```

The low-level failures prove that metadata/version drift is accepted and the publication-ready
validator is absent. TS06 families 3, 5, 7, and 8 fail on the unbound dispatch ref, incomplete
candidate identity, non-executable authority aggregate, and incomplete publisher recovery. WF01
fails when attestation/checksum/no-tool claims are false and when channel-native digests differ.
The other families remain green, so this is not a compile error, timeout, skip, network outage, or
unrelated product regression.

The earlier targeted and terminal receipts remain historical evidence for `6964b11`, but they do
not validate the amended contract. `EWP-P6-T07` returns to red, `D-016` to mapped,
`EWP-P6-TS06` and `EWP-WF01` to failing, and impacted-green/refactor to pending. A corrected focused
green, exact terminal rerun at the next major milestone, fresh adversarial review, and genuine
external public receipts remain required. No external publication or visibility mutation is
authorized by this amendment.

### Refactor correction red execution receipt EWP-P6-TS06

- Validation ID: `EWP-P6-TS06`
- Command: `bun test tests/ergonomics/phase/EWP-P6-TS06.test.ts --test-name-pattern EWP-P6-TS06`
- Exit status: `1`
- Result: 6 tests passed and 4 failed with 145 assertions; all four failures are intended correction reds.
- Revision: `working-tree`

### Refactor correction red execution receipt EWP-WF01

- Validation ID: `EWP-WF01`
- Command: `bun test tests/ergonomics/workflows/EWP-WF01.test.ts --test-name-pattern EWP-WF01`
- Exit status: `1`
- Result: 2 tests passed and 2 failed with 20 assertions; both failures are intended correction reds.
- Revision: `working-tree`

## Official-interface verification and npm bootstrap blocker

The correction implementation was checked against the current official interfaces rather than
assuming static workflow success implied a deployable release. GitHub's OIDC reference confirms
the environment subject, repository visibility, ref, SHA, and workflow-ref claims used by the npm
preflight. GitHub CLI's attestation manual recommends additionally enforcing signer workflow,
source digest, and source ref; the workflow now applies those constraints in preflight, publisher,
and all four public lanes.

That verification exposed one new blocking fact. Read-only registry probes returned `E404` for all
five exact names:

- `@smorinlabs/skillsmith`;
- `@smorinlabs/skillsmith-linux-x64`;
- `@smorinlabs/skillsmith-linux-arm64`;
- `@smorinlabs/skillsmith-darwin-x64`; and
- `@smorinlabs/skillsmith-darwin-arm64`.

Current official [`npm trust`](https://docs.npmjs.com/cli/v11/commands/npm-trust/) documentation
requires the package to exist before configuring trusted publishing. Current
[staged-publishing documentation](https://docs.npmjs.com/staged-publishing/) explicitly says a
brand-new package cannot be staged. The prior `npm trust --dry-run` result described a request the
CLI would form; it did not establish registry acceptance for a nonexistent package. Consequently,
the five-trust precondition is impossible for this first release and the current workflow must not
be signed off as deployable.

The recommended correction is a one-time, protected, exact-SHA bootstrap of the actual 1.0.0
tarballs with a shortest-lived scoped GAT, GitHub provenance, payload-first order, and post-publish
integrity rereads. It must deliberately stop before Homebrew/WF01 until a maintainer configures all
five OIDC trusts, revokes/removes the token, records exact-SHA closure receipts, and reruns from the
retained candidate. This avoids junk placeholder versions and preserves provenance, but it is a
material credential amendment and awaits explicit user approval. No npm token, package, trust,
stage, visibility, tag, release, or other external state was created or changed during this
read-only verification.

## Refactor correction green and exact terminal checkpoint at 5176412

The receipt-binding implementation closed every focused red without weakening or skipping a
contract. `scripts/release-gates.test.ts`, `EWP-P6-TS06`, and `EWP-WF01` passed together with 21
tests, 0 failures, and 252 assertions. The corrected workflow now binds the tag ref and candidate
artifact/metadata identity, invokes executable external readiness, observes authority state,
fails closed on occupied publication mismatches, and measures public checksum, attestation,
completion, no-tool, cleanup, and cross-channel native-byte truth.

The canonical terminal command then ran once from clean immutable revision
`51764125ba5dce5ca39dcfc372b0ed8be7fe441d`:

```text
PATH=/tmp/skillsmith-just-1.50.0/bin:$PATH just check
```

Biome, architectural-boundary ESLint, TypeScript no-emit, generated-reference drift, actionlint,
and the P17 PR-openable gate passed before the terminal suite. The rediscovered stable manifest
ran each tracked test file exactly once, serially, in its own fresh Bun process and emitted:

```text
SERIAL_TEST_FILE_RECEIPT {"assertions":111063,"discovered":366,"duplicates":0,
"executed":366,"passed":366,"skipped":28,"tests":3342,"bun":"1.3.14",
"manifestSha256":"58466748415ce22652f743e2ab08755ca3ee80849be6ab55cfe7c66e10961095"}
```

There were zero failed or duplicate files and no retry. The 28 skips are the frozen opt-in live
external-tool/network cases. All Phase 6 selectors, including the amended `EWP-P6-TS06` and
`EWP-WF01`, passed inside that same exact-once run. Full-history gitleaks found no leaks, `bun
audit --audit-level=high` found no vulnerabilities, and the security ESLint gate passed.

This is a repository-contained checkpoint, not G6-04 sign-off. The newly proven npm first-release
bootstrap contradiction remains open, so lifecycle/catalog state intentionally stays red/pending
until the recommended credential amendment is explicitly approved, implemented, independently
reviewed, and then closed by genuine external/public receipts. No external publication,
visibility, package, trust, tag, release, cask PR, or credential mutation occurred.

## Read-only resume characterization on 2026-08-07

Fresh local and remote inspection confirms that the checkpoint remains intact but none of the
external release prerequisites has silently closed:

- local `agent/p17-execution`, `origin/agent/p17-execution`, and draft PR #44 all point to
  `20339854449554af189c1d5e6a5e526f55b8fe8c`; the worktree is clean and the PR remains mergeable;
- `origin/main` has advanced by two commits to
  `48b8fab20a58d26d551e7038bc3f99ad978dc1c8`, so later merge preparation must integrate and
  revalidate that exact default-branch state rather than assuming the July base is current;
- every CI and commitlint job at the checkpoint SHA still failed before executing a step with
  GitHub's exact account-payment/spending-limit annotation; no four-runner receipt exists;
- `smorinlabs/skillsmith` remains private, only the unrelated `copilot` environment exists, and the
  repository has zero Actions secrets and zero Actions variables;
- the latest release remains `v0.7.0`; there is no `v1.0.0` tag or release;
- read-only npm registry probes still return `E404` for the launcher and all four platform payload
  package names, and the local npm client remains unauthenticated;
- `smorinlabs/homebrew-tap` remains public on `main`, but it has no `Casks/skillsmith.rb`, no
  `release/skillsmith-v1.0.0` branch, and no Skillsmith cask PR.

The current official [`npm trust`](https://docs.npmjs.com/cli/v11/commands/npm-trust/)
documentation still requires the package to exist and additionally says a bypass-2FA granular
token cannot configure trust. The current official
[staged-publishing documentation](https://docs.npmjs.com/staged-publishing/) still says a
brand-new package cannot be staged. Therefore no newly available npm path removes the first-release
bootstrap contradiction. The recommended amendment remains bounded to token-authenticated
publication of the exact validated 1.0.0 tarballs, followed by maintainer-authenticated creation of
the five OIDC trusts and immediate token revocation/removal. It remains unimplemented pending
explicit product/credential-amendment approval. This inspection changed no repository visibility,
environment, secret, variable, tag, release, package, trust, tap branch, cask, or PR state.

## Newly disclosed dependency advisories and focused correction on 2026-08-07

The resumed pre-push security gate found three high-severity advisories published after the prior
July terminal checkpoint. This was a genuine new red, not a regression in the already validated
release implementation:

- `fast-uri` 3.1.4 is affected by
  [`GHSA-7p8r-x3mc-p8w7`](https://github.com/advisories/GHSA-7p8r-x3mc-p8w7); the advisory names
  3.1.5 as the patched release on the already selected 3.x line;
- `brace-expansion` 5.0.8 is affected by
  [`GHSA-rgw5-rvv9-x895`](https://github.com/advisories/GHSA-rgw5-rvv9-x895); the advisory names
  5.0.9 as the patched release and explains that 5.0.8's earlier bound was incomplete; and
- `js-yaml` 3.15.0 is affected by
  [`GHSA-5p4m-2wfm-xmqj`](https://github.com/advisories/GHSA-5p4m-2wfm-xmqj); 3.15.1 is the patched
  release on the transitive consumer's required 3.x line.

The correction raises only the three existing workspace override floors to those first patched
same-major releases and regenerates `bun.lock`. It does not widen the transitive consumers, add a
dependency, change an application API, or alter any release/publication authority. The regenerated
lock resolves exactly `fast-uri` 3.1.5, `brace-expansion` 5.0.9, and `js-yaml` 3.15.1.

Focused verification is green:

```text
bun audit --audit-level=high
PASS; No vulnerabilities found

bun install --frozen-lockfile --ignore-scripts
PASS; 337 installs across 374 packages, no changes

bun test tests/ergonomics/phase/EWP-P1-TS11.test.ts
PASS; 10 tests, 487 assertions

bun test tests/ergonomics/phase/EWP-P6-TS01.test.ts
PASS; 10 tests, 301 assertions

bun run test:smoke
PASS; 5 tests, 3,737 assertions

bun run typecheck
bun run lint
bun run lint:boundaries
bunx eslint --config eslint.security.config.js \
  --suppressions-location eslint-security-suppressions.json \
  --max-warnings 0 'packages/*/src/**/*.ts'
PASS
```

Because the dependency graph changed after the previous immutable terminal receipt, this focused
green was intentionally insufficient for checkpoint publication. The correction was committed as
immutable revision `7f34c331e4da659c686cf9f2f03994e4af822043`, and the required clean terminal gate
then ran once at that revision:

```text
PATH=/tmp/skillsmith-just-1.50.0:$PATH just check

SERIAL_TEST_FILE_RECEIPT {"assertions":111063,"discovered":366,"duplicates":0,
"executed":366,"passed":366,"skipped":28,"tests":3342,"bun":"1.3.14",
"manifestSha256":"58466748415ce22652f743e2ab08755ca3ee80849be6ab55cfe7c66e10961095"}
```

Biome, boundary ESLint, TypeScript no-emit, generated-reference drift, actionlint, and the P17
PR-openable check passed before the terminal runner. All 366 rediscovered files ran exactly once,
serially, in fresh Bun processes; 366 passed with zero failures and zero duplicates. The 28 skips
remain the frozen opt-in live external-tool/network cases. A full-history gitleaks scan of the two
unpushed commits found no leaks, the high-severity Bun audit found no vulnerabilities, and security
ESLint passed. The two checkpoint commits were then pushed to `origin/agent/p17-execution`.

This restores a green repository-contained terminal checkpoint after the new advisories. It does
not advance G6-04 beyond 5/10 or claim external release completion. GitHub runner billing,
release-environment/authority setup, public four-runner receipts, and the npm first-release
bootstrap amendment remain open; the npm amendment still requires explicit user approval.
