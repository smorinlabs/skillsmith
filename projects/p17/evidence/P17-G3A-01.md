# P17-G3A-01 evidence

## Mapping and prerequisites

- **Primary entities:** exactly 13: `COMMAND:status`, `D-008`, `P1-02`, `EWP-P3A-T02`,
  `EWP-P3A-T03`, `EWP-CMD-STATUS-TS01..TS06`, `EWP-P3A-TS02`, and `EWP-P3A-TS04`. All remain
  pristine `planned` at mapping; no entity target or evidence is advanced.
- **Required now and impacted:** the same exact eight validations:
  `EWP-CMD-STATUS-TS01..TS06`, `EWP-P3A-TS02`, and `EWP-P3A-TS04`. Downstream coverage is empty.
  Their immutable planned targets are the six selectors in
  `packages/cli/tests/contracts/status.test.ts`, plus
  `tests/ergonomics/phase/EWP-P3A-TS02.test.ts` and
  `tests/ergonomics/phase/EWP-P3A-TS04.test.ts`; none of those three files exists yet.
- **Dependency:** sole direct dependency P17-G2-05 is signed off. Phase 2 is approved at `2ffbbcc`,
  Phase 3 entry is passed at `b419925`, and the signed documentation-status amendment required by
  entry is independently accepted at `adbea31`.
- **Secondary trace:** signed `EWP-CF-022`, `EWP-CF-031`, and `EWP-CF-040` supply shared project
  context, bounded read selection, and portable-artifact ownership. Future `EWP-CF-028` remains
  owned by G3B-03, and future `EWP-OPT-TS08` remains owned by G5-05; neither advances here.
- **Outcome boundary:** G3A-01 owns the immutable non-mutating correlated state product and the
  dedicated `status [skill...]` surface over manifest, lock, ledger, and live observations. It must
  distinguish partial artifact products, live placement classes, drift, verify state, journals,
  ledger-v1 migration-pending state, filters, cross-scope shadowing, project identity, `--check`,
  and versioned JSON without writing any artifact.
- **Exclusions:** G3B-03 retains ledger migration, writer, journal recovery, resume, and abort.
  G3A-02 retains the general agents/list/commands inventory and fleet-wide duplicate presentation.
  G5-05 retains final option closure. No mutation, recovery, future-command, or general inventory
  authority is pulled into this group.
- **Current gap:** no G3A-01 plan or evidence existed before mapping. Group ownership, test commands,
  implementers, production APIs, state-product algebra, error precedence, renderer/codec boundary,
  and compatibility replay set remain Ready decisions. Likely integration paths are historically
  owned by signed G1/G2 groups, so Ready must enumerate the exact sequential reuse and prove no
  concurrent ownership conflict.
- Read-only orientation agent `/root/phase3_entry_orientation` independently reproduced all 13
  primaries, the 8/8 validation partition, zero downstream coverage, immutable planned targets,
  direct dependency, secondary ownership, missing test files, and the read-only/later-work boundary
  at 0.99 confidence.
- **Result:** prerequisites, entity/validation ownership, immutable targets, current gaps, and
  exclusions are mapped. No Ready, test-first, implementation, entity, or validation gate is
  implied.

## Ready plan

- **Plan:** `projects/p17/plans/P17-G3A-01.md`.
- **Accepted SHA-256:** originally
  `44bd845ce1978dd0a0520f9571b89185c4acd9c219531cf3f154553b6346f5a0`; amended only for the
  byte-exact status@1 golden formatter ownership and exact V1 export-fixture compatibility below.
  The current accepted SHA-256 is
  `e6bf22db8ad815b4513df3affe083d27de5cd970202be87fb5e7a23725e50c83`.
- Three independent read-only audits covered product/error semantics, architecture/ownership, and
  exact test design. They reproduced 13/13 primaries, the exact 8/8 required-now/impacted selector
  set, zero downstream coverage, the signed dependency/reuse surface, and the future-work boundary.
- The accepted plan freezes the previously underspecified readable-context precedence, name-level
  partial-product algebra, live classification versus ledger management, exact drift comparisons,
  journal precedence/remediation, target/filter behavior, error precedence, focused read-only port,
  complete status@1 wire shape, deterministic order, test-first red, fixture sizes, ownership, and
  final-gate policy.
- Product review passed at 0.998 confidence with no residual product or error-semantics findings.
  Architecture review passed at 0.997 confidence with no residual ownership, journal-only-row,
  fact-algebra, retention, legacy-compatibility, codec, or port-boundary findings. Final
  traceability review passed at 0.998 confidence and independently verified the 13 primaries,
  exact eight commands/selectors, 26 substantive test families, the then-current 49 unique owned
  paths, and closed
  later-work exclusions.
- The eight catalog commands are the six separately filtered `EWP-CMD-STATUS-TS01..TS06`
  selectors in `packages/cli/tests/contracts/status.test.ts`, plus the separately filtered
  `EWP-P3A-TS02` and `EWP-P3A-TS04` phase selectors. Ownership is exactly 6 T02 production paths,
  13 T03 production paths, 12 test-first paths, and 19 integration/compatibility/tooling paths.
- `git diff --check`, `bun run check:p17`, `bun scripts/p17-catalog.ts --check`, and
  `bun scripts/p17-documentation-drift.ts --all` passed before advancement. Ready is a read-only
  governance gate, so no smoke or full suite is required here.
- **Result:** P17-G3A-01 advances to `ready`; only `EWP-P3A-T02` and `EWP-P3A-T03` advance with it.
  The three coverage entities and all eight validation entities remain pristine `planned`. No
  test-first or implementation advancement is claimed.

### Ready ownership amendment — byte-exact status@1 golden

- The test-first pre-commit gate found that generic Biome JSON formatting would compact arrays in
  `tests/ergonomics/fixtures/p3a-ts04/status-v1.golden.json`. That file intentionally pins the
  contract's ordinary two-space `JSON.stringify` bytes plus one terminal LF, so generic formatting
  would invalidate the wire golden rather than improve source style.
- `biome.json` is therefore an eighteenth integration/tooling path and the Ready union is 49 unique
  paths. Its existing byte-exact-golden override gains only
  `tests/ergonomics/fixtures/p3a-ts04/*.golden.json`; parsing and linting remain enabled, and the
  signed P2-TS08 exclusion is unchanged. The 6 T02, 13 T03, 12 test-first, eight command, 26-family,
  and 8/8 validation partitions are unchanged.
- The narrow amendment is accepted at the current plan digest above. Configured Biome, diff,
  catalog, documentation-drift, and P17 static checks must pass before the formatter prerequisite is
  committed separately from the immutable test-first boundary.

### Integration ownership amendment — exact V1 export fixture

- Focused P2-TS08 compatibility replay found that
  `tests/ergonomics/fixtures/p2-ts08/public-types.ts` closes `keyof` the V1 runtime exports. Adding
  the planned `statusV1Codec` and `toStatusV1Dto` exports therefore requires the fixture to advance
  with its owning P2-TS08 selector; leaving it unchanged makes the embedded public-type compile gate
  fail for the intended new API.
- That fixture becomes the nineteenth sequential integration/compatibility path and the group union
  becomes 50 unique paths. It may add only status codec/mapper/type closure. The signed six
  persisted-artifact identities, goldens, migrations, repository readers, and artifact counts remain
  unchanged. T02/T03/test-first ownership, all eight commands, 26 families, and the 8/8 validation
  partition remain unchanged.
- The narrow amendment is accepted at the current plan digest above and must receive independent
  ownership/trace review before the fixture edit is integrated.

## Test-first evidence

- **Boundary commit:** `1b9dceddc68ccb0663984a169d82d98709b80a54` contains exactly the 12
  test-first selector, fixture, golden, and supporting unit-test paths frozen by Ready. It contains
  no production, documentation, catalog, evidence, or unrelated files. The byte-exact-golden
  formatter prerequisite is the separate reviewed commit `a2ff2e4`.
- The six command selectors contain exactly families 1–23, P3A-TS02 contains families 24–25, and
  P3A-TS04 contains family 26. Each immutable selector file has exactly one top-level `beforeAll`
  hook; literal hash/count/shape guards execute before dynamic authority loading. There are no
  skipped, todo, conditional, or placeholder selectors.
- Every exact catalog command exits 1 at the intended `missing G3A-01 status authority` boundary.
  Each command selector records 7 guard assertions, P3A-TS02 records 8, and P3A-TS04 records 15.
  The unfiltered three-file run is exactly 0 pass, 3 fail, and 30 assertions, proving one failure per
  selector file rather than six duplicate file-hook failures.
- Supporting human/JSON golden contracts pass 13 tests and 45 assertions. The supporting focused
  core-reader contract is independently honest red with exactly 3 failures at the same missing
  authority. Configured `bun run lint` passes 520 files; focused Biome and `git diff --check` pass.
- No smoke or full suite was run at this boundary: production is unchanged, the accepted plan starts
  smoke after all eight selectors close green, and the full suite remains reserved for final
  G3A-01 sign-off.
- **Result:** P17-G3A-01 passes `test-first` and becomes `active`; T02/T03 become `red`; all eight
  validation entities become `failing` with executable targets bound to the receipts below.
  `COMMAND:status`, `D-008`, and `P1-02` remain pristine `planned` coverage entities. No minimal
  implementation or green gate is claimed.

### Red execution receipt EWP-CMD-STATUS-TS01

- Validation ID: `EWP-CMD-STATUS-TS01`
- Command: `bun test packages/cli/tests/contracts/status.test.ts --test-name-pattern EWP-CMD-STATUS-TS01`
- Exit status: `1`
- Result: 0 passed, 1 failed, 7 assertions; literal guards passed before the exact missing G3A-01
  status authority failure.
- Revision: `1b9dceddc68ccb0663984a169d82d98709b80a54`

### Red execution receipt EWP-CMD-STATUS-TS02

- Validation ID: `EWP-CMD-STATUS-TS02`
- Command: `bun test packages/cli/tests/contracts/status.test.ts --test-name-pattern EWP-CMD-STATUS-TS02`
- Exit status: `1`
- Result: 0 passed, 1 failed, 7 assertions; literal guards passed before the exact missing G3A-01
  status authority failure.
- Revision: `1b9dceddc68ccb0663984a169d82d98709b80a54`

### Red execution receipt EWP-CMD-STATUS-TS03

- Validation ID: `EWP-CMD-STATUS-TS03`
- Command: `bun test packages/cli/tests/contracts/status.test.ts --test-name-pattern EWP-CMD-STATUS-TS03`
- Exit status: `1`
- Result: 0 passed, 1 failed, 7 assertions; literal guards passed before the exact missing G3A-01
  status authority failure.
- Revision: `1b9dceddc68ccb0663984a169d82d98709b80a54`

### Red execution receipt EWP-CMD-STATUS-TS04

- Validation ID: `EWP-CMD-STATUS-TS04`
- Command: `bun test packages/cli/tests/contracts/status.test.ts --test-name-pattern EWP-CMD-STATUS-TS04`
- Exit status: `1`
- Result: 0 passed, 1 failed, 7 assertions; literal guards passed before the exact missing G3A-01
  status authority failure.
- Revision: `1b9dceddc68ccb0663984a169d82d98709b80a54`

### Red execution receipt EWP-CMD-STATUS-TS05

- Validation ID: `EWP-CMD-STATUS-TS05`
- Command: `bun test packages/cli/tests/contracts/status.test.ts --test-name-pattern EWP-CMD-STATUS-TS05`
- Exit status: `1`
- Result: 0 passed, 1 failed, 7 assertions; literal guards passed before the exact missing G3A-01
  status authority failure.
- Revision: `1b9dceddc68ccb0663984a169d82d98709b80a54`

### Red execution receipt EWP-CMD-STATUS-TS06

- Validation ID: `EWP-CMD-STATUS-TS06`
- Command: `bun test packages/cli/tests/contracts/status.test.ts --test-name-pattern EWP-CMD-STATUS-TS06`
- Exit status: `1`
- Result: 0 passed, 1 failed, 7 assertions; literal guards passed before the exact missing G3A-01
  status authority failure.
- Revision: `1b9dceddc68ccb0663984a169d82d98709b80a54`

### Red execution receipt EWP-P3A-TS02

- Validation ID: `EWP-P3A-TS02`
- Command: `bun test tests/ergonomics/phase/EWP-P3A-TS02.test.ts --test-name-pattern EWP-P3A-TS02`
- Exit status: `1`
- Result: 0 passed, 1 failed, 8 assertions; the exact eight-row ledger and signed-golden guards
  passed before the exact missing G3A-01 status authority failure.
- Revision: `1b9dceddc68ccb0663984a169d82d98709b80a54`

### Red execution receipt EWP-P3A-TS04

- Validation ID: `EWP-P3A-TS04`
- Command: `bun test tests/ergonomics/phase/EWP-P3A-TS04.test.ts --test-name-pattern EWP-P3A-TS04`
- Exit status: `1`
- Result: 0 passed, 1 failed, 15 assertions; literal fixture hashes and the 14-name/15-placement
  mixed-fleet guards passed before the exact missing G3A-01 status authority failure.
- Revision: `1b9dceddc68ccb0663984a169d82d98709b80a54`

## Minimal implementation

- **Production commit:** `35ab9519dd484cea18605a7d4f88615e6a6b8a8a` implements the 6-path T02
  boundary and 13-path T03 boundary, with the two test-only compile corrections. The existing
  `packages/core/src/artifacts/index.ts` wildcard already exposes discovery, so it required no byte
  change. No integration, documentation, governance, mutation, or recovery path is in this commit.
- T02 adds total readable artifact-context selection, immutable status types, a deterministic pure
  join, and a focused reader. Final review fixes bound journals to selected names/paths, unify
  logical/legacy pending/committed precedence, perform real logical and legacy retention probes,
  preserve structural/revision/content eligibility, and map zero-comparable relationships to
  `none`. The proposed `unselected-placement` finding was retracted: exact-mappable journals join or
  create a row, multi/null identities map to `multi-resource`, and exact-path mismatches are omitted.
- T03 adds the exact ten-option declarative command, singular-scope relation, one application-owned
  artifact-pair resolution, focused read-port projection, recursive redaction plus strict codec
  reparse, explicit status@1 mapper/schema, wire registration, human/JSON renderers, and public
  declarations. A real repeated `--scope` invocation refuses as usage exit 2. Both renderers consume
  the same redacted and reparsed DTO.
- **Compatibility commit:** `cbc5de768159a273ed279ea24925c5df6dbfc1ad` updates the 18 remaining
  compatibility/documentation paths. The P1 status golden is byte-identical to the P3A golden at
  40,893 bytes and SHA-256
  `0a4cbcb5f2000948e19692153361854779167d9f368c497cf95d5909736df26b`; the live P1 inventory is
  20 commands, 152 options, and SHA-256
  `5dc03fcb3a842873b52415ce3ddd99835ee96243110ffd8d22e25d3a1ead1b24`.
- The reviewed 50th path adds only `StatusV1Dto`, `statusV1Codec`, and `toStatusV1Dto` closure to the
  P2-TS08 public fixture. Artifact inventory remains six and signed golden bytes remain 24,594.
  No persisted artifact, migration, recovery, writer, lock, process, network, clock, or ID contract
  changed.
- **Result:** the minimal implementation gate passes. T02/T03 remain active until focused and
  impacted validation are recorded below.

## Targeted and impacted green

- All eight exact catalog commands pass sequentially, avoiding only the known nested-TypeScript
  timeout caused by high parallelism. The exact totals are 26 passed families, 0 failed, and 350
  assertions: TS01 4/49, TS02 3/46, TS03 3/30, TS04 4/44, TS05 5/57, TS06 4/30,
  P3A-TS02 2/77, and P3A-TS04 1/17.
- Focused output/core/application/discovery/public tests pass 46 tests and 522 assertions. Option
  compatibility passes 24/400. P1-TS05/10/11 pass 27/1,965. P2-TS02 passes 6/371 and P2-TS08
  passes 7/1,624.
- Additional planned impact replay passes: P0A-TS02/03/04/08 14/59;
  P1-TS01/02/03/06/07/08 51/682; P2-TS03 3/144; P2-TS07 12/582; and WF14 2/12.
- `bun run typecheck`, `bun run lint` (527 files), `bun run build`, `bun run check:p17`, catalog
  validation, documentation drift, and `git diff --check` pass. `bun run test:smoke` passes the
  bounded P2-TS04 lane: 5 tests, 3,735 assertions, zero failures.
- No five-minute full suite was run here. The accepted policy reserves it for final G3A-01 sign-off
  after refactor and independent adversarial/trace review.
- **Result:** minimal implementation, targeted green, and impacted green pass. T02/T03 advance to
  `green`; all eight validation entities advance to `passing`; `COMMAND:status`, `D-008`, and
  `P1-02` advance to `validated`. No refactor or review gate is claimed.

### Green execution receipt EWP-CMD-STATUS-TS01

- Validation ID: `EWP-CMD-STATUS-TS01`
- Command: `bun test packages/cli/tests/contracts/status.test.ts --test-name-pattern EWP-CMD-STATUS-TS01`
- Exit status: `0`
- Result: 4 passed, 0 failed, 49 assertions.
- Revision: `cbc5de768159a273ed279ea24925c5df6dbfc1ad`

### Green execution receipt EWP-CMD-STATUS-TS02

- Validation ID: `EWP-CMD-STATUS-TS02`
- Command: `bun test packages/cli/tests/contracts/status.test.ts --test-name-pattern EWP-CMD-STATUS-TS02`
- Exit status: `0`
- Result: 3 passed, 0 failed, 46 assertions.
- Revision: `cbc5de768159a273ed279ea24925c5df6dbfc1ad`

### Green execution receipt EWP-CMD-STATUS-TS03

- Validation ID: `EWP-CMD-STATUS-TS03`
- Command: `bun test packages/cli/tests/contracts/status.test.ts --test-name-pattern EWP-CMD-STATUS-TS03`
- Exit status: `0`
- Result: 3 passed, 0 failed, 30 assertions.
- Revision: `cbc5de768159a273ed279ea24925c5df6dbfc1ad`

### Green execution receipt EWP-CMD-STATUS-TS04

- Validation ID: `EWP-CMD-STATUS-TS04`
- Command: `bun test packages/cli/tests/contracts/status.test.ts --test-name-pattern EWP-CMD-STATUS-TS04`
- Exit status: `0`
- Result: 4 passed, 0 failed, 44 assertions.
- Revision: `cbc5de768159a273ed279ea24925c5df6dbfc1ad`

### Green execution receipt EWP-CMD-STATUS-TS05

- Validation ID: `EWP-CMD-STATUS-TS05`
- Command: `bun test packages/cli/tests/contracts/status.test.ts --test-name-pattern EWP-CMD-STATUS-TS05`
- Exit status: `0`
- Result: 5 passed, 0 failed, 57 assertions.
- Revision: `cbc5de768159a273ed279ea24925c5df6dbfc1ad`

### Green execution receipt EWP-CMD-STATUS-TS06

- Validation ID: `EWP-CMD-STATUS-TS06`
- Command: `bun test packages/cli/tests/contracts/status.test.ts --test-name-pattern EWP-CMD-STATUS-TS06`
- Exit status: `0`
- Result: 4 passed, 0 failed, 30 assertions.
- Revision: `cbc5de768159a273ed279ea24925c5df6dbfc1ad`

### Green execution receipt EWP-P3A-TS02

- Validation ID: `EWP-P3A-TS02`
- Command: `bun test tests/ergonomics/phase/EWP-P3A-TS02.test.ts --test-name-pattern EWP-P3A-TS02`
- Exit status: `0`
- Result: 2 passed, 0 failed, 77 assertions.
- Revision: `cbc5de768159a273ed279ea24925c5df6dbfc1ad`

### Green execution receipt EWP-P3A-TS04

- Validation ID: `EWP-P3A-TS04`
- Command: `bun test tests/ergonomics/phase/EWP-P3A-TS04.test.ts --test-name-pattern EWP-P3A-TS04`
- Exit status: `0`
- Result: 1 passed, 0 failed, 17 assertions.
- Revision: `cbc5de768159a273ed279ea24925c5df6dbfc1ad`
