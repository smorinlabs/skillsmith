# P17-G6-02B evidence

## Mapping and prerequisites

Mapping began from clean signed G6-02A revision
`eb90ee4f5f167292e3772bf30ef50d82052b3221` on 2026-07-26. The starting catalog SHA-256 was
`cad01cce738f291b7bd9f352e53b391331ade92c9de5ed284417189d5705f737`.

- Phase 6 is active and P17-G6-02A is signed off at all ten lifecycle gates.
- G6-02B starts pristine and planned: no owned files, commands, implementers, reviewer, evidence,
  executable targets, or passed gates.
- Primary ownership is exactly 11 entities: `COMMAND:completion`, EWP-CF-014,
  EWP-CMD-COMPLETION-TS01..06, EWP-P6-T03, EWP-P6-TS02, and P2-04.
- Required-now is exactly seven validations: EWP-CMD-COMPLETION-TS01..06 and EWP-P6-TS02.
- Downstream coverage is exactly EWP-WF01, still planned under G6-04.
- The mapped plan proposes 29 unique owned paths. Five newly proposed paths are comment-only
  placeholders and own no executable selector or runtime behavior.

## Existing implementation and gap audit

The existing completion implementation is a useful six-file static baseline but does not meet the
target contract. It walks the live Commander tree only far enough for one-level renderers. Current
scripts omit aliases, nested config actions, inherited flags after subcommands, local providers,
and a tab-time callback. Enum values come from broad Commander parser choices instead of the
command-specific `allowedValues` authority.

The live normalized registry has 23 public top-level commands, five adjacent aliases, four nested
config actions, and exact local/global option metadata. Its capability-aware distinctions are
material: install scope allows only `user|project` even though the parser recognizes
`managed|project|system|user` for capability diagnostics. The G6-02B adapter must consume the
narrow allowed set.

## Dependency and package verification

The approved Phase-6 amendment pins `@bomb.sh/tab` 0.0.21. Verification on the mapping date found:

- npm `latest` remains 0.0.21;
- the package declares no ordinary runtime dependency and exposes `.` plus `/commander` adapters;
- Commander is an optional peer with range `^13.1.0 || ^14.0.0 || ^15.0.0`;
- its public `RootCommand` can own a detached candidate graph and its setup output calls a
  `complete --` protocol; and
- its generic Commander adapter creates parser commands, includes PowerShell, reads broad
  `argChoices`, and does not add Skillsmith aliases, so direct attachment remains prohibited.

Primary package references:

- <https://github.com/bombshell-dev/tab/blob/main/package.json>
- <https://github.com/bombshell-dev/tab/blob/main/src/commander.ts>
- <https://github.com/bombshell-dev/tab/blob/main/README.md>

## Signed dependency and current-surface characterization

The unchanged baseline passed before mapping edits:

```text
bun test packages/cli/tests/completion --max-concurrency 1
24 pass; 0 fail; 73 assertions; 6 files; 2.81 seconds

bun test tests/ergonomics/phase/EWP-P1-TS07.test.ts \
  --test-name-pattern EWP-P1-TS07 --max-concurrency 1
17 pass; 0 fail; 406 assertions; 5.74 seconds
```

These runs characterize current script generation, descriptions/enum declarations, shared
application routing, extension compatibility, and 23-command declarative closure. They are not a
G6-02B targeted-green claim.

## Mapping result

The mapped candidate is one coherent completion replacement:

- exact `@bomb.sh/tab` 0.0.21 is isolated behind a fresh spec-derived adapter;
- the live parser remains exactly 23 commands while exact internal `complete --` requests are
  routed before Commander construction;
- canonical commands, aliases, nested config paths, inherited/local flags, and narrow allowed
  values derive from normalized `CommandSpec`;
- path/manifest/skill providers are local, read-only, codec-backed, symlink-safe, prefix-filtered,
  and explicitly bounded;
- Bash/zsh/Fish script bytes are deterministic and emit-only; and
- G6-01 packaging and G6-04 EWP-WF01 remain future-owned.

No Ready acceptance, executable selector, test-first receipt, dependency edit, implementation,
green result, review GO, traceability closure, or sign-off is claimed by this mapping record.

## Ready review at 85be9bb

Fresh independent reviewer `/root/g6_02b_ready_review` returned **NO-GO** with 0.995 confidence on
immutable commit `85be9bba9dfb2a6752abf74bf2cd82d71389eeeb`. The reviewer edited nothing and
left the worktree/index clean. Plan/catalog ownership remained exact at 29 paths, the primary set
remained 11 entities, the required-now set remained seven validations, downstream coverage
remained exactly EWP-WF01, and all mapping/dependency/package/baseline checks otherwise closed.

`P17-RV-P17-G6-02B-F01` is a critical Ready blocker. Exact `@bomb.sh/tab` 0.0.21 generates a Bash
callback that joins `words[@]` into one command string and passes it to `eval`. Both the coordinator
and reviewer independently sourced those package-generated bytes, supplied literal completion text
containing a harmless command substitution that wrote a temporary canary, invoked the callback,
and observed the canary. The same join/reparse boundary collapses already-tokenized arguments. An
independent attached-value probe also found that `--scope=pr` did not produce the returned
`project` completion because the generated Bash callback filters an unprefixed value against the
full option token.

The pinned zsh skeleton independently contains request-path `eval` and a string-built `_describe`
invocation. Fish already dispatches an argv vector directly. These package templates are therefore
untrusted adapter input, not acceptable public bytes merely because they are deterministic and
syntactically valid.

The user approved the bounded correction on 2026-07-26. The amended plan retains the exact package
and its detached `RootCommand` candidate engine while authorizing one narrow deterministic shell
hardening seam inside the existing 29-path boundary. Exact pinned Bash/zsh anchors are transformed
to direct argv invocation, Bash attached-value completion is repaired, zsh `_describe` flags are
assembled from a fixed allowlist, Fish's direct shape is validated, and any missing/duplicate/
reordered anchor fails closed. Final scripts contain no runtime `eval` or joined request command.

The seven-selector plan now explicitly owns executable sourced-Bash controls for hostile literal
tokens and exact argv/trailing-empty preservation; both separated and attached option values;
template-drift rejection; and a closed candidate/directive line grammar whose controls and every
limit are measured in UTF-8 bytes. The unsafe upstream behavior is retained only as an expected-red
control before the correction. No entity, selector, dependency, package version, owned path,
public command, shell set, provider authority, packaging scope, or future-group lifecycle changes.

Ready remains pending. The amendment must be frozen on an immutable clean commit and receive a
fresh independent read-only Ready GO before any selector, dependency, or product implementation.

## Corrected Ready review at 7c01c14

Fresh independent reviewer `/root/g6_02b_ready_review` performed a complete read-only reassessment
of immutable amendment commit `7c01c1416881ccf862823d66cf31c4311cac52e2` and returned **GO**
with 0.995 confidence. The reviewer edited nothing and left the worktree/index clean. There are no
blocking or non-blocking findings.

F01 is closed at the Ready-plan level: package shell bytes are explicitly untrusted; pinned Bash
and zsh template anchors, counts, and order are exact; request dispatch is direct argv with no
runtime `eval` or joined request string; attached and separated Bash values are owned; template
drift produces no partial script; the protocol rejects controls and unknown directives under exact
UTF-8 byte limits; sourced-Bash hostile-token tests own exact argv/trailing-empty/canary safety; the
unsafe upstream callback remains an expected-red control; and Fish's direct dispatch is validated
without an unnecessary rewrite.

The reviewer independently revalidated exact 29/29 plan/catalog owned paths, 11 unique primary
entities, seven required-now validations, eight impacted validations, and sole downstream
EWP-WF01. G6-02A is signed; G6-01, G6-03, G6-04, EWP-WF01, PowerShell, installers, packaging,
publication, and P3 work remain excluded. Catalog/package structure passed with 426 entities, 244
validation obligations, and 45 groups. The unchanged completion baseline passed 24/24 with 73
assertions, EWP-P1-TS07 passed 17/17 with 406 assertions, range whitespace checks passed, and the
final worktree was clean.

## Accepted Ready result

The approved corrected plan is accepted against immutable commit
`7c01c1416881ccf862823d66cf31c4311cac52e2`. P17-G6-02B advances from `mapped` to `ready` with
exactly its mapped and Ready gates passed. EWP-P6-T03 advances to `ready`; the three coverage
entities remain `mapped`; all seven primary validation entities remain pristine `planned` with
null targets and no execution evidence.

The next authorized change is test-only: replace the two selector placeholders with all seven
executable ID-bound tests, reproduce each implementation-free product red independently, and
record exact commands/results before adding `@bomb.sh/tab` or changing production behavior. No
test-first, implementation, green, refactor, adversarial-review, traceability, or sign-off gate is
implied by Ready acceptance.

## Test-first evidence

Both accepted selector placeholders were replaced on the unchanged product/dependency baseline at
`d1624cf`. Only `packages/cli/tests/contracts/completion.test.ts` and
`tests/ergonomics/phase/EWP-P6-TS02.test.ts` changed before execution. No dependency, lockfile,
production source, existing compatibility test, document, catalog, or generated checklist changed
while the red boundary was established.

The contract file owns six exact selectors; the phase file owns the seventh aggregate. Tests call
the future public internal `complete --` pre-parser for end-to-end closure and the production
`resolveCompletionRequest` seam for the complete registry/value/provider matrices without hundreds
of child processes. TS06 also owns final package-template hardening, sourced-Bash argv/canary
safety, Bash attached values, template-drift rejection, strict protocol validation, syntax, help,
and no-startup-write checks. The pinned unsafe upstream Bash control remains ordered after the
dependency is added and before the hardener; it is not falsely claimed on the dependency-free
baseline.

TypeScript and formatting accepted both test sources before execution:

```text
bun run typecheck
exit 0

bunx biome check packages/cli/tests/contracts/completion.test.ts \
  tests/ergonomics/phase/EWP-P6-TS02.test.ts
checked 2 files; no fixes
```

### Red execution receipt EWP-CMD-COMPLETION-TS01

- Validation ID: `EWP-CMD-COMPLETION-TS01`
- Command: `bun test packages/cli/tests/contracts/completion.test.ts --test-name-pattern EWP-CMD-COMPLETION-TS01`
- Exit status: `1`
- Result: 0 passed, 1 failed because the absent internal pre-parser sends `complete --` to the
  unchanged public root parser, which exits 2 for excess input.
- Revision: `working-tree`

### Red execution receipt EWP-CMD-COMPLETION-TS02

- Validation ID: `EWP-CMD-COMPLETION-TS02`
- Command: `bun test packages/cli/tests/contracts/completion.test.ts --test-name-pattern EWP-CMD-COMPLETION-TS02`
- Exit status: `1`
- Result: 0 passed, 1 failed because `resolveCompletionRequest` is absent; no recursive config
  candidate engine exists.
- Revision: `working-tree`

### Red execution receipt EWP-CMD-COMPLETION-TS03

- Validation ID: `EWP-CMD-COMPLETION-TS03`
- Command: `bun test packages/cli/tests/contracts/completion.test.ts --test-name-pattern EWP-CMD-COMPLETION-TS03`
- Exit status: `1`
- Result: 0 passed, 1 failed because `resolveCompletionRequest` is absent; no canonical/alias path
  can return exact local plus inherited flags.
- Revision: `working-tree`

### Red execution receipt EWP-CMD-COMPLETION-TS04

- Validation ID: `EWP-CMD-COMPLETION-TS04`
- Command: `bun test packages/cli/tests/contracts/completion.test.ts --test-name-pattern EWP-CMD-COMPLETION-TS04`
- Exit status: `1`
- Result: 0 passed, 1 failed because `resolveCompletionRequest` is absent; allowed-value handlers
  and separated/attached value completion do not exist.
- Revision: `working-tree`

### Red execution receipt EWP-CMD-COMPLETION-TS05

- Validation ID: `EWP-CMD-COMPLETION-TS05`
- Command: `bun test packages/cli/tests/contracts/completion.test.ts --test-name-pattern EWP-CMD-COMPLETION-TS05`
- Exit status: `1`
- Result: 0 passed, 1 failed because `resolveCompletionRequest` is absent; bounded local
  path/manifest/skill providers do not exist.
- Revision: `working-tree`

### Red execution receipt EWP-CMD-COMPLETION-TS06

- Validation ID: `EWP-CMD-COMPLETION-TS06`
- Command: `bun test packages/cli/tests/contracts/completion.test.ts --test-name-pattern EWP-CMD-COMPLETION-TS06`
- Exit status: `1`
- Result: 0 passed, 1 failed after deterministic public Bash generation because the unchanged
  static script contains no `skillsmith complete --` callback.
- Revision: `working-tree`

### Red execution receipt EWP-P6-TS02

- Validation ID: `EWP-P6-TS02`
- Command: `bun test tests/ergonomics/phase/EWP-P6-TS02.test.ts --test-name-pattern EWP-P6-TS02`
- Exit status: `1`
- Result: 0 passed, 1 failed because the absent internal pre-parser sends `complete --` to the
  unchanged public root parser, which exits 2 before the aggregate can run its shell/build matrix.
- Revision: `working-tree`

The combined matrix is exact:

```text
bun test packages/cli/tests/contracts/completion.test.ts \
  tests/ergonomics/phase/EWP-P6-TS02.test.ts --max-concurrency 1
0 pass; 7 fail; 11 assertions; 2 files; 1.75 seconds
```

Every selector failed for an intended absent completion capability, not TypeScript, formatting,
fixture, skip/todo, timeout, network, secret, or unrelated regression. The unchanged completion
suite remained 24/24 with 73 assertions. The required smoke lane remained 5/5 with 3,737
assertions. Diff whitespace checks passed.

The test-first result is accepted: EWP-P6-T03 becomes `red`; all seven primary validations become
`failing` at exact ID-bound executable targets; G6-02B becomes `active`; and only the test-first
gate joins mapped/Ready as passed. No dependency/product implementation or green gate is claimed.

## Approved implementation-boundary ownership amendment

The first implementation reached green on all seven required selectors and the existing completion,
help, options, extension, docs-drift, typecheck, and smoke surfaces. The first impacted architecture
and ownership replay then found three precise blockers:

```text
bun run lint:boundaries
2 errors:
  packages/cli/src/completion/providers.ts — node:fs belongs to the runtime capability adapter
  packages/cli/src/completion/transport.ts — process.cwd belongs to the runtime capability adapter

bun test tests/ergonomics/phase/EWP-P1-TS11.test.ts \
  --test-name-pattern 'family 8'
0 pass; 1 fail:
  packages/cli/package.json: dependencies inventory changed
```

These are valid contract failures, not waivers or false positives. Direct filesystem and ambient
cwd access must move behind the existing runtime capability composition. The exact package
inventory must acknowledge the approved pinned `@bomb.sh/tab` dependency. Inspection also proved
that the old completion walker cannot be deleted as the accepted plan requires until its two
remaining compatibility consumers migrate: `packages/cli/tests/contracts/options.test.ts` and
`tests/ergonomics/phase/EWP-P1-TS07.test.ts`.

The user approved the bounded amendment on 2026-07-26. It adds exactly six paths, growing the
ownership manifest from 29 to 35 without changing any primary entity, validation ID, dependency
version, public command, shell, provider kind, limit, downstream owner, or deferred feature:

- `packages/core/src/ports/default.ts` and `packages/core/src/ports/types.ts` expose the regular-file
  byte size through existing metadata so the 256 KiB limit is checked before a manifest read;
- `packages/cli/src/runtime/context.ts` composes cwd, monotonic clock, and existing read ports for
  the hidden completion transport;
- `packages/cli/tests/contracts/options.test.ts` and
  `tests/ergonomics/phase/EWP-P1-TS07.test.ts` migrate their legacy completion assertions to the
  generic spec-derived adapter before `walk.ts`/`types.ts` are removed; and
- `tests/ergonomics/phase/EWP-P1-TS11.test.ts` adds only `@bomb.sh/tab` to the exact CLI dependency
  inventory.

The provider becomes asynchronous at its injected read boundary. To preserve the synchronous
`@bomb.sh/tab` handler API without pre-reading unrelated data, the adapter performs one I/O-free
probe parse, awaits only the selected provider, then performs the final parse from a bounded
candidate snapshot. No new application service, write, network, subprocess, environment, prompt,
packaging, installer, PowerShell, or EWP-WF01 authority is introduced.

## Implementation-boundary amendment review at ff48528

Fresh independent reviewer `/root/g6_02b_ready_review` returned **GO** with 0.96 confidence on
immutable commit `ff48528f62ce7bdd0c987684baf73fb32737d66e`. The reviewer edited nothing and
left the worktree/index clean. Plan and catalog contain the same 35 unique tracked paths; the six
additions are the minimal complete response to the recorded boundary, inventory, and legacy-walker
consumers. No seventh path or primary/downstream/public/future scope change was found.

The reviewer accepted the asynchronous port design with two exact implementation conditions:
clone tokens before every `@bomb.sh/tab` parse because the package mutates its input; and fail
closed when regular-file size metadata is missing, invalid, or over 256 KiB, then recheck returned
bytes after reading to cover a metadata/content race. Catalog/package structure, boundary lint,
typecheck, HEAD identity, and clean-worktree checks passed. G6-02B truthfully remains active at 3/10
gates with minimal implementation next.

## Minimal implementation at 3657662

Immutable commit `3657662e4d3ce2ec29aa239e147fcf73a1765a76` contains the complete bounded
replacement. It pins `@bomb.sh/tab` 0.0.21, derives a fresh detached graph from normalized
`CommandSpec`, routes only adjacent hidden `complete --` requests before Commander/signals,
injects read-only cwd/clock/filesystem capabilities, and emits the closed candidate protocol. The
adapter performs an I/O-free probe parse, awaits only the selected provider, freezes its result,
then clones the tokens for the final package parse.

Before hardening, the exact installed package reproduced the accepted unsafe control: generated
Bash contained one runtime `eval`, joined the completion words into a request string, and a harmless
literal command-substitution token created its temporary marker. The marker fixture was removed
immediately. The committed adapter instead fingerprints exact pinned Bash/zsh/Fish templates,
rewrites Bash/zsh request dispatch to fixed direct argv, removes runtime `eval`, repairs attached
Bash values, builds zsh `_describe` arguments as an array, and rejects any template drift.

Path, manifest, and skill providers are metadata-bound and local. They validate every bounded path
component before reading, reject direct/intermediate symlinks, cap directory entries/ancestors/time,
check manifest size before content reads, recheck identity and bytes afterward, decode only through
the canonical v1 codec, and expose no write/network/subprocess/prompt service. Missing, invalid,
negative, non-integer, oversized, raced, unreadable, or malformed inputs return exact `:5\n`.

The old static walker, its private completion AST, and its dedicated test were deleted. Their two
compatibility consumers now exercise the generic spec-derived adapter. Public Commander/help
remains exactly 23 commands; the hidden transport is absent from parser/help/docs/candidates; and
public completion remains emit-only for Bash, zsh, and Fish.

## Green execution receipt EWP-CMD-COMPLETION-TS01

- Validation ID: `EWP-CMD-COMPLETION-TS01`
- Command: `bun test packages/cli/tests/contracts/completion.test.ts --test-name-pattern EWP-CMD-COMPLETION-TS01`
- Exit status: `0`
- Result: 1 test passed with 155 assertions; the exact 23-command, five-alias, duplicate-rejection, and generic recursive-extension graph closed.
- Revision: `3657662e4d3ce2ec29aa239e147fcf73a1765a76`

## Green execution receipt EWP-CMD-COMPLETION-TS02

- Validation ID: `EWP-CMD-COMPLETION-TS02`
- Command: `bun test packages/cli/tests/contracts/completion.test.ts --test-name-pattern EWP-CMD-COMPLETION-TS02`
- Exit status: `0`
- Result: 1 test passed with 168 assertions; recursive config candidates and nested ownership closed without sibling leakage.
- Revision: `3657662e4d3ce2ec29aa239e147fcf73a1765a76`

## Green execution receipt EWP-CMD-COMPLETION-TS03

- Validation ID: `EWP-CMD-COMPLETION-TS03`
- Command: `bun test packages/cli/tests/contracts/completion.test.ts --test-name-pattern EWP-CMD-COMPLETION-TS03`
- Exit status: `0`
- Result: 1 test passed with 2,171 assertions; every canonical and adjacent alias path matched exact local plus inherited flags.
- Revision: `3657662e4d3ce2ec29aa239e147fcf73a1765a76`

## Green execution receipt EWP-CMD-COMPLETION-TS04

- Validation ID: `EWP-CMD-COMPLETION-TS04`
- Command: `bun test packages/cli/tests/contracts/completion.test.ts --test-name-pattern EWP-CMD-COMPLETION-TS04`
- Exit status: `0`
- Result: 1 test passed with 728 assertions; positional and separated/attached option values matched exact registry allowed sets.
- Revision: `3657662e4d3ce2ec29aa239e147fcf73a1765a76`

## Green execution receipt EWP-CMD-COMPLETION-TS05

- Validation ID: `EWP-CMD-COMPLETION-TS05`
- Command: `bun test packages/cli/tests/contracts/completion.test.ts --test-name-pattern EWP-CMD-COMPLETION-TS05`
- Exit status: `0`
- Result: 1 test passed with 74 assertions; local providers closed all read, byte, time, candidate, metadata-race, and direct/intermediate-symlink bounds.
- Revision: `3657662e4d3ce2ec29aa239e147fcf73a1765a76`

## Green execution receipt EWP-CMD-COMPLETION-TS06

- Validation ID: `EWP-CMD-COMPLETION-TS06`
- Command: `bun test packages/cli/tests/contracts/completion.test.ts --test-name-pattern EWP-CMD-COMPLETION-TS06`
- Exit status: `0`
- Result: 1 test passed with 57 assertions; final scripts, hostile argv, attached values, template drift, strict protocol, help, and startup canaries passed.
- Revision: `3657662e4d3ce2ec29aa239e147fcf73a1765a76`

## Green execution receipt EWP-P6-TS02

- Validation ID: `EWP-P6-TS02`
- Command: `bun test tests/ergonomics/phase/EWP-P6-TS02.test.ts --test-name-pattern EWP-P6-TS02`
- Exit status: `0`
- Result: 1 aggregate test passed with 36 assertions, all three shells, hidden-parser closure, purity checks, and native Linux x64/ARM64 builds.
- Revision: `3657662e4d3ce2ec29aa239e147fcf73a1765a76`

## Targeted green at 3657662

All seven required-now selectors passed independently on the immutable implementation revision:
7 tests, 0 unsuccessful selectors, and 3,389 assertions. The six command contracts contributed
3,353 assertions; the phase aggregate contributed 36 and built both declared native targets. The
post-removal completion compatibility suite separately passed 21/21 with 60 assertions.

## Impacted green at 3657662

The post-commit impacted matrix passed:

```text
EWP-P1-TS07 generic runtime/extension replay       17 pass; 405 assertions
help contract                                      7 pass; 530 assertions
options contract                                  47 pass; 1,638 assertions
EWP-P1-TS11 ownership/inventory family             1 pass; 1 assertion
core default/compatibility read ports              14 pass; 63 assertions
required smoke                                     5 pass; 3,737 assertions
```

The first pre-commit P1-TS07 replay reached Bun's unchanged five-second runner default in its broad
version spawn matrix after 16 tests passed. The unchanged selector immediately passed 17/17 with a
30-second runner timeout and passed again post-commit; no source/fixture timeout was changed.

Post-commit typecheck, capability-boundary lint, Biome over all affected sources/tests, generated
command-reference drift, 27-document drift, catalog validation, package structure, range
whitespace, and clean-worktree checks all passed. Catalog/package validation reported 426 entities,
244 obligations, and 45 groups.

## Refactor result at 3657662

Refactor stayed inside the reviewed 35-path boundary. It removed the superseded command-bearing
walker/AST, generalized arbitrary-depth spec paths, rejected duplicate canonical/alias graph
entries, narrowed provider I/O to one selected snapshot, moved ambient capability composition to
the runtime owner, and added pre-read/post-read metadata plus every-component symlink defenses. No
second command inventory, package singleton, runtime `eval`, public command, write authority,
PowerShell/installer/packaging behavior, or downstream EWP-WF01 ownership remains.

Minimal implementation, targeted green, impacted green, and refactor are accepted. Adversarial
review, traceability closure, and sign-off remain pending.

## Independent adversarial review NO-GO at 92ebdf6

Fresh independent reviewer `/root/g6_02b_ready_review` returned **NO-GO** with 0.99 confidence on
immutable clean commit `92ebdf66b45350dd6d0de0af09cf49d125a4afd1`. The reviewer confirmed the
required selectors, native builds, compatibility suites, boundaries, typecheck, docs/catalog
drift, exact dependency, hidden parser, public surface, aliases, recursion, injected providers,
and existing hostile Bash argv canary. Four blockers prevent the adversarial-review gate:

- `G6B-AR-01` (high): anchor-only hardeners accepted arbitrary non-anchor shell bytes; generated
  scripts retained ambient debug-file append authority, and Fish retained a source-time completion
  probe. Complete upstream-template trust and zero-write requirements were therefore not closed.
- `G6B-AR-02` (medium): protocol validation accepted an earlier `:<digits>` line as a candidate,
  so the final directive was not proven to be the only directive line.
- `G6B-AR-03` (medium): `listDir()` materialized an unbounded directory before checking 512, and
  path metadata/read/metadata calls could not provide an atomic no-follow manifest snapshot. A
  same-metadata substitution canary returned the substituted skill.
- `G6B-AR-04` (medium): `adapter.ts` retained a `get|set|list|unset` ordering table instead of
  deriving nested order solely from normalized registry records.

The user approved the same-boundary correction amendment on 2026-07-26. Adversarial review remains
pending; no review waiver, traceability closure, terminal gate, or sign-off is claimed.

## Adversarial-correction test-first evidence

Before correcting product code, the affected executable selectors were extended with the four
review canaries and run against unchanged commit `92ebdf6`:

```text
bun test packages/cli/tests/contracts/completion.test.ts \
  --test-name-pattern 'EWP-CMD-COMPLETION-TS0(2|5|6)'
0 pass; 3 fail; 263 assertions
```

TS02 received the old hard-coded `get,set,list,unset` order instead of the fixture's normalized
`unset,list,set,get` help order. TS05 returned substituted `omega` bytes instead of atomic-snapshot
`alpha` bytes. TS06 retained `BASH_COMP_DEBUG_FILE` before reaching its additional complete-template
drift and directive-shaped-candidate cases. These are intended failures of unchanged product code,
not compilation, fixture, timeout, skip/todo, network, or unrelated failures.
