# P17-G5-04 evidence

## Mapping and prerequisite audit

Mapping began from clean signed G5-03 revision
`d18da7bceecd20cf898bae79bf6454e8e7fe821e` on 2026-07-23.

- Phase 5 is active.
- Direct dependency P17-G4B-03 is signed off at all ten lifecycle gates.
- G5-04 began planned with no owned files, implementers, reviewer, executable targets, or passed
  gates.
- Before amendment, the primary set was exactly 15 entities, not the initially reported 14:
  `COMMAND:gc`, D-012, EWP-CF-016, GC TS01..08, EWP-P5-T06, EWP-P5-TS04, EWP-WF12, and P2-03.
  The arithmetic is 1 command + 1 decision + 1 finding + 8 command tests + 1 task + 1 phase test +
  1 workflow + 1 recommendation = 15.
- Required-now and impacted validations are identical: exactly GC TS01..08, P5-TS04, and WF12.
  Downstream coverage is empty. Three Phase-6 groups depend on G5-04 plus G5-05, but that dependency
  is not a G5-04 downstream validation obligation.
- Signed reciprocal authorities EWP-CF-028, EWP-CF-031, EWP-CF-035, EWP-CF-037, and EWP-CF-043
  remain compatibility constraints.

The accepted command is flat `skillsmith gc` with exactly dry-run, older-than duration, repeatable
forget-project path, yes, JSON, and inherited no-prompt. It has no target/all, tool/scope, artifact,
force, or batch surface. The v1.0 target registry and migration ledger already contain this exact
future grammar; current CommandSpec/service/wire/renderer state correctly has no public GC yet.

## Existing implementation audit

Root and three read-only specialist audits established these existing facts:

- there is no GC domain, service, command codec, renderer, or current wire mapping;
- store leaves are immutable paths at `<store>/<namespace>/<repo>@<rev>/<skill>`; `.staging` is
  existing lifecycle recovery state, not garbage;
- ledger v2 includes current user/project pairs, derived project registrations, logical
  transactions, and bounded history;
- `projectRegistrations` cannot be independently edited: ledger decode requires it to equal
  `deriveLedgerProjectRegistrations(projects)`, and every existing pair transform re-derives it;
- therefore exact project forget must remove the matching local `projects[root]` subtree and derive
  registrations while leaving transactions/history intact;
- canonical ledger read, lock, migration, persistence, history selection, retained-resource
  validation, store hash, and focused metadata capabilities already exist and must be reused;
- current history cleanup supplies the safe deletion pattern—caller-held ledger lock, exact revision,
  full preflight, stable identities, absence assertion, and parent flush—but owns backup cleanup only;
- adapted-overlay production is deferred and has no physical inventory authority today;
- the ledger is a protection graph, not a complete inventory of unreferenced store leaves;
- current runtime ports can enumerate/read/link/stat-time/metadata, rename/remove/fsync, and inject a
  fake clock without a broad environment bag, but the review proved that GC additionally needs
  focused effective-user/owner metadata and exclusive directory/file creation capabilities.

The detailed Ready candidate is `projects/p17/plans/P17-G5-04.md`.

## Contract amendment EWP-CF-044

The audit found five implementation-blocking gaps in accepted prose:

1. the ledger could not discover a truly orphaned store leaf absent from every ledger edge;
2. older-than had no default, accepted grammar, overflow rule, clock, or equality boundary;
3. exact bytes did not say logical versus allocated bytes or define symlink/directory behavior;
4. adapted overlays were named even though adaptation and its physical inventory are deferred; and
5. GC had no truthful approval action vocabulary or crash-safe ordering/detach protocol.

Reusing the shared `repair` operation would contradict D-012's “repair elsewhere.” Widening
`skillsmith.plan` with machine-local cache deletion would give portable desired-state apply an
unnecessary destructive authority. Direct delete-before-forget could crash with a still-registered
project pointing at deleted content; direct recursive deletion of the live leaf could expose a
partially deleted object.

With the user's explicit amendment approval, EWP-CF-044 now freezes:

- fixed-root strict store candidate enumeration with ledger-authoritative protection and no
  project/home/portable-artifact crawl;
- final-recognized-`@revision` parsing that accepts current local/no-remote publisher basenames with
  spaces, Unicode, or additional `@` instead of retroactively applying remote-name sanitization;
- omitted age = no extra floor; explicit `[1-9][0-9]*(s|m|h|d|w)` safe duration;
  `modifiedAt < cutoff`, with equality protected;
- deterministic logical file bytes plus literal internal-symlink-target UTF-8 bytes, directories
  zero, no symlink following, and fail-closed unstable/unsafe/overflow cases;
- exact lexical absent-root matching, whole project-subtree removal, and derived registration;
- a command-local `migrate-ledger | forget-project | reclaim-store` maintenance action union while
  portable saved-plan kinds remain unchanged;
- a private owner-only strict recovery-v1 record, published and replaced by an exact temp/CAS/
  revision protocol after approval and before mutation, binding the semantic request, approval,
  ledger revisions, every action identity/token/phase, and exact container/payload; matching
  execution phase-aware revalidates pending forget without replan/prompt while mismatch, malformed
  live state, impossible temp transition, multiple records, or unsupported version refuses;
- durable migration/forget before reclaim, then an exclusively created owner-bound action container
  and ordinary same-filesystem atomic leaf rename into its verified absent private `payload` slot,
  parent flushes, detached cleanup, collision/planted-state refusal, and retry isolation from a
  replacement at the original path; and
- no current overlay producer; only a future injected adapter-owned disposable inventory can supply
  that closed branch.

The amendment adds EWP-CF-044 as one required G5-04 primary entity. Current G5-04 arithmetic is
therefore exactly 16 primary entities, 10 required-now validations, and zero downstream selectors.
The catalog is 426 total / 419 required / 7 deferred / 44 findings / 244 validation obligations.

### Amendment validation receipt

```text
bun test scripts/p17-catalog.test.ts
56 pass, 0 fail, 267 assertions

bun scripts/p17-catalog.ts --check
valid: 426 entities (419 required, 7 deferred; 244 validation obligations), 45 groups;
deterministic checklist

bun scripts/check-p17-package.ts --check
structurally valid; Phase 5 active; 37/44 required groups signed off;
356/419 required entities signed off; 63 incomplete; 7 deferred

git diff --check
PASS
```

The amendment changes no product implementation, current CLI surface, executable validation target,
ledger/journal/saved-plan schema, or lifecycle status. It specifies one new private GC recovery-v1
persistence codec; that codec is neither the public `gc@1` DTO nor a portable artifact.

## Amendment review

A fresh independent technical review initially returned NO-GO with seven concrete findings. The
corrections are incorporated in the normative contract and Ready candidate:

1. **F01 candidate contradiction:** registry, WF12, CF-031, and TS01 still implied that ledger rows
   bounded inventory. They now consistently distinguish fixed configured store-root candidates
   from live/ledger/journal/history protection.
2. **F02 lost post-forget authority:** ledger commit alone could not prove which already-approved
   reclaim plan should resume. GC now durably writes a strict owner-only recovery-v1 record before
   migration/forget/reclaim; only the matching semantic execution request resumes that stored plan.
3. **F03 leaf grammar gap:** inventory now has exact three-segment component, revision, skill,
   containment, root/parent, reserved-child, unknown-layout, and parent-pruning rules.
4. **F04 tombstone ambiguity:** destination, directory ownership, no-symlink, collision, planted
   state, per-boundary record phase, cleanup, and accounting rules require the matching recovery
   record; F12 below replaces the initially claimed no-clobber rename with a realizable exclusive
   action-container protocol.
5. **F05 unsafe-sibling ambiguity:** any unsafe, unstable, unreadable, hard-linked, or overflowing
   object refuses the whole invocation before approval/writes; diagnostics expose no actions or
   eligible totals. The global exit precedence now distinguishes invalid observed GC state (3) from
   unsafe request grammar or approval/choice refusal (2).
6. **F06 stale governance counts:** current execution counts now say 426 entities/44 findings and
   PREP explicitly labels 425/43 as the original historical Phase-0 baseline.
7. **F07 `.fetch` location:** `.fetch` is explicitly a data-directory sibling outside store crawl;
   only `.staging` is the exact store-root lifecycle-recovery child.

The focused re-review closed F01, F05, F06, and F07; it kept F02-F04 partially open and returned
NO-GO with five new high findings. The second correction set is now incorporated and awaits another
focused review:

8. **F08 publisher compatibility:** the first strict grammar wrongly rejected legitimate current
   local/no-remote basenames such as `my repo` and `acme@tools`. Inventory now parses the final
   recognized `@revision` suffix and accepts the publisher's exact one-component basename prefix.
9. **F09 retry forget guard:** matching recovery now phase-aware revalidates exact ledger byte/
   semantic revision, key/subtree, absent-including-dangling-symlink, non-current, and no-journal
   preconditions for each uncommitted forget; it adopts only the exact recorded post-forget semantic
   digest after a phase-update crash and otherwise retains the record/refuses.
10. **F10 recovery path authority:** data/recovery ownership, mode, lstat real-directory,
    no-symlink, containment, stable identity, live/temp file, and exact 64-hex plan/action/token/
    revision plus 16-hex CAS grammars are now closed.
11. **F11 recovery-write crashes:** initial/replacement temp filenames, exclusive write/fsync/
    readback, guarded publish/CAS replacement, monotonic transitions, orphan-temp discovery, and
    every open/write/fsync/rename/flush result are now specified. A malformed live record refuses;
    incomplete non-authoritative staging cannot become deletion authority.
12. **F12 realizable detach:** the plan no longer claims an unavailable rename-no-replace primitive.
    It exclusively creates and records a mode-0700 action container/owner marker, revalidates its
    exact absent private `payload` slot, and uses the existing ordinary atomic rename only inside
    that isolated owner-bound container.
13. **F13 runtime capability boundary:** RuntimePorts now owns effective-user identity, optional
    UID/GID lstat metadata, non-recursive exclusive directory creation, and non-overwriting exclusive
    text-file creation with initial mode. The default adapter implements them; the ScanEnv facade
    accepts that same focused implementation as an explicit supplement and reports unavailable only
    when omitted instead of emulating unsafe behavior. Port type/default/compatibility tests, the
    shared runtime fixture, and signed P1-TS08 are in the candidate manifest, and GC domain code
    retains no ambient process/filesystem authority.

### Accepted amendment result

Independent amendment reviewer `/root/g5_04_cf044_review` returned final GO after three focused
correction rounds. F01-F13 are closed with no residual or new blocker. The reviewer independently
recomputed the then-current amendment manifest as exactly 87 unique paths with partition
`11 + 26 + 20 + 14 + 3 + 13`, confirmed the 16/10/0 entity/validation arithmetic, and verified that
the focused RuntimePorts/default/ScanEnv-supplement boundary can implement recovery ownership,
exclusive record/container creation, CAS convergence, and private-payload detach without ambient GC
filesystem imports. Catalog, package structural validation, and diff formatting were green. The
reviewer edited no files.

## Ready review

Fresh independent Ready reviewer `/root/g5_04_ready_review` returned three stable findings, all
corrected and independently re-reviewed:

- **F14 compile-fixture ownership:** signed P1-TS08 compiles its separate read-only capability
  fixture, whose exact RuntimePorts key assertion must change with the focused port. The fixture is
  now owned, expanding only the declared compatibility boundary to the current exact 88 paths.
- **F15 dry-run lock contradiction:** `withLedgerLock` materializes a parent/lock path, contradicting
  dry-run's zero-write contract. Preparation/dry-run now performs strict read-only recovery
  observation without directory creation, lock acquisition/removal, or temp cleanup. Approved
  execution alone locks, rediscovers, requires the observed state still exact, and converges
  recovery. TS06 owns explicit pending-recovery zero-lock/zero-filesystem-write proof.
- **F16 immutable selector title:** expanding the catalog title for TS08 caused immutable-title
  drift. The exact accepted title is restored in normative source and catalog; continuation prose
  and the Ready matrix retain the expanded recovery temp/CAS and action-container/payload coverage.

## Candidate design and ownership

The candidate preserves one immutable observation -> pure plan -> exact approval -> revalidated
execution flow. It reuses the canonical ledger lock/writer/migration and adds focused GC
inventory/reachability/recovery/repository modules. The strict owner-only recovery temp/CAS protocol
commits the approved plan before mutation; forget commits before reclamation. Each leaf is detached
atomically to the absent payload of its exclusively created record-bound action container before
recursive cleanup; the source path is never
partially deleted. A changed ledger or unbound tombstone is never independently deletion authority.
GC never invokes backup/history cleanup, repair, apply, undo, or project artifact editing.

The public `gc@1` DTO reports bounded-default selection, project/migration/duration/approval facts,
ordered project and object classification rows, exact logical item/byte totals, closed local action
and result rows, sanitized recovery status/phase, and sanitized diagnostics. Human and JSON map the
same strict DTO. No raw
ledger/journal, portable artifact bytes, arbitrary discovered path, URL/credential, stack/cause,
capability object, or mutable reference crosses the application boundary.

The exact candidate ownership manifest is 88 unique paths partitioned 11 governance/docs, 26 core
sources, 20 core tests, 14 CLI source/tests, 3 primary process fixtures, and 14 signed compatibility
fixtures/tests. Catalog/checklist remain canonical lifecycle meta-state and are regenerated rather
than recursively owned. The historical v0.7 Commander artifacts and v1.0 target registry stay
immutable.

## Test-first candidate

Exactly four absent implementation-free paths form the proposed red boundary:

- `packages/cli/tests/contracts/gc.test.ts` for TS01..08;
- `tests/ergonomics/fixtures/p5-gc/fleet.ts` for one hermetic process/store/ledger/fake-clock/crash
  fleet;
- `tests/ergonomics/phase/EWP-P5-TS04.test.ts` for reachability/age/bytes composition;
- `tests/ergonomics/workflows/EWP-WF12.test.ts` for uninstall/apply/removal/missing-project forget
  and repeated GC.

Each selector first requires the current GC CommandSpec and application service, producing the same
semantic red `{ commandSpec: false, applicationService: false }`. The ten commands are frozen in
the detailed plan. Missing files/imports/types, timeouts, skips, TODOs, public network, or placeholder
passes are not valid red evidence.

## Accepted Ready result

Focused final re-review of plan
`822d32056c5c1214074c450573b6e7eda51c3d79512f5d11aaff584e5513a8a2` and pre-acceptance
evidence `be408f25ab5fda27cee63144b65c092c8f9dabe704851f3158fb744d77a8ae25` returned **GO** with no
edits. The reviewer confirmed F14-F16 closed, exact 16-primary/10-required-now/0-downstream
partitions, the 88-path `11 + 26 + 20 + 14 + 3 + 14` manifest, dependency/phase readiness, public
grammar, fixed-root and publisher-compatible inventory, protection/retention/forget invariants,
recovery temp/CAS and owner-container/payload feasibility, dry-run zero-write behavior, strict
public/private codec boundaries, all four absent semantic-red targets, and all ten selector commands.

The reviewer recorded 56 catalog tests passed with 267 assertions, deterministic catalog counts of
426 total / 419 required / 7 deferred / 244 validation obligations / 45 groups, structural package
validity, and clean diff formatting. G5-04 may advance to `ready` with mapped/Ready as the only
passed gates. EWP-P5-T06 alone may advance to `ready`; command, decisions/findings/recommendation,
workflow, and all validations remain planned. No product behavior, target, test receipt, or later
lifecycle gate is claimed. The next authorized action is the implementation-free four-file semantic
red boundary for the ten selectors.

## Ready transition validation

The accepted Ready mutation was applied exactly as reviewed: G5-04 is `ready`, owns the frozen 88
paths, names `root-codex-goal` as implementer/integration owner, and binds the ten exact selector
commands. Only mapped and Ready gates pass. EWP-P5-T06 alone is `ready`; the other fifteen primary
entities and all ten validation targets remain `planned`.

Post-mutation structural receipts:

- `bun scripts/p17-catalog.ts --write` rendered 426 entities across 45 groups;
- `bun test scripts/p17-catalog.test.ts` passed 56/56 tests with 267 assertions and zero failures;
- `bun scripts/p17-catalog.ts --check` reported 426 total, 419 required, 7 deferred, 244 validation
  obligations, 45 groups, and a deterministic checklist;
- `bun scripts/check-p17-package.ts --check` reported Phase 5 active, 37/44 required groups signed,
  G5-04 ready with 2/10 lifecycle gates passed, and `test-first` as the next gate;
- `git diff --check` passed.

The rollup remains 356/419 required entities signed with 63 incomplete. Readiness changes lifecycle
authority only; it does not claim tests, product behavior, or validation completion.

## Test-first evidence

The implementation-free four-file boundary is committed at
`6cf2e19a2c0e06329badff085c934db59b8643cb`:

- `packages/cli/tests/contracts/gc.test.ts` owns EWP-CMD-GC-TS01 through TS08;
- `tests/ergonomics/fixtures/p5-gc/fleet.ts` owns one hermetic local store/ledger/project/process
  fleet, deterministic store-object construction and measurement facts, no-network canaries,
  snapshots, and process helpers;
- `tests/ergonomics/phase/EWP-P5-TS04.test.ts` owns the reachability, age, logical-byte, and unsafe
  measurement composition matrix; and
- `tests/ergonomics/workflows/EWP-WF12.test.ts` owns missing-project forget, reclaim, retained live
  state, and repeated convergence through the public CLI process.

Each of the ten frozen commands was run as its own Bun test process after formatting. Every process
exited 1 with exactly zero passes, one failure, and one assertion. Every failure received the same
semantic state `{ commandSpec: false, applicationService: false }` from `requireGcBoundary()`.
There was no missing module/file, syntax or type failure, timeout, skip, TODO, public-network access,
fixture construction before the assertion, or placeholder success.

The exact selector receipts were:

```text
bun test packages/cli/tests/contracts/gc.test.ts --test-name-pattern EWP-CMD-GC-TS01
bun test packages/cli/tests/contracts/gc.test.ts --test-name-pattern EWP-CMD-GC-TS02
bun test packages/cli/tests/contracts/gc.test.ts --test-name-pattern EWP-CMD-GC-TS03
bun test packages/cli/tests/contracts/gc.test.ts --test-name-pattern EWP-CMD-GC-TS04
bun test packages/cli/tests/contracts/gc.test.ts --test-name-pattern EWP-CMD-GC-TS05
bun test packages/cli/tests/contracts/gc.test.ts --test-name-pattern EWP-CMD-GC-TS06
bun test packages/cli/tests/contracts/gc.test.ts --test-name-pattern EWP-CMD-GC-TS07
bun test packages/cli/tests/contracts/gc.test.ts --test-name-pattern EWP-CMD-GC-TS08
bun test tests/ergonomics/phase/EWP-P5-TS04.test.ts --test-name-pattern EWP-P5-TS04
bun test tests/ergonomics/workflows/EWP-WF12.test.ts --test-name-pattern EWP-WF12

each: exit 1; 0 pass; 1 fail; 1 assertion; exact missing public boundary
```

Supporting receipts on the red revision:

```text
bun run typecheck
PASS

bun run lint:boundaries
PASS

bunx biome check <the four red paths>
Checked 4 files; no fixes required after deterministic formatting

bun scripts/p17-catalog.ts --check
valid: 426 entities (419 required, 7 deferred; 244 validation obligations), 45 groups,
deterministic checklist

bun scripts/check-p17-package.ts --check
structurally valid; Phase 5 active; G5-04 ready at 2/10 gates; next test-first

bun run test:smoke
5 pass; 14 filtered out; 0 fail; 3,737 assertions

git diff --check
PASS
```

TypeScript initially identified one fixture-only return annotation whose derivation from a defaulted
parameter collapsed its keys to `never`. Replacing it with the exact explicit two-field return shape
made typecheck pass without changing behavior or the semantic red. Biome then applied only import
ordering and line wrapping; all ten selectors were replayed after those changes.

After anchoring the immutable red commit, the catalog advanced G5-04 from `ready` to `active` and
passed only the test-first gate. Regeneration rendered the same 426 entities across 45 groups. The
full catalog validator passed 56/56 tests with 267 assertions; catalog and package checks reported
G5-04 active at 3/10 gates with minimal implementation next, 37/44 required groups signed, and the
unchanged 356/419 signed required-entity rollup. Diff formatting remained clean. EWP-P5-T06 stays
`ready`; the command, contract selectors, workflow, decisions/findings/recommendation, and P2-03
remain `planned` until implementation and executable evidence exist.

## Minimal implementation and recovery hardening

The implementation advanced in focused immutable boundaries:

- `56dde95` added the effective-user, owner metadata, and exclusive private-state ports;
- `1d3b1e8` added fixed-root inventory, reachability, retention, request normalization, and pure
  planning;
- `30a32ce` added strict private recovery-v1 and owner-bound tombstone persistence;
- `d6e8b4f` connected the application service, public command/codec/renderers, and bounded GC
  execution;
- `0a55ad6`, `fd01985`, and `d4dc813` hardened recovery discovery, temp/live convergence, guarded
  publication, crash ordering, and owner-bound cleanup; and
- `ac027e8` closed injected overlay coverage before the first terminal replay.

The resulting command inventories only the configured store root, treats the canonical ledger,
logical/legacy journals, bounded history, and observed live placements as protection edges, applies
the strict age boundary and logical-byte policy, and constructs one deterministic immutable action
set. Approved execution rediscovers state under the canonical ledger lock, publishes recovery before
mutation, commits migration/forget before reclaim, atomically detaches a selected leaf into its
record-bound private payload, and resumes cleanup from monotonic CAS state.

## Terminal-discovered amendment fallout

The first full terminal replay at `ac027e8` passed type/static/structural checks and the ten G5-04
selectors, but reported 31 failures in the wider suite. Those failures were not 31 independent GC
behavior defects. They fanned out from three stale governance assumptions introduced by the already
approved EWP-CF-044 amendment:

1. package-output compatibility fixtures still froze the pre-amendment 425-total/418-required
   catalog counts instead of 426/419;
2. the accepted-finding validator and its adversarial suite treated EWP-CF-043 as the final finding
   and froze 43 findings, so the valid EWP-CF-044 row invalidated the shared baseline used by many
   negative cases; and
3. the G0-03 reconciliation fixture and validator still pointed at `docs/architecture.md:46` after
   the authoritative sentence moved to line 47.

The efficient response was to stop that known-invalid terminal run, identify the common authorities,
and amend ownership before changing them. The user approved adding exactly five terminal-discovered
compatibility owners: the G0-03 reconciliation fixture, package-output compatibility test,
architecture reconciliation validator, plan-structure/finding validator, and P0A-TS07 adversarial
test. The candidate boundary is therefore now exactly 93 paths rather than the accepted Ready
candidate's 88. The product contract, public grammar, portable artifacts, and required-now selector
set did not expand.

## Independent implementation review and correction

The first independent implementation audit of `ac027e8` returned NO-GO. Its stable findings were:

- a cleaned action could be interrupted after owner removal but before empty-container removal and
  then fail strict discovery instead of resuming;
- approved execution did not repeat every recovery/tombstone/ledger/forget/candidate check after it
  entered the ledger lock;
- TS03, TS04, TS05, and TS08 did not yet prove their full journal/history, exact ±1 ms, unstable/
  overlay, and recovery-boundary contracts;
- lock cancellation and permission errors were not kept distinct through every layer;
- a recovery rename followed by a failing directory fsync could leave a live authoritative record
  while the call reported no pending authority;
- partial reports and application mutation accounting discarded already durable earlier outcomes;
- semantic request identity depended on forget-root input order;
- competing recovery or planted tombstone state could appear between approval and publication;
- public action prose claimed private before/after/precondition fields not present in `gc@1`;
- recovery parsing admitted impossible out-of-order terminal/nonterminal action states;
- already-absent was incorrectly eligible to count as a change; and
- already-absent state could accept an impossible owner container.

Correction commit `388a9c03259cbf7e40de3da9d7be0cfb9346b1d2` addressed that first finding set by:

- adopting the exact identity-bound cleaned empty action container and rejecting any
  already-absent container;
- byte-sorting normalized forget roots and making the request digest permutation-invariant;
- enforcing public action order, deterministic terminal prefixes, at most one active reclaim, and
  phase-specific pending/terminal invariants in the recovery parser;
- reobserving exact live authority after create/replace/converge publication failures;
- rediscovering recovery and tombstones, rereading the ledger, and revalidating forget roots,
  inventory facts, candidates, and absent action paths after lock acquisition and before publication;
- projecting durable migration, forget, cleaned, already-absent, and protected outcomes into partial
  reports and deriving application mutation summaries from those reports;
- forwarding cancellation into lock acquisition and preserving permission errors from lock,
  ledger, record, detach, and cleanup boundaries;
- amending the plan to distinguish the small public `gc@1` action shape from richer private recovery
  preconditions and before/after facts; and
- strengthening the four under-covered signed selectors plus focused repository, recovery,
  execution, planning, and application tests.

## Corrected focused validation

The immutable correction revision has the following current receipts:

```text
bun run typecheck
PASS

bun run lint:boundaries
PASS

bun test packages/core/tests/gc packages/core/tests/application/gc-service.test.ts
40 pass; 0 fail; 164 assertions

bun test packages/core/tests/place/ledger.test.ts
21 pass; 0 fail; 71 assertions

bun test scripts/check-p17-package-output.test.ts
3 pass; 0 fail; 25 assertions

bun test tests/ergonomics/phase/EWP-P0A-TS07.test.ts tests/ergonomics/phase/P17-G0-03.test.ts
37 pass; 0 fail; 63 assertions

bun run test:smoke
5 pass; 14 filtered; 0 fail; 3,737 assertions

bun run test:smoke:p2-ts04:recovery
4 pass; 15 filtered; 0 fail; 453 assertions

bun scripts/p17-catalog.ts --check
valid: 426 entities (419 required, 7 deferred; 244 validation obligations), 45 groups;
deterministic checklist

bun scripts/check-p17-package.ts --check
structurally valid; Phase 5 active; G5-04 at 3/10 gates; minimal implementation next

bun scripts/p17-plan-structure.ts --check
valid: EWP-P0A-TS01, EWP-P0A-TS06, and EWP-P0A-TS07

git diff --check
PASS
```

Nine of the ten separately replayed frozen selectors passed on the first corrected replay. TS02
then caught one newly introduced exit-mapping regression: corrupt/future ledger observation was
safely refused but returned generic failure exit 1 instead of state exit 3. The mapping was narrowed
so only actual permission observation becomes exit 6; invalid observed state remains exit 3. The
exact TS02 process then passed with 12 assertions. Fresh independent re-review of immutable
`388a9c0` nevertheless returned NO-GO: it confirmed the 10/10 required selector rerun but found
remaining permission loss in incomplete-staging cleanup, unguarded observation after successful
recovery rename, incorrect migration/forget failure attribution and durable projection, final
record-delete/fsync status mismatch, and TS03 lineage/TS08 physical-matrix evidence gaps. Those
findings require a second correction revision; no later lifecycle gate or sign-off is claimed here.

## Second correction amendment

The second correction is anchored at
`53b039e` and directly covers the re-review blockers without expanding product scope:

- incomplete initial-staging cleanup now returns a discriminated result carrying exact visible
  recovery state and a permission/cancellation/failure reason through the ledger lock and application
  exit mapping; focused tests inject lock, removal, and parent-fsync EACCES/EPERM;
- create, replace, and staging convergence remember when an exact guarded rename has already made
  the requested record authoritative; a failing post-rename fsync plus failing recovery observer now
  returns that exact pending record with a publication failure rather than reporting no authority;
- result projection consumes an explicit failing action kind or action ID, so migration/forget
  failures no longer blame the first reclaim; successful ledger migration/forget followed by failed
  recovery CAS reports the durable action as succeeded while retaining the older visible recovery
  phase for retry;
- completion cleanup returns exact `none | pending | refused` visible state after failure; a
  successful live-record removal followed by failing parent fsync reports recovery `none`, retains
  the completed reclaim count, and never invents a pending record;
- TS03 now plants a codec-valid committed forward install and committed rollback child whose
  `parentOperationId`, operation intent, before/after resources, retained store resource, and history
  order form actual rollback lineage; and
- TS08 now exercises a cleaned-container interruption plus competing live records, unsafe live-file
  mode, malformed CAS staging, competing initial staging, unsafe action-container mode, wrong owner,
  unsupported tombstone version, and unowned planted action state, proving every refusal leaves the
  detached target absent and reachable content present before exact public retry converges.

The focused second-correction receipts are:

```text
bun run typecheck
PASS

bun run lint:boundaries
PASS

bun test packages/core/tests/gc packages/core/tests/application/gc-service.test.ts
46 pass; 0 fail; 183 assertions

bun test packages/core/tests/place/ledger.test.ts
21 pass; 0 fail; 71 assertions

bun test packages/cli/tests/contracts/gc.test.ts --test-name-pattern 'EWP-CMD-GC-TS0(3|8)'
2 pass; 6 filtered; 0 fail; 42 assertions

bun run test:smoke
5 pass; 14 filtered; 0 fail; 3,737 assertions

bun run test:smoke:p2-ts04:recovery
4 pass; 15 filtered; 0 fail; 453 assertions

bun scripts/p17-catalog.ts --check
valid: 426 entities (419 required, 7 deferred; 244 validation obligations), 45 groups;
deterministic checklist

bun scripts/check-p17-package.ts --check
structurally valid; Phase 5 active; G5-04 at 3/10 gates; minimal implementation next

bun scripts/p17-plan-structure.ts --check
valid: EWP-P0A-TS01, EWP-P0A-TS06, and EWP-P0A-TS07

git diff --check
PASS
```

All ten frozen commands were then replayed as separate processes on the immutable correction:

```text
EWP-CMD-GC-TS01  1 pass; 0 fail;  8 assertions
EWP-CMD-GC-TS02  1 pass; 0 fail; 12 assertions
EWP-CMD-GC-TS03  1 pass; 0 fail;  6 assertions
EWP-CMD-GC-TS04  1 pass; 0 fail; 15 assertions
EWP-CMD-GC-TS05  1 pass; 0 fail; 14 assertions
EWP-CMD-GC-TS06  1 pass; 0 fail; 36 assertions
EWP-CMD-GC-TS07  1 pass; 0 fail; 13 assertions
EWP-CMD-GC-TS08  1 pass; 0 fail; 36 assertions
EWP-P5-TS04      1 pass; 0 fail;  8 assertions
EWP-WF12         1 pass; 0 fail; 12 assertions
total           10 pass; 0 fail; 170 assertions
```

Fresh independent reviewer `/root/g5_04_final_review` returned GO for immutable product correction
`53b039ec177f0322e1f614eba2afb0f41d491f0f`. The reviewer confirmed all four remaining source
blockers and both selector-evidence gaps closed, independently passed typecheck and boundary lint,
reported 48 focused core/application tests with 192 assertions and all ten required selectors with
160 assertions, and found no new release blocker. The reviewer edited no files. That GO authorized
the full terminal gate; it did not by itself claim traceability or sign-off.

## Full terminal discovery and compatibility correction

The first complete serialized terminal gate on reviewed `53b039e` ran for 988.63 seconds and
reported:

```text
bun run check -- --timeout 60000 --max-concurrency 1
3,137 pass; 28 intentional skips; 6 fail; 103,966 assertions
3,171 tests across 341 files
FAIL
```

Every GC command-contract selector passed inside that run. The six failures were exact downstream
compatibility authorities already included in the approved 93-path G5-04 ownership boundary, not
new product behavior defects or a scope expansion:

1. EWP-P2-TS08 family 5 still froze the pre-GC wire inventory and omitted `gc@1`.
2. EWP-P2-TS08 family 6 still froze the pre-GC contracts/v1 runtime exports and omitted
   `gcV1Codec`.
3. EWP-P1-TS05 required README's current inventory to match the live parser while README still
   described GC only as a future P17 target.
4. EWP-P1-TS11 froze the pre-GC command paths, option count, and option-row digest.
5. The P1-TS08 compile-negative fixture froze `FileWritePort` before the already approved optional
   atomic empty-directory removal capability.
6. P1-TS08's ambient-effects scan did not yet classify the two GC modules that convert already
   injected epoch values into canonical ISO strings. Both sites consume `wallNowIso()` or persisted
   `nowMilliseconds`; neither reads ambient time, randomness, process state, or a global clock.

The first focused replay after updating those six authorities correctly exposed two adjacent
assertions inside the same owned surfaces: P2-TS08's separate public-types fixture also froze the v1
runtime key set, and P1-TS05 then detected that README simultaneously called GC current and future.
Those were corrected rather than hidden or filtered.

Immutable compatibility correction `c666de4feeb02fcb3b805e97b8fdf9510aabe6a4` therefore:

- adds `gc@1` and `gcV1Codec` to both executable and compile-time exact P2 inventories;
- moves GC from README's future target into its live parser-derived current inventory and narrows
  the remaining P17 target prose accordingly;
- advances the observation command registry to 28 paths, 270 options, and exact SHA-256 option-row
  digest `ca83f48e2e2690e9f7b8f653cec65424dc2fed84b52b765c2d106d6fb54ec0c9`;
- advances the compile-negative `FileWritePort` key set with optional `removeEmptyDirectory`; and
- extends only the existing injected-timestamp parsing classification for GC service/execution,
  leaving the ambient clock/randomness prohibition unchanged.

The corrected proportional receipts are:

```text
bun test tests/ergonomics/phase/EWP-P2-TS08.test.ts \
  tests/ergonomics/phase/EWP-P1-TS05.test.ts \
  tests/ergonomics/phase/EWP-P1-TS11.test.ts \
  tests/ergonomics/phase/EWP-P1-TS08.test.ts
34 pass; 0 fail; 2,445 assertions

bun run typecheck
PASS

bun run lint:boundaries
PASS

bun run test:smoke
5 pass; 14 filtered; 0 fail; 3,737 assertions

bun run test:smoke:p2-ts04:recovery
4 pass; 15 filtered; 0 fail; 453 assertions

bun scripts/p17-catalog.ts --check
valid: 426 entities (419 required, 7 deferred; 244 validation obligations), 45 groups;
deterministic checklist

bun scripts/check-p17-package.ts --check
structurally valid; Phase 5 active; G5-04 at 3/10 gates

bun scripts/p17-plan-structure.ts --check
valid: EWP-P0A-TS01, EWP-P0A-TS06, and EWP-P0A-TS07

git diff --check
PASS
```

Fresh independent reviewer `/root/g5_04_final_review` returned GO for immutable correction
`c666de4feeb02fcb3b805e97b8fdf9510aabe6a4`. The reviewer confirmed all six terminal failures and
both adjacent exact authorities closed without production behavior changes or weakened equality
checks, independently passed the 34-test affected replay, typecheck, boundary lint, and correction
diff check, and verified by source inspection that both GC timestamp conversions consume injected or
persisted clock values rather than ambient time. The reviewer found no release blocker or omitted
adjacent authority and edited no files.

## Full terminal gate

The corrected immutable product and compatibility revisions then passed the complete serialized
terminal gate:

```text
bun run check -- --timeout 60000 --max-concurrency 1
3,143 pass; 28 intentional skips; 0 fail; 104,168 assertions
3,171 tests across 341 files; 986.69 seconds
PASS
```

All six authorities that failed the first terminal run passed in this rerun, including both exact
P2 export/wire inventories, README/parser agreement, the exact P1 command registry digest, the
compile-negative `FileWritePort` fixture, and the ambient-effects classification. The separately
discovered adjacent P2 public-types and README future/current assertions also passed. All eight GC
command selectors, EWP-P5-TS04, and EWP-WF12 passed inside the same terminal run; TS08 specifically
reproved owner-bound tombstones, crash convergence, and zero reachable deletion through the public
command contract.

The 28 skips are the repository's pre-existing intentional live-environment cases for real external
CLIs, real network access, or real-process SIGKILL boundaries. They are reported rather than hidden,
and none is a G5-04 required-now validation.

### Green execution receipt EWP-CMD-GC-TS01

- Validation ID: `EWP-CMD-GC-TS01`
- Command: `bun test packages/cli/tests/contracts/gc.test.ts --test-name-pattern EWP-CMD-GC-TS01`
- Exit status: `0`
- Result: 1 passed; 8 assertions.
- Revision: `c666de4feeb02fcb3b805e97b8fdf9510aabe6a4`

### Green execution receipt EWP-CMD-GC-TS02

- Validation ID: `EWP-CMD-GC-TS02`
- Command: `bun test packages/cli/tests/contracts/gc.test.ts --test-name-pattern EWP-CMD-GC-TS02`
- Exit status: `0`
- Result: 1 passed; 12 assertions.
- Revision: `c666de4feeb02fcb3b805e97b8fdf9510aabe6a4`

### Green execution receipt EWP-CMD-GC-TS03

- Validation ID: `EWP-CMD-GC-TS03`
- Command: `bun test packages/cli/tests/contracts/gc.test.ts --test-name-pattern EWP-CMD-GC-TS03`
- Exit status: `0`
- Result: 1 passed; 6 assertions.
- Revision: `c666de4feeb02fcb3b805e97b8fdf9510aabe6a4`

### Green execution receipt EWP-CMD-GC-TS04

- Validation ID: `EWP-CMD-GC-TS04`
- Command: `bun test packages/cli/tests/contracts/gc.test.ts --test-name-pattern EWP-CMD-GC-TS04`
- Exit status: `0`
- Result: 1 passed; 15 assertions.
- Revision: `c666de4feeb02fcb3b805e97b8fdf9510aabe6a4`

### Green execution receipt EWP-CMD-GC-TS05

- Validation ID: `EWP-CMD-GC-TS05`
- Command: `bun test packages/cli/tests/contracts/gc.test.ts --test-name-pattern EWP-CMD-GC-TS05`
- Exit status: `0`
- Result: 1 passed; 14 assertions.
- Revision: `c666de4feeb02fcb3b805e97b8fdf9510aabe6a4`

### Green execution receipt EWP-CMD-GC-TS06

- Validation ID: `EWP-CMD-GC-TS06`
- Command: `bun test packages/cli/tests/contracts/gc.test.ts --test-name-pattern EWP-CMD-GC-TS06`
- Exit status: `0`
- Result: 1 passed; 36 assertions.
- Revision: `c666de4feeb02fcb3b805e97b8fdf9510aabe6a4`

### Green execution receipt EWP-CMD-GC-TS07

- Validation ID: `EWP-CMD-GC-TS07`
- Command: `bun test packages/cli/tests/contracts/gc.test.ts --test-name-pattern EWP-CMD-GC-TS07`
- Exit status: `0`
- Result: 1 passed; 13 assertions.
- Revision: `c666de4feeb02fcb3b805e97b8fdf9510aabe6a4`

### Green execution receipt EWP-CMD-GC-TS08

- Validation ID: `EWP-CMD-GC-TS08`
- Command: `bun test packages/cli/tests/contracts/gc.test.ts --test-name-pattern EWP-CMD-GC-TS08`
- Exit status: `0`
- Result: 1 passed; 36 assertions.
- Revision: `c666de4feeb02fcb3b805e97b8fdf9510aabe6a4`

### Green execution receipt EWP-P5-TS04

- Validation ID: `EWP-P5-TS04`
- Command: `bun test tests/ergonomics/phase/EWP-P5-TS04.test.ts --test-name-pattern EWP-P5-TS04`
- Exit status: `0`
- Result: 1 passed; 8 assertions.
- Revision: `c666de4feeb02fcb3b805e97b8fdf9510aabe6a4`

### Green execution receipt EWP-WF12

- Validation ID: `EWP-WF12`
- Command: `bun test tests/ergonomics/workflows/EWP-WF12.test.ts --test-name-pattern EWP-WF12`
- Exit status: `0`
- Result: 1 passed; 12 assertions.
- Revision: `c666de4feeb02fcb3b805e97b8fdf9510aabe6a4`

## Traceability and sign-off

G5-04 is signed off on immutable product correction
`53b039ec177f0322e1f614eba2afb0f41d491f0f` plus downstream compatibility correction
`c666de4feeb02fcb3b805e97b8fdf9510aabe6a4`.

- The approved 93-path ownership boundary remained fixed through terminal closure.
- All ten required-now validations are executable, individually green, and green in the complete
  serialized repository gate.
- Focused core/application, ledger, smoke, recovery-smoke, type, boundary, catalog, package, plan,
  and diff checks are green.
- Fresh independent reviewer `/root/g5_04_final_review` returned GO on both the final product
  correction and the terminal-discovered compatibility correction, edited no files, and found no
  release blocker.
- EWP-CF-044's inventory, measurement, action, and crash-semantics amendment is implemented without
  widening the public contract beyond the accepted `gc@1` surface.
- The earlier 31-failure and six-failure terminal discoveries are retained above as causal evidence;
  neither was converted into a skip, filter, weakened equality, or undocumented exception.

The lifecycle catalog therefore advances P17-G5-04 and its 16 primary entities to `signed-off`, with
minimal implementation, targeted green, impacted green, refactor, adversarial review, traceability,
and signed-off gates backed by this evidence. G5-05 remains the next planned Phase 5 group; this
sign-off does not claim any G5-05 behavior.

Post-sign-off structural receipts:

```text
bun test scripts/p17-catalog.test.ts
56 pass; 0 fail; 267 assertions

bun scripts/p17-catalog.ts --check
valid: 426 entities (419 required, 7 deferred; 244 validation obligations), 45 groups;
deterministic checklist

bun scripts/check-p17-package.ts --check
structurally valid; Phase 5 active; 38/44 required groups signed off; 0 active; 6 planned;
372/419 required entities signed off; 47 incomplete; 7 deferred

bun scripts/check-p17-package.ts --pr-openable
PASS; 86 prep IDs; 23 local links; the same lifecycle rollup

bun scripts/p17-plan-structure.ts --check
valid: EWP-P0A-TS01, EWP-P0A-TS06, and EWP-P0A-TS07

git diff --check
PASS
```

## Whole-Phase-5 review findings and reopened correction — 2026-07-25

Fresh read-only external reviewer `/external-codex/019f9b51-c71d-7c02-91f5-c0e5a4e858f1`
reviewed immutable signed Phase-5 head `47a8d208bc87eabe45db498913894c29e67de22e` and returned
**NO-GO at 0.99 confidence**.

- **P17-RV-P5-F01 (medium, release-blocking):** the exact 93-path plan/catalog ownership set
  includes `packages/cli/tests/output/gc.test.ts`, but the path is absent from every G5-04 and
  Phase-5 reviewed tree and has no Git history. The earlier statement that all 93 paths remained
  fixed is historically retained above, but only 92 were blobs. The correction will materialize the
  promised dedicated renderer test rather than shrink the accepted boundary, and will add a general
  non-planned group-owned-file existence invariant.
- **P17-RV-P5-F02 (low, release-blocking):** the G5-04-owned ledger-history product module exposes
  a Bun-matcher-specific getter and permissive Proxy behind a diagnostic option. The correction will
  return ordinary frozen product data and replace the mutating asymmetric assertion with direct
  field assertions.

Focused product and recovery review found no adjacent runtime defect. GC passed 46 tests / 183
assertions; update/reconcile passed 158 / 1,005; sync passed 34 / 163; scheduler/doctor passed 14 /
84; history/retained/lineage passed 11 / 54; public F16-F20 recovery passed 10 / 100; Phase-5
acceptance passed 5 / 286; smoke passed 5 / 3,737; and recovery smoke passed 4 / 453. The isolated
undo stress test passed in 1.59 seconds with the required 60-second timeout.

G5-04 is therefore reopened at seven of ten gates. Its prior implementation and green evidence
remain valid, while adversarial review, traceability closure, and sign-off must be re-earned on an
immutable correction. Phase-5 closure remains pending and Phase 6 is untouched.

## F01/F02 correction candidate and focused green — 2026-07-25

The bounded correction candidate closes both review findings without changing GC selection,
reachability, deletion authority, recovery, or the public `gc@1` field vocabulary.

- **F01 dedicated output owner:** `packages/cli/tests/output/gc.test.ts` now exists and exercises a
  representative project, object, action, result, diagnostic, and summary through the strict human
  and JSON renderers and current runtime binding. The test exposed one adjacent omission: unlike
  update and undo, `gcV1Codec` did not reject source URLs or credential-shaped strings. The GC codec
  now uses the same shared sensitive-material authority and focused core/CLI regressions prove both
  schema and privacy failure closure. No redaction fork or renderer-only exception was added.
- **General ownership invariant:** every owned path of a non-planned, non-deferred group must now be
  a current regular file or have an exact Git-recorded deletion. Negative self-tests cover missing
  paths and directories; a positive self-test preserves intentional deletion provenance. The first
  global audit found seven absent entries: the two `config-notice` paths appear in two historical
  groups and have exact deletion history, while three never-materialized promises did not. The
  promised project-context and target-selection focused tests and doctor health@2 golden were
  materialized with executable coverage. The catalog now has no unexplained absent owned path.
- **F02 environment-neutral return:** `selectBoundedHistory` now returns one ordinary frozen value
  with a plain `cleanupVictim`; the Bun-specific getter and permissive Proxy are gone. The diagnostic
  trace remains typed and environment-neutral. All affected P3B and ledger-writer assertions read
  exact fields directly, so the test runner cannot replace frozen product properties through an
  asymmetric matcher.

Focused receipts on the correction candidate:

```text
bun test scripts/p17-catalog.test.ts
59 pass; 0 fail; 272 assertions

bun test packages/core/tests/context/project.test.ts \
  packages/core/tests/selection/resolve.test.ts \
  packages/core/tests/contracts/gc.test.ts \
  packages/cli/tests/output/gc.test.ts
10 pass; 0 fail; 39 assertions

bun test packages/core/tests/gc packages/core/tests/application/gc-service.test.ts \
  packages/core/tests/contracts/gc.test.ts packages/cli/tests/output/gc.test.ts --timeout 60000
51 pass; 0 fail; 212 assertions

bun test packages/cli/tests/output/gc.test.ts
3 pass; 0 fail; 18 assertions

bun test tests/ergonomics/phase/EWP-P1-TS10.test.ts \
  --test-name-pattern EWP-P1-TS10 --timeout 60000
10 pass; 0 fail; 1,807 assertions

bun test tests/ergonomics/phase/EWP-P3B-TS04.test.ts \
  --test-name-pattern EWP-P3B-TS04 --timeout 60000
20 pass; 0 fail; 20,231 assertions

bun test packages/core/tests/artifacts/ledger-writer.test.ts --timeout 60000
22 pass; 0 fail; 97 assertions

bun run test:smoke
5 pass; 0 fail; 3,737 assertions

bun run test:smoke:p2-ts04:recovery
4 pass; 0 fail; 453 assertions

bun run typecheck
PASS

bun run lint:boundaries
PASS

bun scripts/p17-catalog.ts --check
valid: 426 entities (419 required, 7 deferred; 244 validations), 45 groups;
deterministic checklist

bun scripts/check-p17-package.ts --pr-openable
PASS; Phase 5 active; G5-04 active at 7/10; Phase 6 planned
```

One intentionally overloaded ledger-writer aggregate crossed its internal time budgets while run
concurrently with TypeScript and other heavy suites, then cascaded through cleaned temporary paths.
All six affected cases passed as isolated serial replays, and the complete 22-test owner then passed
alone in 57.22 seconds. This was invocation load, not a product or assertion defect. Fresh immutable
review and the serialized terminal gate remain required before G5-04 can regain review/sign-off.

## F03 ownership-invariant review NO-GO and correction — 2026-07-25

Fresh read-only external reviewer
`/external-codex/019f9b7f-3bde-7580-97ba-b8d2b0f2a86a` reviewed immutable correction head
`5a4e5cd9f6db8a5131b25e323a19703c89a06e79` and returned **NO-GO at 0.99 confidence**. It
confirmed that P17-RV-P5-F01's concrete GC output owner and P17-RV-P5-F02's ordinary frozen ledger
return are substantively closed, all focused receipts are green, G5-04 owns exactly 93 current
regular blobs, G5-05 owns exactly 85, and all 445 Phase-5 ownership entries are regular blobs.

- **P17-RV-P5-F03 (medium, release-blocking):** the general ownership predicate normalized lexical
  `..` before checking it, inspected only the final path component with `lstat`, and classified a
  dangling symlink as absent through `existsSync`. It could therefore accept an in-repository
  traversal, a file reached through a symlinked parent, or a dangling symlink at an exact historical
  deletion path. The committed tree itself contains none of these invalid entries, but the validator
  did not fail closed for future governance states.
- **Correction boundary:** require canonical portable repository-relative grammar before any
  filesystem or Git check; walk every path component with `lstat` and reject all symlinks,
  non-directory parents, non-file leaves, and unreadable states; consult deletion provenance only
  for a canonically named genuinely absent path; preserve the exact latest `D\t<path>` equality.
  Add regressions for POSIX and Windows traversal, absolute paths, symlinked parents, dangling
  symlinks at a deleted path, stale/non-deletion latest history, ordinary absence/directories, and
  the valid exact-deletion case.

The exact terminal remains blocked until this correction is committed and a fresh immutable review
returns GO. G5-04 stays active at 7/10 and Phase 6 remains planned and untouched.

Focused correction receipt:

```text
bun test scripts/p17-catalog.test.ts
65 pass; 0 fail; 284 assertions

bun scripts/p17-catalog.ts --check
valid: 426 entities (419 required, 7 deferred; 244 validation obligations), 45 groups;
deterministic checklist

bun scripts/check-p17-package.ts --check
PASS; Phase 5 active; G5-04 active at 7/10; Phase 6 planned

bun scripts/check-p17-package.ts --pr-openable
PASS; 86 prep IDs; 23 local links; Phase 5 active; G5-04 active at 7/10; Phase 6 planned

bun scripts/p17-plan-structure.ts --check
valid: EWP-P0A-TS01, EWP-P0A-TS06, and EWP-P0A-TS07

bun run typecheck
PASS

bun run lint:boundaries
PASS

bunx biome check scripts/p17-catalog.ts scripts/p17-catalog.test.ts
PASS

git diff --check
PASS
```

The six added catalog cases reject lexical POSIX traversal, Windows traversal, an absolute path, a
regular leaf reached through a symlinked parent, a dangling symlink at an exact deleted path, and an
absent path whose latest exact Git event is not deletion. Existing ordinary-absence, directory, and
positive exact-deletion cases remain green. No product source, public contract, catalog lifecycle,
or Phase-6 state changed.

## F03 provenance re-review NO-GO and bounded correction — 2026-07-25

Fresh read-only external reviewer
`/external-codex/019f9b8f-812f-7a70-937b-0e62629593b3` reviewed immutable head
`1ee9e6dc235ee15777335f386831897b55a5f196` and returned **NO-GO at 0.99 confidence**. It
confirmed the portable grammar, static symlink rejection, focused validation results, exact current
ownership arithmetic, and untouched Phase-6 boundary, but kept P17-RV-P5-F03 open for deletion
provenance.

- Git's path-limited name-status output collapses the repository's historical
  `commitlint.config.js` rename-away into `D`, providing a concrete false acceptance.
- The deletion subprocess inherited repository-selecting Git environment variables and did not
  require a successful exit, so a foreign repository or Git error could supply or obscure
  provenance.
- `ENOTDIR` was classified as absence, and the earlier stale-history case covered an initial add
  rather than delete-then-add/modify or rename status.

The bounded second correction imports the same canonical repository-local Git environment denylist
as production, runs Git with both `-C <root>` and sanitized environment, requires successful
commands and one canonical lowercase commit identifier, and parses the complete unfiltered
commit's NUL-delimited name-status records. Only one exact `D` row for the owned path is accepted;
rename/copy participation, any later add or modification, malformed output, a foreign repository,
or a Git failure is rejected. Filesystem inspection now treats `ENOTDIR` as non-file, resolves the
physical root, verifies the final real path, and repeats a successful component walk to narrow
replacement races in this read-only governance check. All catalog Git receipt checks now use the
same sanitized repository binding.

Hermetic temporary repositories prove an exact latest deletion is accepted while delete-then-add,
delete-then-modify, explicit rename, ambient `GIT_DIR`/`GIT_WORK_TREE`, and Git-launch failure are
rejected. Integrated cases reject the concrete `commitlint.config.js` rename-away and a
non-directory parent. No product contract, lifecycle state, ownership count, or Phase-6 state
changed.

Focused correction receipt:

```text
bun test scripts/p17-catalog.test.ts
71 pass; 0 fail; 293 assertions

bun test tests/ergonomics/phase/EWP-P0A-TS09.test.ts \
  --test-name-pattern EWP-P0A-TS09 --timeout 60000
33 pass; 0 fail; 110 assertions

bun run test:smoke
5 pass; 0 fail; 3,737 assertions

bun scripts/p17-catalog.ts --check
valid: 426 entities (419 required, 7 deferred; 244 validation obligations), 45 groups;
deterministic checklist

bun scripts/check-p17-package.ts --check
PASS; Phase 5 active; G5-04 active at 7/10; Phase 6 planned

bun scripts/check-p17-package.ts --pr-openable
PASS; 86 prep IDs; 23 local links; Phase 5 active; G5-04 active at 7/10; Phase 6 planned

bun scripts/p17-plan-structure.ts --check
valid: EWP-P0A-TS01, EWP-P0A-TS06, and EWP-P0A-TS07

bun run typecheck
PASS

bun run lint:boundaries
PASS

bunx biome check scripts/p17-catalog.ts scripts/p17-catalog.test.ts
PASS

git diff --check
PASS
```

The exact terminal remains blocked until this candidate is committed and a fresh independent
review returns GO. G5-04 therefore remains active at 7/10, Phase-5 review/approval/exit remain
pending, and Phase 6 remains planned and untouched.

## F03 second-correction review NO-GO and fixture-isolation correction — 2026-07-25

Fresh read-only external reviewer
`/external-codex/019f9ba3-36a5-7960-b861-4fa1d24ddfa3` reviewed immutable head
`bcbe2a8c2d40ff83356195b9260637bb38b6dd31` and returned **NO-GO at 0.99 confidence**. It found
no remaining production false acceptance: path grammar, filesystem classification, intended-repo
Git binding, full-commit status parsing, exact deletion behavior, signed-receipt checks, and the
import-safe command modes were sound. It independently reproduced 878 regular unique owned paths
plus two exact deletions, 93/93 G5-04 blobs, and 445/445 Phase-5 ownership entries as blobs.

P17-RV-P5-F03 remained open for one adjacent test-isolation defect. The new temporary-repository
setup helper inherited ambient repository-selecting Git variables. Under the repository's pre-push
hook, `GIT_DIR` or related variables could redirect fixture `config`, `add`, or `commit` writes away
from the registered temporary directory, defeating both independence and cleanup. The bounded
correction removes that local helper and reuses the existing canonical `runGit` test fixture, which
scrubs the complete production denylist and pins global/system Git configuration before every
fixture command. No product, catalog state, ownership boundary, or Phase-6 file changes.

Sequential correction receipt (the two catalog lanes are intentionally serialized because older
cases temporarily exercise the same repository paths):

```text
bun test scripts/p17-catalog.test.ts
71 pass; 0 fail; 293 assertions

env GIT_DIR=/definitely/foreign.git GIT_WORK_TREE=/definitely/foreign \
  GIT_INDEX_FILE=/definitely/foreign.index \
  GIT_OBJECT_DIRECTORY=/definitely/foreign.objects GIT_CONFIG_COUNT=not-a-number \
  bun test scripts/p17-catalog.test.ts
71 pass; 0 fail; 293 assertions

bun run typecheck
PASS

bun run lint:boundaries
PASS

bun scripts/p17-catalog.ts --check
valid: 426 entities (419 required, 7 deferred; 244 validation obligations), 45 groups;
deterministic checklist

bun scripts/check-p17-package.ts --check
PASS; Phase 5 active; G5-04 active at 7/10; Phase 6 planned

bun scripts/check-p17-package.ts --pr-openable
PASS; 86 prep IDs; 23 local links; Phase 5 active; G5-04 active at 7/10; Phase 6 planned

bun scripts/p17-plan-structure.ts --check
valid: EWP-P0A-TS01, EWP-P0A-TS06, and EWP-P0A-TS07

bunx biome check scripts/p17-catalog.test.ts
PASS

git diff --check
PASS
```

The full terminal remains blocked until this fixture-only correction is committed and a fresh
independent review returns GO.

## F03 final adversarial review GO at b94e7ad

Fresh read-only external reviewer
`/external-codex/019f9bb0-cb94-7330-a0ee-8dc57878e679` reviewed immutable head
`b94e7ade08c3e390e347558f9932e6e37288e270` against parent
`bcbe2a8c2d40ff83356195b9260637bb38b6dd31` and the combined correction from
`1ee9e6dc235ee15777335f386831897b55a5f196`. It returned **GO at 0.99 confidence** with no
critical, high, medium, or low findings.

- The canonical test helper scrubs every production repository selector, pins global/system Git
  configuration, owns all fixture `init`/`config`/`add`/`commit` calls, and leaves no duplicate Git
  spawn in the catalog test.
- The production predicate accepts the two genuine exact deletions and rejects rename-away, stale
  add/modify, foreign-repository, Git-error, symlink, traversal, non-file, and `ENOTDIR` cases.
- Independent arithmetic is exact: 2,260 progressed-group ownership entries reduce to 880 unique
  paths, comprising 878 current blobs plus two exact deletions; G5-04 has 93/93 blobs, 16 primary
  entities, and ten required-now validations; all 445 Phase-5 group ownership entries are blobs.
- Phase 6 remains five planned groups with zero owned files, zero of 50 group gates passed, and 40
  primary entities with zero owned entries. No Phase-6 state or file changed.

P17-RV-P5-F03 is closed. G5-04 may advance from active at seven gates to reviewed at eight gates.
The remaining group gates are traceability closure and sign-off; the serialized full terminal must
pass before either is recorded.

## Post-F03 terminal and renewed sign-off at 4092eec

The accepted F03 correction and its independent review were recorded at immutable head
`4092eec9288a3dae00f70f4dfc203059a2d0d399`. The required serialized terminal then passed at that
exact head:

```text
bun run check -- --timeout 60000 --max-concurrency 1
3,217 pass; 28 intentional skips; 0 fail; 104,768 assertions
3,245 tests across 353 files; 1,594.43 seconds
PASS
```

The 28 reported skips are the repository's intentional real-CLI, real-network, and real-process
live-environment cases. None is one of G5-04's ten required-now validations. All ten required-now
selectors also passed in an immediate serialized individual replay at the same immutable head:

```text
EWP-CMD-GC-TS01  1 pass; 0 fail;  8 assertions
EWP-CMD-GC-TS02  1 pass; 0 fail; 12 assertions
EWP-CMD-GC-TS03  1 pass; 0 fail;  6 assertions
EWP-CMD-GC-TS04  1 pass; 0 fail; 15 assertions
EWP-CMD-GC-TS05  1 pass; 0 fail; 14 assertions
EWP-CMD-GC-TS06  1 pass; 0 fail; 36 assertions
EWP-CMD-GC-TS07  1 pass; 0 fail; 13 assertions
EWP-CMD-GC-TS08  1 pass; 0 fail; 36 assertions
EWP-P5-TS04      1 pass; 0 fail;  8 assertions
EWP-WF12         1 pass; 0 fail; 12 assertions
```

The structural replay passed with `71` catalog tests and `293` assertions, plus catalog generation,
package structure, PR-openable structure, plan structure, typecheck, boundary lint, and diff checks.
Fresh independent reviewer `/external-codex/019f9bb0-cb94-7330-a0ee-8dc57878e679` had already
returned GO at 0.99 confidence on the complete F03 correction with no critical, high, medium, or
low findings.

Traceability is therefore closed again over the unchanged 16-entity G5-04 primary set, the exact
93/93 owned blobs, and the ten executable required-now validations. The correction changed only
the lifecycle validator and its hermetic fixture coverage; it did not weaken the implemented GC
contract or alter any Phase-6 state. G5-04 and its 16 primary entities may return to `signed-off`,
with all ten lifecycle gates earned. Phase 5 remains active pending its whole-phase review,
approval, and exit.
