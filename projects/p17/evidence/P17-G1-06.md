# P17-G1-06 evidence

## Mapping and prerequisites

- **Primary IDs:** `EWP-CF-037`, `EWP-P1-T12`, and `EWP-P1-TS10`.
- **Formal dependency:** P17-G1-03 is signed off at
  `cd7ec024dc9beef0872dfd06eaa72024fffe2966`; its accepted implementation is
  `3fb4c1a86d424be9010f04394a5546a4eedd93ac`.
- **Preservation constraints:** P17-G1-04 is signed off at
  `8961e79a4931ab188ea6da0206df20165c85e9a9`, and P17-G1-05 is signed off at
  `3772a74dddd77ce282527395a7b1c612ce2b08a9` with accepted implementation
  `05f55938c5bced26f0c6d05dc2dae443b6201ea7`. G1-06 codecs remain pure, preserve focused ports,
  and derive verify/capability facts from the signed tool registry.
- **Required now:** exactly `EWP-P1-TS10`. `EWP-P2-TS08` remains downstream-only and owns
  persisted manifest, lock, plan, ledger, and journal codecs and migrations.
- Independent read-only mapper `/root/g1_06_mapping` identified the exact primary IDs, dependency,
  12 current JSON-capable command paths, ten shared output codecs, separate error/capability
  contracts, public-subpath gap, current renderer/schema duplication, false-red risks, and Phase-2
  exclusions. Independent ready and red-design audits were commissioned before lifecycle advance.
- Mapping found independent schemas/renderers for agents, commands, health, flip, install,
  uninstall, list, and verify; direct config serialization in the shared renderer; and an
  independently built error envelope. Install, uninstall, and flip currently remove domain-only
  `error` fields ad hoc. Current Zod object schemas default to stripping unknown keys rather than
  recursively refusing them.
- Current core exports only the root package entry point; dedicated `contracts`, `contracts/v1`,
  and `contracts/v2` entry points are absent.
- Focused current baseline passed 117 tests with 407 assertions across CLI output/runtime/check/
  verify and ledger characterization. The standalone spawned-help baseline passed 9 tests with 68
  assertions. Catalog generation and P17 PR-openable validation passed at G1-05 sign-off.
- Focused baseline command:
  `bun test packages/cli/tests/output packages/cli/tests/runtime/adapter.test.ts packages/cli/tests/contracts/check.test.ts packages/cli/tests/contracts/verify.test.ts packages/core/tests/place/ledger.test.ts packages/core/tests/place/ledger-additive.test.ts`.
- **Mapping result:** exact scope, prerequisites, current wire surfaces, byte-compatibility promise,
  validation ownership, public boundary, and prohibited downstream work mapped by root Codex goal
  on 2026-07-12.

## Ready plan

- **Plan:** `projects/p17/plans/P17-G1-06.md`.
- ADR 0008, one validated immutable codec registry, codec-derived DTOs, explicit domain mappers,
  exact 12-command closure, error/capability contracts, strict decoding, deterministic existing
  framing, and dedicated public subpaths are frozen as the implementation boundary.
- Existing embedded kinds/versions, absent envelopes, property order, formatting, omission behavior,
  and terminal-newline presence or absence remain byte-exact. Config get/list retain no embedded
  kind or schema version; versioning exists only in codec metadata.
- Root owns shared contracts, registry, exports, integration, catalog, evidence, and commits. V1 and
  V2 codec/mappers may proceed in parallel only after test-first and interface-freeze commits with
  disjoint ownership.
- Independent reviewer `/root/g1_06_ready_audit` rejected the first draft for contract-ID/version
  contradiction, underdefined public codec errors/signatures and capability DTO, unstated
  compatibility/framing, and an implicit core/CLI closure boundary. It accepted the corrected plan
  at 0.99 confidence after multi-version lookup, Result-based non-Zod APIs, exact breakage/framing
  policy, descriptor-only capability shape, and CLI-owned `CURRENT_COMMAND_SPECS` closure were
  frozen. It independently reproduced the 117/407 baseline.
- Independent red-design reviewer `/root/g1_06_red_design` found no remaining ready-plan blocker
  and confirmed the exact 12 live paths, config get/list legacy variants, terminal framing,
  parse-only-golden false-red risk, fixture/public compile strategy, mapper canaries, and AST-scoped
  ownership gates. Its characterization/authority split is the test-first boundary.
- Exact required command is
  `bun test tests/ergonomics/phase/EWP-P1-TS10.test.ts --test-name-pattern EWP-P1-TS10`.
- **Result:** ready for test-first implementation; no artifact migration, later operation-observer
  behavior, or later lifecycle gate is implied.

## Test-first evidence

- `EWP-P1-TS10` contains ten executable families. The live 12-command inventory and all 13
  committed final-stdout/framing goldens pass against current production. Eight families fail only
  on the absent canonical registry, codecs/mappers, strict Result decoding, public subpaths,
  CLI-owned mapping closure, and migrated ownership.
- The selector binds all 14 mapper cases—including scoped/unscoped config variants—to their codec
  and exact bytes; proves the error/capability/verify contracts; rejects hostile descriptor,
  mapping, version, migration, purity, immutability, strictness, and public-type cases; exercises
  all 12 mapped command paths; and uses parsed-TypeScript ownership gates with narrow exact-count
  exemptions.
- Standalone public compile fixtures freeze every root/v1/v2 symbol, codec/descriptor/Result/error
  shape, DTO inference, version closure, lifecycle internal-error exclusion, capability bundle/port/
  function exclusion, mapper result, and structural future codec. They fail only because the three
  planned public subpaths are absent.
- Initial independent reviewer `/root/g1_06_red_review` rejected revision
  `e521629e5f03e9bfea056702d13c86326673fdf6` for mapper, capability/verify, builder, strict-error,
  path-byte, public-surface, AST, and fixture-extension loopholes. Correction
  `8984c6470389ed270c6ce0ec0160bc9d550db679` closed every category.
- Fresh reviewer `/root/g1_06_red_final_review` inspected exact clean correction revision
  `8984c6470389ed270c6ce0ec0160bc9d550db679` and returned **PASS at 0.99 confidence**. It replayed
  the selector, scoped Biome, repository typecheck, catalog, P17 PR-openable, and diff gates and
  found no false red, unsafe cast, unimplementable assertion, overconstraint, missing current
  behavior, or Phase-2 scope theft.
- **Result:** `EWP-P1-TS10` is genuinely red against the planned G1-06 production authority; both
  characterization families remain green and every failure is actionable.

### Red execution receipt EWP-P1-TS10

- Validation ID: `EWP-P1-TS10`
- Command: `bun test tests/ergonomics/phase/EWP-P1-TS10.test.ts --test-name-pattern EWP-P1-TS10`
- Exit status: `1`
- Result: 2 passed, 8 failed, 89 assertions; exact current path and byte/framing characterization
  passed, while all eight planned authority families failed.
- Revision: `8984c6470389ed270c6ce0ec0160bc9d550db679`

## Implementation and validation

### Execution receipt EWP-P1-TS10

- Validation ID: `EWP-P1-TS10`
- Command: `bun test tests/ergonomics/phase/EWP-P1-TS10.test.ts --test-name-pattern EWP-P1-TS10`
- Exit status: `0`
- Result: 10 passed, 0 failed, 1,261 assertions. The exact 12-command inventory, immutable
  registry, strict codecs and errors, public DTO compilation, all named mappers, exact current
  bytes and framing, capability/verify derivation, closed public subpaths, AST ownership, and
  fixture extension families passed.
- Revision: `5d5d6780cd4caa5ca5cb7a58fb6160e84ad243bb`

- Initial implementation `e99907011c88592e31ddcbac89473e72e19b009c` added ADR 0008, the
  canonical registry and codecs, 12 CLI mappings, explicit domain-to-DTO mappers, current renderer
  integration, exact byte/framing preservation, and dedicated `contracts`, `contracts/v1`, and
  `contracts/v2` public subpaths.
- Hardening revision `5f3a35fa56307c47936886e8cc18dbcd45cd68db` added real migration
  ownership, descriptor/schema drift checks, private factory identity, structural-codec policy,
  recursive strictness, defensive registry copies, hostile dynamic-key preservation, declaration
  closure, and exact implementation-to-public DTO equality for all 12 contracts.
- Transformed-output revision `7974ed7a1e83420e9701ee38ba438305169e28ba` reparsed schema output and
  required a stable fixed point, rejecting transform-introduced version, kind, unknown-key, and
  accessor drift.
- Final correction `5d5d6780cd4caa5ca5cb7a58fb6160e84ad243bb` contains structural delegate
  throws, rejects exotic/sparse/accessor-bearing inputs before schema work, attests intrinsic
  prototypes, and makes repeated encoding deterministic with mutation-aware fingerprints covering
  aliases, nested identity, signed zero, prototypes, descriptors, extensibility, and array length.
- Standalone public consumer compilation passed with no Zod or internal-source leakage. The exact
  impacted baseline passed 117 tests with 407 assertions. Final `bun run check` passed Biome,
  dependency boundaries, typecheck, actionlint, P17 PR-openable validation, and 1,358 tests with 28
  intentional live skips, 0 failures, and 8,356 assertions across 163 files.
- **Gate result:** minimal implementation, targeted green, impacted green, and refactor passed at
  revision `5d5d6780cd4caa5ca5cb7a58fb6160e84ad243bb`.

## Adversarial review

- The initial implementation was rejected for unverifiable migration behavior, descriptor and
  factory-identity drift, parsed agents clones, and incomplete public declarations. Revision
  `5f3a35fa56307c47936886e8cc18dbcd45cd68db` closed those classes.
- Pinned reviewer `/root/g1_06_final_pinned_review` rejected that correction at 0.995 confidence
  because transformed schema output was not revalidated. After `7974ed7a1e83420e9701ee38ba438305169e28ba`,
  pinned reviewer `/root/g1_06_final_pinned_review_2` rejected at 0.999 confidence for cross-call
  stateful transforms, raw structural `decode`/`encode` throws, and inherited sparse-array getters.
- Independent correction audit `/root/g1_06_cache_audit` then reproduced array-length cache
  invalidation, shared-reference/cycle confusion, accepted prototype/descriptor/extensibility
  changes, signed-zero collision, standard/inherited prototype getters, and incomplete intrinsic
  state attestation. Every finding became a zero-read, no-throw, or stale-cache regression before
  the final commit; the audit returned PASS at 0.999 confidence.
- Fresh pinned reviewer `/root/g1_06_final_pinned_review_3`, with delegated static scope reviewer
  `/root/g1_06_final_pinned_review_3/static_scope_audit`, inspected exact clean revision
  `5d5d6780cd4caa5ca5cb7a58fb6160e84ad243bb` and returned **PASS at 0.998 confidence**. It replayed
  TS10 (10/10, 1,261 assertions), public compilation, the impacted baseline (117/117, 407
  assertions), and full check (1,358 pass, 28 skip, 0 fail, 8,356 assertions); HEAD and worktree
  stayed pinned and clean.
- The final review found no residual transformed-output, cache, accessor, proxy, sparse-array,
  structural containment, migration, dynamic-key, declaration, mapper, byte-closure, AST,
  extensibility, or Phase-2 scope blocker.
- **Gate result:** adversarial review accepted on 2026-07-12 America/Los_Angeles.

## Traceability and sign-off

- **Accepted revision:** `5d5d6780cd4caa5ca5cb7a58fb6160e84ad243bb`.
- `EWP-CF-037` and `EWP-P1-T12` resolve to ADR 0008, the validated immutable wire registry,
  canonical strict codecs, explicit mappers, exact current renderer bytes, public contract
  subpaths, the `EWP-P1-TS10` receipt, and fresh pinned acceptance.
- `EWP-P1-TS10` is signed off by the executable receipt above. `EWP-P2-TS08` remains
  downstream-only and retains ownership of manifest, lock, plan, ledger, and journal codecs and
  migrations; no Phase-2 artifact implementation moved into G1-06.
- Standing user authorization covers this group without a human-review pause; test-first,
  implementation, correction, adversarial, immutable-revision, and traceability gates all passed.
- **Traceability result:** passed by root Codex goal on 2026-07-12 America/Los_Angeles.
- **Group sign-off:** both primary coverage/work entities, the required-now validation entity, all
  ten lifecycle gates, focused and full validation, pinned reviewer acceptance, catalog closure,
  and generated checklist are complete. P17-G1-06 signed off by root Codex goal on 2026-07-12.
