# P17-G3B-06 evidence

## Mapping and prerequisites

- **Primary entities:** exactly two: phase task `EWP-P3B-T07` and phase selector
  `EWP-P3B-TS07`. Mapping advances only the owning group and its first lifecycle gate; both
  entities remain pristine `planned`.
- **Formal dependencies:** signed G3B-02 supplies deterministic scheduling, coordinator semantics,
  operation/result identity, partial-pair handling, and the distinction between started,
  cancelled, and skipped work. Its sign-off is `b8630f3` and its accepted implementation pin is
  `52133f3f824f8cf45d28d1ede65cb0522ef1d739`. Signed G1-07 supplies ADR 0009, immutable
  `OperationContext`, the closed 15-kind event registry, best-effort `ObservationEmitter`, shared
  CLI command correlation, redaction, and output isolation. Its accepted implementation pin is
  `9e0594b185bde7724a0f4a5ef5a14d7a932a3e46`.
- **Signed preservation baselines:** G3B-03 implementation
  `7b7d285eee051e24c7e5d7624c33e4cd5a2e9ac6` remains authoritative for journals, ledger writes,
  recovery, resume, abort, and repair; G3B-04 implementation
  `b7817ac4eb5aa5c85e12977e5721a4b990490631` remains authoritative for pure planners, immutable
  snapshots, repositories, expected revisions, and extracted execution; G3B-05 implementation
  `462ab46a0ed1911ba9ad586e2fd6d91143ab1545` remains authoritative for registry-bound lifecycle
  execution. These are integration constraints, not silent amendments to the accepted execution
  map.
- **Validation partition:** `EWP-P3B-TS07` is the sole required-now and impacted validation.
  Downstream coverage is empty. Signed `EWP-P1-TS11` remains the G1-07 selector and becomes a
  mandatory compatibility replay because its deliberate Phase-3B deferral gates must be narrowed;
  it is not reassigned to this group. `EWP-CF-038` remains signed under G1-07 with G3B-06 as its
  secondary closure group.
- **Already complete:** root CLI contexts and command events, immutable context derivation,
  operation/group/pair IDs, typed detection and verification events, observer failure isolation,
  and the journal-v1 scalar fields needed for correlation already exist. No second observation or
  transaction model is required.
- **Current residuals:** mutating application services discard their available observation bundle;
  planners have no post-plan event; coordinator/scheduler requests carry no context; acquisition
  and placement verification omit available observation; journals synthesize parent, command,
  workflow, and attempt values; and no mutating operation, transaction, rollback, resume, cleanup,
  or recovery lifecycle events are emitted. The TS07 owner file does not yet exist.
- **Accepted identity model:** retain the CLI-created root context as command identity. For each
  actually started planned operation, derive one internal child context whose `operationId` is the
  deterministic planned operation ID, whose parent is the root operation ID, and whose group/pair
  values match the plan. Recovery reconstructs the same operation identity and increments the
  attempt. Unstarted cancelled or skipped operations receive no fabricated start event.
- **Accepted event ownership:** the application/runner edge emits `plan.created` only after pure
  planning succeeds; the scheduler owns `operation.started/completed`; existing detection and
  verification wrappers own tool events; the physical durable journal-transition authority owns
  transaction stages and commit/rollback; and recovery wrappers own resume, rollback, cleanup, and
  recovery spans. A lifecycle fact has one emitter and is not duplicated across layers.
- **Journal boundary:** project only existing scalar context into journal-v1. Journal intent keeps
  operation/group/pair identity and the existing transaction ID; journal context keeps parent,
  command, workflow, attempt, and durable start time. Events and observer objects are never
  persisted, and no schema/version change is accepted. Ready must reconcile the signed pending-abort
  parent-ID rewrite with forward/resume correlation explicitly.
- **Purity and result authority:** planners, reducers, repositories, codecs, and state readers stay
  observation-free. Event emission is best-effort, cannot be awaited as authority, and cannot feed
  branches, results, exit codes, journal bytes, state, rollback, or recovery. Structured results and
  durable journals remain the only semantic authorities.
- **Consolidation boundary:** use the existing `ObservationBundle` and one focused private
  planned-operation context helper. Add optional internal propagation at application,
  coordinator/scheduler, execution, swap, and recovery seams. Do not add global context,
  AsyncLocalStorage, middleware/decorators, an event bus, telemetry SDK, observer queue, lifecycle
  framework, metadata bag, or public/wire context fields.
- **Runner constraint:** `packages/core/src/acquire/run.ts` is at its 3,955-line ceiling and
  `packages/core/src/place/run.ts` has one remaining line under its 2,917-line ceiling. G3B-06 must
  use vertical extraction and cannot solve propagation by growing either runner.
- **Test-first direction:** the exact future selector command is
  `bun test tests/ergonomics/phase/EWP-P3B-TS07.test.ts --test-name-pattern EWP-P3B-TS07`.
  Required families cover registry/static ownership, root-to-plan-to-operation derivation, success
  and partial-failure sequences, durable transaction stages, rollback/resume/cleanup attempts,
  fixture adapter verification/detection, throwing/rejecting/hanging observer isolation, proof
  events never drive semantics, output compatibility, runner ceilings, and prior-selector replay.
- **Verification policy:** ordinary iterations run TS07, changed focused tests, TS11, standard
  smoke, recovery smoke after coherent durability slices, typecheck, targeted formatting/boundary
  checks, package governance, and diff checks. P3B-TS03 through TS06 plus the complete P2-TS04 gate
  run at the targeted/impacted milestone after recovery behavior stabilizes. One clean committed
  `bun run check` plus exact TS07 replay is reserved for terminal G3B-06 sign-off.
- **Explicit exclusions:** no command, option, saved-plan/apply behavior, Phase-4/5 feature,
  public report or wire field, journal version, event persistence, adapter-specific branch,
  verification reorder solely for observation, new retry scheduler, or change to human/JSON bytes,
  exits, transaction semantics, rollback semantics, or recovery authority.
- **Independent review:** the scope audit found the propagation residual bounded and dependency
  ready at 97% confidence. The validation audit reproduced TS11, focused observation/execution,
  focused recovery/swap, standard smoke, and package governance on the clean baseline and accepted
  the selector boundary at 97%. The architecture/simplicity review accepted bounded propagation
  rather than a rewrite at 95%, with 98-99% confidence on planner purity and single event ownership.
- **Result:** exact scope, formal dependencies, signed integration baselines, identity hierarchy,
  event ownership, journal/purity boundaries, consolidation limits, test policy, and exclusions are
  mapped. No Ready, test-first, implementation, entity, validation, or sign-off gate is implied.

## Ready plan

- **Accepted plan:** [P17-G3B-06.md](../plans/P17-G3B-06.md), SHA-256
  `3e58c96078d3d6f323eb58e1146f7384be554c8647d8affd0d3d5affcdf37de2`.
- **Exact scope:** two primary entities, one required-now/impacted selector, no downstream
  coverage, and exactly 21 owned product/test paths: 19 architecture/production paths and two
  selector paths. Governance files remain outside the product manifest.
- **Identity hierarchy:** command contexts retain the CLI invocation operation ID and no parent;
  actually started planned operations use their deterministic plan operation ID with the command
  ID as parent and the plan group/pair IDs; transactions and recovery use the transaction ID with
  the journal intent operation ID as parent and the journal group/pair IDs. The private plan ID is
  `plan:v1:<sha256(canonicalPlanningString(operationPlan))>` and is not persisted or added to a
  public plan, report, or wire shape.
- **Propagation boundary:** the existing `ObservationBundle` remains authoritative. One private
  execution observation module supplies scalar context derivation and best-effort emission;
  private source-module-only `*Observed` seams carry it through application, scheduler,
  acquisition, placement, transaction, and recovery edges. Public package-root options, reports,
  exports, planners, repositories, codecs, state authorities, adapters, and CLI surfaces remain
  unchanged and observation-free.
- **Journal and recovery boundary:** new journals unconditionally project the intent operation ID
  into `context.parentOperationId`, independent of observer presence. Pending recovery increments
  attempt through one narrow pure reducer that updates the logical journal and matching physical
  shadow while preserving the original start time. Observer and no-observer execution produce
  identical corrected journal bytes; legacy null-parent journals remain valid and reconstruct
  event parentage with `parentOperationId ?? intent.operationId` without rewriting committed
  history solely for correlation.
- **Single ownership and durable stages:** CLI owns command events; application/read edges own
  `plan.created`; the scheduler owns events only for actually started operations; existing
  acquisition/verification seams own tool events; swap and ledger durable-transition authorities
  own transaction stages; and per-transaction recovery wrappers own resume, rollback, cleanup, and
  recovery spans. A stage completion acknowledges its durable write-ahead marker, so later physical
  rename failure does not emit a second completion for the same stage. Ledger migration emits live
  only at the authoritative durable handoff and never duplicates an already-proven cursor.
- **Selector contract:** the exact future command is
  `bun test tests/ergonomics/phase/EWP-P3B-TS07.test.ts --test-name-pattern EWP-P3B-TS07`.
  Ten executable families cover static ownership, deterministic plan identity, three-level context,
  unstarted work, all five operation outcomes and thrown-error mapping, durable swap crash windows,
  recovery attempts, ledger migration/doctor/acquisition seams, observer fault isolation, and
  compatibility/no-growth. TS11 narrows to the literal owned import/event boundary while preserving
  all decision-use bans.
- **Independent acceptance:** `/root/g3b06_scope_audit` accepted the 21-path scope and architecture
  at 98%; `/root/g3b06_validation_audit` accepted selector and replay completeness at 99%; and
  `/root/g3b05_final_simplicity_audit` accepted the private propagation, journal, cleanup, ledger,
  and reducer boundaries at 98%. No review found an architecture rewrite or additional path
  necessary.
- **Catalog transition:** P17-G3B-06 advances only from `mapped` to `ready` and passes only its
  Ready gate. `EWP-P3B-T07` advances from `planned` to `ready` with the same 21 paths and exact
  command. `EWP-P3B-TS07` remains `planned`; no test-first, implementation, validation, review,
  entity, or sign-off gate is implied.
- **Verification policy:** Ready receives deterministic catalog/checklist, package, diff, manifest,
  and governance validation only because runtime behavior is unchanged. TS07, TS11, focused tests,
  and standard smoke begin at test-first. Recovery smoke follows coherent durability slices; the
  complete repository suite remains reserved for terminal G3B-06 sign-off.
- **Result:** identity, ownership, journal/recovery correlation, durable stage semantics, observer
  isolation, public/private boundaries, selector obligations, exclusions, and the milestone test
  policy are frozen. G3B-06 is Ready for its implementation-free executable red selector.

## Test-first evidence

- **Committed boundary:** implementation-free commit
  `f775023af8708a44771175c65f7bf2d7a710f5c3` adds the sole TS07 selector and narrows the signed
  TS11 Phase-3B ownership deferral to the accepted 14 observation-capable paths, two deferred event
  literal authorities, and three observation-free durable-state paths. It changes no production
  code, public contract, journal schema, command output, recovery behavior, or architecture. The
  committed SHA-256 values are
  `59e12514d488da674e2e480e1a8c6c29e6acbd33cca441a0fa59c37e7c0f9a90` for TS07 and
  `ae292ee68265e0dfc54c0e3d73dccc2f211eb17fcf86b7426af0b38635fc7077` for TS11.
- **Characterization green:** ten tests and 42 assertions preserve the exact closed observation
  registry, canonical plan bytes, pre-aborted unstarted scheduling, all five execution result
  outcomes, physical swap ownership, pending-attempt source facts, canonical ledger cursors,
  generic acquisition detection, current observer isolation, public shapes, and runner ceilings.
- **Executable reds:** ten tests fail only on missing G3B-06 authority. They bind one private
  observation helper, canonical `plan.created`, actually-started operation contexts and outcome
  mapping, durable swap happy/crash/acquisition paths, atomic resume/rollback/cleanup recovery,
  seven-cursor migration notification and typed handed-off-to-live mapping, read-service doctor
  plan ownership plus a private observed doctor executor, novel registry-selected detection and
  verification, exact swap/recovery result-byte-filesystem parity under hostile observers, and
  private-only public compatibility.
- **Durability and isolation fixtures:** the selector uses deterministic temporary filesystem
  lanes only. It performs no Git initialization, subprocess, network, skip, or todo. One no-Git
  promote fixture is reused for happy swap, backed-up/live crash windows, recovery, and observer
  parity; one matching install operation proves acquisition-to-swap propagation. Cursor callbacks
  execute through interruption and resume, contain throws, notify all seven transitions once, and
  do not replay terminal recovery.
- **Architecture closure:** pure planners remain observation-free; the doctor read service owns
  empty-plan emission while a private observed executor owns repair operations; existing
  compatibility entry points stay unchanged while source-module `*Observed` seams are required;
  migration maps only prepared/staged/backed-up/handed-off-as-live/committed; the writer,
  ledger-persistence gateway, and logical reducer remain observation-free; and observer output
  cannot change result objects, ledger bytes, live/store hashes or contents, residue, rollback, or
  recovery.
- **Independent acceptance:** iterative review first blocked a Git-backed fixture, a pure-doctor
  planner violation, source-only cursor coverage, incomplete recovery classification, scheduler-only
  observer isolation, a false-green built-in detector, missing migration event mapping, and absent
  acquisition/rollback/cleanup paths. Corrections removed each issue. Final architecture and
  governance reviews both returned GO/PASS at 99% confidence; the proportionality review returned
  GO at 95% and accepted the 1,985-line selector as bounded below existing phase selectors.
- **Bounded validation:** the exact TS07 command exits 1 with ten passes, ten intentional failures,
  and 52 assertions. Characterization-only replay passes ten tests and 42 assertions. TS11 passes
  10/10 with 487 assertions. Standard smoke passes 5/5 with 3,735 assertions. TypeScript, targeted
  Biome, no skip/todo/subprocess/network scan, diff checks, commit hooks, and secret scanning pass.
  Initial parallel timeout failures were reproduced as resource contention and disappeared on
  sequential replay; TS11's AST ownership test receives a 60-second ceiling without assertion
  changes. The complete repository suite remains reserved for terminal G3B-06 sign-off.
- **Catalog transition:** P17-G3B-06 advances only from `ready` to `active` and passes only its
  test-first gate. `EWP-P3B-T07` advances from `ready` to `red`; `EWP-P3B-TS07` advances from
  `planned` to `failing` with its exact target, command, two owned selector paths, and receipt
  below. No minimal-implementation, targeted-green, impacted-green, refactor, review,
  traceability, or sign-off credit is implied.
- **Result:** the accepted G3B-06 architecture is now bound by a truthful, executable, hermetic
  red selector without production implementation or later-gate advancement.

### Red execution receipt EWP-P3B-TS07

- Validation ID: `EWP-P3B-TS07`
- Command: `bun test tests/ergonomics/phase/EWP-P3B-TS07.test.ts --test-name-pattern EWP-P3B-TS07`
- Exit status: `1`
- Result: 10 passed and 10 failed intentionally with 52 assertions; every failure reaches missing
  private plan, scheduler, transaction, recovery, migration, acquisition, doctor, isolation, or
  ownership authority while every characterization clause remains green.
- Revision: `f775023af8708a44771175c65f7bf2d7a710f5c3`

## Minimal implementation

- **Accepted implementation:** commit `3bd2759f4c4677b5d4591546951a209b6e639615`
  connects the existing command, planned-operation, transaction, and recovery identities through
  one private `execution/observation.ts` utility and source-only observed entry points. It adds no
  public package-root export, command/report field, wire shape, event kind, journal version,
  observer queue, global context, event bus, or second execution/recovery authority.
- **Single lifecycle ownership:** application/read and acquisition runner edges emit deterministic
  `plan.created`; the scheduler observes only actually started operations; acquisition reuses the
  registered detection/verification seams; swap and the writer cursor acknowledge durable
  transaction stages; and placement, migration, and committed-acquisition wrappers own recovery
  spans. Observer returns and failures do not participate in planning, persistence, cleanup,
  results, or error decisions.
- **Durable recovery identity:** logical placement recovery advances the pending journal and its
  matching physical shadow through one pure reducer before persistence. Ledger migration records a
  resumed attempt by CAS-amending the existing recovery pointer before forward work, preserves the
  exact journal image for every already-durable phase, advances only future phase images, and
  writes the final attempt into committed history before pointer cleanup. Retries therefore advance
  1→2→3; post-commit cleanup reuses the terminal attempt and emits no duplicate commit.
- **False-green corrections:** independent reviews found and closed legacy-only cleanup omission,
  an observation-specific public abort option, deleted runner-invariant comments, recovery spans
  that missed reducer/persistence failures, migration attempts finalized only after pointer
  deletion, and hostile-attempt observer parity at zero/negative and `MAX_SAFE_INTEGER` boundaries.
  Malformed pointers now validate all five attempt values before observation; an exhausted
  placement attempt returns the same normal failure with or without an observer.
- **Compatibility and proportionality:** planners, repositories, codecs, adapter interfaces,
  renderers, and public option/report types remain unchanged and observation-free. The existing
  ledger pointer schema remains version 1. `acquire/run.ts` is 3,942/3,955 lines and
  `place/run.ts` is 2,845/2,917 lines after vertical extraction into existing owned execution
  modules. All 17 changed product/test paths are inside the frozen 21-path manifest.
- **Exact selector and compatibility receipts:** `EWP-P3B-TS07` passes **24/24 with 281
  assertions** and SHA-256
  `076b9ca53f49eac45e241e137a4b29982cec0414c4036b33d590cadfed09c454`.
  Signed compatibility selector `EWP-P1-TS11` passes **10/10 with 487 assertions** and retains
  SHA-256 `ae292ee68265e0dfc54c0e3d73dccc2f211eb17fcf86b7426af0b38635fc7077`.
- **Focused and recurring receipts:** the required 13-file focused aggregate passes **202/202 with
  1,024 assertions**; the post-correction writer/recovery/swap lane passes **37/37 with 176
  assertions**; standard smoke passes **5/5 with 3,735 assertions**; and recovery smoke passes
  **4/4 with 453 assertions**.
- **Static, governance, and safety receipts:** TypeScript, targeted Biome over the exact 19 planned
  paths, boundary lint, catalog/checklist validation, package validation, `check:p17
  --pr-openable`, `git diff --check`, no skip/todo/subprocess/network scans, commit hooks, and
  Gitleaks pass. Catalog validation remains deterministic at 425 entities, 45 groups, and 244
  validation obligations.
- **Independent acceptance:** the final correctness re-audit returns **GO at 97% confidence** and
  the architecture/simplicity audit returns **GO at 96% confidence**. The latter records one
  nonblocking consolidation candidate: recovery observation currently projects through the same
  pure reducer before the authoritative persistence plan so thrown pre-persistence failures retain
  a truthful span. This is one authority, not a second framework, and remains for the explicit
  refactor gate to reassess.
- **Gate boundary:** only minimal-implementation passes on
  `3bd2759f4c4677b5d4591546951a209b6e639615`. Targeted-green, impacted-green, refactor,
  adversarial review, traceability, and sign-off remain pending. The full `EWP-P2-TS04` selector is
  reserved for the next major milestone, and the complete repository suite remains reserved for
  terminal G3B-06 sign-off.

## Minimal implementation correction and selector closure

- **Adversarial cleanup correction:** commit
  `b0c4aae313d5f6f22088b47e16417826628eb018` replaces per-target committed-acquisition
  canonicalization with one exact classifier, physical cleanup pass, and atomic terminal ledger
  projection. Canonical update/remove cleanup is now independent of target insertion order,
  preserves history and latest-model/CAS behavior, re-derives project registrations, safely
  reclaims only hash-authorized backups, and remains retry-idempotent after persistence failure.
- **Cancellation truth:** a pre-aborted canonical cleanup returns the typed `cancelled` error before
  filesystem mutation or persistence and closes its recovery span as `cancelled/cancelled`.
  Cancellation is not reclassified only for telemetry, and no abort check interrupts convergence
  after physical cleanup has begun.
- **Coverage-only closure:** commit
  `1c34062c17b0b3fa8cdf34683898f3606c21cec6` changes only
  `tests/ergonomics/phase/EWP-P3B-TS07.test.ts`. It makes completed-plan edges, continue-on-error
  and cancellation variants, marker failure/cancellation, rollback retry, doctor repair identity,
  and hostile-observer human/JSON exit parity directly executable. All formerly `red:` test names
  now use `contract:`; no passing test name retains the historical `red:` label.
- **Accepted boundaries:** the corrected product pin is
  `b0c4aae313d5f6f22088b47e16417826628eb018`; the combined product/selector pin is
  `1c34062c17b0b3fa8cdf34683898f3606c21cec6`. The final committed SHA-256 values are
  `33b8ed4f22f8d37e3386d4ed6537dc9ef8c165f681036dc6fbdbb7601ae0e298` for
  `packages/core/src/place/swap.ts`,
  `cd7e8cda70c6f7260808c3686c89aaa8cb08b4d2390d3302368453404215c709` for TS07, and the
  unchanged `ae292ee68265e0dfc54c0e3d73dccc2f211eb17fcf86b7426af0b38635fc7077` for TS11.
- **Independent acceptance:** cleanup architecture and correctness reviews return GO at 96-99%
  confidence. Final selector quality and traceability reviews both return GO at 98% confidence,
  finding all ten frozen families and all six formerly missing clauses executable with no
  false-green, product blocker, new framework, or required project-scoped duplicate fixture.

## Targeted and impacted green

- **Required selector:** exact `EWP-P3B-TS07` passes **31/31 with 421 assertions**, 0 failures, and
  0 skips. Its SHA-256 is
  `cd7e8cda70c6f7260808c3686c89aaa8cb08b4d2390d3302368453404215c709`.
- **Signed compatibility replay:** `EWP-P1-TS11` passes **10/10 with 487 assertions** and retains
  SHA-256 `ae292ee68265e0dfc54c0e3d73dccc2f211eb17fcf86b7426af0b38635fc7077`.
  `EWP-P1-TS08` passes **10/10 with 146 assertions**.
- **Phase-3B signed selectors:** `EWP-P3B-TS01` passes **1/1 with 206 assertions**,
  TS02 **1/1 with 316**, TS03 **14/14 with 172**, TS04 **20/20 with 20,227**, TS05 **26/26
  with 328**, and TS06 **20/20 with 147**. Every selector ran through its exact file and
  `--test-name-pattern` command on clean combined pin
  `1c34062c17b0b3fa8cdf34683898f3606c21cec6`.
- **Complete durable milestone selector:** the full `EWP-P2-TS04` selector passes **19/19 with
  49,356 assertions** in **308.19 seconds**. It includes the complete permission, SIGKILL, SIGINT,
  overlap, lock, and persisted-recovery crash matrices and was run once, alone, at this milestone.
- **Focused and recurring gates:** the accepted 13-file focused aggregate passes **202/202 with
  1,024 assertions** in **84.66 seconds**; standard smoke passes **5/5 with 3,735 assertions**;
  recovery smoke passes **4/4 with 453 assertions**.
- **Static, governance, and safety gates:** TypeScript, boundary lint, Biome over all 19 planned
  TypeScript paths, catalog/checklist validation, package validation, `check:p17 --pr-openable`,
  `git diff --check`, no skip/todo/subprocess/network scans, commit hooks, and Gitleaks over 423
  commits pass. Package validation remains deterministic at **425 entities, 45 groups, and 244
  validation obligations**.
- **Result:** targeted-green and impacted-green pass on combined pin
  `1c34062c17b0b3fa8cdf34683898f3606c21cec6`, with corrected product pin
  `b0c4aae313d5f6f22088b47e16417826628eb018`. There is no downstream coverage to advance.
  Refactor, adversarial review, traceability closure, and sign-off remain pending; the complete
  repository suite remains reserved for terminal G3B-06 sign-off.

### Final green execution receipt EWP-P3B-TS07

- Validation ID: `EWP-P3B-TS07`
- Command: `bun test tests/ergonomics/phase/EWP-P3B-TS07.test.ts --test-name-pattern EWP-P3B-TS07`
- Exit status: `0`
- Result: 31 passed, 0 failed, 0 skipped, 421 assertions.
- Revision: `1c34062c17b0b3fa8cdf34683898f3606c21cec6`

## Final-review ownership amendment

- **Trigger:** fresh architecture and adversarial review after the first targeted/impacted milestone
  found four narrow product regressions: hostile thrown-value inspection, record-only acquire
  observation loss, lifecycle/coordinator callback-shape leakage, and mid-batch committed cleanup
  cancellation. The product corrections remain inside the original 19 production paths.
- **Bounded test ownership:** before accepting or committing the correction tests, the exact
  product/test manifest expands from 21 to 27 paths by adding only
  `packages/core/tests/acquire/install-run.test.ts`,
  `packages/core/tests/acquire/uninstall-run.test.ts`,
  `packages/core/tests/application/lifecycle-services.test.ts`,
  `packages/core/tests/execution/coordinator.test.ts`,
  `packages/core/tests/execution/scheduler.test.ts`, and
  `packages/core/tests/place/recovery.test.ts`.
- **Why the overlap is necessary:** these focused tests uniquely bind both real-filesystem
  record-only acquisition branches, all five lifecycle override wrappers, the public coordinator
  callback arity, and Proxy trap avoidance/identity. Moving all of that into TS07 would duplicate
  existing fixtures and make the sole selector less focused. The amendment grants additive
  regression-test ownership only; it does not transfer prior-group production ownership or alter
  primary entities, validation ownership, commands, public APIs, or durable schemas.
- **Independent scope verdict:** the fresh correction re-audit accepted F02, F03, F04, F06, F07,
  documentation truth, runner ceilings, and all functional tests at 98% confidence, blocking only
  on the missing test-path amendment. This amendment closes that sole governance finding before
  the correction tests are retained in a commit.

## Refactor and adversarial review

- **Accepted correction pin:** test-only ownership amendment
  `8daa71d880828b6bd4375d7e0ed6dfb6d82fe739` precedes reviewed product/test correction
  `0673d2420615d44ccabbd4fe42b11d66e4e253e7`. The amended plan SHA-256 is
  `c470209d2a2e9f1900fdc9fd1f68d44dec250d97952801de094afd7c03516325`.
- **Finding closure:** `P17-RV-G3B-06-F01` corrects stale architecture documentation; F02 makes
  thrown-value classification total without invoking Proxy traps or replacing the semantic error;
  F03 honors cancellation before canonical cleanup but completes all targets once convergence
  starts; F04 gives the real-filesystem hostile-observer contract a bounded 30-second ceiling; F06
  propagates planned-operation observation through record-only install repair and stale uninstall;
  and F07 preserves historical public lifecycle and coordinator callback argument counts. No
  finding adds an event kind, durable schema, public API, workflow framework, or second authority.
- **Refactor verdict:** no additional refactor is required before sign-off. The observed wrappers
  are one-way private compatibility funnels into existing lifecycle owners, not duplicated
  frameworks. Pure planners, repositories, codecs, wire models, adapter interfaces, and public
  reports remain observation-free. `acquire/run.ts` is **3,944/3,955** lines and `place/run.ts` is
  **2,845/2,917**; the next acquisition feature must extract vertically before adding runner
  behavior, but extraction now would mix successor scope into a stable correction.
- **Independent acceptance:** `/root/g3b06_final_architecture` returns PASS at 98% with no
  architecture blocker or required refactor. `/root/g3b06_correction_reaudit` returns final PASS at
  99% after verifying the committed 27-path amendment closes its sole governance finding.
  `/root/g3b06_writer_design` independently accepts the record-only acquisition correction at 99%.
- **Corrected selector receipts:** exact TS07 passes **31/31 with 431 assertions** and SHA-256
  `997e419815f1d4ff970c6c7b0117617e4f6357923a13979dc4ec93b1a4f54520`. TS11 passes
  **10/10 with 487 assertions** and retains SHA-256
  `ae292ee68265e0dfc54c0e3d73dccc2f211eb17fcf86b7426af0b38635fc7077`. P1-TS08 passes
  **10/10 with 146 assertions**. P3B-TS01 through TS06 pass **82/82 with 21,396 assertions**.
- **Focused and durable receipts:** the amended 13-file focused aggregate passes **206/206 with
  1,065 assertions**. Standard smoke passes **5/5 with 3,735 assertions** and recovery smoke passes
  **4/4 with 453 assertions**. The milestone-only complete P2-TS04 selector passes **19/19 with
  49,356 assertions** in **356.75 seconds**.
- **Static and safety receipts:** TypeScript, Biome over every changed TypeScript path, boundary
  lint, catalog/package validation, `git diff --check`, no skip/todo/subprocess/network scans,
  commit hooks, and Gitleaks over 426 commits pass. The worktree is clean on the reviewed pin.
- **Gate result:** refactor and adversarial-review pass. G3B-06 is reviewed; terminal full
  repository validation, exact same-pin TS07 replay, traceability closure, and sign-off remain
  pending.
