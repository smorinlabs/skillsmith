# P17-G1-01 evidence

## Mapping and prerequisites

- **Primary IDs:** `EWP-CF-022`, `EWP-CF-024`, `EWP-CF-031`, `EWP-P1-T01..03`, `P0-01..03`,
  `EWP-OPT-TS06`, `EWP-P1-TS01..03`, and `EWP-WF14`.
- **Dependencies:** none; Phase 1 entry is passed. G1-02A is signed off and supplies the shared
  Commander/error/exit boundary that G1-01 must consume.
- **Required now:** exactly `EWP-OPT-TS06`, `EWP-P1-TS01`, `EWP-P1-TS02`, `EWP-P1-TS03`, and the
  Phase-1 context/selection slice of `EWP-WF14`; all future command and Phase-2+ workflow behavior
  remains downstream.
- Read-only mapper `/root/g1_01_map` verified current cwd/config/root defects, exact acceptance
  targets, core/CLI seams, downstream ownership, and the integration boundary with G1-02A.
- **Mapping result:** exact scope, validation impact, prerequisites, and integration order mapped by
  root Codex goal on 2026-07-12.

## Ready plan

- **Plan:** `projects/p17/plans/P17-G1-01.md`.
- Root owns shared exports and CLI integration; core context/config and selection work can proceed in
  disjoint slices after the exact red receipts exist.
- Exact red targets, minimal slices, current call sites, downstream obligations, and prohibited
  later-phase work are recorded.
- **Result:** ready for test-first implementation; no later lifecycle gate is implied.

## Test-first evidence

- `EWP-OPT-TS06` and `EWP-P1-TS03` exited 1 because the planned shared
  `packages/core/src/selection/resolve.ts` contract did not exist.
- `EWP-P1-TS01` and `EWP-WF14` exited 1 because the planned immutable
  `packages/core/src/context/project.ts` contract did not exist.
- `EWP-P1-TS02` exited 1 because the planned operational
  `packages/core/src/config/effective.ts` resolver did not exist.
- The five targets encode hermetic spawned and pure coverage for `-C`, Git/worktree/symlink/non-Git
  identity, config precedence/path bases, typed capability/selection behavior, bounded defaults,
  zero-match non-widening, and the truthful Phase-1 WF14 slice. Biome and diff checks passed.
- **Result:** all five required-now selectors are genuinely red at the group boundary; no shared
  context/config/selection implementation existed when recorded on 2026-07-12.

### Execution receipt EWP-OPT-TS06

- Validation ID: `EWP-OPT-TS06`
- Command: `bun test packages/cli/tests/contracts/options.test.ts --test-name-pattern EWP-OPT-TS06`
- Exit status: `1`
- Result: failed because the shared selection resolver module did not exist.
- Revision: `working-tree`

### Execution receipt EWP-P1-TS01

- Validation ID: `EWP-P1-TS01`
- Command: `bun test tests/ergonomics/phase/EWP-P1-TS01.test.ts --test-name-pattern EWP-P1-TS01`
- Exit status: `1`
- Result: failed because the immutable project-context module did not exist.
- Revision: `working-tree`

### Execution receipt EWP-P1-TS02

- Validation ID: `EWP-P1-TS02`
- Command: `bun test tests/ergonomics/phase/EWP-P1-TS02.test.ts --test-name-pattern EWP-P1-TS02`
- Exit status: `1`
- Result: failed because the operational effective-config module did not exist.
- Revision: `working-tree`

### Execution receipt EWP-P1-TS03

- Validation ID: `EWP-P1-TS03`
- Command: `bun test tests/ergonomics/phase/EWP-P1-TS03.test.ts --test-name-pattern EWP-P1-TS03`
- Exit status: `1`
- Result: failed because the shared selection resolver module did not exist.
- Revision: `working-tree`

### Execution receipt EWP-WF14

- Validation ID: `EWP-WF14`
- Command: `bun test tests/ergonomics/workflows/EWP-WF14.test.ts --test-name-pattern EWP-WF14`
- Exit status: `1`
- Result: failed because the immutable project-context module did not exist.
- Revision: `working-tree`
