# P17-G1-01 evidence

## Mapping and prerequisites

- **Primary IDs:** `EWP-CF-022`, `EWP-CF-024`, `EWP-CF-031`, `EWP-P1-T01..03`, `P0-01..03`,
  `EWP-OPT-TS06`, `EWP-P1-TS01..03`, and `EWP-WF14`.
- **Dependencies:** none; Phase 1 entry is passed. G1-02A is signed off and supplies the shared
  Commander/error/exit boundary that G1-01 must consume.
- **Required now:** exactly `EWP-OPT-TS06`, `EWP-P1-TS01`, `EWP-P1-TS02`, `EWP-P1-TS03`, and the
  Phase-1 context/selection slice of `EWP-WF14`; all future command and Phase-2+ workflow behavior
  remains downstream.
- Read-only mapper `/root/g1_01_map` verified current cwd/config/root defects, exact acceptance
  targets, core/CLI seams, downstream ownership, and the integration boundary with G1-02A.
- **Mapping result:** exact scope, validation impact, prerequisites, and integration order mapped by
  root Codex goal on 2026-07-12.

## Ready plan

- **Plan:** `projects/p17/plans/P17-G1-01.md`.
- Root owns shared exports and CLI integration; core context/config and selection work can proceed in
  disjoint slices after the exact red receipts exist.
- Exact red targets, minimal slices, current call sites, downstream obligations, and prohibited
  later-phase work are recorded.
- **Result:** ready for test-first implementation; no later lifecycle gate is implied.

## Test-first evidence

- `EWP-OPT-TS06` and `EWP-P1-TS03` exited 1 because the planned shared
  `packages/core/src/selection/resolve.ts` contract did not exist.
- `EWP-P1-TS01` and `EWP-WF14` exited 1 because the planned immutable
  `packages/core/src/context/project.ts` contract did not exist.
- `EWP-P1-TS02` exited 1 because the planned operational
  `packages/core/src/config/effective.ts` resolver did not exist.
- The five targets encode hermetic spawned and pure coverage for `-C`, Git/worktree/symlink/non-Git
  identity, config precedence/path bases, typed capability/selection behavior, bounded defaults,
  zero-match non-widening, and the truthful Phase-1 WF14 slice. Biome and diff checks passed.
- **Result:** all five required-now selectors are genuinely red at the group boundary; no shared
  context/config/selection implementation existed when recorded on 2026-07-12.

### Execution receipt EWP-OPT-TS06

- Validation ID: `EWP-OPT-TS06`
- Command: `bun test packages/cli/tests/contracts/options.test.ts --test-name-pattern EWP-OPT-TS06`
- Exit status: `0`
- Result: 6 passed, 0 failed, 27 assertions; grammar, non-widening, provenance, no-op, and help passed.
- Revision: `fab3e22f0a41b8bdf113bfb102e663b299223701`

### Execution receipt EWP-P1-TS01

- Validation ID: `EWP-P1-TS01`
- Command: `bun test tests/ergonomics/phase/EWP-P1-TS01.test.ts --test-name-pattern EWP-P1-TS01`
- Exit status: `0`
- Result: 5 passed, 0 failed, 19 assertions; Git, worktree, symlink, non-Git, and spawned `-C` passed.
- Revision: `fab3e22f0a41b8bdf113bfb102e663b299223701`

### Execution receipt EWP-P1-TS02

- Validation ID: `EWP-P1-TS02`
- Command: `bun test tests/ergonomics/phase/EWP-P1-TS02.test.ts --test-name-pattern EWP-P1-TS02`
- Exit status: `0`
- Result: 4 passed, 0 failed, 34 assertions; seven-layer precedence, path bases, env fallback, and flag override passed.
- Revision: `fab3e22f0a41b8bdf113bfb102e663b299223701`

### Execution receipt EWP-P1-TS03

- Validation ID: `EWP-P1-TS03`
- Command: `bun test tests/ergonomics/phase/EWP-P1-TS03.test.ts --test-name-pattern EWP-P1-TS03`
- Exit status: `0`
- Result: 8 passed, 0 failed, 96 assertions; pure and spawned enum/capability/selection matrices passed.
- Revision: `fab3e22f0a41b8bdf113bfb102e663b299223701`

### Execution receipt EWP-WF14

- Validation ID: `EWP-WF14`
- Command: `bun test tests/ergonomics/workflows/EWP-WF14.test.ts --test-name-pattern EWP-WF14`
- Exit status: `0`
- Result: 2 passed, 0 failed, 12 assertions; cross-command root/config identity and read-only behavior passed.
- Revision: `fab3e22f0a41b8bdf113bfb102e663b299223701`

## Implementation and validation

- Added one frozen `ProjectContext` that resolves effective cwd without `process.chdir`, discovers
  canonical Git/worktree or nearest non-Git roots, and keeps live project identity separate from
  discovered and explicit config paths. Every current project-aware adapter consumes that root.
- Added operational effective config with CLI > environment > explicit file > discovered project >
  user > system > defaults precedence, per-key source/path metadata, `SKILLSMITH_CONFIG` fallback,
  and flag-over-environment behavior after `-C`. List and commands consume its default tool.
- Added shared typed tool/scope/capability policies and target selection with explicit/bulk/bounded
  provenance, scope-first name/path/glob matching, ambiguity refusal, non-widening unmatched
  behavior, explained filter-to-zero no-op, and explicit absent-create gating.
- Migrated every live command family through typed validation. Unknown enums are usage exit 2;
  known unsupported mutation/verification tools and scopes are capability exit 4; no raw string is
  cast into a core adapter. The evolving Commander state records the known enum choices while the
  immutable v0.7 history and hashes remain unchanged.
- P0-01's remaining runtime/output, observer verbosity/debug, and final TTY/color behavior is now
  mechanically preserved downstream by `EWP-P1-TS07`, `EWP-P1-TS11`, and `EWP-P6-TS03`; G1-01
  does not claim or implement those prohibited later architecture slices.
- Exact required selectors at `fab3e22f0a41b8bdf113bfb102e663b299223701`: OPT06 6/6,
  TS01 5/5, TS02 4/4, TS03 8/8, and WF14 2/2. Full `bun run check` passed lint, boundaries,
  typecheck, actionlint, P17 integrity, and 1,199 tests with 28 conditional skips and zero failures.

## Adversarial review

- Independent reviewer `/root/g1_01_review` rejected initial revision
  `a2b6eaedf2e12581373bb8bd73bfb9e26d0790ae` with `P17-RV-G1-01-F01..F03`: live adapters
  bypassed typed validation, `SKILLSMITH_CONFIG` was ignored, and P0-01's later global-output
  obligations were not truthfully mapped.
- Correction revision `fab3e22f0a41b8bdf113bfb102e663b299223701` adds spawned live-command
  matrices, shared adapter policies, env/flag config precedence, and generator-enforced downstream
  ownership without pulling in prohibited runtime/observer/TTY architecture.
- The same independent reviewer replayed every F01/F02 exploit, verified F03's catalog generator and
  regression, reran all five exact selectors plus Commander-history closure, and accepted the
  corrected revision with no new blocker.
- **Gate result:** adversarial-review accepted on 2026-07-12 America/Los_Angeles.

## Traceability and sign-off

- **Implementation revision:** `fab3e22f0a41b8bdf113bfb102e663b299223701`; initial rejected
  implementation revision: `a2b6eaedf2e12581373bb8bd73bfb9e26d0790ae`.
- All fourteen primary entities resolve to the five executable receipts, reviewed implementation
  files, exact accepted findings/recommendations, and explicit downstream obligations. The mapping
  amendment is generated and regression-locked rather than prose-only.
- Required gates have no skip. Full repository verification is green, immutable historical
  Commander inputs are untouched, and no future command, manifest/lock writer, runtime, observer,
  or TTY implementation was introduced.
- **Traceability result:** passed by root Codex goal on 2026-07-12 America/Los_Angeles.
- **Group sign-off:** F01-F03 are closed and independently accepted; P17-G1-01 signed off by root
  Codex goal on 2026-07-12.
