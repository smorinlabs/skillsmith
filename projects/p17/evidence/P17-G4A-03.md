# P17-G4A-03 evidence

## Mapping and prerequisites

- **Primary entities:** exactly ten: `COMMAND:init`, `EWP-CF-041`,
  `EWP-CMD-INIT-TS01..TS05`, `EWP-P4A-T04`, `EWP-P4A-TS04`, and `P1-05`. Mapping
  advances only the owning group and its first lifecycle gate after accepted review; all ten
  entities remain pristine `planned` at this stage.
- **Validation partition:** exactly nine required-now validations: five init command selectors,
  signed option gates `EWP-OPT-TS03` and `EWP-OPT-TS04`, signed Phase-2 selector
  `EWP-P2-TS05`, and new phase selector `EWP-P4A-TS04`. `EWP-OPT-TS05` and `EWP-WF03`
  are immutable downstream coverage under Phase 6 and G4B-03 respectively and do not activate here.
- **Formal dependencies:** signed G2-04 supplies the path-free immutable init classifier and sole
  lossless conversion; signed G3B-03 supplies durable interruption/recovery; signed G3B-04 supplies
  immutable observations, revision guards, pure planning, and execution coordination. Signed
  Phase 1/2 surfaces supply shared context, config, artifact selection, codecs, command runtime,
  outputs, and one-member artifact coordination. No prerequisite is reopened or duplicated.
- **Product boundary:** one invocation selects one manifest, produces create/replace/migrate/noop/
  refusal, and either previews or applies exactly that immutable manifest effect. Lockfile, live,
  store, ledger, and unrelated artifacts remain byte-identical. Init performs no inventory or
  network work and has no approval step.
- **Mapped destination:** explicit file wins without rebasing project identity; explicit user uses
  XDG desired state; explicit project uses the Git root only for a Git context and always uses
  effective cwd outside Git, including below an ancestor discovered manifest; bare init uses Git
  root when present and user otherwise. Output reports the derived sibling path but no execution
  authority includes it.
- **Mapped defaults:** explicit tools and scope are persistent legacy intent. Otherwise existing
  effective configuration, then bounded writable-tool adapter detection, may seed declaration-empty
  defaults; zero detected tools leaves the field absent and detection errors refuse. When the
  automatic discovered project config is also the target, only semantically valid canonical or
  exact safely migratable legacy bytes participate as config; every other unchanged snapshot is
  omitted from merging and passed to the signed classifier, including future and unsafe legacy.
  Explicit `--config`/environment-config collision remains authoritative and invalid content
  refuses; valid canonical/safe-legacy same-path roles share one byte snapshot. Automatic destination
  scope alone does not invent persistent scope. Unrepresentable defaults refuse rather than drop.
- **Capability boundary:** writable Claude/Codex selections may seed the manifest. Explicit or
  effective Kilo/OpenCode, mixed writable/read-only sets, and effective read-only scope refuse with
  capability 4 before planning/coordination; unknown CLI values remain usage 2.
- **Planner gap:** signed `OperationImage` cannot truthfully represent mixed/empty/malformed/
  unknown/semantically-invalid canonical before bytes. The Ready candidate adds a closed
  digest/shape/path-only in-memory opaque manifest before-image accepted only by `write-manifest`;
  it adds no new executable operation kind, exposes no raw bytes, and does not change persisted
  `PlanImageV1`, `SavedPlanV1`, or saved-plan v1 codecs.
- **Scheduler/coordinator gap:** scheduler artifact validation currently rejects opaque before
  images and every null-pair conflict, while the execution coordinator misroutes an unknown image
  to ledger identity. The candidate permits only same-location `write-manifest` opaque before,
  only init replacement's exact destination conflict, and maps opaque to `manifest-bytes`; all
  prior command/artifact/placement cases remain closed and are replayed.
- **Force boundary:** replacement carries the existing bounded `destination-exists` conflict and
  reports `createBoundedForceEffect` fields for requested-unused preview and applied execution.
  Create/migrate/noop force is unused; force never implies yes or bypasses another safety boundary.
- **Digest boundary:** Phase-2/result before/after byte hashes stay in the `manifest-bytes` domain;
  operation images, resource revisions/preconditions, actual-before images, and opaque backup
  authorization use the `resource` domain. Both derive from the same owned bytes and are never
  copied across domains.
- **Report boundary:** `init@1` is a closed success DTO with exact request/default/selection/result/
  force/effect/summary unions. Refusal and failed/cancelled execution use sanitized shared error
  envelopes rather than pretending to be a fifth success action.
- **Execution gap:** the signed one-member coordinator owns the correct atomic/crash-safe physical
  seam but `recordFor` validates every backup as UTF-8/sensitivity-safe. The candidate adds an
  internal exact-resource-digest old-byte backup policy only for a validated init replacement,
  including a canonical before-image; it leaves candidate validation strict, moves old bytes only
  inside an owner-only transaction, records no content, and keeps the existing invalid-lock/
  lock-canonical branch and config-save callers strict.
  Shape/action/path validation closes at plan/scheduler/binding construction. Execution compares
  only the approved exact resource revision/digest/mode and planned after bytes; it never replans.
  Shared scheduler/precondition/result behavior remains authoritative.
- **Test-first direction:** create only `packages/cli/tests/contracts/init.test.ts` with five exact
  command owners and `tests/ergonomics/phase/EWP-P4A-TS04.test.ts` with the independent phase
  matrix. Required signed option and Phase-2 selectors remain green characterization until product
  activation.
- **Ownership candidate:** exactly 57 unique product/test paths: 29 architecture/production, 13
  focused tests, two catalog owners, and 13 bounded compatibility amendments. Governance files are
  outside product ownership. Newly proved overlap requires an evidence/Ready amendment before edit.
- **Verification:** focused exact selectors and units plus standard smoke during iteration;
  recovery smoke and full P2-TS04 at stable durability milestones; direct signed compatibility
  replays before green; clean committed full repository check and all nine exact required-now
  selectors at terminal.
- **Exclusions:** no WF03/OPT-TS05 activation, plan/apply or saved init plan, lock/live/ledger/store
  mutation, inventory import, network, approval, batch, multi-manifest, schema v2, or duplicate
  planner/coordinator/migration authority.
- **Result:** entity ownership, dependencies, validation partition, destination/default semantics,
  matrix, shared-planner gap, manifest-only execution seam, wire boundary, tests, ownership,
  verification, and exclusions are mapped in the Ready candidate. No catalog gate, test-first,
  implementation, green, review, traceability, entity, validation, or sign-off state advances until
  fresh independent Ready acceptance.

## Ready plan

Fresh independent reviewer `/root/g4a03_ready_review` returned **GO** at 0.97 confidence on
2026-07-18. The reviewer verified exact candidate hashes
`e1e4ce9db98723140d1b592a067adcc45f4be0349b36d901337ad2e2919e5e6e` for the plan and
`b9328d9f1aa82dd7f4f18c0af95e25acc65be00eb73dee92bceb60b069f5c1cc` for the pre-acceptance
evidence. The accepted plan owns exactly 57 unique paths: 29 architecture/production, 13 focused
tests, two selector owners, and 13 bounded compatibility amendments.

The review established and closed twelve stable findings before acceptance:

- `P17-RV-G4A-03-F01` corrected non-Git explicit project destination to effective cwd;
- `F02` added the exact bounded destination conflict and force effect;
- `F03` kept runtime opaque images out of persisted saved-plan v1;
- `F04` closed the success/refusal DTO and effect/count unions;
- `F05` pinned known read-only tool/scope capability exit 4;
- `F06` separated automatic invalid-target config exclusion from explicit config authority;
- `F07` restored explicit/config/writable-detection defaults precedence;
- `F08` mapped exact resource-domain, owner-only opaque backup and invalid-lock compatibility;
- `F09` added scheduler/execution-coordinator ownership and narrow init validation;
- `F10` removed execution-time replanning;
- `F11` separated manifest-byte result hashes from resource operation/revision hashes; and
- `F12` closed impossible init DTO cross-field combinations.

The reviewer independently confirmed the ten primary entities, nine required-now validations, two
downstream obligations, and signed G2-04/G3B-03/G3B-04 prerequisites. The existing required-now
characterizations passed before Ready: `EWP-OPT-TS03` 10 tests/387 assertions,
`EWP-OPT-TS04` 6/88, and `EWP-P2-TS05` 6/1,072. Deterministic catalog validation, package
structure, P17 plan structure, and `git diff --check` pass with no product/test edits.

**Ready result:** PASS. G4A-03 and work entity `EWP-P4A-T04` may advance to `ready`; all coverage
and validation primaries remain pristine `planned` until their owning lifecycle gates. No
test-first, implementation, green, review, traceability, or sign-off claim is implied.

## Test-first evidence

- Added exactly the two Ready-owned selector files and no production path:
  `packages/cli/tests/contracts/init.test.ts` with five independently selectable command families,
  and `tests/ergonomics/phase/EWP-P4A-TS04.test.ts` with the independent phase boundary.
- The selector hashes are
  `7367cf95995b6bbda5f37fb2594761e6160e67924950a030294711f585349888` and
  `63f3560bf49001c431924e49324d5dd69721eaa843027e1ae2b78e3e760e770f` respectively.
- Each of the six exact new commands exited 1 with one selected failing test and zero selected
  passes. The failures are the intended absent-product boundary: bare init returns top-level help
  and creates no manifest; optioned init rejects `--file`; init help lacks the grammar.
- Both files pass Biome, diff hygiene, and no-skip/no-todo/no-network guards. They use only temporary
  HOME/XDG/work directories and local process execution.
- The three signed required-now characterizations remain green after the red files land:
  `EWP-OPT-TS03` 10 tests/387 assertions, `EWP-OPT-TS04` 6/88, and `EWP-P2-TS05` 6/1,072.

**Test-first result:** PASS. G4A-03 becomes `active`, work entity `EWP-P4A-T04` becomes `red`, and
the five init command validations plus `EWP-P4A-TS04` become `failing` with exact executable
targets. Coverage primaries and signed required-now validations do not advance. No production
implementation or green gate is claimed.

## Red execution receipt EWP-CMD-INIT-TS01

- Validation ID: `EWP-CMD-INIT-TS01`
- Command: `bun test packages/cli/tests/contracts/init.test.ts --test-name-pattern EWP-CMD-INIT-TS01`
- Exit status: `1`
- Result: 0 passed, 1 failed, and 4 filtered. Bare init returned exit 0/top-level help but the exact
  XDG manifest was absent (`ENOENT`), proving missing registration/execution rather than a fixture
  error.
- Revision: `working-tree`

## Red execution receipt EWP-CMD-INIT-TS02

- Validation ID: `EWP-CMD-INIT-TS02`
- Command: `bun test packages/cli/tests/contracts/init.test.ts --test-name-pattern EWP-CMD-INIT-TS02`
- Exit status: `1`
- Result: 0 passed, 1 failed, and 4 filtered. The CLI returned structured
  `commander.unknownOption` for `--file` instead of the exact explicit selection preview.
- Revision: `working-tree`

## Red execution receipt EWP-CMD-INIT-TS03

- Validation ID: `EWP-CMD-INIT-TS03`
- Command: `bun test packages/cli/tests/contracts/init.test.ts --test-name-pattern EWP-CMD-INIT-TS03`
- Exit status: `1`
- Result: 0 passed, 1 failed, and 4 filtered. The CLI rejected `--file` before the selected-manifest
  force replacement and sibling-canary assertions could run.
- Revision: `working-tree`

## Red execution receipt EWP-CMD-INIT-TS04

- Validation ID: `EWP-CMD-INIT-TS04`
- Command: `bun test packages/cli/tests/contracts/init.test.ts --test-name-pattern EWP-CMD-INIT-TS04`
- Exit status: `1`
- Result: 0 passed, 1 failed, and 4 filtered. The CLI rejected `--file` before producing the
  immutable dry-run operation required for execution equality.
- Revision: `working-tree`

## Red execution receipt EWP-CMD-INIT-TS05

- Validation ID: `EWP-CMD-INIT-TS05`
- Command: `bun test packages/cli/tests/contracts/init.test.ts --test-name-pattern EWP-CMD-INIT-TS05`
- Exit status: `1`
- Result: 0 passed, 1 failed, and 4 filtered. `init --help` rendered top-level help without
  `--file <path>`, proving the absent declarative grammar/help/completion surface.
- Revision: `working-tree`

## Red execution receipt EWP-P4A-TS04

- Validation ID: `EWP-P4A-TS04`
- Command: `bun test tests/ergonomics/phase/EWP-P4A-TS04.test.ts --test-name-pattern EWP-P4A-TS04`
- Exit status: `1`
- Result: 0 passed and 1 failed. The independent phase preview received
  `commander.unknownOption --file` instead of a planned one-manifest operation.
- Revision: `working-tree`

## Minimal implementation

- **Accepted implementation pin:** `88d54958eee76bbb53f2c84992c1aef00008f516` adds the complete
  init product and test surface in the 57-path Ready ownership boundary.
- **Shared planning and execution:** init now prepares one immutable create, replace, migrate, or
  noop manifest operation and executes that exact plan through the existing scheduler,
  precondition coordinator, and one-member artifact coordinator. Dry-run and execution share the
  prepared operation; execution never replans and never mutates the sibling lock.
- **Bounded opaque replacement:** a runtime-only, exact-resource-digest opaque manifest image
  authorizes backup of the selected legacy bytes. The authority is closed to an exact ordinary
  data object and exact digest, keeps candidate validation strict, preserves mode, and leaves
  saved-plan v1 and every non-init coordinator caller unchanged.
- **Application and contract boundary:** declarative grammar, explicit/config/detected defaults,
  project/XDG/explicit destinations, capability refusals, force behavior, normalized registry
  identities, strict `init@1`, exact public types, and human/JSON rendering are registered through
  the current application-service and wire registries.
- **Compatibility and hygiene:** the implementation pin passed TypeScript, changed-file Biome,
  boundary lint, `git diff --check`, pre-commit gitleaks, and the focused 13-file aggregate. Signed
  public-wire, option-registry, durability, smoke, recovery-smoke, and bounded compatibility lanes
  remained green during implementation.
- **Result:** the minimal-implementation gate passes on
  `88d54958eee76bbb53f2c84992c1aef00008f516`. Targeted and impacted green remain pending until the
  nine exact required-now selectors are replayed on this immutable pin.

## Targeted and impacted green

- **Accepted green sequence:** the complete implementation remains pinned at
  `88d54958eee76bbb53f2c84992c1aef00008f516`; refactor correction
  `47f1046e259ac9c7dd4c92680c9cede23f9d234e` preserves an execution-time observation permission
  failure through the shared precondition coordinator instead of collapsing it to state exit 3.
- **Required-now selectors:** all nine exact commands pass on the corrected committed bytes: the
  five init command owners pass **12/12 with 141 assertions**, `EWP-OPT-TS03` passes **10/10 with
  393**, `EWP-OPT-TS04` passes **6/6 with 88**, `EWP-P2-TS05` passes **6/6 with 1,072**, and
  `EWP-P4A-TS04` passes **2/2 with 20**. Total: **36 tests and 1,714 assertions**.
- **Owned focused proof:** the coordinator is isolated because Bun executes aggregate files in
  parallel against a strict five-second durability timeout. It passes **74/74 with 577 assertions**;
  the other twelve focused files plus the corrected init-run regression pass **71/71 with 487**.
  Total focused proof: **145/145 with 1,064 assertions**. Two contended aggregate attempts timed out
  one otherwise-green coordinator row; isolated replay closed both rows in 175 ms and 85 ms.
- **Direct compatibility:** P1-TS03/05/07/10/11 pass respectively **8/96**, **7/113**, **16/347**,
  **10/1,653**, and **10/487**. P3B-TS01/03/04/05/06/07 pass respectively **1/206**,
  **14/172**, **20/20,227**, **26/328**, **20/147**, and **31/431**. The unchanged saved-plan
  codec/repository characterization passes **20/20 with 245 assertions**.
- **Durability and static proof:** full signed `EWP-P2-TS04` passed **19/19 with 49,356 assertions**
  after the stable opaque-backup milestone. After the permission refactor, standard smoke passes
  **5/5 with 3,735**, recovery smoke **4/4 with 453**, and the directly affected P3B-TS03 passes
  **14/14 with 172**. TypeScript, changed-file Biome, boundary lint, gitleaks hooks,
  `git diff --check`, no-skip/no-todo/no-network guards, and deterministic P17 package validation
  pass.
- **Parallel replay note:** one nine-way supporting replay produced expected bounded
  `init-lock-contention` refusals in TS03, TS04, and P4A-TS04 under host pressure. Those failures do
  not receive green credit; the exact commands were rerun serially on the same commit and passed
  **4/49**, **1/13**, and **2/20**.
- **Result:** targeted-green and impacted-green pass on
  `47f1046e259ac9c7dd4c92680c9cede23f9d234e`. Downstream `EWP-OPT-TS05` and `EWP-WF03` remain
  planned obligations and receive no status advance.

### Final green execution receipt EWP-CMD-INIT-TS01

- Validation ID: `EWP-CMD-INIT-TS01`
- Command: `bun test packages/cli/tests/contracts/init.test.ts --test-name-pattern EWP-CMD-INIT-TS01`
- Exit status: `0`
- Result: 2 passed, 0 failed, 10 filtered, 16 assertions.
- Revision: `47f1046e259ac9c7dd4c92680c9cede23f9d234e`

### Final green execution receipt EWP-CMD-INIT-TS02

- Validation ID: `EWP-CMD-INIT-TS02`
- Command: `bun test packages/cli/tests/contracts/init.test.ts --test-name-pattern EWP-CMD-INIT-TS02`
- Exit status: `0`
- Result: 3 passed, 0 failed, 9 filtered, 35 assertions.
- Revision: `47f1046e259ac9c7dd4c92680c9cede23f9d234e`

### Final green execution receipt EWP-CMD-INIT-TS03

- Validation ID: `EWP-CMD-INIT-TS03`
- Command: `bun test packages/cli/tests/contracts/init.test.ts --test-name-pattern EWP-CMD-INIT-TS03`
- Exit status: `0`
- Result: 4 passed, 0 failed, 8 filtered, 49 assertions.
- Revision: `47f1046e259ac9c7dd4c92680c9cede23f9d234e`

### Final green execution receipt EWP-CMD-INIT-TS04

- Validation ID: `EWP-CMD-INIT-TS04`
- Command: `bun test packages/cli/tests/contracts/init.test.ts --test-name-pattern EWP-CMD-INIT-TS04`
- Exit status: `0`
- Result: 1 passed, 0 failed, 11 filtered, 13 assertions.
- Revision: `47f1046e259ac9c7dd4c92680c9cede23f9d234e`

### Final green execution receipt EWP-CMD-INIT-TS05

- Validation ID: `EWP-CMD-INIT-TS05`
- Command: `bun test packages/cli/tests/contracts/init.test.ts --test-name-pattern EWP-CMD-INIT-TS05`
- Exit status: `0`
- Result: 2 passed, 0 failed, 10 filtered, 28 assertions.
- Revision: `47f1046e259ac9c7dd4c92680c9cede23f9d234e`

### Final green execution receipt EWP-OPT-TS03

- Validation ID: `EWP-OPT-TS03`
- Command: `bun test packages/cli/tests/contracts/options.test.ts --test-name-pattern EWP-OPT-TS03`
- Exit status: `0`
- Result: 10 passed, 0 failed, 28 filtered, 393 assertions.
- Revision: `47f1046e259ac9c7dd4c92680c9cede23f9d234e`

### Final green execution receipt EWP-OPT-TS04

- Validation ID: `EWP-OPT-TS04`
- Command: `bun test packages/cli/tests/contracts/options.test.ts --test-name-pattern EWP-OPT-TS04`
- Exit status: `0`
- Result: 6 passed, 0 failed, 32 filtered, 88 assertions.
- Revision: `47f1046e259ac9c7dd4c92680c9cede23f9d234e`

### Final green execution receipt EWP-P2-TS05

- Validation ID: `EWP-P2-TS05`
- Command: `bun test tests/ergonomics/phase/EWP-P2-TS05.test.ts --test-name-pattern EWP-P2-TS05`
- Exit status: `0`
- Result: 6 passed, 0 failed, 1,072 assertions.
- Revision: `47f1046e259ac9c7dd4c92680c9cede23f9d234e`

### Final green execution receipt EWP-P4A-TS04

- Validation ID: `EWP-P4A-TS04`
- Command: `bun test tests/ergonomics/phase/EWP-P4A-TS04.test.ts --test-name-pattern EWP-P4A-TS04`
- Exit status: `0`
- Result: 2 passed, 0 failed, 20 assertions.
- Revision: `47f1046e259ac9c7dd4c92680c9cede23f9d234e`

## Refactor

- The simplicity audit found one semantic error-mapping leak: both prepared precondition and
  actual-before observation failures were sanitized by the shared coordinator into state errors,
  losing init's permission exit. Correction `47f1046e259ac9c7dd4c92680c9cede23f9d234e` records the
  local typed observation failure and restores it only after the coordinator returns; cancellation
  retains precedence and the shared coordinator remains unchanged.
- Plan and execution retain one classifier call, one immutable prepared product, one scheduler,
  one precondition coordinator, and one artifact coordinator. Runtime opaque authority stays in the
  init binding; public reports, saved-plan v1, invalid-lock replacement, and strict configuration
  save callers remain closed.
- Small repeated machine-bound resource projections remain local to pure planning and execution
  modules. Extracting a new shared init framework for two private constructors would increase
  coupling without removing policy or effect authority.
- **Result:** no known required refactor or architecture debt remains. The refactor gate passes on
  `47f1046e259ac9c7dd4c92680c9cede23f9d234e`; independent adversarial review remains pending.

## Independent adversarial review and Ready amendment

Fresh independent reviewer `/root/g4a03_adversarial_review` returned **BLOCK** at 0.99 confidence
against committed revision `537ef4dd5f7b79b1f7cf1c0f51f7b98515314ff2`. Findings
`P17-RV-G4A-03-F13..F22` cover snapshot-bound config existence, sole unsafe-legacy classification,
inode/parent revision guards, observation error taxonomy, exact old-byte backup authority, proxy
reflection safety, post-commit cancellation reporting, strict `init@1` cross-field validation,
wire digest-domain proof, and substantive exact-selector coverage.

The finding reopens targeted green, impacted green, refactor, and independent adversarial review at
the earliest affected gates. Corrections are in progress; no prior green/refactor receipt advances
the reopened gates, and the adversarial gate remains pending until the same independent reviewer
returns a refreshed non-blocking verdict over the corrected committed revision.

Two findings require this explicit Ready amendment:

- **F17:** `opaqueManifestBackup` is an internal exact-resource-digest authorization for the old
  bytes of every validated init `replace-manifest` conflict, including a semantically valid
  canonical before-image. It skips only old backup-sink validation. Candidate bytes remain strict;
  create, migrate, noop, config, lock, and all non-init callers omit the authority. Runtime
  `opaque-manifest` operation-image acceptance is unchanged and remains closed.
- **F21:** `init@1` keeps unbranded `ArtifactDigest` strings for compatibility, so a decoder cannot
  distinguish arbitrary swaps between byte and semantic digest fields. The enforceable v1 contract
  is producer-side domain-specific derivation and mapping, proved with known fixture values, while
  hostile cross-field DTO combinations still reject. Tagged digest representation is reserved for
  a future wire version.

These amendments narrow the proof obligation without adding a path, operation kind, public field,
or mutation authority. The accepted 57-path product/test ownership union remains unchanged. The
amended plan and all F13..F22 dispositions must be revalidated by independent review before sign-off.

Reviewer refresh identified and closed one wording tension in the amended Ready text: generic
failed/cancelled no-application language now explicitly applies only while durable state remains
before. A cancellation observed after durable commit retains the truthful internal applied-state
report required by F19, while the CLI continues to emit only the shared cancelled error envelope.

## Adversarial correction and reopened green

- **Correction pins:** Ready amendment `21be4fb`, product/test correction
  `6b1f2fb5f08a73064c1a8044ddb5f5e89adf8f2f`, and governance-only durable-cancellation
  clarification `07be49203359ac6f5bece8303bb55cbf4a78499b`. The final amended Ready plan hash is
  `c0f4e682b2af705b1c80ac7b6b5a8bd1d88eab8591ed95b3509f75006735561f`.
- **F13/F14:** effective config accepts caller-bound existence and byte snapshots; automatic target
  eligibility uses the same safe canonical/legacy inspection and exact migration authority as init
  planning. Deterministic appearance, disappearance, and parseable unsafe-legacy rows pass while
  explicit same-path config remains authoritative.
- **F15/F16:** observed facts now include file identity and parent revision through planning,
  preconditions, actual-before, and final edit. Same-byte inode and parent replacements refuse;
  only coordinator-owned missing-parent materialization is accepted. ENOENT/ESTALE, EACCES/EPERM,
  EIO, and cancellation retain state, permission, failure, and cancelled taxonomy respectively.
- **F17/F18:** every validated replacement conflict supplies exact-resource-digest old-byte backup
  authority, including canonical secret-looking comments, while candidates and all non-init callers
  stay strict. Nonobjects and proxies reject before any reflective operation; the proxy trap canary
  remains zero.
- **F19/F20/F21:** precommit and post-durable cancellation now retain truthful before/after durable
  state and applied mutation facts. Strict `init@1` validation closes scope/selection, destination,
  path, registry, sensitive-value, tool-source, force, effect, and summary cross-products. Hard-coded
  known fixture hashes prove producer-side manifest-byte, semantic, and resource digest placement
  under the amended v1 contract.
- **F22 selector proof:** exact TS01..TS05 plus P4A-TS04 pass **27/27 with 341 assertions** in the
  independent replay. The root serial replay passes the same **27/27 with 341 assertions**; TS02
  owns config/detection/snapshot/capability/placement rows, TS03 the complete bounded force matrix,
  TS04 revision/cancellation/hostile-plan rows, TS05 production parse/wire/exit rows, and P4A-TS04
  independent writer/digest/crash convergence.
- **Required-now compatibility:** `EWP-OPT-TS03` passes **10/393**, `EWP-OPT-TS04` **6/88**, and
  `EWP-P2-TS05` **6/1,072**. The directly focused config/init/planning/execution/output surfaces pass,
  and the independent six-file replay passes **121/121 with 1,032 expectations**.
- **Impacted compatibility:** P1-TS03/05/07/10/11 pass **8/96**, **7/113**, **16/347**,
  **10/1,653**, and **10/487**. P3B-TS01/03/04/05/06/07 pass **1/206**, **14/172**,
  **20/20,227**, **26/328**, **20/147**, and **31/431**. Public API, artifact repository, scheduler,
  init application, artifact init, and the complete option contracts pass **75/75 with 1,167
  assertions**.
- **Durability/static replay:** full `EWP-P2-TS04` passes **19/19 with 49,356 assertions**;
  recovery smoke passes **4/453** and standard smoke **5/3,735**. Typecheck, changed-file Biome,
  boundary lint, gitleaks, P17 package validation, diff hygiene, and skip/TODO/network guards pass.
- **Refactor result:** the correction retains one immutable prepared product, the signed planner,
  scheduler, precondition coordinator, and one-member artifact coordinator. Snapshot eligibility,
  revision comparison, and cancellation durability remain small private policy helpers; extracting
  another init framework would duplicate authority. Reopened targeted-green, impacted-green, and
  refactor gates pass on the correction pin with no known architecture debt.

## Independent adversarial review

- Independent reviewer `/root/g4a03_adversarial_review` re-reviewed exact product/test pin
  `6b1f2fb5f08a73064c1a8044ddb5f5e89adf8f2f` and governance clarification
  `07be49203359ac6f5bece8303bb55cbf4a78499b`, returning **PASS at 0.99 confidence**.
- Every stable finding F13..F22 is **CLOSED** and no new finding ID was opened. The reviewer
  reproduced the snapshot races, unsafe legacy refusal, inode/parent refusals, error taxonomy,
  canonical secret-comment replacement, proxy canary, durable cancellation split, strict DTO
  negatives, known digest fixtures, and substantive exact-selector ownership.
- The reviewer confirmed the correction range changes 17 product/test paths, all within the
  accepted 57-path union, with only the two authorized governance files outside that product
  ownership. Typecheck, Biome, boundary lint, P17 validation, diff/skip/TODO/network guards, and a
  510-commit gitleaks scan all pass on a clean worktree.
- **Result:** the F17/F21 Ready amendment and F19 clarification are accepted; they add no path,
  public field, operation kind, or unrelated mutation authority. No blocking, deferred, safety,
  ownership, coverage, or compatibility finding remains. The adversarial-review gate passes;
  terminal traceability closure and sign-off remain pending.

## Terminal full-repository ownership and architecture amendment

- The clean exact reviewed pin `1ad369bfbf565abef60a168cc87f32b0a22e536a` passed Biome over
  683 files, boundary lint, TypeScript, actionlint, P17 governance, and 2,577 of 2,580 executed
  tests. It skipped 28 explicitly live-only tests and executed 97,594 assertions across 268 files
  in 803.51 seconds.
- The terminal gate exposed exactly three bounded compatibility failures. P2-TS08 retained the
  pre-init wire descriptor inventory and v1 runtime-export inventory. P1-TS09 correctly rejected
  hard-coded writable-tool order and executable tool branches in the generic init application and
  versioned wire modules. No init command, planner, durability, recovery, or required-now selector
  failed.
- Before editing the signed compatibility inventory, ownership adds exactly
  `tests/ergonomics/phase/EWP-P2-TS08.test.ts`. Group and `EWP-P4A-T04` ownership remain identical
  at **58 unique paths**: 29 architecture/production, 13 focused tests, two catalog owners, and 14
  signed compatibility amendments. The amended Ready plan SHA-256 is
  `7968eb75f6a83078b947f0348ca5fa02abe3882c9856ba1590a0c489c4fdaf6e`.
- P1-TS09 remains unedited. The planned correction derives writable init order from the existing
  registry `plan` capability in both application and `init@1` validation, removes the generic
  configured-home tool branch, and makes the spawned init contract PATH hermetic so adapter
  detection cannot depend on host-installed binaries. P2-TS08 may change only to add `init@1`,
  `initV1Codec`, `toInitV1Dto`, and their exact registry-identity assertion.
- This product/test correction reopens the group to `active` and adversarial review to `pending`.
  P1-TS09 and P2-TS08 are explicit impacted replays; the same independent reviewer must accept the
  corrected committed terminal candidate before another clean full-repository gate, traceability
  closure, or sign-off can pass.
- After the two original P2-TS08 inventory failures closed, its now-reachable public compile fixture
  exposed the same pre-init v1 runtime-key union. Before editing that fixture, ownership additionally
  adds exactly `tests/ergonomics/fixtures/p2-ts08/public-types.ts` for the additive `initV1Codec` /
  `toInitV1Dto` public identity and typed codec/mapper proof. Group and work ownership therefore
  remain identical at **59 unique paths**: 29 architecture/production, 13 focused tests, two catalog
  owners, and 15 signed compatibility amendments. This adds no runtime behavior or public API; it
  closes the compile-time inventory for the already implemented `init@1` surface. The superseding
  amended Ready plan SHA-256 is
  `28ddf705d5eada90fd0b8a2624124136b559b182cef3242b13241a9f61a82c3c`.
