# P17-G4B-01 evidence

## Mapping and dependency audit

- **Current catalog:** G4B-01 is planned after signed G4A-04 and currently owns 22 primary entities
  with 17 required-now validations. No product ownership, implementation target, or gate is active.
- **Executable candidate:** exactly 18 primaries and 13 required-now validations: `COMMAND:plan`,
  `D-001`, `P1-07`, P4B-T01/T02, plan TS01..TS10/TS12, P4B-TS01, and P4B-TS04.
- **Structural schedule defect:** plan TS11, P4B-TS02, WF06, and WF08 each normatively invoke or
  assert `apply`. G4B-02 owns `COMMAND:apply` and depends on G4B-01. Those four targets cannot be
  truthfully green in G4B-01 without implementing future primary scope or weakening assertions.
- **Candidate correction:** move those four validation primaries unchanged to dependency-complete
  G4B-02, retain G4B-01 secondary/downstream coverage, and add G4B-02 secondary traceability to the
  plan command/decision/recommendation/tasks. Target paths, selectors, tiers, text, entity count,
  execution-group dependencies, and behavior remain unchanged.
- **Later-primary traceability:** D-002/P4B-T03 retain G4B-02 primary ownership while adding G4B-01
  generation-side secondary coverage; D-015/P4B-T05 retain G4B-03 primary ownership while adding
  G4B-01 resolution/locked-preview secondary coverage. No future lifecycle state advances.
- **Approval boundary:** this file and the Ready-plan candidate document the correction only. The
  catalog generator, generated catalog/checklist, and consolidated normative plan remain at the
  signed baseline until a fresh independent review returns GO and the user approves the exact
  amendment.
- **Dependency proof:** signed Phase 2/3B/G4A work already supplies artifact selection, canonical
  readers, immutable observations, generic operation planning, repositories, capabilities,
  saved-plan v1 codec, and coordination primitives. No existing plan application service, CLI
  command, report contract, renderer, output writer, or exact plan selector exists.
- **Boundary proof:** G4B-01 can compute and save exact operations without executing them. Existing
  saved-plan v1 can carry operations and preconditions; exact execution/staleness/approval remains
  G4B-02. Current adapters do not expose an adaptation transform, so production refuses unavailable
  adaptation while a canonical fixture proves renderer coverage.
- **Prune proof:** deletion authority is bounded by selected manifest plus explicit tool/scope
  filters. Empty/zero/unsupported/ambiguous state cannot widen it; unmanaged and other project/pair
  resources remain excluded.
- **Output proof:** one strict report drives human and JSON output. Existing canonical saved-plan
  codec drives artifact bytes. A focused safe-port writer provides owner-only create or exact-path
  force replacement without joining selected-state transactions.
- **Frozen candidate ownership:** 48 exact product/test paths are listed in the Ready plan. Eight
  compatibility paths are additive owners for the option inventory, 18th JSON command path,
  renderer/codec/public-v1 inventories, public type/implementation fixtures, report fixture, and
  exact plan golden. Existing command bytes and signed semantic assertions remain unchanged; all
  other compatibility tests are replay-only.
- **Test-first direction:** 13 exact selectors cover the plan command/phase work. The four moved
  selectors are neither created nor claimed until G4B-02.
- **Verification:** exact-selector replays, focused suites, smoke, static/boundary/governance,
  hygiene, secret/network/skip guards, then a clean serial full check. Durability/recovery milestone
  suites are conditional on actually changing their signed primitives.
- **Result:** the dependency gap, product contract, architecture boundary, test matrix, ownership,
  verification, and exclusions are mapped. No lifecycle, entity, validation, or implementation
  gate advances before independent review and explicit amendment approval.

## Ready review

Fresh independent reviewer `/root/g4b01_ready_review` returned **NO-GO** with 0.97 confidence on
candidate commit `d352822a80e41f98cd6fd175a8845477472ac2a4`. The reviewed plan SHA-256 was
`cff9f9cb77bd3723112f2c2a32a2e08d2736d60b8e09cdfa0ef79140bc1ebaa8`; evidence was
`2b3823ed3bc827eb1fc4bbe92488868b23b217c59d31a1369215d66aefd86fd9`. The reviewer edited
nothing and confirmed the unchanged catalog baseline valid.

Stable findings and corrections:

- `P17-RV-G4B-01-F01` (**high**) found the 40-path boundary omitted exact-inventory compatibility
  tests/fixtures required by a new command and v1 report. The correction adds eight narrowly
  additive paths and four exact replay commands, freezes 48 paths, and forbids weakening existing
  semantics or bytes.
- `P17-RV-G4B-01-F02` (**medium**) found the grammar omitted the accepted `-f` short spelling. The
  correction records `[-f, --force]` and distinguishes command aliases from option spellings.
- `P17-RV-G4B-01-F03` (**medium**) found missing later-primary secondary relations. The correction
  adds G4B-01 generation-side traceability to D-002/P4B-T03 and resolution-side traceability to
  D-015/P4B-T05 without moving or advancing them.

The reviewer independently confirmed that the four-validation move is necessary, sufficient, and
schedule-only; the 18/13/four arithmetic is exact; no other validation must move; and the product
semantics, 13-selector matrix, and apply/adaptation/durability exclusions otherwise align with the
normative plan.

Corrected candidate commit `d4491fa28bba6ae664c1da091b557637ecbc24d6` received **GO** with
0.98 confidence from the same independent reviewer after a full read-only reassessment. The
accepted Ready-plan SHA-256 is
`ab82817988f7632c19e4ca93a511fe517e3987dfd5174ed69afa30f6f92d6e50`; the reviewed
pre-acceptance evidence SHA-256 is
`9a2370cb9c0c3148f53ded0e620954b9100af7058b86535c35d46595d1093b3a`.

All three findings are closed and no new finding was raised. The reviewer reconfirmed the
four-validation correction as necessary, sufficient, and schedule-only; the exact 18/13/four
partition; the command/artifact/lock/source/prune/report/saved-output/portability/redaction/exit
contracts; the 13-selector matrix; the 48-path boundary; the compatibility replays; and the
apply/adaptation/lock-commit/recovery exclusions. The unchanged catalog baseline passed with 425
entities, 45 groups, and 244 validation obligations. The reviewer edited nothing and left the
worktree clean.

**Ready review result:** PASS, conditional only on explicit user approval of the exact schedule and
secondary-traceability amendment. No catalog, consolidated plan, lifecycle, entity, validation, or
implementation gate advances before that approval.

## Approved amendment and Ready result

The user explicitly approved the independently reviewed amendment on 2026-07-18. The accepted
catalog change is exactly:

- move plan TS11, P4B-TS02, WF06, and WF08 unchanged to G4B-02 primary required-now ownership;
- retain those four as G4B-01 downstream coverage with G4B-01 secondary traceability;
- retain G4B-02 secondary terminal coverage on the plan command, D-001, P1-07, T01, and T02;
- add G4B-01 generation-side secondary traceability to D-002/P4B-T03 while G4B-02 remains primary;
  and
- add G4B-01 resolution/locked-preview secondary traceability to D-015/P4B-T05 while G4B-03
  remains primary.

All selector text, assertions, target paths, tiers, behavior, entity count, product scope, and group
dependencies remain unchanged. The generator regression test freezes the 18-primary,
13-required-now, and four-downstream partition plus all named secondary relations.

**Ready result:** PASS. G4B-01 and P4B-T01/T02 may advance to `ready`; mapping and Ready gates may
pass with this evidence. All 13 required validation entities remain pristine planned until
implementation-free executable red evidence. The four downstream validations remain pristine
planned under G4B-02. No test-first, implementation, green, refactor, adversarial, traceability, or
sign-off gate is implied.

## Test-first evidence

Pending acceptance of the amendment and Ready plan. No implementation selector or fixture exists;
no executable red receipt is claimed.

## Minimal implementation

Pending.

## Targeted and impacted green

Pending.

## Refactor

Pending.

## Adversarial review

Pending.

## Traceability and terminal verification

Pending.

## Sign-off

Pending.
