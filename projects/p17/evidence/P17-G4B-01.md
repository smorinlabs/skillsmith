# P17-G4B-01 evidence

## Mapping and dependency audit

- **Current catalog:** G4B-01 is planned after signed G4A-04 and currently owns 22 primary entities
  with 17 required-now validations. No product ownership, implementation target, or gate is active.
- **Executable candidate:** exactly 18 primaries and 13 required-now validations: `COMMAND:plan`,
  `D-001`, `P1-07`, P4B-T01/T02, plan TS01..TS10/TS12, P4B-TS01, and P4B-TS04.
- **Structural schedule defect:** plan TS11, P4B-TS02, WF06, and WF08 each normatively invoke or
  assert `apply`. G4B-02 owns `COMMAND:apply` and depends on G4B-01. Those four targets cannot be
  truthfully green in G4B-01 without implementing future primary scope or weakening assertions.
- **Candidate correction:** move those four validation primaries unchanged to dependency-complete
  G4B-02, retain G4B-01 secondary/downstream coverage, and add G4B-02 secondary traceability to the
  plan command/decision/recommendation/tasks. Target paths, selectors, tiers, text, entity count,
  execution-group dependencies, and behavior remain unchanged.
- **Later-primary traceability:** D-002/P4B-T03 retain G4B-02 primary ownership while adding G4B-01
  generation-side secondary coverage; D-015/P4B-T05 retain G4B-03 primary ownership while adding
  G4B-01 resolution/locked-preview secondary coverage. No future lifecycle state advances.
- **Approval boundary:** this file and the Ready-plan candidate document the correction only. The
  catalog generator, generated catalog/checklist, and consolidated normative plan remain at the
  signed baseline until a fresh independent review returns GO and the user approves the exact
  amendment.
- **Dependency proof:** signed Phase 2/3B/G4A work already supplies artifact selection, canonical
  readers, immutable observations, generic operation planning, repositories, capabilities,
  saved-plan v1 codec, and coordination primitives. No existing plan application service, CLI
  command, report contract, renderer, output writer, or exact plan selector exists.
- **Boundary proof:** G4B-01 can compute and save exact operations without executing them. Existing
  saved-plan v1 can carry operations and preconditions; exact execution/staleness/approval remains
  G4B-02. Current adapters do not expose an adaptation transform, so production refuses unavailable
  adaptation while a canonical fixture proves renderer coverage.
- **Prune proof:** deletion authority is bounded by selected manifest plus explicit tool/scope
  filters. Empty/zero/unsupported/ambiguous state cannot widen it; unmanaged and other project/pair
  resources remain excluded.
- **Output proof:** one strict report drives human and JSON output. Existing canonical saved-plan
  codec drives artifact bytes. A focused safe-port writer provides owner-only create or exact-path
  force replacement without joining selected-state transactions.
- **Frozen candidate ownership:** 48 exact product/test paths are listed in the Ready plan. Eight
  compatibility paths are additive owners for the option inventory, 18th JSON command path,
  renderer/codec/public-v1 inventories, public type/implementation fixtures, report fixture, and
  exact plan golden. Existing command bytes and signed semantic assertions remain unchanged; all
  other compatibility tests are replay-only.
- **Test-first direction:** 13 exact selectors cover the plan command/phase work. The four moved
  selectors are neither created nor claimed until G4B-02.
- **Verification:** exact-selector replays, focused suites, smoke, static/boundary/governance,
  hygiene, secret/network/skip guards, then a clean serial full check. Durability/recovery milestone
  suites are conditional on actually changing their signed primitives.
- **Result:** the dependency gap, product contract, architecture boundary, test matrix, ownership,
  verification, and exclusions are mapped. No lifecycle, entity, validation, or implementation
  gate advances before independent review and explicit amendment approval.

## Ready review

Fresh independent reviewer `/root/g4b01_ready_review` returned **NO-GO** with 0.97 confidence on
candidate commit `d352822a80e41f98cd6fd175a8845477472ac2a4`. The reviewed plan SHA-256 was
`cff9f9cb77bd3723112f2c2a32a2e08d2736d60b8e09cdfa0ef79140bc1ebaa8`; evidence was
`2b3823ed3bc827eb1fc4bbe92488868b23b217c59d31a1369215d66aefd86fd9`. The reviewer edited
nothing and confirmed the unchanged catalog baseline valid.

Stable findings and corrections:

- `P17-RV-G4B-01-F01` (**high**) found the 40-path boundary omitted exact-inventory compatibility
  tests/fixtures required by a new command and v1 report. The correction adds eight narrowly
  additive paths and four exact replay commands, freezes 48 paths, and forbids weakening existing
  semantics or bytes.
- `P17-RV-G4B-01-F02` (**medium**) found the grammar omitted the accepted `-f` short spelling. The
  correction records `[-f, --force]` and distinguishes command aliases from option spellings.
- `P17-RV-G4B-01-F03` (**medium**) found missing later-primary secondary relations. The correction
  adds G4B-01 generation-side traceability to D-002/P4B-T03 and resolution-side traceability to
  D-015/P4B-T05 without moving or advancing them.

The reviewer independently confirmed that the four-validation move is necessary, sufficient, and
schedule-only; the 18/13/four arithmetic is exact; no other validation must move; and the product
semantics, 13-selector matrix, and apply/adaptation/durability exclusions otherwise align with the
normative plan.

Corrected candidate commit `d4491fa28bba6ae664c1da091b557637ecbc24d6` received **GO** with
0.98 confidence from the same independent reviewer after a full read-only reassessment. The
accepted Ready-plan SHA-256 is
`ab82817988f7632c19e4ca93a511fe517e3987dfd5174ed69afa30f6f92d6e50`; the reviewed
pre-acceptance evidence SHA-256 is
`9a2370cb9c0c3148f53ded0e620954b9100af7058b86535c35d46595d1093b3a`.

All three findings are closed and no new finding was raised. The reviewer reconfirmed the
four-validation correction as necessary, sufficient, and schedule-only; the exact 18/13/four
partition; the command/artifact/lock/source/prune/report/saved-output/portability/redaction/exit
contracts; the 13-selector matrix; the 48-path boundary; the compatibility replays; and the
apply/adaptation/lock-commit/recovery exclusions. The unchanged catalog baseline passed with 425
entities, 45 groups, and 244 validation obligations. The reviewer edited nothing and left the
worktree clean.

**Ready review result:** PASS, conditional only on explicit user approval of the exact schedule and
secondary-traceability amendment. No catalog, consolidated plan, lifecycle, entity, validation, or
implementation gate advances before that approval.

## Approved amendment and Ready result

The user explicitly approved the independently reviewed amendment on 2026-07-18. The accepted
catalog change is exactly:

- move plan TS11, P4B-TS02, WF06, and WF08 unchanged to G4B-02 primary required-now ownership;
- retain those four as G4B-01 downstream coverage with G4B-01 secondary traceability;
- retain G4B-02 secondary terminal coverage on the plan command, D-001, P1-07, T01, and T02;
- add G4B-01 generation-side secondary traceability to D-002/P4B-T03 while G4B-02 remains primary;
  and
- add G4B-01 resolution/locked-preview secondary traceability to D-015/P4B-T05 while G4B-03
  remains primary.

All selector text, assertions, target paths, tiers, behavior, entity count, product scope, and group
dependencies remain unchanged. The generator regression test freezes the 18-primary,
13-required-now, and four-downstream partition plus all named secondary relations.

**Ready result:** PASS. G4B-01 and P4B-T01/T02 may advance to `ready`; mapping and Ready gates may
pass with this evidence. All 13 required validation entities remain pristine planned until
implementation-free executable red evidence. The four downstream validations remain pristine
planned under G4B-02. No test-first, implementation, green, refactor, adversarial, traceability, or
sign-off gate is implied.

## Test-first evidence

Only the five accepted new selector/fixture paths were created at test pin
`9bad1863bd18c9c3d3605bb6638f9cac61ed1aea`. No production file or existing compatibility test was
edited. The fixtures contain canonical manifest/lock builders, hermetic HOME/XDG/data/cache roots,
`fixture.invalid` sources, immutable snapshot helpers, the exact operation/lock/prune/exit matrices,
and bounded representative-fleet generation.

All 13 exact selectors fail independently for a product gap. Eleven command selectors and both
phase selectors reach the spawned CLI and receive exit 2 because `plan --file` is not registered;
P4B-TS01 additionally binds the absent future plan renderer after the command surface exists. No
selector fails from TypeScript, formatting, fixture serialization, public network, external secret,
timeout, skip/todo, or missing test data. The two initial unsorted-lock fixture failures were
corrected and replayed to the same missing-product red before pinning.

The exact aggregate is 13 failing tests, zero passing tests, and zero skipped tests across 13
independent selector commands. `bun run test:smoke` passes 5 tests with 3,735 assertions; repository
typecheck, changed-file Biome, architecture boundaries, diff hygiene, skip/todo, public-network, and
gitleaks guards pass.

**Test-first result:** PASS. G4B-01 advances to active and P4B-T01/T02 to red with the test-first
gate passed. The 13 validation entities advance to failing at their exact executable targets.
Minimal implementation and every green/review/sign-off gate remain pending.

## Approved execution ownership amendment

The first implementation replay made 10 of 13 owned selectors green and exposed two compatibility
obligations omitted from the 48-path Ready boundary:

- adding the accepted `plan` member to `CurrentMutatorCommand` truthfully invalidates only the
  closed parity assertion in `packages/core/tests/planning/types.test.ts`; and
- Commander accepts undeclared excess positionals unless the generic CommandSpec builder closes
  argument arity explicitly, so plan TS03 proved that `packages/cli/src/runtime/command-spec.ts`
  needs one registry-derived `allowExcessArguments(false)` correction.

Independent reviewer `/root/g4b01_ready_review` first approved the planning-union addition with
0.995 confidence. The reviewer then returned NO-GO 0.97 on a command-builder-only proposal because
a generic cross-command correction required its existing generic regression owner. The corrected
combined amendment adds `tests/ergonomics/phase/EWP-P1-TS07.test.ts` and bounded assertions for
no-argument rejection, fixed required/optional arity, excess rejection, and preserved variadic
acceptance. The corrected proposal received GO with 0.995 confidence and no remaining finding.

The user explicitly approved the corrected combined amendment on 2026-07-19. The frozen boundary
is now exactly 51 paths with ten additive compatibility paths. The three added paths are limited to:

- `packages/core/tests/planning/types.test.ts`: append only `'plan'` to the expected closed union;
- `packages/cli/src/runtime/command-spec.ts`: enforce the arguments already declared by every
  authoritative CommandSpec without command/tool branching; and
- `tests/ergonomics/phase/EWP-P1-TS07.test.ts`: add the independently required generic arity
  regression while preserving every existing assertion.

No entity, selector, tier, dependency, schedule, operation kind, output contract, or future-group
scope changes. Immediate amendment replays are P1-TS07, options, plan TS03, typecheck, and smoke.

## Approved runtime-export fixture ownership amendment

The impacted compatibility replay made every accepted selector and all but one compatibility row
green. `EWP-P2-TS08` family 6 compiled its closed public-types fixture and proved that adding the
accepted `planV1Codec` runtime export also requires the fixture's exact `keyof V1Runtime` inventory
to include that one value export. The fixture path was not in the 51-path boundary even though the
owning P2-TS08 test path was.

Fresh read-only reviewer `/root/step10_drift_review` returned **GO** with 0.99 confidence for adding
only `tests/ergonomics/fixtures/p2-ts08/public-types.ts` and only the `planV1Codec` runtime-key union
member. The reviewer confirmed that this is necessary and sufficient, that the P1-TS10 fixtures
already own the plan DTO typing assertions, and that type-only `PlanV1Dto` and
`PlanOperationV1Dto` exports must remain absent from `keyof typeof import(...)`. The user standing
approval authorizes reviewed completion work without another approval stop. The frozen boundary is
therefore exactly 52 paths with eleven additive compatibility paths. No entity, selector, tier,
dependency, schedule, product behavior, or future-group scope changes.

## Approved stream-drain and dependency-ordering ownership amendment

Independent read-only review returned `GO` with 99% confidence for adding exactly
`packages/cli/src/index.ts`, `packages/core/src/planning/order.ts`, and
`packages/core/tests/planning/order.test.ts` to the frozen boundary.

- The representative-fleet selector proved that eager `process.exit(...)` could truncate buffered
  CLI output. The entrypoint now sets `process.exitCode` on success and failure, preserving exact
  exit mappings while allowing stdout/stderr to drain.
- The real `write-lock` prerequisite proved that prefix chronology was being checked before
  dependency ordering. The generic planner now validates cross-group prefix shape before
  deterministic Kahn ordering and validates earlier/latest prefix position afterward.
- The order regression forces adversarial group hashes, covers all six input permutations, and
  retains partial, arbitrary, later-prefix, and stale-prefix rejection.

No command-specific branch, operation kind, execution behavior, or future-group scope was added.
Review evidence was order 7/7 with 22 assertions, P4B-TS01 2/2 with 153 assertions, a complete
465,792-byte 96-operation report with terminal LF, and clean typecheck/Biome/diff hygiene. The
frozen boundary advanced from 52 to 55 paths.

## Approved saved-plan traversal-capacity ownership amendment

The shared report/saved projection exposed two independent capacity boundaries: no-output planning
failed at 128 rows against the plan codec's 20,000-node ownership snapshot, while `--out` failed at
104 rows against the generic 16,384-node canonical encoder. Independent review returned `GO` with
99% confidence for adding only `packages/core/src/artifacts/plan-codec.ts` and reusing the existing
bounded 65,536-node repository ceiling for saved plans in both traversals. Journal traversal limits
remain unchanged. Splitting report-only projection was rejected because it would hide the report
failure while leaving saved output broken and would create two projection paths.

TS12 now proves deterministic 128-operation reporting, 128-operation saved output, canonical
decode, terminal LF, and the existing runtime bound. The frozen boundary is exactly 56 product/test
paths; the eleven additive compatibility paths remain unchanged.

## Approved permission-classification ownership amendment

Independent adversarial review reproduced normalized `pathKind` and `realpath` permission failures
being collapsed to state exit 3 while resolving an explicit artifact selector. The correction adds
only `packages/core/src/artifacts/pair.ts`, `packages/core/src/ports/errors.ts`, and
`packages/core/tests/artifacts/pair.test.ts` to the frozen boundary. Port errors created at the
trusted port boundary now carry an unforgeable in-process marker; the artifact-pair authority maps
only a marked `permission` error to permission exit 6 and continues treating arbitrary thrown
values as opaque state failures without inspecting hostile properties.

Focused artifact-pair review passed 11/11 tests with 154 assertions, repository typecheck passed,
and the full smoke lane passed 5/5 tests with 3,735 assertions. The frozen boundary is exactly 59
product/test paths; no artifact discovery, path containment, or external I/O authority changed.

### Red execution receipt EWP-CMD-PLAN-TS01

- Validation ID: `EWP-CMD-PLAN-TS01`
- Command: `bun test packages/cli/tests/contracts/plan.test.ts --test-name-pattern EWP-CMD-PLAN-TS01`
- Exit status: `1`
- Result: 0 passed, 1 failed because `plan --file` is unregistered (CLI exit 2).
- Revision: `9bad1863bd18c9c3d3605bb6638f9cac61ed1aea`

### Red execution receipt EWP-CMD-PLAN-TS02

- Validation ID: `EWP-CMD-PLAN-TS02`
- Command: `bun test packages/cli/tests/contracts/plan.test.ts --test-name-pattern EWP-CMD-PLAN-TS02`
- Exit status: `1`
- Result: 0 passed, 1 failed because deterministic plan invocation exits 2.
- Revision: `9bad1863bd18c9c3d3605bb6638f9cac61ed1aea`

### Red execution receipt EWP-CMD-PLAN-TS03

- Validation ID: `EWP-CMD-PLAN-TS03`
- Command: `bun test packages/cli/tests/contracts/plan.test.ts --test-name-pattern EWP-CMD-PLAN-TS03`
- Exit status: `1`
- Result: 0 passed, 1 failed because exact artifact selection is unavailable.
- Revision: `9bad1863bd18c9c3d3605bb6638f9cac61ed1aea`

### Red execution receipt EWP-CMD-PLAN-TS04

- Validation ID: `EWP-CMD-PLAN-TS04`
- Command: `bun test packages/cli/tests/contracts/plan.test.ts --test-name-pattern EWP-CMD-PLAN-TS04`
- Exit status: `1`
- Result: 0 passed, 1 failed because plan lock policy is unavailable.
- Revision: `9bad1863bd18c9c3d3605bb6638f9cac61ed1aea`

### Red execution receipt EWP-CMD-PLAN-TS05

- Validation ID: `EWP-CMD-PLAN-TS05`
- Command: `bun test packages/cli/tests/contracts/plan.test.ts --test-name-pattern EWP-CMD-PLAN-TS05`
- Exit status: `1`
- Result: 0 passed, 1 failed because bounded plan selection/prune is unavailable.
- Revision: `9bad1863bd18c9c3d3605bb6638f9cac61ed1aea`

### Red execution receipt EWP-CMD-PLAN-TS06

- Validation ID: `EWP-CMD-PLAN-TS06`
- Command: `bun test packages/cli/tests/contracts/plan.test.ts --test-name-pattern EWP-CMD-PLAN-TS06`
- Exit status: `1`
- Result: 0 passed, 1 failed because capability/refusal plan reporting is unavailable.
- Revision: `9bad1863bd18c9c3d3605bb6638f9cac61ed1aea`

### Red execution receipt EWP-CMD-PLAN-TS07

- Validation ID: `EWP-CMD-PLAN-TS07`
- Command: `bun test packages/cli/tests/contracts/plan.test.ts --test-name-pattern EWP-CMD-PLAN-TS07`
- Exit status: `1`
- Result: 0 passed, 1 failed because plan check expects 0 but the absent surface exits 2.
- Revision: `9bad1863bd18c9c3d3605bb6638f9cac61ed1aea`

### Red execution receipt EWP-CMD-PLAN-TS08

- Validation ID: `EWP-CMD-PLAN-TS08`
- Command: `bun test packages/cli/tests/contracts/plan.test.ts --test-name-pattern EWP-CMD-PLAN-TS08`
- Exit status: `1`
- Result: 0 passed, 1 failed because plan human/JSON renderers are unavailable.
- Revision: `9bad1863bd18c9c3d3605bb6638f9cac61ed1aea`

### Red execution receipt EWP-CMD-PLAN-TS09

- Validation ID: `EWP-CMD-PLAN-TS09`
- Command: `bun test packages/cli/tests/contracts/plan.test.ts --test-name-pattern EWP-CMD-PLAN-TS09`
- Exit status: `1`
- Result: 0 passed, 1 failed because saved-plan output creation is unavailable.
- Revision: `9bad1863bd18c9c3d3605bb6638f9cac61ed1aea`

### Red execution receipt EWP-CMD-PLAN-TS10

- Validation ID: `EWP-CMD-PLAN-TS10`
- Command: `bun test packages/cli/tests/contracts/plan.test.ts --test-name-pattern EWP-CMD-PLAN-TS10`
- Exit status: `1`
- Result: 0 passed, 1 failed because read-only plan reporting is unavailable.
- Revision: `9bad1863bd18c9c3d3605bb6638f9cac61ed1aea`

### Red execution receipt EWP-CMD-PLAN-TS12

- Validation ID: `EWP-CMD-PLAN-TS12`
- Command: `bun test packages/cli/tests/contracts/plan.test.ts --test-name-pattern EWP-CMD-PLAN-TS12`
- Exit status: `1`
- Result: 0 passed, 1 failed because representative-fleet plan exits 2.
- Revision: `9bad1863bd18c9c3d3605bb6638f9cac61ed1aea`

### Red execution receipt EWP-P4B-TS01

- Validation ID: `EWP-P4B-TS01`
- Command: `bun test tests/ergonomics/phase/EWP-P4B-TS01.test.ts --test-name-pattern EWP-P4B-TS01`
- Exit status: `1`
- Result: 0 passed, 1 failed because the plan command/renderer matrix is unavailable.
- Revision: `9bad1863bd18c9c3d3605bb6638f9cac61ed1aea`

### Red execution receipt EWP-P4B-TS04

- Validation ID: `EWP-P4B-TS04`
- Command: `bun test tests/ergonomics/phase/EWP-P4B-TS04.test.ts --test-name-pattern EWP-P4B-TS04`
- Exit status: `1`
- Result: 0 passed, 1 failed because prune preview exits 2 before safety reporting.
- Revision: `9bad1863bd18c9c3d3605bb6638f9cac61ed1aea`

## Independent prune-authority architecture review

- Result: `GO` to correct the requirement terminology; `NO-GO` to add artifact-pair provenance
  or a ledger schema migration in G4B-01.
- Confidence: 99%.
- Finding: `LedgerPairV1Dto` and `ObservedPrunePlacement` carry no manifest/lock artifact-pair
  identifier. The fixture formerly labeled `other-artifact-pair` concretely proves a selected-lock
  versus ledger-origin source mismatch.
- Resolution: terminology is corrected to `wrong-source` and unproven selected-artifact authority.
  Product behavior, fail-closed source/ref/content/path correlation, validation ownership, schema,
  and future-group scope are unchanged.
- Review boundary: no new provenance field or compatibility migration is justified for G4B-01.

## Minimal implementation

Implemented the accepted read-only `plan` command as one bounded application pipeline:

1. authoritative manifest/lock discovery and immutable observation;
2. selected lock resolution with strict `--locked`, retained-pin, source, permission, and
   cancellation outcomes;
3. pure desired/current reconciliation with deterministic dependency-aware operation ordering;
4. one validated saved-plan projection supplying the exact execution-bound precondition IDs to both
   report and optional saved output;
5. strict JSON and semantic human renderers over the same report DTO; and
6. an owner-only create/atomic-force saved-plan writer outside manifest/lock mutation authority.

Prune remains fail-closed on exact selected-lock/live/ledger source/ref/content/path correlation.
Planning does not acquire mutation locks, write live/ledger/store/config state, or implement apply,
approval, recovery, or execution behavior reserved for G4B-02/G4B-03.

## Targeted and impacted green

- Required target aggregate:
  `bun test packages/cli/tests/contracts/plan.test.ts tests/ergonomics/phase/EWP-P4B-TS01.test.ts tests/ergonomics/phase/EWP-P4B-TS04.test.ts`
  passed 16/16 tests with 390 assertions.
- Core-owned aggregate passed 87/87 tests with 732 assertions before the final missing-lock
  regression was added; the focused saved-projection replay then passed 8/8 with 54 assertions.
- Impacted compatibility aggregate across options, P1-TS07, P1-TS10, P1-TS11, P2-TS08, and
  planning types passed 87/87 tests with 4,940 assertions.
- `bun run typecheck` passed.
- `bun run test:smoke` passed 5/5 tests with 3,735 assertions after the final production changes.
- Changed-file Biome and `git diff --check` passed.

### Green execution receipt EWP-CMD-PLAN-TS01

- Validation ID: `EWP-CMD-PLAN-TS01`
- Command: `bun test packages/cli/tests/contracts/plan.test.ts --test-name-pattern EWP-CMD-PLAN-TS01`
- Exit status: `0`
- Result: 1 test passed with 12 assertions; 0 failed and 0 skipped.
- Revision: `working-tree`

### Green execution receipt EWP-CMD-PLAN-TS02

- Validation ID: `EWP-CMD-PLAN-TS02`
- Command: `bun test packages/cli/tests/contracts/plan.test.ts --test-name-pattern EWP-CMD-PLAN-TS02`
- Exit status: `0`
- Result: 1 test passed with 6 assertions; 0 failed and 0 skipped.
- Revision: `working-tree`

### Green execution receipt EWP-CMD-PLAN-TS03

- Validation ID: `EWP-CMD-PLAN-TS03`
- Command: `bun test packages/cli/tests/contracts/plan.test.ts --test-name-pattern EWP-CMD-PLAN-TS03`
- Exit status: `0`
- Result: 2 tests passed with 11 assertions; 0 failed and 0 skipped.
- Revision: `working-tree`

### Green execution receipt EWP-CMD-PLAN-TS04

- Validation ID: `EWP-CMD-PLAN-TS04`
- Command: `bun test packages/cli/tests/contracts/plan.test.ts --test-name-pattern EWP-CMD-PLAN-TS04`
- Exit status: `0`
- Result: 1 test passed with 19 assertions; 0 failed and 0 skipped.
- Revision: `working-tree`

### Green execution receipt EWP-CMD-PLAN-TS05

- Validation ID: `EWP-CMD-PLAN-TS05`
- Command: `bun test packages/cli/tests/contracts/plan.test.ts --test-name-pattern EWP-CMD-PLAN-TS05`
- Exit status: `0`
- Result: 1 test passed with 17 assertions; 0 failed and 0 skipped.
- Revision: `working-tree`

### Green execution receipt EWP-CMD-PLAN-TS06

- Validation ID: `EWP-CMD-PLAN-TS06`
- Command: `bun test packages/cli/tests/contracts/plan.test.ts --test-name-pattern EWP-CMD-PLAN-TS06`
- Exit status: `0`
- Result: 1 test passed with 3 assertions; 0 failed and 0 skipped.
- Revision: `working-tree`

### Green execution receipt EWP-CMD-PLAN-TS07

- Validation ID: `EWP-CMD-PLAN-TS07`
- Command: `bun test packages/cli/tests/contracts/plan.test.ts --test-name-pattern EWP-CMD-PLAN-TS07`
- Exit status: `0`
- Result: 1 test passed with 89 assertions; 0 failed and 0 skipped.
- Revision: `working-tree`

### Green execution receipt EWP-CMD-PLAN-TS08

- Validation ID: `EWP-CMD-PLAN-TS08`
- Command: `bun test packages/cli/tests/contracts/plan.test.ts --test-name-pattern EWP-CMD-PLAN-TS08`
- Exit status: `0`
- Result: 1 test passed with 10 assertions; 0 failed and 0 skipped.
- Revision: `working-tree`

### Green execution receipt EWP-CMD-PLAN-TS09

- Validation ID: `EWP-CMD-PLAN-TS09`
- Command: `bun test packages/cli/tests/contracts/plan.test.ts --test-name-pattern EWP-CMD-PLAN-TS09`
- Exit status: `0`
- Result: 1 test passed with 11 assertions; 0 failed and 0 skipped.
- Revision: `working-tree`

### Green execution receipt EWP-CMD-PLAN-TS10

- Validation ID: `EWP-CMD-PLAN-TS10`
- Command: `bun test packages/cli/tests/contracts/plan.test.ts --test-name-pattern EWP-CMD-PLAN-TS10`
- Exit status: `0`
- Result: 2 tests passed with 23 assertions; 0 failed and 0 skipped.
- Revision: `working-tree`

### Green execution receipt EWP-CMD-PLAN-TS12

- Validation ID: `EWP-CMD-PLAN-TS12`
- Command: `bun test packages/cli/tests/contracts/plan.test.ts --test-name-pattern EWP-CMD-PLAN-TS12`
- Exit status: `0`
- Result: 1 test passed with 8 assertions; 0 failed and 0 skipped.
- Revision: `working-tree`

### Green execution receipt EWP-P4B-TS01

- Validation ID: `EWP-P4B-TS01`
- Command: `bun test tests/ergonomics/phase/EWP-P4B-TS01.test.ts --test-name-pattern EWP-P4B-TS01`
- Exit status: `0`
- Result: 2 tests passed with 153 assertions; 0 failed and 0 skipped.
- Revision: `working-tree`

### Green execution receipt EWP-P4B-TS04

- Validation ID: `EWP-P4B-TS04`
- Command: `bun test tests/ergonomics/phase/EWP-P4B-TS04.test.ts --test-name-pattern EWP-P4B-TS04`
- Exit status: `0`
- Result: 1 test passed with 28 assertions; 0 failed and 0 skipped.
- Revision: `working-tree`

## Refactor

The implementation was reviewed for accidental complexity after the complete matrix was green.
The retained boundaries are the smallest reusable authorities already present in the architecture:
artifact observation/resolution, the pure operation planner, one saved/report projection, the
artifact registry codec, and presentation-only renderers. Generic fixes were kept in their existing
owners: command process draining in the entrypoint, dependency topology in planning order, and
artifact traversal limits in the plan codec. No tool-specific renderer/planner branch, second saved
projection, new provenance schema, new coordinator, or apply/recovery abstraction was introduced.

The formerly named `other-artifact-pair` fixture was corrected to the observable `wrong-source`
authority mismatch instead of adding an unjustified ledger migration. This preserves the safety
claim while avoiding a speculative compatibility surface.

## Adversarial review

Independent reviewer `/root/g4b01_final_adversarial` returned **GO** at **0.98 confidence** on the
reviewed working tree. The review remained within authorized local open-source CLI reliability
fixtures and used no external systems, accounts, credentials, or networks. No P0, policy, or human
review blocker remains.

Stable finding dispositions:

- `ADV-P17-G4B01-PORT-01` — **closed**. Portable resource preconditions and operation, group,
  pair, check, and diagnostic identities are recomputed from tokenized facts. Stable semantic
  ordering and report-reference remapping preserve exact report/saved parity. Two independently
  discovered but byte-identical project fixtures under different absolute roots encode identical
  saved-plan objects and bytes; standard refusal paths and saved diagnostic messages contain no
  local root while the runtime report retains its exact path-bearing remediation.
- `ADV-P17-G4B01-REASON-02` — **closed**. Every absolute artifact selector, custom live target,
  and local development source reason references only its exact matching artifact, live, or store
  resource precondition. There is no manifest fallback. A path-only binding without a truthful
  resource precondition returns the deterministic `plan-machine-binding-precondition` state error
  instead of inventing resource authority.
- `ADV-P17-G4B01-ALIAS-03` — **closed**. Output-alias inspection recognizes only trusted normalized
  port errors. A hostile thrown proxy remains opaque and returns the fixed state diagnostic without
  property inspection; normalized permission errors retain permission classification.
- `ADV-P17-G4B01-REG-04` — **closed**. Source-resolution temporary materialization cleans its exact
  bounded root; saved output cancellation publishes nothing or restores prior bytes at rollback-safe
  boundaries; all singular scalar options and duplicate tools fail before context reads; trusted
  artifact-selector permission errors retain exit 6; and combined v1-ledger/missing-lock plans use
  the stable semantic order `migrate-ledger`, `write-lock`, `install` across adversarial group IDs.

Independent replay evidence:

- Core correction aggregate across saved projection, reconciliation, application service,
  artifact-pair, plan-writer, and generic ordering: **60/60 tests, 540 assertions**.
- Full CLI plan contract file: **14/14 tests, 214 assertions**.
- `bun run typecheck`: passed.
- changed-file Biome over saved projection, application service, and their focused tests: passed.
- `git diff --check`: passed.
- `bun run test:smoke`: **5/5 tests, 3,735 assertions**.

The reviewer separately replayed cross-root CLI TS09 saved bytes, report operation IDs,
dependencies, required check IDs, and precondition references. The exact 59-path ownership boundary
is sufficient; no new schema, command-specific scheduler branch, or future apply/execution authority
was introduced.

**Adversarial-review result:** PASS. The group advances to reviewed; traceability closure, a clean
40-character pin, terminal full-repository validation, and signed-off status remain pending.

## Traceability and terminal verification

Pending.

## Sign-off

Pending.
