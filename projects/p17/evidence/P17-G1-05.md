# P17-G1-05 evidence

## Mapping and prerequisites

- **Primary IDs:** `COMMAND:verify`, `EWP-CF-007`, `EWP-CF-036`,
  `EWP-CMD-VERIFY-TS01` through `EWP-CMD-VERIFY-TS04`, `EWP-P1-T11`, `EWP-P1-TS09`, and
  `P2-06`.
- **Formal dependency:** P17-G1-03 is signed off at
  `cd7ec024dc9beef0872dfd06eaa72024fffe2966`; its accepted implementation is
  `3fb4c1a86d424be9010f04394a5546a4eedd93ac`.
- **Preservation constraint:** P17-G1-04 is signed off at
  `8961e79a4931ab188ea6da0206df20165c85e9a9`; its accepted implementation is
  `c9d3fa87eaf902692da09335a8f0ff4717965d10`. G1-05 retains focused ports and does not
  reintroduce aggregate environment authority.
- **Required now:** exactly `EWP-CMD-VERIFY-TS01`, `EWP-CMD-VERIFY-TS02`,
  `EWP-CMD-VERIFY-TS03`, `EWP-CMD-VERIFY-TS04`, and `EWP-P1-TS09`.
  `EWP-CMD-AGENTS-TS03`, `EWP-P3B-TS06`, `EWP-WF01`, `EWP-WF05`, and `EWP-WF07` remain
  downstream-only.
- Independent read-only mappers `/root/g1_04_effect_migration` and
  `/root/g1_04_interface_audit`, plus delegated mapper
  `/root/g1_04_effect_migration/g1_05_plan_mapper`, identified the exact descriptor/bundle model,
  duplicated tool authorities, generic known-ID branches, current verify behavior, red-test
  families, false-red risks, compatibility surfaces, and downstream exclusions.
- Mapping found four independent built-in tool lists, direct verifier dispatch, direct
  Claude/Codex placement maps, and 13 direct known-ID policy branches across current generic core
  and CLI TypeScript (`===`/`!==` comparison count, excluding adapters/tests/data). These are
  migration targets, while adapter-owned literals remain legitimate.
- Current focused baseline passed 44 tests with 102 assertions and 11 intentional environment-gated
  live skips across core verification and CLI verify tests. Catalog generation and PR-openable P17
  validation passed before lifecycle advancement.
- Baseline command:
  `bun test packages/core/tests/verify packages/cli/tests/commands/verify.test.ts packages/cli/tests/commands/verify-live.test.ts`.
- **Mapping result:** exact scope, dependencies, compatibility obligations, validation ownership,
  generic branch boundary, and prohibited downstream behavior mapped by root Codex goal on
  2026-07-12.

## Ready plan

- **Plan:** `projects/p17/plans/P17-G1-05.md`.
- ADR 0007, one validated descriptor/bundle registry, exact four-tool operation/scope matrix,
  derived 1.x projections, bundle-owned current policies/facts, and unchanged verify/lifecycle wire
  behavior are frozen as the implementation boundary.
- Each initial descriptor uses positive safe-integer `capabilityVersion: 1`. Exact scopes are:
  unscoped detection; `user/project/system/managed` inventory and diagnostics for all four;
  explicit `artifact` verification only for Claude/Codex; and `user/project/custom` lifecycle and
  desired-state operations only for Claude/Codex.
- Root owns shared contracts/integration. Verification and placement migrations may proceed in
  parallel only after test-first and interface-freeze commits, with disjoint file ownership.
- Exact required commands are:
  - `bun test packages/cli/tests/contracts/verify.test.ts --test-name-pattern EWP-CMD-VERIFY-TS01`
  - `bun test packages/cli/tests/contracts/verify.test.ts --test-name-pattern EWP-CMD-VERIFY-TS02`
  - `bun test packages/cli/tests/contracts/verify.test.ts --test-name-pattern EWP-CMD-VERIFY-TS03`
  - `bun test packages/cli/tests/contracts/verify.test.ts --test-name-pattern EWP-CMD-VERIFY-TS04`
  - `bun test tests/ergonomics/phase/EWP-P1-TS09.test.ts --test-name-pattern EWP-P1-TS09`
- **Result:** ready for test-first implementation; no later lifecycle gate or downstream validation
  is implied.

## Test-first evidence

- The four command selectors preserve shipped behavior while failing only their named missing
  authority: registered target/wrapper manifests, registry-driven verifier dispatch, shared exit
  policy plus truthful cancellation JSON, and descriptor-derived versions/schema choices.
- `EWP-P1-TS09` fails seven architecture families on the absent validated production registry and
  builder plus 15 current generic-authority violations. Its fixture adapters, exact operation/scope
  matrix, rejection categories, compatibility/config/env/help/completion derivations, capability
  remediation, verifier/gate facts, and static gates are executable rather than documentary.
- The deterministic command selectors include plugin/bare/missing/invalid target behavior,
  automatic/explicit absence and deep-gap behavior, failure precedence, a marker-synchronized
  SIGINT verifier, human/current-v1 JSON/golden parity, and exact/drift/unknown/not-installed
  versions. No live network or installed tool is required.
- Combined red result is 5 characterization tests passed, 14 planned-authority tests failed, and 59
  assertions. Repository typecheck, targeted Biome, and diff checks pass.
- Independent reviewer `/root/g1_04_effect_migration` rejected the first draft for incomplete
  builder, derivation, fixture-routing, verifier-policy, static-authority, absence, cancellation,
  wrapper, and version coverage. It accepted the corrected boundary with 0.98 confidence and no
  remaining blocker, false red, overconstraint, or downstream scope theft.
- Fresh final reviewer `/root/g1_05_red_final_review` then found two residual gaps: TS04 used
  synthetic version reports and TS01 did not exercise registered failing-verifier cleanup. After
  real Claude/Codex verifier fakes and result-error/throw wrapper cleanup were added, it returned
  **PASS at 0.99 confidence** with typecheck/lint clean and no remaining gap.
- **Result:** all five required selectors are genuinely red against planned G1-05 production
  authority; their characterization clauses remain green and every failure is actionable.

### Execution receipt EWP-CMD-VERIFY-TS01

- Validation ID: `EWP-CMD-VERIFY-TS01`
- Command: `bun test packages/cli/tests/contracts/verify.test.ts --test-name-pattern EWP-CMD-VERIFY-TS01`
- Exit status: `1`
- Result: 1 passed and 3 failed with 10 assertions; failures name registry-derived plugin/bare
  wrapper manifests and registered failing-verifier finally cleanup.
- Revision: `working-tree`

### Execution receipt EWP-CMD-VERIFY-TS02

- Validation ID: `EWP-CMD-VERIFY-TS02`
- Command: `bun test packages/cli/tests/contracts/verify.test.ts --test-name-pattern EWP-CMD-VERIFY-TS02`
- Exit status: `1`
- Result: 1 passed and 1 failed with 13 assertions; failure names injected registry verifier
  dispatch after automatic/explicit/absence/deep-gap characterization passes.
- Revision: `working-tree`

### Execution receipt EWP-CMD-VERIFY-TS03

- Validation ID: `EWP-CMD-VERIFY-TS03`
- Command: `bun test packages/cli/tests/contracts/verify.test.ts --test-name-pattern EWP-CMD-VERIFY-TS03`
- Exit status: `1`
- Result: 1 passed and 2 failed with 7 assertions; failures expose cancellation JSON exit 1 versus
  process exit 130 and the absent shared core verify-exit policy owner.
- Revision: `working-tree`

### Execution receipt EWP-CMD-VERIFY-TS04

- Validation ID: `EWP-CMD-VERIFY-TS04`
- Command: `bun test packages/cli/tests/contracts/verify.test.ts --test-name-pattern EWP-CMD-VERIFY-TS04`
- Exit status: `1`
- Result: 2 passed and 1 failed with 22 assertions; failure names the absent registry-derived v1
  verify schema factory after real-verifier version and output/global-context/golden
  characterization passes.
- Revision: `working-tree`

### Execution receipt EWP-P1-TS09

- Validation ID: `EWP-P1-TS09`
- Command: `bun test tests/ergonomics/phase/EWP-P1-TS09.test.ts --test-name-pattern EWP-P1-TS09`
- Exit status: `1`
- Result: 0 passed and 7 failed with 7 assertions; failures name the absent registry/builder and
  remaining independent arrays, maps, imports, and known-ID policy branches.
- Revision: `working-tree`

## Implementation and validation

Pending.

## Adversarial review

Pending.

## Traceability and sign-off

Pending.
