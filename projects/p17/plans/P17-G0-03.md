# P17-G0-03 implementation plan

## Contract and ownership

P17-G0-03 reconciles artifact terminology, dependency order, architectural precedent, and the broad
quality scan without editing runtime behavior or documentation. Primary IDs are `EWP-CF-001` and
`EWP-P0A-T03..05`; G0-01 is signed off. G0-02 was integrated first as the declared merge order.

Owned implementation files are:

- `projects/p17/reviews/P17-G0-03-reconciliation.json`
- `scripts/p17-architecture-reconciliation.ts`
- `tests/ergonomics/phase/P17-G0-03.test.ts`

The JSON is the machine-readable review authority. It contains an exact four-artifact responsibility
matrix; exact Phase 2 → 3A → 3B → 4A → 4B dependency order; four factor-review buckets; a closed
quality scan; architecture-drift classifications; and exact CF-001 downstream validation coverage.

## Test-first and implementation

Add a group-boundary characterization test that initially fails because the reconciliation artifact
and validator are absent. Implement deterministic validation of exact sets, unique IDs/pointers,
meaningful non-comment content, closed dispositions, non-overlapping writer authority/forbidden
contents, dependency order, and exact downstream validation equality.

Negative mutations cover missing/duplicate artifact roles; config/manifest conflation; responsibility
overlap; wrong dependency order; missing factor bucket/disposition/evidence; unresolved or duplicate
quality findings; missing contract/validation links; and unclassified architecture drift.

Exact commands:

- `bun test tests/ergonomics/phase/P17-G0-03.test.ts`
- `bun scripts/p17-architecture-reconciliation.ts --check`
- exact signed G0-01 selectors TS01/06/07, plan/package/catalog checks, `git diff --check`, and
  `bun run check`

No parser/runtime changes, artifact codecs/writers, docs drift edits, catalog-policy work, or future
validation activation are allowed. G0-04 owns document edits; G0-05 owns catalog policy/TS09.
