# P17-G0-04 implementation plan

P17-G0-04 closes section-level documentation drift without implementing future product behavior or
the Phase-1 full help/README rewrite. Primary IDs: `EWP-CF-017`, `EWP-P0A-T08`, and
`EWP-P0A-TS05`. G0-02 and G0-03 are signed off. Required-now is TS05 only; P1-TS05 and WF16 remain
downstream.

Owned control files are `projects/p17/reviews/P17-G0-04-documentation-drift.json`,
`scripts/p17-documentation-drift.ts`, and `tests/ergonomics/phase/EWP-P0A-TS05.test.ts`. Narrow
authority-marker/current-behavior corrections are permitted in `CLAUDE.md`, `README.md`,
`docs/architecture.md`, `packages/core/README.md`, `packages/cli/README.md`,
`packages/cli/src/help/topics.ts`, `projects/p17/EXECUTION.md`, the active `research/skillsmith-*`
documents and every active page under `research/commands/`, and the active P09, promote/dev, and P13
specs/plans named in the mapping handoff. The help-topic inventory is exhaustive across all live
topics, including `environment` and `sources`; mixed historical command pages require distinct
section-level dispositions rather than one document-wide disclaimer.

Test first: TS05 fails because ledger/validator are absent. Then add an exact reviewed-document and
section inventory with real pointers, visible disposition markers, exact disposition enum, G0-03
D01/D02 closure, known stale-claim classification, and immutable downstream ownership. Negative
mutations cover missing/duplicate/comment-only rows, shifted headings, generic disclaimers,
noncanonical or incompletely modeled pointers, unresolved/invalid dispositions, invalid array
members, stale authoritative text, cross-section marker/claim movement, malformed current-behavior
notes, archive errors, known stale families omitted, catalog/status drift, and downstream activation.

No runtime/parser changes, full target-doc rewrite, future validation execution, artifact
implementation, or G0-05 catalog-policy work. Exact commands are TS05 selector, documentation and
architecture CLIs, G0-03 boundary, plan/package/catalog/diff checks, and `bun run check`.
