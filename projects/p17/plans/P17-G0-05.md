# P17-G0-05 implementation plan

P17-G0-05 closes Phase 0's executable verification-catalog contract without pulling Phase 6
publication work forward. Primary IDs are `EWP-CF-018`, `EWP-CF-039`, `EWP-P0A-T09`,
`EWP-P0A-T12`, `EWP-P0A-TS09`, and `P0-08`. Dependencies G0-01, G0-02, and G0-03 are signed
off. Required-now validations are the already-signed TS06 and TS07 plus new TS09; P6-TS06 remains
downstream.

Owned implementation files are `scripts/p17-catalog.ts` and
`tests/ergonomics/phase/EWP-P0A-TS09.test.ts`. G0-05 also owns its plan/evidence records,
`projects/p17/catalog.json`, generated `projects/p17/CHECKLIST.md`, and only the narrow mechanically
truthful Phase-0 status/checklist closure in the consolidated plan. `scripts/p17-catalog.test.ts` is
impacted but remains G0-01 history and must not be modified.

Test first: the exact TS09 selector fails because its executable target is absent. Minimal
implementation then extends the catalog validator and mutation suite to reject missing, extra,
duplicate, malformed, and orphaned records; non-executable prose/directory targets; required skips;
unreferenced deferred items; planned validations without phase/path ownership; wrong selectors,
commands, receipts, or signed revisions; future-group required-now dependencies; disappearing
downstream coverage; and nondeterministic plan-to-catalog-to-checklist output.

Impacted verification includes exact TS06/TS07/TS09 selectors, the existing catalog self-tests,
plan structure, catalog/checklist determinism, package/diff gates, and `bun run check`. Prohibited
scope: runtime packages, `justfile`, package scripts, CI/release workflows, release recipes,
four-target builds, live-tool checks, and any P6-TS06 execution.
