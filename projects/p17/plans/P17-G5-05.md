# P17-G5-05 mapped/Ready amendment candidate plan

## State and coherent boundary

P17-G5-05 closes Phase 5 by composing already-signed sync, update, undo, apply, scheduler,
approval, artifact-pair, and transaction authorities. Its only missing product behavior is the
deliberately deferred update-to-undo seam: a successful update must retain exact artifact
preimages and occurrence lineage for `undo` to restore the prior manifest/lock bytes, modes, and
intent together with the prior live resolution, including a custom selected artifact pair.

The group does not add a command, option, scheduler, approval adapter, output format, recovery
engine, or transaction schema. It activates six planned validation owners and replays eight signed
dependency validations as one dependency-complete matrix.

Primary ownership is exactly seven entities:

- `EWP-CMD-UNDO-TS03`;
- `EWP-CMD-UPDATE-TS07`;
- `EWP-OPT-TS08`;
- `EWP-P5-TS05`;
- `EWP-WF09`;
- `EWP-WF13`; and
- `P1-04`.

Required-now is exactly 14 validations:

- the six primary selectors above; and
- signed dependency replays `EWP-CMD-APPLY-TS09`, `EWP-CMD-APPLY-TS11`,
  `EWP-CMD-APPLY-TS13`, `EWP-CMD-SYNC-TS09`, `EWP-CMD-UPDATE-TS06`,
  `EWP-CMD-UPDATE-TS08`, `EWP-CMD-UPDATE-TS09`, and `EWP-P4A-TS02`.

There is no downstream coverage. G5-05 must leave no planned Phase-5 catalog entity.

## Dependency and authority audit

- Phase 5 is active.
- Direct prerequisites G5-01, G5-02, and G5-03 are signed off at all ten lifecycle gates.
- `COMMAND:update`, `COMMAND:undo`, D-010, D-011, CF-003, CF-008, CF-026, CF-027, CF-031,
  CF-040, P1-11, and P2-02 are signed authorities. P1-04 is the only remaining coverage
  recommendation owned here.
- G3B-02 owns one deterministic operation-group scheduler, fail-fast/continue policy,
  cancellation, and result/exit precedence.
- G4B-02 and G5-01 through G5-03 own exact preview, one bulk approval, dry-run/no-prompt/JSON
  behavior, and the command-specific application boundaries.
- G2-01/CF-040 own sibling lock derivation, explicit `--file` plus `--lockfile`, invocation-only
  custom-pair selection, and every prohibited selector combination.
- G5-02 owns update selection, candidate/ref/pin policy, verification, artifact-before-live
  ordering, per-tool drift truth, retained prior store objects, and `update@1`.
- G5-03 owns scope-aware undo selection, pending-before-history precedence, committed placement
  reversal, compatibility aliases, `undo@1`, and repeated no-toggle behavior.
- The artifact-pair coordinator remains the only manifest/lock mutation and crash-recovery
  authority. Placement swap/recovery remains the only live/store/ledger reversal authority.

## Existing implementation audit

The dependency implementation already supplies:

- deterministic groups and `--continue-on-error` across apply, sync, update, and undo;
- exact bulk approval and noninteractive refusal through shared interaction/application policy;
- cancellation that stops unstarted groups and exit selection that preserves the real error;
- update artifact operations followed by independent tool-pair placement operations;
- prior-store retention in each committed update placement journal;
- custom-pair update selection and exact selected-pair output;
- scope/tool/target/all undo selection and committed reversal for dev, promote, install, and
  uninstall; and
- strict update and undo v1 reports with shared immutable operations/results.

The bounded missing seam is explicit in `undo/observe.ts`: an update candidate is recognized and
then refused as `undo-update-unavailable`. A committed update placement journal retains the live
before/after and prior store, but update artifact operations currently have no durable logical
history carrier. Guessing the artifact pair from cwd, deriving a sibling lock after a custom-pair
update, or reconstructing prior portable intent from current files would violate CF-040 and the
no-guessing undo contract.

Eight already-owned dependency selectors are green on the current baseline when serialized. An
attempt to run all stateful CLI fixture processes concurrently caused UPDATE-TS06 and TS09 to
collide at ledger-directory bootstrap; each passed immediately in serialized replay and both pass
inside the complete `--max-concurrency 1` terminal gate. G5-05 public-process fixtures therefore run
serially. Pure unit/static checks may still run concurrently.

## Durable update artifact lineage

### Forward update carrier

Every changing update declaration already has one deterministic operation group containing zero or
one manifest write, zero or one lock write, and one live operation per selected tool. G5-05 makes
the changing artifact operations durable logical history members of that same group:

- each artifact journal uses the existing strict logical-journal v1 shape;
- `pairId` remains null internally for the forward artifact carrier;
- intent and actual images contain the exact selected machine-bound artifact location and strict
  canonical manifest/lock before and after identities already present on the approved operation;
- command/workflow remain `update`; operation and group IDs remain those in the approved update
  plan;
- the pending carrier is durable before the artifact coordinator mutates a file;
- artifact-coordinator recovery and logical-journal phase advance converge together under the
  canonical ledger lock; and
- the carrier enters bounded committed history only after the exact physical after-image is
  authoritative.

The implementation must not add optional fields to the journal codec, persist CLI selectors in a
manifest/config, copy credentials/local paths into portable files, or create a second artifact
recovery record. Existing pre-G5-05 update history without an exact artifact carrier is visibly
non-reversible for artifacts and refuses with state remediation; undo never guesses or silently
restores only live state.

Manifest history is required when update changed requested ref/pin intent. Lock history is required
whenever update changed the selected resolution. A normal moving update commonly has only a lock
carrier; `--ref ... --pin` may have both. No-op artifact rows manufacture no carrier.

### Exact retained preimages

An operation image proves semantic and byte identity but does not contain losslessly authored TOML
bytes or filesystem mode. G5-05 therefore does not attempt to reconstruct a manifest from its
normalized snapshot. Before each changing artifact operation, while the existing artifact-group,
member, and ledger locks are held, one update-history binding:

1. appends the strict pair-null logical journal in `prepared` phase, with a conditional retention
   resource declared by the approved operation;
2. creates a deterministic owner-private backup envelope containing the exact regular-file bytes
   plus a strict canonical receipt for source role and observed permission mode, then fsyncs every
   envelope file and directory;
3. advances the same journal to `backed-up` with one `actual.retained` backup row whose source role,
   path, repository revision, and content hash match that complete durable envelope;
4. invokes the existing artifact-pair coordinator for the already-approved edit/replace action;
5. observes the authoritative physical after-image and advances the journal through `live` to
   `committed`; and
6. leaves the exact backup retained until the whole update occurrence is no longer inside the undo
   window and existing history cleanup removes it.

This backup envelope is retained restoration material, not a second recovery record: the strict
pending journal is the recovery authority and the existing artifact coordinator remains the only
manifest/lock mutation authority. Its canonical envelope digest authenticates both payload and mode
receipt, solving the fact that journal v1 has no standalone mode field. A crash before the envelope
is durable leaves a prepared carrier and no mutation. A crash after envelope fsync reuses the
deterministic copy. A crash after the coordinator reaches the after-image advances the pending
carrier rather than replaying the edit. Conflicting bytes, type, identity, receipt, mode, ownership,
permission, missing backup, or unmatched recovery state refuses without guessing. Both manifest
and lock preimages are retained when they change so exact bytes and modes—not merely normalized
values—are restorable.

The private backup may contain losslessly authored portable content, so it stays under the existing
owner-private data boundary, is never emitted in normal human/JSON output, and is covered by the
existing secret/redaction and history-cleanup rules. The approved public operation continues to
carry only hashes, normalized values, and its already-authorized machine-bound artifact location.

### Transition-specific identity and one history occurrence

`updateGroupIdV1` currently hashes the resulting source but not the prior artifact state, so
A->B, B->C, C->B can reuse the B group and operation identities. G5-05 changes only update's group
identity input: the existing `target` fact becomes a versioned digest of the exact manifest and lock
before/after identities. It contains no raw bytes or path. Dry-run and execution remain identical,
while A->B and C->B no longer collide.

Group ID is still not treated as a globally unique historical occurrence. Correlation starts from
the exact selected placement source transaction, then walks commit order to the nearest unconsumed
forward artifact carriers with the same transition-specific group, exact role/path/image chain,
and matching update command/workflow. A later exact repeat selects its later carrier; rollback
children consume only their exact parent transaction. Missing, duplicated, crossed, or superseded
occurrences refuse. Tests include A->B->C->B, an exact repeated transition, different prior
manifest trivia, two tools, intervening history, and sibling/custom pairs.

### Observation and lineage closure

Undo continues to select placement/history pairs through status. For an update source group it
additionally correlates sibling committed artifact carriers by exact source transaction,
transition-specific group ID, commit order, and artifact image chain, and validates:

- the selected live journals are committed forward updates or their exact rollback children;
- every artifact carrier has one unambiguous selected path, exact before/after image, and readable
  exact retained preimage with the original mode;
- its current physical artifact equals the expected after-image, or already equals the exact prior
  image after a committed rollback child;
- no newer committed or pending artifact operation supersedes that path/group lineage;
- a custom manifest/lock pair comes only from the recorded carrier, never sibling derivation;
- all selected pair journals and artifact carriers agree on one historical occurrence, source
  group, command/workflow, and project/scope identity; and
- malformed, missing, future, ambiguous, stale, partially observed, or permission-denied state
  refuses before approval or writes.

One selected update history group may contain multiple tool pairs but only one artifact transition.
Tool filtering may select a subset of pairs; artifact restoration is then explicit shared group
work and any remaining updated pair is reported as ordinary declared/live drift. An unfiltered
`undo <skill>` restores all selectable pairs and the artifact pair together.

For each artifact path, a multi-group selection must form one contiguous suffix of unconsumed
forward transitions. Planning orders that suffix newest-first. Selecting an older transition while
omitting a newer transition on the same path refuses with remediation to undo the newer group or
select the complete suffix. Distinct recorded custom pairs never collapse into sibling derivation;
if one invocation selects incompatible pair authorities that cannot share the signed lock topology,
it refuses before approval and names the conflicting recorded paths rather than partially mutating.

### Pure inverse plan

The immutable undo planner adds the exact inverse manifest/lock operations to the already-selected
update group by swapping the recorded forward artifact before/after images and binding the private
replacement action to the validated retained bytes/mode. It does not parse current portable intent
as restoration material, allocate a new source candidate, or invoke the update planner.

- inverse artifact operations use fresh deterministic rollback operation/transaction IDs and point
  internally to their exact forward parent operation IDs;
- operations remain in the existing undo operation vocabulary (`write-manifest`, `write-lock`);
- artifact operations remain group-level with `pairId`, skill, source, tool, and scope all null;
- `group.operations` exactly partitions into the canonical group-level artifact prefix plus the
  concatenated live-only `pair.operations`; omission, duplication, reordering, or pair-binding is
  invalid;
- the public pair retains its live source/active/parent lineage; artifact parent linkage remains in
  the private logical journal and exact operation images;
- an already-restored artifact transition contributes no operation;
- a group with all live pairs and artifacts already reversed remains `already-reversed` with zero
  mutation; and
- dry-run and execution expose byte-identical operations, dependencies, checks, selected group IDs,
  and artifact locations.

No field or enum is added to `undo@1`. Its validator admits the already-existing `write-manifest`
and `write-lock` operation vocabulary for this group-level partition. Pair outcomes reduce only
their live operations. Group outcome/failure reduces every group operation in deterministic result
order, so an artifact failure is never hidden behind a pair `not-run` result. Existing effect
projection remains schema-frozen; artifact truth is visible in the operation/result vectors. If
implementation cannot stay inside this shape, work stops for an explicit contract amendment before
product code continues.

### Schema-preserving artifact rollback lineage

Journal v1 already admits pair-null manifest/lock operations, but its fresh-rollback resolver and
logical-transaction helpers currently require a placement inverse with a legacy pair shadow. G5-05
adds an explicit artifact branch without changing serialized fields:

- a forward `write-manifest` or `write-lock` parent is eligible only when committed, pair-null,
  path-exact, and backed by the declared digest-authenticated preimage envelope;
- a rollback child keeps the parent's intent images and retained facts, names a fresh transaction
  and operation, names the exact parent operation in context, and advances actual state from the
  parent's after-image back to its before-image;
- no legacy placement shadow is required or written for an artifact child;
- one parent has at most one exact child, and resume finds the child by transaction lineage rather
  than operation ID alone; and
- wrong role/path/image, missing backup, duplicate child, future shape, stale phase, or mismatched
  command/workflow makes the ledger invalid before mutation.

Prepared through committed phases, pending resume/abort, duplicate and hostile parents, and child
cleanup are covered at the logical-transaction, ledger-codec, and history layers.

### Atomic bounded-history unit

History retention treats one update occurrence as an indivisible dependency unit: its forward
artifact carriers, every placement journal correlated to the occurrence, every rollback child, and
every retained preimage. If any member is newest for a placement/artifact anchor, is a pending
parent, has live retention, or is otherwise protected, the complete unit is protected and effective
capacity expands. When excluded, cleanup preflights retained resources and removes the complete
dependency unit through the existing restartable victim protocol. It never evicts an artifact
carrier while leaving its update placement selectable.

Occurrence units derive from transaction/commit-order and exact image-chain facts, not group ID
alone, so a later identical transition does not retain or delete the wrong earlier unit. Coverage
includes more than 256 history entries, two tools, custom pairs, pending children, cleanup restart,
an exact repeated transition, and safe backup deletion only after the final reference leaves the
window.

## Execution, crash, and rerun contract

Approved update reversal uses one scheduler and the two signed physical authorities:

1. reacquire the canonical ledger lock and reobserve the exact update group, artifact carriers,
   selected live/store state, and expected revisions;
2. recover any same-operation artifact or placement record before new work;
3. publish rollback logical authority for the exact artifact parent before artifact mutation;
4. restore the exact selected artifact preimages through the artifact coordinator in reverse
   forward order (lock, then manifest when both changed), preserving the retained mode;
5. reverse each selected committed placement through the existing committed placement-reversal
   path, retaining pair independence and prior-store validation;
6. append committed rollback children without editing/removing forward history; and
7. clean private recovery state only after every corresponding terminal image is durable.

Artifact restoration precedes live rollback. This guarantees there is no state in which every live
pair appears terminally reversed while the artifact restoration has never become authoritative.
Each durable artifact sub-boundary has a pending rollback child, so a failure between lock and
manifest restoration is truthful recoverable state, not an unjournaled inconsistency. If a later
artifact or pair fails, the failure remains nonzero, successful reversals remain committed, and a
fresh rerun skips exact committed children while converging only remaining work.

Cancellation before a durable boundary performs no write. Cancellation after a boundary leaves one
strict pending record and stops all later operations/groups. Permission and external-writer errors
retain their exact exit classes. Fail-fast marks later groups `skipped-after-failure`; explicit
continuation runs independent later groups but never hides the original failure. Invocation-level
syntax, ambiguity, state, artifact-lineage, approval, and no-prompt failures abort before mutation
under both policies.

## Shared bulk approval and scheduling closure

P1-04 is satisfied by composition, not a new facade:

- each command prepares one complete immutable selection and operation set;
- changing human execution receives one exact bulk prompt for that set;
- JSON, `--no-prompt`, and non-TTY execution require `--yes` where the command contract requires
  approval;
- `--yes` conflicts with dry-run/check before discovery or I/O;
- refusal/cancellation creates no artifact, ledger, live, store, backup, or recovery write;
- group order is deterministic and pair results remain independently visible;
- default fail-fast and explicit continuation have identical within-group semantics; and
- cancellation always stops regardless of continuation.

EWP-P5-TS05 proves this across sync, update, and undo. EWP-WF13 completes the dependency-scheduled
install/apply/sync/update/promote multi-tool matrix while retaining P4A-TS02 as its signed crash and
partial-failure foundation.

## Uniform artifact-option closure

EWP-OPT-TS08 is characterization plus cross-command integration, not a new option implementation.
It asserts, from live command declarations and spawned commands, that:

- status, doctor, check, install, uninstall, export, plan, fresh apply, sync-save, and update derive
  a sibling lock by default and accept one explicit advanced lock override only with one file;
- explicit paths are reported exactly, are never persisted, and must be repeated on later direct
  invocations;
- repeated singular file/lock selectors refuse before discovery;
- init rejects lockfile; no-save install/uninstall reject either selector; unsaved sync rejects
  either selector; and saved-plan apply rejects both;
- ordinary omission after a custom-pair command deliberately returns to sibling derivation; and
- portable-relative versus machine-bound absolute custom paths retain the signed plan behavior.

No command or option relation is changed unless this executable matrix identifies a real divergence
from CF-040. Any such divergence is corrected at the existing shared selector/registry authority,
not patched per command.

## Test-first boundary

Seven implementation-facing test paths establish the dependency-complete boundary:

- `packages/cli/tests/contracts/undo.test.ts` adds EWP-CMD-UNDO-TS03;
- `packages/cli/tests/contracts/update.test.ts` adds EWP-CMD-UPDATE-TS07;
- `packages/cli/tests/contracts/options.test.ts` adds EWP-OPT-TS08;
- `tests/ergonomics/phase/EWP-P5-TS05.test.ts` adds the Phase-5 bulk/scheduler matrix;
- `tests/ergonomics/workflows/EWP-WF09.test.ts` adds the update lifecycle;
- `tests/ergonomics/workflows/EWP-WF13.test.ts` adds the dependency-complete partial-failure
  workflow; and
- `tests/ergonomics/fixtures/p5-batch/fleet.ts` provides hermetic local Git, artifact, tool,
  permission/cancellation, and public-process helpers without public network or real credentials.

The existing `p5-update` and `p5-undo` fleets may be extended only for command-specific state that
would be duplicated unsafely in the shared fleet. Tests import public APIs or spawn the real local
CLI; they do not import private product implementation to manufacture success.

Expected first execution:

- UNDO-TS03, UPDATE-TS07, and WF09 fail on the exact current `undo-update-unavailable` boundary or
  the absent durable artifact carrier;
- OPT-TS08, P5-TS05, and WF13 may begin as green characterization where their signed dependencies
  already satisfy the contract; a failing negative architecture/lineage assertion is added where
  needed rather than manufacturing a false functional red; and
- all eight signed dependency selectors remain green when serialized.

### Primary selector matrices

`EWP-CMD-UNDO-TS03` covers ordinary moving update, explicit ref, pin transition, sibling and custom
artifact pair, one/two-tool selection, exact manifest/lock/live restoration, retained authored TOML
bytes and modes, prior-store retention, stale/missing/superseded artifact lineage refusal,
contiguous newest-first transition chains, partial reversal, crash points, and repeated zero-toggle
convergence.

`EWP-CMD-UPDATE-TS07` proves a new update writes exact reversible artifact lineage and owner-private
preimage retention, retains prior store state through the undo window, exposes no secret/local-path
leakage beyond machine-bound operation locations, and feeds direct undo without a second recovery
format.

`EWP-OPT-TS08` owns the complete advanced-pair matrix described above.

`EWP-P5-TS05` owns exact one-prompt/no-prompt/JSON/TTY behavior, deterministic group order,
within-group pair truth, fail-fast skipped markers, continuation, cancellation, exits, dry-run
identity, and rerun behavior across sync/update/undo.

`EWP-WF09` runs the normative command sequence for check, ref/pin check, targetless refusal,
dry-run, execute, bulk preview, and undo across moving/fixed/offline/failure/hash matrices.

`EWP-WF13` runs both Claude-success/Codex-failure directions across install, apply, sync, update,
and promote, proving invocation-level abort, pair independence, revision truth, scheduling policy,
nonzero failure preservation, and fresh rerun convergence.

## Candidate ownership manifest

The amended candidate boundary is 49 unique paths: 21 product/architecture paths, 20 focused
unit/contract paths, six dependency-complete acceptance owners/fixtures, and plan/evidence.

Product and architecture:

- `docs/architecture.md`
- `packages/core/src/application/undo-service.ts`
- `packages/core/src/application/update-service.ts`
- `packages/core/src/artifacts/ledger-codec.ts`
- `packages/core/src/artifacts/ledger-history.ts`
- `packages/core/src/artifacts/plan-codec.ts`
- `packages/core/src/artifacts/execution.ts`
- `packages/core/src/contracts/v1/undo.ts`
- `packages/core/src/reconcile/apply-execution.ts`
- `packages/core/src/place/logical-transactions.ts`
- `packages/core/src/status/join.ts`
- `packages/core/src/status/types.ts`
- `packages/core/src/undo/artifacts.ts`
- `packages/core/src/undo/execute.ts`
- `packages/core/src/undo/index.ts`
- `packages/core/src/undo/observe.ts`
- `packages/core/src/undo/plan.ts`
- `packages/core/src/undo/types.ts`
- `packages/core/src/update/execute.ts`
- `packages/core/src/update/history.ts`
- `packages/core/src/update/plan.ts`

Focused unit and contract tests:

- `packages/core/tests/application/undo-service.test.ts`
- `packages/core/tests/application/update-service.test.ts`
- `packages/core/tests/artifacts/ledger-history.test.ts`
- `packages/core/tests/artifacts/ledger-writer.test.ts`
- `packages/core/tests/artifacts/execution.test.ts`
- `packages/core/tests/contracts/undo.test.ts`
- `packages/core/tests/place/logical-transactions.test.ts`
- `packages/core/tests/reconcile/apply-execution.test.ts`
- `packages/core/tests/status/read.test.ts`
- `packages/core/tests/undo/artifacts.test.ts`
- `packages/core/tests/undo/execute.test.ts`
- `packages/core/tests/undo/observe.test.ts`
- `packages/core/tests/undo/plan.test.ts`
- `packages/core/tests/update/execute.test.ts`
- `packages/core/tests/update/history.test.ts`
- `packages/core/tests/update/plan.test.ts`
- `packages/cli/tests/contracts/options.test.ts`
- `packages/cli/tests/contracts/undo.test.ts`
- `packages/cli/tests/contracts/update.test.ts`
- `packages/cli/tests/output/undo.test.ts`

Acceptance owners and fixtures:

- `tests/ergonomics/fixtures/p5-batch/fleet.ts`
- `tests/ergonomics/fixtures/p5-undo/fleet.ts`
- `tests/ergonomics/fixtures/p5-update/fleet.ts`
- `tests/ergonomics/phase/EWP-P5-TS05.test.ts`
- `tests/ergonomics/workflows/EWP-WF09.test.ts`
- `tests/ergonomics/workflows/EWP-WF13.test.ts`

Governance:

- `projects/p17/plans/P17-G5-05.md`
- `projects/p17/evidence/P17-G5-05.md`

New module/test paths are candidates, not pre-authorized placeholders. Ready review may narrow or
replace them while preserving the exact behavior and 14-validation boundary. Implementation cannot
edit outside the accepted manifest without a recorded amendment and fresh review.

## Implementation order

1. Add the six primary selectors and hermetic fixture boundary; record honest red/characterization
   results and the eight serialized dependency baselines.
2. Make update group identity transition-specific and add strict forward artifact-history
   projection, exact preimage retention, and codec/history validation around approved artifact
   operations.
3. Bind prepared/backed-up/live/committed artifact lineage to the existing artifact-coordinator
   execution and recovery boundary under the ledger lock.
4. Generalize artifact rollback parent/child lineage and whole-occurrence bounded-history units
   without changing journal v1.
5. Correlate update artifact lineage from exact placement source transactions during undo
   observation and reject missing/stale/ambiguous/non-contiguous history before approval.
6. Build pair-null inverse artifact operations and exact group-level/pair-level `undo@1` reductions
   without changing its serialized fields or enums.
7. Execute retained-preimage artifact operations through external scheduler bindings and committed
   placement reversal through the signed swap authority.
8. Close application approval/mutation accounting, output parity, custom-pair, partial-failure,
   cancellation, history pressure, and crash/rerun matrices.
9. Refactor only after all primary and required-now selectors are green.

## Validation policy

Inner loops run the changed core suites, one primary selector at a time, and `bun run test:smoke`.
Because ledger, recovery, artifact barriers, cancellation, and crash behavior are in scope, every
recovery-affecting iteration also runs `bun run test:smoke:p2-ts04:recovery`. Stateful CLI fixture
selectors run serially; pure unit/static suites may run concurrently.

Targeted green requires all six primary selectors as separate processes. Impacted green requires
all 14 frozen commands, also as separate serialized processes, with exact receipts and no required
skip. Typecheck, boundary lint, catalog/package/plan structure, and diff checks are mandatory. A
full serialized terminal `bun run check -- --timeout 60000 --max-concurrency 1` runs only after
fresh independent GO and all proportional gates pass.

## Non-goals and stop conditions

- no new command, option, alias, output schema version, journal schema field, or recovery format;
- no normalized reconstruction where a retained exact artifact preimage is required;
- no undo of arbitrary current files or pre-lineage update history by inference;
- no global “latest operation” lookup or cross-project crawl;
- no change to saved-plan authorization, GC, distribution, help/completion, or Phase 6;
- no second scheduler, approval loop, artifact writer, or placement reversal engine;
- no parallel execution of stateful public-process fixtures; and
- no public-contract widening discovered during implementation without an explicit amendment,
  structural validation, fresh adversarial review, and user authorization.

## Ready-review questions

The independent reviewer must attempt to falsify at least:

- whether strict journal v1 can truthfully carry artifact operations without a schema change;
- whether artifact logical history and artifact recovery can diverge at any crash boundary;
- whether custom-pair identity survives update-to-undo without guessing;
- whether retained bytes and mode restore losslessly authored manifest/lock state for ref/pin
  transitions without a second recovery format;
- whether pair-null group-level artifact operations keep strict `undo@1` coverage and group/pair
  result truth;
- whether artifact rollback children validate without a legacy placement shadow;
- whether bounded history retains and evicts one exact update occurrence atomically;
- whether transition-specific identity plus source-transaction/commit-order correlation separates
  A->B->C->B and exact repeated occurrences;
- whether artifact-first reversal and fresh rerun converge after every pair failure/cancellation;
- whether old update history refuses safely and actionably;
- whether the 49-path boundary omits a real shared authority or includes unnecessary scope; and
- whether the six primary selectors plus eight dependency replays completely prove P1-04,
  EWP-WF09, EWP-WF13, CF-026, and CF-040 without weakening signed tests.
