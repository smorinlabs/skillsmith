# P17-G5-03 Ready plan

## State and boundary

P17-G5-03 adds one public `undo` lifecycle for scope-aware pending abort and eligible committed
reversal. It extends the already-signed Phase-3B transaction/history/recovery foundation and the
Phase-1 selection/application/runtime foundations. It does not create a second transaction engine,
guess the latest operation globally, reinterpret retention, or implement update reversal.

The group has exactly 13 required-now validations:

- signed impacted replays `EWP-CMD-DEV-TS05` and `EWP-CMD-PROMOTE-TS05`;
- new undo selectors TS01, TS02, and TS04 through TS09;
- `EWP-P5-TS03`, `EWP-WF05`, and `EWP-WF11`.

`EWP-CMD-UNDO-TS03`, `EWP-CMD-UPDATE-TS07`, and `EWP-WF09` remain downstream under
P17-G5-05 because all three require update-to-undo lock restoration. They remain reciprocal
secondary coverage for G5-03 and are not executed or advanced here.

## Dependency and authority audit

- Phase 5 is active. Direct dependency P17-G4B-03 is signed off at all ten gates.
- D-010, CF-006, CF-024, CF-026, CF-028, CF-031, CF-034, CF-035, and CF-036 are signed off.
- Dev/promote TS05 are signed off and retain exact compatibility output/plan identities.
- Ledger v2 `transactions` and bounded committed `history` are the only logical recovery and
  reversal authority. Missing alone means empty; invalid/newer state refuses.
- `abortPendingLogicalTransaction` owns the pure forward-to-rollback transition after retained
  resource validation. Placement recovery owns target-correlated logical placement and move-scope
  rollback. Separate artifact-pair recovery records remain coordinator-owned same-operation
  recovery/blockers because they do not carry enough skill/tool/scope/parent identity for safe
  target-bound undo selection.
- Status correlation owns pending-before-history precedence, journal membership, retained-resource
  observation, eligibility, and exact remediation argv. Undo consumes these facts; it does not
  independently derive a competing eligibility result. Its disposition-aware join marks a terminal
  rollback non-reversible with no reverse argv while keeping a pending rollback eligible for the
  same undo argv that resumes it; this does not change the public `status@1` shape.
- Shared selection owns target-or-all grammar, exact path/name/tool/scope matching, ambiguity,
  unmatched refusal, filter-zero behavior, and selection-source metadata.
- The shared scheduler owns deterministic group order, fail-fast, explicit continuation,
  cancellation, and exit precedence.

## Public request and selection contract

The exact command is `skillsmith undo <skill...> | --all` with repeatable `--tool`, one
`--scope user|project` or matching `--user`/`--project` sugar, `--dry-run`, `--yes`,
`--continue-on-error`, and `--json`.

Validation happens before context discovery or I/O:

- no target and no `--all` is usage exit 2;
- positional targets conflict with `--all`;
- `--yes` conflicts with `--dry-run`;
- scope sugar is exclusive and must agree with `--scope`;
- repeated singular scope values refuse;
- unknown tools are usage errors and registered tools without undo capability are capability exit 4.

Candidate observation is bounded to user plus current-project history. Outside a project, project
scope requires an explicit valid project context and is never inferred. An unscoped name matching
both user and current-project history is ambiguous; explicit scope or one exact placement path
selects one scope. Within one selected scope, the same exact skill name may select at most one pair
per selected tool when every pair has the same canonical source operation-group ID. That exact
literal name resolves to one name/scope group with deterministically ordered tool pairs, as required
by WF05's exact `undo review --project` command after its grouped Claude and Codex promote. A glob,
different names, matches spanning scopes or source group IDs, or multiple anchors within one
tool/scope remain ambiguous and require a narrower scope, tool, or exact path. Explicit unmatched
targets refuse and never widen. `--all` spans both writable scopes unless narrowed and reports
`explicit-all`. Active tool/scope filters that reduce a valid all-set to zero are a successful
explained no-op.

For each selected pair, an attached pending logical or preserved legacy journal wins over committed
history. Otherwise the newest retained committed journal for that exact pair anchor is selected.
An ineligible newest candidate refuses; undo never skips it to expose an older action. Repeated
names, paths, or transaction IDs place each selected pair exactly once inside its canonical
name/scope/source group; distinct tool/pair rows are never merged.

Disposition is part of candidate identity. A forward pending transaction is eligible for durable
conversion to rollback. A rollback pending transaction is eligible only to resume that same
rollback and retains whether it originated as pending abort or committed reversal through its
transaction/operation/parent linkage. The newest committed rollback for the selected parent is
explained as `already-reversed` when its restored image remains current; it creates no new reversal
operation and is never reversed again, and selection never falls through to the older forward
history row. A surviving committed physical cleanup shadow may still require terminal recovery
during an approved execute or an all-cleanup-only execute, as frozen by F22. Missing or inconsistent
origin linkage refuses as invalid state.

## Immutable observation and plan

New private `undo` modules produce a deeply immutable observation and pure plan:

- `observe.ts` reads the canonical ledger, user/current-project placement facts, status journal
  correlation, retained-resource probes, capability facts, and migration state through focused
  ports/repositories;
- `plan.ts` consumes only that observation plus the normalized request and composes the shared
  snapshot-bound placement planner to create stable group, operation, precondition, check, and
  diagnostic identities;
- `types.ts` closes requests, candidate facts, plan groups, execution results, and errors;
- `execute.ts` consumes the exact approved plan through the shared placement-plan executor and
  revalidates it under the canonical lock;
- `index.ts` is the private domain entrypoint.

`undo` is added to the shared `CurrentMutatorCommand` and immutable-plan validator. Like dev and
promote, undo prohibits bounded-default selection. The existing retained-history rollback request,
planner overload, validator, and placement-plan execution input are generalized from dev/promote to
undo; no private planning or execution substitute is permitted. Alias projections continue to use
the same rollback plan and executor with `reportOp: rollback` compatibility output.

Each canonical group records its exact group ID, skill, scope, deterministically ordered pair rows,
aggregate operation IDs, aggregate outcome, and aggregate failure. Each pair row is labeled exactly
`abort-pending` or `reverse-committed` and records tool/pair identity, source and active transaction
IDs, source and active operation IDs, parent operation ID, disposition, phase, execution mode
(`convert-to-rollback` or `resume-rollback`), operation family, before-state class, eligibility,
retention requirements, exact public operation IDs, pair outcome, and pair failure. One group
attempts and reports every sibling pair through the signed scheduler; a pair may be
`already-reversed` without manufacturing an operation while another pair in the same group remains
actionable. Group outcome is the deterministic reduction of its pair outcomes and never hides a
pair failure. Planning is deterministic under input permutation and does not allocate clocks/IDs
until a genuinely new executable group starts; resumption reuses the existing rollback identities.

The undo@1 group/pair reduction is closed:

- pair rows use canonical registry tool order; each pair's operation IDs retain public plan order;
- `already-reversed` pairs have zero new reversal operations, no scheduler result, null failure, and
  manufacture no new reversal work; actionable pairs are `planned` before execution. F22 cleanup
  of an already committed reversal is recovery preflight, not a report operation/effect;
- after execution, pair outcome is `failed` if any operation failed, otherwise `cancelled` if any
  cancelled, otherwise `not-run` if any operation is `skipped-after-failure` or an expected result
  is absent, otherwise `succeeded`; pair failure is the first failed operation's sanitized public
  error in operation order and is null otherwise;
- group operation IDs are the duplicate-free concatenation of pair operation IDs in pair order;
  group outcome precedence is `failed > cancelled > not-run > succeeded > planned >
  already-reversed`, with `already-reversed` only when every pair has that outcome;
- group failure is the first failed pair's public failure in pair order; each pair retains its own
  failure. A mixed actionable/already-reversed group takes the actionable aggregate outcome;
- `summary.selected` counts canonical groups, `actionable` counts groups containing an actionable
  pair, and `alreadyReversed` counts all-already-reversed groups. Planned/succeeded/failed/cancelled/
  notRun count aggregate group outcomes. `skipped-after-failure` maps to `not-run`, so the retained
  `skipped` summary field is zero for G5-03. Effects continue to count exact operation effects.

Only undo@1 gains the group/pair representation; flip@4 remains schema-frozen. Both rollback aliases
reuse the same prepared selector, plan, and executor. On one unchanged multi-tool fleet, direct undo,
`dev --rollback`, and `promote --rollback` expose identical operations, checks, and diagnostics in
the same order, including their shared group ID and distinct pair/operation IDs. Flip compatibility
results retain one row per pair in canonical pair order. Focused projection tests cover sibling
success plus pair failure, cancellation, and not-run without hiding any execution result. TS08's
same-fleet no-write proof and separate human deprecation invocation remain unchanged; signed
DEV-TS05 and PROMOTE-TS05 remain mandatory.

Version-1 ledger reads and dry-runs remain byte-identical and visibly prefix the shared
`migrate-ledger` operation. The first real undo performs the same signed migration under the
existing ledger lock. A migration race stales the approved plan; a legacy pending pair journal is
preserved and remains selectable only for same-operation resume or pending undo.

## Pending abort execution

Logical forward pending abort uses the signed Phase-3B sequence:

1. reobserve the selected journal, current resources, and retained resource revisions;
2. durably begin one recovery attempt with command `skillsmith-undo` and workflow `undo`;
3. call `abortPendingLogicalTransactionAfterRecoveryAttempt` to convert the same transaction ID to
   rollback disposition at `prepared` while preserving start time and parent operation identity;
4. persist that prepared rollback before any physical mutation;
5. dispatch exact placement or move-scope recovery through the existing physical authority;
6. atomically reach committed rollback history, remove pending state, and finish cleanup.

An already-pending rollback bypasses the forward-only abort reducer and resumes physical rollback
with the existing transaction/operation/parent identities. This applies both to a crash after
forward-pending conversion and to a crash during a fresh committed reversal. Missing, stale, shared,
mismatched, unowned, or unverifiable before/retained state refuses without best effort.
Cancellation before a durable boundary leaves the old phase authoritative; after a boundary it
leaves one resumable rollback phase. A crash after conversion may resume rollback only; rerunning
the forward command refuses. Pending state always wins over an older committed candidate.

A preserved v1 legacy pair journal has no logical transaction entry and never enters the v2 abort
reducer. A real mutation first performs the signed byte-preserving v1-to-v2 migration when needed,
then the existing legacy placement recovery path rolls back that same pair journal without
fabricating a logical transaction or changing its ID/schema. Crash/retry resumes the legacy path.

An overlapping standalone artifact-pair recovery record is not selected or reinterpreted by
target-bound undo. It blocks with the coordinator's exact same-operation remediation and must first
converge through that owning command. Undo never guesses correlation from artifact paths or mutates
the recovery-record format in this group.

## Committed reversal execution

Committed promote, dev, install, and uninstall reversal creates one new rollback-disposition
transaction; it never edits or removes the original committed history row. The new transaction has
fresh operation/transaction identity, the original operation as parent, and the original forward
intent orientation: `intent.before` remains the state to restore and `intent.after` remains the
state being reversed. The rollback reducer owns the inversion; undo must not invert the intent a
second time. The approved current image must equal the original `intent.after`, the new
`actual.before` records that current image, and the new `actual.after` records the restored original
`intent.before`. The original retained resources remain required to restore that prior state.

The canonical swap terminal seam becomes rollback-origin aware. Converted pending abort preserves
its signed terminal projection. A fresh committed reversal is distinguished by its active operation
and parent linkage and commits `actual.before` as the approved current/original-after image and
`actual.after` as the physically restored parent-forward-before image. It never substitutes current
state for restored state merely because the disposition is rollback. Swap crash tests prove both
origins at every durable phase and refuse inconsistent linkage.

The canonical repository/coordinator/swap authorities stage, commit, roll back, recover, and clean
the inverse work. Retention and current-resource revisions are revalidated before staging. An
edited current copy is preserved through the existing backup policy. Successful reversal appends
one complete committed history row. A pending instance resumes that same inverse transaction. Once
committed, another undo reports `already-reversed` with zero new reversal mutation when the restored
image still matches; it never reverses the rollback or falls back to older history. A surviving F22
cleanup shadow is terminal recovery for that already committed reversal and does not change this
classification. Stale, inconsistent, or unrestorable state refuses without falling back to older
history.

Update reversal and portable lock restoration are deliberately recognized but refused as
downstream-not-yet-available in this group. G5-05 owns UPDATE-TS07, UNDO-TS03, and WF09 and will add
that exact operation-family composition without changing G5-03 selection semantics.

## Compatibility aliases

`dev --rollback` and `promote --rollback` become deprecated adapters over the same prepared undo
product. They do not retain an independent selector or executor. Their compatibility projection
preserves the signed flip@4 plan/result bytes and exit behavior required by DEV-TS05 and
PROMOTE-TS05 while adding one structured deprecation warning naming `skillsmith undo`. Direct
`runRollback` remains a compatibility API, not public command authority.

## Application, approval, output, and exit policy

`undo-service.ts` normalizes the parser request, prepares once, presents one exact bulk preview,
and executes the same frozen plan after approval. Dry-run never prompts or writes. Interactive
refusal and cancellation perform no migration, ledger, live, artifact, or cleanup write. JSON and
`--no-prompt` require `--yes` for changing execution.

The exact preview contains ordered group and operation IDs. The application context gains no raw
filesystem/network/process bag; it composes existing focused runtime and artifact coordinator
ports. Mutation counts derive from exact operations/results, including truthful partial success.

`undo@1` is a strict named codec and report with:

- `kind`, `schemaVersion`, and `command` identity;
- selection source/outcome, targets, tools, scopes, and project context;
- mode and approval state;
- ordered groups with skill/scope, aggregate operation/outcome/failure facts, and ordered pair rows;
- per-pair tool/action/phase/before/eligibility/retention, disposition, source/active/parent linkage,
  execution mode, operation/outcome/failure, and terminal versus F22 cleanup-pending
  `already-reversed` facts through its correlated diagnostic;
- exact public operations, checks, results, effects, diagnostics, and summary;
- no internal journal object, capability object, stack/cause, credential, or mutable reference.

Human and JSON render the same facts. Usage/capability/state/failure/permission/cancellation remain
numeric exits 2/4/3/1/6/130 through shared application exit policy. Default scheduling stops later
groups after the first actual failure; `--continue-on-error` continues independent groups while a
real error still determines the final exit.

## Test-first contract

Exactly five implementation-free paths establish the red boundary:

- `packages/cli/tests/contracts/undo.test.ts` — TS01, TS02, TS04..TS09;
- `tests/ergonomics/fixtures/p5-undo/fleet.ts`;
- `tests/ergonomics/phase/EWP-P5-TS03.test.ts`;
- `tests/ergonomics/workflows/EWP-WF05.test.ts`;
- `tests/ergonomics/workflows/EWP-WF11.test.ts`.

The 11 new selectors must execute independently and fail only because public undo behavior is
absent. Missing selectors, skip/todo, type errors, impossible fixtures, public network access,
placeholder success, or an implementation import from fixtures are invalid red. Signed dev and
promote TS05 remain green at the red checkpoint.

TS01, TS02, TS04, TS05, and TS09 explicitly cover preserved-legacy migration/rollback/retry, fresh
committed reversal, logical rollback-pending resumption after each durable boundary, status
eligibility/remediation for pending versus terminal rollback, and repeated post-commit convergence
to `already-reversed` without toggling or older-history fallback.

Required selector commands are the eight ID-bound commands against `undo.test.ts`, plus the exact
P5-TS03, WF05, WF11, DEV-TS05, and PROMOTE-TS05 commands declared by the catalog. Every green
receipt is replayed individually on the terminal commit.

## Exact owned path manifest

The amended candidate owns exactly 88 product/test/governance paths. Plan and evidence records are
added to catalog ownership but are not product paths.

### Architecture and production (41)

- `README.md`
- `docs/architecture.md`
- `packages/core/src/artifacts/ledger-codec.ts`
- `packages/core/src/artifacts/ledger-history.ts`
- `packages/core/src/artifacts/plan-codec.ts`
- `packages/core/src/application/current-services.ts`
- `packages/core/src/application/lifecycle-services.ts`
- `packages/core/src/application/types.ts`
- `packages/core/src/application/undo-service.ts`
- `packages/core/src/contracts/v1/index.d.ts`
- `packages/core/src/contracts/v1/index.ts`
- `packages/core/src/contracts/v1/undo.ts`
- `packages/core/src/execution/coordinator.ts`
- `packages/core/src/execution/types.ts`
- `packages/core/src/index.ts`
- `packages/core/src/place/logical-transactions.ts`
- `packages/core/src/place/execute.ts`
- `packages/core/src/place/plan.ts`
- `packages/core/src/place/recovery.ts`
- `packages/core/src/place/swap.ts`
- `packages/core/src/planning/create.ts`
- `packages/core/src/planning/types.ts`
- `packages/core/src/public-types.ts`
- `packages/core/src/status/join.ts`
- `packages/core/src/status/read.ts`
- `packages/core/src/status/types.ts`
- `packages/core/src/undo/execute.ts`
- `packages/core/src/undo/index.ts`
- `packages/core/src/undo/observe.ts`
- `packages/core/src/undo/plan.ts`
- `packages/core/src/undo/types.ts`
- `packages/cli/src/contracts/cli-migration-ledger.ts`
- `packages/cli/src/contracts/cli-target-registry-v1.0.0.json`
- `packages/cli/src/contracts/commander-current-state-v0.json`
- `packages/cli/src/contracts/wire-contracts.ts`
- `packages/cli/src/output/undo-human.ts`
- `packages/cli/src/output/undo-json.ts`
- `packages/cli/src/runtime/current-renderers.ts`
- `packages/cli/src/runtime/interaction.ts`
- `packages/cli/src/spec/options.ts`
- `packages/cli/src/spec/registry.ts`

### Focused production tests (21)

- `packages/core/tests/application/current-services.test.ts`
- `packages/core/tests/application/lifecycle-services.test.ts`
- `packages/core/tests/application/undo-service.test.ts`
- `packages/core/tests/artifacts/codec.test.ts`
- `packages/core/tests/artifacts/ledger-writer.test.ts`
- `packages/core/tests/contracts/undo.test.ts`
- `packages/core/tests/execution/coordinator.test.ts`
- `packages/core/tests/place/execute.test.ts`
- `packages/core/tests/place/plan.test.ts`
- `packages/core/tests/place/recovery.test.ts`
- `packages/core/tests/place/swap.test.ts`
- `packages/core/tests/planning/create.test.ts`
- `packages/core/tests/planning/types.test.ts`
- `packages/core/tests/public-api.test.ts`
- `packages/core/tests/status/read.test.ts`
- `packages/core/tests/undo/execute.test.ts`
- `packages/core/tests/undo/observe.test.ts`
- `packages/core/tests/undo/plan.test.ts`
- `packages/cli/tests/output/flip-report-v4.test.ts`
- `packages/cli/tests/output/undo.test.ts`
- `packages/cli/tests/runtime/interaction.test.ts`

### Primary selector owners and fleet (5)

- `packages/cli/tests/contracts/undo.test.ts`
- `tests/ergonomics/fixtures/p5-undo/fleet.ts`
- `tests/ergonomics/phase/EWP-P5-TS03.test.ts`
- `tests/ergonomics/workflows/EWP-WF05.test.ts`
- `tests/ergonomics/workflows/EWP-WF11.test.ts`

### Signed compatibility owners (19)

- `packages/cli/tests/contracts/options.test.ts`
- `tests/ergonomics/fixtures/p1-ts08/read-only-capability.ts`
- `tests/ergonomics/fixtures/p1-ts09/write-adapter.ts`
- `tests/ergonomics/fixtures/p1-ts10/goldens/undo.stdout`
- `tests/ergonomics/fixtures/p1-ts10/public-wire-implementation.ts`
- `tests/ergonomics/fixtures/p1-ts10/public-wire-types.ts`
- `tests/ergonomics/fixtures/p1-ts10/reports.ts`
- `tests/ergonomics/fixtures/p2-ts08/public-types.ts`
- `tests/ergonomics/fixtures/p3b-ts06/write-adapter.ts`
- `tests/ergonomics/phase/EWP-P1-TS05.test.ts`
- `tests/ergonomics/phase/EWP-P1-TS07.test.ts`
- `tests/ergonomics/phase/EWP-P1-TS08.test.ts`
- `tests/ergonomics/phase/EWP-P1-TS09.test.ts`
- `tests/ergonomics/phase/EWP-P1-TS10.test.ts`
- `tests/ergonomics/phase/EWP-P1-TS11.test.ts`
- `tests/ergonomics/phase/EWP-P2-TS08.test.ts`
- `tests/ergonomics/phase/EWP-P3B-TS05.test.ts`
- `tests/ergonomics/phase/EWP-P3B-TS06.test.ts`
- `packages/cli/tests/help.test.ts`

### Governance owners (2)

- `scripts/p17-catalog.ts`
- `scripts/p17-catalog.test.ts`

Any executable red that proves a necessary path outside this manifest requires a narrow ownership
amendment and fresh independent review before edit.

## Implementation-discovered ownership amendment — fresh reversal compatibility shadow

The first implementation pass proved that fresh committed reversal cannot safely publish through
the canonical placement swap while the compatibility-shadow codec treats every rollback
disposition as the interrupted-forward `rollback` physical operation. A committed reversal must
keep the parent's forward intent orientation, while its physical journal describes the inverse
operation that restores that intent's before-image. Reusing the interrupted-forward rollback path
is not an alternative: that path restores an existing backup and cannot materialize retained
dev/store state for a completed promote, dev, install, or uninstall.

The amendment adds exactly six paths:

- `packages/core/src/artifacts/ledger-codec.ts` and
  `packages/core/tests/artifacts/codec.test.ts` own the origin-aware shadow predicate and hostile
  cross-reference coverage;
- `packages/core/src/artifacts/ledger-history.ts` and
  `packages/core/tests/artifacts/ledger-writer.test.ts` own dependency-closed bounded retention and
  cleanup/crash coverage;
- `packages/core/src/status/read.ts` and `packages/core/src/status/types.ts` own one private
  lifecycle-history observation mode that skips unrelated manifest/lock reads while reusing status
  ledger/live/retention/join authority. The already-owned status read test proves that mode.

No registry, artifact schema/DTO, history-capacity, placement-runner, ledger-writer production, or
recovery-record path is added. The exact manifest changes from 78 paths at initial Ready to 84
paths: 38 production, 20 focused tests, 5 selectors/fleet, 19 signed compatibility owners, and 2
governance owners. Catalog ownership is 86 paths including this plan and its evidence record.

The codec distinction is structural and closed:

- a converted pending abort has rollback disposition with `parentOperationId` equal to its own
  operation ID and keeps the signed physical `rollback` shadow;
- a fresh committed reversal has rollback disposition with a distinct non-null parent operation,
  whose retained committed forward journal must match the rollback intent orientation, approved
  current image, and retained resources exactly;
- its sole allowed physical shadow is derived from the preserved forward before/after images for a
  committed `install`, `remove`, `link-dev`, or `promote` parent: absent forward-before restores
  through `uninstall`, development forward-before through `dev`, non-development placement before
  with absent forward-after through `install`, and non-development placement before with placement
  forward-after through `promote`; managed store-linked state normalizes consistently with pinned,
  while update, repair, move-scope, unmanaged/unrestorable, and non-placement orientations refuse;
- missing, pending, rollback, duplicate, mismatched, or otherwise inconsistent parent linkage is
  invalid ledger state, never a reason to accept another shadow operation.

Bounded history preserves that invariant transitively. Committed-history operation IDs are unique,
pending operation IDs are unique within the pending map, and a distinct-parent fresh rollback
child's operation ID cannot collide with committed history; every such parent reference resolves
exactly one committed forward row. The signed compatibility case where a legacy pending-forward
snapshot reuses its committed operation ID remains readable but never supplies fresh-parent
authority. Every protected fresh-rollback child protects its exact committed-forward parent, and fair
selection admits a child only with its parent as one deterministic unit, raising effective capacity
when required. Visible cleanup-frontier rows are dependency-closed; a parent cannot be a victim
while a surviving child depends on it. Shared retained-backup cleanup authorizes only the complete
excluded dependency closure and removes that closure together, so ordinary writes and
crash/restart never publish a child without its parent. The public capacity constant and
one-victim API remain unchanged.

Undo status observation uses a request-only `lifecycle-history` unselected-artifact mode. It is
valid only for an empty status-target set with bounded-default selection, a unique nonempty subset
of user/project scopes, exact project placement provenance whenever project is included, and a
unique caller-bounded set of known tools. Explicit scope/tool filters retain their existing source
labels; command-default tools and command-default user/current-project scopes use the existing
unbounded label within this lifecycle-only universe.
The mode reads the ledger, selected live roots, and retention probes exactly once through current
status authority, reads no manifest/lock path or bytes, and projects the existing wire-compatible
unselected artifact summary. It does not widen public status CLI grammar or change `status@1`.

Canonical forward placement shadows must preserve later reversal authority instead of hardcoding
`reversibility: none` and an empty retained set. For committed install/remove/link-dev/promote with
a non-absent forward before-image, the logical intent carries the approved operation's conditional
pair resource ID and `actual.retained` carries exactly one matching source-content resource:

- development before-image: role `store` in the journal's existing source-content retention class,
  exact machine-bound development source/link target, exact recorded content/resource digest, and
  null `retainUntil`;
- pinned or managed store-linked before-image: role `store`, exact retained store path, exact
  content/resource digest, and null `retainUntil`.

The retained resource ID is the operation's pair ID. Missing path, digest, ownership/orientation,
or ID parity is never fabricated: the journal remains visibly retention-incomplete and status
refuses reversal. Status probes the same ID/path/digest and current live image before reporting
eligible. A fresh install or fresh link-dev whose approved before-image is absent is the exact
zero-retention exception: its forward journal carries `none`/empty retention, and committed status
is reversible only when the current live image exactly matches its recorded forward after-image;
its inverse is removal. No other `none` journal becomes reversible. Swap and status tests plus TS02
prove all four command families and hostile missing/mismatched retained state.

Fresh reversal creates its logical rollback in memory and atomically publishes that journal with
the inverse physical shadow at the first write-ahead placement boundary. It never persists a
logical pending placement journal without its required pair shadow. A crash after that boundary
resumes the inverse physical shadow through normal placement resume; a converted pending abort
continues through the existing backup-restoring rollback path. Focused codec and swap/recovery tests
must cover every allowed mapping, invalid linkage/orientation, the atomic first boundary, and resume
at every durable phase before the amendment is accepted as implemented.

Seven implementation-free selector/fixture assertions are corrected without changing their paths, IDs, or
behavioral scope. TS01 expects the exact shared inverse sequence `migrate-ledger`, then `remove` for
its legacy pending dev whose before-image is absent; no synthetic rollback/restore operation kind is
added. That preserved v1 forward-pending pair journal has no published rollback transaction to
resume, so TS01 reports `convert-to-rollback` and routes through the signed legacy backup-restoring
path without fabricating a logical transaction. For pending-abort candidates, a journal already
carrying rollback disposition reports `resume-rollback`. This distinction does not change a fresh
committed-forward reversal candidate's `resume-rollback` mode; its first atomic write publishes the
new distinct-parent rollback child and inverse physical shadow. TS08 keeps JSON aliases strict
flip@4 and stderr-clean, compares their operations/checks/
diagnostics to direct undo, then separately invokes the human alias and requires the structured
deprecation warning to name `skillsmith undo` and the frozen removal version. Because operation,
pair, check, and precondition identities intentionally include machine-bound project, source, store,
ledger, and live locations, the direct and alias dry-runs use the same seeded fleet and each proves
that its snapshot is unchanged before their exact equality is asserted; comparing separate
`mkdtemp` fleets would test different semantic identities rather than shared routing.

P5-TS03's unscoped explicit-target rows report the accepted F13 command-default scope universe
`['user', 'project']`; the single matched group remains project-scoped, and the later same-name
ambiguity/explicit-scope rows remain unchanged. The shared fleet's fake Claude and Codex executables
emit the minimum valid deterministic transcripts for WF05's already-required strict static+deep
verification: a Claude stream-JSON init loading `review:review`, and Codex marketplace add/plugin
add/plugin list plus successful exec. They perform no network or external tool work. After the
crash helper has sent SIGKILL and awaited child exit, it ages only that dead child's
`placements.json.lock` directory beyond the existing 30-second stale horizon before returning. This
lets TS09 exercise the signed stale-lock reclamation policy without changing runtime deadlines,
retry policy, live-holder exclusion, product code, or the selector's durable recovery assertions.

## Implementation order

1. Accept independent Ready review; apply only reviewed plan/catalog/evidence corrections.
2. Advance mapped/Ready mechanically and commit the five-file implementation-free red boundary.
3. Implement pure candidate observation/selection and deterministic undo plans.
4. Implement pending abort composition over the signed logical and physical recovery authorities.
5. Implement committed promote/dev/install/uninstall reversal with exact retained-state checks.
6. Add undo@1, application approval, renderers, CommandSpec, relations, and runtime registry.
7. Route rollback aliases through undo while preserving flip@4 compatibility.
8. Close focused units, 11 new selectors, two signed impacted selectors, smoke/static/recovery gates,
   refactor, and independent adversarial review.
9. Run catalog/package validation, one clean committed terminal repository gate, and all 13
   required-now selector commands individually before traceability sign-off.

## Required milestone gates

Ordinary iteration uses focused undo units, the changed selector, signed alias selectors,
TypeScript, boundary lint, catalog/package validation, and diff hygiene. The terminal milestone is
`bun run check -- --timeout 60000 --max-concurrency 1` on a clean immutable commit. The 13 exact
selectors are replayed only after that command passes.

## Explicit exclusions

- update reversal, lock restoration, UPDATE-TS07, UNDO-TS03, and WF09;
- GC/retention expiry or deletion;
- latest-operation guessing or older-history fallback;
- new recover/abort subcommands;
- public target-bound abort of standalone artifact-pair recovery records or a new recovery-record
  identity/schema; those remain same-operation coordinator recovery/blockers;
- new transaction, scheduler, repository, writer, journal, or artifact coordinator engines;
- changes to history capacity, cleanup selection, runtime deadlines, retry, skip, or todo policy;
- Kilo/OpenCode write enablement;
- public network fixtures or credential-bearing reports.

## Ready acceptance

Ready requires independent confirmation that the 13/3 validation partition is dependency-complete,
the 14 primary entities are exact, status/recovery/history authorities are reused, the committed
reversal design is crash-safe and history-preserving, compatibility aliases do not retain a second
engine, the amended 84-path manifest is sufficient/minimal, and the proposed selector red can exist without
implementation. No lifecycle gate advances before that review is accepted.

## Post-implementation adversarial correction — durable terminal cleanup and exact effects

Independent adversarial review of immutable implementation `c34be66b7e2008fbfa713e7024490cb4388925c6`
returned **NO-GO** at 0.99 confidence despite all 215 focused tests, all 13 required selectors,
TypeScript, boundary lint, package validation, ownership validation, and diff hygiene passing. It
identified two acceptance blockers. These corrections change no public command grammar, operation
family, target-selection rule, report schema version, catalog relation, ownership path, path count,
or lifecycle gate.

`P17-RV-P17-G5-03-F22` is blocking and high severity: fresh reversal commit currently moves the
rollback child to committed history and clears or deletes its physical pair shadow before backup
reclamation and directory fsync. A crash or filesystem failure in that window leaves committed
rollback history but no discoverable `backupPath` cleanup authority, so retry truthfully reports
already reversed while residue survives. The corrected fresh-reversal terminal protocol is exact:

1. after live publication and the existing skills-root fsync, atomically move the distinct-parent
   rollback child from pending to committed history exactly once while retaining its exact physical
   pair shadow advanced to `committed` in a cleanup-carrier pair. For `install`, `dev`, and `promote`
   the carrier contains the final membership plus the shadow; for `uninstall` it necessarily retains
   the pre-terminal pair plus shadow even though live is already absent, because pair deletion is
   itself the final step;
2. validate the child, its unique committed forward parent, pair identity, inverse operation,
   before-image, transaction/phase/time linkage, and retained resources, then idempotently reclaim
   or intentionally retain the deterministic backup and fsync the skills root even when the backup
   is already absent;
3. only after that cleanup decision and fsync, persist the terminal pair projection: physical
   `uninstall` deletes the pair, while physical `install`, `dev`, or `promote` keeps the pair and
   sets its journal to null. The committed rollback child remains once in history and is never
   toggled or appended again.

The committed child plus committed shadow is a cleanup-pending state, not a pending logical
transaction and not permission to rerun `forward`. One shared exact classifier/cleanup/finalizer is
used by both recovery entry paths: targeted `recoverPlacement` (including its observed/with-
observation forms) detects the committed history child before pending-attempt persistence, while the
compatibly named global `recoverCommittedAcquirePlacements` sweep (including its observed form) is
generalized across user/project scopes and all four inverse operations. Both resolve the exact
history child and unique parent, perform cleanup only, and then terminalize; neither advances a
recovery-attempt counter, mutates live/staging, calls `forward`, nor creates/commits another child.
Missing backup is an idempotent cleanup success after fsync; a hash-mismatched backup is retained
with a warning and then terminalized; cleanup, fsync, cancellation, or terminal-persist failure
leaves the shadow durable for retry. Converted pending aborts and ordinary forward commit behavior
remain unchanged.

Observation and dry-run remain read-only. Internally, an exact committed child/shadow is classified
`cleanup-pending` while its public pair outcome remains `already-reversed`; the public plan contains
exactly one warning diagnostic code `undo-cleanup-pending` correlated to that group/pair and no
operation or effect for the cleanup. The diagnostic is the wire-visible authority for exact preview
and approval validation without changing undo@1's schema version or group/pair shape. A terminal
already-reversed pair with no shadow has no such diagnostic and remains a true zero-mutation no-op.

Cleanup executes only after approval. A mixed actionable/cleanup-pending selection waits for the
same exact-plan approval; an all-cleanup-pending selection also requires approval even with zero
operations because it mutates the ledger or filesystem. Refused, cancelled, JSON-without-`--yes`,
noninteractive-without-`--yes`, and dry-run paths never clean or write. Once approved, selected
cleanup-pending recovery runs before any scheduler operation; a cleanup failure or cancellation
starts no operation. Successful cleanup preserves pair/group outcome `already-reversed`, produces
no operation, result, or undo effect, and an all-cleanup-only report is `completed` with zero changed
operations and approval `required: true, outcome: approved`. An intentionally retained backup is
emitted as one sanitized application warning while the report remains successful. Cleanup error or
cancellation emits the canonical top-level diagnostic and corresponding non-success exit with no
undo report or fabricated operation result/effect; processed targets may already be terminal, while
the failing and all unprocessed targets retain their shadows for deterministic retry. Only after
successful finalization do later invocations become true zero-mutation already-reversed no-ops.
The exact approval preview includes actionable operation IDs plus cleanup-pending group, pair, and
active transaction IDs in canonical group/pair order. Report and codec approval are required iff
the plan has an executable operation or an exact `undo-cleanup-pending` diagnostic; otherwise
approval is not required.

The admitted fresh inverse orientations remain closed and are now tested individually: install
absent-to-managed and link-dev absent-to-development reverse through `uninstall`; link-dev
managed-to-development reverses through `promote`; promote development-to-managed reverses through
`dev`; promote managed-to-managed reverses through `promote`; remove managed-to-absent reverses
through `install`; and remove development-to-absent reverses through `dev`. No update, repair,
move-scope, unmanaged, unrestorable, or other orientation is admitted. For managed-to-managed
reversal, the physical shadow before-image binds to the rollback child's actual before-image while
the terminal pair independently binds to its actual after-image; it must not compare the physical
before-image to the already-restored terminal pin.

The pure logical reducer may add only a narrow retain-shadow commit transition and an exact
committed-shadow finalizer. The former performs the existing commit validation, moves pending to
history, and retains the matching committed cleanup-carrier shadow. The latter validates the exact
history child, parent, shadow, physical before-image, and terminal-membership rule before clearing or
deleting only the pair projection; it never changes history. Every orientation binds the physical
shadow before-image to the rollback child's `actual.before`. Present terminal orientations
independently bind the retained pair to `actual.after`; `uninstall` instead requires `actual.after`
and observed live state to be absent before deleting its explicitly nonterminal carrier pair.
Focused codec, reducer, swap, recovery, and undo integration coverage must prove all seven
orientations, user/project discovery, exact matcher refusal, and failures immediately after the
committed-retaining write, during cleanup, after backup removal but before fsync, after fsync but
before terminal persistence, and at terminal persistence. Every retry must converge without a
second child, live rename, placement toggle, or unreported residue.

`P17-RV-P17-G5-03-F23` is acceptance-blocking: undo@1 currently validates effect references and
count but can accept an effect vector the canonical report projector cannot produce. The codec must
derive the exact ordered vector from the accepted operations, mode, approval, and results. For each
operation in public plan order it emits `ledger` then `live` according to the operation's mutation
flags, with exact operation/group IDs and action equal to the operation kind. Dry-run effects are
`planned`. Execute effects are `failed` for failed results, `cancelled` for cancelled results,
`not-run` for skipped-after-failure or absent results (including pending/refused/cancelled approval),
and `succeeded` for succeeded or rolled-back results. The supplied effect vector must equal that
derived vector in length, order, and every field. Hostile tests must reject omission, duplication,
reordering, wrong role, wrong action, wrong identity, and result-contradicting outcome; the canonical
fixture must carry both ledger and live effects for its declared operation.

F22 and F23 remain inside the accepted 84 product/test/governance paths and 86 catalog-owned paths.
No product or test edit for these corrections is authorized until fresh independent review accepts
this exact amendment. After correction, the resulting immutable commit requires another independent
adversarial review before the clean terminal repository gate may run.

Fresh independent re-review returned **GO** at 0.99 confidence for plan
`15f6311719ea09c4489043add3ef34368f79109f481b94d3585ed289c7aa0dbb` and evidence
`69676f5bd5d833199fc2e2c574dfac9e500b7254fc03d8ef2f3bb237a7ad364d`. The reviewer confirmed the
cleanup-pending/no-new-reversal distinction, exact diagnostic and approval boundary, dry/refusal
write prohibition, mixed and cleanup-only outcomes, uninstall carrier, generalized before/after/live
validation, both cleanup-only recovery entry paths, all seven orientations, F23's complete effect
vector, and unchanged 84/86 ownership. No new stable finding remained. This GO authorizes only F22/
F23 correction within the existing manifest and advances no lifecycle gate.

## Pre-schedule cleanup authority amendment

Implementation integration identified `P17-RV-P17-G5-03-F24`: cleanup-pending terminalization
changes the global ledger byte revision. Running cleanup before the canonical execution coordinator
would therefore make a mixed cleanup/actionable plan fail its own approved ledger precondition;
dropping that precondition, rebasing its identity, replanning after approval, or cleaning before
approval would violate the accepted snapshot and approval contracts. Running cleanup from a binding
observation is also too late or misleading because repository lifecycle staging must observe the
original durable revision that remains on disk until the first operation write.

The narrow correction adds exactly three shared-authority paths to ownership:

- `packages/core/src/execution/types.ts` declares one optional pre-schedule hook;
- `packages/core/src/execution/coordinator.ts` invokes it exactly once under the complete existing
  lock hierarchy, after generic preconditions and every actual-before binding validate, but before
  any scheduler dispatch;
- `packages/core/tests/execution/coordinator.test.ts` proves exact ordering, zero-operation
  invocation, cancellation/failure behavior, and no invocation after precondition/binding failure.

The hook is optional and changes no caller without an explicit callback. It receives no raw ports or
authority, allocates no operation/result/effect, and cannot alter the approved plan or bindings. If
it throws or cancels, the scheduler starts nothing and existing lock release/error behavior applies.
Observed and unobserved coordinator routes use the same seam. No new coordinator, scheduler, lock,
precondition, or revision engine is introduced.

Undo uses the seam only after approval. The placement adapter performs exact physical cleanup and
skills-root fsync for selected cleanup-pending carriers without yet persisting terminal membership,
then applies the already-reviewed pure finalizer to an in-memory ledger model. For a mixed plan, the
original durable ledger revision remains the lifecycle/CAS expected-before authority; the finalized
in-memory model becomes only the candidate base, and the first actionable operation's P1 write
atomically publishes all cleanup finalizations together with its own prepared state. The adapter
must not bind or stage against a fictional already-persisted terminal revision. If that first
operation fails before its P1 write, the durable carriers remain and retry is idempotent even though
physical backups may already be absent. For an all-cleanup-only plan, no operation can publish the
candidate, so the same hook persists the aggregate terminal model itself after physical cleanup and
fsync. Any cleanup failure occurs before scheduler dispatch and leaves its carrier retryable.

Cleanup-pending exact logical history is ledger v2 authority, so a plan containing both cleanup
markers and `migrate-ledger` is invalid and refuses before the hook; the hook never composes with a
migration operation. Deterministic target order, warning order, same-child/parent reuse, no live
toggle, and one terminalization remain governed by F22. Standalone targeted/global recovery may use
the same lower classifier/physical cleanup/finalizer and persist immediately because no approved
actionable plan follows.

F24 changes the exact manifest from 84 to 87 product/test/governance paths: 40 production, 21
focused tests, 5 selectors/fleet, 19 signed compatibility owners, and 2 governance owners. Catalog
ownership becomes 89 including plan/evidence. It changes no public CLI/report schema, validation ID,
catalog relation, operation/effect, or lifecycle gate. No edit to the three new paths is authorized
until fresh independent review accepts this exact amendment.

Fresh independent review accepted the F24 coordinator design at 0.99 confidence but initially
returned NO-GO because the three paths existed only in the plan manifest. After catalog-only
correction, final review returned **GO** at 0.99 confidence for catalog
`9e30b2a61ad1ca42e060cb5e283f5e87887ac9a40cc4e42bc096c619ec292cc1`, unchanged plan
`1ee8a45fc7c8c12c8582821a243e43899cf062a270872b5465c8cd6255acc086`, and unchanged evidence
`be8b47876d2172814c61eeb68b9bb132a3650058f42108a1da0e88f5c49b9965`. The reviewer verified 89
unique catalog-owned files, 87 excluding plan/evidence, exact manifest order, all three new paths
untouched, and no new stable finding. F24 is authorized and advances no lifecycle gate.

## Runtime managed-promote journal amendment

Lower-layer implementation then identified `P17-RV-P17-G5-03-F25`: the accepted fresh inverse
matrix includes a committed forward `promote` from managed placement A to managed placement B,
reversed physically by `promote` from B back to A. The ledger cross-reference matcher admits that
closed orientation, but the shared runtime journal intent codec currently rejects every `promote`
whose before classification is not development. The ledger therefore fails decoding before it can
validate the exact parent/child/shadow relationship. Removing this one orientation would contradict
the accepted seven-orientation matrix and leave a reachable committed promote non-reversible.

The correction adds exactly `packages/core/src/artifacts/plan-codec.ts` to ownership. Its existing
private `runtimeJournalOperationMatchesMatrix` may accept a `promote` intent with exact placement
identity, portable source, pinned/managed placement before, pinned/managed placement after, canonical
ledger+live mutation flags, and matching image scopes. The public/saved-plan `operationMatchesMatrix`
is unchanged and must continue to reject managed-to-managed promote; no saved-plan grammar or
operation kind is widened. Existing source-relationship validation remains mandatory, and wrong
source, absent/development after-image, identity, scope, mutation flags, or other operation family
still refuses.

The already-owned artifact codec test proves direct runtime intent acceptance, saved-plan rejection,
ledger round-trip for the exact committed parent plus fresh rollback child/shadow, and hostile
near-misses. F25 changes the current manifest from 87 to 88 product/test/governance paths: 41
production, 21 focused tests, 5 selectors/fleet, 19 signed compatibility owners, and 2 governance
owners. Catalog ownership becomes 90 including plan/evidence. It changes no public schema version,
catalog relation, selector, or lifecycle gate. The new codec path remains untouched until fresh
independent review accepts this exact amendment.

Fresh independent review returned **GO** at 0.99 confidence for catalog
`90eabc051f50a3c35ef6022b7f3e1bcdd4a5dda7624ae8db316d55c5e99025df`, plan
`4de0ceca13802bf1345af29dfb47aacf63c010c301697fbff02eca6b230f95e5`, and evidence
`4c2bc745db4f63f83db84e8481321f81d77fa44f62193637b36f28e0eef5788b`. The reviewer confirmed the
runtime-journal-only managed promote exception, unchanged saved-plan rejection, all retained
identity/source/scope/flag/relationship checks, exact positive/hostile coverage, the untouched codec
path, faithful F24 record, and exact 88/90 manifest. No stable finding remained. F25 is authorized
and advances no lifecycle gate.
